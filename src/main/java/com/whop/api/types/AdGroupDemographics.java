/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AdGroupDemographics.Builder.class)
public final class AdGroupDemographics {
    private final boolean automatic;

    private final AdGroupDemographicsGender gender;

    private final Optional<Double> maximumAge;

    private final Optional<Double> minimumAge;

    private final Map<String, Object> additionalProperties;

    private AdGroupDemographics(
            boolean automatic,
            AdGroupDemographicsGender gender,
            Optional<Double> maximumAge,
            Optional<Double> minimumAge,
            Map<String, Object> additionalProperties) {
        this.automatic = automatic;
        this.gender = gender;
        this.maximumAge = maximumAge;
        this.minimumAge = minimumAge;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether automatic audience targeting is on (Advantage+ on Meta). When <code>true</code>, the platform can deliver beyond the ages, genders, and detailed targeting you set, treating them as suggestions.
     */
    @JsonProperty("automatic")
    public boolean getAutomatic() {
        return automatic;
    }

    /**
     * @return Gender targeted.
     */
    @JsonProperty("gender")
    public AdGroupDemographicsGender getGender() {
        return gender;
    }

    /**
     * @return Oldest age targeted. <code>null</code> when no maximum is set.
     */
    @JsonIgnore
    public Optional<Double> getMaximumAge() {
        if (maximumAge == null) {
            return Optional.empty();
        }
        return maximumAge;
    }

    /**
     * @return Youngest age targeted. <code>null</code> when no minimum is set.
     */
    @JsonIgnore
    public Optional<Double> getMinimumAge() {
        if (minimumAge == null) {
            return Optional.empty();
        }
        return minimumAge;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("maximum_age")
    private Optional<Double> _getMaximumAge() {
        return maximumAge;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("minimum_age")
    private Optional<Double> _getMinimumAge() {
        return minimumAge;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AdGroupDemographics && equalTo((AdGroupDemographics) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AdGroupDemographics other) {
        return automatic == other.automatic
                && gender.equals(other.gender)
                && maximumAge.equals(other.maximumAge)
                && minimumAge.equals(other.minimumAge);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.automatic, this.gender, this.maximumAge, this.minimumAge);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AutomaticStage builder() {
        return new Builder();
    }

    public interface AutomaticStage {
        /**
         * <p>Whether automatic audience targeting is on (Advantage+ on Meta). When <code>true</code>, the platform can deliver beyond the ages, genders, and detailed targeting you set, treating them as suggestions.</p>
         */
        GenderStage automatic(boolean automatic);

        Builder from(AdGroupDemographics other);
    }

    public interface GenderStage {
        /**
         * <p>Gender targeted.</p>
         */
        _FinalStage gender(@NotNull AdGroupDemographicsGender gender);
    }

    public interface _FinalStage {
        AdGroupDemographics build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Oldest age targeted. <code>null</code> when no maximum is set.</p>
         */
        _FinalStage maximumAge(Optional<Double> maximumAge);

        _FinalStage maximumAge(Double maximumAge);

        _FinalStage maximumAge(Nullable<Double> maximumAge);

        /**
         * <p>Youngest age targeted. <code>null</code> when no minimum is set.</p>
         */
        _FinalStage minimumAge(Optional<Double> minimumAge);

        _FinalStage minimumAge(Double minimumAge);

        _FinalStage minimumAge(Nullable<Double> minimumAge);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AutomaticStage, GenderStage, _FinalStage {
        private boolean automatic;

        private AdGroupDemographicsGender gender;

        private Optional<Double> minimumAge = Optional.empty();

        private Optional<Double> maximumAge = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AdGroupDemographics other) {
            automatic(other.getAutomatic());
            gender(other.getGender());
            maximumAge(other.getMaximumAge());
            minimumAge(other.getMinimumAge());
            return this;
        }

        /**
         * <p>Whether automatic audience targeting is on (Advantage+ on Meta). When <code>true</code>, the platform can deliver beyond the ages, genders, and detailed targeting you set, treating them as suggestions.</p>
         * <p>Whether automatic audience targeting is on (Advantage+ on Meta). When <code>true</code>, the platform can deliver beyond the ages, genders, and detailed targeting you set, treating them as suggestions.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("automatic")
        public GenderStage automatic(boolean automatic) {
            this.automatic = automatic;
            return this;
        }

        /**
         * <p>Gender targeted.</p>
         * <p>Gender targeted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("gender")
        public _FinalStage gender(@NotNull AdGroupDemographicsGender gender) {
            this.gender = Objects.requireNonNull(gender, "gender must not be null");
            return this;
        }

        /**
         * <p>Youngest age targeted. <code>null</code> when no minimum is set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage minimumAge(Nullable<Double> minimumAge) {
            if (minimumAge.isNull()) {
                this.minimumAge = null;
            } else if (minimumAge.isEmpty()) {
                this.minimumAge = Optional.empty();
            } else {
                this.minimumAge = Optional.of(minimumAge.get());
            }
            return this;
        }

        /**
         * <p>Youngest age targeted. <code>null</code> when no minimum is set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage minimumAge(Double minimumAge) {
            this.minimumAge = Optional.ofNullable(minimumAge);
            return this;
        }

        /**
         * <p>Youngest age targeted. <code>null</code> when no minimum is set.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "minimum_age", nulls = Nulls.SKIP)
        public _FinalStage minimumAge(Optional<Double> minimumAge) {
            this.minimumAge = minimumAge;
            return this;
        }

        /**
         * <p>Oldest age targeted. <code>null</code> when no maximum is set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage maximumAge(Nullable<Double> maximumAge) {
            if (maximumAge.isNull()) {
                this.maximumAge = null;
            } else if (maximumAge.isEmpty()) {
                this.maximumAge = Optional.empty();
            } else {
                this.maximumAge = Optional.of(maximumAge.get());
            }
            return this;
        }

        /**
         * <p>Oldest age targeted. <code>null</code> when no maximum is set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage maximumAge(Double maximumAge) {
            this.maximumAge = Optional.ofNullable(maximumAge);
            return this;
        }

        /**
         * <p>Oldest age targeted. <code>null</code> when no maximum is set.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "maximum_age", nulls = Nulls.SKIP)
        public _FinalStage maximumAge(Optional<Double> maximumAge) {
            this.maximumAge = maximumAge;
            return this;
        }

        @java.lang.Override
        public AdGroupDemographics build() {
            return new AdGroupDemographics(automatic, gender, maximumAge, minimumAge, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
