/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Export.Builder.class)
public final class Export {
    private final String createdAt;

    private final Optional<String> downloadUrl;

    private final String expiresAt;

    private final String id;

    private final Optional<Integer> progressPercent;

    private final ExportResource resource;

    private final ExportStatus status;

    private final String updatedAt;

    private final Map<String, Object> additionalProperties;

    private Export(
            String createdAt,
            Optional<String> downloadUrl,
            String expiresAt,
            String id,
            Optional<Integer> progressPercent,
            ExportResource resource,
            ExportStatus status,
            String updatedAt,
            Map<String, Object> additionalProperties) {
        this.createdAt = createdAt;
        this.downloadUrl = downloadUrl;
        this.expiresAt = expiresAt;
        this.id = id;
        this.progressPercent = progressPercent;
        this.resource = resource;
        this.status = status;
        this.updatedAt = updatedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return When the export was requested, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return A short-lived link to download the finished CSV. <code>null</code> until <code>status</code> is <code>completed</code>, and again once the export has expired.
     */
    @JsonIgnore
    public Optional<String> getDownloadUrl() {
        if (downloadUrl == null) {
            return Optional.empty();
        }
        return downloadUrl;
    }

    /**
     * @return When the CSV is deleted and the export moves to <code>expired</code>, as an ISO 8601 timestamp. Exports are retained for 30 days.
     */
    @JsonProperty("expires_at")
    public String getExpiresAt() {
        return expiresAt;
    }

    /**
     * @return Export ID, prefixed <code>exprt_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Estimated completion percentage from 0 to 100.
     */
    @JsonIgnore
    public Optional<Integer> getProgressPercent() {
        if (progressPercent == null) {
            return Optional.empty();
        }
        return progressPercent;
    }

    /**
     * @return The resource that was exported, e.g. <code>receipts</code>, <code>members</code>, or <code>payouts</code>.
     */
    @JsonProperty("resource")
    public ExportResource getResource() {
        return resource;
    }

    /**
     * @return <code>pending</code> or <code>processing</code> while the CSV is generated, <code>completed</code> when the download is ready, <code>failed</code> if it errored, <code>expired</code> once the CSV has been deleted.
     */
    @JsonProperty("status")
    public ExportStatus getStatus() {
        return status;
    }

    /**
     * @return When the export last changed, as an ISO 8601 timestamp.
     */
    @JsonProperty("updated_at")
    public String getUpdatedAt() {
        return updatedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("download_url")
    private Optional<String> _getDownloadUrl() {
        return downloadUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("progress_percent")
    private Optional<Integer> _getProgressPercent() {
        return progressPercent;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Export && equalTo((Export) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Export other) {
        return createdAt.equals(other.createdAt)
                && downloadUrl.equals(other.downloadUrl)
                && expiresAt.equals(other.expiresAt)
                && id.equals(other.id)
                && progressPercent.equals(other.progressPercent)
                && resource.equals(other.resource)
                && status.equals(other.status)
                && updatedAt.equals(other.updatedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.createdAt,
                this.downloadUrl,
                this.expiresAt,
                this.id,
                this.progressPercent,
                this.resource,
                this.status,
                this.updatedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CreatedAtStage builder() {
        return new Builder();
    }

    public interface CreatedAtStage {
        /**
         * <p>When the export was requested, as an ISO 8601 timestamp.</p>
         */
        ExpiresAtStage createdAt(@NotNull String createdAt);

        Builder from(Export other);
    }

    public interface ExpiresAtStage {
        /**
         * <p>When the CSV is deleted and the export moves to <code>expired</code>, as an ISO 8601 timestamp. Exports are retained for 30 days.</p>
         */
        IdStage expiresAt(@NotNull String expiresAt);
    }

    public interface IdStage {
        /**
         * <p>Export ID, prefixed <code>exprt_</code>.</p>
         */
        ResourceStage id(@NotNull String id);
    }

    public interface ResourceStage {
        /**
         * <p>The resource that was exported, e.g. <code>receipts</code>, <code>members</code>, or <code>payouts</code>.</p>
         */
        StatusStage resource(@NotNull ExportResource resource);
    }

    public interface StatusStage {
        /**
         * <p><code>pending</code> or <code>processing</code> while the CSV is generated, <code>completed</code> when the download is ready, <code>failed</code> if it errored, <code>expired</code> once the CSV has been deleted.</p>
         */
        UpdatedAtStage status(@NotNull ExportStatus status);
    }

    public interface UpdatedAtStage {
        /**
         * <p>When the export last changed, as an ISO 8601 timestamp.</p>
         */
        _FinalStage updatedAt(@NotNull String updatedAt);
    }

    public interface _FinalStage {
        Export build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>A short-lived link to download the finished CSV. <code>null</code> until <code>status</code> is <code>completed</code>, and again once the export has expired.</p>
         */
        _FinalStage downloadUrl(Optional<String> downloadUrl);

        _FinalStage downloadUrl(String downloadUrl);

        _FinalStage downloadUrl(Nullable<String> downloadUrl);

        /**
         * <p>Estimated completion percentage from 0 to 100.</p>
         */
        _FinalStage progressPercent(Optional<Integer> progressPercent);

        _FinalStage progressPercent(Integer progressPercent);

        _FinalStage progressPercent(Nullable<Integer> progressPercent);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements CreatedAtStage,
                    ExpiresAtStage,
                    IdStage,
                    ResourceStage,
                    StatusStage,
                    UpdatedAtStage,
                    _FinalStage {
        private String createdAt;

        private String expiresAt;

        private String id;

        private ExportResource resource;

        private ExportStatus status;

        private String updatedAt;

        private Optional<Integer> progressPercent = Optional.empty();

        private Optional<String> downloadUrl = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Export other) {
            createdAt(other.getCreatedAt());
            downloadUrl(other.getDownloadUrl());
            expiresAt(other.getExpiresAt());
            id(other.getId());
            progressPercent(other.getProgressPercent());
            resource(other.getResource());
            status(other.getStatus());
            updatedAt(other.getUpdatedAt());
            return this;
        }

        /**
         * <p>When the export was requested, as an ISO 8601 timestamp.</p>
         * <p>When the export was requested, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public ExpiresAtStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>When the CSV is deleted and the export moves to <code>expired</code>, as an ISO 8601 timestamp. Exports are retained for 30 days.</p>
         * <p>When the CSV is deleted and the export moves to <code>expired</code>, as an ISO 8601 timestamp. Exports are retained for 30 days.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("expires_at")
        public IdStage expiresAt(@NotNull String expiresAt) {
            this.expiresAt = Objects.requireNonNull(expiresAt, "expiresAt must not be null");
            return this;
        }

        /**
         * <p>Export ID, prefixed <code>exprt_</code>.</p>
         * <p>Export ID, prefixed <code>exprt_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public ResourceStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The resource that was exported, e.g. <code>receipts</code>, <code>members</code>, or <code>payouts</code>.</p>
         * <p>The resource that was exported, e.g. <code>receipts</code>, <code>members</code>, or <code>payouts</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("resource")
        public StatusStage resource(@NotNull ExportResource resource) {
            this.resource = Objects.requireNonNull(resource, "resource must not be null");
            return this;
        }

        /**
         * <p><code>pending</code> or <code>processing</code> while the CSV is generated, <code>completed</code> when the download is ready, <code>failed</code> if it errored, <code>expired</code> once the CSV has been deleted.</p>
         * <p><code>pending</code> or <code>processing</code> while the CSV is generated, <code>completed</code> when the download is ready, <code>failed</code> if it errored, <code>expired</code> once the CSV has been deleted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public UpdatedAtStage status(@NotNull ExportStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>When the export last changed, as an ISO 8601 timestamp.</p>
         * <p>When the export last changed, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("updated_at")
        public _FinalStage updatedAt(@NotNull String updatedAt) {
            this.updatedAt = Objects.requireNonNull(updatedAt, "updatedAt must not be null");
            return this;
        }

        /**
         * <p>Estimated completion percentage from 0 to 100.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage progressPercent(Nullable<Integer> progressPercent) {
            if (progressPercent.isNull()) {
                this.progressPercent = null;
            } else if (progressPercent.isEmpty()) {
                this.progressPercent = Optional.empty();
            } else {
                this.progressPercent = Optional.of(progressPercent.get());
            }
            return this;
        }

        /**
         * <p>Estimated completion percentage from 0 to 100.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage progressPercent(Integer progressPercent) {
            this.progressPercent = Optional.ofNullable(progressPercent);
            return this;
        }

        /**
         * <p>Estimated completion percentage from 0 to 100.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "progress_percent", nulls = Nulls.SKIP)
        public _FinalStage progressPercent(Optional<Integer> progressPercent) {
            this.progressPercent = progressPercent;
            return this;
        }

        /**
         * <p>A short-lived link to download the finished CSV. <code>null</code> until <code>status</code> is <code>completed</code>, and again once the export has expired.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage downloadUrl(Nullable<String> downloadUrl) {
            if (downloadUrl.isNull()) {
                this.downloadUrl = null;
            } else if (downloadUrl.isEmpty()) {
                this.downloadUrl = Optional.empty();
            } else {
                this.downloadUrl = Optional.of(downloadUrl.get());
            }
            return this;
        }

        /**
         * <p>A short-lived link to download the finished CSV. <code>null</code> until <code>status</code> is <code>completed</code>, and again once the export has expired.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage downloadUrl(String downloadUrl) {
            this.downloadUrl = Optional.ofNullable(downloadUrl);
            return this;
        }

        /**
         * <p>A short-lived link to download the finished CSV. <code>null</code> until <code>status</code> is <code>completed</code>, and again once the export has expired.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "download_url", nulls = Nulls.SKIP)
        public _FinalStage downloadUrl(Optional<String> downloadUrl) {
            this.downloadUrl = downloadUrl;
            return this;
        }

        @java.lang.Override
        public Export build() {
            return new Export(
                    createdAt,
                    downloadUrl,
                    expiresAt,
                    id,
                    progressPercent,
                    resource,
                    status,
                    updatedAt,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
