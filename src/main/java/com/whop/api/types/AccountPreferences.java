/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AccountPreferences.Builder.class)
public final class AccountPreferences {
    private final Map<String, Object> adsAgreement;

    private final Optional<Map<String, Object>> adsPaymentMethods;

    private final String adsReportingCurrency;

    private final String adsSchedulingTimezone;

    private final Map<String, Object> adsTripleWhaleIntegration;

    private final boolean cardsAutoTopUp;

    private final boolean disputeFighterEnabled;

    private final Map<String, Object> additionalProperties;

    private AccountPreferences(
            Map<String, Object> adsAgreement,
            Optional<Map<String, Object>> adsPaymentMethods,
            String adsReportingCurrency,
            String adsSchedulingTimezone,
            Map<String, Object> adsTripleWhaleIntegration,
            boolean cardsAutoTopUp,
            boolean disputeFighterEnabled,
            Map<String, Object> additionalProperties) {
        this.adsAgreement = adsAgreement;
        this.adsPaymentMethods = adsPaymentMethods;
        this.adsReportingCurrency = adsReportingCurrency;
        this.adsSchedulingTimezone = adsSchedulingTimezone;
        this.adsTripleWhaleIntegration = adsTripleWhaleIntegration;
        this.cardsAutoTopUp = cardsAutoTopUp;
        this.disputeFighterEnabled = disputeFighterEnabled;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The account's Whop Ads services and payment authorization agreement. <code>status</code> is <code>not_required</code>, <code>pending_signature</code> (a signature has been requested and campaign launch is blocked until it is provided), or <code>signed</code>. While pending, read the fields to answer from <code>GET /verifications/{id}</code> and sign by submitting them via <code>PATCH /verifications/{id}</code>.
     */
    @JsonProperty("ads_agreement")
    public Map<String, Object> getAdsAgreement() {
        return adsAgreement;
    }

    /**
     * @return How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when it fails. Each entry has a <code>type</code> of <code>platform_balance</code> (id <code>ldgr_</code>) or <code>card</code> (id <code>payt_</code>), plus display fields so the configured source renders even for a viewer who doesn't own it. <code>backup</code> is <code>null</code> when only one method is configured. <code>null</code> until ads billing has been configured.
     */
    @JsonIgnore
    public Optional<Map<String, Object>> getAdsPaymentMethods() {
        if (adsPaymentMethods == null) {
            return Optional.empty();
        }
        return adsPaymentMethods;
    }

    /**
     * @return Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.
     */
    @JsonProperty("ads_reporting_currency")
    public String getAdsReportingCurrency() {
        return adsReportingCurrency;
    }

    /**
     * @return IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.
     */
    @JsonProperty("ads_scheduling_timezone")
    public String getAdsSchedulingTimezone() {
        return adsSchedulingTimezone;
    }

    /**
     * @return The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel. <code>status</code> is <code>connected</code>, <code>not_connected</code>, or <code>requires_shopify_store</code> (Triple Whale keys records by Shopify shop, so spend only flows while a store is connected). <code>masked_api_key</code> shows the leading characters of the stored key; the full key is never returned. <code>shop_domain</code> is the Shopify store spend is reported for.
     */
    @JsonProperty("ads_triple_whale_integration")
    public Map<String, Object> getAdsTripleWhaleIntegration() {
        return adsTripleWhaleIntegration;
    }

    /**
     * @return Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.
     */
    @JsonProperty("cards_auto_top_up")
    public boolean getCardsAutoTopUp() {
        return cardsAutoTopUp;
    }

    /**
     * @return Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.
     */
    @JsonProperty("dispute_fighter_enabled")
    public boolean getDisputeFighterEnabled() {
        return disputeFighterEnabled;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("ads_payment_methods")
    private Optional<Map<String, Object>> _getAdsPaymentMethods() {
        return adsPaymentMethods;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AccountPreferences && equalTo((AccountPreferences) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AccountPreferences other) {
        return adsAgreement.equals(other.adsAgreement)
                && adsPaymentMethods.equals(other.adsPaymentMethods)
                && adsReportingCurrency.equals(other.adsReportingCurrency)
                && adsSchedulingTimezone.equals(other.adsSchedulingTimezone)
                && adsTripleWhaleIntegration.equals(other.adsTripleWhaleIntegration)
                && cardsAutoTopUp == other.cardsAutoTopUp
                && disputeFighterEnabled == other.disputeFighterEnabled;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.adsAgreement,
                this.adsPaymentMethods,
                this.adsReportingCurrency,
                this.adsSchedulingTimezone,
                this.adsTripleWhaleIntegration,
                this.cardsAutoTopUp,
                this.disputeFighterEnabled);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AdsReportingCurrencyStage builder() {
        return new Builder();
    }

    public interface AdsReportingCurrencyStage {
        /**
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.</p>
         */
        AdsSchedulingTimezoneStage adsReportingCurrency(@NotNull String adsReportingCurrency);

        Builder from(AccountPreferences other);
    }

    public interface AdsSchedulingTimezoneStage {
        /**
         * <p>IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.</p>
         */
        CardsAutoTopUpStage adsSchedulingTimezone(@NotNull String adsSchedulingTimezone);
    }

    public interface CardsAutoTopUpStage {
        /**
         * <p>Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.</p>
         */
        DisputeFighterEnabledStage cardsAutoTopUp(boolean cardsAutoTopUp);
    }

    public interface DisputeFighterEnabledStage {
        /**
         * <p>Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.</p>
         */
        _FinalStage disputeFighterEnabled(boolean disputeFighterEnabled);
    }

    public interface _FinalStage {
        AccountPreferences build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The account's Whop Ads services and payment authorization agreement. <code>status</code> is <code>not_required</code>, <code>pending_signature</code> (a signature has been requested and campaign launch is blocked until it is provided), or <code>signed</code>. While pending, read the fields to answer from <code>GET /verifications/{id}</code> and sign by submitting them via <code>PATCH /verifications/{id}</code>.</p>
         */
        _FinalStage adsAgreement(Map<String, Object> adsAgreement);

        _FinalStage putAllAdsAgreement(Map<String, Object> adsAgreement);

        _FinalStage adsAgreement(String key, Object value);

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when it fails. Each entry has a <code>type</code> of <code>platform_balance</code> (id <code>ldgr_</code>) or <code>card</code> (id <code>payt_</code>), plus display fields so the configured source renders even for a viewer who doesn't own it. <code>backup</code> is <code>null</code> when only one method is configured. <code>null</code> until ads billing has been configured.</p>
         */
        _FinalStage adsPaymentMethods(Optional<Map<String, Object>> adsPaymentMethods);

        _FinalStage adsPaymentMethods(Map<String, Object> adsPaymentMethods);

        _FinalStage adsPaymentMethods(Nullable<Map<String, Object>> adsPaymentMethods);

        /**
         * <p>The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel. <code>status</code> is <code>connected</code>, <code>not_connected</code>, or <code>requires_shopify_store</code> (Triple Whale keys records by Shopify shop, so spend only flows while a store is connected). <code>masked_api_key</code> shows the leading characters of the stored key; the full key is never returned. <code>shop_domain</code> is the Shopify store spend is reported for.</p>
         */
        _FinalStage adsTripleWhaleIntegration(Map<String, Object> adsTripleWhaleIntegration);

        _FinalStage putAllAdsTripleWhaleIntegration(Map<String, Object> adsTripleWhaleIntegration);

        _FinalStage adsTripleWhaleIntegration(String key, Object value);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AdsReportingCurrencyStage,
                    AdsSchedulingTimezoneStage,
                    CardsAutoTopUpStage,
                    DisputeFighterEnabledStage,
                    _FinalStage {
        private String adsReportingCurrency;

        private String adsSchedulingTimezone;

        private boolean cardsAutoTopUp;

        private boolean disputeFighterEnabled;

        private Map<String, Object> adsTripleWhaleIntegration = new LinkedHashMap<>();

        private Optional<Map<String, Object>> adsPaymentMethods = Optional.empty();

        private Map<String, Object> adsAgreement = new LinkedHashMap<>();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AccountPreferences other) {
            adsAgreement(other.getAdsAgreement());
            adsPaymentMethods(other.getAdsPaymentMethods());
            adsReportingCurrency(other.getAdsReportingCurrency());
            adsSchedulingTimezone(other.getAdsSchedulingTimezone());
            adsTripleWhaleIntegration(other.getAdsTripleWhaleIntegration());
            cardsAutoTopUp(other.getCardsAutoTopUp());
            disputeFighterEnabled(other.getDisputeFighterEnabled());
            return this;
        }

        /**
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.</p>
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ads_reporting_currency")
        public AdsSchedulingTimezoneStage adsReportingCurrency(@NotNull String adsReportingCurrency) {
            this.adsReportingCurrency =
                    Objects.requireNonNull(adsReportingCurrency, "adsReportingCurrency must not be null");
            return this;
        }

        /**
         * <p>IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.</p>
         * <p>IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ads_scheduling_timezone")
        public CardsAutoTopUpStage adsSchedulingTimezone(@NotNull String adsSchedulingTimezone) {
            this.adsSchedulingTimezone =
                    Objects.requireNonNull(adsSchedulingTimezone, "adsSchedulingTimezone must not be null");
            return this;
        }

        /**
         * <p>Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.</p>
         * <p>Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("cards_auto_top_up")
        public DisputeFighterEnabledStage cardsAutoTopUp(boolean cardsAutoTopUp) {
            this.cardsAutoTopUp = cardsAutoTopUp;
            return this;
        }

        /**
         * <p>Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.</p>
         * <p>Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("dispute_fighter_enabled")
        public _FinalStage disputeFighterEnabled(boolean disputeFighterEnabled) {
            this.disputeFighterEnabled = disputeFighterEnabled;
            return this;
        }

        /**
         * <p>The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel. <code>status</code> is <code>connected</code>, <code>not_connected</code>, or <code>requires_shopify_store</code> (Triple Whale keys records by Shopify shop, so spend only flows while a store is connected). <code>masked_api_key</code> shows the leading characters of the stored key; the full key is never returned. <code>shop_domain</code> is the Shopify store spend is reported for.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage adsTripleWhaleIntegration(String key, Object value) {
            this.adsTripleWhaleIntegration.put(key, value);
            return this;
        }

        /**
         * <p>The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel. <code>status</code> is <code>connected</code>, <code>not_connected</code>, or <code>requires_shopify_store</code> (Triple Whale keys records by Shopify shop, so spend only flows while a store is connected). <code>masked_api_key</code> shows the leading characters of the stored key; the full key is never returned. <code>shop_domain</code> is the Shopify store spend is reported for.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage putAllAdsTripleWhaleIntegration(Map<String, Object> adsTripleWhaleIntegration) {
            if (adsTripleWhaleIntegration != null) {
                this.adsTripleWhaleIntegration.putAll(adsTripleWhaleIntegration);
            }
            return this;
        }

        /**
         * <p>The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel. <code>status</code> is <code>connected</code>, <code>not_connected</code>, or <code>requires_shopify_store</code> (Triple Whale keys records by Shopify shop, so spend only flows while a store is connected). <code>masked_api_key</code> shows the leading characters of the stored key; the full key is never returned. <code>shop_domain</code> is the Shopify store spend is reported for.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "ads_triple_whale_integration", nulls = Nulls.SKIP)
        public _FinalStage adsTripleWhaleIntegration(Map<String, Object> adsTripleWhaleIntegration) {
            this.adsTripleWhaleIntegration.clear();
            if (adsTripleWhaleIntegration != null) {
                this.adsTripleWhaleIntegration.putAll(adsTripleWhaleIntegration);
            }
            return this;
        }

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when it fails. Each entry has a <code>type</code> of <code>platform_balance</code> (id <code>ldgr_</code>) or <code>card</code> (id <code>payt_</code>), plus display fields so the configured source renders even for a viewer who doesn't own it. <code>backup</code> is <code>null</code> when only one method is configured. <code>null</code> until ads billing has been configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage adsPaymentMethods(Nullable<Map<String, Object>> adsPaymentMethods) {
            if (adsPaymentMethods.isNull()) {
                this.adsPaymentMethods = null;
            } else if (adsPaymentMethods.isEmpty()) {
                this.adsPaymentMethods = Optional.empty();
            } else {
                this.adsPaymentMethods = Optional.of(adsPaymentMethods.get());
            }
            return this;
        }

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when it fails. Each entry has a <code>type</code> of <code>platform_balance</code> (id <code>ldgr_</code>) or <code>card</code> (id <code>payt_</code>), plus display fields so the configured source renders even for a viewer who doesn't own it. <code>backup</code> is <code>null</code> when only one method is configured. <code>null</code> until ads billing has been configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage adsPaymentMethods(Map<String, Object> adsPaymentMethods) {
            this.adsPaymentMethods = Optional.ofNullable(adsPaymentMethods);
            return this;
        }

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when it fails. Each entry has a <code>type</code> of <code>platform_balance</code> (id <code>ldgr_</code>) or <code>card</code> (id <code>payt_</code>), plus display fields so the configured source renders even for a viewer who doesn't own it. <code>backup</code> is <code>null</code> when only one method is configured. <code>null</code> until ads billing has been configured.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "ads_payment_methods", nulls = Nulls.SKIP)
        public _FinalStage adsPaymentMethods(Optional<Map<String, Object>> adsPaymentMethods) {
            this.adsPaymentMethods = adsPaymentMethods;
            return this;
        }

        /**
         * <p>The account's Whop Ads services and payment authorization agreement. <code>status</code> is <code>not_required</code>, <code>pending_signature</code> (a signature has been requested and campaign launch is blocked until it is provided), or <code>signed</code>. While pending, read the fields to answer from <code>GET /verifications/{id}</code> and sign by submitting them via <code>PATCH /verifications/{id}</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage adsAgreement(String key, Object value) {
            this.adsAgreement.put(key, value);
            return this;
        }

        /**
         * <p>The account's Whop Ads services and payment authorization agreement. <code>status</code> is <code>not_required</code>, <code>pending_signature</code> (a signature has been requested and campaign launch is blocked until it is provided), or <code>signed</code>. While pending, read the fields to answer from <code>GET /verifications/{id}</code> and sign by submitting them via <code>PATCH /verifications/{id}</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage putAllAdsAgreement(Map<String, Object> adsAgreement) {
            if (adsAgreement != null) {
                this.adsAgreement.putAll(adsAgreement);
            }
            return this;
        }

        /**
         * <p>The account's Whop Ads services and payment authorization agreement. <code>status</code> is <code>not_required</code>, <code>pending_signature</code> (a signature has been requested and campaign launch is blocked until it is provided), or <code>signed</code>. While pending, read the fields to answer from <code>GET /verifications/{id}</code> and sign by submitting them via <code>PATCH /verifications/{id}</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "ads_agreement", nulls = Nulls.SKIP)
        public _FinalStage adsAgreement(Map<String, Object> adsAgreement) {
            this.adsAgreement.clear();
            if (adsAgreement != null) {
                this.adsAgreement.putAll(adsAgreement);
            }
            return this;
        }

        @java.lang.Override
        public AccountPreferences build() {
            return new AccountPreferences(
                    adsAgreement,
                    adsPaymentMethods,
                    adsReportingCurrency,
                    adsSchedulingTimezone,
                    adsTripleWhaleIntegration,
                    cardsAutoTopUp,
                    disputeFighterEnabled,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
