/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Notification.Builder.class)
public final class Notification {
    private final Optional<NotificationAccount> account;

    private final String attachmentUrl;

    private final String content;

    private final String createdAt;

    private final Optional<NotificationExperience> experience;

    private final String id;

    private final Optional<String> iframeLink;

    private final Optional<String> link;

    private final boolean mentionsMe;

    private final Optional<String> restPath;

    private final Optional<NotificationSender> sender;

    private final String subject;

    private final Optional<NotificationTopicSummary> topic;

    private final Map<String, Object> additionalProperties;

    private Notification(
            Optional<NotificationAccount> account,
            String attachmentUrl,
            String content,
            String createdAt,
            Optional<NotificationExperience> experience,
            String id,
            Optional<String> iframeLink,
            Optional<String> link,
            boolean mentionsMe,
            Optional<String> restPath,
            Optional<NotificationSender> sender,
            String subject,
            Optional<NotificationTopicSummary> topic,
            Map<String, Object> additionalProperties) {
        this.account = account;
        this.attachmentUrl = attachmentUrl;
        this.content = content;
        this.createdAt = createdAt;
        this.experience = experience;
        this.id = id;
        this.iframeLink = iframeLink;
        this.link = link;
        this.mentionsMe = mentionsMe;
        this.restPath = restPath;
        this.sender = sender;
        this.subject = subject;
        this.topic = topic;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Account the notification belongs to. <code>null</code> when the notification is not associated with an account.
     */
    @JsonIgnore
    public Optional<NotificationAccount> getAccount() {
        if (account == null) {
            return Optional.empty();
        }
        return account;
    }

    /**
     * @return Image displayed alongside the notification — the sender's avatar, or the default notification image.
     */
    @JsonProperty("attachment_url")
    public String getAttachmentUrl() {
        return attachmentUrl;
    }

    /**
     * @return The body text of the notification displayed to the user.
     */
    @JsonProperty("content")
    public String getContent() {
        return content;
    }

    /**
     * @return When the notification was created, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Experience the notification is related to. <code>null</code> when not tied to a specific experience.
     */
    @JsonIgnore
    public Optional<NotificationExperience> getExperience() {
        if (experience == null) {
            return Optional.empty();
        }
        return experience;
    }

    /**
     * @return Notification ID. Feed rows carry a composite id that doubles as the list cursor.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The same destination on the app's own domain, which the Whop web and mobile clients embed instead of navigating to it. Only relevant if you render Whop apps yourself. <code>null</code> when the notification carries its own <code>link</code>.
     */
    @JsonIgnore
    public Optional<String> getIframeLink() {
        if (iframeLink == null) {
            return Optional.empty();
        }
        return iframeLink;
    }

    /**
     * @return The whop.com page the notification opens, as a normal top-level navigation. This is the link to use unless you host Whop apps yourself. <code>null</code> when the notification has no click-through destination.
     */
    @JsonIgnore
    public Optional<String> getLink() {
        if (link == null) {
            return Optional.empty();
        }
        return link;
    }

    /**
     * @return Whether the authenticated user was directly mentioned in this notification.
     */
    @JsonProperty("mentions_me")
    public boolean getMentionsMe() {
        return mentionsMe;
    }

    /**
     * @return Additional path information appended to the notification's deep link, if any.
     */
    @JsonIgnore
    public Optional<String> getRestPath() {
        if (restPath == null) {
            return Optional.empty();
        }
        return restPath;
    }

    /**
     * @return User who triggered the notification. <code>null</code> when it was system-generated.
     */
    @JsonIgnore
    public Optional<NotificationSender> getSender() {
        if (sender == null) {
            return Optional.empty();
        }
        return sender;
    }

    /**
     * @return The title line of the notification displayed to the user.
     */
    @JsonProperty("subject")
    public String getSubject() {
        return subject;
    }

    /**
     * @return Topic category the notification belongs to, used for grouping and preference management. <code>null</code> when uncategorized.
     */
    @JsonIgnore
    public Optional<NotificationTopicSummary> getTopic() {
        if (topic == null) {
            return Optional.empty();
        }
        return topic;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("account")
    private Optional<NotificationAccount> _getAccount() {
        return account;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("experience")
    private Optional<NotificationExperience> _getExperience() {
        return experience;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("iframe_link")
    private Optional<String> _getIframeLink() {
        return iframeLink;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("link")
    private Optional<String> _getLink() {
        return link;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("rest_path")
    private Optional<String> _getRestPath() {
        return restPath;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("sender")
    private Optional<NotificationSender> _getSender() {
        return sender;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("topic")
    private Optional<NotificationTopicSummary> _getTopic() {
        return topic;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Notification && equalTo((Notification) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Notification other) {
        return account.equals(other.account)
                && attachmentUrl.equals(other.attachmentUrl)
                && content.equals(other.content)
                && createdAt.equals(other.createdAt)
                && experience.equals(other.experience)
                && id.equals(other.id)
                && iframeLink.equals(other.iframeLink)
                && link.equals(other.link)
                && mentionsMe == other.mentionsMe
                && restPath.equals(other.restPath)
                && sender.equals(other.sender)
                && subject.equals(other.subject)
                && topic.equals(other.topic);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.account,
                this.attachmentUrl,
                this.content,
                this.createdAt,
                this.experience,
                this.id,
                this.iframeLink,
                this.link,
                this.mentionsMe,
                this.restPath,
                this.sender,
                this.subject,
                this.topic);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AttachmentUrlStage builder() {
        return new Builder();
    }

    public interface AttachmentUrlStage {
        /**
         * <p>Image displayed alongside the notification — the sender's avatar, or the default notification image.</p>
         */
        ContentStage attachmentUrl(@NotNull String attachmentUrl);

        Builder from(Notification other);
    }

    public interface ContentStage {
        /**
         * <p>The body text of the notification displayed to the user.</p>
         */
        CreatedAtStage content(@NotNull String content);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the notification was created, as an ISO 8601 timestamp.</p>
         */
        IdStage createdAt(@NotNull String createdAt);
    }

    public interface IdStage {
        /**
         * <p>Notification ID. Feed rows carry a composite id that doubles as the list cursor.</p>
         */
        MentionsMeStage id(@NotNull String id);
    }

    public interface MentionsMeStage {
        /**
         * <p>Whether the authenticated user was directly mentioned in this notification.</p>
         */
        SubjectStage mentionsMe(boolean mentionsMe);
    }

    public interface SubjectStage {
        /**
         * <p>The title line of the notification displayed to the user.</p>
         */
        _FinalStage subject(@NotNull String subject);
    }

    public interface _FinalStage {
        Notification build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Account the notification belongs to. <code>null</code> when the notification is not associated with an account.</p>
         */
        _FinalStage account(Optional<NotificationAccount> account);

        _FinalStage account(NotificationAccount account);

        _FinalStage account(Nullable<NotificationAccount> account);

        /**
         * <p>Experience the notification is related to. <code>null</code> when not tied to a specific experience.</p>
         */
        _FinalStage experience(Optional<NotificationExperience> experience);

        _FinalStage experience(NotificationExperience experience);

        _FinalStage experience(Nullable<NotificationExperience> experience);

        /**
         * <p>The same destination on the app's own domain, which the Whop web and mobile clients embed instead of navigating to it. Only relevant if you render Whop apps yourself. <code>null</code> when the notification carries its own <code>link</code>.</p>
         */
        _FinalStage iframeLink(Optional<String> iframeLink);

        _FinalStage iframeLink(String iframeLink);

        _FinalStage iframeLink(Nullable<String> iframeLink);

        /**
         * <p>The whop.com page the notification opens, as a normal top-level navigation. This is the link to use unless you host Whop apps yourself. <code>null</code> when the notification has no click-through destination.</p>
         */
        _FinalStage link(Optional<String> link);

        _FinalStage link(String link);

        _FinalStage link(Nullable<String> link);

        /**
         * <p>Additional path information appended to the notification's deep link, if any.</p>
         */
        _FinalStage restPath(Optional<String> restPath);

        _FinalStage restPath(String restPath);

        _FinalStage restPath(Nullable<String> restPath);

        /**
         * <p>User who triggered the notification. <code>null</code> when it was system-generated.</p>
         */
        _FinalStage sender(Optional<NotificationSender> sender);

        _FinalStage sender(NotificationSender sender);

        _FinalStage sender(Nullable<NotificationSender> sender);

        /**
         * <p>Topic category the notification belongs to, used for grouping and preference management. <code>null</code> when uncategorized.</p>
         */
        _FinalStage topic(Optional<NotificationTopicSummary> topic);

        _FinalStage topic(NotificationTopicSummary topic);

        _FinalStage topic(Nullable<NotificationTopicSummary> topic);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AttachmentUrlStage,
                    ContentStage,
                    CreatedAtStage,
                    IdStage,
                    MentionsMeStage,
                    SubjectStage,
                    _FinalStage {
        private String attachmentUrl;

        private String content;

        private String createdAt;

        private String id;

        private boolean mentionsMe;

        private String subject;

        private Optional<NotificationTopicSummary> topic = Optional.empty();

        private Optional<NotificationSender> sender = Optional.empty();

        private Optional<String> restPath = Optional.empty();

        private Optional<String> link = Optional.empty();

        private Optional<String> iframeLink = Optional.empty();

        private Optional<NotificationExperience> experience = Optional.empty();

        private Optional<NotificationAccount> account = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Notification other) {
            account(other.getAccount());
            attachmentUrl(other.getAttachmentUrl());
            content(other.getContent());
            createdAt(other.getCreatedAt());
            experience(other.getExperience());
            id(other.getId());
            iframeLink(other.getIframeLink());
            link(other.getLink());
            mentionsMe(other.getMentionsMe());
            restPath(other.getRestPath());
            sender(other.getSender());
            subject(other.getSubject());
            topic(other.getTopic());
            return this;
        }

        /**
         * <p>Image displayed alongside the notification — the sender's avatar, or the default notification image.</p>
         * <p>Image displayed alongside the notification — the sender's avatar, or the default notification image.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("attachment_url")
        public ContentStage attachmentUrl(@NotNull String attachmentUrl) {
            this.attachmentUrl = Objects.requireNonNull(attachmentUrl, "attachmentUrl must not be null");
            return this;
        }

        /**
         * <p>The body text of the notification displayed to the user.</p>
         * <p>The body text of the notification displayed to the user.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("content")
        public CreatedAtStage content(@NotNull String content) {
            this.content = Objects.requireNonNull(content, "content must not be null");
            return this;
        }

        /**
         * <p>When the notification was created, as an ISO 8601 timestamp.</p>
         * <p>When the notification was created, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public IdStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Notification ID. Feed rows carry a composite id that doubles as the list cursor.</p>
         * <p>Notification ID. Feed rows carry a composite id that doubles as the list cursor.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public MentionsMeStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Whether the authenticated user was directly mentioned in this notification.</p>
         * <p>Whether the authenticated user was directly mentioned in this notification.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("mentions_me")
        public SubjectStage mentionsMe(boolean mentionsMe) {
            this.mentionsMe = mentionsMe;
            return this;
        }

        /**
         * <p>The title line of the notification displayed to the user.</p>
         * <p>The title line of the notification displayed to the user.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("subject")
        public _FinalStage subject(@NotNull String subject) {
            this.subject = Objects.requireNonNull(subject, "subject must not be null");
            return this;
        }

        /**
         * <p>Topic category the notification belongs to, used for grouping and preference management. <code>null</code> when uncategorized.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage topic(Nullable<NotificationTopicSummary> topic) {
            if (topic.isNull()) {
                this.topic = null;
            } else if (topic.isEmpty()) {
                this.topic = Optional.empty();
            } else {
                this.topic = Optional.of(topic.get());
            }
            return this;
        }

        /**
         * <p>Topic category the notification belongs to, used for grouping and preference management. <code>null</code> when uncategorized.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage topic(NotificationTopicSummary topic) {
            this.topic = Optional.ofNullable(topic);
            return this;
        }

        /**
         * <p>Topic category the notification belongs to, used for grouping and preference management. <code>null</code> when uncategorized.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "topic", nulls = Nulls.SKIP)
        public _FinalStage topic(Optional<NotificationTopicSummary> topic) {
            this.topic = topic;
            return this;
        }

        /**
         * <p>User who triggered the notification. <code>null</code> when it was system-generated.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sender(Nullable<NotificationSender> sender) {
            if (sender.isNull()) {
                this.sender = null;
            } else if (sender.isEmpty()) {
                this.sender = Optional.empty();
            } else {
                this.sender = Optional.of(sender.get());
            }
            return this;
        }

        /**
         * <p>User who triggered the notification. <code>null</code> when it was system-generated.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sender(NotificationSender sender) {
            this.sender = Optional.ofNullable(sender);
            return this;
        }

        /**
         * <p>User who triggered the notification. <code>null</code> when it was system-generated.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "sender", nulls = Nulls.SKIP)
        public _FinalStage sender(Optional<NotificationSender> sender) {
            this.sender = sender;
            return this;
        }

        /**
         * <p>Additional path information appended to the notification's deep link, if any.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage restPath(Nullable<String> restPath) {
            if (restPath.isNull()) {
                this.restPath = null;
            } else if (restPath.isEmpty()) {
                this.restPath = Optional.empty();
            } else {
                this.restPath = Optional.of(restPath.get());
            }
            return this;
        }

        /**
         * <p>Additional path information appended to the notification's deep link, if any.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage restPath(String restPath) {
            this.restPath = Optional.ofNullable(restPath);
            return this;
        }

        /**
         * <p>Additional path information appended to the notification's deep link, if any.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "rest_path", nulls = Nulls.SKIP)
        public _FinalStage restPath(Optional<String> restPath) {
            this.restPath = restPath;
            return this;
        }

        /**
         * <p>The whop.com page the notification opens, as a normal top-level navigation. This is the link to use unless you host Whop apps yourself. <code>null</code> when the notification has no click-through destination.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage link(Nullable<String> link) {
            if (link.isNull()) {
                this.link = null;
            } else if (link.isEmpty()) {
                this.link = Optional.empty();
            } else {
                this.link = Optional.of(link.get());
            }
            return this;
        }

        /**
         * <p>The whop.com page the notification opens, as a normal top-level navigation. This is the link to use unless you host Whop apps yourself. <code>null</code> when the notification has no click-through destination.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage link(String link) {
            this.link = Optional.ofNullable(link);
            return this;
        }

        /**
         * <p>The whop.com page the notification opens, as a normal top-level navigation. This is the link to use unless you host Whop apps yourself. <code>null</code> when the notification has no click-through destination.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "link", nulls = Nulls.SKIP)
        public _FinalStage link(Optional<String> link) {
            this.link = link;
            return this;
        }

        /**
         * <p>The same destination on the app's own domain, which the Whop web and mobile clients embed instead of navigating to it. Only relevant if you render Whop apps yourself. <code>null</code> when the notification carries its own <code>link</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage iframeLink(Nullable<String> iframeLink) {
            if (iframeLink.isNull()) {
                this.iframeLink = null;
            } else if (iframeLink.isEmpty()) {
                this.iframeLink = Optional.empty();
            } else {
                this.iframeLink = Optional.of(iframeLink.get());
            }
            return this;
        }

        /**
         * <p>The same destination on the app's own domain, which the Whop web and mobile clients embed instead of navigating to it. Only relevant if you render Whop apps yourself. <code>null</code> when the notification carries its own <code>link</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage iframeLink(String iframeLink) {
            this.iframeLink = Optional.ofNullable(iframeLink);
            return this;
        }

        /**
         * <p>The same destination on the app's own domain, which the Whop web and mobile clients embed instead of navigating to it. Only relevant if you render Whop apps yourself. <code>null</code> when the notification carries its own <code>link</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "iframe_link", nulls = Nulls.SKIP)
        public _FinalStage iframeLink(Optional<String> iframeLink) {
            this.iframeLink = iframeLink;
            return this;
        }

        /**
         * <p>Experience the notification is related to. <code>null</code> when not tied to a specific experience.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage experience(Nullable<NotificationExperience> experience) {
            if (experience.isNull()) {
                this.experience = null;
            } else if (experience.isEmpty()) {
                this.experience = Optional.empty();
            } else {
                this.experience = Optional.of(experience.get());
            }
            return this;
        }

        /**
         * <p>Experience the notification is related to. <code>null</code> when not tied to a specific experience.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage experience(NotificationExperience experience) {
            this.experience = Optional.ofNullable(experience);
            return this;
        }

        /**
         * <p>Experience the notification is related to. <code>null</code> when not tied to a specific experience.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "experience", nulls = Nulls.SKIP)
        public _FinalStage experience(Optional<NotificationExperience> experience) {
            this.experience = experience;
            return this;
        }

        /**
         * <p>Account the notification belongs to. <code>null</code> when the notification is not associated with an account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage account(Nullable<NotificationAccount> account) {
            if (account.isNull()) {
                this.account = null;
            } else if (account.isEmpty()) {
                this.account = Optional.empty();
            } else {
                this.account = Optional.of(account.get());
            }
            return this;
        }

        /**
         * <p>Account the notification belongs to. <code>null</code> when the notification is not associated with an account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage account(NotificationAccount account) {
            this.account = Optional.ofNullable(account);
            return this;
        }

        /**
         * <p>Account the notification belongs to. <code>null</code> when the notification is not associated with an account.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "account", nulls = Nulls.SKIP)
        public _FinalStage account(Optional<NotificationAccount> account) {
            this.account = account;
            return this;
        }

        @java.lang.Override
        public Notification build() {
            return new Notification(
                    account,
                    attachmentUrl,
                    content,
                    createdAt,
                    experience,
                    id,
                    iframeLink,
                    link,
                    mentionsMe,
                    restPath,
                    sender,
                    subject,
                    topic,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
