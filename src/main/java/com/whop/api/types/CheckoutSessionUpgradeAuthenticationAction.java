/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionUpgradeAuthenticationAction.Builder.class)
public final class CheckoutSessionUpgradeAuthenticationAction {
    private final boolean blocking;

    private final String email;

    private final Optional<String> sessionIntentId;

    private final CheckoutSessionUpgradeAuthenticationActionSignInIntent signInIntent;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionUpgradeAuthenticationAction(
            boolean blocking,
            String email,
            Optional<String> sessionIntentId,
            CheckoutSessionUpgradeAuthenticationActionSignInIntent signInIntent,
            Map<String, Object> additionalProperties) {
        this.blocking = blocking;
        this.email = email;
        this.sessionIntentId = sessionIntentId;
        this.signInIntent = signInIntent;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.
     */
    @JsonProperty("blocking")
    public boolean getBlocking() {
        return blocking;
    }

    /**
     * @return The address the sign-in starts with — the buyer's own, resolved by this checkout. Pass it to the session-intent create; the one-time code goes to this inbox, and holding that inbox is the entire proof.
     */
    @JsonProperty("email")
    public String getEmail() {
        return email;
    }

    /**
     * @return The continuation, when this reader can take it: the id (<code>ses_…</code>) of the buyer's own mid-checkout sign-in, named only when the read presented the credential that sign-in's verify installed and the sign-in can still be finished. Skip the create and the verify — the emailed factor is already proven — and call THIS intent's <code>upgrade</code> endpoint naming the checkout session: on a first-party page the same credential authorizes it with no bearer at all, and the login collects only what it still owes (nothing, or the account's second factor). <code>null</code> runs the full recipe; a refused continuation is answered the same way, by falling back to it.
     */
    @JsonIgnore
    public Optional<String> getSessionIntentId() {
        if (sessionIntentId == null) {
            return Optional.empty();
        }
        return sessionIntentId;
    }

    /**
     * @return The <code>intent</code> to declare on the session-intent create. Declaring it is what keeps the verification credential checkout-scoped — the full login is minted only by the upgrade call, never by the code alone.
     */
    @JsonProperty("sign_in_intent")
    public CheckoutSessionUpgradeAuthenticationActionSignInIntent getSignInIntent() {
        return signInIntent;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("session_intent_id")
    private Optional<String> _getSessionIntentId() {
        return sessionIntentId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionUpgradeAuthenticationAction
                && equalTo((CheckoutSessionUpgradeAuthenticationAction) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionUpgradeAuthenticationAction other) {
        return blocking == other.blocking
                && email.equals(other.email)
                && sessionIntentId.equals(other.sessionIntentId)
                && signInIntent.equals(other.signInIntent);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.blocking, this.email, this.sessionIntentId, this.signInIntent);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BlockingStage builder() {
        return new Builder();
    }

    public interface BlockingStage {
        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         */
        EmailStage blocking(boolean blocking);

        Builder from(CheckoutSessionUpgradeAuthenticationAction other);
    }

    public interface EmailStage {
        /**
         * <p>The address the sign-in starts with — the buyer's own, resolved by this checkout. Pass it to the session-intent create; the one-time code goes to this inbox, and holding that inbox is the entire proof.</p>
         */
        SignInIntentStage email(@NotNull String email);
    }

    public interface SignInIntentStage {
        /**
         * <p>The <code>intent</code> to declare on the session-intent create. Declaring it is what keeps the verification credential checkout-scoped — the full login is minted only by the upgrade call, never by the code alone.</p>
         */
        _FinalStage signInIntent(@NotNull CheckoutSessionUpgradeAuthenticationActionSignInIntent signInIntent);
    }

    public interface _FinalStage {
        CheckoutSessionUpgradeAuthenticationAction build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The continuation, when this reader can take it: the id (<code>ses_…</code>) of the buyer's own mid-checkout sign-in, named only when the read presented the credential that sign-in's verify installed and the sign-in can still be finished. Skip the create and the verify — the emailed factor is already proven — and call THIS intent's <code>upgrade</code> endpoint naming the checkout session: on a first-party page the same credential authorizes it with no bearer at all, and the login collects only what it still owes (nothing, or the account's second factor). <code>null</code> runs the full recipe; a refused continuation is answered the same way, by falling back to it.</p>
         */
        _FinalStage sessionIntentId(Optional<String> sessionIntentId);

        _FinalStage sessionIntentId(String sessionIntentId);

        _FinalStage sessionIntentId(Nullable<String> sessionIntentId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BlockingStage, EmailStage, SignInIntentStage, _FinalStage {
        private boolean blocking;

        private String email;

        private CheckoutSessionUpgradeAuthenticationActionSignInIntent signInIntent;

        private Optional<String> sessionIntentId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionUpgradeAuthenticationAction other) {
            blocking(other.getBlocking());
            email(other.getEmail());
            sessionIntentId(other.getSessionIntentId());
            signInIntent(other.getSignInIntent());
            return this;
        }

        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("blocking")
        public EmailStage blocking(boolean blocking) {
            this.blocking = blocking;
            return this;
        }

        /**
         * <p>The address the sign-in starts with — the buyer's own, resolved by this checkout. Pass it to the session-intent create; the one-time code goes to this inbox, and holding that inbox is the entire proof.</p>
         * <p>The address the sign-in starts with — the buyer's own, resolved by this checkout. Pass it to the session-intent create; the one-time code goes to this inbox, and holding that inbox is the entire proof.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("email")
        public SignInIntentStage email(@NotNull String email) {
            this.email = Objects.requireNonNull(email, "email must not be null");
            return this;
        }

        /**
         * <p>The <code>intent</code> to declare on the session-intent create. Declaring it is what keeps the verification credential checkout-scoped — the full login is minted only by the upgrade call, never by the code alone.</p>
         * <p>The <code>intent</code> to declare on the session-intent create. Declaring it is what keeps the verification credential checkout-scoped — the full login is minted only by the upgrade call, never by the code alone.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("sign_in_intent")
        public _FinalStage signInIntent(@NotNull CheckoutSessionUpgradeAuthenticationActionSignInIntent signInIntent) {
            this.signInIntent = Objects.requireNonNull(signInIntent, "signInIntent must not be null");
            return this;
        }

        /**
         * <p>The continuation, when this reader can take it: the id (<code>ses_…</code>) of the buyer's own mid-checkout sign-in, named only when the read presented the credential that sign-in's verify installed and the sign-in can still be finished. Skip the create and the verify — the emailed factor is already proven — and call THIS intent's <code>upgrade</code> endpoint naming the checkout session: on a first-party page the same credential authorizes it with no bearer at all, and the login collects only what it still owes (nothing, or the account's second factor). <code>null</code> runs the full recipe; a refused continuation is answered the same way, by falling back to it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sessionIntentId(Nullable<String> sessionIntentId) {
            if (sessionIntentId.isNull()) {
                this.sessionIntentId = null;
            } else if (sessionIntentId.isEmpty()) {
                this.sessionIntentId = Optional.empty();
            } else {
                this.sessionIntentId = Optional.of(sessionIntentId.get());
            }
            return this;
        }

        /**
         * <p>The continuation, when this reader can take it: the id (<code>ses_…</code>) of the buyer's own mid-checkout sign-in, named only when the read presented the credential that sign-in's verify installed and the sign-in can still be finished. Skip the create and the verify — the emailed factor is already proven — and call THIS intent's <code>upgrade</code> endpoint naming the checkout session: on a first-party page the same credential authorizes it with no bearer at all, and the login collects only what it still owes (nothing, or the account's second factor). <code>null</code> runs the full recipe; a refused continuation is answered the same way, by falling back to it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sessionIntentId(String sessionIntentId) {
            this.sessionIntentId = Optional.ofNullable(sessionIntentId);
            return this;
        }

        /**
         * <p>The continuation, when this reader can take it: the id (<code>ses_…</code>) of the buyer's own mid-checkout sign-in, named only when the read presented the credential that sign-in's verify installed and the sign-in can still be finished. Skip the create and the verify — the emailed factor is already proven — and call THIS intent's <code>upgrade</code> endpoint naming the checkout session: on a first-party page the same credential authorizes it with no bearer at all, and the login collects only what it still owes (nothing, or the account's second factor). <code>null</code> runs the full recipe; a refused continuation is answered the same way, by falling back to it.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "session_intent_id", nulls = Nulls.SKIP)
        public _FinalStage sessionIntentId(Optional<String> sessionIntentId) {
            this.sessionIntentId = sessionIntentId;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionUpgradeAuthenticationAction build() {
            return new CheckoutSessionUpgradeAuthenticationAction(
                    blocking, email, sessionIntentId, signInIntent, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
