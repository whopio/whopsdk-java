/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AudienceMatchRate.Builder.class)
public final class AudienceMatchRate {
    private final Optional<Double> lowerBound;

    private final AudienceMatchRatePlatform platform;

    private final Optional<AudienceMatchRateStatus> status;

    private final Optional<Double> upperBound;

    private final Map<String, Object> additionalProperties;

    private AudienceMatchRate(
            Optional<Double> lowerBound,
            AudienceMatchRatePlatform platform,
            Optional<AudienceMatchRateStatus> status,
            Optional<Double> upperBound,
            Map<String, Object> additionalProperties) {
        this.lowerBound = lowerBound;
        this.platform = platform;
        this.status = status;
        this.upperBound = upperBound;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Lower bound of the estimated match rate percentage. <code>null</code> until available.
     */
    @JsonIgnore
    public Optional<Double> getLowerBound() {
        if (lowerBound == null) {
            return Optional.empty();
        }
        return lowerBound;
    }

    /**
     * @return The ad platform that provided the match-rate estimate.
     */
    @JsonProperty("platform")
    public AudienceMatchRatePlatform getPlatform() {
        return platform;
    }

    /**
     * @return Availability of the estimated match rate.
     */
    @JsonIgnore
    public Optional<AudienceMatchRateStatus> getStatus() {
        if (status == null) {
            return Optional.empty();
        }
        return status;
    }

    /**
     * @return Upper bound of the estimated match rate percentage. <code>null</code> until available.
     */
    @JsonIgnore
    public Optional<Double> getUpperBound() {
        if (upperBound == null) {
            return Optional.empty();
        }
        return upperBound;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("lower_bound")
    private Optional<Double> _getLowerBound() {
        return lowerBound;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("status")
    private Optional<AudienceMatchRateStatus> _getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("upper_bound")
    private Optional<Double> _getUpperBound() {
        return upperBound;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AudienceMatchRate && equalTo((AudienceMatchRate) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AudienceMatchRate other) {
        return lowerBound.equals(other.lowerBound)
                && platform.equals(other.platform)
                && status.equals(other.status)
                && upperBound.equals(other.upperBound);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.lowerBound, this.platform, this.status, this.upperBound);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static PlatformStage builder() {
        return new Builder();
    }

    public interface PlatformStage {
        /**
         * <p>The ad platform that provided the match-rate estimate.</p>
         */
        _FinalStage platform(@NotNull AudienceMatchRatePlatform platform);

        Builder from(AudienceMatchRate other);
    }

    public interface _FinalStage {
        AudienceMatchRate build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Lower bound of the estimated match rate percentage. <code>null</code> until available.</p>
         */
        _FinalStage lowerBound(Optional<Double> lowerBound);

        _FinalStage lowerBound(Double lowerBound);

        _FinalStage lowerBound(Nullable<Double> lowerBound);

        /**
         * <p>Availability of the estimated match rate.</p>
         */
        _FinalStage status(Optional<AudienceMatchRateStatus> status);

        _FinalStage status(AudienceMatchRateStatus status);

        _FinalStage status(Nullable<AudienceMatchRateStatus> status);

        /**
         * <p>Upper bound of the estimated match rate percentage. <code>null</code> until available.</p>
         */
        _FinalStage upperBound(Optional<Double> upperBound);

        _FinalStage upperBound(Double upperBound);

        _FinalStage upperBound(Nullable<Double> upperBound);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements PlatformStage, _FinalStage {
        private AudienceMatchRatePlatform platform;

        private Optional<Double> upperBound = Optional.empty();

        private Optional<AudienceMatchRateStatus> status = Optional.empty();

        private Optional<Double> lowerBound = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AudienceMatchRate other) {
            lowerBound(other.getLowerBound());
            platform(other.getPlatform());
            status(other.getStatus());
            upperBound(other.getUpperBound());
            return this;
        }

        /**
         * <p>The ad platform that provided the match-rate estimate.</p>
         * <p>The ad platform that provided the match-rate estimate.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("platform")
        public _FinalStage platform(@NotNull AudienceMatchRatePlatform platform) {
            this.platform = Objects.requireNonNull(platform, "platform must not be null");
            return this;
        }

        /**
         * <p>Upper bound of the estimated match rate percentage. <code>null</code> until available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage upperBound(Nullable<Double> upperBound) {
            if (upperBound.isNull()) {
                this.upperBound = null;
            } else if (upperBound.isEmpty()) {
                this.upperBound = Optional.empty();
            } else {
                this.upperBound = Optional.of(upperBound.get());
            }
            return this;
        }

        /**
         * <p>Upper bound of the estimated match rate percentage. <code>null</code> until available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage upperBound(Double upperBound) {
            this.upperBound = Optional.ofNullable(upperBound);
            return this;
        }

        /**
         * <p>Upper bound of the estimated match rate percentage. <code>null</code> until available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "upper_bound", nulls = Nulls.SKIP)
        public _FinalStage upperBound(Optional<Double> upperBound) {
            this.upperBound = upperBound;
            return this;
        }

        /**
         * <p>Availability of the estimated match rate.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage status(Nullable<AudienceMatchRateStatus> status) {
            if (status.isNull()) {
                this.status = null;
            } else if (status.isEmpty()) {
                this.status = Optional.empty();
            } else {
                this.status = Optional.of(status.get());
            }
            return this;
        }

        /**
         * <p>Availability of the estimated match rate.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage status(AudienceMatchRateStatus status) {
            this.status = Optional.ofNullable(status);
            return this;
        }

        /**
         * <p>Availability of the estimated match rate.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "status", nulls = Nulls.SKIP)
        public _FinalStage status(Optional<AudienceMatchRateStatus> status) {
            this.status = status;
            return this;
        }

        /**
         * <p>Lower bound of the estimated match rate percentage. <code>null</code> until available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lowerBound(Nullable<Double> lowerBound) {
            if (lowerBound.isNull()) {
                this.lowerBound = null;
            } else if (lowerBound.isEmpty()) {
                this.lowerBound = Optional.empty();
            } else {
                this.lowerBound = Optional.of(lowerBound.get());
            }
            return this;
        }

        /**
         * <p>Lower bound of the estimated match rate percentage. <code>null</code> until available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lowerBound(Double lowerBound) {
            this.lowerBound = Optional.ofNullable(lowerBound);
            return this;
        }

        /**
         * <p>Lower bound of the estimated match rate percentage. <code>null</code> until available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "lower_bound", nulls = Nulls.SKIP)
        public _FinalStage lowerBound(Optional<Double> lowerBound) {
            this.lowerBound = lowerBound;
            return this;
        }

        @java.lang.Override
        public AudienceMatchRate build() {
            return new AudienceMatchRate(lowerBound, platform, status, upperBound, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
