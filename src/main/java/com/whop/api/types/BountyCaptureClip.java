/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = BountyCaptureClip.Builder.class)
public final class BountyCaptureClip {
    private final String bountySubmissionId;

    private final String createdAt;

    private final Optional<Integer> durationSeconds;

    private final Optional<String> failureCode;

    private final Optional<String> failureMessage;

    private final Optional<String> framesUrl;

    private final String id;

    private final Optional<String> imuUrl;

    private final Optional<String> manifestUrl;

    private final Optional<String> readyAt;

    private final int sequence;

    private final BountyCaptureClipStatus status;

    private final String updatedAt;

    private final Optional<String> videoUrl;

    private final Map<String, Object> additionalProperties;

    private BountyCaptureClip(
            String bountySubmissionId,
            String createdAt,
            Optional<Integer> durationSeconds,
            Optional<String> failureCode,
            Optional<String> failureMessage,
            Optional<String> framesUrl,
            String id,
            Optional<String> imuUrl,
            Optional<String> manifestUrl,
            Optional<String> readyAt,
            int sequence,
            BountyCaptureClipStatus status,
            String updatedAt,
            Optional<String> videoUrl,
            Map<String, Object> additionalProperties) {
        this.bountySubmissionId = bountySubmissionId;
        this.createdAt = createdAt;
        this.durationSeconds = durationSeconds;
        this.failureCode = failureCode;
        this.failureMessage = failureMessage;
        this.framesUrl = framesUrl;
        this.id = id;
        this.imuUrl = imuUrl;
        this.manifestUrl = manifestUrl;
        this.readyAt = readyAt;
        this.sequence = sequence;
        this.status = status;
        this.updatedAt = updatedAt;
        this.videoUrl = videoUrl;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The bounty submission (attempt) this clip belongs to, prefixed <code>btys_</code>.
     */
    @JsonProperty("bounty_submission_id")
    public String getBountySubmissionId() {
        return bountySubmissionId;
    }

    /**
     * @return When the clip was created, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Server-validated clip duration in whole seconds. <code>null</code> until validation completes.
     */
    @JsonIgnore
    public Optional<Integer> getDurationSeconds() {
        if (durationSeconds == null) {
            return Optional.empty();
        }
        return durationSeconds;
    }

    /**
     * @return Stable validation failure code. <code>null</code> unless <code>status</code> is <code>failed</code>.
     */
    @JsonIgnore
    public Optional<String> getFailureCode() {
        if (failureCode == null) {
            return Optional.empty();
        }
        return failureCode;
    }

    /**
     * @return Human-readable validation failure reason. <code>null</code> unless <code>status</code> is <code>failed</code>.
     */
    @JsonIgnore
    public Optional<String> getFailureMessage() {
        if (failureMessage == null) {
            return Optional.empty();
        }
        return failureMessage;
    }

    /**
     * @return Temporary signed URL for the video frame timestamp log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.
     */
    @JsonIgnore
    public Optional<String> getFramesUrl() {
        if (framesUrl == null) {
            return Optional.empty();
        }
        return framesUrl;
    }

    /**
     * @return Capture clip ID, prefixed <code>bclip_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Temporary signed URL for the IMU (accelerometer + gyroscope) log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.
     */
    @JsonIgnore
    public Optional<String> getImuUrl() {
        if (imuUrl == null) {
            return Optional.empty();
        }
        return imuUrl;
    }

    /**
     * @return Temporary signed URL for the capture manifest. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.
     */
    @JsonIgnore
    public Optional<String> getManifestUrl() {
        if (manifestUrl == null) {
            return Optional.empty();
        }
        return manifestUrl;
    }

    /**
     * @return When server-side validation completed successfully, as an ISO 8601 timestamp. <code>null</code> until then.
     */
    @JsonIgnore
    public Optional<String> getReadyAt() {
        if (readyAt == null) {
            return Optional.empty();
        }
        return readyAt;
    }

    /**
     * @return The clip's stable order within the attempt, starting at 1.
     */
    @JsonProperty("sequence")
    public int getSequence() {
        return sequence;
    }

    /**
     * @return Recording and validation state. <code>recording</code> is still capturing; <code>verifying</code> is running server-side validation; <code>ready</code> passed validation and counts toward the verified-duration payout gate; <code>failed</code> did not validate.
     */
    @JsonProperty("status")
    public BountyCaptureClipStatus getStatus() {
        return status;
    }

    /**
     * @return When the clip was last updated, as an ISO 8601 timestamp.
     */
    @JsonProperty("updated_at")
    public String getUpdatedAt() {
        return updatedAt;
    }

    /**
     * @return Temporary signed URL for the synchronized MP4 video. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.
     */
    @JsonIgnore
    public Optional<String> getVideoUrl() {
        if (videoUrl == null) {
            return Optional.empty();
        }
        return videoUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("duration_seconds")
    private Optional<Integer> _getDurationSeconds() {
        return durationSeconds;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("failure_code")
    private Optional<String> _getFailureCode() {
        return failureCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("failure_message")
    private Optional<String> _getFailureMessage() {
        return failureMessage;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("frames_url")
    private Optional<String> _getFramesUrl() {
        return framesUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("imu_url")
    private Optional<String> _getImuUrl() {
        return imuUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("manifest_url")
    private Optional<String> _getManifestUrl() {
        return manifestUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("ready_at")
    private Optional<String> _getReadyAt() {
        return readyAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("video_url")
    private Optional<String> _getVideoUrl() {
        return videoUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof BountyCaptureClip && equalTo((BountyCaptureClip) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(BountyCaptureClip other) {
        return bountySubmissionId.equals(other.bountySubmissionId)
                && createdAt.equals(other.createdAt)
                && durationSeconds.equals(other.durationSeconds)
                && failureCode.equals(other.failureCode)
                && failureMessage.equals(other.failureMessage)
                && framesUrl.equals(other.framesUrl)
                && id.equals(other.id)
                && imuUrl.equals(other.imuUrl)
                && manifestUrl.equals(other.manifestUrl)
                && readyAt.equals(other.readyAt)
                && sequence == other.sequence
                && status.equals(other.status)
                && updatedAt.equals(other.updatedAt)
                && videoUrl.equals(other.videoUrl);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.bountySubmissionId,
                this.createdAt,
                this.durationSeconds,
                this.failureCode,
                this.failureMessage,
                this.framesUrl,
                this.id,
                this.imuUrl,
                this.manifestUrl,
                this.readyAt,
                this.sequence,
                this.status,
                this.updatedAt,
                this.videoUrl);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BountySubmissionIdStage builder() {
        return new Builder();
    }

    public interface BountySubmissionIdStage {
        /**
         * <p>The bounty submission (attempt) this clip belongs to, prefixed <code>btys_</code>.</p>
         */
        CreatedAtStage bountySubmissionId(@NotNull String bountySubmissionId);

        Builder from(BountyCaptureClip other);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the clip was created, as an ISO 8601 timestamp.</p>
         */
        IdStage createdAt(@NotNull String createdAt);
    }

    public interface IdStage {
        /**
         * <p>Capture clip ID, prefixed <code>bclip_</code>.</p>
         */
        SequenceStage id(@NotNull String id);
    }

    public interface SequenceStage {
        /**
         * <p>The clip's stable order within the attempt, starting at 1.</p>
         */
        StatusStage sequence(int sequence);
    }

    public interface StatusStage {
        /**
         * <p>Recording and validation state. <code>recording</code> is still capturing; <code>verifying</code> is running server-side validation; <code>ready</code> passed validation and counts toward the verified-duration payout gate; <code>failed</code> did not validate.</p>
         */
        UpdatedAtStage status(@NotNull BountyCaptureClipStatus status);
    }

    public interface UpdatedAtStage {
        /**
         * <p>When the clip was last updated, as an ISO 8601 timestamp.</p>
         */
        _FinalStage updatedAt(@NotNull String updatedAt);
    }

    public interface _FinalStage {
        BountyCaptureClip build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Server-validated clip duration in whole seconds. <code>null</code> until validation completes.</p>
         */
        _FinalStage durationSeconds(Optional<Integer> durationSeconds);

        _FinalStage durationSeconds(Integer durationSeconds);

        _FinalStage durationSeconds(Nullable<Integer> durationSeconds);

        /**
         * <p>Stable validation failure code. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         */
        _FinalStage failureCode(Optional<String> failureCode);

        _FinalStage failureCode(String failureCode);

        _FinalStage failureCode(Nullable<String> failureCode);

        /**
         * <p>Human-readable validation failure reason. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         */
        _FinalStage failureMessage(Optional<String> failureMessage);

        _FinalStage failureMessage(String failureMessage);

        _FinalStage failureMessage(Nullable<String> failureMessage);

        /**
         * <p>Temporary signed URL for the video frame timestamp log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        _FinalStage framesUrl(Optional<String> framesUrl);

        _FinalStage framesUrl(String framesUrl);

        _FinalStage framesUrl(Nullable<String> framesUrl);

        /**
         * <p>Temporary signed URL for the IMU (accelerometer + gyroscope) log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        _FinalStage imuUrl(Optional<String> imuUrl);

        _FinalStage imuUrl(String imuUrl);

        _FinalStage imuUrl(Nullable<String> imuUrl);

        /**
         * <p>Temporary signed URL for the capture manifest. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        _FinalStage manifestUrl(Optional<String> manifestUrl);

        _FinalStage manifestUrl(String manifestUrl);

        _FinalStage manifestUrl(Nullable<String> manifestUrl);

        /**
         * <p>When server-side validation completed successfully, as an ISO 8601 timestamp. <code>null</code> until then.</p>
         */
        _FinalStage readyAt(Optional<String> readyAt);

        _FinalStage readyAt(String readyAt);

        _FinalStage readyAt(Nullable<String> readyAt);

        /**
         * <p>Temporary signed URL for the synchronized MP4 video. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        _FinalStage videoUrl(Optional<String> videoUrl);

        _FinalStage videoUrl(String videoUrl);

        _FinalStage videoUrl(Nullable<String> videoUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements BountySubmissionIdStage,
                    CreatedAtStage,
                    IdStage,
                    SequenceStage,
                    StatusStage,
                    UpdatedAtStage,
                    _FinalStage {
        private String bountySubmissionId;

        private String createdAt;

        private String id;

        private int sequence;

        private BountyCaptureClipStatus status;

        private String updatedAt;

        private Optional<String> videoUrl = Optional.empty();

        private Optional<String> readyAt = Optional.empty();

        private Optional<String> manifestUrl = Optional.empty();

        private Optional<String> imuUrl = Optional.empty();

        private Optional<String> framesUrl = Optional.empty();

        private Optional<String> failureMessage = Optional.empty();

        private Optional<String> failureCode = Optional.empty();

        private Optional<Integer> durationSeconds = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(BountyCaptureClip other) {
            bountySubmissionId(other.getBountySubmissionId());
            createdAt(other.getCreatedAt());
            durationSeconds(other.getDurationSeconds());
            failureCode(other.getFailureCode());
            failureMessage(other.getFailureMessage());
            framesUrl(other.getFramesUrl());
            id(other.getId());
            imuUrl(other.getImuUrl());
            manifestUrl(other.getManifestUrl());
            readyAt(other.getReadyAt());
            sequence(other.getSequence());
            status(other.getStatus());
            updatedAt(other.getUpdatedAt());
            videoUrl(other.getVideoUrl());
            return this;
        }

        /**
         * <p>The bounty submission (attempt) this clip belongs to, prefixed <code>btys_</code>.</p>
         * <p>The bounty submission (attempt) this clip belongs to, prefixed <code>btys_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("bounty_submission_id")
        public CreatedAtStage bountySubmissionId(@NotNull String bountySubmissionId) {
            this.bountySubmissionId = Objects.requireNonNull(bountySubmissionId, "bountySubmissionId must not be null");
            return this;
        }

        /**
         * <p>When the clip was created, as an ISO 8601 timestamp.</p>
         * <p>When the clip was created, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public IdStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Capture clip ID, prefixed <code>bclip_</code>.</p>
         * <p>Capture clip ID, prefixed <code>bclip_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public SequenceStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The clip's stable order within the attempt, starting at 1.</p>
         * <p>The clip's stable order within the attempt, starting at 1.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("sequence")
        public StatusStage sequence(int sequence) {
            this.sequence = sequence;
            return this;
        }

        /**
         * <p>Recording and validation state. <code>recording</code> is still capturing; <code>verifying</code> is running server-side validation; <code>ready</code> passed validation and counts toward the verified-duration payout gate; <code>failed</code> did not validate.</p>
         * <p>Recording and validation state. <code>recording</code> is still capturing; <code>verifying</code> is running server-side validation; <code>ready</code> passed validation and counts toward the verified-duration payout gate; <code>failed</code> did not validate.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public UpdatedAtStage status(@NotNull BountyCaptureClipStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>When the clip was last updated, as an ISO 8601 timestamp.</p>
         * <p>When the clip was last updated, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("updated_at")
        public _FinalStage updatedAt(@NotNull String updatedAt) {
            this.updatedAt = Objects.requireNonNull(updatedAt, "updatedAt must not be null");
            return this;
        }

        /**
         * <p>Temporary signed URL for the synchronized MP4 video. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage videoUrl(Nullable<String> videoUrl) {
            if (videoUrl.isNull()) {
                this.videoUrl = null;
            } else if (videoUrl.isEmpty()) {
                this.videoUrl = Optional.empty();
            } else {
                this.videoUrl = Optional.of(videoUrl.get());
            }
            return this;
        }

        /**
         * <p>Temporary signed URL for the synchronized MP4 video. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage videoUrl(String videoUrl) {
            this.videoUrl = Optional.ofNullable(videoUrl);
            return this;
        }

        /**
         * <p>Temporary signed URL for the synchronized MP4 video. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "video_url", nulls = Nulls.SKIP)
        public _FinalStage videoUrl(Optional<String> videoUrl) {
            this.videoUrl = videoUrl;
            return this;
        }

        /**
         * <p>When server-side validation completed successfully, as an ISO 8601 timestamp. <code>null</code> until then.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage readyAt(Nullable<String> readyAt) {
            if (readyAt.isNull()) {
                this.readyAt = null;
            } else if (readyAt.isEmpty()) {
                this.readyAt = Optional.empty();
            } else {
                this.readyAt = Optional.of(readyAt.get());
            }
            return this;
        }

        /**
         * <p>When server-side validation completed successfully, as an ISO 8601 timestamp. <code>null</code> until then.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage readyAt(String readyAt) {
            this.readyAt = Optional.ofNullable(readyAt);
            return this;
        }

        /**
         * <p>When server-side validation completed successfully, as an ISO 8601 timestamp. <code>null</code> until then.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "ready_at", nulls = Nulls.SKIP)
        public _FinalStage readyAt(Optional<String> readyAt) {
            this.readyAt = readyAt;
            return this;
        }

        /**
         * <p>Temporary signed URL for the capture manifest. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage manifestUrl(Nullable<String> manifestUrl) {
            if (manifestUrl.isNull()) {
                this.manifestUrl = null;
            } else if (manifestUrl.isEmpty()) {
                this.manifestUrl = Optional.empty();
            } else {
                this.manifestUrl = Optional.of(manifestUrl.get());
            }
            return this;
        }

        /**
         * <p>Temporary signed URL for the capture manifest. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage manifestUrl(String manifestUrl) {
            this.manifestUrl = Optional.ofNullable(manifestUrl);
            return this;
        }

        /**
         * <p>Temporary signed URL for the capture manifest. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "manifest_url", nulls = Nulls.SKIP)
        public _FinalStage manifestUrl(Optional<String> manifestUrl) {
            this.manifestUrl = manifestUrl;
            return this;
        }

        /**
         * <p>Temporary signed URL for the IMU (accelerometer + gyroscope) log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage imuUrl(Nullable<String> imuUrl) {
            if (imuUrl.isNull()) {
                this.imuUrl = null;
            } else if (imuUrl.isEmpty()) {
                this.imuUrl = Optional.empty();
            } else {
                this.imuUrl = Optional.of(imuUrl.get());
            }
            return this;
        }

        /**
         * <p>Temporary signed URL for the IMU (accelerometer + gyroscope) log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage imuUrl(String imuUrl) {
            this.imuUrl = Optional.ofNullable(imuUrl);
            return this;
        }

        /**
         * <p>Temporary signed URL for the IMU (accelerometer + gyroscope) log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "imu_url", nulls = Nulls.SKIP)
        public _FinalStage imuUrl(Optional<String> imuUrl) {
            this.imuUrl = imuUrl;
            return this;
        }

        /**
         * <p>Temporary signed URL for the video frame timestamp log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage framesUrl(Nullable<String> framesUrl) {
            if (framesUrl.isNull()) {
                this.framesUrl = null;
            } else if (framesUrl.isEmpty()) {
                this.framesUrl = Optional.empty();
            } else {
                this.framesUrl = Optional.of(framesUrl.get());
            }
            return this;
        }

        /**
         * <p>Temporary signed URL for the video frame timestamp log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage framesUrl(String framesUrl) {
            this.framesUrl = Optional.ofNullable(framesUrl);
            return this;
        }

        /**
         * <p>Temporary signed URL for the video frame timestamp log. Returned only on single-clip reads for an authorized viewer; <code>null</code> on list responses or until the artifact is attached.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "frames_url", nulls = Nulls.SKIP)
        public _FinalStage framesUrl(Optional<String> framesUrl) {
            this.framesUrl = framesUrl;
            return this;
        }

        /**
         * <p>Human-readable validation failure reason. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage failureMessage(Nullable<String> failureMessage) {
            if (failureMessage.isNull()) {
                this.failureMessage = null;
            } else if (failureMessage.isEmpty()) {
                this.failureMessage = Optional.empty();
            } else {
                this.failureMessage = Optional.of(failureMessage.get());
            }
            return this;
        }

        /**
         * <p>Human-readable validation failure reason. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage failureMessage(String failureMessage) {
            this.failureMessage = Optional.ofNullable(failureMessage);
            return this;
        }

        /**
         * <p>Human-readable validation failure reason. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "failure_message", nulls = Nulls.SKIP)
        public _FinalStage failureMessage(Optional<String> failureMessage) {
            this.failureMessage = failureMessage;
            return this;
        }

        /**
         * <p>Stable validation failure code. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage failureCode(Nullable<String> failureCode) {
            if (failureCode.isNull()) {
                this.failureCode = null;
            } else if (failureCode.isEmpty()) {
                this.failureCode = Optional.empty();
            } else {
                this.failureCode = Optional.of(failureCode.get());
            }
            return this;
        }

        /**
         * <p>Stable validation failure code. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage failureCode(String failureCode) {
            this.failureCode = Optional.ofNullable(failureCode);
            return this;
        }

        /**
         * <p>Stable validation failure code. <code>null</code> unless <code>status</code> is <code>failed</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "failure_code", nulls = Nulls.SKIP)
        public _FinalStage failureCode(Optional<String> failureCode) {
            this.failureCode = failureCode;
            return this;
        }

        /**
         * <p>Server-validated clip duration in whole seconds. <code>null</code> until validation completes.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage durationSeconds(Nullable<Integer> durationSeconds) {
            if (durationSeconds.isNull()) {
                this.durationSeconds = null;
            } else if (durationSeconds.isEmpty()) {
                this.durationSeconds = Optional.empty();
            } else {
                this.durationSeconds = Optional.of(durationSeconds.get());
            }
            return this;
        }

        /**
         * <p>Server-validated clip duration in whole seconds. <code>null</code> until validation completes.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage durationSeconds(Integer durationSeconds) {
            this.durationSeconds = Optional.ofNullable(durationSeconds);
            return this;
        }

        /**
         * <p>Server-validated clip duration in whole seconds. <code>null</code> until validation completes.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "duration_seconds", nulls = Nulls.SKIP)
        public _FinalStage durationSeconds(Optional<Integer> durationSeconds) {
            this.durationSeconds = durationSeconds;
            return this;
        }

        @java.lang.Override
        public BountyCaptureClip build() {
            return new BountyCaptureClip(
                    bountySubmissionId,
                    createdAt,
                    durationSeconds,
                    failureCode,
                    failureMessage,
                    framesUrl,
                    id,
                    imuUrl,
                    manifestUrl,
                    readyAt,
                    sequence,
                    status,
                    updatedAt,
                    videoUrl,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
