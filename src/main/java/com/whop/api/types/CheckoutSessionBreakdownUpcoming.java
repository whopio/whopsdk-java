/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class CheckoutSessionBreakdownUpcoming {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private CheckoutSessionBreakdownUpcoming(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static CheckoutSessionBreakdownUpcoming installments(CheckoutSessionBreakdownUpcomingInstallments value) {
        return new CheckoutSessionBreakdownUpcoming(new InstallmentsValue(value));
    }

    public static CheckoutSessionBreakdownUpcoming oneTime(CheckoutSessionBreakdownUpcomingOneTime value) {
        return new CheckoutSessionBreakdownUpcoming(new OneTimeValue(value));
    }

    public static CheckoutSessionBreakdownUpcoming recurring(CheckoutSessionBreakdownUpcomingRecurring value) {
        return new CheckoutSessionBreakdownUpcoming(new RecurringValue(value));
    }

    public boolean isInstallments() {
        return value instanceof InstallmentsValue;
    }

    public boolean isOneTime() {
        return value instanceof OneTimeValue;
    }

    public boolean isRecurring() {
        return value instanceof RecurringValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CheckoutSessionBreakdownUpcomingInstallments> getInstallments() {
        if (isInstallments()) {
            return Optional.of(((InstallmentsValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionBreakdownUpcomingOneTime> getOneTime() {
        if (isOneTime()) {
            return Optional.of(((OneTimeValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionBreakdownUpcomingRecurring> getRecurring() {
        if (isRecurring()) {
            return Optional.of(((RecurringValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionBreakdownUpcoming
                && value.equals(((CheckoutSessionBreakdownUpcoming) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitInstallments(CheckoutSessionBreakdownUpcomingInstallments installments);

        T visitOneTime(CheckoutSessionBreakdownUpcomingOneTime oneTime);

        T visitRecurring(CheckoutSessionBreakdownUpcomingRecurring recurring);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(InstallmentsValue.class),
        @JsonSubTypes.Type(OneTimeValue.class),
        @JsonSubTypes.Type(RecurringValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("installments")
    @JsonIgnoreProperties("type")
    private static final class InstallmentsValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionBreakdownUpcomingInstallments value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private InstallmentsValue() {}

        private InstallmentsValue(CheckoutSessionBreakdownUpcomingInstallments value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitInstallments(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof InstallmentsValue && equalTo((InstallmentsValue) other);
        }

        private boolean equalTo(InstallmentsValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionBreakdownUpcoming{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("one_time")
    @JsonIgnoreProperties("type")
    private static final class OneTimeValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionBreakdownUpcomingOneTime value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private OneTimeValue() {}

        private OneTimeValue(CheckoutSessionBreakdownUpcomingOneTime value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitOneTime(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof OneTimeValue && equalTo((OneTimeValue) other);
        }

        private boolean equalTo(OneTimeValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionBreakdownUpcoming{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("recurring")
    @JsonIgnoreProperties("type")
    private static final class RecurringValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionBreakdownUpcomingRecurring value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RecurringValue() {}

        private RecurringValue(CheckoutSessionBreakdownUpcomingRecurring value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRecurring(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RecurringValue && equalTo((RecurringValue) other);
        }

        private boolean equalTo(RecurringValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionBreakdownUpcoming{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionBreakdownUpcoming{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
