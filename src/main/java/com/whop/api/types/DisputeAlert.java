/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = DisputeAlert.Builder.class)
public final class DisputeAlert {
    private final Optional<String> accountId;

    private final boolean actionable;

    private final double amount;

    private final Optional<String> cardBrand;

    private final String createdAt;

    private final String currency;

    private final boolean feeCharged;

    private final String id;

    private final Optional<String> issuer;

    private final Optional<DisputeAlertNotActionableReason> notActionableReason;

    private final Optional<String> paymentId;

    private final Optional<String> productId;

    private final String reportedAt;

    private final Optional<String> transactionAt;

    private final DisputeAlertType type;

    private final String updatedAt;

    private final Map<String, Object> additionalProperties;

    private DisputeAlert(
            Optional<String> accountId,
            boolean actionable,
            double amount,
            Optional<String> cardBrand,
            String createdAt,
            String currency,
            boolean feeCharged,
            String id,
            Optional<String> issuer,
            Optional<DisputeAlertNotActionableReason> notActionableReason,
            Optional<String> paymentId,
            Optional<String> productId,
            String reportedAt,
            Optional<String> transactionAt,
            DisputeAlertType type,
            String updatedAt,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.actionable = actionable;
        this.amount = amount;
        this.cardBrand = cardBrand;
        this.createdAt = createdAt;
        this.currency = currency;
        this.feeCharged = feeCharged;
        this.id = id;
        this.issuer = issuer;
        this.notActionableReason = notActionableReason;
        this.paymentId = paymentId;
        this.productId = productId;
        this.reportedAt = reportedAt;
        this.transactionAt = transactionAt;
        this.type = type;
        this.updatedAt = updatedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The account the alerted payment belongs to, prefixed <code>biz_</code>. <code>null</code> while the alert is unmatched.
     */
    @JsonIgnore
    public Optional<String> getAccountId() {
        if (accountId == null) {
            return Optional.empty();
        }
        return accountId;
    }

    /**
     * @return Whether refunding the payment can still avoid a chargeback. <code>false</code> once the payment has been disputed or fully refunded, or when the alert could not be matched to a payment — <code>not_actionable_reason</code> says which.
     */
    @JsonProperty("actionable")
    public boolean getActionable() {
        return actionable;
    }

    /**
     * @return The alerted amount, in whole units of <code>currency</code>. This is what the issuer reported, which can differ from the payment's own amount.
     */
    @JsonProperty("amount")
    public double getAmount() {
        return amount;
    }

    /**
     * @return The card network as reported by the issuer, lowercased, such as <code>visa</code> or <code>mastercard</code>. <code>unknown</code> when the report carries neither a network nor a recognizable BIN.
     */
    @JsonIgnore
    public Optional<String> getCardBrand() {
        if (cardBrand == null) {
            return Optional.empty();
        }
        return cardBrand;
    }

    /**
     * @return When Whop received the alert, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Three-letter ISO currency code of the alerted amount.
     */
    @JsonProperty("currency")
    public String getCurrency() {
        return currency;
    }

    /**
     * @return Whether Whop charged the account an alert fee for this one. Always <code>false</code> for <code>early_fraud_warning</code>, which Whop is not billed for and never passes on.
     */
    @JsonProperty("fee_charged")
    public boolean getFeeCharged() {
        return feeCharged;
    }

    /**
     * @return Dispute alert ID, prefixed <code>dspa_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Name of the bank that issued the card and filed the report.
     */
    @JsonIgnore
    public Optional<String> getIssuer() {
        if (issuer == null) {
            return Optional.empty();
        }
        return issuer;
    }

    /**
     * @return Why refunding can no longer avoid a chargeback. <code>network_resolved</code> when a Visa RDR already closed the case, <code>payment_unmatched</code> when no payment matched, <code>payment_not_captured</code> when it never captured money, <code>payment_disputed</code> once the payment carries a dispute, <code>payment_refunded</code> once fully refunded. <code>null</code> while <code>actionable</code> is true.
     */
    @JsonIgnore
    public Optional<DisputeAlertNotActionableReason> getNotActionableReason() {
        if (notActionableReason == null) {
            return Optional.empty();
        }
        return notActionableReason;
    }

    /**
     * @return The payment the issuer reported, prefixed <code>pay_</code>. <code>null</code> when Whop could not match the report to a payment.
     */
    @JsonIgnore
    public Optional<String> getPaymentId() {
        if (paymentId == null) {
            return Optional.empty();
        }
        return paymentId;
    }

    /**
     * @return The product the alerted payment was for, prefixed <code>prod_</code>.
     */
    @JsonIgnore
    public Optional<String> getProductId() {
        if (productId == null) {
            return Optional.empty();
        }
        return productId;
    }

    /**
     * @return When the issuer filed the report, as an ISO 8601 timestamp. Earlier than <code>created_at</code>, which is when Whop received it.
     */
    @JsonProperty("reported_at")
    public String getReportedAt() {
        return reportedAt;
    }

    /**
     * @return When the reported transaction was made, as an ISO 8601 timestamp.
     */
    @JsonIgnore
    public Optional<String> getTransactionAt() {
        if (transactionAt == null) {
            return Optional.empty();
        }
        return transactionAt;
    }

    /**
     * @return What the issuer sent. <code>early_fraud_warning</code> is a fraud report on a settled payment (Visa TC40 / Mastercard SAFE) — refunding still avoids the chargeback, and Whop never charges a fee for one. <code>dispute_alert</code> is a pre-dispute notice from the issuer's alert network, which Whop pays for and passes on as a fee. <code>rapid_dispute_resolution</code> is a Visa RDR case the network already closed by refunding the payment — nothing is left to act on.
     */
    @JsonProperty("type")
    public DisputeAlertType getType() {
        return type;
    }

    /**
     * @return When the alert was last changed, as an ISO 8601 timestamp.
     */
    @JsonProperty("updated_at")
    public String getUpdatedAt() {
        return updatedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("account_id")
    private Optional<String> _getAccountId() {
        return accountId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("card_brand")
    private Optional<String> _getCardBrand() {
        return cardBrand;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("issuer")
    private Optional<String> _getIssuer() {
        return issuer;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("not_actionable_reason")
    private Optional<DisputeAlertNotActionableReason> _getNotActionableReason() {
        return notActionableReason;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payment_id")
    private Optional<String> _getPaymentId() {
        return paymentId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("product_id")
    private Optional<String> _getProductId() {
        return productId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("transaction_at")
    private Optional<String> _getTransactionAt() {
        return transactionAt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof DisputeAlert && equalTo((DisputeAlert) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(DisputeAlert other) {
        return accountId.equals(other.accountId)
                && actionable == other.actionable
                && amount == other.amount
                && cardBrand.equals(other.cardBrand)
                && createdAt.equals(other.createdAt)
                && currency.equals(other.currency)
                && feeCharged == other.feeCharged
                && id.equals(other.id)
                && issuer.equals(other.issuer)
                && notActionableReason.equals(other.notActionableReason)
                && paymentId.equals(other.paymentId)
                && productId.equals(other.productId)
                && reportedAt.equals(other.reportedAt)
                && transactionAt.equals(other.transactionAt)
                && type.equals(other.type)
                && updatedAt.equals(other.updatedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.actionable,
                this.amount,
                this.cardBrand,
                this.createdAt,
                this.currency,
                this.feeCharged,
                this.id,
                this.issuer,
                this.notActionableReason,
                this.paymentId,
                this.productId,
                this.reportedAt,
                this.transactionAt,
                this.type,
                this.updatedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ActionableStage builder() {
        return new Builder();
    }

    public interface ActionableStage {
        /**
         * <p>Whether refunding the payment can still avoid a chargeback. <code>false</code> once the payment has been disputed or fully refunded, or when the alert could not be matched to a payment — <code>not_actionable_reason</code> says which.</p>
         */
        AmountStage actionable(boolean actionable);

        Builder from(DisputeAlert other);
    }

    public interface AmountStage {
        /**
         * <p>The alerted amount, in whole units of <code>currency</code>. This is what the issuer reported, which can differ from the payment's own amount.</p>
         */
        CreatedAtStage amount(double amount);
    }

    public interface CreatedAtStage {
        /**
         * <p>When Whop received the alert, as an ISO 8601 timestamp.</p>
         */
        CurrencyStage createdAt(@NotNull String createdAt);
    }

    public interface CurrencyStage {
        /**
         * <p>Three-letter ISO currency code of the alerted amount.</p>
         */
        FeeChargedStage currency(@NotNull String currency);
    }

    public interface FeeChargedStage {
        /**
         * <p>Whether Whop charged the account an alert fee for this one. Always <code>false</code> for <code>early_fraud_warning</code>, which Whop is not billed for and never passes on.</p>
         */
        IdStage feeCharged(boolean feeCharged);
    }

    public interface IdStage {
        /**
         * <p>Dispute alert ID, prefixed <code>dspa_</code>.</p>
         */
        ReportedAtStage id(@NotNull String id);
    }

    public interface ReportedAtStage {
        /**
         * <p>When the issuer filed the report, as an ISO 8601 timestamp. Earlier than <code>created_at</code>, which is when Whop received it.</p>
         */
        TypeStage reportedAt(@NotNull String reportedAt);
    }

    public interface TypeStage {
        /**
         * <p>What the issuer sent. <code>early_fraud_warning</code> is a fraud report on a settled payment (Visa TC40 / Mastercard SAFE) — refunding still avoids the chargeback, and Whop never charges a fee for one. <code>dispute_alert</code> is a pre-dispute notice from the issuer's alert network, which Whop pays for and passes on as a fee. <code>rapid_dispute_resolution</code> is a Visa RDR case the network already closed by refunding the payment — nothing is left to act on.</p>
         */
        UpdatedAtStage type(@NotNull DisputeAlertType type);
    }

    public interface UpdatedAtStage {
        /**
         * <p>When the alert was last changed, as an ISO 8601 timestamp.</p>
         */
        _FinalStage updatedAt(@NotNull String updatedAt);
    }

    public interface _FinalStage {
        DisputeAlert build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The account the alerted payment belongs to, prefixed <code>biz_</code>. <code>null</code> while the alert is unmatched.</p>
         */
        _FinalStage accountId(Optional<String> accountId);

        _FinalStage accountId(String accountId);

        _FinalStage accountId(Nullable<String> accountId);

        /**
         * <p>The card network as reported by the issuer, lowercased, such as <code>visa</code> or <code>mastercard</code>. <code>unknown</code> when the report carries neither a network nor a recognizable BIN.</p>
         */
        _FinalStage cardBrand(Optional<String> cardBrand);

        _FinalStage cardBrand(String cardBrand);

        _FinalStage cardBrand(Nullable<String> cardBrand);

        /**
         * <p>Name of the bank that issued the card and filed the report.</p>
         */
        _FinalStage issuer(Optional<String> issuer);

        _FinalStage issuer(String issuer);

        _FinalStage issuer(Nullable<String> issuer);

        /**
         * <p>Why refunding can no longer avoid a chargeback. <code>network_resolved</code> when a Visa RDR already closed the case, <code>payment_unmatched</code> when no payment matched, <code>payment_not_captured</code> when it never captured money, <code>payment_disputed</code> once the payment carries a dispute, <code>payment_refunded</code> once fully refunded. <code>null</code> while <code>actionable</code> is true.</p>
         */
        _FinalStage notActionableReason(Optional<DisputeAlertNotActionableReason> notActionableReason);

        _FinalStage notActionableReason(DisputeAlertNotActionableReason notActionableReason);

        _FinalStage notActionableReason(Nullable<DisputeAlertNotActionableReason> notActionableReason);

        /**
         * <p>The payment the issuer reported, prefixed <code>pay_</code>. <code>null</code> when Whop could not match the report to a payment.</p>
         */
        _FinalStage paymentId(Optional<String> paymentId);

        _FinalStage paymentId(String paymentId);

        _FinalStage paymentId(Nullable<String> paymentId);

        /**
         * <p>The product the alerted payment was for, prefixed <code>prod_</code>.</p>
         */
        _FinalStage productId(Optional<String> productId);

        _FinalStage productId(String productId);

        _FinalStage productId(Nullable<String> productId);

        /**
         * <p>When the reported transaction was made, as an ISO 8601 timestamp.</p>
         */
        _FinalStage transactionAt(Optional<String> transactionAt);

        _FinalStage transactionAt(String transactionAt);

        _FinalStage transactionAt(Nullable<String> transactionAt);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements ActionableStage,
                    AmountStage,
                    CreatedAtStage,
                    CurrencyStage,
                    FeeChargedStage,
                    IdStage,
                    ReportedAtStage,
                    TypeStage,
                    UpdatedAtStage,
                    _FinalStage {
        private boolean actionable;

        private double amount;

        private String createdAt;

        private String currency;

        private boolean feeCharged;

        private String id;

        private String reportedAt;

        private DisputeAlertType type;

        private String updatedAt;

        private Optional<String> transactionAt = Optional.empty();

        private Optional<String> productId = Optional.empty();

        private Optional<String> paymentId = Optional.empty();

        private Optional<DisputeAlertNotActionableReason> notActionableReason = Optional.empty();

        private Optional<String> issuer = Optional.empty();

        private Optional<String> cardBrand = Optional.empty();

        private Optional<String> accountId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(DisputeAlert other) {
            accountId(other.getAccountId());
            actionable(other.getActionable());
            amount(other.getAmount());
            cardBrand(other.getCardBrand());
            createdAt(other.getCreatedAt());
            currency(other.getCurrency());
            feeCharged(other.getFeeCharged());
            id(other.getId());
            issuer(other.getIssuer());
            notActionableReason(other.getNotActionableReason());
            paymentId(other.getPaymentId());
            productId(other.getProductId());
            reportedAt(other.getReportedAt());
            transactionAt(other.getTransactionAt());
            type(other.getType());
            updatedAt(other.getUpdatedAt());
            return this;
        }

        /**
         * <p>Whether refunding the payment can still avoid a chargeback. <code>false</code> once the payment has been disputed or fully refunded, or when the alert could not be matched to a payment — <code>not_actionable_reason</code> says which.</p>
         * <p>Whether refunding the payment can still avoid a chargeback. <code>false</code> once the payment has been disputed or fully refunded, or when the alert could not be matched to a payment — <code>not_actionable_reason</code> says which.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("actionable")
        public AmountStage actionable(boolean actionable) {
            this.actionable = actionable;
            return this;
        }

        /**
         * <p>The alerted amount, in whole units of <code>currency</code>. This is what the issuer reported, which can differ from the payment's own amount.</p>
         * <p>The alerted amount, in whole units of <code>currency</code>. This is what the issuer reported, which can differ from the payment's own amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public CreatedAtStage amount(double amount) {
            this.amount = amount;
            return this;
        }

        /**
         * <p>When Whop received the alert, as an ISO 8601 timestamp.</p>
         * <p>When Whop received the alert, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public CurrencyStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Three-letter ISO currency code of the alerted amount.</p>
         * <p>Three-letter ISO currency code of the alerted amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public FeeChargedStage currency(@NotNull String currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>Whether Whop charged the account an alert fee for this one. Always <code>false</code> for <code>early_fraud_warning</code>, which Whop is not billed for and never passes on.</p>
         * <p>Whether Whop charged the account an alert fee for this one. Always <code>false</code> for <code>early_fraud_warning</code>, which Whop is not billed for and never passes on.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("fee_charged")
        public IdStage feeCharged(boolean feeCharged) {
            this.feeCharged = feeCharged;
            return this;
        }

        /**
         * <p>Dispute alert ID, prefixed <code>dspa_</code>.</p>
         * <p>Dispute alert ID, prefixed <code>dspa_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public ReportedAtStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>When the issuer filed the report, as an ISO 8601 timestamp. Earlier than <code>created_at</code>, which is when Whop received it.</p>
         * <p>When the issuer filed the report, as an ISO 8601 timestamp. Earlier than <code>created_at</code>, which is when Whop received it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("reported_at")
        public TypeStage reportedAt(@NotNull String reportedAt) {
            this.reportedAt = Objects.requireNonNull(reportedAt, "reportedAt must not be null");
            return this;
        }

        /**
         * <p>What the issuer sent. <code>early_fraud_warning</code> is a fraud report on a settled payment (Visa TC40 / Mastercard SAFE) — refunding still avoids the chargeback, and Whop never charges a fee for one. <code>dispute_alert</code> is a pre-dispute notice from the issuer's alert network, which Whop pays for and passes on as a fee. <code>rapid_dispute_resolution</code> is a Visa RDR case the network already closed by refunding the payment — nothing is left to act on.</p>
         * <p>What the issuer sent. <code>early_fraud_warning</code> is a fraud report on a settled payment (Visa TC40 / Mastercard SAFE) — refunding still avoids the chargeback, and Whop never charges a fee for one. <code>dispute_alert</code> is a pre-dispute notice from the issuer's alert network, which Whop pays for and passes on as a fee. <code>rapid_dispute_resolution</code> is a Visa RDR case the network already closed by refunding the payment — nothing is left to act on.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("type")
        public UpdatedAtStage type(@NotNull DisputeAlertType type) {
            this.type = Objects.requireNonNull(type, "type must not be null");
            return this;
        }

        /**
         * <p>When the alert was last changed, as an ISO 8601 timestamp.</p>
         * <p>When the alert was last changed, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("updated_at")
        public _FinalStage updatedAt(@NotNull String updatedAt) {
            this.updatedAt = Objects.requireNonNull(updatedAt, "updatedAt must not be null");
            return this;
        }

        /**
         * <p>When the reported transaction was made, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage transactionAt(Nullable<String> transactionAt) {
            if (transactionAt.isNull()) {
                this.transactionAt = null;
            } else if (transactionAt.isEmpty()) {
                this.transactionAt = Optional.empty();
            } else {
                this.transactionAt = Optional.of(transactionAt.get());
            }
            return this;
        }

        /**
         * <p>When the reported transaction was made, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage transactionAt(String transactionAt) {
            this.transactionAt = Optional.ofNullable(transactionAt);
            return this;
        }

        /**
         * <p>When the reported transaction was made, as an ISO 8601 timestamp.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "transaction_at", nulls = Nulls.SKIP)
        public _FinalStage transactionAt(Optional<String> transactionAt) {
            this.transactionAt = transactionAt;
            return this;
        }

        /**
         * <p>The product the alerted payment was for, prefixed <code>prod_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage productId(Nullable<String> productId) {
            if (productId.isNull()) {
                this.productId = null;
            } else if (productId.isEmpty()) {
                this.productId = Optional.empty();
            } else {
                this.productId = Optional.of(productId.get());
            }
            return this;
        }

        /**
         * <p>The product the alerted payment was for, prefixed <code>prod_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage productId(String productId) {
            this.productId = Optional.ofNullable(productId);
            return this;
        }

        /**
         * <p>The product the alerted payment was for, prefixed <code>prod_</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "product_id", nulls = Nulls.SKIP)
        public _FinalStage productId(Optional<String> productId) {
            this.productId = productId;
            return this;
        }

        /**
         * <p>The payment the issuer reported, prefixed <code>pay_</code>. <code>null</code> when Whop could not match the report to a payment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage paymentId(Nullable<String> paymentId) {
            if (paymentId.isNull()) {
                this.paymentId = null;
            } else if (paymentId.isEmpty()) {
                this.paymentId = Optional.empty();
            } else {
                this.paymentId = Optional.of(paymentId.get());
            }
            return this;
        }

        /**
         * <p>The payment the issuer reported, prefixed <code>pay_</code>. <code>null</code> when Whop could not match the report to a payment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage paymentId(String paymentId) {
            this.paymentId = Optional.ofNullable(paymentId);
            return this;
        }

        /**
         * <p>The payment the issuer reported, prefixed <code>pay_</code>. <code>null</code> when Whop could not match the report to a payment.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payment_id", nulls = Nulls.SKIP)
        public _FinalStage paymentId(Optional<String> paymentId) {
            this.paymentId = paymentId;
            return this;
        }

        /**
         * <p>Why refunding can no longer avoid a chargeback. <code>network_resolved</code> when a Visa RDR already closed the case, <code>payment_unmatched</code> when no payment matched, <code>payment_not_captured</code> when it never captured money, <code>payment_disputed</code> once the payment carries a dispute, <code>payment_refunded</code> once fully refunded. <code>null</code> while <code>actionable</code> is true.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage notActionableReason(Nullable<DisputeAlertNotActionableReason> notActionableReason) {
            if (notActionableReason.isNull()) {
                this.notActionableReason = null;
            } else if (notActionableReason.isEmpty()) {
                this.notActionableReason = Optional.empty();
            } else {
                this.notActionableReason = Optional.of(notActionableReason.get());
            }
            return this;
        }

        /**
         * <p>Why refunding can no longer avoid a chargeback. <code>network_resolved</code> when a Visa RDR already closed the case, <code>payment_unmatched</code> when no payment matched, <code>payment_not_captured</code> when it never captured money, <code>payment_disputed</code> once the payment carries a dispute, <code>payment_refunded</code> once fully refunded. <code>null</code> while <code>actionable</code> is true.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage notActionableReason(DisputeAlertNotActionableReason notActionableReason) {
            this.notActionableReason = Optional.ofNullable(notActionableReason);
            return this;
        }

        /**
         * <p>Why refunding can no longer avoid a chargeback. <code>network_resolved</code> when a Visa RDR already closed the case, <code>payment_unmatched</code> when no payment matched, <code>payment_not_captured</code> when it never captured money, <code>payment_disputed</code> once the payment carries a dispute, <code>payment_refunded</code> once fully refunded. <code>null</code> while <code>actionable</code> is true.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "not_actionable_reason", nulls = Nulls.SKIP)
        public _FinalStage notActionableReason(Optional<DisputeAlertNotActionableReason> notActionableReason) {
            this.notActionableReason = notActionableReason;
            return this;
        }

        /**
         * <p>Name of the bank that issued the card and filed the report.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage issuer(Nullable<String> issuer) {
            if (issuer.isNull()) {
                this.issuer = null;
            } else if (issuer.isEmpty()) {
                this.issuer = Optional.empty();
            } else {
                this.issuer = Optional.of(issuer.get());
            }
            return this;
        }

        /**
         * <p>Name of the bank that issued the card and filed the report.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage issuer(String issuer) {
            this.issuer = Optional.ofNullable(issuer);
            return this;
        }

        /**
         * <p>Name of the bank that issued the card and filed the report.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "issuer", nulls = Nulls.SKIP)
        public _FinalStage issuer(Optional<String> issuer) {
            this.issuer = issuer;
            return this;
        }

        /**
         * <p>The card network as reported by the issuer, lowercased, such as <code>visa</code> or <code>mastercard</code>. <code>unknown</code> when the report carries neither a network nor a recognizable BIN.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage cardBrand(Nullable<String> cardBrand) {
            if (cardBrand.isNull()) {
                this.cardBrand = null;
            } else if (cardBrand.isEmpty()) {
                this.cardBrand = Optional.empty();
            } else {
                this.cardBrand = Optional.of(cardBrand.get());
            }
            return this;
        }

        /**
         * <p>The card network as reported by the issuer, lowercased, such as <code>visa</code> or <code>mastercard</code>. <code>unknown</code> when the report carries neither a network nor a recognizable BIN.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage cardBrand(String cardBrand) {
            this.cardBrand = Optional.ofNullable(cardBrand);
            return this;
        }

        /**
         * <p>The card network as reported by the issuer, lowercased, such as <code>visa</code> or <code>mastercard</code>. <code>unknown</code> when the report carries neither a network nor a recognizable BIN.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "card_brand", nulls = Nulls.SKIP)
        public _FinalStage cardBrand(Optional<String> cardBrand) {
            this.cardBrand = cardBrand;
            return this;
        }

        /**
         * <p>The account the alerted payment belongs to, prefixed <code>biz_</code>. <code>null</code> while the alert is unmatched.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountId(Nullable<String> accountId) {
            if (accountId.isNull()) {
                this.accountId = null;
            } else if (accountId.isEmpty()) {
                this.accountId = Optional.empty();
            } else {
                this.accountId = Optional.of(accountId.get());
            }
            return this;
        }

        /**
         * <p>The account the alerted payment belongs to, prefixed <code>biz_</code>. <code>null</code> while the alert is unmatched.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountId(String accountId) {
            this.accountId = Optional.ofNullable(accountId);
            return this;
        }

        /**
         * <p>The account the alerted payment belongs to, prefixed <code>biz_</code>. <code>null</code> while the alert is unmatched.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "account_id", nulls = Nulls.SKIP)
        public _FinalStage accountId(Optional<String> accountId) {
            this.accountId = accountId;
            return this;
        }

        @java.lang.Override
        public DisputeAlert build() {
            return new DisputeAlert(
                    accountId,
                    actionable,
                    amount,
                    cardBrand,
                    createdAt,
                    currency,
                    feeCharged,
                    id,
                    issuer,
                    notActionableReason,
                    paymentId,
                    productId,
                    reportedAt,
                    transactionAt,
                    type,
                    updatedAt,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
