/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PaymentMethodCardPaymentMethodCard.Builder.class)
public final class PaymentMethodCardPaymentMethodCard {
    private final Optional<CardBrands> brand;

    private final Optional<Integer> expMonth;

    private final Optional<Integer> expYear;

    private final boolean expired;

    private final Optional<String> fingerprint;

    private final Optional<CardFundingTypes> fundingType;

    private final Optional<String> last4;

    private final boolean threeDsVerified;

    private final Map<String, Object> additionalProperties;

    private PaymentMethodCardPaymentMethodCard(
            Optional<CardBrands> brand,
            Optional<Integer> expMonth,
            Optional<Integer> expYear,
            boolean expired,
            Optional<String> fingerprint,
            Optional<CardFundingTypes> fundingType,
            Optional<String> last4,
            boolean threeDsVerified,
            Map<String, Object> additionalProperties) {
        this.brand = brand;
        this.expMonth = expMonth;
        this.expYear = expYear;
        this.expired = expired;
        this.fingerprint = fingerprint;
        this.fundingType = fundingType;
        this.last4 = last4;
        this.threeDsVerified = threeDsVerified;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The card network (e.g., visa, mastercard, amex). Null if the brand could not be determined.
     */
    @JsonIgnore
    public Optional<CardBrands> getBrand() {
        if (brand == null) {
            return Optional.empty();
        }
        return brand;
    }

    /**
     * @return The two-digit expiration month of the card (1-12). Null if not available.
     */
    @JsonIgnore
    public Optional<Integer> getExpMonth() {
        if (expMonth == null) {
            return Optional.empty();
        }
        return expMonth;
    }

    /**
     * @return The two-digit expiration year of the card (e.g., 27 for 2027). Null if not available.
     */
    @JsonIgnore
    public Optional<Integer> getExpYear() {
        if (expYear == null) {
            return Optional.empty();
        }
        return expYear;
    }

    /**
     * @return Whether the card is past its expiration month. An expired card cannot take a new charge.
     */
    @JsonProperty("expired")
    public boolean getExpired() {
        return expired;
    }

    /**
     * @return A stable identifier for the underlying card. Two payment methods with the same fingerprint are the same card. Null if not available.
     */
    @JsonIgnore
    public Optional<String> getFingerprint() {
        if (fingerprint == null) {
            return Optional.empty();
        }
        return fingerprint;
    }

    /**
     * @return How the card is funded by the issuer. Null if the funding type could not be determined.
     */
    @JsonIgnore
    public Optional<CardFundingTypes> getFundingType() {
        if (fundingType == null) {
            return Optional.empty();
        }
        return fundingType;
    }

    /**
     * @return The last four digits of the card number. Null if not available.
     */
    @JsonIgnore
    public Optional<String> getLast4() {
        if (last4 == null) {
            return Optional.empty();
        }
        return last4;
    }

    /**
     * @return Whether this card was verified with 3D Secure, either when it was saved or on a payment that used it.
     */
    @JsonProperty("three_ds_verified")
    public boolean getThreeDsVerified() {
        return threeDsVerified;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("brand")
    private Optional<CardBrands> _getBrand() {
        return brand;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("exp_month")
    private Optional<Integer> _getExpMonth() {
        return expMonth;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("exp_year")
    private Optional<Integer> _getExpYear() {
        return expYear;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("fingerprint")
    private Optional<String> _getFingerprint() {
        return fingerprint;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("funding_type")
    private Optional<CardFundingTypes> _getFundingType() {
        return fundingType;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last4")
    private Optional<String> _getLast4() {
        return last4;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentMethodCardPaymentMethodCard
                && equalTo((PaymentMethodCardPaymentMethodCard) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PaymentMethodCardPaymentMethodCard other) {
        return brand.equals(other.brand)
                && expMonth.equals(other.expMonth)
                && expYear.equals(other.expYear)
                && expired == other.expired
                && fingerprint.equals(other.fingerprint)
                && fundingType.equals(other.fundingType)
                && last4.equals(other.last4)
                && threeDsVerified == other.threeDsVerified;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.brand,
                this.expMonth,
                this.expYear,
                this.expired,
                this.fingerprint,
                this.fundingType,
                this.last4,
                this.threeDsVerified);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ExpiredStage builder() {
        return new Builder();
    }

    public interface ExpiredStage {
        /**
         * <p>Whether the card is past its expiration month. An expired card cannot take a new charge.</p>
         */
        ThreeDsVerifiedStage expired(boolean expired);

        Builder from(PaymentMethodCardPaymentMethodCard other);
    }

    public interface ThreeDsVerifiedStage {
        /**
         * <p>Whether this card was verified with 3D Secure, either when it was saved or on a payment that used it.</p>
         */
        _FinalStage threeDsVerified(boolean threeDsVerified);
    }

    public interface _FinalStage {
        PaymentMethodCardPaymentMethodCard build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The card network (e.g., visa, mastercard, amex). Null if the brand could not be determined.</p>
         */
        _FinalStage brand(Optional<CardBrands> brand);

        _FinalStage brand(CardBrands brand);

        _FinalStage brand(Nullable<CardBrands> brand);

        /**
         * <p>The two-digit expiration month of the card (1-12). Null if not available.</p>
         */
        _FinalStage expMonth(Optional<Integer> expMonth);

        _FinalStage expMonth(Integer expMonth);

        _FinalStage expMonth(Nullable<Integer> expMonth);

        /**
         * <p>The two-digit expiration year of the card (e.g., 27 for 2027). Null if not available.</p>
         */
        _FinalStage expYear(Optional<Integer> expYear);

        _FinalStage expYear(Integer expYear);

        _FinalStage expYear(Nullable<Integer> expYear);

        /**
         * <p>A stable identifier for the underlying card. Two payment methods with the same fingerprint are the same card. Null if not available.</p>
         */
        _FinalStage fingerprint(Optional<String> fingerprint);

        _FinalStage fingerprint(String fingerprint);

        _FinalStage fingerprint(Nullable<String> fingerprint);

        /**
         * <p>How the card is funded by the issuer. Null if the funding type could not be determined.</p>
         */
        _FinalStage fundingType(Optional<CardFundingTypes> fundingType);

        _FinalStage fundingType(CardFundingTypes fundingType);

        _FinalStage fundingType(Nullable<CardFundingTypes> fundingType);

        /**
         * <p>The last four digits of the card number. Null if not available.</p>
         */
        _FinalStage last4(Optional<String> last4);

        _FinalStage last4(String last4);

        _FinalStage last4(Nullable<String> last4);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements ExpiredStage, ThreeDsVerifiedStage, _FinalStage {
        private boolean expired;

        private boolean threeDsVerified;

        private Optional<String> last4 = Optional.empty();

        private Optional<CardFundingTypes> fundingType = Optional.empty();

        private Optional<String> fingerprint = Optional.empty();

        private Optional<Integer> expYear = Optional.empty();

        private Optional<Integer> expMonth = Optional.empty();

        private Optional<CardBrands> brand = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PaymentMethodCardPaymentMethodCard other) {
            brand(other.getBrand());
            expMonth(other.getExpMonth());
            expYear(other.getExpYear());
            expired(other.getExpired());
            fingerprint(other.getFingerprint());
            fundingType(other.getFundingType());
            last4(other.getLast4());
            threeDsVerified(other.getThreeDsVerified());
            return this;
        }

        /**
         * <p>Whether the card is past its expiration month. An expired card cannot take a new charge.</p>
         * <p>Whether the card is past its expiration month. An expired card cannot take a new charge.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("expired")
        public ThreeDsVerifiedStage expired(boolean expired) {
            this.expired = expired;
            return this;
        }

        /**
         * <p>Whether this card was verified with 3D Secure, either when it was saved or on a payment that used it.</p>
         * <p>Whether this card was verified with 3D Secure, either when it was saved or on a payment that used it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("three_ds_verified")
        public _FinalStage threeDsVerified(boolean threeDsVerified) {
            this.threeDsVerified = threeDsVerified;
            return this;
        }

        /**
         * <p>The last four digits of the card number. Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage last4(Nullable<String> last4) {
            if (last4.isNull()) {
                this.last4 = null;
            } else if (last4.isEmpty()) {
                this.last4 = Optional.empty();
            } else {
                this.last4 = Optional.of(last4.get());
            }
            return this;
        }

        /**
         * <p>The last four digits of the card number. Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage last4(String last4) {
            this.last4 = Optional.ofNullable(last4);
            return this;
        }

        /**
         * <p>The last four digits of the card number. Null if not available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last4", nulls = Nulls.SKIP)
        public _FinalStage last4(Optional<String> last4) {
            this.last4 = last4;
            return this;
        }

        /**
         * <p>How the card is funded by the issuer. Null if the funding type could not be determined.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fundingType(Nullable<CardFundingTypes> fundingType) {
            if (fundingType.isNull()) {
                this.fundingType = null;
            } else if (fundingType.isEmpty()) {
                this.fundingType = Optional.empty();
            } else {
                this.fundingType = Optional.of(fundingType.get());
            }
            return this;
        }

        /**
         * <p>How the card is funded by the issuer. Null if the funding type could not be determined.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fundingType(CardFundingTypes fundingType) {
            this.fundingType = Optional.ofNullable(fundingType);
            return this;
        }

        /**
         * <p>How the card is funded by the issuer. Null if the funding type could not be determined.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "funding_type", nulls = Nulls.SKIP)
        public _FinalStage fundingType(Optional<CardFundingTypes> fundingType) {
            this.fundingType = fundingType;
            return this;
        }

        /**
         * <p>A stable identifier for the underlying card. Two payment methods with the same fingerprint are the same card. Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fingerprint(Nullable<String> fingerprint) {
            if (fingerprint.isNull()) {
                this.fingerprint = null;
            } else if (fingerprint.isEmpty()) {
                this.fingerprint = Optional.empty();
            } else {
                this.fingerprint = Optional.of(fingerprint.get());
            }
            return this;
        }

        /**
         * <p>A stable identifier for the underlying card. Two payment methods with the same fingerprint are the same card. Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fingerprint(String fingerprint) {
            this.fingerprint = Optional.ofNullable(fingerprint);
            return this;
        }

        /**
         * <p>A stable identifier for the underlying card. Two payment methods with the same fingerprint are the same card. Null if not available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "fingerprint", nulls = Nulls.SKIP)
        public _FinalStage fingerprint(Optional<String> fingerprint) {
            this.fingerprint = fingerprint;
            return this;
        }

        /**
         * <p>The two-digit expiration year of the card (e.g., 27 for 2027). Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expYear(Nullable<Integer> expYear) {
            if (expYear.isNull()) {
                this.expYear = null;
            } else if (expYear.isEmpty()) {
                this.expYear = Optional.empty();
            } else {
                this.expYear = Optional.of(expYear.get());
            }
            return this;
        }

        /**
         * <p>The two-digit expiration year of the card (e.g., 27 for 2027). Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expYear(Integer expYear) {
            this.expYear = Optional.ofNullable(expYear);
            return this;
        }

        /**
         * <p>The two-digit expiration year of the card (e.g., 27 for 2027). Null if not available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "exp_year", nulls = Nulls.SKIP)
        public _FinalStage expYear(Optional<Integer> expYear) {
            this.expYear = expYear;
            return this;
        }

        /**
         * <p>The two-digit expiration month of the card (1-12). Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expMonth(Nullable<Integer> expMonth) {
            if (expMonth.isNull()) {
                this.expMonth = null;
            } else if (expMonth.isEmpty()) {
                this.expMonth = Optional.empty();
            } else {
                this.expMonth = Optional.of(expMonth.get());
            }
            return this;
        }

        /**
         * <p>The two-digit expiration month of the card (1-12). Null if not available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expMonth(Integer expMonth) {
            this.expMonth = Optional.ofNullable(expMonth);
            return this;
        }

        /**
         * <p>The two-digit expiration month of the card (1-12). Null if not available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "exp_month", nulls = Nulls.SKIP)
        public _FinalStage expMonth(Optional<Integer> expMonth) {
            this.expMonth = expMonth;
            return this;
        }

        /**
         * <p>The card network (e.g., visa, mastercard, amex). Null if the brand could not be determined.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage brand(Nullable<CardBrands> brand) {
            if (brand.isNull()) {
                this.brand = null;
            } else if (brand.isEmpty()) {
                this.brand = Optional.empty();
            } else {
                this.brand = Optional.of(brand.get());
            }
            return this;
        }

        /**
         * <p>The card network (e.g., visa, mastercard, amex). Null if the brand could not be determined.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage brand(CardBrands brand) {
            this.brand = Optional.ofNullable(brand);
            return this;
        }

        /**
         * <p>The card network (e.g., visa, mastercard, amex). Null if the brand could not be determined.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "brand", nulls = Nulls.SKIP)
        public _FinalStage brand(Optional<CardBrands> brand) {
            this.brand = brand;
            return this;
        }

        @java.lang.Override
        public PaymentMethodCardPaymentMethodCard build() {
            return new PaymentMethodCardPaymentMethodCard(
                    brand,
                    expMonth,
                    expYear,
                    expired,
                    fingerprint,
                    fundingType,
                    last4,
                    threeDsVerified,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
