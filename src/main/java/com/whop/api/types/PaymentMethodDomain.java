/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PaymentMethodDomain.Builder.class)
public final class PaymentMethodDomain {
    private final Optional<String> accountId;

    private final String createdAt;

    private final String hostname;

    private final String id;

    private final PaymentMethodDomainProvider provider;

    private final PaymentMethodDomainStatus status;

    private final String updatedAt;

    private final Map<String, Object> additionalProperties;

    private PaymentMethodDomain(
            Optional<String> accountId,
            String createdAt,
            String hostname,
            String id,
            PaymentMethodDomainProvider provider,
            PaymentMethodDomainStatus status,
            String updatedAt,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.createdAt = createdAt;
        this.hostname = hostname;
        this.id = id;
        this.provider = provider;
        this.status = status;
        this.updatedAt = updatedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return ID of the account the domain is registered for, prefixed <code>biz_</code>.
     */
    @JsonIgnore
    public Optional<String> getAccountId() {
        if (accountId == null) {
            return Optional.empty();
        }
        return accountId;
    }

    /**
     * @return When the domain was created, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Hostname the checkout is served from (e.g. <code>checkout.example.com</code>).
     */
    @JsonProperty("hostname")
    public String getHostname() {
        return hostname;
    }

    /**
     * @return Payment method domain ID, prefixed <code>pmd_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Wallet provider the domain is registered with.
     */
    @JsonProperty("provider")
    public PaymentMethodDomainProvider getProvider() {
        return provider;
    }

    /**
     * @return Verification status. <code>pending</code> means the provider could not fetch the domain-association file yet; only <code>verified</code> domains show wallet payment methods at checkout.
     */
    @JsonProperty("status")
    public PaymentMethodDomainStatus getStatus() {
        return status;
    }

    /**
     * @return When the domain was last updated, as an ISO 8601 timestamp.
     */
    @JsonProperty("updated_at")
    public String getUpdatedAt() {
        return updatedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("account_id")
    private Optional<String> _getAccountId() {
        return accountId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentMethodDomain && equalTo((PaymentMethodDomain) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PaymentMethodDomain other) {
        return accountId.equals(other.accountId)
                && createdAt.equals(other.createdAt)
                && hostname.equals(other.hostname)
                && id.equals(other.id)
                && provider.equals(other.provider)
                && status.equals(other.status)
                && updatedAt.equals(other.updatedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId, this.createdAt, this.hostname, this.id, this.provider, this.status, this.updatedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CreatedAtStage builder() {
        return new Builder();
    }

    public interface CreatedAtStage {
        /**
         * <p>When the domain was created, as an ISO 8601 timestamp.</p>
         */
        HostnameStage createdAt(@NotNull String createdAt);

        Builder from(PaymentMethodDomain other);
    }

    public interface HostnameStage {
        /**
         * <p>Hostname the checkout is served from (e.g. <code>checkout.example.com</code>).</p>
         */
        IdStage hostname(@NotNull String hostname);
    }

    public interface IdStage {
        /**
         * <p>Payment method domain ID, prefixed <code>pmd_</code>.</p>
         */
        ProviderStage id(@NotNull String id);
    }

    public interface ProviderStage {
        /**
         * <p>Wallet provider the domain is registered with.</p>
         */
        StatusStage provider(@NotNull PaymentMethodDomainProvider provider);
    }

    public interface StatusStage {
        /**
         * <p>Verification status. <code>pending</code> means the provider could not fetch the domain-association file yet; only <code>verified</code> domains show wallet payment methods at checkout.</p>
         */
        UpdatedAtStage status(@NotNull PaymentMethodDomainStatus status);
    }

    public interface UpdatedAtStage {
        /**
         * <p>When the domain was last updated, as an ISO 8601 timestamp.</p>
         */
        _FinalStage updatedAt(@NotNull String updatedAt);
    }

    public interface _FinalStage {
        PaymentMethodDomain build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>ID of the account the domain is registered for, prefixed <code>biz_</code>.</p>
         */
        _FinalStage accountId(Optional<String> accountId);

        _FinalStage accountId(String accountId);

        _FinalStage accountId(Nullable<String> accountId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements CreatedAtStage, HostnameStage, IdStage, ProviderStage, StatusStage, UpdatedAtStage, _FinalStage {
        private String createdAt;

        private String hostname;

        private String id;

        private PaymentMethodDomainProvider provider;

        private PaymentMethodDomainStatus status;

        private String updatedAt;

        private Optional<String> accountId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PaymentMethodDomain other) {
            accountId(other.getAccountId());
            createdAt(other.getCreatedAt());
            hostname(other.getHostname());
            id(other.getId());
            provider(other.getProvider());
            status(other.getStatus());
            updatedAt(other.getUpdatedAt());
            return this;
        }

        /**
         * <p>When the domain was created, as an ISO 8601 timestamp.</p>
         * <p>When the domain was created, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public HostnameStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Hostname the checkout is served from (e.g. <code>checkout.example.com</code>).</p>
         * <p>Hostname the checkout is served from (e.g. <code>checkout.example.com</code>).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("hostname")
        public IdStage hostname(@NotNull String hostname) {
            this.hostname = Objects.requireNonNull(hostname, "hostname must not be null");
            return this;
        }

        /**
         * <p>Payment method domain ID, prefixed <code>pmd_</code>.</p>
         * <p>Payment method domain ID, prefixed <code>pmd_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public ProviderStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Wallet provider the domain is registered with.</p>
         * <p>Wallet provider the domain is registered with.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("provider")
        public StatusStage provider(@NotNull PaymentMethodDomainProvider provider) {
            this.provider = Objects.requireNonNull(provider, "provider must not be null");
            return this;
        }

        /**
         * <p>Verification status. <code>pending</code> means the provider could not fetch the domain-association file yet; only <code>verified</code> domains show wallet payment methods at checkout.</p>
         * <p>Verification status. <code>pending</code> means the provider could not fetch the domain-association file yet; only <code>verified</code> domains show wallet payment methods at checkout.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public UpdatedAtStage status(@NotNull PaymentMethodDomainStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>When the domain was last updated, as an ISO 8601 timestamp.</p>
         * <p>When the domain was last updated, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("updated_at")
        public _FinalStage updatedAt(@NotNull String updatedAt) {
            this.updatedAt = Objects.requireNonNull(updatedAt, "updatedAt must not be null");
            return this;
        }

        /**
         * <p>ID of the account the domain is registered for, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountId(Nullable<String> accountId) {
            if (accountId.isNull()) {
                this.accountId = null;
            } else if (accountId.isEmpty()) {
                this.accountId = Optional.empty();
            } else {
                this.accountId = Optional.of(accountId.get());
            }
            return this;
        }

        /**
         * <p>ID of the account the domain is registered for, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountId(String accountId) {
            this.accountId = Optional.ofNullable(accountId);
            return this;
        }

        /**
         * <p>ID of the account the domain is registered for, prefixed <code>biz_</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "account_id", nulls = Nulls.SKIP)
        public _FinalStage accountId(Optional<String> accountId) {
            this.accountId = accountId;
            return this;
        }

        @java.lang.Override
        public PaymentMethodDomain build() {
            return new PaymentMethodDomain(
                    accountId, createdAt, hostname, id, provider, status, updatedAt, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
