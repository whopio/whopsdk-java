/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class MemberStatus {
    public static final MemberStatus LEFT = new MemberStatus(Value.LEFT, "left");

    public static final MemberStatus JOINED = new MemberStatus(Value.JOINED, "joined");

    private final Value value;

    private final String string;

    MemberStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof MemberStatus && this.string.equals(((MemberStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case LEFT:
                return visitor.visitLeft();
            case JOINED:
                return visitor.visitJoined();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static MemberStatus valueOf(String value) {
        switch (value) {
            case "left":
                return LEFT;
            case "joined":
                return JOINED;
            default:
                return new MemberStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        JOINED,

        LEFT,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitJoined();

        T visitLeft();

        T visitUnknown(String unknownType);
    }
}
