/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Member.Builder.class)
public final class Member {
    private final MemberAccessLevel accessLevel;

    private final String accountId;

    private final String createdAt;

    private final String id;

    private final String joinedAt;

    private final Optional<String> lastAccessedAt;

    private final MemberStatus status;

    private final Optional<UserSummary> user;

    private final Map<String, Object> additionalProperties;

    private Member(
            MemberAccessLevel accessLevel,
            String accountId,
            String createdAt,
            String id,
            String joinedAt,
            Optional<String> lastAccessedAt,
            MemberStatus status,
            Optional<UserSummary> user,
            Map<String, Object> additionalProperties) {
        this.accessLevel = accessLevel;
        this.accountId = accountId;
        this.createdAt = createdAt;
        this.id = id;
        this.joinedAt = joinedAt;
        this.lastAccessedAt = lastAccessedAt;
        this.status = status;
        this.user = user;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.
     */
    @JsonProperty("access_level")
    public MemberAccessLevel getAccessLevel() {
        return accessLevel;
    }

    /**
     * @return The account this member belongs to, prefixed <code>biz_</code>.
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return When the member record was created, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Member ID, prefixed <code>mber_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return When the member first joined the account, as an ISO 8601 timestamp.
     */
    @JsonProperty("joined_at")
    public String getJoinedAt() {
        return joinedAt;
    }

    /**
     * @return When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.
     */
    @JsonIgnore
    public Optional<String> getLastAccessedAt() {
        if (lastAccessedAt == null) {
            return Optional.empty();
        }
        return lastAccessedAt;
    }

    /**
     * @return <code>joined</code> while the member is part of the account, <code>left</code> after they leave.
     */
    @JsonProperty("status")
    public MemberStatus getStatus() {
        return status;
    }

    /**
     * @return The user behind this member. <code>null</code> when the buyer is another business rather than a person.
     */
    @JsonIgnore
    public Optional<UserSummary> getUser() {
        if (user == null) {
            return Optional.empty();
        }
        return user;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_accessed_at")
    private Optional<String> _getLastAccessedAt() {
        return lastAccessedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("user")
    private Optional<UserSummary> _getUser() {
        return user;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Member && equalTo((Member) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Member other) {
        return accessLevel.equals(other.accessLevel)
                && accountId.equals(other.accountId)
                && createdAt.equals(other.createdAt)
                && id.equals(other.id)
                && joinedAt.equals(other.joinedAt)
                && lastAccessedAt.equals(other.lastAccessedAt)
                && status.equals(other.status)
                && user.equals(other.user);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accessLevel,
                this.accountId,
                this.createdAt,
                this.id,
                this.joinedAt,
                this.lastAccessedAt,
                this.status,
                this.user);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccessLevelStage builder() {
        return new Builder();
    }

    public interface AccessLevelStage {
        /**
         * <p>What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.</p>
         */
        AccountIdStage accessLevel(@NotNull MemberAccessLevel accessLevel);

        Builder from(Member other);
    }

    public interface AccountIdStage {
        /**
         * <p>The account this member belongs to, prefixed <code>biz_</code>.</p>
         */
        CreatedAtStage accountId(@NotNull String accountId);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the member record was created, as an ISO 8601 timestamp.</p>
         */
        IdStage createdAt(@NotNull String createdAt);
    }

    public interface IdStage {
        /**
         * <p>Member ID, prefixed <code>mber_</code>.</p>
         */
        JoinedAtStage id(@NotNull String id);
    }

    public interface JoinedAtStage {
        /**
         * <p>When the member first joined the account, as an ISO 8601 timestamp.</p>
         */
        StatusStage joinedAt(@NotNull String joinedAt);
    }

    public interface StatusStage {
        /**
         * <p><code>joined</code> while the member is part of the account, <code>left</code> after they leave.</p>
         */
        _FinalStage status(@NotNull MemberStatus status);
    }

    public interface _FinalStage {
        Member build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         */
        _FinalStage lastAccessedAt(Optional<String> lastAccessedAt);

        _FinalStage lastAccessedAt(String lastAccessedAt);

        _FinalStage lastAccessedAt(Nullable<String> lastAccessedAt);

        /**
         * <p>The user behind this member. <code>null</code> when the buyer is another business rather than a person.</p>
         */
        _FinalStage user(Optional<UserSummary> user);

        _FinalStage user(UserSummary user);

        _FinalStage user(Nullable<UserSummary> user);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AccessLevelStage,
                    AccountIdStage,
                    CreatedAtStage,
                    IdStage,
                    JoinedAtStage,
                    StatusStage,
                    _FinalStage {
        private MemberAccessLevel accessLevel;

        private String accountId;

        private String createdAt;

        private String id;

        private String joinedAt;

        private MemberStatus status;

        private Optional<UserSummary> user = Optional.empty();

        private Optional<String> lastAccessedAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Member other) {
            accessLevel(other.getAccessLevel());
            accountId(other.getAccountId());
            createdAt(other.getCreatedAt());
            id(other.getId());
            joinedAt(other.getJoinedAt());
            lastAccessedAt(other.getLastAccessedAt());
            status(other.getStatus());
            user(other.getUser());
            return this;
        }

        /**
         * <p>What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.</p>
         * <p>What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("access_level")
        public AccountIdStage accessLevel(@NotNull MemberAccessLevel accessLevel) {
            this.accessLevel = Objects.requireNonNull(accessLevel, "accessLevel must not be null");
            return this;
        }

        /**
         * <p>The account this member belongs to, prefixed <code>biz_</code>.</p>
         * <p>The account this member belongs to, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public CreatedAtStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        /**
         * <p>When the member record was created, as an ISO 8601 timestamp.</p>
         * <p>When the member record was created, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public IdStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Member ID, prefixed <code>mber_</code>.</p>
         * <p>Member ID, prefixed <code>mber_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public JoinedAtStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>When the member first joined the account, as an ISO 8601 timestamp.</p>
         * <p>When the member first joined the account, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("joined_at")
        public StatusStage joinedAt(@NotNull String joinedAt) {
            this.joinedAt = Objects.requireNonNull(joinedAt, "joinedAt must not be null");
            return this;
        }

        /**
         * <p><code>joined</code> while the member is part of the account, <code>left</code> after they leave.</p>
         * <p><code>joined</code> while the member is part of the account, <code>left</code> after they leave.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull MemberStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>The user behind this member. <code>null</code> when the buyer is another business rather than a person.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage user(Nullable<UserSummary> user) {
            if (user.isNull()) {
                this.user = null;
            } else if (user.isEmpty()) {
                this.user = Optional.empty();
            } else {
                this.user = Optional.of(user.get());
            }
            return this;
        }

        /**
         * <p>The user behind this member. <code>null</code> when the buyer is another business rather than a person.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage user(UserSummary user) {
            this.user = Optional.ofNullable(user);
            return this;
        }

        /**
         * <p>The user behind this member. <code>null</code> when the buyer is another business rather than a person.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "user", nulls = Nulls.SKIP)
        public _FinalStage user(Optional<UserSummary> user) {
            this.user = user;
            return this;
        }

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastAccessedAt(Nullable<String> lastAccessedAt) {
            if (lastAccessedAt.isNull()) {
                this.lastAccessedAt = null;
            } else if (lastAccessedAt.isEmpty()) {
                this.lastAccessedAt = Optional.empty();
            } else {
                this.lastAccessedAt = Optional.of(lastAccessedAt.get());
            }
            return this;
        }

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastAccessedAt(String lastAccessedAt) {
            this.lastAccessedAt = Optional.ofNullable(lastAccessedAt);
            return this;
        }

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_accessed_at", nulls = Nulls.SKIP)
        public _FinalStage lastAccessedAt(Optional<String> lastAccessedAt) {
            this.lastAccessedAt = lastAccessedAt;
            return this;
        }

        @java.lang.Override
        public Member build() {
            return new Member(
                    accessLevel,
                    accountId,
                    createdAt,
                    id,
                    joinedAt,
                    lastAccessedAt,
                    status,
                    user,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
