/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ReleaseMethod {
    public static final ReleaseMethod WAITLIST = new ReleaseMethod(Value.WAITLIST, "waitlist");

    public static final ReleaseMethod BUY_NOW = new ReleaseMethod(Value.BUY_NOW, "buy_now");

    private final Value value;

    private final String string;

    ReleaseMethod(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ReleaseMethod && this.string.equals(((ReleaseMethod) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case WAITLIST:
                return visitor.visitWaitlist();
            case BUY_NOW:
                return visitor.visitBuyNow();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ReleaseMethod valueOf(String value) {
        switch (value) {
            case "waitlist":
                return WAITLIST;
            case "buy_now":
                return BUY_NOW;
            default:
                return new ReleaseMethod(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        BUY_NOW,

        WAITLIST,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitBuyNow();

        T visitWaitlist();

        T visitUnknown(String unknownType);
    }
}
