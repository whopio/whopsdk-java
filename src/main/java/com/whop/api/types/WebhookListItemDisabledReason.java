/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class WebhookListItemDisabledReason {
    public static final WebhookListItemDisabledReason DELIVERY_FAILURES =
            new WebhookListItemDisabledReason(Value.DELIVERY_FAILURES, "delivery_failures");

    private final Value value;

    private final String string;

    WebhookListItemDisabledReason(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof WebhookListItemDisabledReason
                        && this.string.equals(((WebhookListItemDisabledReason) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DELIVERY_FAILURES:
                return visitor.visitDeliveryFailures();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static WebhookListItemDisabledReason valueOf(String value) {
        switch (value) {
            case "delivery_failures":
                return DELIVERY_FAILURES;
            default:
                return new WebhookListItemDisabledReason(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        DELIVERY_FAILURES,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitDeliveryFailures();

        T visitUnknown(String unknownType);
    }
}
