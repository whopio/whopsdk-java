/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AdStatus {
    public static final AdStatus PAUSED = new AdStatus(Value.PAUSED, "paused");

    public static final AdStatus REJECTED = new AdStatus(Value.REJECTED, "rejected");

    public static final AdStatus IN_REVIEW = new AdStatus(Value.IN_REVIEW, "in_review");

    public static final AdStatus ACTIVE = new AdStatus(Value.ACTIVE, "active");

    private final Value value;

    private final String string;

    AdStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof AdStatus && this.string.equals(((AdStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PAUSED:
                return visitor.visitPaused();
            case REJECTED:
                return visitor.visitRejected();
            case IN_REVIEW:
                return visitor.visitInReview();
            case ACTIVE:
                return visitor.visitActive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AdStatus valueOf(String value) {
        switch (value) {
            case "paused":
                return PAUSED;
            case "rejected":
                return REJECTED;
            case "in_review":
                return IN_REVIEW;
            case "active":
                return ACTIVE;
            default:
                return new AdStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        PAUSED,

        IN_REVIEW,

        REJECTED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitPaused();

        T visitInReview();

        T visitRejected();

        T visitUnknown(String unknownType);
    }
}
