/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AdCampaignBudgetOptimization {
    public static final AdCampaignBudgetOptimization AD_GROUP =
            new AdCampaignBudgetOptimization(Value.AD_GROUP, "ad_group");

    public static final AdCampaignBudgetOptimization AD_CAMPAIGN =
            new AdCampaignBudgetOptimization(Value.AD_CAMPAIGN, "ad_campaign");

    private final Value value;

    private final String string;

    AdCampaignBudgetOptimization(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof AdCampaignBudgetOptimization
                        && this.string.equals(((AdCampaignBudgetOptimization) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case AD_GROUP:
                return visitor.visitAdGroup();
            case AD_CAMPAIGN:
                return visitor.visitAdCampaign();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AdCampaignBudgetOptimization valueOf(String value) {
        switch (value) {
            case "ad_group":
                return AD_GROUP;
            case "ad_campaign":
                return AD_CAMPAIGN;
            default:
                return new AdCampaignBudgetOptimization(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        AD_CAMPAIGN,

        AD_GROUP,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitAdCampaign();

        T visitAdGroup();

        T visitUnknown(String unknownType);
    }
}
