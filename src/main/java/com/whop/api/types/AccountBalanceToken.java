/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AccountBalanceToken.Builder.class)
public final class AccountBalanceToken {
    private final String balance;

    private final AccountBalanceBreakdown breakdown;

    private final Optional<String> iconUrl;

    private final String name;

    private final Optional<Double> priceUsd;

    private final String symbol;

    private final Optional<String> valueUsd;

    private final Map<String, Object> additionalProperties;

    private AccountBalanceToken(
            String balance,
            AccountBalanceBreakdown breakdown,
            Optional<String> iconUrl,
            String name,
            Optional<Double> priceUsd,
            String symbol,
            Optional<String> valueUsd,
            Map<String, Object> additionalProperties) {
        this.balance = balance;
        this.breakdown = breakdown;
        this.iconUrl = iconUrl;
        this.name = name;
        this.priceUsd = priceUsd;
        this.symbol = symbol;
        this.valueUsd = valueUsd;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Total amount held in native units, as a decimal string.
     */
    @JsonProperty("balance")
    public String getBalance() {
        return balance;
    }

    /**
     * @return Balance split into available, pending, and reserve amounts, as native-unit decimal strings, with the days the pending amount is expected to settle. On-chain crypto is entirely available; good_funds and fiat cash can have pending or reserve portions.
     */
    @JsonProperty("breakdown")
    public AccountBalanceBreakdown getBreakdown() {
        return breakdown;
    }

    /**
     * @return Holding icon URL.
     */
    @JsonIgnore
    public Optional<String> getIconUrl() {
        if (iconUrl == null) {
            return Optional.empty();
        }
        return iconUrl;
    }

    /**
     * @return The holding's display name
     */
    @JsonProperty("name")
    public String getName() {
        return name;
    }

    /**
     * @return USD price per unit, or <code>null</code> when no exchange rate is available.
     */
    @JsonIgnore
    public Optional<Double> getPriceUsd() {
        if (priceUsd == null) {
            return Optional.empty();
        }
        return priceUsd;
    }

    /**
     * @return Holding display symbol, such as <code>USDT</code>, <code>cbBTC</code>, or <code>EUR</code>.
     */
    @JsonProperty("symbol")
    public String getSymbol() {
        return symbol;
    }

    /**
     * @return Holding USD value, or <code>null</code> when no exchange rate is available.
     */
    @JsonIgnore
    public Optional<String> getValueUsd() {
        if (valueUsd == null) {
            return Optional.empty();
        }
        return valueUsd;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("icon_url")
    private Optional<String> _getIconUrl() {
        return iconUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("price_usd")
    private Optional<Double> _getPriceUsd() {
        return priceUsd;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("value_usd")
    private Optional<String> _getValueUsd() {
        return valueUsd;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AccountBalanceToken && equalTo((AccountBalanceToken) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AccountBalanceToken other) {
        return balance.equals(other.balance)
                && breakdown.equals(other.breakdown)
                && iconUrl.equals(other.iconUrl)
                && name.equals(other.name)
                && priceUsd.equals(other.priceUsd)
                && symbol.equals(other.symbol)
                && valueUsd.equals(other.valueUsd);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.balance, this.breakdown, this.iconUrl, this.name, this.priceUsd, this.symbol, this.valueUsd);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BalanceStage builder() {
        return new Builder();
    }

    public interface BalanceStage {
        /**
         * <p>Total amount held in native units, as a decimal string.</p>
         */
        BreakdownStage balance(@NotNull String balance);

        Builder from(AccountBalanceToken other);
    }

    public interface BreakdownStage {
        /**
         * <p>Balance split into available, pending, and reserve amounts, as native-unit decimal strings, with the days the pending amount is expected to settle. On-chain crypto is entirely available; good_funds and fiat cash can have pending or reserve portions.</p>
         */
        NameStage breakdown(@NotNull AccountBalanceBreakdown breakdown);
    }

    public interface NameStage {
        /**
         * <p>The holding's display name</p>
         */
        SymbolStage name(@NotNull String name);
    }

    public interface SymbolStage {
        /**
         * <p>Holding display symbol, such as <code>USDT</code>, <code>cbBTC</code>, or <code>EUR</code>.</p>
         */
        _FinalStage symbol(@NotNull String symbol);
    }

    public interface _FinalStage {
        AccountBalanceToken build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Holding icon URL.</p>
         */
        _FinalStage iconUrl(Optional<String> iconUrl);

        _FinalStage iconUrl(String iconUrl);

        _FinalStage iconUrl(Nullable<String> iconUrl);

        /**
         * <p>USD price per unit, or <code>null</code> when no exchange rate is available.</p>
         */
        _FinalStage priceUsd(Optional<Double> priceUsd);

        _FinalStage priceUsd(Double priceUsd);

        _FinalStage priceUsd(Nullable<Double> priceUsd);

        /**
         * <p>Holding USD value, or <code>null</code> when no exchange rate is available.</p>
         */
        _FinalStage valueUsd(Optional<String> valueUsd);

        _FinalStage valueUsd(String valueUsd);

        _FinalStage valueUsd(Nullable<String> valueUsd);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BalanceStage, BreakdownStage, NameStage, SymbolStage, _FinalStage {
        private String balance;

        private AccountBalanceBreakdown breakdown;

        private String name;

        private String symbol;

        private Optional<String> valueUsd = Optional.empty();

        private Optional<Double> priceUsd = Optional.empty();

        private Optional<String> iconUrl = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AccountBalanceToken other) {
            balance(other.getBalance());
            breakdown(other.getBreakdown());
            iconUrl(other.getIconUrl());
            name(other.getName());
            priceUsd(other.getPriceUsd());
            symbol(other.getSymbol());
            valueUsd(other.getValueUsd());
            return this;
        }

        /**
         * <p>Total amount held in native units, as a decimal string.</p>
         * <p>Total amount held in native units, as a decimal string.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("balance")
        public BreakdownStage balance(@NotNull String balance) {
            this.balance = Objects.requireNonNull(balance, "balance must not be null");
            return this;
        }

        /**
         * <p>Balance split into available, pending, and reserve amounts, as native-unit decimal strings, with the days the pending amount is expected to settle. On-chain crypto is entirely available; good_funds and fiat cash can have pending or reserve portions.</p>
         * <p>Balance split into available, pending, and reserve amounts, as native-unit decimal strings, with the days the pending amount is expected to settle. On-chain crypto is entirely available; good_funds and fiat cash can have pending or reserve portions.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("breakdown")
        public NameStage breakdown(@NotNull AccountBalanceBreakdown breakdown) {
            this.breakdown = Objects.requireNonNull(breakdown, "breakdown must not be null");
            return this;
        }

        /**
         * <p>The holding's display name</p>
         * <p>The holding's display name</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("name")
        public SymbolStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>Holding display symbol, such as <code>USDT</code>, <code>cbBTC</code>, or <code>EUR</code>.</p>
         * <p>Holding display symbol, such as <code>USDT</code>, <code>cbBTC</code>, or <code>EUR</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("symbol")
        public _FinalStage symbol(@NotNull String symbol) {
            this.symbol = Objects.requireNonNull(symbol, "symbol must not be null");
            return this;
        }

        /**
         * <p>Holding USD value, or <code>null</code> when no exchange rate is available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage valueUsd(Nullable<String> valueUsd) {
            if (valueUsd.isNull()) {
                this.valueUsd = null;
            } else if (valueUsd.isEmpty()) {
                this.valueUsd = Optional.empty();
            } else {
                this.valueUsd = Optional.of(valueUsd.get());
            }
            return this;
        }

        /**
         * <p>Holding USD value, or <code>null</code> when no exchange rate is available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage valueUsd(String valueUsd) {
            this.valueUsd = Optional.ofNullable(valueUsd);
            return this;
        }

        /**
         * <p>Holding USD value, or <code>null</code> when no exchange rate is available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "value_usd", nulls = Nulls.SKIP)
        public _FinalStage valueUsd(Optional<String> valueUsd) {
            this.valueUsd = valueUsd;
            return this;
        }

        /**
         * <p>USD price per unit, or <code>null</code> when no exchange rate is available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage priceUsd(Nullable<Double> priceUsd) {
            if (priceUsd.isNull()) {
                this.priceUsd = null;
            } else if (priceUsd.isEmpty()) {
                this.priceUsd = Optional.empty();
            } else {
                this.priceUsd = Optional.of(priceUsd.get());
            }
            return this;
        }

        /**
         * <p>USD price per unit, or <code>null</code> when no exchange rate is available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage priceUsd(Double priceUsd) {
            this.priceUsd = Optional.ofNullable(priceUsd);
            return this;
        }

        /**
         * <p>USD price per unit, or <code>null</code> when no exchange rate is available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "price_usd", nulls = Nulls.SKIP)
        public _FinalStage priceUsd(Optional<Double> priceUsd) {
            this.priceUsd = priceUsd;
            return this;
        }

        /**
         * <p>Holding icon URL.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage iconUrl(Nullable<String> iconUrl) {
            if (iconUrl.isNull()) {
                this.iconUrl = null;
            } else if (iconUrl.isEmpty()) {
                this.iconUrl = Optional.empty();
            } else {
                this.iconUrl = Optional.of(iconUrl.get());
            }
            return this;
        }

        /**
         * <p>Holding icon URL.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage iconUrl(String iconUrl) {
            this.iconUrl = Optional.ofNullable(iconUrl);
            return this;
        }

        /**
         * <p>Holding icon URL.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "icon_url", nulls = Nulls.SKIP)
        public _FinalStage iconUrl(Optional<String> iconUrl) {
            this.iconUrl = iconUrl;
            return this;
        }

        @java.lang.Override
        public AccountBalanceToken build() {
            return new AccountBalanceToken(
                    balance, breakdown, iconUrl, name, priceUsd, symbol, valueUsd, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
