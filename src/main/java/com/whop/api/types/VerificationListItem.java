/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = VerificationListItem.Builder.class)
public final class VerificationListItem {
    private final String id;

    private final Optional<VerificationErrorCodes> lastErrorCode;

    private final Optional<String> lastErrorReason;

    private final VerificationStatuses status;

    private final Map<String, Object> additionalProperties;

    private VerificationListItem(
            String id,
            Optional<VerificationErrorCodes> lastErrorCode,
            Optional<String> lastErrorReason,
            VerificationStatuses status,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.lastErrorCode = lastErrorCode;
        this.lastErrorReason = lastErrorReason;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The numeric id of the verification record.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The most recent error code returned during verification. Null if no error has occurred.
     */
    @JsonIgnore
    public Optional<VerificationErrorCodes> getLastErrorCode() {
        if (lastErrorCode == null) {
            return Optional.empty();
        }
        return lastErrorCode;
    }

    /**
     * @return A human-readable explanation of the most recent verification error. Null if no error has occurred.
     */
    @JsonIgnore
    public Optional<String> getLastErrorReason() {
        if (lastErrorReason == null) {
            return Optional.empty();
        }
        return lastErrorReason;
    }

    /**
     * @return The current status of this verification session.
     */
    @JsonProperty("status")
    public VerificationStatuses getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_error_code")
    private Optional<VerificationErrorCodes> _getLastErrorCode() {
        return lastErrorCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_error_reason")
    private Optional<String> _getLastErrorReason() {
        return lastErrorReason;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof VerificationListItem && equalTo((VerificationListItem) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(VerificationListItem other) {
        return id.equals(other.id)
                && lastErrorCode.equals(other.lastErrorCode)
                && lastErrorReason.equals(other.lastErrorReason)
                && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.lastErrorCode, this.lastErrorReason, this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The numeric id of the verification record.</p>
         */
        StatusStage id(@NotNull String id);

        Builder from(VerificationListItem other);
    }

    public interface StatusStage {
        /**
         * <p>The current status of this verification session.</p>
         */
        _FinalStage status(@NotNull VerificationStatuses status);
    }

    public interface _FinalStage {
        VerificationListItem build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The most recent error code returned during verification. Null if no error has occurred.</p>
         */
        _FinalStage lastErrorCode(Optional<VerificationErrorCodes> lastErrorCode);

        _FinalStage lastErrorCode(VerificationErrorCodes lastErrorCode);

        _FinalStage lastErrorCode(Nullable<VerificationErrorCodes> lastErrorCode);

        /**
         * <p>A human-readable explanation of the most recent verification error. Null if no error has occurred.</p>
         */
        _FinalStage lastErrorReason(Optional<String> lastErrorReason);

        _FinalStage lastErrorReason(String lastErrorReason);

        _FinalStage lastErrorReason(Nullable<String> lastErrorReason);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, StatusStage, _FinalStage {
        private String id;

        private VerificationStatuses status;

        private Optional<String> lastErrorReason = Optional.empty();

        private Optional<VerificationErrorCodes> lastErrorCode = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(VerificationListItem other) {
            id(other.getId());
            lastErrorCode(other.getLastErrorCode());
            lastErrorReason(other.getLastErrorReason());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>The numeric id of the verification record.</p>
         * <p>The numeric id of the verification record.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public StatusStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The current status of this verification session.</p>
         * <p>The current status of this verification session.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull VerificationStatuses status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>A human-readable explanation of the most recent verification error. Null if no error has occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastErrorReason(Nullable<String> lastErrorReason) {
            if (lastErrorReason.isNull()) {
                this.lastErrorReason = null;
            } else if (lastErrorReason.isEmpty()) {
                this.lastErrorReason = Optional.empty();
            } else {
                this.lastErrorReason = Optional.of(lastErrorReason.get());
            }
            return this;
        }

        /**
         * <p>A human-readable explanation of the most recent verification error. Null if no error has occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastErrorReason(String lastErrorReason) {
            this.lastErrorReason = Optional.ofNullable(lastErrorReason);
            return this;
        }

        /**
         * <p>A human-readable explanation of the most recent verification error. Null if no error has occurred.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_error_reason", nulls = Nulls.SKIP)
        public _FinalStage lastErrorReason(Optional<String> lastErrorReason) {
            this.lastErrorReason = lastErrorReason;
            return this;
        }

        /**
         * <p>The most recent error code returned during verification. Null if no error has occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastErrorCode(Nullable<VerificationErrorCodes> lastErrorCode) {
            if (lastErrorCode.isNull()) {
                this.lastErrorCode = null;
            } else if (lastErrorCode.isEmpty()) {
                this.lastErrorCode = Optional.empty();
            } else {
                this.lastErrorCode = Optional.of(lastErrorCode.get());
            }
            return this;
        }

        /**
         * <p>The most recent error code returned during verification. Null if no error has occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastErrorCode(VerificationErrorCodes lastErrorCode) {
            this.lastErrorCode = Optional.ofNullable(lastErrorCode);
            return this;
        }

        /**
         * <p>The most recent error code returned during verification. Null if no error has occurred.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_error_code", nulls = Nulls.SKIP)
        public _FinalStage lastErrorCode(Optional<VerificationErrorCodes> lastErrorCode) {
            this.lastErrorCode = lastErrorCode;
            return this;
        }

        @java.lang.Override
        public VerificationListItem build() {
            return new VerificationListItem(id, lastErrorCode, lastErrorReason, status, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
