/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PaymentListItemProduct.Builder.class)
public final class PaymentListItemProduct {
    private final String id;

    private final Optional<Map<String, Object>> metadata;

    private final String route;

    private final String title;

    private final Map<String, Object> additionalProperties;

    private PaymentListItemProduct(
            String id,
            Optional<Map<String, Object>> metadata,
            String route,
            String title,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.metadata = metadata;
        this.route = route;
        this.title = title;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The unique identifier for the product.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value.
     */
    @JsonIgnore
    public Optional<Map<String, Object>> getMetadata() {
        if (metadata == null) {
            return Optional.empty();
        }
        return metadata;
    }

    /**
     * @return URL slug in the product's public link, e.g. <code>pickaxe-analytics</code> in whop.com/company/pickaxe-analytics.
     */
    @JsonProperty("route")
    public String getRoute() {
        return route;
    }

    /**
     * @return The display name of the product shown to customers on the product page and in search results.
     */
    @JsonProperty("title")
    public String getTitle() {
        return title;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("metadata")
    private Optional<Map<String, Object>> _getMetadata() {
        return metadata;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentListItemProduct && equalTo((PaymentListItemProduct) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PaymentListItemProduct other) {
        return id.equals(other.id)
                && metadata.equals(other.metadata)
                && route.equals(other.route)
                && title.equals(other.title);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.metadata, this.route, this.title);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The unique identifier for the product.</p>
         */
        RouteStage id(@NotNull String id);

        Builder from(PaymentListItemProduct other);
    }

    public interface RouteStage {
        /**
         * <p>URL slug in the product's public link, e.g. <code>pickaxe-analytics</code> in whop.com/company/pickaxe-analytics.</p>
         */
        TitleStage route(@NotNull String route);
    }

    public interface TitleStage {
        /**
         * <p>The display name of the product shown to customers on the product page and in search results.</p>
         */
        _FinalStage title(@NotNull String title);
    }

    public interface _FinalStage {
        PaymentListItemProduct build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value.</p>
         */
        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);

        _FinalStage metadata(Nullable<Map<String, Object>> metadata);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, RouteStage, TitleStage, _FinalStage {
        private String id;

        private String route;

        private String title;

        private Optional<Map<String, Object>> metadata = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PaymentListItemProduct other) {
            id(other.getId());
            metadata(other.getMetadata());
            route(other.getRoute());
            title(other.getTitle());
            return this;
        }

        /**
         * <p>The unique identifier for the product.</p>
         * <p>The unique identifier for the product.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public RouteStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>URL slug in the product's public link, e.g. <code>pickaxe-analytics</code> in whop.com/company/pickaxe-analytics.</p>
         * <p>URL slug in the product's public link, e.g. <code>pickaxe-analytics</code> in whop.com/company/pickaxe-analytics.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("route")
        public TitleStage route(@NotNull String route) {
            this.route = Objects.requireNonNull(route, "route must not be null");
            return this;
        }

        /**
         * <p>The display name of the product shown to customers on the product page and in search results.</p>
         * <p>The display name of the product shown to customers on the product page and in search results.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("title")
        public _FinalStage title(@NotNull String title) {
            this.title = Objects.requireNonNull(title, "title must not be null");
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Nullable<Map<String, Object>> metadata) {
            if (metadata.isNull()) {
                this.metadata = null;
            } else if (metadata.isEmpty()) {
                this.metadata = Optional.empty();
            } else {
                this.metadata = Optional.of(metadata.get());
            }
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        @java.lang.Override
        public PaymentListItemProduct build() {
            return new PaymentListItemProduct(id, metadata, route, title, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
