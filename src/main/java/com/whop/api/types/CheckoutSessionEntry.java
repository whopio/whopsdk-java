/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionEntry.Builder.class)
public final class CheckoutSessionEntry {
    private final String id;

    private final CheckoutSessionEntryStatus status;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionEntry(
            String id, CheckoutSessionEntryStatus status, Map<String, Object> additionalProperties) {
        this.id = id;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The waitlist entry created by the confirm, prefixed <code>entry_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Where the join stands. <code>succeeded</code> — the join stands (whether the seller accepts it is the entry resource's own story). <code>requires_action</code> — the card save has a step left, carried by <code>next_action</code>. <code>processing</code> — the save is being decided; hold. <code>failed</code> — the card save died: the buyer is NOT on the waitlist, and needs a fresh checkout to join.
     */
    @JsonProperty("status")
    public CheckoutSessionEntryStatus getStatus() {
        return status;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionEntry && equalTo((CheckoutSessionEntry) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionEntry other) {
        return id.equals(other.id) && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The waitlist entry created by the confirm, prefixed <code>entry_</code>.</p>
         */
        StatusStage id(@NotNull String id);

        Builder from(CheckoutSessionEntry other);
    }

    public interface StatusStage {
        /**
         * <p>Where the join stands. <code>succeeded</code> — the join stands (whether the seller accepts it is the entry resource's own story). <code>requires_action</code> — the card save has a step left, carried by <code>next_action</code>. <code>processing</code> — the save is being decided; hold. <code>failed</code> — the card save died: the buyer is NOT on the waitlist, and needs a fresh checkout to join.</p>
         */
        _FinalStage status(@NotNull CheckoutSessionEntryStatus status);
    }

    public interface _FinalStage {
        CheckoutSessionEntry build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, StatusStage, _FinalStage {
        private String id;

        private CheckoutSessionEntryStatus status;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionEntry other) {
            id(other.getId());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>The waitlist entry created by the confirm, prefixed <code>entry_</code>.</p>
         * <p>The waitlist entry created by the confirm, prefixed <code>entry_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public StatusStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Where the join stands. <code>succeeded</code> — the join stands (whether the seller accepts it is the entry resource's own story). <code>requires_action</code> — the card save has a step left, carried by <code>next_action</code>. <code>processing</code> — the save is being decided; hold. <code>failed</code> — the card save died: the buyer is NOT on the waitlist, and needs a fresh checkout to join.</p>
         * <p>Where the join stands. <code>succeeded</code> — the join stands (whether the seller accepts it is the entry resource's own story). <code>requires_action</code> — the card save has a step left, carried by <code>next_action</code>. <code>processing</code> — the save is being decided; hold. <code>failed</code> — the card save died: the buyer is NOT on the waitlist, and needs a fresh checkout to join.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull CheckoutSessionEntryStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        @java.lang.Override
        public CheckoutSessionEntry build() {
            return new CheckoutSessionEntry(id, status, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
