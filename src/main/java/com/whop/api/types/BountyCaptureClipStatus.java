/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class BountyCaptureClipStatus {
    public static final BountyCaptureClipStatus FAILED = new BountyCaptureClipStatus(Value.FAILED, "failed");

    public static final BountyCaptureClipStatus RECORDING = new BountyCaptureClipStatus(Value.RECORDING, "recording");

    public static final BountyCaptureClipStatus VERIFYING = new BountyCaptureClipStatus(Value.VERIFYING, "verifying");

    public static final BountyCaptureClipStatus READY = new BountyCaptureClipStatus(Value.READY, "ready");

    private final Value value;

    private final String string;

    BountyCaptureClipStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof BountyCaptureClipStatus
                        && this.string.equals(((BountyCaptureClipStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FAILED:
                return visitor.visitFailed();
            case RECORDING:
                return visitor.visitRecording();
            case VERIFYING:
                return visitor.visitVerifying();
            case READY:
                return visitor.visitReady();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static BountyCaptureClipStatus valueOf(String value) {
        switch (value) {
            case "failed":
                return FAILED;
            case "recording":
                return RECORDING;
            case "verifying":
                return VERIFYING;
            case "ready":
                return READY;
            default:
                return new BountyCaptureClipStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        RECORDING,

        VERIFYING,

        READY,

        FAILED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitRecording();

        T visitVerifying();

        T visitReady();

        T visitFailed();

        T visitUnknown(String unknownType);
    }
}
