/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionAwaitClaimAction.Builder.class)
public final class CheckoutSessionAwaitClaimAction {
    private final boolean blocking;

    private final CheckoutSessionAwaitClaimActionState state;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionAwaitClaimAction(
            boolean blocking, CheckoutSessionAwaitClaimActionState state, Map<String, Object> additionalProperties) {
        this.blocking = blocking;
        this.state = state;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.
     */
    @JsonProperty("blocking")
    public boolean getBlocking() {
        return blocking;
    }

    /**
     * @return <code>check_email</code> — the claim is not recorded yet: tell the buyer to check their email to claim their purchase. <code>handed_off</code> — the claim is recorded and the checkout finished on another device: this reader keeps seeing it, which is correct — the session never unlocks across devices.
     */
    @JsonProperty("state")
    public CheckoutSessionAwaitClaimActionState getState() {
        return state;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionAwaitClaimAction && equalTo((CheckoutSessionAwaitClaimAction) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionAwaitClaimAction other) {
        return blocking == other.blocking && state.equals(other.state);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.blocking, this.state);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BlockingStage builder() {
        return new Builder();
    }

    public interface BlockingStage {
        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         */
        StateStage blocking(boolean blocking);

        Builder from(CheckoutSessionAwaitClaimAction other);
    }

    public interface StateStage {
        /**
         * <p><code>check_email</code> — the claim is not recorded yet: tell the buyer to check their email to claim their purchase. <code>handed_off</code> — the claim is recorded and the checkout finished on another device: this reader keeps seeing it, which is correct — the session never unlocks across devices.</p>
         */
        _FinalStage state(@NotNull CheckoutSessionAwaitClaimActionState state);
    }

    public interface _FinalStage {
        CheckoutSessionAwaitClaimAction build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BlockingStage, StateStage, _FinalStage {
        private boolean blocking;

        private CheckoutSessionAwaitClaimActionState state;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionAwaitClaimAction other) {
            blocking(other.getBlocking());
            state(other.getState());
            return this;
        }

        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("blocking")
        public StateStage blocking(boolean blocking) {
            this.blocking = blocking;
            return this;
        }

        /**
         * <p><code>check_email</code> — the claim is not recorded yet: tell the buyer to check their email to claim their purchase. <code>handed_off</code> — the claim is recorded and the checkout finished on another device: this reader keeps seeing it, which is correct — the session never unlocks across devices.</p>
         * <p><code>check_email</code> — the claim is not recorded yet: tell the buyer to check their email to claim their purchase. <code>handed_off</code> — the claim is recorded and the checkout finished on another device: this reader keeps seeing it, which is correct — the session never unlocks across devices.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("state")
        public _FinalStage state(@NotNull CheckoutSessionAwaitClaimActionState state) {
            this.state = Objects.requireNonNull(state, "state must not be null");
            return this;
        }

        @java.lang.Override
        public CheckoutSessionAwaitClaimAction build() {
            return new CheckoutSessionAwaitClaimAction(blocking, state, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
