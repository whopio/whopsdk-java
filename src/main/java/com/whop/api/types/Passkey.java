/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Passkey.Builder.class)
public final class Passkey {
    private final String createdAt;

    private final String credentialId;

    private final String id;

    private final Optional<String> lastUsedAt;

    private final String nickname;

    private final Map<String, Object> additionalProperties;

    private Passkey(
            String createdAt,
            String credentialId,
            String id,
            Optional<String> lastUsedAt,
            String nickname,
            Map<String, Object> additionalProperties) {
        this.createdAt = createdAt;
        this.credentialId = credentialId;
        this.id = id;
        this.lastUsedAt = lastUsedAt;
        this.nickname = nickname;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return When the user registered this passkey, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return The WebAuthn credential ID as a base64url string. Pass it in <code>allowCredentials</code> when you run a ceremony against this specific passkey.
     */
    @JsonProperty("credential_id")
    public String getCredentialId() {
        return credentialId;
    }

    /**
     * @return Passkey ID, prefixed <code>wcred_</code>. Use it to delete the passkey.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return When this passkey last completed a WebAuthn ceremony, as an ISO 8601 timestamp, or <code>null</code> if it never has.
     */
    @JsonIgnore
    public Optional<String> getLastUsedAt() {
        if (lastUsedAt == null) {
            return Optional.empty();
        }
        return lastUsedAt;
    }

    /**
     * @return The name the user gave this passkey, usually the device it lives on.
     */
    @JsonProperty("nickname")
    public String getNickname() {
        return nickname;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_used_at")
    private Optional<String> _getLastUsedAt() {
        return lastUsedAt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Passkey && equalTo((Passkey) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Passkey other) {
        return createdAt.equals(other.createdAt)
                && credentialId.equals(other.credentialId)
                && id.equals(other.id)
                && lastUsedAt.equals(other.lastUsedAt)
                && nickname.equals(other.nickname);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.createdAt, this.credentialId, this.id, this.lastUsedAt, this.nickname);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CreatedAtStage builder() {
        return new Builder();
    }

    public interface CreatedAtStage {
        /**
         * <p>When the user registered this passkey, as an ISO 8601 timestamp.</p>
         */
        CredentialIdStage createdAt(@NotNull String createdAt);

        Builder from(Passkey other);
    }

    public interface CredentialIdStage {
        /**
         * <p>The WebAuthn credential ID as a base64url string. Pass it in <code>allowCredentials</code> when you run a ceremony against this specific passkey.</p>
         */
        IdStage credentialId(@NotNull String credentialId);
    }

    public interface IdStage {
        /**
         * <p>Passkey ID, prefixed <code>wcred_</code>. Use it to delete the passkey.</p>
         */
        NicknameStage id(@NotNull String id);
    }

    public interface NicknameStage {
        /**
         * <p>The name the user gave this passkey, usually the device it lives on.</p>
         */
        _FinalStage nickname(@NotNull String nickname);
    }

    public interface _FinalStage {
        Passkey build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When this passkey last completed a WebAuthn ceremony, as an ISO 8601 timestamp, or <code>null</code> if it never has.</p>
         */
        _FinalStage lastUsedAt(Optional<String> lastUsedAt);

        _FinalStage lastUsedAt(String lastUsedAt);

        _FinalStage lastUsedAt(Nullable<String> lastUsedAt);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements CreatedAtStage, CredentialIdStage, IdStage, NicknameStage, _FinalStage {
        private String createdAt;

        private String credentialId;

        private String id;

        private String nickname;

        private Optional<String> lastUsedAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Passkey other) {
            createdAt(other.getCreatedAt());
            credentialId(other.getCredentialId());
            id(other.getId());
            lastUsedAt(other.getLastUsedAt());
            nickname(other.getNickname());
            return this;
        }

        /**
         * <p>When the user registered this passkey, as an ISO 8601 timestamp.</p>
         * <p>When the user registered this passkey, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public CredentialIdStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>The WebAuthn credential ID as a base64url string. Pass it in <code>allowCredentials</code> when you run a ceremony against this specific passkey.</p>
         * <p>The WebAuthn credential ID as a base64url string. Pass it in <code>allowCredentials</code> when you run a ceremony against this specific passkey.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("credential_id")
        public IdStage credentialId(@NotNull String credentialId) {
            this.credentialId = Objects.requireNonNull(credentialId, "credentialId must not be null");
            return this;
        }

        /**
         * <p>Passkey ID, prefixed <code>wcred_</code>. Use it to delete the passkey.</p>
         * <p>Passkey ID, prefixed <code>wcred_</code>. Use it to delete the passkey.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public NicknameStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The name the user gave this passkey, usually the device it lives on.</p>
         * <p>The name the user gave this passkey, usually the device it lives on.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("nickname")
        public _FinalStage nickname(@NotNull String nickname) {
            this.nickname = Objects.requireNonNull(nickname, "nickname must not be null");
            return this;
        }

        /**
         * <p>When this passkey last completed a WebAuthn ceremony, as an ISO 8601 timestamp, or <code>null</code> if it never has.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastUsedAt(Nullable<String> lastUsedAt) {
            if (lastUsedAt.isNull()) {
                this.lastUsedAt = null;
            } else if (lastUsedAt.isEmpty()) {
                this.lastUsedAt = Optional.empty();
            } else {
                this.lastUsedAt = Optional.of(lastUsedAt.get());
            }
            return this;
        }

        /**
         * <p>When this passkey last completed a WebAuthn ceremony, as an ISO 8601 timestamp, or <code>null</code> if it never has.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastUsedAt(String lastUsedAt) {
            this.lastUsedAt = Optional.ofNullable(lastUsedAt);
            return this;
        }

        /**
         * <p>When this passkey last completed a WebAuthn ceremony, as an ISO 8601 timestamp, or <code>null</code> if it never has.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_used_at", nulls = Nulls.SKIP)
        public _FinalStage lastUsedAt(Optional<String> lastUsedAt) {
            this.lastUsedAt = lastUsedAt;
            return this;
        }

        @java.lang.Override
        public Passkey build() {
            return new Passkey(createdAt, credentialId, id, lastUsedAt, nickname, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
