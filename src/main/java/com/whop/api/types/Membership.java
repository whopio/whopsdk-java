/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Membership.Builder.class)
public final class Membership {
    private final MembershipAccount account;

    private final boolean cancelAtPeriodEnd;

    private final String createdAt;

    private final Optional<String> currentPeriodEnd;

    private final String id;

    private final Optional<String> licenseKey;

    private final Optional<MembershipMember> member;

    private final Map<String, Object> metadata;

    private final String planId;

    private final String productId;

    private final MembershipStatus status;

    private final Optional<String> userId;

    private final Map<String, Object> additionalProperties;

    private Membership(
            MembershipAccount account,
            boolean cancelAtPeriodEnd,
            String createdAt,
            Optional<String> currentPeriodEnd,
            String id,
            Optional<String> licenseKey,
            Optional<MembershipMember> member,
            Map<String, Object> metadata,
            String planId,
            String productId,
            MembershipStatus status,
            Optional<String> userId,
            Map<String, Object> additionalProperties) {
        this.account = account;
        this.cancelAtPeriodEnd = cancelAtPeriodEnd;
        this.createdAt = createdAt;
        this.currentPeriodEnd = currentPeriodEnd;
        this.id = id;
        this.licenseKey = licenseKey;
        this.member = member;
        this.metadata = metadata;
        this.planId = planId;
        this.productId = productId;
        this.status = status;
        this.userId = userId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The account (seller) this membership belongs to.
     */
    @JsonProperty("account")
    public MembershipAccount getAccount() {
        return account;
    }

    /**
     * @return Whether the membership is set to cancel when the current billing period ends. Only meaningful for recurring plans.
     */
    @JsonProperty("cancel_at_period_end")
    public boolean getCancelAtPeriodEnd() {
        return cancelAtPeriodEnd;
    }

    /**
     * @return When the membership was created, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return When the current billing period renews, or when a non-renewing membership expires, as an ISO 8601 timestamp. <code>null</code> for one-time purchases with no expiration.
     */
    @JsonIgnore
    public Optional<String> getCurrentPeriodEnd() {
        if (currentPeriodEnd == null) {
            return Optional.empty();
        }
        return currentPeriodEnd;
    }

    /**
     * @return Membership ID, prefixed <code>mem_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The software license key for this membership. Only present when the product includes a software licensing experience.
     */
    @JsonIgnore
    public Optional<String> getLicenseKey() {
        if (licenseKey == null) {
            return Optional.empty();
        }
        return licenseKey;
    }

    /**
     * @return The caller's member row on the account. Present only when the membership belongs to the caller; <code>null</code> on seller-side reads.
     */
    @JsonIgnore
    public Optional<MembershipMember> getMember() {
        if (member == null) {
            return Optional.empty();
        }
        return member;
    }

    /**
     * @return Custom key-value pairs stored on the membership, commonly used for software licensing.
     */
    @JsonProperty("metadata")
    public Map<String, Object> getMetadata() {
        return metadata;
    }

    /**
     * @return The plan the buyer purchased, prefixed <code>plan_</code>.
     */
    @JsonProperty("plan_id")
    public String getPlanId() {
        return planId;
    }

    /**
     * @return The product this membership grants access to, prefixed <code>prod_</code>.
     */
    @JsonProperty("product_id")
    public String getProductId() {
        return productId;
    }

    /**
     * @return Billing state of the membership. <code>active</code>/<code>trialing</code> memberships grant access; <code>past_due</code> is the grace period after a failed payment; <code>completed</code> one-time purchases keep access; <code>canceled</code>/<code>expired</code> do not.
     */
    @JsonProperty("status")
    public MembershipStatus getStatus() {
        return status;
    }

    /**
     * @return The buyer, prefixed <code>user_</code>. <code>null</code> when the buyer is another business or the membership is unclaimed.
     */
    @JsonIgnore
    public Optional<String> getUserId() {
        if (userId == null) {
            return Optional.empty();
        }
        return userId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("current_period_end")
    private Optional<String> _getCurrentPeriodEnd() {
        return currentPeriodEnd;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("license_key")
    private Optional<String> _getLicenseKey() {
        return licenseKey;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("member")
    private Optional<MembershipMember> _getMember() {
        return member;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("user_id")
    private Optional<String> _getUserId() {
        return userId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Membership && equalTo((Membership) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Membership other) {
        return account.equals(other.account)
                && cancelAtPeriodEnd == other.cancelAtPeriodEnd
                && createdAt.equals(other.createdAt)
                && currentPeriodEnd.equals(other.currentPeriodEnd)
                && id.equals(other.id)
                && licenseKey.equals(other.licenseKey)
                && member.equals(other.member)
                && metadata.equals(other.metadata)
                && planId.equals(other.planId)
                && productId.equals(other.productId)
                && status.equals(other.status)
                && userId.equals(other.userId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.account,
                this.cancelAtPeriodEnd,
                this.createdAt,
                this.currentPeriodEnd,
                this.id,
                this.licenseKey,
                this.member,
                this.metadata,
                this.planId,
                this.productId,
                this.status,
                this.userId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountStage builder() {
        return new Builder();
    }

    public interface AccountStage {
        /**
         * <p>The account (seller) this membership belongs to.</p>
         */
        CancelAtPeriodEndStage account(@NotNull MembershipAccount account);

        Builder from(Membership other);
    }

    public interface CancelAtPeriodEndStage {
        /**
         * <p>Whether the membership is set to cancel when the current billing period ends. Only meaningful for recurring plans.</p>
         */
        CreatedAtStage cancelAtPeriodEnd(boolean cancelAtPeriodEnd);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the membership was created, as an ISO 8601 timestamp.</p>
         */
        IdStage createdAt(@NotNull String createdAt);
    }

    public interface IdStage {
        /**
         * <p>Membership ID, prefixed <code>mem_</code>.</p>
         */
        PlanIdStage id(@NotNull String id);
    }

    public interface PlanIdStage {
        /**
         * <p>The plan the buyer purchased, prefixed <code>plan_</code>.</p>
         */
        ProductIdStage planId(@NotNull String planId);
    }

    public interface ProductIdStage {
        /**
         * <p>The product this membership grants access to, prefixed <code>prod_</code>.</p>
         */
        StatusStage productId(@NotNull String productId);
    }

    public interface StatusStage {
        /**
         * <p>Billing state of the membership. <code>active</code>/<code>trialing</code> memberships grant access; <code>past_due</code> is the grace period after a failed payment; <code>completed</code> one-time purchases keep access; <code>canceled</code>/<code>expired</code> do not.</p>
         */
        _FinalStage status(@NotNull MembershipStatus status);
    }

    public interface _FinalStage {
        Membership build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When the current billing period renews, or when a non-renewing membership expires, as an ISO 8601 timestamp. <code>null</code> for one-time purchases with no expiration.</p>
         */
        _FinalStage currentPeriodEnd(Optional<String> currentPeriodEnd);

        _FinalStage currentPeriodEnd(String currentPeriodEnd);

        _FinalStage currentPeriodEnd(Nullable<String> currentPeriodEnd);

        /**
         * <p>The software license key for this membership. Only present when the product includes a software licensing experience.</p>
         */
        _FinalStage licenseKey(Optional<String> licenseKey);

        _FinalStage licenseKey(String licenseKey);

        _FinalStage licenseKey(Nullable<String> licenseKey);

        /**
         * <p>The caller's member row on the account. Present only when the membership belongs to the caller; <code>null</code> on seller-side reads.</p>
         */
        _FinalStage member(Optional<MembershipMember> member);

        _FinalStage member(MembershipMember member);

        _FinalStage member(Nullable<MembershipMember> member);

        /**
         * <p>Custom key-value pairs stored on the membership, commonly used for software licensing.</p>
         */
        _FinalStage metadata(Map<String, Object> metadata);

        _FinalStage putAllMetadata(Map<String, Object> metadata);

        _FinalStage metadata(String key, Object value);

        /**
         * <p>The buyer, prefixed <code>user_</code>. <code>null</code> when the buyer is another business or the membership is unclaimed.</p>
         */
        _FinalStage userId(Optional<String> userId);

        _FinalStage userId(String userId);

        _FinalStage userId(Nullable<String> userId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AccountStage,
                    CancelAtPeriodEndStage,
                    CreatedAtStage,
                    IdStage,
                    PlanIdStage,
                    ProductIdStage,
                    StatusStage,
                    _FinalStage {
        private MembershipAccount account;

        private boolean cancelAtPeriodEnd;

        private String createdAt;

        private String id;

        private String planId;

        private String productId;

        private MembershipStatus status;

        private Optional<String> userId = Optional.empty();

        private Map<String, Object> metadata = new LinkedHashMap<>();

        private Optional<MembershipMember> member = Optional.empty();

        private Optional<String> licenseKey = Optional.empty();

        private Optional<String> currentPeriodEnd = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Membership other) {
            account(other.getAccount());
            cancelAtPeriodEnd(other.getCancelAtPeriodEnd());
            createdAt(other.getCreatedAt());
            currentPeriodEnd(other.getCurrentPeriodEnd());
            id(other.getId());
            licenseKey(other.getLicenseKey());
            member(other.getMember());
            metadata(other.getMetadata());
            planId(other.getPlanId());
            productId(other.getProductId());
            status(other.getStatus());
            userId(other.getUserId());
            return this;
        }

        /**
         * <p>The account (seller) this membership belongs to.</p>
         * <p>The account (seller) this membership belongs to.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account")
        public CancelAtPeriodEndStage account(@NotNull MembershipAccount account) {
            this.account = Objects.requireNonNull(account, "account must not be null");
            return this;
        }

        /**
         * <p>Whether the membership is set to cancel when the current billing period ends. Only meaningful for recurring plans.</p>
         * <p>Whether the membership is set to cancel when the current billing period ends. Only meaningful for recurring plans.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("cancel_at_period_end")
        public CreatedAtStage cancelAtPeriodEnd(boolean cancelAtPeriodEnd) {
            this.cancelAtPeriodEnd = cancelAtPeriodEnd;
            return this;
        }

        /**
         * <p>When the membership was created, as an ISO 8601 timestamp.</p>
         * <p>When the membership was created, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public IdStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Membership ID, prefixed <code>mem_</code>.</p>
         * <p>Membership ID, prefixed <code>mem_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public PlanIdStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The plan the buyer purchased, prefixed <code>plan_</code>.</p>
         * <p>The plan the buyer purchased, prefixed <code>plan_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("plan_id")
        public ProductIdStage planId(@NotNull String planId) {
            this.planId = Objects.requireNonNull(planId, "planId must not be null");
            return this;
        }

        /**
         * <p>The product this membership grants access to, prefixed <code>prod_</code>.</p>
         * <p>The product this membership grants access to, prefixed <code>prod_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("product_id")
        public StatusStage productId(@NotNull String productId) {
            this.productId = Objects.requireNonNull(productId, "productId must not be null");
            return this;
        }

        /**
         * <p>Billing state of the membership. <code>active</code>/<code>trialing</code> memberships grant access; <code>past_due</code> is the grace period after a failed payment; <code>completed</code> one-time purchases keep access; <code>canceled</code>/<code>expired</code> do not.</p>
         * <p>Billing state of the membership. <code>active</code>/<code>trialing</code> memberships grant access; <code>past_due</code> is the grace period after a failed payment; <code>completed</code> one-time purchases keep access; <code>canceled</code>/<code>expired</code> do not.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull MembershipStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>The buyer, prefixed <code>user_</code>. <code>null</code> when the buyer is another business or the membership is unclaimed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage userId(Nullable<String> userId) {
            if (userId.isNull()) {
                this.userId = null;
            } else if (userId.isEmpty()) {
                this.userId = Optional.empty();
            } else {
                this.userId = Optional.of(userId.get());
            }
            return this;
        }

        /**
         * <p>The buyer, prefixed <code>user_</code>. <code>null</code> when the buyer is another business or the membership is unclaimed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage userId(String userId) {
            this.userId = Optional.ofNullable(userId);
            return this;
        }

        /**
         * <p>The buyer, prefixed <code>user_</code>. <code>null</code> when the buyer is another business or the membership is unclaimed.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "user_id", nulls = Nulls.SKIP)
        public _FinalStage userId(Optional<String> userId) {
            this.userId = userId;
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the membership, commonly used for software licensing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(String key, Object value) {
            this.metadata.put(key, value);
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the membership, commonly used for software licensing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage putAllMetadata(Map<String, Object> metadata) {
            if (metadata != null) {
                this.metadata.putAll(metadata);
            }
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the membership, commonly used for software licensing.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata.clear();
            if (metadata != null) {
                this.metadata.putAll(metadata);
            }
            return this;
        }

        /**
         * <p>The caller's member row on the account. Present only when the membership belongs to the caller; <code>null</code> on seller-side reads.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage member(Nullable<MembershipMember> member) {
            if (member.isNull()) {
                this.member = null;
            } else if (member.isEmpty()) {
                this.member = Optional.empty();
            } else {
                this.member = Optional.of(member.get());
            }
            return this;
        }

        /**
         * <p>The caller's member row on the account. Present only when the membership belongs to the caller; <code>null</code> on seller-side reads.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage member(MembershipMember member) {
            this.member = Optional.ofNullable(member);
            return this;
        }

        /**
         * <p>The caller's member row on the account. Present only when the membership belongs to the caller; <code>null</code> on seller-side reads.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "member", nulls = Nulls.SKIP)
        public _FinalStage member(Optional<MembershipMember> member) {
            this.member = member;
            return this;
        }

        /**
         * <p>The software license key for this membership. Only present when the product includes a software licensing experience.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage licenseKey(Nullable<String> licenseKey) {
            if (licenseKey.isNull()) {
                this.licenseKey = null;
            } else if (licenseKey.isEmpty()) {
                this.licenseKey = Optional.empty();
            } else {
                this.licenseKey = Optional.of(licenseKey.get());
            }
            return this;
        }

        /**
         * <p>The software license key for this membership. Only present when the product includes a software licensing experience.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage licenseKey(String licenseKey) {
            this.licenseKey = Optional.ofNullable(licenseKey);
            return this;
        }

        /**
         * <p>The software license key for this membership. Only present when the product includes a software licensing experience.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "license_key", nulls = Nulls.SKIP)
        public _FinalStage licenseKey(Optional<String> licenseKey) {
            this.licenseKey = licenseKey;
            return this;
        }

        /**
         * <p>When the current billing period renews, or when a non-renewing membership expires, as an ISO 8601 timestamp. <code>null</code> for one-time purchases with no expiration.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currentPeriodEnd(Nullable<String> currentPeriodEnd) {
            if (currentPeriodEnd.isNull()) {
                this.currentPeriodEnd = null;
            } else if (currentPeriodEnd.isEmpty()) {
                this.currentPeriodEnd = Optional.empty();
            } else {
                this.currentPeriodEnd = Optional.of(currentPeriodEnd.get());
            }
            return this;
        }

        /**
         * <p>When the current billing period renews, or when a non-renewing membership expires, as an ISO 8601 timestamp. <code>null</code> for one-time purchases with no expiration.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currentPeriodEnd(String currentPeriodEnd) {
            this.currentPeriodEnd = Optional.ofNullable(currentPeriodEnd);
            return this;
        }

        /**
         * <p>When the current billing period renews, or when a non-renewing membership expires, as an ISO 8601 timestamp. <code>null</code> for one-time purchases with no expiration.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "current_period_end", nulls = Nulls.SKIP)
        public _FinalStage currentPeriodEnd(Optional<String> currentPeriodEnd) {
            this.currentPeriodEnd = currentPeriodEnd;
            return this;
        }

        @java.lang.Override
        public Membership build() {
            return new Membership(
                    account,
                    cancelAtPeriodEnd,
                    createdAt,
                    currentPeriodEnd,
                    id,
                    licenseKey,
                    member,
                    metadata,
                    planId,
                    productId,
                    status,
                    userId,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
