/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AdReportBreakdownItemGranularityItem.Builder.class)
public final class AdReportBreakdownItemGranularityItem {
    private final OffsetDateTime bucketStart;

    private final int clicks;

    private final Granularities granularity;

    private final int impressions;

    private final int reach;

    private final Optional<Integer> resultCount;

    private final Optional<ResultLabelKeys> resultLabelKey;

    private final Optional<String> resultLabelOverride;

    private final double spend;

    private final Currencies spendCurrency;

    private final OffsetDateTime statDate;

    private final Optional<Integer> statHour;

    private final Map<String, Object> additionalProperties;

    private AdReportBreakdownItemGranularityItem(
            OffsetDateTime bucketStart,
            int clicks,
            Granularities granularity,
            int impressions,
            int reach,
            Optional<Integer> resultCount,
            Optional<ResultLabelKeys> resultLabelKey,
            Optional<String> resultLabelOverride,
            double spend,
            Currencies spendCurrency,
            OffsetDateTime statDate,
            Optional<Integer> statHour,
            Map<String, Object> additionalProperties) {
        this.bucketStart = bucketStart;
        this.clicks = clicks;
        this.granularity = granularity;
        this.impressions = impressions;
        this.reach = reach;
        this.resultCount = resultCount;
        this.resultLabelKey = resultLabelKey;
        this.resultLabelOverride = resultLabelOverride;
        this.spend = spend;
        this.spendCurrency = spendCurrency;
        this.statDate = statDate;
        this.statHour = statHour;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The bucket's start time as a real UTC instant. <code>(statDate, statHour)</code> resolved in the ad account's reporting timezone — render this in the viewer's local timezone.
     */
    @JsonProperty("bucket_start")
    public OffsetDateTime getBucketStart() {
        return bucketStart;
    }

    /**
     * @return Clicks in this bucket.
     */
    @JsonProperty("clicks")
    public int getClicks() {
        return clicks;
    }

    /**
     * @return The bucket size of this row (<code>hourly</code>, <code>daily</code>, <code>weekly</code>, or <code>monthly</code>).
     */
    @JsonProperty("granularity")
    public Granularities getGranularity() {
        return granularity;
    }

    /**
     * @return Impressions in this bucket.
     */
    @JsonProperty("impressions")
    public int getImpressions() {
        return impressions;
    }

    /**
     * @return Unique users reached in this bucket. Always <code>0</code> for hourly rows (Meta does not return reach at hourly grain).
     */
    @JsonProperty("reach")
    public int getReach() {
        return reach;
    }

    /**
     * @return Count of the primary optimization result in this bucket.
     */
    @JsonIgnore
    public Optional<Integer> getResultCount() {
        if (resultCount == null) {
            return Optional.empty();
        }
        return resultCount;
    }

    /**
     * @return The type of optimization result represented by <code>resultCount</code>.
     */
    @JsonIgnore
    public Optional<ResultLabelKeys> getResultLabelKey() {
        if (resultLabelKey == null) {
            return Optional.empty();
        }
        return resultLabelKey;
    }

    /**
     * @return Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.
     */
    @JsonIgnore
    public Optional<String> getResultLabelOverride() {
        if (resultLabelOverride == null) {
            return Optional.empty();
        }
        return resultLabelOverride;
    }

    /**
     * @return Charged spend in this bucket in the requested reporting currency — the amount billed including platform fees, not the platform-side net spend.
     */
    @JsonProperty("spend")
    public double getSpend() {
        return spend;
    }

    /**
     * @return Currency of the <code>spend</code> value.
     */
    @JsonProperty("spend_currency")
    public Currencies getSpendCurrency() {
        return spendCurrency;
    }

    /**
     * @return The date these stats cover (midnight UTC). For hourly rows, see <code>statHour</code> and <code>bucketStart</code>.
     */
    @JsonProperty("stat_date")
    public OffsetDateTime getStatDate() {
        return statDate;
    }

    /**
     * @return Hour of the day in the ad account's reporting timezone (0-23). <code>null</code> for daily rows.
     */
    @JsonIgnore
    public Optional<Integer> getStatHour() {
        if (statHour == null) {
            return Optional.empty();
        }
        return statHour;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("result_count")
    private Optional<Integer> _getResultCount() {
        return resultCount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("result_label_key")
    private Optional<ResultLabelKeys> _getResultLabelKey() {
        return resultLabelKey;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("result_label_override")
    private Optional<String> _getResultLabelOverride() {
        return resultLabelOverride;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("stat_hour")
    private Optional<Integer> _getStatHour() {
        return statHour;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AdReportBreakdownItemGranularityItem
                && equalTo((AdReportBreakdownItemGranularityItem) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AdReportBreakdownItemGranularityItem other) {
        return bucketStart.equals(other.bucketStart)
                && clicks == other.clicks
                && granularity.equals(other.granularity)
                && impressions == other.impressions
                && reach == other.reach
                && resultCount.equals(other.resultCount)
                && resultLabelKey.equals(other.resultLabelKey)
                && resultLabelOverride.equals(other.resultLabelOverride)
                && spend == other.spend
                && spendCurrency.equals(other.spendCurrency)
                && statDate.equals(other.statDate)
                && statHour.equals(other.statHour);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.bucketStart,
                this.clicks,
                this.granularity,
                this.impressions,
                this.reach,
                this.resultCount,
                this.resultLabelKey,
                this.resultLabelOverride,
                this.spend,
                this.spendCurrency,
                this.statDate,
                this.statHour);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BucketStartStage builder() {
        return new Builder();
    }

    public interface BucketStartStage {
        /**
         * <p>The bucket's start time as a real UTC instant. <code>(statDate, statHour)</code> resolved in the ad account's reporting timezone — render this in the viewer's local timezone.</p>
         */
        ClicksStage bucketStart(@NotNull OffsetDateTime bucketStart);

        Builder from(AdReportBreakdownItemGranularityItem other);
    }

    public interface ClicksStage {
        /**
         * <p>Clicks in this bucket.</p>
         */
        GranularityStage clicks(int clicks);
    }

    public interface GranularityStage {
        /**
         * <p>The bucket size of this row (<code>hourly</code>, <code>daily</code>, <code>weekly</code>, or <code>monthly</code>).</p>
         */
        ImpressionsStage granularity(@NotNull Granularities granularity);
    }

    public interface ImpressionsStage {
        /**
         * <p>Impressions in this bucket.</p>
         */
        ReachStage impressions(int impressions);
    }

    public interface ReachStage {
        /**
         * <p>Unique users reached in this bucket. Always <code>0</code> for hourly rows (Meta does not return reach at hourly grain).</p>
         */
        SpendStage reach(int reach);
    }

    public interface SpendStage {
        /**
         * <p>Charged spend in this bucket in the requested reporting currency — the amount billed including platform fees, not the platform-side net spend.</p>
         */
        SpendCurrencyStage spend(double spend);
    }

    public interface SpendCurrencyStage {
        /**
         * <p>Currency of the <code>spend</code> value.</p>
         */
        StatDateStage spendCurrency(@NotNull Currencies spendCurrency);
    }

    public interface StatDateStage {
        /**
         * <p>The date these stats cover (midnight UTC). For hourly rows, see <code>statHour</code> and <code>bucketStart</code>.</p>
         */
        _FinalStage statDate(@NotNull OffsetDateTime statDate);
    }

    public interface _FinalStage {
        AdReportBreakdownItemGranularityItem build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Count of the primary optimization result in this bucket.</p>
         */
        _FinalStage resultCount(Optional<Integer> resultCount);

        _FinalStage resultCount(Integer resultCount);

        _FinalStage resultCount(Nullable<Integer> resultCount);

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         */
        _FinalStage resultLabelKey(Optional<ResultLabelKeys> resultLabelKey);

        _FinalStage resultLabelKey(ResultLabelKeys resultLabelKey);

        _FinalStage resultLabelKey(Nullable<ResultLabelKeys> resultLabelKey);

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         */
        _FinalStage resultLabelOverride(Optional<String> resultLabelOverride);

        _FinalStage resultLabelOverride(String resultLabelOverride);

        _FinalStage resultLabelOverride(Nullable<String> resultLabelOverride);

        /**
         * <p>Hour of the day in the ad account's reporting timezone (0-23). <code>null</code> for daily rows.</p>
         */
        _FinalStage statHour(Optional<Integer> statHour);

        _FinalStage statHour(Integer statHour);

        _FinalStage statHour(Nullable<Integer> statHour);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements BucketStartStage,
                    ClicksStage,
                    GranularityStage,
                    ImpressionsStage,
                    ReachStage,
                    SpendStage,
                    SpendCurrencyStage,
                    StatDateStage,
                    _FinalStage {
        private OffsetDateTime bucketStart;

        private int clicks;

        private Granularities granularity;

        private int impressions;

        private int reach;

        private double spend;

        private Currencies spendCurrency;

        private OffsetDateTime statDate;

        private Optional<Integer> statHour = Optional.empty();

        private Optional<String> resultLabelOverride = Optional.empty();

        private Optional<ResultLabelKeys> resultLabelKey = Optional.empty();

        private Optional<Integer> resultCount = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AdReportBreakdownItemGranularityItem other) {
            bucketStart(other.getBucketStart());
            clicks(other.getClicks());
            granularity(other.getGranularity());
            impressions(other.getImpressions());
            reach(other.getReach());
            resultCount(other.getResultCount());
            resultLabelKey(other.getResultLabelKey());
            resultLabelOverride(other.getResultLabelOverride());
            spend(other.getSpend());
            spendCurrency(other.getSpendCurrency());
            statDate(other.getStatDate());
            statHour(other.getStatHour());
            return this;
        }

        /**
         * <p>The bucket's start time as a real UTC instant. <code>(statDate, statHour)</code> resolved in the ad account's reporting timezone — render this in the viewer's local timezone.</p>
         * <p>The bucket's start time as a real UTC instant. <code>(statDate, statHour)</code> resolved in the ad account's reporting timezone — render this in the viewer's local timezone.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("bucket_start")
        public ClicksStage bucketStart(@NotNull OffsetDateTime bucketStart) {
            this.bucketStart = Objects.requireNonNull(bucketStart, "bucketStart must not be null");
            return this;
        }

        /**
         * <p>Clicks in this bucket.</p>
         * <p>Clicks in this bucket.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("clicks")
        public GranularityStage clicks(int clicks) {
            this.clicks = clicks;
            return this;
        }

        /**
         * <p>The bucket size of this row (<code>hourly</code>, <code>daily</code>, <code>weekly</code>, or <code>monthly</code>).</p>
         * <p>The bucket size of this row (<code>hourly</code>, <code>daily</code>, <code>weekly</code>, or <code>monthly</code>).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("granularity")
        public ImpressionsStage granularity(@NotNull Granularities granularity) {
            this.granularity = Objects.requireNonNull(granularity, "granularity must not be null");
            return this;
        }

        /**
         * <p>Impressions in this bucket.</p>
         * <p>Impressions in this bucket.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("impressions")
        public ReachStage impressions(int impressions) {
            this.impressions = impressions;
            return this;
        }

        /**
         * <p>Unique users reached in this bucket. Always <code>0</code> for hourly rows (Meta does not return reach at hourly grain).</p>
         * <p>Unique users reached in this bucket. Always <code>0</code> for hourly rows (Meta does not return reach at hourly grain).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("reach")
        public SpendStage reach(int reach) {
            this.reach = reach;
            return this;
        }

        /**
         * <p>Charged spend in this bucket in the requested reporting currency — the amount billed including platform fees, not the platform-side net spend.</p>
         * <p>Charged spend in this bucket in the requested reporting currency — the amount billed including platform fees, not the platform-side net spend.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("spend")
        public SpendCurrencyStage spend(double spend) {
            this.spend = spend;
            return this;
        }

        /**
         * <p>Currency of the <code>spend</code> value.</p>
         * <p>Currency of the <code>spend</code> value.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("spend_currency")
        public StatDateStage spendCurrency(@NotNull Currencies spendCurrency) {
            this.spendCurrency = Objects.requireNonNull(spendCurrency, "spendCurrency must not be null");
            return this;
        }

        /**
         * <p>The date these stats cover (midnight UTC). For hourly rows, see <code>statHour</code> and <code>bucketStart</code>.</p>
         * <p>The date these stats cover (midnight UTC). For hourly rows, see <code>statHour</code> and <code>bucketStart</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("stat_date")
        public _FinalStage statDate(@NotNull OffsetDateTime statDate) {
            this.statDate = Objects.requireNonNull(statDate, "statDate must not be null");
            return this;
        }

        /**
         * <p>Hour of the day in the ad account's reporting timezone (0-23). <code>null</code> for daily rows.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage statHour(Nullable<Integer> statHour) {
            if (statHour.isNull()) {
                this.statHour = null;
            } else if (statHour.isEmpty()) {
                this.statHour = Optional.empty();
            } else {
                this.statHour = Optional.of(statHour.get());
            }
            return this;
        }

        /**
         * <p>Hour of the day in the ad account's reporting timezone (0-23). <code>null</code> for daily rows.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage statHour(Integer statHour) {
            this.statHour = Optional.ofNullable(statHour);
            return this;
        }

        /**
         * <p>Hour of the day in the ad account's reporting timezone (0-23). <code>null</code> for daily rows.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "stat_hour", nulls = Nulls.SKIP)
        public _FinalStage statHour(Optional<Integer> statHour) {
            this.statHour = statHour;
            return this;
        }

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelOverride(Nullable<String> resultLabelOverride) {
            if (resultLabelOverride.isNull()) {
                this.resultLabelOverride = null;
            } else if (resultLabelOverride.isEmpty()) {
                this.resultLabelOverride = Optional.empty();
            } else {
                this.resultLabelOverride = Optional.of(resultLabelOverride.get());
            }
            return this;
        }

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelOverride(String resultLabelOverride) {
            this.resultLabelOverride = Optional.ofNullable(resultLabelOverride);
            return this;
        }

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "result_label_override", nulls = Nulls.SKIP)
        public _FinalStage resultLabelOverride(Optional<String> resultLabelOverride) {
            this.resultLabelOverride = resultLabelOverride;
            return this;
        }

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelKey(Nullable<ResultLabelKeys> resultLabelKey) {
            if (resultLabelKey.isNull()) {
                this.resultLabelKey = null;
            } else if (resultLabelKey.isEmpty()) {
                this.resultLabelKey = Optional.empty();
            } else {
                this.resultLabelKey = Optional.of(resultLabelKey.get());
            }
            return this;
        }

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelKey(ResultLabelKeys resultLabelKey) {
            this.resultLabelKey = Optional.ofNullable(resultLabelKey);
            return this;
        }

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "result_label_key", nulls = Nulls.SKIP)
        public _FinalStage resultLabelKey(Optional<ResultLabelKeys> resultLabelKey) {
            this.resultLabelKey = resultLabelKey;
            return this;
        }

        /**
         * <p>Count of the primary optimization result in this bucket.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultCount(Nullable<Integer> resultCount) {
            if (resultCount.isNull()) {
                this.resultCount = null;
            } else if (resultCount.isEmpty()) {
                this.resultCount = Optional.empty();
            } else {
                this.resultCount = Optional.of(resultCount.get());
            }
            return this;
        }

        /**
         * <p>Count of the primary optimization result in this bucket.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultCount(Integer resultCount) {
            this.resultCount = Optional.ofNullable(resultCount);
            return this;
        }

        /**
         * <p>Count of the primary optimization result in this bucket.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "result_count", nulls = Nulls.SKIP)
        public _FinalStage resultCount(Optional<Integer> resultCount) {
            this.resultCount = resultCount;
            return this;
        }

        @java.lang.Override
        public AdReportBreakdownItemGranularityItem build() {
            return new AdReportBreakdownItemGranularityItem(
                    bucketStart,
                    clicks,
                    granularity,
                    impressions,
                    reach,
                    resultCount,
                    resultLabelKey,
                    resultLabelOverride,
                    spend,
                    spendCurrency,
                    statDate,
                    statHour,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
