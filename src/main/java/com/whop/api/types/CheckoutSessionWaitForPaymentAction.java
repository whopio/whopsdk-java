/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionWaitForPaymentAction.Builder.class)
public final class CheckoutSessionWaitForPaymentAction {
    private final boolean blocking;

    private final int pollAfterSeconds;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionWaitForPaymentAction(
            boolean blocking, int pollAfterSeconds, Map<String, Object> additionalProperties) {
        this.blocking = blocking;
        this.pollAfterSeconds = pollAfterSeconds;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.
     */
    @JsonProperty("blocking")
    public boolean getBlocking() {
        return blocking;
    }

    /**
     * @return How long to hold before reading the session again, in seconds. A hint for the next read's timing, not a budget: how long a surface is willing to keep the buyer waiting before saying &quot;we'll email you when it clears&quot; is the surface's own call.
     */
    @JsonProperty("poll_after_seconds")
    public int getPollAfterSeconds() {
        return pollAfterSeconds;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionWaitForPaymentAction
                && equalTo((CheckoutSessionWaitForPaymentAction) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionWaitForPaymentAction other) {
        return blocking == other.blocking && pollAfterSeconds == other.pollAfterSeconds;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.blocking, this.pollAfterSeconds);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BlockingStage builder() {
        return new Builder();
    }

    public interface BlockingStage {
        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         */
        PollAfterSecondsStage blocking(boolean blocking);

        Builder from(CheckoutSessionWaitForPaymentAction other);
    }

    public interface PollAfterSecondsStage {
        /**
         * <p>How long to hold before reading the session again, in seconds. A hint for the next read's timing, not a budget: how long a surface is willing to keep the buyer waiting before saying &quot;we'll email you when it clears&quot; is the surface's own call.</p>
         */
        _FinalStage pollAfterSeconds(int pollAfterSeconds);
    }

    public interface _FinalStage {
        CheckoutSessionWaitForPaymentAction build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BlockingStage, PollAfterSecondsStage, _FinalStage {
        private boolean blocking;

        private int pollAfterSeconds;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionWaitForPaymentAction other) {
            blocking(other.getBlocking());
            pollAfterSeconds(other.getPollAfterSeconds());
            return this;
        }

        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("blocking")
        public PollAfterSecondsStage blocking(boolean blocking) {
            this.blocking = blocking;
            return this;
        }

        /**
         * <p>How long to hold before reading the session again, in seconds. A hint for the next read's timing, not a budget: how long a surface is willing to keep the buyer waiting before saying &quot;we'll email you when it clears&quot; is the surface's own call.</p>
         * <p>How long to hold before reading the session again, in seconds. A hint for the next read's timing, not a budget: how long a surface is willing to keep the buyer waiting before saying &quot;we'll email you when it clears&quot; is the surface's own call.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("poll_after_seconds")
        public _FinalStage pollAfterSeconds(int pollAfterSeconds) {
            this.pollAfterSeconds = pollAfterSeconds;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionWaitForPaymentAction build() {
            return new CheckoutSessionWaitForPaymentAction(blocking, pollAfterSeconds, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
