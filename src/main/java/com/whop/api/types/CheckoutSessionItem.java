/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionItem.Builder.class)
public final class CheckoutSessionItem {
    private final Optional<String> description;

    private final Optional<Integer> expirationDays;

    private final Optional<String> headline;

    private final Optional<String> imageUrl;

    private final String name;

    private final String plan;

    private final int quantity;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionItem(
            Optional<String> description,
            Optional<Integer> expirationDays,
            Optional<String> headline,
            Optional<String> imageUrl,
            String name,
            String plan,
            int quantity,
            Map<String, Object> additionalProperties) {
        this.description = description;
        this.expirationDays = expirationDays;
        this.headline = headline;
        this.imageUrl = imageUrl;
        this.name = name;
        this.plan = plan;
        this.quantity = quantity;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The seller's longer description of the item, or <code>null</code>. Multi-line text as the seller wrote it, suited to a collapsible details block.
     */
    @JsonIgnore
    public Optional<String> getDescription() {
        if (description == null) {
            return Optional.empty();
        }
        return description;
    }

    /**
     * @return How many days of access a one-time purchase grants, or <code>null</code> when access does not expire (a renewing plan's access follows its billing instead).
     */
    @JsonIgnore
    public Optional<Integer> getExpirationDays() {
        if (expirationDays == null) {
            return Optional.empty();
        }
        return expirationDays;
    }

    /**
     * @return One line describing the item, or <code>null</code>. The checkout link's own description when the seller wrote one, otherwise the product's headline.
     */
    @JsonIgnore
    public Optional<String> getHeadline() {
        if (headline == null) {
            return Optional.empty();
        }
        return headline;
    }

    /**
     * @return The item's square art image URL, or <code>null</code> when the seller uploaded none.
     */
    @JsonIgnore
    public Optional<String> getImageUrl() {
        if (imageUrl == null) {
            return Optional.empty();
        }
        return imageUrl;
    }

    /**
     * @return What this item is called, ready to render — the product's title, falling back to the seller's name. Never empty.
     */
    @JsonProperty("name")
    public String getName() {
        return name;
    }

    /**
     * @return The plan being purchased (<code>plan_…</code>). The plan is the price — sessions never carry client-asserted amounts.
     */
    @JsonProperty("plan")
    public String getPlan() {
        return plan;
    }

    /**
     * @return How many of the plan the buyer is purchasing. At least 1.
     */
    @JsonProperty("quantity")
    public int getQuantity() {
        return quantity;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("description")
    private Optional<String> _getDescription() {
        return description;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("expiration_days")
    private Optional<Integer> _getExpirationDays() {
        return expirationDays;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("headline")
    private Optional<String> _getHeadline() {
        return headline;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("image_url")
    private Optional<String> _getImageUrl() {
        return imageUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionItem && equalTo((CheckoutSessionItem) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionItem other) {
        return description.equals(other.description)
                && expirationDays.equals(other.expirationDays)
                && headline.equals(other.headline)
                && imageUrl.equals(other.imageUrl)
                && name.equals(other.name)
                && plan.equals(other.plan)
                && quantity == other.quantity;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.description,
                this.expirationDays,
                this.headline,
                this.imageUrl,
                this.name,
                this.plan,
                this.quantity);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static NameStage builder() {
        return new Builder();
    }

    public interface NameStage {
        /**
         * <p>What this item is called, ready to render — the product's title, falling back to the seller's name. Never empty.</p>
         */
        PlanStage name(@NotNull String name);

        Builder from(CheckoutSessionItem other);
    }

    public interface PlanStage {
        /**
         * <p>The plan being purchased (<code>plan_…</code>). The plan is the price — sessions never carry client-asserted amounts.</p>
         */
        QuantityStage plan(@NotNull String plan);
    }

    public interface QuantityStage {
        /**
         * <p>How many of the plan the buyer is purchasing. At least 1.</p>
         */
        _FinalStage quantity(int quantity);
    }

    public interface _FinalStage {
        CheckoutSessionItem build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The seller's longer description of the item, or <code>null</code>. Multi-line text as the seller wrote it, suited to a collapsible details block.</p>
         */
        _FinalStage description(Optional<String> description);

        _FinalStage description(String description);

        _FinalStage description(Nullable<String> description);

        /**
         * <p>How many days of access a one-time purchase grants, or <code>null</code> when access does not expire (a renewing plan's access follows its billing instead).</p>
         */
        _FinalStage expirationDays(Optional<Integer> expirationDays);

        _FinalStage expirationDays(Integer expirationDays);

        _FinalStage expirationDays(Nullable<Integer> expirationDays);

        /**
         * <p>One line describing the item, or <code>null</code>. The checkout link's own description when the seller wrote one, otherwise the product's headline.</p>
         */
        _FinalStage headline(Optional<String> headline);

        _FinalStage headline(String headline);

        _FinalStage headline(Nullable<String> headline);

        /**
         * <p>The item's square art image URL, or <code>null</code> when the seller uploaded none.</p>
         */
        _FinalStage imageUrl(Optional<String> imageUrl);

        _FinalStage imageUrl(String imageUrl);

        _FinalStage imageUrl(Nullable<String> imageUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements NameStage, PlanStage, QuantityStage, _FinalStage {
        private String name;

        private String plan;

        private int quantity;

        private Optional<String> imageUrl = Optional.empty();

        private Optional<String> headline = Optional.empty();

        private Optional<Integer> expirationDays = Optional.empty();

        private Optional<String> description = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionItem other) {
            description(other.getDescription());
            expirationDays(other.getExpirationDays());
            headline(other.getHeadline());
            imageUrl(other.getImageUrl());
            name(other.getName());
            plan(other.getPlan());
            quantity(other.getQuantity());
            return this;
        }

        /**
         * <p>What this item is called, ready to render — the product's title, falling back to the seller's name. Never empty.</p>
         * <p>What this item is called, ready to render — the product's title, falling back to the seller's name. Never empty.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("name")
        public PlanStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>The plan being purchased (<code>plan_…</code>). The plan is the price — sessions never carry client-asserted amounts.</p>
         * <p>The plan being purchased (<code>plan_…</code>). The plan is the price — sessions never carry client-asserted amounts.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("plan")
        public QuantityStage plan(@NotNull String plan) {
            this.plan = Objects.requireNonNull(plan, "plan must not be null");
            return this;
        }

        /**
         * <p>How many of the plan the buyer is purchasing. At least 1.</p>
         * <p>How many of the plan the buyer is purchasing. At least 1.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("quantity")
        public _FinalStage quantity(int quantity) {
            this.quantity = quantity;
            return this;
        }

        /**
         * <p>The item's square art image URL, or <code>null</code> when the seller uploaded none.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage imageUrl(Nullable<String> imageUrl) {
            if (imageUrl.isNull()) {
                this.imageUrl = null;
            } else if (imageUrl.isEmpty()) {
                this.imageUrl = Optional.empty();
            } else {
                this.imageUrl = Optional.of(imageUrl.get());
            }
            return this;
        }

        /**
         * <p>The item's square art image URL, or <code>null</code> when the seller uploaded none.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage imageUrl(String imageUrl) {
            this.imageUrl = Optional.ofNullable(imageUrl);
            return this;
        }

        /**
         * <p>The item's square art image URL, or <code>null</code> when the seller uploaded none.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "image_url", nulls = Nulls.SKIP)
        public _FinalStage imageUrl(Optional<String> imageUrl) {
            this.imageUrl = imageUrl;
            return this;
        }

        /**
         * <p>One line describing the item, or <code>null</code>. The checkout link's own description when the seller wrote one, otherwise the product's headline.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage headline(Nullable<String> headline) {
            if (headline.isNull()) {
                this.headline = null;
            } else if (headline.isEmpty()) {
                this.headline = Optional.empty();
            } else {
                this.headline = Optional.of(headline.get());
            }
            return this;
        }

        /**
         * <p>One line describing the item, or <code>null</code>. The checkout link's own description when the seller wrote one, otherwise the product's headline.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage headline(String headline) {
            this.headline = Optional.ofNullable(headline);
            return this;
        }

        /**
         * <p>One line describing the item, or <code>null</code>. The checkout link's own description when the seller wrote one, otherwise the product's headline.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "headline", nulls = Nulls.SKIP)
        public _FinalStage headline(Optional<String> headline) {
            this.headline = headline;
            return this;
        }

        /**
         * <p>How many days of access a one-time purchase grants, or <code>null</code> when access does not expire (a renewing plan's access follows its billing instead).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expirationDays(Nullable<Integer> expirationDays) {
            if (expirationDays.isNull()) {
                this.expirationDays = null;
            } else if (expirationDays.isEmpty()) {
                this.expirationDays = Optional.empty();
            } else {
                this.expirationDays = Optional.of(expirationDays.get());
            }
            return this;
        }

        /**
         * <p>How many days of access a one-time purchase grants, or <code>null</code> when access does not expire (a renewing plan's access follows its billing instead).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expirationDays(Integer expirationDays) {
            this.expirationDays = Optional.ofNullable(expirationDays);
            return this;
        }

        /**
         * <p>How many days of access a one-time purchase grants, or <code>null</code> when access does not expire (a renewing plan's access follows its billing instead).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "expiration_days", nulls = Nulls.SKIP)
        public _FinalStage expirationDays(Optional<Integer> expirationDays) {
            this.expirationDays = expirationDays;
            return this;
        }

        /**
         * <p>The seller's longer description of the item, or <code>null</code>. Multi-line text as the seller wrote it, suited to a collapsible details block.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage description(Nullable<String> description) {
            if (description.isNull()) {
                this.description = null;
            } else if (description.isEmpty()) {
                this.description = Optional.empty();
            } else {
                this.description = Optional.of(description.get());
            }
            return this;
        }

        /**
         * <p>The seller's longer description of the item, or <code>null</code>. Multi-line text as the seller wrote it, suited to a collapsible details block.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage description(String description) {
            this.description = Optional.ofNullable(description);
            return this;
        }

        /**
         * <p>The seller's longer description of the item, or <code>null</code>. Multi-line text as the seller wrote it, suited to a collapsible details block.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "description", nulls = Nulls.SKIP)
        public _FinalStage description(Optional<String> description) {
            this.description = description;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionItem build() {
            return new CheckoutSessionItem(
                    description, expirationDays, headline, imageUrl, name, plan, quantity, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
