/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = LedgerActivity.Builder.class)
public final class LedgerActivity {
    private final Optional<LedgerActivityAccount> account;

    private final String amount;

    private final Optional<OffsetDateTime> availableAt;

    private final Optional<OffsetDateTime> createdAt;

    private final LedgerActivityCurrency currency;

    private final String id;

    private final Optional<String> ledgerAccountId;

    private final LedgerActivityLineType lineType;

    private final LedgerActivityObject object;

    private final Optional<LedgerActivityPayment> payment;

    private final OffsetDateTime postedAt;

    private final Optional<LedgerActivityResource> resource;

    private final Optional<LedgerActivitySource> source;

    private final Map<String, Object> additionalProperties;

    private LedgerActivity(
            Optional<LedgerActivityAccount> account,
            String amount,
            Optional<OffsetDateTime> availableAt,
            Optional<OffsetDateTime> createdAt,
            LedgerActivityCurrency currency,
            String id,
            Optional<String> ledgerAccountId,
            LedgerActivityLineType lineType,
            LedgerActivityObject object,
            Optional<LedgerActivityPayment> payment,
            OffsetDateTime postedAt,
            Optional<LedgerActivityResource> resource,
            Optional<LedgerActivitySource> source,
            Map<String, Object> additionalProperties) {
        this.account = account;
        this.amount = amount;
        this.availableAt = availableAt;
        this.createdAt = createdAt;
        this.currency = currency;
        this.id = id;
        this.ledgerAccountId = ledgerAccountId;
        this.lineType = lineType;
        this.object = object;
        this.payment = payment;
        this.postedAt = postedAt;
        this.resource = resource;
        this.source = source;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The viewer account that owns this row's ledger. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise.
     */
    @JsonProperty("account")
    public Optional<LedgerActivityAccount> getAccount() {
        return account;
    }

    /**
     * @return Signed amount in the currency's smallest precision units.
     */
    @JsonProperty("amount")
    public String getAmount() {
        return amount;
    }

    /**
     * @return ISO 8601 timestamp these funds became (or are scheduled to become) withdrawable: the posted time for already-settled funds, or 00:00:00 UTC on the scheduled release date for pending funds. Present only on inflows entering the balance (payments, top-ups, incoming transfers/affiliate); null on withdrawals, refunds, disputes and on-chain rows. The available_after/before filters window on its UTC settlement date.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getAvailableAt() {
        if (availableAt == null) {
            return Optional.empty();
        }
        return availableAt;
    }

    /**
     * @return When the activity record was created.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getCreatedAt() {
        if (createdAt == null) {
            return Optional.empty();
        }
        return createdAt;
    }

    /**
     * @return Currency for this ledger activity.
     */
    @JsonProperty("currency")
    public LedgerActivityCurrency getCurrency() {
        return currency;
    }

    /**
     * @return Ledger activity ID.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The ledger account (a ldgr_ identifier) this row belongs to. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise. Pair it with <code>account</code> to scope drawers and dashboard links to the owning business.
     */
    @JsonIgnore
    public Optional<String> getLedgerAccountId() {
        if (ledgerAccountId == null) {
            return Optional.empty();
        }
        return ledgerAccountId;
    }

    /**
     * @return The ledger line category this activity was posted under.
     */
    @JsonProperty("line_type")
    public LedgerActivityLineType getLineType() {
        return lineType;
    }

    @JsonProperty("object")
    public LedgerActivityObject getObject() {
        return object;
    }

    /**
     * @return Payment related to this ledger activity. Included when rich resource hydration is enabled and the movement is tied to a payment.
     */
    @JsonIgnore
    public Optional<LedgerActivityPayment> getPayment() {
        if (payment == null) {
            return Optional.empty();
        }
        return payment;
    }

    /**
     * @return When the activity posted to the ledger.
     */
    @JsonProperty("posted_at")
    public OffsetDateTime getPostedAt() {
        return postedAt;
    }

    /**
     * @return Resource associated with this ledger activity.
     */
    @JsonIgnore
    public Optional<LedgerActivityResource> getResource() {
        if (resource == null) {
            return Optional.empty();
        }
        return resource;
    }

    /**
     * @return Source of this ledger activity.
     */
    @JsonIgnore
    public Optional<LedgerActivitySource> getSource() {
        if (source == null) {
            return Optional.empty();
        }
        return source;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("available_at")
    private Optional<OffsetDateTime> _getAvailableAt() {
        return availableAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("created_at")
    private Optional<OffsetDateTime> _getCreatedAt() {
        return createdAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("ledger_account_id")
    private Optional<String> _getLedgerAccountId() {
        return ledgerAccountId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payment")
    private Optional<LedgerActivityPayment> _getPayment() {
        return payment;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("resource")
    private Optional<LedgerActivityResource> _getResource() {
        return resource;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("source")
    private Optional<LedgerActivitySource> _getSource() {
        return source;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof LedgerActivity && equalTo((LedgerActivity) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(LedgerActivity other) {
        return account.equals(other.account)
                && amount.equals(other.amount)
                && availableAt.equals(other.availableAt)
                && createdAt.equals(other.createdAt)
                && currency.equals(other.currency)
                && id.equals(other.id)
                && ledgerAccountId.equals(other.ledgerAccountId)
                && lineType.equals(other.lineType)
                && object.equals(other.object)
                && payment.equals(other.payment)
                && postedAt.equals(other.postedAt)
                && resource.equals(other.resource)
                && source.equals(other.source);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.account,
                this.amount,
                this.availableAt,
                this.createdAt,
                this.currency,
                this.id,
                this.ledgerAccountId,
                this.lineType,
                this.object,
                this.payment,
                this.postedAt,
                this.resource,
                this.source);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>Signed amount in the currency's smallest precision units.</p>
         */
        CurrencyStage amount(@NotNull String amount);

        Builder from(LedgerActivity other);
    }

    public interface CurrencyStage {
        /**
         * <p>Currency for this ledger activity.</p>
         */
        IdStage currency(@NotNull LedgerActivityCurrency currency);
    }

    public interface IdStage {
        /**
         * <p>Ledger activity ID.</p>
         */
        LineTypeStage id(@NotNull String id);
    }

    public interface LineTypeStage {
        /**
         * <p>The ledger line category this activity was posted under.</p>
         */
        ObjectStage lineType(@NotNull LedgerActivityLineType lineType);
    }

    public interface ObjectStage {
        PostedAtStage object(@NotNull LedgerActivityObject object);
    }

    public interface PostedAtStage {
        /**
         * <p>When the activity posted to the ledger.</p>
         */
        _FinalStage postedAt(@NotNull OffsetDateTime postedAt);
    }

    public interface _FinalStage {
        LedgerActivity build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The viewer account that owns this row's ledger. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise.</p>
         */
        _FinalStage account(Optional<LedgerActivityAccount> account);

        _FinalStage account(LedgerActivityAccount account);

        /**
         * <p>ISO 8601 timestamp these funds became (or are scheduled to become) withdrawable: the posted time for already-settled funds, or 00:00:00 UTC on the scheduled release date for pending funds. Present only on inflows entering the balance (payments, top-ups, incoming transfers/affiliate); null on withdrawals, refunds, disputes and on-chain rows. The available_after/before filters window on its UTC settlement date.</p>
         */
        _FinalStage availableAt(Optional<OffsetDateTime> availableAt);

        _FinalStage availableAt(OffsetDateTime availableAt);

        _FinalStage availableAt(Nullable<OffsetDateTime> availableAt);

        /**
         * <p>When the activity record was created.</p>
         */
        _FinalStage createdAt(Optional<OffsetDateTime> createdAt);

        _FinalStage createdAt(OffsetDateTime createdAt);

        _FinalStage createdAt(Nullable<OffsetDateTime> createdAt);

        /**
         * <p>The ledger account (a ldgr_ identifier) this row belongs to. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise. Pair it with <code>account</code> to scope drawers and dashboard links to the owning business.</p>
         */
        _FinalStage ledgerAccountId(Optional<String> ledgerAccountId);

        _FinalStage ledgerAccountId(String ledgerAccountId);

        _FinalStage ledgerAccountId(Nullable<String> ledgerAccountId);

        /**
         * <p>Payment related to this ledger activity. Included when rich resource hydration is enabled and the movement is tied to a payment.</p>
         */
        _FinalStage payment(Optional<LedgerActivityPayment> payment);

        _FinalStage payment(LedgerActivityPayment payment);

        _FinalStage payment(Nullable<LedgerActivityPayment> payment);

        /**
         * <p>Resource associated with this ledger activity.</p>
         */
        _FinalStage resource(Optional<LedgerActivityResource> resource);

        _FinalStage resource(LedgerActivityResource resource);

        _FinalStage resource(Nullable<LedgerActivityResource> resource);

        /**
         * <p>Source of this ledger activity.</p>
         */
        _FinalStage source(Optional<LedgerActivitySource> source);

        _FinalStage source(LedgerActivitySource source);

        _FinalStage source(Nullable<LedgerActivitySource> source);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AmountStage, CurrencyStage, IdStage, LineTypeStage, ObjectStage, PostedAtStage, _FinalStage {
        private String amount;

        private LedgerActivityCurrency currency;

        private String id;

        private LedgerActivityLineType lineType;

        private LedgerActivityObject object;

        private OffsetDateTime postedAt;

        private Optional<LedgerActivitySource> source = Optional.empty();

        private Optional<LedgerActivityResource> resource = Optional.empty();

        private Optional<LedgerActivityPayment> payment = Optional.empty();

        private Optional<String> ledgerAccountId = Optional.empty();

        private Optional<OffsetDateTime> createdAt = Optional.empty();

        private Optional<OffsetDateTime> availableAt = Optional.empty();

        private Optional<LedgerActivityAccount> account = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(LedgerActivity other) {
            account(other.getAccount());
            amount(other.getAmount());
            availableAt(other.getAvailableAt());
            createdAt(other.getCreatedAt());
            currency(other.getCurrency());
            id(other.getId());
            ledgerAccountId(other.getLedgerAccountId());
            lineType(other.getLineType());
            object(other.getObject());
            payment(other.getPayment());
            postedAt(other.getPostedAt());
            resource(other.getResource());
            source(other.getSource());
            return this;
        }

        /**
         * <p>Signed amount in the currency's smallest precision units.</p>
         * <p>Signed amount in the currency's smallest precision units.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public CurrencyStage amount(@NotNull String amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>Currency for this ledger activity.</p>
         * <p>Currency for this ledger activity.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public IdStage currency(@NotNull LedgerActivityCurrency currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>Ledger activity ID.</p>
         * <p>Ledger activity ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public LineTypeStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The ledger line category this activity was posted under.</p>
         * <p>The ledger line category this activity was posted under.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("line_type")
        public ObjectStage lineType(@NotNull LedgerActivityLineType lineType) {
            this.lineType = Objects.requireNonNull(lineType, "lineType must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("object")
        public PostedAtStage object(@NotNull LedgerActivityObject object) {
            this.object = Objects.requireNonNull(object, "object must not be null");
            return this;
        }

        /**
         * <p>When the activity posted to the ledger.</p>
         * <p>When the activity posted to the ledger.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("posted_at")
        public _FinalStage postedAt(@NotNull OffsetDateTime postedAt) {
            this.postedAt = Objects.requireNonNull(postedAt, "postedAt must not be null");
            return this;
        }

        /**
         * <p>Source of this ledger activity.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage source(Nullable<LedgerActivitySource> source) {
            if (source.isNull()) {
                this.source = null;
            } else if (source.isEmpty()) {
                this.source = Optional.empty();
            } else {
                this.source = Optional.of(source.get());
            }
            return this;
        }

        /**
         * <p>Source of this ledger activity.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage source(LedgerActivitySource source) {
            this.source = Optional.ofNullable(source);
            return this;
        }

        /**
         * <p>Source of this ledger activity.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "source", nulls = Nulls.SKIP)
        public _FinalStage source(Optional<LedgerActivitySource> source) {
            this.source = source;
            return this;
        }

        /**
         * <p>Resource associated with this ledger activity.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resource(Nullable<LedgerActivityResource> resource) {
            if (resource.isNull()) {
                this.resource = null;
            } else if (resource.isEmpty()) {
                this.resource = Optional.empty();
            } else {
                this.resource = Optional.of(resource.get());
            }
            return this;
        }

        /**
         * <p>Resource associated with this ledger activity.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resource(LedgerActivityResource resource) {
            this.resource = Optional.ofNullable(resource);
            return this;
        }

        /**
         * <p>Resource associated with this ledger activity.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "resource", nulls = Nulls.SKIP)
        public _FinalStage resource(Optional<LedgerActivityResource> resource) {
            this.resource = resource;
            return this;
        }

        /**
         * <p>Payment related to this ledger activity. Included when rich resource hydration is enabled and the movement is tied to a payment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payment(Nullable<LedgerActivityPayment> payment) {
            if (payment.isNull()) {
                this.payment = null;
            } else if (payment.isEmpty()) {
                this.payment = Optional.empty();
            } else {
                this.payment = Optional.of(payment.get());
            }
            return this;
        }

        /**
         * <p>Payment related to this ledger activity. Included when rich resource hydration is enabled and the movement is tied to a payment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payment(LedgerActivityPayment payment) {
            this.payment = Optional.ofNullable(payment);
            return this;
        }

        /**
         * <p>Payment related to this ledger activity. Included when rich resource hydration is enabled and the movement is tied to a payment.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payment", nulls = Nulls.SKIP)
        public _FinalStage payment(Optional<LedgerActivityPayment> payment) {
            this.payment = payment;
            return this;
        }

        /**
         * <p>The ledger account (a ldgr_ identifier) this row belongs to. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise. Pair it with <code>account</code> to scope drawers and dashboard links to the owning business.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage ledgerAccountId(Nullable<String> ledgerAccountId) {
            if (ledgerAccountId.isNull()) {
                this.ledgerAccountId = null;
            } else if (ledgerAccountId.isEmpty()) {
                this.ledgerAccountId = Optional.empty();
            } else {
                this.ledgerAccountId = Optional.of(ledgerAccountId.get());
            }
            return this;
        }

        /**
         * <p>The ledger account (a ldgr_ identifier) this row belongs to. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise. Pair it with <code>account</code> to scope drawers and dashboard links to the owning business.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage ledgerAccountId(String ledgerAccountId) {
            this.ledgerAccountId = Optional.ofNullable(ledgerAccountId);
            return this;
        }

        /**
         * <p>The ledger account (a ldgr_ identifier) this row belongs to. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise. Pair it with <code>account</code> to scope drawers and dashboard links to the owning business.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "ledger_account_id", nulls = Nulls.SKIP)
        public _FinalStage ledgerAccountId(Optional<String> ledgerAccountId) {
            this.ledgerAccountId = ledgerAccountId;
            return this;
        }

        /**
         * <p>When the activity record was created.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage createdAt(Nullable<OffsetDateTime> createdAt) {
            if (createdAt.isNull()) {
                this.createdAt = null;
            } else if (createdAt.isEmpty()) {
                this.createdAt = Optional.empty();
            } else {
                this.createdAt = Optional.of(createdAt.get());
            }
            return this;
        }

        /**
         * <p>When the activity record was created.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage createdAt(OffsetDateTime createdAt) {
            this.createdAt = Optional.ofNullable(createdAt);
            return this;
        }

        /**
         * <p>When the activity record was created.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "created_at", nulls = Nulls.SKIP)
        public _FinalStage createdAt(Optional<OffsetDateTime> createdAt) {
            this.createdAt = createdAt;
            return this;
        }

        /**
         * <p>ISO 8601 timestamp these funds became (or are scheduled to become) withdrawable: the posted time for already-settled funds, or 00:00:00 UTC on the scheduled release date for pending funds. Present only on inflows entering the balance (payments, top-ups, incoming transfers/affiliate); null on withdrawals, refunds, disputes and on-chain rows. The available_after/before filters window on its UTC settlement date.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage availableAt(Nullable<OffsetDateTime> availableAt) {
            if (availableAt.isNull()) {
                this.availableAt = null;
            } else if (availableAt.isEmpty()) {
                this.availableAt = Optional.empty();
            } else {
                this.availableAt = Optional.of(availableAt.get());
            }
            return this;
        }

        /**
         * <p>ISO 8601 timestamp these funds became (or are scheduled to become) withdrawable: the posted time for already-settled funds, or 00:00:00 UTC on the scheduled release date for pending funds. Present only on inflows entering the balance (payments, top-ups, incoming transfers/affiliate); null on withdrawals, refunds, disputes and on-chain rows. The available_after/before filters window on its UTC settlement date.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage availableAt(OffsetDateTime availableAt) {
            this.availableAt = Optional.ofNullable(availableAt);
            return this;
        }

        /**
         * <p>ISO 8601 timestamp these funds became (or are scheduled to become) withdrawable: the posted time for already-settled funds, or 00:00:00 UTC on the scheduled release date for pending funds. Present only on inflows entering the balance (payments, top-ups, incoming transfers/affiliate); null on withdrawals, refunds, disputes and on-chain rows. The available_after/before filters window on its UTC settlement date.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "available_at", nulls = Nulls.SKIP)
        public _FinalStage availableAt(Optional<OffsetDateTime> availableAt) {
            this.availableAt = availableAt;
            return this;
        }

        /**
         * <p>The viewer account that owns this row's ledger. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage account(LedgerActivityAccount account) {
            this.account = Optional.ofNullable(account);
            return this;
        }

        /**
         * <p>The viewer account that owns this row's ledger. Present only when the response aggregates owned accounts (include_owned_accounts=true); omitted otherwise.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "account", nulls = Nulls.SKIP)
        public _FinalStage account(Optional<LedgerActivityAccount> account) {
            this.account = account;
            return this;
        }

        @java.lang.Override
        public LedgerActivity build() {
            return new LedgerActivity(
                    account,
                    amount,
                    availableAt,
                    createdAt,
                    currency,
                    id,
                    ledgerAccountId,
                    lineType,
                    object,
                    payment,
                    postedAt,
                    resource,
                    source,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
