/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PaymentPlan.Builder.class)
public final class PaymentPlan {
    private final String id;

    private final Optional<String> internalNotes;

    private final Optional<Map<String, Object>> metadata;

    private final Map<String, Object> additionalProperties;

    private PaymentPlan(
            String id,
            Optional<String> internalNotes,
            Optional<Map<String, Object>> metadata,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.internalNotes = internalNotes;
        this.metadata = metadata;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The unique identifier for the plan.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return A personal description or notes section for the business.
     */
    @JsonIgnore
    public Optional<String> getInternalNotes() {
        if (internalNotes == null) {
            return Optional.empty();
        }
        return internalNotes;
    }

    /**
     * @return Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys <code>custom_cta</code> and <code>custom_cta_url</code>, when set, override the product's checkout call to action for this plan.
     */
    @JsonIgnore
    public Optional<Map<String, Object>> getMetadata() {
        if (metadata == null) {
            return Optional.empty();
        }
        return metadata;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("internal_notes")
    private Optional<String> _getInternalNotes() {
        return internalNotes;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("metadata")
    private Optional<Map<String, Object>> _getMetadata() {
        return metadata;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentPlan && equalTo((PaymentPlan) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PaymentPlan other) {
        return id.equals(other.id) && internalNotes.equals(other.internalNotes) && metadata.equals(other.metadata);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.internalNotes, this.metadata);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The unique identifier for the plan.</p>
         */
        _FinalStage id(@NotNull String id);

        Builder from(PaymentPlan other);
    }

    public interface _FinalStage {
        PaymentPlan build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>A personal description or notes section for the business.</p>
         */
        _FinalStage internalNotes(Optional<String> internalNotes);

        _FinalStage internalNotes(String internalNotes);

        _FinalStage internalNotes(Nullable<String> internalNotes);

        /**
         * <p>Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys <code>custom_cta</code> and <code>custom_cta_url</code>, when set, override the product's checkout call to action for this plan.</p>
         */
        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);

        _FinalStage metadata(Nullable<Map<String, Object>> metadata);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, _FinalStage {
        private String id;

        private Optional<Map<String, Object>> metadata = Optional.empty();

        private Optional<String> internalNotes = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PaymentPlan other) {
            id(other.getId());
            internalNotes(other.getInternalNotes());
            metadata(other.getMetadata());
            return this;
        }

        /**
         * <p>The unique identifier for the plan.</p>
         * <p>The unique identifier for the plan.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public _FinalStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys <code>custom_cta</code> and <code>custom_cta_url</code>, when set, override the product's checkout call to action for this plan.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Nullable<Map<String, Object>> metadata) {
            if (metadata.isNull()) {
                this.metadata = null;
            } else if (metadata.isEmpty()) {
                this.metadata = Optional.empty();
            } else {
                this.metadata = Optional.of(metadata.get());
            }
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys <code>custom_cta</code> and <code>custom_cta_url</code>, when set, override the product's checkout call to action for this plan.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        /**
         * <p>Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys <code>custom_cta</code> and <code>custom_cta_url</code>, when set, override the product's checkout call to action for this plan.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        /**
         * <p>A personal description or notes section for the business.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage internalNotes(Nullable<String> internalNotes) {
            if (internalNotes.isNull()) {
                this.internalNotes = null;
            } else if (internalNotes.isEmpty()) {
                this.internalNotes = Optional.empty();
            } else {
                this.internalNotes = Optional.of(internalNotes.get());
            }
            return this;
        }

        /**
         * <p>A personal description or notes section for the business.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage internalNotes(String internalNotes) {
            this.internalNotes = Optional.ofNullable(internalNotes);
            return this;
        }

        /**
         * <p>A personal description or notes section for the business.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "internal_notes", nulls = Nulls.SKIP)
        public _FinalStage internalNotes(Optional<String> internalNotes) {
            this.internalNotes = internalNotes;
            return this;
        }

        @java.lang.Override
        public PaymentPlan build() {
            return new PaymentPlan(id, internalNotes, metadata, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
