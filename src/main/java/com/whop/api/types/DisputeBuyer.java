/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = DisputeBuyer.Builder.class)
public final class DisputeBuyer {
    private final Optional<String> email;

    private final Optional<String> memberId;

    private final Optional<String> name;

    private final Optional<String> userId;

    private final Optional<String> username;

    private final Map<String, Object> additionalProperties;

    private DisputeBuyer(
            Optional<String> email,
            Optional<String> memberId,
            Optional<String> name,
            Optional<String> userId,
            Optional<String> username,
            Map<String, Object> additionalProperties) {
        this.email = email;
        this.memberId = memberId;
        this.name = name;
        this.userId = userId;
        this.username = username;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The customer's email address. Requires the <code>member:email:read</code> scope; <code>null</code> without it.
     */
    @JsonIgnore
    public Optional<String> getEmail() {
        if (email == null) {
            return Optional.empty();
        }
        return email;
    }

    /**
     * @return The customer's member row on the account, prefixed <code>mem_</code>.
     */
    @JsonIgnore
    public Optional<String> getMemberId() {
        if (memberId == null) {
            return Optional.empty();
        }
        return memberId;
    }

    /**
     * @return The customer's display name.
     */
    @JsonIgnore
    public Optional<String> getName() {
        if (name == null) {
            return Optional.empty();
        }
        return name;
    }

    /**
     * @return The customer's user ID, prefixed <code>user_</code>. <code>null</code> for a guest checkout.
     */
    @JsonIgnore
    public Optional<String> getUserId() {
        if (userId == null) {
            return Optional.empty();
        }
        return userId;
    }

    /**
     * @return The customer's Whop username.
     */
    @JsonIgnore
    public Optional<String> getUsername() {
        if (username == null) {
            return Optional.empty();
        }
        return username;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("email")
    private Optional<String> _getEmail() {
        return email;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("member_id")
    private Optional<String> _getMemberId() {
        return memberId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("name")
    private Optional<String> _getName() {
        return name;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("user_id")
    private Optional<String> _getUserId() {
        return userId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("username")
    private Optional<String> _getUsername() {
        return username;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof DisputeBuyer && equalTo((DisputeBuyer) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(DisputeBuyer other) {
        return email.equals(other.email)
                && memberId.equals(other.memberId)
                && name.equals(other.name)
                && userId.equals(other.userId)
                && username.equals(other.username);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.email, this.memberId, this.name, this.userId, this.username);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<String> email = Optional.empty();

        private Optional<String> memberId = Optional.empty();

        private Optional<String> name = Optional.empty();

        private Optional<String> userId = Optional.empty();

        private Optional<String> username = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(DisputeBuyer other) {
            email(other.getEmail());
            memberId(other.getMemberId());
            name(other.getName());
            userId(other.getUserId());
            username(other.getUsername());
            return this;
        }

        /**
         * <p>The customer's email address. Requires the <code>member:email:read</code> scope; <code>null</code> without it.</p>
         */
        @JsonSetter(value = "email", nulls = Nulls.SKIP)
        public Builder email(Optional<String> email) {
            this.email = email;
            return this;
        }

        public Builder email(String email) {
            this.email = Optional.ofNullable(email);
            return this;
        }

        public Builder email(Nullable<String> email) {
            if (email.isNull()) {
                this.email = null;
            } else if (email.isEmpty()) {
                this.email = Optional.empty();
            } else {
                this.email = Optional.of(email.get());
            }
            return this;
        }

        /**
         * <p>The customer's member row on the account, prefixed <code>mem_</code>.</p>
         */
        @JsonSetter(value = "member_id", nulls = Nulls.SKIP)
        public Builder memberId(Optional<String> memberId) {
            this.memberId = memberId;
            return this;
        }

        public Builder memberId(String memberId) {
            this.memberId = Optional.ofNullable(memberId);
            return this;
        }

        public Builder memberId(Nullable<String> memberId) {
            if (memberId.isNull()) {
                this.memberId = null;
            } else if (memberId.isEmpty()) {
                this.memberId = Optional.empty();
            } else {
                this.memberId = Optional.of(memberId.get());
            }
            return this;
        }

        /**
         * <p>The customer's display name.</p>
         */
        @JsonSetter(value = "name", nulls = Nulls.SKIP)
        public Builder name(Optional<String> name) {
            this.name = name;
            return this;
        }

        public Builder name(String name) {
            this.name = Optional.ofNullable(name);
            return this;
        }

        public Builder name(Nullable<String> name) {
            if (name.isNull()) {
                this.name = null;
            } else if (name.isEmpty()) {
                this.name = Optional.empty();
            } else {
                this.name = Optional.of(name.get());
            }
            return this;
        }

        /**
         * <p>The customer's user ID, prefixed <code>user_</code>. <code>null</code> for a guest checkout.</p>
         */
        @JsonSetter(value = "user_id", nulls = Nulls.SKIP)
        public Builder userId(Optional<String> userId) {
            this.userId = userId;
            return this;
        }

        public Builder userId(String userId) {
            this.userId = Optional.ofNullable(userId);
            return this;
        }

        public Builder userId(Nullable<String> userId) {
            if (userId.isNull()) {
                this.userId = null;
            } else if (userId.isEmpty()) {
                this.userId = Optional.empty();
            } else {
                this.userId = Optional.of(userId.get());
            }
            return this;
        }

        /**
         * <p>The customer's Whop username.</p>
         */
        @JsonSetter(value = "username", nulls = Nulls.SKIP)
        public Builder username(Optional<String> username) {
            this.username = username;
            return this;
        }

        public Builder username(String username) {
            this.username = Optional.ofNullable(username);
            return this;
        }

        public Builder username(Nullable<String> username) {
            if (username.isNull()) {
                this.username = null;
            } else if (username.isEmpty()) {
                this.username = Optional.empty();
            } else {
                this.username = Optional.of(username.get());
            }
            return this;
        }

        public DisputeBuyer build() {
            return new DisputeBuyer(email, memberId, name, userId, username, additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
