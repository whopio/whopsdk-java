/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class UserRecommendedActionStatus {
    public static final UserRecommendedActionStatus OPTIONAL =
            new UserRecommendedActionStatus(Value.OPTIONAL, "optional");

    private final Value value;

    private final String string;

    UserRecommendedActionStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof UserRecommendedActionStatus
                        && this.string.equals(((UserRecommendedActionStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case OPTIONAL:
                return visitor.visitOptional();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static UserRecommendedActionStatus valueOf(String value) {
        switch (value) {
            case "optional":
                return OPTIONAL;
            default:
                return new UserRecommendedActionStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        OPTIONAL,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitOptional();

        T visitUnknown(String unknownType);
    }
}
