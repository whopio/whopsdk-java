/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutConfigurationListItemPlan.Builder.class)
public final class CheckoutConfigurationListItemPlan {
    private final boolean adaptivePricingEnabled;

    private final Optional<Integer> billingPeriod;

    private final Currencies currency;

    private final Optional<Integer> expirationDays;

    private final String id;

    private final double initialPrice;

    private final PlanTypes planType;

    private final ReleaseMethod releaseMethod;

    private final double renewalPrice;

    private final Optional<PlanThreeDsLevels> threeDsLevel;

    private final Optional<Integer> trialPeriodDays;

    private final Visibility visibility;

    private final Map<String, Object> additionalProperties;

    private CheckoutConfigurationListItemPlan(
            boolean adaptivePricingEnabled,
            Optional<Integer> billingPeriod,
            Currencies currency,
            Optional<Integer> expirationDays,
            String id,
            double initialPrice,
            PlanTypes planType,
            ReleaseMethod releaseMethod,
            double renewalPrice,
            Optional<PlanThreeDsLevels> threeDsLevel,
            Optional<Integer> trialPeriodDays,
            Visibility visibility,
            Map<String, Object> additionalProperties) {
        this.adaptivePricingEnabled = adaptivePricingEnabled;
        this.billingPeriod = billingPeriod;
        this.currency = currency;
        this.expirationDays = expirationDays;
        this.id = id;
        this.initialPrice = initialPrice;
        this.planType = planType;
        this.releaseMethod = releaseMethod;
        this.renewalPrice = renewalPrice;
        this.threeDsLevel = threeDsLevel;
        this.trialPeriodDays = trialPeriodDays;
        this.visibility = visibility;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether the creator has turned on adaptive pricing for this plan. Raw setting — does not check processor compatibility or feature flags.
     */
    @JsonProperty("adaptive_pricing_enabled")
    public boolean getAdaptivePricingEnabled() {
        return adaptivePricingEnabled;
    }

    /**
     * @return Number of days between recurring charges, such as 30 for monthly or 365 for annual. <code>null</code> for one-time plans.
     */
    @JsonIgnore
    public Optional<Integer> getBillingPeriod() {
        if (billingPeriod == null) {
            return Optional.empty();
        }
        return billingPeriod;
    }

    /**
     * @return The currency used for all prices on this plan (e.g., 'usd', 'eur'). All monetary amounts on the plan are denominated in this currency.
     */
    @JsonProperty("currency")
    public Currencies getCurrency() {
        return currency;
    }

    /**
     * @return Access duration in days for expiration-based plans, such as 365 for a one-year pass.
     */
    @JsonIgnore
    public Optional<Integer> getExpirationDays() {
        if (expirationDays == null) {
            return Optional.empty();
        }
        return expirationDays;
    }

    /**
     * @return The unique identifier for the plan.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The initial purchase price in the plan's base_currency (e.g., 49.99 for $49.99). For one-time plans, this is the full price. For renewal plans, this is charged on top of the first renewal_price.
     */
    @JsonProperty("initial_price")
    public double getInitialPrice() {
        return initialPrice;
    }

    /**
     * @return The billing model for this plan: 'renewal' for recurring subscriptions or 'one_time' for single payments.
     */
    @JsonProperty("plan_type")
    public PlanTypes getPlanType() {
        return planType;
    }

    /**
     * @return Sales method for this plan: <code>buy_now</code> for immediate purchase or <code>waitlist</code> for waitlist-based access.
     */
    @JsonProperty("release_method")
    public ReleaseMethod getReleaseMethod() {
        return releaseMethod;
    }

    /**
     * @return The recurring price charged every billing_period in the plan's base_currency (e.g., 9.99 for $9.99/period). Zero for one-time plans.
     */
    @JsonProperty("renewal_price")
    public double getRenewalPrice() {
        return renewalPrice;
    }

    /**
     * @return The 3D Secure behavior for this plan. Null means the plan inherits the account default.
     */
    @JsonIgnore
    public Optional<PlanThreeDsLevels> getThreeDsLevel() {
        if (threeDsLevel == null) {
            return Optional.empty();
        }
        return threeDsLevel;
    }

    /**
     * @return Free trial days before first renewal charge. <code>null</code> if no trial is configured or the user has already used a trial for this plan.
     */
    @JsonIgnore
    public Optional<Integer> getTrialPeriodDays() {
        if (trialPeriodDays == null) {
            return Optional.empty();
        }
        return trialPeriodDays;
    }

    /**
     * @return Controls whether the plan is visible to customers. When set to 'hidden', the plan is only accessible via direct link.
     */
    @JsonProperty("visibility")
    public Visibility getVisibility() {
        return visibility;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("billing_period")
    private Optional<Integer> _getBillingPeriod() {
        return billingPeriod;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("expiration_days")
    private Optional<Integer> _getExpirationDays() {
        return expirationDays;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("three_ds_level")
    private Optional<PlanThreeDsLevels> _getThreeDsLevel() {
        return threeDsLevel;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("trial_period_days")
    private Optional<Integer> _getTrialPeriodDays() {
        return trialPeriodDays;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutConfigurationListItemPlan && equalTo((CheckoutConfigurationListItemPlan) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutConfigurationListItemPlan other) {
        return adaptivePricingEnabled == other.adaptivePricingEnabled
                && billingPeriod.equals(other.billingPeriod)
                && currency.equals(other.currency)
                && expirationDays.equals(other.expirationDays)
                && id.equals(other.id)
                && initialPrice == other.initialPrice
                && planType.equals(other.planType)
                && releaseMethod.equals(other.releaseMethod)
                && renewalPrice == other.renewalPrice
                && threeDsLevel.equals(other.threeDsLevel)
                && trialPeriodDays.equals(other.trialPeriodDays)
                && visibility.equals(other.visibility);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.adaptivePricingEnabled,
                this.billingPeriod,
                this.currency,
                this.expirationDays,
                this.id,
                this.initialPrice,
                this.planType,
                this.releaseMethod,
                this.renewalPrice,
                this.threeDsLevel,
                this.trialPeriodDays,
                this.visibility);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AdaptivePricingEnabledStage builder() {
        return new Builder();
    }

    public interface AdaptivePricingEnabledStage {
        /**
         * <p>Whether the creator has turned on adaptive pricing for this plan. Raw setting — does not check processor compatibility or feature flags.</p>
         */
        CurrencyStage adaptivePricingEnabled(boolean adaptivePricingEnabled);

        Builder from(CheckoutConfigurationListItemPlan other);
    }

    public interface CurrencyStage {
        /**
         * <p>The currency used for all prices on this plan (e.g., 'usd', 'eur'). All monetary amounts on the plan are denominated in this currency.</p>
         */
        IdStage currency(@NotNull Currencies currency);
    }

    public interface IdStage {
        /**
         * <p>The unique identifier for the plan.</p>
         */
        InitialPriceStage id(@NotNull String id);
    }

    public interface InitialPriceStage {
        /**
         * <p>The initial purchase price in the plan's base_currency (e.g., 49.99 for $49.99). For one-time plans, this is the full price. For renewal plans, this is charged on top of the first renewal_price.</p>
         */
        PlanTypeStage initialPrice(double initialPrice);
    }

    public interface PlanTypeStage {
        /**
         * <p>The billing model for this plan: 'renewal' for recurring subscriptions or 'one_time' for single payments.</p>
         */
        ReleaseMethodStage planType(@NotNull PlanTypes planType);
    }

    public interface ReleaseMethodStage {
        /**
         * <p>Sales method for this plan: <code>buy_now</code> for immediate purchase or <code>waitlist</code> for waitlist-based access.</p>
         */
        RenewalPriceStage releaseMethod(@NotNull ReleaseMethod releaseMethod);
    }

    public interface RenewalPriceStage {
        /**
         * <p>The recurring price charged every billing_period in the plan's base_currency (e.g., 9.99 for $9.99/period). Zero for one-time plans.</p>
         */
        VisibilityStage renewalPrice(double renewalPrice);
    }

    public interface VisibilityStage {
        /**
         * <p>Controls whether the plan is visible to customers. When set to 'hidden', the plan is only accessible via direct link.</p>
         */
        _FinalStage visibility(@NotNull Visibility visibility);
    }

    public interface _FinalStage {
        CheckoutConfigurationListItemPlan build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Number of days between recurring charges, such as 30 for monthly or 365 for annual. <code>null</code> for one-time plans.</p>
         */
        _FinalStage billingPeriod(Optional<Integer> billingPeriod);

        _FinalStage billingPeriod(Integer billingPeriod);

        _FinalStage billingPeriod(Nullable<Integer> billingPeriod);

        /**
         * <p>Access duration in days for expiration-based plans, such as 365 for a one-year pass.</p>
         */
        _FinalStage expirationDays(Optional<Integer> expirationDays);

        _FinalStage expirationDays(Integer expirationDays);

        _FinalStage expirationDays(Nullable<Integer> expirationDays);

        /**
         * <p>The 3D Secure behavior for this plan. Null means the plan inherits the account default.</p>
         */
        _FinalStage threeDsLevel(Optional<PlanThreeDsLevels> threeDsLevel);

        _FinalStage threeDsLevel(PlanThreeDsLevels threeDsLevel);

        _FinalStage threeDsLevel(Nullable<PlanThreeDsLevels> threeDsLevel);

        /**
         * <p>Free trial days before first renewal charge. <code>null</code> if no trial is configured or the user has already used a trial for this plan.</p>
         */
        _FinalStage trialPeriodDays(Optional<Integer> trialPeriodDays);

        _FinalStage trialPeriodDays(Integer trialPeriodDays);

        _FinalStage trialPeriodDays(Nullable<Integer> trialPeriodDays);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AdaptivePricingEnabledStage,
                    CurrencyStage,
                    IdStage,
                    InitialPriceStage,
                    PlanTypeStage,
                    ReleaseMethodStage,
                    RenewalPriceStage,
                    VisibilityStage,
                    _FinalStage {
        private boolean adaptivePricingEnabled;

        private Currencies currency;

        private String id;

        private double initialPrice;

        private PlanTypes planType;

        private ReleaseMethod releaseMethod;

        private double renewalPrice;

        private Visibility visibility;

        private Optional<Integer> trialPeriodDays = Optional.empty();

        private Optional<PlanThreeDsLevels> threeDsLevel = Optional.empty();

        private Optional<Integer> expirationDays = Optional.empty();

        private Optional<Integer> billingPeriod = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutConfigurationListItemPlan other) {
            adaptivePricingEnabled(other.getAdaptivePricingEnabled());
            billingPeriod(other.getBillingPeriod());
            currency(other.getCurrency());
            expirationDays(other.getExpirationDays());
            id(other.getId());
            initialPrice(other.getInitialPrice());
            planType(other.getPlanType());
            releaseMethod(other.getReleaseMethod());
            renewalPrice(other.getRenewalPrice());
            threeDsLevel(other.getThreeDsLevel());
            trialPeriodDays(other.getTrialPeriodDays());
            visibility(other.getVisibility());
            return this;
        }

        /**
         * <p>Whether the creator has turned on adaptive pricing for this plan. Raw setting — does not check processor compatibility or feature flags.</p>
         * <p>Whether the creator has turned on adaptive pricing for this plan. Raw setting — does not check processor compatibility or feature flags.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("adaptive_pricing_enabled")
        public CurrencyStage adaptivePricingEnabled(boolean adaptivePricingEnabled) {
            this.adaptivePricingEnabled = adaptivePricingEnabled;
            return this;
        }

        /**
         * <p>The currency used for all prices on this plan (e.g., 'usd', 'eur'). All monetary amounts on the plan are denominated in this currency.</p>
         * <p>The currency used for all prices on this plan (e.g., 'usd', 'eur'). All monetary amounts on the plan are denominated in this currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public IdStage currency(@NotNull Currencies currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>The unique identifier for the plan.</p>
         * <p>The unique identifier for the plan.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public InitialPriceStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The initial purchase price in the plan's base_currency (e.g., 49.99 for $49.99). For one-time plans, this is the full price. For renewal plans, this is charged on top of the first renewal_price.</p>
         * <p>The initial purchase price in the plan's base_currency (e.g., 49.99 for $49.99). For one-time plans, this is the full price. For renewal plans, this is charged on top of the first renewal_price.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("initial_price")
        public PlanTypeStage initialPrice(double initialPrice) {
            this.initialPrice = initialPrice;
            return this;
        }

        /**
         * <p>The billing model for this plan: 'renewal' for recurring subscriptions or 'one_time' for single payments.</p>
         * <p>The billing model for this plan: 'renewal' for recurring subscriptions or 'one_time' for single payments.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("plan_type")
        public ReleaseMethodStage planType(@NotNull PlanTypes planType) {
            this.planType = Objects.requireNonNull(planType, "planType must not be null");
            return this;
        }

        /**
         * <p>Sales method for this plan: <code>buy_now</code> for immediate purchase or <code>waitlist</code> for waitlist-based access.</p>
         * <p>Sales method for this plan: <code>buy_now</code> for immediate purchase or <code>waitlist</code> for waitlist-based access.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("release_method")
        public RenewalPriceStage releaseMethod(@NotNull ReleaseMethod releaseMethod) {
            this.releaseMethod = Objects.requireNonNull(releaseMethod, "releaseMethod must not be null");
            return this;
        }

        /**
         * <p>The recurring price charged every billing_period in the plan's base_currency (e.g., 9.99 for $9.99/period). Zero for one-time plans.</p>
         * <p>The recurring price charged every billing_period in the plan's base_currency (e.g., 9.99 for $9.99/period). Zero for one-time plans.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("renewal_price")
        public VisibilityStage renewalPrice(double renewalPrice) {
            this.renewalPrice = renewalPrice;
            return this;
        }

        /**
         * <p>Controls whether the plan is visible to customers. When set to 'hidden', the plan is only accessible via direct link.</p>
         * <p>Controls whether the plan is visible to customers. When set to 'hidden', the plan is only accessible via direct link.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("visibility")
        public _FinalStage visibility(@NotNull Visibility visibility) {
            this.visibility = Objects.requireNonNull(visibility, "visibility must not be null");
            return this;
        }

        /**
         * <p>Free trial days before first renewal charge. <code>null</code> if no trial is configured or the user has already used a trial for this plan.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage trialPeriodDays(Nullable<Integer> trialPeriodDays) {
            if (trialPeriodDays.isNull()) {
                this.trialPeriodDays = null;
            } else if (trialPeriodDays.isEmpty()) {
                this.trialPeriodDays = Optional.empty();
            } else {
                this.trialPeriodDays = Optional.of(trialPeriodDays.get());
            }
            return this;
        }

        /**
         * <p>Free trial days before first renewal charge. <code>null</code> if no trial is configured or the user has already used a trial for this plan.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage trialPeriodDays(Integer trialPeriodDays) {
            this.trialPeriodDays = Optional.ofNullable(trialPeriodDays);
            return this;
        }

        /**
         * <p>Free trial days before first renewal charge. <code>null</code> if no trial is configured or the user has already used a trial for this plan.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "trial_period_days", nulls = Nulls.SKIP)
        public _FinalStage trialPeriodDays(Optional<Integer> trialPeriodDays) {
            this.trialPeriodDays = trialPeriodDays;
            return this;
        }

        /**
         * <p>The 3D Secure behavior for this plan. Null means the plan inherits the account default.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage threeDsLevel(Nullable<PlanThreeDsLevels> threeDsLevel) {
            if (threeDsLevel.isNull()) {
                this.threeDsLevel = null;
            } else if (threeDsLevel.isEmpty()) {
                this.threeDsLevel = Optional.empty();
            } else {
                this.threeDsLevel = Optional.of(threeDsLevel.get());
            }
            return this;
        }

        /**
         * <p>The 3D Secure behavior for this plan. Null means the plan inherits the account default.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage threeDsLevel(PlanThreeDsLevels threeDsLevel) {
            this.threeDsLevel = Optional.ofNullable(threeDsLevel);
            return this;
        }

        /**
         * <p>The 3D Secure behavior for this plan. Null means the plan inherits the account default.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "three_ds_level", nulls = Nulls.SKIP)
        public _FinalStage threeDsLevel(Optional<PlanThreeDsLevels> threeDsLevel) {
            this.threeDsLevel = threeDsLevel;
            return this;
        }

        /**
         * <p>Access duration in days for expiration-based plans, such as 365 for a one-year pass.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expirationDays(Nullable<Integer> expirationDays) {
            if (expirationDays.isNull()) {
                this.expirationDays = null;
            } else if (expirationDays.isEmpty()) {
                this.expirationDays = Optional.empty();
            } else {
                this.expirationDays = Optional.of(expirationDays.get());
            }
            return this;
        }

        /**
         * <p>Access duration in days for expiration-based plans, such as 365 for a one-year pass.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expirationDays(Integer expirationDays) {
            this.expirationDays = Optional.ofNullable(expirationDays);
            return this;
        }

        /**
         * <p>Access duration in days for expiration-based plans, such as 365 for a one-year pass.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "expiration_days", nulls = Nulls.SKIP)
        public _FinalStage expirationDays(Optional<Integer> expirationDays) {
            this.expirationDays = expirationDays;
            return this;
        }

        /**
         * <p>Number of days between recurring charges, such as 30 for monthly or 365 for annual. <code>null</code> for one-time plans.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage billingPeriod(Nullable<Integer> billingPeriod) {
            if (billingPeriod.isNull()) {
                this.billingPeriod = null;
            } else if (billingPeriod.isEmpty()) {
                this.billingPeriod = Optional.empty();
            } else {
                this.billingPeriod = Optional.of(billingPeriod.get());
            }
            return this;
        }

        /**
         * <p>Number of days between recurring charges, such as 30 for monthly or 365 for annual. <code>null</code> for one-time plans.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage billingPeriod(Integer billingPeriod) {
            this.billingPeriod = Optional.ofNullable(billingPeriod);
            return this;
        }

        /**
         * <p>Number of days between recurring charges, such as 30 for monthly or 365 for annual. <code>null</code> for one-time plans.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "billing_period", nulls = Nulls.SKIP)
        public _FinalStage billingPeriod(Optional<Integer> billingPeriod) {
            this.billingPeriod = billingPeriod;
            return this;
        }

        @java.lang.Override
        public CheckoutConfigurationListItemPlan build() {
            return new CheckoutConfigurationListItemPlan(
                    adaptivePricingEnabled,
                    billingPeriod,
                    currency,
                    expirationDays,
                    id,
                    initialPrice,
                    planType,
                    releaseMethod,
                    renewalPrice,
                    threeDsLevel,
                    trialPeriodDays,
                    visibility,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
