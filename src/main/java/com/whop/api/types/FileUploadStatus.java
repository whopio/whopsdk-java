/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class FileUploadStatus {
    public static final FileUploadStatus FAILED = new FileUploadStatus(Value.FAILED, "failed");

    public static final FileUploadStatus PENDING = new FileUploadStatus(Value.PENDING, "pending");

    public static final FileUploadStatus PROCESSING = new FileUploadStatus(Value.PROCESSING, "processing");

    public static final FileUploadStatus READY = new FileUploadStatus(Value.READY, "ready");

    private final Value value;

    private final String string;

    FileUploadStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof FileUploadStatus && this.string.equals(((FileUploadStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FAILED:
                return visitor.visitFailed();
            case PENDING:
                return visitor.visitPending();
            case PROCESSING:
                return visitor.visitProcessing();
            case READY:
                return visitor.visitReady();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static FileUploadStatus valueOf(String value) {
        switch (value) {
            case "failed":
                return FAILED;
            case "pending":
                return PENDING;
            case "processing":
                return PROCESSING;
            case "ready":
                return READY;
            default:
                return new FileUploadStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        PROCESSING,

        READY,

        FAILED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitProcessing();

        T visitReady();

        T visitFailed();

        T visitUnknown(String unknownType);
    }
}
