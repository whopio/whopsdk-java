/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionPromo.Builder.class)
public final class CheckoutSessionPromo {
    private final double amountOff;

    private final String code;

    private final Optional<CheckoutSessionPromoCurrency> currency;

    private final CheckoutSessionPromoDuration duration;

    private final Optional<Integer> numberOfIntervals;

    private final CheckoutSessionPromoPromoType promoType;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionPromo(
            double amountOff,
            String code,
            Optional<CheckoutSessionPromoCurrency> currency,
            CheckoutSessionPromoDuration duration,
            Optional<Integer> numberOfIntervals,
            CheckoutSessionPromoPromoType promoType,
            Map<String, Object> additionalProperties) {
        this.amountOff = amountOff;
        this.code = code;
        this.currency = currency;
        this.duration = duration;
        this.numberOfIntervals = numberOfIntervals;
        this.promoType = promoType;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What each covered charge is discounted by. Percentage discounts are represented as a decimal fraction; fixed-amount discounts are in <code>currency</code>.
     */
    @JsonProperty("amount_off")
    public double getAmountOff() {
        return amountOff;
    }

    /**
     * @return The promo code, exactly as <code>promo_code</code> states it.
     */
    @JsonProperty("code")
    public String getCode() {
        return code;
    }

    /**
     * @return Currency of a fixed-amount discount; <code>null</code> for a percentage one.
     */
    @JsonIgnore
    public Optional<CheckoutSessionPromoCurrency> getCurrency() {
        if (currency == null) {
            return Optional.empty();
        }
        return currency;
    }

    /**
     * @return Which charges the discount covers: <code>forever</code> discounts every charge; <code>once</code> covers only the charge at purchase — the code is spent then even when it made that charge free, except on a free trial with nothing due today, where it holds until the trial's first real charge; <code>repeating</code> covers every charge landing within <code>number_of_intervals</code> calendar months of purchase.
     */
    @JsonProperty("duration")
    public CheckoutSessionPromoDuration getDuration() {
        return duration;
    }

    /**
     * @return How many calendar months of charges a <code>repeating</code> promo covers, counted from purchase — a renewal scheduled past that window bills full price however few charges came before it. <code>null</code> for the other durations.
     */
    @JsonIgnore
    public Optional<Integer> getNumberOfIntervals() {
        if (numberOfIntervals == null) {
            return Optional.empty();
        }
        return numberOfIntervals;
    }

    /**
     * @return Whether the discount is percentage-based or a fixed amount.
     */
    @JsonProperty("promo_type")
    public CheckoutSessionPromoPromoType getPromoType() {
        return promoType;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("currency")
    private Optional<CheckoutSessionPromoCurrency> _getCurrency() {
        return currency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("number_of_intervals")
    private Optional<Integer> _getNumberOfIntervals() {
        return numberOfIntervals;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionPromo && equalTo((CheckoutSessionPromo) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionPromo other) {
        return amountOff == other.amountOff
                && code.equals(other.code)
                && currency.equals(other.currency)
                && duration.equals(other.duration)
                && numberOfIntervals.equals(other.numberOfIntervals)
                && promoType.equals(other.promoType);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amountOff, this.code, this.currency, this.duration, this.numberOfIntervals, this.promoType);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountOffStage builder() {
        return new Builder();
    }

    public interface AmountOffStage {
        /**
         * <p>What each covered charge is discounted by. Percentage discounts are represented as a decimal fraction; fixed-amount discounts are in <code>currency</code>.</p>
         */
        CodeStage amountOff(double amountOff);

        Builder from(CheckoutSessionPromo other);
    }

    public interface CodeStage {
        /**
         * <p>The promo code, exactly as <code>promo_code</code> states it.</p>
         */
        DurationStage code(@NotNull String code);
    }

    public interface DurationStage {
        /**
         * <p>Which charges the discount covers: <code>forever</code> discounts every charge; <code>once</code> covers only the charge at purchase — the code is spent then even when it made that charge free, except on a free trial with nothing due today, where it holds until the trial's first real charge; <code>repeating</code> covers every charge landing within <code>number_of_intervals</code> calendar months of purchase.</p>
         */
        PromoTypeStage duration(@NotNull CheckoutSessionPromoDuration duration);
    }

    public interface PromoTypeStage {
        /**
         * <p>Whether the discount is percentage-based or a fixed amount.</p>
         */
        _FinalStage promoType(@NotNull CheckoutSessionPromoPromoType promoType);
    }

    public interface _FinalStage {
        CheckoutSessionPromo build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Currency of a fixed-amount discount; <code>null</code> for a percentage one.</p>
         */
        _FinalStage currency(Optional<CheckoutSessionPromoCurrency> currency);

        _FinalStage currency(CheckoutSessionPromoCurrency currency);

        _FinalStage currency(Nullable<CheckoutSessionPromoCurrency> currency);

        /**
         * <p>How many calendar months of charges a <code>repeating</code> promo covers, counted from purchase — a renewal scheduled past that window bills full price however few charges came before it. <code>null</code> for the other durations.</p>
         */
        _FinalStage numberOfIntervals(Optional<Integer> numberOfIntervals);

        _FinalStage numberOfIntervals(Integer numberOfIntervals);

        _FinalStage numberOfIntervals(Nullable<Integer> numberOfIntervals);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AmountOffStage, CodeStage, DurationStage, PromoTypeStage, _FinalStage {
        private double amountOff;

        private String code;

        private CheckoutSessionPromoDuration duration;

        private CheckoutSessionPromoPromoType promoType;

        private Optional<Integer> numberOfIntervals = Optional.empty();

        private Optional<CheckoutSessionPromoCurrency> currency = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionPromo other) {
            amountOff(other.getAmountOff());
            code(other.getCode());
            currency(other.getCurrency());
            duration(other.getDuration());
            numberOfIntervals(other.getNumberOfIntervals());
            promoType(other.getPromoType());
            return this;
        }

        /**
         * <p>What each covered charge is discounted by. Percentage discounts are represented as a decimal fraction; fixed-amount discounts are in <code>currency</code>.</p>
         * <p>What each covered charge is discounted by. Percentage discounts are represented as a decimal fraction; fixed-amount discounts are in <code>currency</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount_off")
        public CodeStage amountOff(double amountOff) {
            this.amountOff = amountOff;
            return this;
        }

        /**
         * <p>The promo code, exactly as <code>promo_code</code> states it.</p>
         * <p>The promo code, exactly as <code>promo_code</code> states it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("code")
        public DurationStage code(@NotNull String code) {
            this.code = Objects.requireNonNull(code, "code must not be null");
            return this;
        }

        /**
         * <p>Which charges the discount covers: <code>forever</code> discounts every charge; <code>once</code> covers only the charge at purchase — the code is spent then even when it made that charge free, except on a free trial with nothing due today, where it holds until the trial's first real charge; <code>repeating</code> covers every charge landing within <code>number_of_intervals</code> calendar months of purchase.</p>
         * <p>Which charges the discount covers: <code>forever</code> discounts every charge; <code>once</code> covers only the charge at purchase — the code is spent then even when it made that charge free, except on a free trial with nothing due today, where it holds until the trial's first real charge; <code>repeating</code> covers every charge landing within <code>number_of_intervals</code> calendar months of purchase.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("duration")
        public PromoTypeStage duration(@NotNull CheckoutSessionPromoDuration duration) {
            this.duration = Objects.requireNonNull(duration, "duration must not be null");
            return this;
        }

        /**
         * <p>Whether the discount is percentage-based or a fixed amount.</p>
         * <p>Whether the discount is percentage-based or a fixed amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("promo_type")
        public _FinalStage promoType(@NotNull CheckoutSessionPromoPromoType promoType) {
            this.promoType = Objects.requireNonNull(promoType, "promoType must not be null");
            return this;
        }

        /**
         * <p>How many calendar months of charges a <code>repeating</code> promo covers, counted from purchase — a renewal scheduled past that window bills full price however few charges came before it. <code>null</code> for the other durations.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage numberOfIntervals(Nullable<Integer> numberOfIntervals) {
            if (numberOfIntervals.isNull()) {
                this.numberOfIntervals = null;
            } else if (numberOfIntervals.isEmpty()) {
                this.numberOfIntervals = Optional.empty();
            } else {
                this.numberOfIntervals = Optional.of(numberOfIntervals.get());
            }
            return this;
        }

        /**
         * <p>How many calendar months of charges a <code>repeating</code> promo covers, counted from purchase — a renewal scheduled past that window bills full price however few charges came before it. <code>null</code> for the other durations.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage numberOfIntervals(Integer numberOfIntervals) {
            this.numberOfIntervals = Optional.ofNullable(numberOfIntervals);
            return this;
        }

        /**
         * <p>How many calendar months of charges a <code>repeating</code> promo covers, counted from purchase — a renewal scheduled past that window bills full price however few charges came before it. <code>null</code> for the other durations.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "number_of_intervals", nulls = Nulls.SKIP)
        public _FinalStage numberOfIntervals(Optional<Integer> numberOfIntervals) {
            this.numberOfIntervals = numberOfIntervals;
            return this;
        }

        /**
         * <p>Currency of a fixed-amount discount; <code>null</code> for a percentage one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(Nullable<CheckoutSessionPromoCurrency> currency) {
            if (currency.isNull()) {
                this.currency = null;
            } else if (currency.isEmpty()) {
                this.currency = Optional.empty();
            } else {
                this.currency = Optional.of(currency.get());
            }
            return this;
        }

        /**
         * <p>Currency of a fixed-amount discount; <code>null</code> for a percentage one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(CheckoutSessionPromoCurrency currency) {
            this.currency = Optional.ofNullable(currency);
            return this;
        }

        /**
         * <p>Currency of a fixed-amount discount; <code>null</code> for a percentage one.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "currency", nulls = Nulls.SKIP)
        public _FinalStage currency(Optional<CheckoutSessionPromoCurrency> currency) {
            this.currency = currency;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionPromo build() {
            return new CheckoutSessionPromo(
                    amountOff, code, currency, duration, numberOfIntervals, promoType, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
