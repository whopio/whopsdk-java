/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class PaymentMethodDomainStatus {
    public static final PaymentMethodDomainStatus PENDING = new PaymentMethodDomainStatus(Value.PENDING, "pending");

    public static final PaymentMethodDomainStatus VERIFIED = new PaymentMethodDomainStatus(Value.VERIFIED, "verified");

    private final Value value;

    private final String string;

    PaymentMethodDomainStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof PaymentMethodDomainStatus
                        && this.string.equals(((PaymentMethodDomainStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PENDING:
                return visitor.visitPending();
            case VERIFIED:
                return visitor.visitVerified();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static PaymentMethodDomainStatus valueOf(String value) {
        switch (value) {
            case "pending":
                return PENDING;
            case "verified":
                return VERIFIED;
            default:
                return new PaymentMethodDomainStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        VERIFIED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitVerified();

        T visitUnknown(String unknownType);
    }
}
