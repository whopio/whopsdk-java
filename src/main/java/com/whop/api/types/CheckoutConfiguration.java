/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutConfiguration.Builder.class)
public final class CheckoutConfiguration {
    private final String accountId;

    private final Optional<String> affiliateCode;

    private final String createdAt;

    private final Optional<CheckoutConfigurationCurrency> currency;

    private final Optional<CheckoutSessionPaymentMethodConfiguration> effectivePaymentMethodConfiguration;

    private final String id;

    private final Optional<Map<String, Object>> metadata;

    private final CheckoutConfigurationMode mode;

    private final Optional<Map<String, Object>> paymentMethodConfiguration;

    private final Optional<CheckoutConfigurationPlan> plan;

    private final Optional<String> purchaseUrl;

    private final Optional<String> redirectUrl;

    private final Optional<CheckoutConfigurationThreeDsLevel> threeDsLevel;

    private final String updatedAt;

    private final Map<String, Object> additionalProperties;

    private CheckoutConfiguration(
            String accountId,
            Optional<String> affiliateCode,
            String createdAt,
            Optional<CheckoutConfigurationCurrency> currency,
            Optional<CheckoutSessionPaymentMethodConfiguration> effectivePaymentMethodConfiguration,
            String id,
            Optional<Map<String, Object>> metadata,
            CheckoutConfigurationMode mode,
            Optional<Map<String, Object>> paymentMethodConfiguration,
            Optional<CheckoutConfigurationPlan> plan,
            Optional<String> purchaseUrl,
            Optional<String> redirectUrl,
            Optional<CheckoutConfigurationThreeDsLevel> threeDsLevel,
            String updatedAt,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.affiliateCode = affiliateCode;
        this.createdAt = createdAt;
        this.currency = currency;
        this.effectivePaymentMethodConfiguration = effectivePaymentMethodConfiguration;
        this.id = id;
        this.metadata = metadata;
        this.mode = mode;
        this.paymentMethodConfiguration = paymentMethodConfiguration;
        this.plan = plan;
        this.purchaseUrl = purchaseUrl;
        this.redirectUrl = redirectUrl;
        this.threeDsLevel = threeDsLevel;
        this.updatedAt = updatedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Account ID, prefixed <code>biz_</code>.
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return Affiliate code applied at checkout, or <code>null</code> when none is set.
     */
    @JsonIgnore
    public Optional<String> getAffiliateCode() {
        if (affiliateCode == null) {
            return Optional.empty();
        }
        return affiliateCode;
    }

    /**
     * @return When the checkout configuration was created, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Currency used for setup-mode payment method availability; defaults to <code>usd</code> when omitted.
     */
    @JsonIgnore
    public Optional<CheckoutConfigurationCurrency> getCurrency() {
        if (currency == null) {
            return Optional.empty();
        }
        return currency;
    }

    /**
     * @return The configuration governing a checkout mounted from this configuration, resolved through every layer (its own overrides, the plan's, and the account's) — the shape a session's <code>payment_method_configuration</code> carries. Apply it over the payment method types catalogue for the offerable set. <code>null</code> means platform defaults; <code>payment_method_configuration</code> stays this configuration's own editable override.
     */
    @JsonIgnore
    public Optional<CheckoutSessionPaymentMethodConfiguration> getEffectivePaymentMethodConfiguration() {
        if (effectivePaymentMethodConfiguration == null) {
            return Optional.empty();
        }
        return effectivePaymentMethodConfiguration;
    }

    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Custom key-value metadata copied to payments and memberships. <code>null</code> without the <code>checkout_configuration:basic:read</code> scope.
     */
    @JsonIgnore
    public Optional<Map<String, Object>> getMetadata() {
        if (metadata == null) {
            return Optional.empty();
        }
        return metadata;
    }

    /**
     * @return Controls whether checkout charges the buyer immediately or saves payment details for later.
     */
    @JsonProperty("mode")
    public CheckoutConfigurationMode getMode() {
        return mode;
    }

    /**
     * @return Payment method overrides for this checkout. <code>null</code> when it uses the plan or platform defaults.
     */
    @JsonIgnore
    public Optional<Map<String, Object>> getPaymentMethodConfiguration() {
        if (paymentMethodConfiguration == null) {
            return Optional.empty();
        }
        return paymentMethodConfiguration;
    }

    /**
     * @return Plan used for payment checkout. <code>null</code> in setup mode.
     */
    @JsonIgnore
    public Optional<CheckoutConfigurationPlan> getPlan() {
        if (plan == null) {
            return Optional.empty();
        }
        return plan;
    }

    /**
     * @return Checkout URL you can send to customers.
     */
    @JsonIgnore
    public Optional<String> getPurchaseUrl() {
        if (purchaseUrl == null) {
            return Optional.empty();
        }
        return purchaseUrl;
    }

    /**
     * @return URL customers are sent to after checkout, or <code>null</code> when no redirect is configured.
     */
    @JsonIgnore
    public Optional<String> getRedirectUrl() {
        if (redirectUrl == null) {
            return Optional.empty();
        }
        return redirectUrl;
    }

    /**
     * @return 3D Secure behavior for this checkout, or <code>null</code> to use the account default.
     */
    @JsonIgnore
    public Optional<CheckoutConfigurationThreeDsLevel> getThreeDsLevel() {
        if (threeDsLevel == null) {
            return Optional.empty();
        }
        return threeDsLevel;
    }

    /**
     * @return When the checkout configuration was last updated, as an ISO 8601 timestamp.
     */
    @JsonProperty("updated_at")
    public String getUpdatedAt() {
        return updatedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("affiliate_code")
    private Optional<String> _getAffiliateCode() {
        return affiliateCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("currency")
    private Optional<CheckoutConfigurationCurrency> _getCurrency() {
        return currency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("effective_payment_method_configuration")
    private Optional<CheckoutSessionPaymentMethodConfiguration> _getEffectivePaymentMethodConfiguration() {
        return effectivePaymentMethodConfiguration;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("metadata")
    private Optional<Map<String, Object>> _getMetadata() {
        return metadata;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payment_method_configuration")
    private Optional<Map<String, Object>> _getPaymentMethodConfiguration() {
        return paymentMethodConfiguration;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("plan")
    private Optional<CheckoutConfigurationPlan> _getPlan() {
        return plan;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("purchase_url")
    private Optional<String> _getPurchaseUrl() {
        return purchaseUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("redirect_url")
    private Optional<String> _getRedirectUrl() {
        return redirectUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("three_ds_level")
    private Optional<CheckoutConfigurationThreeDsLevel> _getThreeDsLevel() {
        return threeDsLevel;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutConfiguration && equalTo((CheckoutConfiguration) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutConfiguration other) {
        return accountId.equals(other.accountId)
                && affiliateCode.equals(other.affiliateCode)
                && createdAt.equals(other.createdAt)
                && currency.equals(other.currency)
                && effectivePaymentMethodConfiguration.equals(other.effectivePaymentMethodConfiguration)
                && id.equals(other.id)
                && metadata.equals(other.metadata)
                && mode.equals(other.mode)
                && paymentMethodConfiguration.equals(other.paymentMethodConfiguration)
                && plan.equals(other.plan)
                && purchaseUrl.equals(other.purchaseUrl)
                && redirectUrl.equals(other.redirectUrl)
                && threeDsLevel.equals(other.threeDsLevel)
                && updatedAt.equals(other.updatedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.affiliateCode,
                this.createdAt,
                this.currency,
                this.effectivePaymentMethodConfiguration,
                this.id,
                this.metadata,
                this.mode,
                this.paymentMethodConfiguration,
                this.plan,
                this.purchaseUrl,
                this.redirectUrl,
                this.threeDsLevel,
                this.updatedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountIdStage builder() {
        return new Builder();
    }

    public interface AccountIdStage {
        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         */
        CreatedAtStage accountId(@NotNull String accountId);

        Builder from(CheckoutConfiguration other);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the checkout configuration was created, as an ISO 8601 timestamp.</p>
         */
        IdStage createdAt(@NotNull String createdAt);
    }

    public interface IdStage {
        ModeStage id(@NotNull String id);
    }

    public interface ModeStage {
        /**
         * <p>Controls whether checkout charges the buyer immediately or saves payment details for later.</p>
         */
        UpdatedAtStage mode(@NotNull CheckoutConfigurationMode mode);
    }

    public interface UpdatedAtStage {
        /**
         * <p>When the checkout configuration was last updated, as an ISO 8601 timestamp.</p>
         */
        _FinalStage updatedAt(@NotNull String updatedAt);
    }

    public interface _FinalStage {
        CheckoutConfiguration build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Affiliate code applied at checkout, or <code>null</code> when none is set.</p>
         */
        _FinalStage affiliateCode(Optional<String> affiliateCode);

        _FinalStage affiliateCode(String affiliateCode);

        _FinalStage affiliateCode(Nullable<String> affiliateCode);

        /**
         * <p>Currency used for setup-mode payment method availability; defaults to <code>usd</code> when omitted.</p>
         */
        _FinalStage currency(Optional<CheckoutConfigurationCurrency> currency);

        _FinalStage currency(CheckoutConfigurationCurrency currency);

        _FinalStage currency(Nullable<CheckoutConfigurationCurrency> currency);

        /**
         * <p>The configuration governing a checkout mounted from this configuration, resolved through every layer (its own overrides, the plan's, and the account's) — the shape a session's <code>payment_method_configuration</code> carries. Apply it over the payment method types catalogue for the offerable set. <code>null</code> means platform defaults; <code>payment_method_configuration</code> stays this configuration's own editable override.</p>
         */
        _FinalStage effectivePaymentMethodConfiguration(
                Optional<CheckoutSessionPaymentMethodConfiguration> effectivePaymentMethodConfiguration);

        _FinalStage effectivePaymentMethodConfiguration(
                CheckoutSessionPaymentMethodConfiguration effectivePaymentMethodConfiguration);

        _FinalStage effectivePaymentMethodConfiguration(
                Nullable<CheckoutSessionPaymentMethodConfiguration> effectivePaymentMethodConfiguration);

        /**
         * <p>Custom key-value metadata copied to payments and memberships. <code>null</code> without the <code>checkout_configuration:basic:read</code> scope.</p>
         */
        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);

        _FinalStage metadata(Nullable<Map<String, Object>> metadata);

        /**
         * <p>Payment method overrides for this checkout. <code>null</code> when it uses the plan or platform defaults.</p>
         */
        _FinalStage paymentMethodConfiguration(Optional<Map<String, Object>> paymentMethodConfiguration);

        _FinalStage paymentMethodConfiguration(Map<String, Object> paymentMethodConfiguration);

        _FinalStage paymentMethodConfiguration(Nullable<Map<String, Object>> paymentMethodConfiguration);

        /**
         * <p>Plan used for payment checkout. <code>null</code> in setup mode.</p>
         */
        _FinalStage plan(Optional<CheckoutConfigurationPlan> plan);

        _FinalStage plan(CheckoutConfigurationPlan plan);

        _FinalStage plan(Nullable<CheckoutConfigurationPlan> plan);

        /**
         * <p>Checkout URL you can send to customers.</p>
         */
        _FinalStage purchaseUrl(Optional<String> purchaseUrl);

        _FinalStage purchaseUrl(String purchaseUrl);

        _FinalStage purchaseUrl(Nullable<String> purchaseUrl);

        /**
         * <p>URL customers are sent to after checkout, or <code>null</code> when no redirect is configured.</p>
         */
        _FinalStage redirectUrl(Optional<String> redirectUrl);

        _FinalStage redirectUrl(String redirectUrl);

        _FinalStage redirectUrl(Nullable<String> redirectUrl);

        /**
         * <p>3D Secure behavior for this checkout, or <code>null</code> to use the account default.</p>
         */
        _FinalStage threeDsLevel(Optional<CheckoutConfigurationThreeDsLevel> threeDsLevel);

        _FinalStage threeDsLevel(CheckoutConfigurationThreeDsLevel threeDsLevel);

        _FinalStage threeDsLevel(Nullable<CheckoutConfigurationThreeDsLevel> threeDsLevel);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AccountIdStage, CreatedAtStage, IdStage, ModeStage, UpdatedAtStage, _FinalStage {
        private String accountId;

        private String createdAt;

        private String id;

        private CheckoutConfigurationMode mode;

        private String updatedAt;

        private Optional<CheckoutConfigurationThreeDsLevel> threeDsLevel = Optional.empty();

        private Optional<String> redirectUrl = Optional.empty();

        private Optional<String> purchaseUrl = Optional.empty();

        private Optional<CheckoutConfigurationPlan> plan = Optional.empty();

        private Optional<Map<String, Object>> paymentMethodConfiguration = Optional.empty();

        private Optional<Map<String, Object>> metadata = Optional.empty();

        private Optional<CheckoutSessionPaymentMethodConfiguration> effectivePaymentMethodConfiguration =
                Optional.empty();

        private Optional<CheckoutConfigurationCurrency> currency = Optional.empty();

        private Optional<String> affiliateCode = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutConfiguration other) {
            accountId(other.getAccountId());
            affiliateCode(other.getAffiliateCode());
            createdAt(other.getCreatedAt());
            currency(other.getCurrency());
            effectivePaymentMethodConfiguration(other.getEffectivePaymentMethodConfiguration());
            id(other.getId());
            metadata(other.getMetadata());
            mode(other.getMode());
            paymentMethodConfiguration(other.getPaymentMethodConfiguration());
            plan(other.getPlan());
            purchaseUrl(other.getPurchaseUrl());
            redirectUrl(other.getRedirectUrl());
            threeDsLevel(other.getThreeDsLevel());
            updatedAt(other.getUpdatedAt());
            return this;
        }

        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public CreatedAtStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        /**
         * <p>When the checkout configuration was created, as an ISO 8601 timestamp.</p>
         * <p>When the checkout configuration was created, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public IdStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("id")
        public ModeStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Controls whether checkout charges the buyer immediately or saves payment details for later.</p>
         * <p>Controls whether checkout charges the buyer immediately or saves payment details for later.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("mode")
        public UpdatedAtStage mode(@NotNull CheckoutConfigurationMode mode) {
            this.mode = Objects.requireNonNull(mode, "mode must not be null");
            return this;
        }

        /**
         * <p>When the checkout configuration was last updated, as an ISO 8601 timestamp.</p>
         * <p>When the checkout configuration was last updated, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("updated_at")
        public _FinalStage updatedAt(@NotNull String updatedAt) {
            this.updatedAt = Objects.requireNonNull(updatedAt, "updatedAt must not be null");
            return this;
        }

        /**
         * <p>3D Secure behavior for this checkout, or <code>null</code> to use the account default.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage threeDsLevel(Nullable<CheckoutConfigurationThreeDsLevel> threeDsLevel) {
            if (threeDsLevel.isNull()) {
                this.threeDsLevel = null;
            } else if (threeDsLevel.isEmpty()) {
                this.threeDsLevel = Optional.empty();
            } else {
                this.threeDsLevel = Optional.of(threeDsLevel.get());
            }
            return this;
        }

        /**
         * <p>3D Secure behavior for this checkout, or <code>null</code> to use the account default.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage threeDsLevel(CheckoutConfigurationThreeDsLevel threeDsLevel) {
            this.threeDsLevel = Optional.ofNullable(threeDsLevel);
            return this;
        }

        /**
         * <p>3D Secure behavior for this checkout, or <code>null</code> to use the account default.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "three_ds_level", nulls = Nulls.SKIP)
        public _FinalStage threeDsLevel(Optional<CheckoutConfigurationThreeDsLevel> threeDsLevel) {
            this.threeDsLevel = threeDsLevel;
            return this;
        }

        /**
         * <p>URL customers are sent to after checkout, or <code>null</code> when no redirect is configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage redirectUrl(Nullable<String> redirectUrl) {
            if (redirectUrl.isNull()) {
                this.redirectUrl = null;
            } else if (redirectUrl.isEmpty()) {
                this.redirectUrl = Optional.empty();
            } else {
                this.redirectUrl = Optional.of(redirectUrl.get());
            }
            return this;
        }

        /**
         * <p>URL customers are sent to after checkout, or <code>null</code> when no redirect is configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage redirectUrl(String redirectUrl) {
            this.redirectUrl = Optional.ofNullable(redirectUrl);
            return this;
        }

        /**
         * <p>URL customers are sent to after checkout, or <code>null</code> when no redirect is configured.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "redirect_url", nulls = Nulls.SKIP)
        public _FinalStage redirectUrl(Optional<String> redirectUrl) {
            this.redirectUrl = redirectUrl;
            return this;
        }

        /**
         * <p>Checkout URL you can send to customers.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage purchaseUrl(Nullable<String> purchaseUrl) {
            if (purchaseUrl.isNull()) {
                this.purchaseUrl = null;
            } else if (purchaseUrl.isEmpty()) {
                this.purchaseUrl = Optional.empty();
            } else {
                this.purchaseUrl = Optional.of(purchaseUrl.get());
            }
            return this;
        }

        /**
         * <p>Checkout URL you can send to customers.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage purchaseUrl(String purchaseUrl) {
            this.purchaseUrl = Optional.ofNullable(purchaseUrl);
            return this;
        }

        /**
         * <p>Checkout URL you can send to customers.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "purchase_url", nulls = Nulls.SKIP)
        public _FinalStage purchaseUrl(Optional<String> purchaseUrl) {
            this.purchaseUrl = purchaseUrl;
            return this;
        }

        /**
         * <p>Plan used for payment checkout. <code>null</code> in setup mode.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage plan(Nullable<CheckoutConfigurationPlan> plan) {
            if (plan.isNull()) {
                this.plan = null;
            } else if (plan.isEmpty()) {
                this.plan = Optional.empty();
            } else {
                this.plan = Optional.of(plan.get());
            }
            return this;
        }

        /**
         * <p>Plan used for payment checkout. <code>null</code> in setup mode.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage plan(CheckoutConfigurationPlan plan) {
            this.plan = Optional.ofNullable(plan);
            return this;
        }

        /**
         * <p>Plan used for payment checkout. <code>null</code> in setup mode.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "plan", nulls = Nulls.SKIP)
        public _FinalStage plan(Optional<CheckoutConfigurationPlan> plan) {
            this.plan = plan;
            return this;
        }

        /**
         * <p>Payment method overrides for this checkout. <code>null</code> when it uses the plan or platform defaults.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage paymentMethodConfiguration(Nullable<Map<String, Object>> paymentMethodConfiguration) {
            if (paymentMethodConfiguration.isNull()) {
                this.paymentMethodConfiguration = null;
            } else if (paymentMethodConfiguration.isEmpty()) {
                this.paymentMethodConfiguration = Optional.empty();
            } else {
                this.paymentMethodConfiguration = Optional.of(paymentMethodConfiguration.get());
            }
            return this;
        }

        /**
         * <p>Payment method overrides for this checkout. <code>null</code> when it uses the plan or platform defaults.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage paymentMethodConfiguration(Map<String, Object> paymentMethodConfiguration) {
            this.paymentMethodConfiguration = Optional.ofNullable(paymentMethodConfiguration);
            return this;
        }

        /**
         * <p>Payment method overrides for this checkout. <code>null</code> when it uses the plan or platform defaults.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payment_method_configuration", nulls = Nulls.SKIP)
        public _FinalStage paymentMethodConfiguration(Optional<Map<String, Object>> paymentMethodConfiguration) {
            this.paymentMethodConfiguration = paymentMethodConfiguration;
            return this;
        }

        /**
         * <p>Custom key-value metadata copied to payments and memberships. <code>null</code> without the <code>checkout_configuration:basic:read</code> scope.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Nullable<Map<String, Object>> metadata) {
            if (metadata.isNull()) {
                this.metadata = null;
            } else if (metadata.isEmpty()) {
                this.metadata = Optional.empty();
            } else {
                this.metadata = Optional.of(metadata.get());
            }
            return this;
        }

        /**
         * <p>Custom key-value metadata copied to payments and memberships. <code>null</code> without the <code>checkout_configuration:basic:read</code> scope.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        /**
         * <p>Custom key-value metadata copied to payments and memberships. <code>null</code> without the <code>checkout_configuration:basic:read</code> scope.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        /**
         * <p>The configuration governing a checkout mounted from this configuration, resolved through every layer (its own overrides, the plan's, and the account's) — the shape a session's <code>payment_method_configuration</code> carries. Apply it over the payment method types catalogue for the offerable set. <code>null</code> means platform defaults; <code>payment_method_configuration</code> stays this configuration's own editable override.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage effectivePaymentMethodConfiguration(
                Nullable<CheckoutSessionPaymentMethodConfiguration> effectivePaymentMethodConfiguration) {
            if (effectivePaymentMethodConfiguration.isNull()) {
                this.effectivePaymentMethodConfiguration = null;
            } else if (effectivePaymentMethodConfiguration.isEmpty()) {
                this.effectivePaymentMethodConfiguration = Optional.empty();
            } else {
                this.effectivePaymentMethodConfiguration = Optional.of(effectivePaymentMethodConfiguration.get());
            }
            return this;
        }

        /**
         * <p>The configuration governing a checkout mounted from this configuration, resolved through every layer (its own overrides, the plan's, and the account's) — the shape a session's <code>payment_method_configuration</code> carries. Apply it over the payment method types catalogue for the offerable set. <code>null</code> means platform defaults; <code>payment_method_configuration</code> stays this configuration's own editable override.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage effectivePaymentMethodConfiguration(
                CheckoutSessionPaymentMethodConfiguration effectivePaymentMethodConfiguration) {
            this.effectivePaymentMethodConfiguration = Optional.ofNullable(effectivePaymentMethodConfiguration);
            return this;
        }

        /**
         * <p>The configuration governing a checkout mounted from this configuration, resolved through every layer (its own overrides, the plan's, and the account's) — the shape a session's <code>payment_method_configuration</code> carries. Apply it over the payment method types catalogue for the offerable set. <code>null</code> means platform defaults; <code>payment_method_configuration</code> stays this configuration's own editable override.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "effective_payment_method_configuration", nulls = Nulls.SKIP)
        public _FinalStage effectivePaymentMethodConfiguration(
                Optional<CheckoutSessionPaymentMethodConfiguration> effectivePaymentMethodConfiguration) {
            this.effectivePaymentMethodConfiguration = effectivePaymentMethodConfiguration;
            return this;
        }

        /**
         * <p>Currency used for setup-mode payment method availability; defaults to <code>usd</code> when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(Nullable<CheckoutConfigurationCurrency> currency) {
            if (currency.isNull()) {
                this.currency = null;
            } else if (currency.isEmpty()) {
                this.currency = Optional.empty();
            } else {
                this.currency = Optional.of(currency.get());
            }
            return this;
        }

        /**
         * <p>Currency used for setup-mode payment method availability; defaults to <code>usd</code> when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(CheckoutConfigurationCurrency currency) {
            this.currency = Optional.ofNullable(currency);
            return this;
        }

        /**
         * <p>Currency used for setup-mode payment method availability; defaults to <code>usd</code> when omitted.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "currency", nulls = Nulls.SKIP)
        public _FinalStage currency(Optional<CheckoutConfigurationCurrency> currency) {
            this.currency = currency;
            return this;
        }

        /**
         * <p>Affiliate code applied at checkout, or <code>null</code> when none is set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage affiliateCode(Nullable<String> affiliateCode) {
            if (affiliateCode.isNull()) {
                this.affiliateCode = null;
            } else if (affiliateCode.isEmpty()) {
                this.affiliateCode = Optional.empty();
            } else {
                this.affiliateCode = Optional.of(affiliateCode.get());
            }
            return this;
        }

        /**
         * <p>Affiliate code applied at checkout, or <code>null</code> when none is set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage affiliateCode(String affiliateCode) {
            this.affiliateCode = Optional.ofNullable(affiliateCode);
            return this;
        }

        /**
         * <p>Affiliate code applied at checkout, or <code>null</code> when none is set.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "affiliate_code", nulls = Nulls.SKIP)
        public _FinalStage affiliateCode(Optional<String> affiliateCode) {
            this.affiliateCode = affiliateCode;
            return this;
        }

        @java.lang.Override
        public CheckoutConfiguration build() {
            return new CheckoutConfiguration(
                    accountId,
                    affiliateCode,
                    createdAt,
                    currency,
                    effectivePaymentMethodConfiguration,
                    id,
                    metadata,
                    mode,
                    paymentMethodConfiguration,
                    plan,
                    purchaseUrl,
                    redirectUrl,
                    threeDsLevel,
                    updatedAt,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
