/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionRedirectAction.Builder.class)
public final class CheckoutSessionRedirectAction {
    private final boolean blocking;

    private final String destinationUrl;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionRedirectAction(
            boolean blocking, String destinationUrl, Map<String, Object> additionalProperties) {
        this.blocking = blocking;
        this.destinationUrl = destinationUrl;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.
     */
    @JsonProperty("blocking")
    public boolean getBlocking() {
        return blocking;
    }

    /**
     * @return Where to send the buyer. An absolute HTTP or HTTPS URL, resolved from the seller's post-purchase settings and already carrying the order's query parameters (<code>receipt_id</code>, <code>payment_id</code>, <code>checkout_status</code>, <code>status</code>, <code>state_id</code>) — navigate straight to it. Distinct from <code>return_url</code>, which is the payment provider's return leg.
     */
    @JsonProperty("destination_url")
    public String getDestinationUrl() {
        return destinationUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionRedirectAction && equalTo((CheckoutSessionRedirectAction) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionRedirectAction other) {
        return blocking == other.blocking && destinationUrl.equals(other.destinationUrl);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.blocking, this.destinationUrl);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BlockingStage builder() {
        return new Builder();
    }

    public interface BlockingStage {
        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         */
        DestinationUrlStage blocking(boolean blocking);

        Builder from(CheckoutSessionRedirectAction other);
    }

    public interface DestinationUrlStage {
        /**
         * <p>Where to send the buyer. An absolute HTTP or HTTPS URL, resolved from the seller's post-purchase settings and already carrying the order's query parameters (<code>receipt_id</code>, <code>payment_id</code>, <code>checkout_status</code>, <code>status</code>, <code>state_id</code>) — navigate straight to it. Distinct from <code>return_url</code>, which is the payment provider's return leg.</p>
         */
        _FinalStage destinationUrl(@NotNull String destinationUrl);
    }

    public interface _FinalStage {
        CheckoutSessionRedirectAction build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BlockingStage, DestinationUrlStage, _FinalStage {
        private boolean blocking;

        private String destinationUrl;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionRedirectAction other) {
            blocking(other.getBlocking());
            destinationUrl(other.getDestinationUrl());
            return this;
        }

        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("blocking")
        public DestinationUrlStage blocking(boolean blocking) {
            this.blocking = blocking;
            return this;
        }

        /**
         * <p>Where to send the buyer. An absolute HTTP or HTTPS URL, resolved from the seller's post-purchase settings and already carrying the order's query parameters (<code>receipt_id</code>, <code>payment_id</code>, <code>checkout_status</code>, <code>status</code>, <code>state_id</code>) — navigate straight to it. Distinct from <code>return_url</code>, which is the payment provider's return leg.</p>
         * <p>Where to send the buyer. An absolute HTTP or HTTPS URL, resolved from the seller's post-purchase settings and already carrying the order's query parameters (<code>receipt_id</code>, <code>payment_id</code>, <code>checkout_status</code>, <code>status</code>, <code>state_id</code>) — navigate straight to it. Distinct from <code>return_url</code>, which is the payment provider's return leg.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("destination_url")
        public _FinalStage destinationUrl(@NotNull String destinationUrl) {
            this.destinationUrl = Objects.requireNonNull(destinationUrl, "destinationUrl must not be null");
            return this;
        }

        @java.lang.Override
        public CheckoutSessionRedirectAction build() {
            return new CheckoutSessionRedirectAction(blocking, destinationUrl, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
