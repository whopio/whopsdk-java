/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ExperienceAccessLevels {
    public static final ExperienceAccessLevels PRIVATE = new ExperienceAccessLevels(Value.PRIVATE, "private");

    public static final ExperienceAccessLevels PUBLIC = new ExperienceAccessLevels(Value.PUBLIC, "public");

    private final Value value;

    private final String string;

    ExperienceAccessLevels(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ExperienceAccessLevels
                        && this.string.equals(((ExperienceAccessLevels) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PRIVATE:
                return visitor.visitPrivate();
            case PUBLIC:
                return visitor.visitPublic();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ExperienceAccessLevels valueOf(String value) {
        switch (value) {
            case "private":
                return PRIVATE;
            case "public":
                return PUBLIC;
            default:
                return new ExperienceAccessLevels(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PUBLIC,

        PRIVATE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPublic();

        T visitPrivate();

        T visitUnknown(String unknownType);
    }
}
