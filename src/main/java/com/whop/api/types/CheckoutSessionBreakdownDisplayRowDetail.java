/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionBreakdownDisplayRowDetail.Builder.class)
public final class CheckoutSessionBreakdownDisplayRowDetail {
    private final Optional<Integer> accessDays;

    private final Optional<String> accessUntil;

    private final Optional<CheckoutSessionBreakdownDisplayRowDetailApplied> applied;

    private final Optional<String> chargeAt;

    private final Optional<String> code;

    private final Optional<Money> initialFee;

    private final Optional<Integer> intervalDays;

    private final Optional<Boolean> invoice;

    private final Optional<Boolean> paidTrial;

    private final Optional<String> rate;

    private final Optional<Integer> remainingPayments;

    private final Optional<Integer> totalPayments;

    private final Optional<Integer> trialDays;

    private final Optional<Boolean> withoutTax;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionBreakdownDisplayRowDetail(
            Optional<Integer> accessDays,
            Optional<String> accessUntil,
            Optional<CheckoutSessionBreakdownDisplayRowDetailApplied> applied,
            Optional<String> chargeAt,
            Optional<String> code,
            Optional<Money> initialFee,
            Optional<Integer> intervalDays,
            Optional<Boolean> invoice,
            Optional<Boolean> paidTrial,
            Optional<String> rate,
            Optional<Integer> remainingPayments,
            Optional<Integer> totalPayments,
            Optional<Integer> trialDays,
            Optional<Boolean> withoutTax,
            Map<String, Object> additionalProperties) {
        this.accessDays = accessDays;
        this.accessUntil = accessUntil;
        this.applied = applied;
        this.chargeAt = chargeAt;
        this.code = code;
        this.initialFee = initialFee;
        this.intervalDays = intervalDays;
        this.invoice = invoice;
        this.paidTrial = paidTrial;
        this.rate = rate;
        this.remainingPayments = remainingPayments;
        this.totalPayments = totalPayments;
        this.trialDays = trialDays;
        this.withoutTax = withoutTax;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return How many days of access a one-time purchase grants, when it expires.
     */
    @JsonProperty("access_days")
    public Optional<Integer> getAccessDays() {
        return accessDays;
    }

    /**
     * @return When a transferred membership's access ends, as an ISO 8601 timestamp — the recipient gets the origin's REMAINING time, never a fresh grant of the plan's window.
     */
    @JsonProperty("access_until")
    public Optional<String> getAccessUntil() {
        return accessUntil;
    }

    /**
     * @return The tax row's application: <code>added</code> counts toward the total; <code>included</code> is already inside the prices and is disclosed, never added.
     */
    @JsonProperty("applied")
    public Optional<CheckoutSessionBreakdownDisplayRowDetailApplied> getApplied() {
        return applied;
    }

    /**
     * @return The instant this row's charge lands, as an ISO 8601 timestamp — format it in the buyer's locale rather than deriving a date client-side.
     */
    @JsonProperty("charge_at")
    public Optional<String> getChargeAt() {
        return chargeAt;
    }

    /**
     * @return The promo code (uppercased) whose coverage this row states — &quot;Then your first month is free with FREETODAY&quot;.
     */
    @JsonProperty("code")
    public Optional<String> getCode() {
        return code;
    }

    /**
     * @return The one-off fee inside today's <code>due_today</code> charge — name it (&quot;Due today — includes a one-time $50.00 fee&quot;) so the figure does not read as the recurring rate.
     */
    @JsonProperty("initial_fee")
    public Optional<Money> getInitialFee() {
        return initialFee;
    }

    /**
     * @return Days between recurring charges, for wording the billing period (30 is monthly, 365 yearly).
     */
    @JsonProperty("interval_days")
    public Optional<Integer> getIntervalDays() {
        return intervalDays;
    }

    /**
     * @return Present (and <code>true</code>) when this one-time payment collects a seller-issued invoice — word it as a bill (&quot;Invoice — one-time payment.&quot;) beside the session's own due date, never as a storefront purchase.
     */
    @JsonProperty("invoice")
    public Optional<Boolean> getInvoice() {
        return invoice;
    }

    /**
     * @return Present (and <code>true</code>) when the trial this row follows charges a fee today — the wording drops the word &quot;trial&quot; for the neutral &quot;after N days&quot;, exactly as the legacy checkout worded a paid trial.
     */
    @JsonProperty("paid_trial")
    public Optional<Boolean> getPaidTrial() {
        return paidTrial;
    }

    /**
     * @return The tax row's effective rate as a decimal fraction (<code>&quot;0.0725&quot;</code> is 7.25%), when the calculation stated one.
     */
    @JsonProperty("rate")
    public Optional<String> getRate() {
        return rate;
    }

    /**
     * @return How many payments are still AHEAD at this row's amount — today's paying receipt already consumed one of the plan's total, exactly as billing counts receipts. Word it as the future stream (&quot;for 3 more payments&quot;), never as the plan's total.
     */
    @JsonProperty("remaining_payments")
    public Optional<Integer> getRemainingPayments() {
        return remainingPayments;
    }

    /**
     * @return An installment plan's PLAN-WIDE payment count, today's paying charge included — word it as the plan's total (&quot;for a total of 4 payments&quot;). Mutually exclusive with <code>remaining_payments</code>: a row states whichever count it means.
     */
    @JsonProperty("total_payments")
    public Optional<Integer> getTotalPayments() {
        return totalPayments;
    }

    /**
     * @return The free-trial length this row speaks about, in days.
     */
    @JsonProperty("trial_days")
    public Optional<Integer> getTrialDays() {
        return trialDays;
    }

    /**
     * @return Present (and <code>true</code>) when this row's amount is stated before tax and the wording must say so — the checkout adds exclusive tax the future charge will carry on top.
     */
    @JsonProperty("without_tax")
    public Optional<Boolean> getWithoutTax() {
        return withoutTax;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionBreakdownDisplayRowDetail
                && equalTo((CheckoutSessionBreakdownDisplayRowDetail) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionBreakdownDisplayRowDetail other) {
        return accessDays.equals(other.accessDays)
                && accessUntil.equals(other.accessUntil)
                && applied.equals(other.applied)
                && chargeAt.equals(other.chargeAt)
                && code.equals(other.code)
                && initialFee.equals(other.initialFee)
                && intervalDays.equals(other.intervalDays)
                && invoice.equals(other.invoice)
                && paidTrial.equals(other.paidTrial)
                && rate.equals(other.rate)
                && remainingPayments.equals(other.remainingPayments)
                && totalPayments.equals(other.totalPayments)
                && trialDays.equals(other.trialDays)
                && withoutTax.equals(other.withoutTax);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accessDays,
                this.accessUntil,
                this.applied,
                this.chargeAt,
                this.code,
                this.initialFee,
                this.intervalDays,
                this.invoice,
                this.paidTrial,
                this.rate,
                this.remainingPayments,
                this.totalPayments,
                this.trialDays,
                this.withoutTax);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<Integer> accessDays = Optional.empty();

        private Optional<String> accessUntil = Optional.empty();

        private Optional<CheckoutSessionBreakdownDisplayRowDetailApplied> applied = Optional.empty();

        private Optional<String> chargeAt = Optional.empty();

        private Optional<String> code = Optional.empty();

        private Optional<Money> initialFee = Optional.empty();

        private Optional<Integer> intervalDays = Optional.empty();

        private Optional<Boolean> invoice = Optional.empty();

        private Optional<Boolean> paidTrial = Optional.empty();

        private Optional<String> rate = Optional.empty();

        private Optional<Integer> remainingPayments = Optional.empty();

        private Optional<Integer> totalPayments = Optional.empty();

        private Optional<Integer> trialDays = Optional.empty();

        private Optional<Boolean> withoutTax = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(CheckoutSessionBreakdownDisplayRowDetail other) {
            accessDays(other.getAccessDays());
            accessUntil(other.getAccessUntil());
            applied(other.getApplied());
            chargeAt(other.getChargeAt());
            code(other.getCode());
            initialFee(other.getInitialFee());
            intervalDays(other.getIntervalDays());
            invoice(other.getInvoice());
            paidTrial(other.getPaidTrial());
            rate(other.getRate());
            remainingPayments(other.getRemainingPayments());
            totalPayments(other.getTotalPayments());
            trialDays(other.getTrialDays());
            withoutTax(other.getWithoutTax());
            return this;
        }

        /**
         * <p>How many days of access a one-time purchase grants, when it expires.</p>
         */
        @JsonSetter(value = "access_days", nulls = Nulls.SKIP)
        public Builder accessDays(Optional<Integer> accessDays) {
            this.accessDays = accessDays;
            return this;
        }

        public Builder accessDays(Integer accessDays) {
            this.accessDays = Optional.ofNullable(accessDays);
            return this;
        }

        /**
         * <p>When a transferred membership's access ends, as an ISO 8601 timestamp — the recipient gets the origin's REMAINING time, never a fresh grant of the plan's window.</p>
         */
        @JsonSetter(value = "access_until", nulls = Nulls.SKIP)
        public Builder accessUntil(Optional<String> accessUntil) {
            this.accessUntil = accessUntil;
            return this;
        }

        public Builder accessUntil(String accessUntil) {
            this.accessUntil = Optional.ofNullable(accessUntil);
            return this;
        }

        /**
         * <p>The tax row's application: <code>added</code> counts toward the total; <code>included</code> is already inside the prices and is disclosed, never added.</p>
         */
        @JsonSetter(value = "applied", nulls = Nulls.SKIP)
        public Builder applied(Optional<CheckoutSessionBreakdownDisplayRowDetailApplied> applied) {
            this.applied = applied;
            return this;
        }

        public Builder applied(CheckoutSessionBreakdownDisplayRowDetailApplied applied) {
            this.applied = Optional.ofNullable(applied);
            return this;
        }

        /**
         * <p>The instant this row's charge lands, as an ISO 8601 timestamp — format it in the buyer's locale rather than deriving a date client-side.</p>
         */
        @JsonSetter(value = "charge_at", nulls = Nulls.SKIP)
        public Builder chargeAt(Optional<String> chargeAt) {
            this.chargeAt = chargeAt;
            return this;
        }

        public Builder chargeAt(String chargeAt) {
            this.chargeAt = Optional.ofNullable(chargeAt);
            return this;
        }

        /**
         * <p>The promo code (uppercased) whose coverage this row states — &quot;Then your first month is free with FREETODAY&quot;.</p>
         */
        @JsonSetter(value = "code", nulls = Nulls.SKIP)
        public Builder code(Optional<String> code) {
            this.code = code;
            return this;
        }

        public Builder code(String code) {
            this.code = Optional.ofNullable(code);
            return this;
        }

        /**
         * <p>The one-off fee inside today's <code>due_today</code> charge — name it (&quot;Due today — includes a one-time $50.00 fee&quot;) so the figure does not read as the recurring rate.</p>
         */
        @JsonSetter(value = "initial_fee", nulls = Nulls.SKIP)
        public Builder initialFee(Optional<Money> initialFee) {
            this.initialFee = initialFee;
            return this;
        }

        public Builder initialFee(Money initialFee) {
            this.initialFee = Optional.ofNullable(initialFee);
            return this;
        }

        /**
         * <p>Days between recurring charges, for wording the billing period (30 is monthly, 365 yearly).</p>
         */
        @JsonSetter(value = "interval_days", nulls = Nulls.SKIP)
        public Builder intervalDays(Optional<Integer> intervalDays) {
            this.intervalDays = intervalDays;
            return this;
        }

        public Builder intervalDays(Integer intervalDays) {
            this.intervalDays = Optional.ofNullable(intervalDays);
            return this;
        }

        /**
         * <p>Present (and <code>true</code>) when this one-time payment collects a seller-issued invoice — word it as a bill (&quot;Invoice — one-time payment.&quot;) beside the session's own due date, never as a storefront purchase.</p>
         */
        @JsonSetter(value = "invoice", nulls = Nulls.SKIP)
        public Builder invoice(Optional<Boolean> invoice) {
            this.invoice = invoice;
            return this;
        }

        public Builder invoice(Boolean invoice) {
            this.invoice = Optional.ofNullable(invoice);
            return this;
        }

        /**
         * <p>Present (and <code>true</code>) when the trial this row follows charges a fee today — the wording drops the word &quot;trial&quot; for the neutral &quot;after N days&quot;, exactly as the legacy checkout worded a paid trial.</p>
         */
        @JsonSetter(value = "paid_trial", nulls = Nulls.SKIP)
        public Builder paidTrial(Optional<Boolean> paidTrial) {
            this.paidTrial = paidTrial;
            return this;
        }

        public Builder paidTrial(Boolean paidTrial) {
            this.paidTrial = Optional.ofNullable(paidTrial);
            return this;
        }

        /**
         * <p>The tax row's effective rate as a decimal fraction (<code>&quot;0.0725&quot;</code> is 7.25%), when the calculation stated one.</p>
         */
        @JsonSetter(value = "rate", nulls = Nulls.SKIP)
        public Builder rate(Optional<String> rate) {
            this.rate = rate;
            return this;
        }

        public Builder rate(String rate) {
            this.rate = Optional.ofNullable(rate);
            return this;
        }

        /**
         * <p>How many payments are still AHEAD at this row's amount — today's paying receipt already consumed one of the plan's total, exactly as billing counts receipts. Word it as the future stream (&quot;for 3 more payments&quot;), never as the plan's total.</p>
         */
        @JsonSetter(value = "remaining_payments", nulls = Nulls.SKIP)
        public Builder remainingPayments(Optional<Integer> remainingPayments) {
            this.remainingPayments = remainingPayments;
            return this;
        }

        public Builder remainingPayments(Integer remainingPayments) {
            this.remainingPayments = Optional.ofNullable(remainingPayments);
            return this;
        }

        /**
         * <p>An installment plan's PLAN-WIDE payment count, today's paying charge included — word it as the plan's total (&quot;for a total of 4 payments&quot;). Mutually exclusive with <code>remaining_payments</code>: a row states whichever count it means.</p>
         */
        @JsonSetter(value = "total_payments", nulls = Nulls.SKIP)
        public Builder totalPayments(Optional<Integer> totalPayments) {
            this.totalPayments = totalPayments;
            return this;
        }

        public Builder totalPayments(Integer totalPayments) {
            this.totalPayments = Optional.ofNullable(totalPayments);
            return this;
        }

        /**
         * <p>The free-trial length this row speaks about, in days.</p>
         */
        @JsonSetter(value = "trial_days", nulls = Nulls.SKIP)
        public Builder trialDays(Optional<Integer> trialDays) {
            this.trialDays = trialDays;
            return this;
        }

        public Builder trialDays(Integer trialDays) {
            this.trialDays = Optional.ofNullable(trialDays);
            return this;
        }

        /**
         * <p>Present (and <code>true</code>) when this row's amount is stated before tax and the wording must say so — the checkout adds exclusive tax the future charge will carry on top.</p>
         */
        @JsonSetter(value = "without_tax", nulls = Nulls.SKIP)
        public Builder withoutTax(Optional<Boolean> withoutTax) {
            this.withoutTax = withoutTax;
            return this;
        }

        public Builder withoutTax(Boolean withoutTax) {
            this.withoutTax = Optional.ofNullable(withoutTax);
            return this;
        }

        public CheckoutSessionBreakdownDisplayRowDetail build() {
            return new CheckoutSessionBreakdownDisplayRowDetail(
                    accessDays,
                    accessUntil,
                    applied,
                    chargeAt,
                    code,
                    initialFee,
                    intervalDays,
                    invoice,
                    paidTrial,
                    rate,
                    remainingPayments,
                    totalPayments,
                    trialDays,
                    withoutTax,
                    additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
