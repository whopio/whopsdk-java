/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionSeller.Builder.class)
public final class CheckoutSessionSeller {
    private final String id;

    private final Optional<String> logoUrl;

    private final Optional<String> redirectPurchaseUrl;

    private final String route;

    private final CheckoutSessionSellerTerms terms;

    private final String title;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionSeller(
            String id,
            Optional<String> logoUrl,
            Optional<String> redirectPurchaseUrl,
            String route,
            CheckoutSessionSellerTerms terms,
            String title,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.logoUrl = logoUrl;
        this.redirectPurchaseUrl = redirectPurchaseUrl;
        this.route = route;
        this.terms = terms;
        this.title = title;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Account ID, prefixed <code>biz_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The seller's logo image URL, or <code>null</code> when they have not uploaded one.
     */
    @JsonIgnore
    public Optional<String> getLogoUrl() {
        if (logoUrl == null) {
            return Optional.empty();
        }
        return logoUrl;
    }

    /**
     * @return Where this seller sends buyers after any purchase, or <code>null</code>. The plan's and the checkout configuration's own redirects take precedence over it.
     */
    @JsonIgnore
    public Optional<String> getRedirectPurchaseUrl() {
        if (redirectPurchaseUrl == null) {
            return Optional.empty();
        }
        return redirectPurchaseUrl;
    }

    /**
     * @return The seller's store-page slug — the <code>:route</code> in <code>whop.com/joined/:route</code>, which is where a purchase lands when nothing more specific is configured.
     */
    @JsonProperty("route")
    public String getRoute() {
        return route;
    }

    /**
     * @return The seller's checkout legal surface: whether explicit acceptance is required, and the policy documents it covers.
     */
    @JsonProperty("terms")
    public CheckoutSessionSellerTerms getTerms() {
        return terms;
    }

    /**
     * @return The seller's public name, as the buyer knows them.
     */
    @JsonProperty("title")
    public String getTitle() {
        return title;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("logo_url")
    private Optional<String> _getLogoUrl() {
        return logoUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("redirect_purchase_url")
    private Optional<String> _getRedirectPurchaseUrl() {
        return redirectPurchaseUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionSeller && equalTo((CheckoutSessionSeller) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionSeller other) {
        return id.equals(other.id)
                && logoUrl.equals(other.logoUrl)
                && redirectPurchaseUrl.equals(other.redirectPurchaseUrl)
                && route.equals(other.route)
                && terms.equals(other.terms)
                && title.equals(other.title);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.logoUrl, this.redirectPurchaseUrl, this.route, this.terms, this.title);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         */
        RouteStage id(@NotNull String id);

        Builder from(CheckoutSessionSeller other);
    }

    public interface RouteStage {
        /**
         * <p>The seller's store-page slug — the <code>:route</code> in <code>whop.com/joined/:route</code>, which is where a purchase lands when nothing more specific is configured.</p>
         */
        TermsStage route(@NotNull String route);
    }

    public interface TermsStage {
        /**
         * <p>The seller's checkout legal surface: whether explicit acceptance is required, and the policy documents it covers.</p>
         */
        TitleStage terms(@NotNull CheckoutSessionSellerTerms terms);
    }

    public interface TitleStage {
        /**
         * <p>The seller's public name, as the buyer knows them.</p>
         */
        _FinalStage title(@NotNull String title);
    }

    public interface _FinalStage {
        CheckoutSessionSeller build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The seller's logo image URL, or <code>null</code> when they have not uploaded one.</p>
         */
        _FinalStage logoUrl(Optional<String> logoUrl);

        _FinalStage logoUrl(String logoUrl);

        _FinalStage logoUrl(Nullable<String> logoUrl);

        /**
         * <p>Where this seller sends buyers after any purchase, or <code>null</code>. The plan's and the checkout configuration's own redirects take precedence over it.</p>
         */
        _FinalStage redirectPurchaseUrl(Optional<String> redirectPurchaseUrl);

        _FinalStage redirectPurchaseUrl(String redirectPurchaseUrl);

        _FinalStage redirectPurchaseUrl(Nullable<String> redirectPurchaseUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, RouteStage, TermsStage, TitleStage, _FinalStage {
        private String id;

        private String route;

        private CheckoutSessionSellerTerms terms;

        private String title;

        private Optional<String> redirectPurchaseUrl = Optional.empty();

        private Optional<String> logoUrl = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionSeller other) {
            id(other.getId());
            logoUrl(other.getLogoUrl());
            redirectPurchaseUrl(other.getRedirectPurchaseUrl());
            route(other.getRoute());
            terms(other.getTerms());
            title(other.getTitle());
            return this;
        }

        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public RouteStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The seller's store-page slug — the <code>:route</code> in <code>whop.com/joined/:route</code>, which is where a purchase lands when nothing more specific is configured.</p>
         * <p>The seller's store-page slug — the <code>:route</code> in <code>whop.com/joined/:route</code>, which is where a purchase lands when nothing more specific is configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("route")
        public TermsStage route(@NotNull String route) {
            this.route = Objects.requireNonNull(route, "route must not be null");
            return this;
        }

        /**
         * <p>The seller's checkout legal surface: whether explicit acceptance is required, and the policy documents it covers.</p>
         * <p>The seller's checkout legal surface: whether explicit acceptance is required, and the policy documents it covers.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("terms")
        public TitleStage terms(@NotNull CheckoutSessionSellerTerms terms) {
            this.terms = Objects.requireNonNull(terms, "terms must not be null");
            return this;
        }

        /**
         * <p>The seller's public name, as the buyer knows them.</p>
         * <p>The seller's public name, as the buyer knows them.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("title")
        public _FinalStage title(@NotNull String title) {
            this.title = Objects.requireNonNull(title, "title must not be null");
            return this;
        }

        /**
         * <p>Where this seller sends buyers after any purchase, or <code>null</code>. The plan's and the checkout configuration's own redirects take precedence over it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage redirectPurchaseUrl(Nullable<String> redirectPurchaseUrl) {
            if (redirectPurchaseUrl.isNull()) {
                this.redirectPurchaseUrl = null;
            } else if (redirectPurchaseUrl.isEmpty()) {
                this.redirectPurchaseUrl = Optional.empty();
            } else {
                this.redirectPurchaseUrl = Optional.of(redirectPurchaseUrl.get());
            }
            return this;
        }

        /**
         * <p>Where this seller sends buyers after any purchase, or <code>null</code>. The plan's and the checkout configuration's own redirects take precedence over it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage redirectPurchaseUrl(String redirectPurchaseUrl) {
            this.redirectPurchaseUrl = Optional.ofNullable(redirectPurchaseUrl);
            return this;
        }

        /**
         * <p>Where this seller sends buyers after any purchase, or <code>null</code>. The plan's and the checkout configuration's own redirects take precedence over it.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "redirect_purchase_url", nulls = Nulls.SKIP)
        public _FinalStage redirectPurchaseUrl(Optional<String> redirectPurchaseUrl) {
            this.redirectPurchaseUrl = redirectPurchaseUrl;
            return this;
        }

        /**
         * <p>The seller's logo image URL, or <code>null</code> when they have not uploaded one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage logoUrl(Nullable<String> logoUrl) {
            if (logoUrl.isNull()) {
                this.logoUrl = null;
            } else if (logoUrl.isEmpty()) {
                this.logoUrl = Optional.empty();
            } else {
                this.logoUrl = Optional.of(logoUrl.get());
            }
            return this;
        }

        /**
         * <p>The seller's logo image URL, or <code>null</code> when they have not uploaded one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage logoUrl(String logoUrl) {
            this.logoUrl = Optional.ofNullable(logoUrl);
            return this;
        }

        /**
         * <p>The seller's logo image URL, or <code>null</code> when they have not uploaded one.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "logo_url", nulls = Nulls.SKIP)
        public _FinalStage logoUrl(Optional<String> logoUrl) {
            this.logoUrl = logoUrl;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionSeller build() {
            return new CheckoutSessionSeller(
                    id, logoUrl, redirectPurchaseUrl, route, terms, title, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
