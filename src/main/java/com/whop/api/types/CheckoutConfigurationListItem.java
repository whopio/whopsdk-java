/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutConfigurationListItem.Builder.class)
public final class CheckoutConfigurationListItem {
    private final Optional<String> affiliateCode;

    private final String companyId;

    private final Optional<Currencies> currency;

    private final String id;

    private final Optional<Map<String, Object>> metadata;

    private final CheckoutModes mode;

    private final Optional<CheckoutConfigurationListItemPaymentMethodConfiguration> paymentMethodConfiguration;

    private final Optional<CheckoutConfigurationListItemPlan> plan;

    private final String purchaseUrl;

    private final Optional<String> redirectUrl;

    private final Map<String, Object> additionalProperties;

    private CheckoutConfigurationListItem(
            Optional<String> affiliateCode,
            String companyId,
            Optional<Currencies> currency,
            String id,
            Optional<Map<String, Object>> metadata,
            CheckoutModes mode,
            Optional<CheckoutConfigurationListItemPaymentMethodConfiguration> paymentMethodConfiguration,
            Optional<CheckoutConfigurationListItemPlan> plan,
            String purchaseUrl,
            Optional<String> redirectUrl,
            Map<String, Object> additionalProperties) {
        this.affiliateCode = affiliateCode;
        this.companyId = companyId;
        this.currency = currency;
        this.id = id;
        this.metadata = metadata;
        this.mode = mode;
        this.paymentMethodConfiguration = paymentMethodConfiguration;
        this.plan = plan;
        this.purchaseUrl = purchaseUrl;
        this.redirectUrl = redirectUrl;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The affiliate code to use for the checkout configuration
     */
    @JsonIgnore
    public Optional<String> getAffiliateCode() {
        if (affiliateCode == null) {
            return Optional.empty();
        }
        return affiliateCode;
    }

    /**
     * @return The ID of the company to use for the checkout configuration
     */
    @JsonProperty("company_id")
    public String getCompanyId() {
        return companyId;
    }

    /**
     * @return The currency to use for the configuration when in 'setup' mode. This is used to target which currency specific payment methods are available. If not provided, it will default to 'usd' when in setup mode.
     */
    @JsonIgnore
    public Optional<Currencies> getCurrency() {
        if (currency == null) {
            return Optional.empty();
        }
        return currency;
    }

    /**
     * @return The unique identifier for the checkout session.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The metadata to use for the checkout configuration
     */
    @JsonIgnore
    public Optional<Map<String, Object>> getMetadata() {
        if (metadata == null) {
            return Optional.empty();
        }
        return metadata;
    }

    /**
     * @return The mode of the checkout session.
     */
    @JsonProperty("mode")
    public CheckoutModes getMode() {
        return mode;
    }

    /**
     * @return The explicit payment method configuration for the session, if any. This currently only works in 'setup' mode. Use the plan's payment_method_configuration for payment method.
     */
    @JsonIgnore
    public Optional<CheckoutConfigurationListItemPaymentMethodConfiguration> getPaymentMethodConfiguration() {
        if (paymentMethodConfiguration == null) {
            return Optional.empty();
        }
        return paymentMethodConfiguration;
    }

    /**
     * @return The plan to use for the checkout configuration
     */
    @JsonIgnore
    public Optional<CheckoutConfigurationListItemPlan> getPlan() {
        if (plan == null) {
            return Optional.empty();
        }
        return plan;
    }

    /**
     * @return A URL you can send to customers to complete a checkout. It looks like <code>/checkout/ch_xxxx/</code>
     */
    @JsonProperty("purchase_url")
    public String getPurchaseUrl() {
        return purchaseUrl;
    }

    /**
     * @return The URL to redirect the user to after the checkout configuration is created
     */
    @JsonIgnore
    public Optional<String> getRedirectUrl() {
        if (redirectUrl == null) {
            return Optional.empty();
        }
        return redirectUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("affiliate_code")
    private Optional<String> _getAffiliateCode() {
        return affiliateCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("currency")
    private Optional<Currencies> _getCurrency() {
        return currency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("metadata")
    private Optional<Map<String, Object>> _getMetadata() {
        return metadata;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payment_method_configuration")
    private Optional<CheckoutConfigurationListItemPaymentMethodConfiguration> _getPaymentMethodConfiguration() {
        return paymentMethodConfiguration;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("plan")
    private Optional<CheckoutConfigurationListItemPlan> _getPlan() {
        return plan;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("redirect_url")
    private Optional<String> _getRedirectUrl() {
        return redirectUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutConfigurationListItem && equalTo((CheckoutConfigurationListItem) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutConfigurationListItem other) {
        return affiliateCode.equals(other.affiliateCode)
                && companyId.equals(other.companyId)
                && currency.equals(other.currency)
                && id.equals(other.id)
                && metadata.equals(other.metadata)
                && mode.equals(other.mode)
                && paymentMethodConfiguration.equals(other.paymentMethodConfiguration)
                && plan.equals(other.plan)
                && purchaseUrl.equals(other.purchaseUrl)
                && redirectUrl.equals(other.redirectUrl);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.affiliateCode,
                this.companyId,
                this.currency,
                this.id,
                this.metadata,
                this.mode,
                this.paymentMethodConfiguration,
                this.plan,
                this.purchaseUrl,
                this.redirectUrl);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CompanyIdStage builder() {
        return new Builder();
    }

    public interface CompanyIdStage {
        /**
         * <p>The ID of the company to use for the checkout configuration</p>
         */
        IdStage companyId(@NotNull String companyId);

        Builder from(CheckoutConfigurationListItem other);
    }

    public interface IdStage {
        /**
         * <p>The unique identifier for the checkout session.</p>
         */
        ModeStage id(@NotNull String id);
    }

    public interface ModeStage {
        /**
         * <p>The mode of the checkout session.</p>
         */
        PurchaseUrlStage mode(@NotNull CheckoutModes mode);
    }

    public interface PurchaseUrlStage {
        /**
         * <p>A URL you can send to customers to complete a checkout. It looks like <code>/checkout/ch_xxxx/</code></p>
         */
        _FinalStage purchaseUrl(@NotNull String purchaseUrl);
    }

    public interface _FinalStage {
        CheckoutConfigurationListItem build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The affiliate code to use for the checkout configuration</p>
         */
        _FinalStage affiliateCode(Optional<String> affiliateCode);

        _FinalStage affiliateCode(String affiliateCode);

        _FinalStage affiliateCode(Nullable<String> affiliateCode);

        /**
         * <p>The currency to use for the configuration when in 'setup' mode. This is used to target which currency specific payment methods are available. If not provided, it will default to 'usd' when in setup mode.</p>
         */
        _FinalStage currency(Optional<Currencies> currency);

        _FinalStage currency(Currencies currency);

        _FinalStage currency(Nullable<Currencies> currency);

        /**
         * <p>The metadata to use for the checkout configuration</p>
         */
        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);

        _FinalStage metadata(Nullable<Map<String, Object>> metadata);

        /**
         * <p>The explicit payment method configuration for the session, if any. This currently only works in 'setup' mode. Use the plan's payment_method_configuration for payment method.</p>
         */
        _FinalStage paymentMethodConfiguration(
                Optional<CheckoutConfigurationListItemPaymentMethodConfiguration> paymentMethodConfiguration);

        _FinalStage paymentMethodConfiguration(
                CheckoutConfigurationListItemPaymentMethodConfiguration paymentMethodConfiguration);

        _FinalStage paymentMethodConfiguration(
                Nullable<CheckoutConfigurationListItemPaymentMethodConfiguration> paymentMethodConfiguration);

        /**
         * <p>The plan to use for the checkout configuration</p>
         */
        _FinalStage plan(Optional<CheckoutConfigurationListItemPlan> plan);

        _FinalStage plan(CheckoutConfigurationListItemPlan plan);

        _FinalStage plan(Nullable<CheckoutConfigurationListItemPlan> plan);

        /**
         * <p>The URL to redirect the user to after the checkout configuration is created</p>
         */
        _FinalStage redirectUrl(Optional<String> redirectUrl);

        _FinalStage redirectUrl(String redirectUrl);

        _FinalStage redirectUrl(Nullable<String> redirectUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements CompanyIdStage, IdStage, ModeStage, PurchaseUrlStage, _FinalStage {
        private String companyId;

        private String id;

        private CheckoutModes mode;

        private String purchaseUrl;

        private Optional<String> redirectUrl = Optional.empty();

        private Optional<CheckoutConfigurationListItemPlan> plan = Optional.empty();

        private Optional<CheckoutConfigurationListItemPaymentMethodConfiguration> paymentMethodConfiguration =
                Optional.empty();

        private Optional<Map<String, Object>> metadata = Optional.empty();

        private Optional<Currencies> currency = Optional.empty();

        private Optional<String> affiliateCode = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutConfigurationListItem other) {
            affiliateCode(other.getAffiliateCode());
            companyId(other.getCompanyId());
            currency(other.getCurrency());
            id(other.getId());
            metadata(other.getMetadata());
            mode(other.getMode());
            paymentMethodConfiguration(other.getPaymentMethodConfiguration());
            plan(other.getPlan());
            purchaseUrl(other.getPurchaseUrl());
            redirectUrl(other.getRedirectUrl());
            return this;
        }

        /**
         * <p>The ID of the company to use for the checkout configuration</p>
         * <p>The ID of the company to use for the checkout configuration</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("company_id")
        public IdStage companyId(@NotNull String companyId) {
            this.companyId = Objects.requireNonNull(companyId, "companyId must not be null");
            return this;
        }

        /**
         * <p>The unique identifier for the checkout session.</p>
         * <p>The unique identifier for the checkout session.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public ModeStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The mode of the checkout session.</p>
         * <p>The mode of the checkout session.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("mode")
        public PurchaseUrlStage mode(@NotNull CheckoutModes mode) {
            this.mode = Objects.requireNonNull(mode, "mode must not be null");
            return this;
        }

        /**
         * <p>A URL you can send to customers to complete a checkout. It looks like <code>/checkout/ch_xxxx/</code></p>
         * <p>A URL you can send to customers to complete a checkout. It looks like <code>/checkout/ch_xxxx/</code></p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("purchase_url")
        public _FinalStage purchaseUrl(@NotNull String purchaseUrl) {
            this.purchaseUrl = Objects.requireNonNull(purchaseUrl, "purchaseUrl must not be null");
            return this;
        }

        /**
         * <p>The URL to redirect the user to after the checkout configuration is created</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage redirectUrl(Nullable<String> redirectUrl) {
            if (redirectUrl.isNull()) {
                this.redirectUrl = null;
            } else if (redirectUrl.isEmpty()) {
                this.redirectUrl = Optional.empty();
            } else {
                this.redirectUrl = Optional.of(redirectUrl.get());
            }
            return this;
        }

        /**
         * <p>The URL to redirect the user to after the checkout configuration is created</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage redirectUrl(String redirectUrl) {
            this.redirectUrl = Optional.ofNullable(redirectUrl);
            return this;
        }

        /**
         * <p>The URL to redirect the user to after the checkout configuration is created</p>
         */
        @java.lang.Override
        @JsonSetter(value = "redirect_url", nulls = Nulls.SKIP)
        public _FinalStage redirectUrl(Optional<String> redirectUrl) {
            this.redirectUrl = redirectUrl;
            return this;
        }

        /**
         * <p>The plan to use for the checkout configuration</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage plan(Nullable<CheckoutConfigurationListItemPlan> plan) {
            if (plan.isNull()) {
                this.plan = null;
            } else if (plan.isEmpty()) {
                this.plan = Optional.empty();
            } else {
                this.plan = Optional.of(plan.get());
            }
            return this;
        }

        /**
         * <p>The plan to use for the checkout configuration</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage plan(CheckoutConfigurationListItemPlan plan) {
            this.plan = Optional.ofNullable(plan);
            return this;
        }

        /**
         * <p>The plan to use for the checkout configuration</p>
         */
        @java.lang.Override
        @JsonSetter(value = "plan", nulls = Nulls.SKIP)
        public _FinalStage plan(Optional<CheckoutConfigurationListItemPlan> plan) {
            this.plan = plan;
            return this;
        }

        /**
         * <p>The explicit payment method configuration for the session, if any. This currently only works in 'setup' mode. Use the plan's payment_method_configuration for payment method.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage paymentMethodConfiguration(
                Nullable<CheckoutConfigurationListItemPaymentMethodConfiguration> paymentMethodConfiguration) {
            if (paymentMethodConfiguration.isNull()) {
                this.paymentMethodConfiguration = null;
            } else if (paymentMethodConfiguration.isEmpty()) {
                this.paymentMethodConfiguration = Optional.empty();
            } else {
                this.paymentMethodConfiguration = Optional.of(paymentMethodConfiguration.get());
            }
            return this;
        }

        /**
         * <p>The explicit payment method configuration for the session, if any. This currently only works in 'setup' mode. Use the plan's payment_method_configuration for payment method.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage paymentMethodConfiguration(
                CheckoutConfigurationListItemPaymentMethodConfiguration paymentMethodConfiguration) {
            this.paymentMethodConfiguration = Optional.ofNullable(paymentMethodConfiguration);
            return this;
        }

        /**
         * <p>The explicit payment method configuration for the session, if any. This currently only works in 'setup' mode. Use the plan's payment_method_configuration for payment method.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payment_method_configuration", nulls = Nulls.SKIP)
        public _FinalStage paymentMethodConfiguration(
                Optional<CheckoutConfigurationListItemPaymentMethodConfiguration> paymentMethodConfiguration) {
            this.paymentMethodConfiguration = paymentMethodConfiguration;
            return this;
        }

        /**
         * <p>The metadata to use for the checkout configuration</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Nullable<Map<String, Object>> metadata) {
            if (metadata.isNull()) {
                this.metadata = null;
            } else if (metadata.isEmpty()) {
                this.metadata = Optional.empty();
            } else {
                this.metadata = Optional.of(metadata.get());
            }
            return this;
        }

        /**
         * <p>The metadata to use for the checkout configuration</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        /**
         * <p>The metadata to use for the checkout configuration</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        /**
         * <p>The currency to use for the configuration when in 'setup' mode. This is used to target which currency specific payment methods are available. If not provided, it will default to 'usd' when in setup mode.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(Nullable<Currencies> currency) {
            if (currency.isNull()) {
                this.currency = null;
            } else if (currency.isEmpty()) {
                this.currency = Optional.empty();
            } else {
                this.currency = Optional.of(currency.get());
            }
            return this;
        }

        /**
         * <p>The currency to use for the configuration when in 'setup' mode. This is used to target which currency specific payment methods are available. If not provided, it will default to 'usd' when in setup mode.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(Currencies currency) {
            this.currency = Optional.ofNullable(currency);
            return this;
        }

        /**
         * <p>The currency to use for the configuration when in 'setup' mode. This is used to target which currency specific payment methods are available. If not provided, it will default to 'usd' when in setup mode.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "currency", nulls = Nulls.SKIP)
        public _FinalStage currency(Optional<Currencies> currency) {
            this.currency = currency;
            return this;
        }

        /**
         * <p>The affiliate code to use for the checkout configuration</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage affiliateCode(Nullable<String> affiliateCode) {
            if (affiliateCode.isNull()) {
                this.affiliateCode = null;
            } else if (affiliateCode.isEmpty()) {
                this.affiliateCode = Optional.empty();
            } else {
                this.affiliateCode = Optional.of(affiliateCode.get());
            }
            return this;
        }

        /**
         * <p>The affiliate code to use for the checkout configuration</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage affiliateCode(String affiliateCode) {
            this.affiliateCode = Optional.ofNullable(affiliateCode);
            return this;
        }

        /**
         * <p>The affiliate code to use for the checkout configuration</p>
         */
        @java.lang.Override
        @JsonSetter(value = "affiliate_code", nulls = Nulls.SKIP)
        public _FinalStage affiliateCode(Optional<String> affiliateCode) {
            this.affiliateCode = affiliateCode;
            return this;
        }

        @java.lang.Override
        public CheckoutConfigurationListItem build() {
            return new CheckoutConfigurationListItem(
                    affiliateCode,
                    companyId,
                    currency,
                    id,
                    metadata,
                    mode,
                    paymentMethodConfiguration,
                    plan,
                    purchaseUrl,
                    redirectUrl,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
