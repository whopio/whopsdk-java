/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class DisputeEvidenceLockedReason {
    public static final DisputeEvidenceLockedReason RESPONSE_WINDOW_CLOSED =
            new DisputeEvidenceLockedReason(Value.RESPONSE_WINDOW_CLOSED, "response_window_closed");

    public static final DisputeEvidenceLockedReason SUBMITTED =
            new DisputeEvidenceLockedReason(Value.SUBMITTED, "submitted");

    public static final DisputeEvidenceLockedReason NOT_CONTESTABLE =
            new DisputeEvidenceLockedReason(Value.NOT_CONTESTABLE, "not_contestable");

    private final Value value;

    private final String string;

    DisputeEvidenceLockedReason(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof DisputeEvidenceLockedReason
                        && this.string.equals(((DisputeEvidenceLockedReason) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case RESPONSE_WINDOW_CLOSED:
                return visitor.visitResponseWindowClosed();
            case SUBMITTED:
                return visitor.visitSubmitted();
            case NOT_CONTESTABLE:
                return visitor.visitNotContestable();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static DisputeEvidenceLockedReason valueOf(String value) {
        switch (value) {
            case "response_window_closed":
                return RESPONSE_WINDOW_CLOSED;
            case "submitted":
                return SUBMITTED;
            case "not_contestable":
                return NOT_CONTESTABLE;
            default:
                return new DisputeEvidenceLockedReason(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        SUBMITTED,

        RESPONSE_WINDOW_CLOSED,

        NOT_CONTESTABLE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitSubmitted();

        T visitResponseWindowClosed();

        T visitNotContestable();

        T visitUnknown(String unknownType);
    }
}
