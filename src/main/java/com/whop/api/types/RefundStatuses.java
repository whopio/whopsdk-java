/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class RefundStatuses {
    public static final RefundStatuses FAILED = new RefundStatuses(Value.FAILED, "failed");

    public static final RefundStatuses SUCCEEDED = new RefundStatuses(Value.SUCCEEDED, "succeeded");

    public static final RefundStatuses PENDING = new RefundStatuses(Value.PENDING, "pending");

    public static final RefundStatuses CANCELED = new RefundStatuses(Value.CANCELED, "canceled");

    public static final RefundStatuses REQUIRES_ACTION = new RefundStatuses(Value.REQUIRES_ACTION, "requires_action");

    private final Value value;

    private final String string;

    RefundStatuses(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof RefundStatuses && this.string.equals(((RefundStatuses) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FAILED:
                return visitor.visitFailed();
            case SUCCEEDED:
                return visitor.visitSucceeded();
            case PENDING:
                return visitor.visitPending();
            case CANCELED:
                return visitor.visitCanceled();
            case REQUIRES_ACTION:
                return visitor.visitRequiresAction();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static RefundStatuses valueOf(String value) {
        switch (value) {
            case "failed":
                return FAILED;
            case "succeeded":
                return SUCCEEDED;
            case "pending":
                return PENDING;
            case "canceled":
                return CANCELED;
            case "requires_action":
                return REQUIRES_ACTION;
            default:
                return new RefundStatuses(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        REQUIRES_ACTION,

        SUCCEEDED,

        FAILED,

        CANCELED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitRequiresAction();

        T visitSucceeded();

        T visitFailed();

        T visitCanceled();

        T visitUnknown(String unknownType);
    }
}
