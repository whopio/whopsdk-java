/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionBreakdownDisplayRow.Builder.class)
public final class CheckoutSessionBreakdownDisplayRow {
    private final Optional<Money> amount;

    private final Optional<CheckoutSessionBreakdownDisplayRowDetail> detail;

    private final CheckoutSessionBreakdownDisplayRowKind kind;

    private final Optional<String> label;

    private final Optional<CheckoutSessionBreakdownDisplayRowStatus> status;

    private final Optional<String> text;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionBreakdownDisplayRow(
            Optional<Money> amount,
            Optional<CheckoutSessionBreakdownDisplayRowDetail> detail,
            CheckoutSessionBreakdownDisplayRowKind kind,
            Optional<String> label,
            Optional<CheckoutSessionBreakdownDisplayRowStatus> status,
            Optional<String> text,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.detail = detail;
        this.kind = kind;
        this.label = label;
        this.status = status;
        this.text = text;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The row's figure as a Money object, when it has one — what a surface formats in the buyer's locale.
     */
    @JsonIgnore
    public Optional<Money> getAmount() {
        if (amount == null) {
            return Optional.empty();
        }
        return amount;
    }

    /**
     * @return The typed parameters this row's wording is built from — the localization rail. Absent when the kind needs none.
     */
    @JsonIgnore
    public Optional<CheckoutSessionBreakdownDisplayRowDetail> getDetail() {
        if (detail == null) {
            return Optional.empty();
        }
        return detail;
    }

    /**
     * @return Which row this is. Render a kind you know from <code>detail</code> in your own wording; render one you do not from <code>label</code> and <code>text</code> verbatim — never drop it.
     */
    @JsonProperty("kind")
    public CheckoutSessionBreakdownDisplayRowKind getKind() {
        return kind;
    }

    /**
     * @return The row's left side in English — the fallback for a kind the surface cannot name. Absent on a row that is a single line rather than a label/value pair (the headline's context).
     */
    @JsonProperty("label")
    public Optional<String> getLabel() {
        return label;
    }

    /**
     * @return Present only on a row whose figure is still being calculated — render it in a loading state and expect <code>calculate_breakdown</code> to resolve it.
     */
    @JsonProperty("status")
    public Optional<CheckoutSessionBreakdownDisplayRowStatus> getStatus() {
        return status;
    }

    /**
     * @return The row's value (or its whole line) in English — the fallback for a kind the surface cannot name. Absent while <code>status</code> is <code>pending</code>.
     */
    @JsonProperty("text")
    public Optional<String> getText() {
        return text;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("amount")
    private Optional<Money> _getAmount() {
        return amount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("detail")
    private Optional<CheckoutSessionBreakdownDisplayRowDetail> _getDetail() {
        return detail;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionBreakdownDisplayRow
                && equalTo((CheckoutSessionBreakdownDisplayRow) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionBreakdownDisplayRow other) {
        return amount.equals(other.amount)
                && detail.equals(other.detail)
                && kind.equals(other.kind)
                && label.equals(other.label)
                && status.equals(other.status)
                && text.equals(other.text);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.amount, this.detail, this.kind, this.label, this.status, this.text);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static KindStage builder() {
        return new Builder();
    }

    public interface KindStage {
        /**
         * <p>Which row this is. Render a kind you know from <code>detail</code> in your own wording; render one you do not from <code>label</code> and <code>text</code> verbatim — never drop it.</p>
         */
        _FinalStage kind(@NotNull CheckoutSessionBreakdownDisplayRowKind kind);

        Builder from(CheckoutSessionBreakdownDisplayRow other);
    }

    public interface _FinalStage {
        CheckoutSessionBreakdownDisplayRow build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The row's figure as a Money object, when it has one — what a surface formats in the buyer's locale.</p>
         */
        _FinalStage amount(Optional<Money> amount);

        _FinalStage amount(Money amount);

        _FinalStage amount(Nullable<Money> amount);

        /**
         * <p>The typed parameters this row's wording is built from — the localization rail. Absent when the kind needs none.</p>
         */
        _FinalStage detail(Optional<CheckoutSessionBreakdownDisplayRowDetail> detail);

        _FinalStage detail(CheckoutSessionBreakdownDisplayRowDetail detail);

        _FinalStage detail(Nullable<CheckoutSessionBreakdownDisplayRowDetail> detail);

        /**
         * <p>The row's left side in English — the fallback for a kind the surface cannot name. Absent on a row that is a single line rather than a label/value pair (the headline's context).</p>
         */
        _FinalStage label(Optional<String> label);

        _FinalStage label(String label);

        /**
         * <p>Present only on a row whose figure is still being calculated — render it in a loading state and expect <code>calculate_breakdown</code> to resolve it.</p>
         */
        _FinalStage status(Optional<CheckoutSessionBreakdownDisplayRowStatus> status);

        _FinalStage status(CheckoutSessionBreakdownDisplayRowStatus status);

        /**
         * <p>The row's value (or its whole line) in English — the fallback for a kind the surface cannot name. Absent while <code>status</code> is <code>pending</code>.</p>
         */
        _FinalStage text(Optional<String> text);

        _FinalStage text(String text);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements KindStage, _FinalStage {
        private CheckoutSessionBreakdownDisplayRowKind kind;

        private Optional<String> text = Optional.empty();

        private Optional<CheckoutSessionBreakdownDisplayRowStatus> status = Optional.empty();

        private Optional<String> label = Optional.empty();

        private Optional<CheckoutSessionBreakdownDisplayRowDetail> detail = Optional.empty();

        private Optional<Money> amount = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionBreakdownDisplayRow other) {
            amount(other.getAmount());
            detail(other.getDetail());
            kind(other.getKind());
            label(other.getLabel());
            status(other.getStatus());
            text(other.getText());
            return this;
        }

        /**
         * <p>Which row this is. Render a kind you know from <code>detail</code> in your own wording; render one you do not from <code>label</code> and <code>text</code> verbatim — never drop it.</p>
         * <p>Which row this is. Render a kind you know from <code>detail</code> in your own wording; render one you do not from <code>label</code> and <code>text</code> verbatim — never drop it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("kind")
        public _FinalStage kind(@NotNull CheckoutSessionBreakdownDisplayRowKind kind) {
            this.kind = Objects.requireNonNull(kind, "kind must not be null");
            return this;
        }

        /**
         * <p>The row's value (or its whole line) in English — the fallback for a kind the surface cannot name. Absent while <code>status</code> is <code>pending</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage text(String text) {
            this.text = Optional.ofNullable(text);
            return this;
        }

        /**
         * <p>The row's value (or its whole line) in English — the fallback for a kind the surface cannot name. Absent while <code>status</code> is <code>pending</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "text", nulls = Nulls.SKIP)
        public _FinalStage text(Optional<String> text) {
            this.text = text;
            return this;
        }

        /**
         * <p>Present only on a row whose figure is still being calculated — render it in a loading state and expect <code>calculate_breakdown</code> to resolve it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage status(CheckoutSessionBreakdownDisplayRowStatus status) {
            this.status = Optional.ofNullable(status);
            return this;
        }

        /**
         * <p>Present only on a row whose figure is still being calculated — render it in a loading state and expect <code>calculate_breakdown</code> to resolve it.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "status", nulls = Nulls.SKIP)
        public _FinalStage status(Optional<CheckoutSessionBreakdownDisplayRowStatus> status) {
            this.status = status;
            return this;
        }

        /**
         * <p>The row's left side in English — the fallback for a kind the surface cannot name. Absent on a row that is a single line rather than a label/value pair (the headline's context).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage label(String label) {
            this.label = Optional.ofNullable(label);
            return this;
        }

        /**
         * <p>The row's left side in English — the fallback for a kind the surface cannot name. Absent on a row that is a single line rather than a label/value pair (the headline's context).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "label", nulls = Nulls.SKIP)
        public _FinalStage label(Optional<String> label) {
            this.label = label;
            return this;
        }

        /**
         * <p>The typed parameters this row's wording is built from — the localization rail. Absent when the kind needs none.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage detail(Nullable<CheckoutSessionBreakdownDisplayRowDetail> detail) {
            if (detail.isNull()) {
                this.detail = null;
            } else if (detail.isEmpty()) {
                this.detail = Optional.empty();
            } else {
                this.detail = Optional.of(detail.get());
            }
            return this;
        }

        /**
         * <p>The typed parameters this row's wording is built from — the localization rail. Absent when the kind needs none.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage detail(CheckoutSessionBreakdownDisplayRowDetail detail) {
            this.detail = Optional.ofNullable(detail);
            return this;
        }

        /**
         * <p>The typed parameters this row's wording is built from — the localization rail. Absent when the kind needs none.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "detail", nulls = Nulls.SKIP)
        public _FinalStage detail(Optional<CheckoutSessionBreakdownDisplayRowDetail> detail) {
            this.detail = detail;
            return this;
        }

        /**
         * <p>The row's figure as a Money object, when it has one — what a surface formats in the buyer's locale.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amount(Nullable<Money> amount) {
            if (amount.isNull()) {
                this.amount = null;
            } else if (amount.isEmpty()) {
                this.amount = Optional.empty();
            } else {
                this.amount = Optional.of(amount.get());
            }
            return this;
        }

        /**
         * <p>The row's figure as a Money object, when it has one — what a surface formats in the buyer's locale.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amount(Money amount) {
            this.amount = Optional.ofNullable(amount);
            return this;
        }

        /**
         * <p>The row's figure as a Money object, when it has one — what a surface formats in the buyer's locale.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount", nulls = Nulls.SKIP)
        public _FinalStage amount(Optional<Money> amount) {
            this.amount = amount;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionBreakdownDisplayRow build() {
            return new CheckoutSessionBreakdownDisplayRow(
                    amount, detail, kind, label, status, text, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
