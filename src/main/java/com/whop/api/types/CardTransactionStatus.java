/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CardTransactionStatus {
    public static final CardTransactionStatus DECLINED = new CardTransactionStatus(Value.DECLINED, "declined");

    public static final CardTransactionStatus PENDING = new CardTransactionStatus(Value.PENDING, "pending");

    public static final CardTransactionStatus COMPLETED = new CardTransactionStatus(Value.COMPLETED, "completed");

    public static final CardTransactionStatus REVERSED = new CardTransactionStatus(Value.REVERSED, "reversed");

    private final Value value;

    private final String string;

    CardTransactionStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CardTransactionStatus
                        && this.string.equals(((CardTransactionStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DECLINED:
                return visitor.visitDeclined();
            case PENDING:
                return visitor.visitPending();
            case COMPLETED:
                return visitor.visitCompleted();
            case REVERSED:
                return visitor.visitReversed();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CardTransactionStatus valueOf(String value) {
        switch (value) {
            case "declined":
                return DECLINED;
            case "pending":
                return PENDING;
            case "completed":
                return COMPLETED;
            case "reversed":
                return REVERSED;
            default:
                return new CardTransactionStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        COMPLETED,

        REVERSED,

        DECLINED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitCompleted();

        T visitReversed();

        T visitDeclined();

        T visitUnknown(String unknownType);
    }
}
