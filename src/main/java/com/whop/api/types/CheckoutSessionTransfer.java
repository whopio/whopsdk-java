/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionTransfer.Builder.class)
public final class CheckoutSessionTransfer {
    private final String code;

    private final Optional<String> originExpiresAt;

    private final Optional<String> originRenewalPeriodEnd;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionTransfer(
            String code,
            Optional<String> originExpiresAt,
            Optional<String> originRenewalPeriodEnd,
            Map<String, Object> additionalProperties) {
        this.code = code;
        this.originExpiresAt = originExpiresAt;
        this.originRenewalPeriodEnd = originRenewalPeriodEnd;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The 40-character transfer code this checkout was opened from — the same value the link carried.
     */
    @JsonProperty("code")
    public String getCode() {
        return code;
    }

    /**
     * @return When the transferred membership's access expires, as an ISO 8601 timestamp, or <code>null</code> when it does not. The recipient inherits this — a transfer moves the remaining time, it does not restart it.
     */
    @JsonIgnore
    public Optional<String> getOriginExpiresAt() {
        if (originExpiresAt == null) {
            return Optional.empty();
        }
        return originExpiresAt;
    }

    /**
     * @return When the transferred membership's current billing period ends, as an ISO 8601 timestamp, or <code>null</code> for a non-renewing membership. The recipient's first renewal charge falls here — nothing is charged at the transfer itself.
     */
    @JsonIgnore
    public Optional<String> getOriginRenewalPeriodEnd() {
        if (originRenewalPeriodEnd == null) {
            return Optional.empty();
        }
        return originRenewalPeriodEnd;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("origin_expires_at")
    private Optional<String> _getOriginExpiresAt() {
        return originExpiresAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("origin_renewal_period_end")
    private Optional<String> _getOriginRenewalPeriodEnd() {
        return originRenewalPeriodEnd;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionTransfer && equalTo((CheckoutSessionTransfer) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionTransfer other) {
        return code.equals(other.code)
                && originExpiresAt.equals(other.originExpiresAt)
                && originRenewalPeriodEnd.equals(other.originRenewalPeriodEnd);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.code, this.originExpiresAt, this.originRenewalPeriodEnd);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CodeStage builder() {
        return new Builder();
    }

    public interface CodeStage {
        /**
         * <p>The 40-character transfer code this checkout was opened from — the same value the link carried.</p>
         */
        _FinalStage code(@NotNull String code);

        Builder from(CheckoutSessionTransfer other);
    }

    public interface _FinalStage {
        CheckoutSessionTransfer build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When the transferred membership's access expires, as an ISO 8601 timestamp, or <code>null</code> when it does not. The recipient inherits this — a transfer moves the remaining time, it does not restart it.</p>
         */
        _FinalStage originExpiresAt(Optional<String> originExpiresAt);

        _FinalStage originExpiresAt(String originExpiresAt);

        _FinalStage originExpiresAt(Nullable<String> originExpiresAt);

        /**
         * <p>When the transferred membership's current billing period ends, as an ISO 8601 timestamp, or <code>null</code> for a non-renewing membership. The recipient's first renewal charge falls here — nothing is charged at the transfer itself.</p>
         */
        _FinalStage originRenewalPeriodEnd(Optional<String> originRenewalPeriodEnd);

        _FinalStage originRenewalPeriodEnd(String originRenewalPeriodEnd);

        _FinalStage originRenewalPeriodEnd(Nullable<String> originRenewalPeriodEnd);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements CodeStage, _FinalStage {
        private String code;

        private Optional<String> originRenewalPeriodEnd = Optional.empty();

        private Optional<String> originExpiresAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionTransfer other) {
            code(other.getCode());
            originExpiresAt(other.getOriginExpiresAt());
            originRenewalPeriodEnd(other.getOriginRenewalPeriodEnd());
            return this;
        }

        /**
         * <p>The 40-character transfer code this checkout was opened from — the same value the link carried.</p>
         * <p>The 40-character transfer code this checkout was opened from — the same value the link carried.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("code")
        public _FinalStage code(@NotNull String code) {
            this.code = Objects.requireNonNull(code, "code must not be null");
            return this;
        }

        /**
         * <p>When the transferred membership's current billing period ends, as an ISO 8601 timestamp, or <code>null</code> for a non-renewing membership. The recipient's first renewal charge falls here — nothing is charged at the transfer itself.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage originRenewalPeriodEnd(Nullable<String> originRenewalPeriodEnd) {
            if (originRenewalPeriodEnd.isNull()) {
                this.originRenewalPeriodEnd = null;
            } else if (originRenewalPeriodEnd.isEmpty()) {
                this.originRenewalPeriodEnd = Optional.empty();
            } else {
                this.originRenewalPeriodEnd = Optional.of(originRenewalPeriodEnd.get());
            }
            return this;
        }

        /**
         * <p>When the transferred membership's current billing period ends, as an ISO 8601 timestamp, or <code>null</code> for a non-renewing membership. The recipient's first renewal charge falls here — nothing is charged at the transfer itself.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage originRenewalPeriodEnd(String originRenewalPeriodEnd) {
            this.originRenewalPeriodEnd = Optional.ofNullable(originRenewalPeriodEnd);
            return this;
        }

        /**
         * <p>When the transferred membership's current billing period ends, as an ISO 8601 timestamp, or <code>null</code> for a non-renewing membership. The recipient's first renewal charge falls here — nothing is charged at the transfer itself.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "origin_renewal_period_end", nulls = Nulls.SKIP)
        public _FinalStage originRenewalPeriodEnd(Optional<String> originRenewalPeriodEnd) {
            this.originRenewalPeriodEnd = originRenewalPeriodEnd;
            return this;
        }

        /**
         * <p>When the transferred membership's access expires, as an ISO 8601 timestamp, or <code>null</code> when it does not. The recipient inherits this — a transfer moves the remaining time, it does not restart it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage originExpiresAt(Nullable<String> originExpiresAt) {
            if (originExpiresAt.isNull()) {
                this.originExpiresAt = null;
            } else if (originExpiresAt.isEmpty()) {
                this.originExpiresAt = Optional.empty();
            } else {
                this.originExpiresAt = Optional.of(originExpiresAt.get());
            }
            return this;
        }

        /**
         * <p>When the transferred membership's access expires, as an ISO 8601 timestamp, or <code>null</code> when it does not. The recipient inherits this — a transfer moves the remaining time, it does not restart it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage originExpiresAt(String originExpiresAt) {
            this.originExpiresAt = Optional.ofNullable(originExpiresAt);
            return this;
        }

        /**
         * <p>When the transferred membership's access expires, as an ISO 8601 timestamp, or <code>null</code> when it does not. The recipient inherits this — a transfer moves the remaining time, it does not restart it.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "origin_expires_at", nulls = Nulls.SKIP)
        public _FinalStage originExpiresAt(Optional<String> originExpiresAt) {
            this.originExpiresAt = originExpiresAt;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionTransfer build() {
            return new CheckoutSessionTransfer(code, originExpiresAt, originRenewalPeriodEnd, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
