/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = NotificationTopic.Builder.class)
public final class NotificationTopic {
    private final boolean defaultPreferenceValue;

    private final Optional<String> description;

    private final String id;

    private final String identifier;

    private final boolean isMention;

    private final String name;

    private final NotificationTopicTopicType topicType;

    private final Map<String, Object> additionalProperties;

    private NotificationTopic(
            boolean defaultPreferenceValue,
            Optional<String> description,
            String id,
            String identifier,
            boolean isMention,
            String name,
            NotificationTopicTopicType topicType,
            Map<String, Object> additionalProperties) {
        this.defaultPreferenceValue = defaultPreferenceValue;
        this.description = description;
        this.id = id;
        this.identifier = identifier;
        this.isMention = isMention;
        this.name = name;
        this.topicType = topicType;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether notifications for this topic are enabled by default when the user has not set a preference.
     */
    @JsonProperty("default_preference_value")
    public boolean getDefaultPreferenceValue() {
        return defaultPreferenceValue;
    }

    /**
     * @return Human-readable explanation of what notifications in this topic are about. <code>null</code> when no description has been set.
     */
    @JsonIgnore
    public Optional<String> getDescription() {
        if (description == null) {
            return Optional.empty();
        }
        return description;
    }

    /**
     * @return Notification topic ID, prefixed <code>topic_</code>. This is the value the notification preference endpoints take as <code>topic_id</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Stable, human-readable name for the category, such as <code>new-follower</code>. Unlike <code>id</code>, it is the same in every environment, which makes it the value to match on in code and to read in logs. Treat it as an opaque string: the set is open and the casing is historical rather than normalized.
     */
    @JsonProperty("identifier")
    public String getIdentifier() {
        return identifier;
    }

    /**
     * @return Whether this topic exclusively handles mention-based notifications.
     */
    @JsonProperty("is_mention")
    public boolean getIsMention() {
        return isMention;
    }

    /**
     * @return Display name shown in notification preference settings.
     */
    @JsonProperty("name")
    public String getName() {
        return name;
    }

    /**
     * @return Scope of the topic: whether it applies to an account, a user, or an account's team.
     */
    @JsonProperty("topic_type")
    public NotificationTopicTopicType getTopicType() {
        return topicType;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("description")
    private Optional<String> _getDescription() {
        return description;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof NotificationTopic && equalTo((NotificationTopic) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(NotificationTopic other) {
        return defaultPreferenceValue == other.defaultPreferenceValue
                && description.equals(other.description)
                && id.equals(other.id)
                && identifier.equals(other.identifier)
                && isMention == other.isMention
                && name.equals(other.name)
                && topicType.equals(other.topicType);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.defaultPreferenceValue,
                this.description,
                this.id,
                this.identifier,
                this.isMention,
                this.name,
                this.topicType);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static DefaultPreferenceValueStage builder() {
        return new Builder();
    }

    public interface DefaultPreferenceValueStage {
        /**
         * <p>Whether notifications for this topic are enabled by default when the user has not set a preference.</p>
         */
        IdStage defaultPreferenceValue(boolean defaultPreferenceValue);

        Builder from(NotificationTopic other);
    }

    public interface IdStage {
        /**
         * <p>Notification topic ID, prefixed <code>topic_</code>. This is the value the notification preference endpoints take as <code>topic_id</code>.</p>
         */
        IdentifierStage id(@NotNull String id);
    }

    public interface IdentifierStage {
        /**
         * <p>Stable, human-readable name for the category, such as <code>new-follower</code>. Unlike <code>id</code>, it is the same in every environment, which makes it the value to match on in code and to read in logs. Treat it as an opaque string: the set is open and the casing is historical rather than normalized.</p>
         */
        IsMentionStage identifier(@NotNull String identifier);
    }

    public interface IsMentionStage {
        /**
         * <p>Whether this topic exclusively handles mention-based notifications.</p>
         */
        NameStage isMention(boolean isMention);
    }

    public interface NameStage {
        /**
         * <p>Display name shown in notification preference settings.</p>
         */
        TopicTypeStage name(@NotNull String name);
    }

    public interface TopicTypeStage {
        /**
         * <p>Scope of the topic: whether it applies to an account, a user, or an account's team.</p>
         */
        _FinalStage topicType(@NotNull NotificationTopicTopicType topicType);
    }

    public interface _FinalStage {
        NotificationTopic build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Human-readable explanation of what notifications in this topic are about. <code>null</code> when no description has been set.</p>
         */
        _FinalStage description(Optional<String> description);

        _FinalStage description(String description);

        _FinalStage description(Nullable<String> description);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements DefaultPreferenceValueStage,
                    IdStage,
                    IdentifierStage,
                    IsMentionStage,
                    NameStage,
                    TopicTypeStage,
                    _FinalStage {
        private boolean defaultPreferenceValue;

        private String id;

        private String identifier;

        private boolean isMention;

        private String name;

        private NotificationTopicTopicType topicType;

        private Optional<String> description = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(NotificationTopic other) {
            defaultPreferenceValue(other.getDefaultPreferenceValue());
            description(other.getDescription());
            id(other.getId());
            identifier(other.getIdentifier());
            isMention(other.getIsMention());
            name(other.getName());
            topicType(other.getTopicType());
            return this;
        }

        /**
         * <p>Whether notifications for this topic are enabled by default when the user has not set a preference.</p>
         * <p>Whether notifications for this topic are enabled by default when the user has not set a preference.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("default_preference_value")
        public IdStage defaultPreferenceValue(boolean defaultPreferenceValue) {
            this.defaultPreferenceValue = defaultPreferenceValue;
            return this;
        }

        /**
         * <p>Notification topic ID, prefixed <code>topic_</code>. This is the value the notification preference endpoints take as <code>topic_id</code>.</p>
         * <p>Notification topic ID, prefixed <code>topic_</code>. This is the value the notification preference endpoints take as <code>topic_id</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public IdentifierStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Stable, human-readable name for the category, such as <code>new-follower</code>. Unlike <code>id</code>, it is the same in every environment, which makes it the value to match on in code and to read in logs. Treat it as an opaque string: the set is open and the casing is historical rather than normalized.</p>
         * <p>Stable, human-readable name for the category, such as <code>new-follower</code>. Unlike <code>id</code>, it is the same in every environment, which makes it the value to match on in code and to read in logs. Treat it as an opaque string: the set is open and the casing is historical rather than normalized.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("identifier")
        public IsMentionStage identifier(@NotNull String identifier) {
            this.identifier = Objects.requireNonNull(identifier, "identifier must not be null");
            return this;
        }

        /**
         * <p>Whether this topic exclusively handles mention-based notifications.</p>
         * <p>Whether this topic exclusively handles mention-based notifications.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("is_mention")
        public NameStage isMention(boolean isMention) {
            this.isMention = isMention;
            return this;
        }

        /**
         * <p>Display name shown in notification preference settings.</p>
         * <p>Display name shown in notification preference settings.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("name")
        public TopicTypeStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>Scope of the topic: whether it applies to an account, a user, or an account's team.</p>
         * <p>Scope of the topic: whether it applies to an account, a user, or an account's team.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("topic_type")
        public _FinalStage topicType(@NotNull NotificationTopicTopicType topicType) {
            this.topicType = Objects.requireNonNull(topicType, "topicType must not be null");
            return this;
        }

        /**
         * <p>Human-readable explanation of what notifications in this topic are about. <code>null</code> when no description has been set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage description(Nullable<String> description) {
            if (description.isNull()) {
                this.description = null;
            } else if (description.isEmpty()) {
                this.description = Optional.empty();
            } else {
                this.description = Optional.of(description.get());
            }
            return this;
        }

        /**
         * <p>Human-readable explanation of what notifications in this topic are about. <code>null</code> when no description has been set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage description(String description) {
            this.description = Optional.ofNullable(description);
            return this;
        }

        /**
         * <p>Human-readable explanation of what notifications in this topic are about. <code>null</code> when no description has been set.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "description", nulls = Nulls.SKIP)
        public _FinalStage description(Optional<String> description) {
            this.description = description;
            return this;
        }

        @java.lang.Override
        public NotificationTopic build() {
            return new NotificationTopic(
                    defaultPreferenceValue,
                    description,
                    id,
                    identifier,
                    isMention,
                    name,
                    topicType,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
