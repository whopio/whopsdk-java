/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class LedgerTypes {
    public static final LedgerTypes POOL = new LedgerTypes(Value.POOL, "pool");

    public static final LedgerTypes PRIMARY = new LedgerTypes(Value.PRIMARY, "primary");

    private final Value value;

    private final String string;

    LedgerTypes(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof LedgerTypes && this.string.equals(((LedgerTypes) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case POOL:
                return visitor.visitPool();
            case PRIMARY:
                return visitor.visitPrimary();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static LedgerTypes valueOf(String value) {
        switch (value) {
            case "pool":
                return POOL;
            case "primary":
                return PRIMARY;
            default:
                return new LedgerTypes(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PRIMARY,

        POOL,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPrimary();

        T visitPool();

        T visitUnknown(String unknownType);
    }
}
