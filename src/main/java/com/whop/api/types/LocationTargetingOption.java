/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = LocationTargetingOption.Builder.class)
public final class LocationTargetingOption {
    private final Optional<String> code;

    private final Optional<String> countryCode;

    private final Optional<String> countryName;

    private final String key;

    private final LocationTargetingOptionLocationType locationType;

    private final String name;

    private final Optional<String> region;

    private final Map<String, Object> additionalProperties;

    private LocationTargetingOption(
            Optional<String> code,
            Optional<String> countryCode,
            Optional<String> countryName,
            String key,
            LocationTargetingOptionLocationType locationType,
            String name,
            Optional<String> region,
            Map<String, Object> additionalProperties) {
        this.code = code;
        this.countryCode = countryCode;
        this.countryName = countryName;
        this.key = key;
        this.locationType = locationType;
        this.name = name;
        this.region = region;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The standardized code the ad-group <code>regions</code> field takes: an ISO 3166-1 code for countries (<code>US</code>) or an ISO 3166-2 code for states and provinces (<code>US-CA</code>, <code>CA-ON</code>). Null for a location that has no standard code, such as a city or a metro area — target those by <code>key</code> in the <code>regions</code> cities list instead.
     */
    @JsonIgnore
    public Optional<String> getCode() {
        if (code == null) {
            return Optional.empty();
        }
        return code;
    }

    /**
     * @return ISO 3166-1 code of the country the location sits in.
     */
    @JsonIgnore
    public Optional<String> getCountryCode() {
        if (countryCode == null) {
            return Optional.empty();
        }
        return countryCode;
    }

    /**
     * @return Name of the country the location sits in.
     */
    @JsonIgnore
    public Optional<String> getCountryName() {
        if (countryName == null) {
            return Optional.empty();
        }
        return countryName;
    }

    /**
     * @return The ad platform's key for the location in its location taxonomy. Use it as the <code>key</code> of a <code>regions</code> city or zip entry.
     */
    @JsonProperty("key")
    public String getKey() {
        return key;
    }

    /**
     * @return Granularity of the location. Which of these an ad platform reports depends on how finely it divides its location taxonomy.
     */
    @JsonProperty("location_type")
    public LocationTargetingOptionLocationType getLocationType() {
        return locationType;
    }

    /**
     * @return Display name, such as <code>California</code>.
     */
    @JsonProperty("name")
    public String getName() {
        return name;
    }

    /**
     * @return Name of the state or province a city sits in. Null for everything but cities.
     */
    @JsonIgnore
    public Optional<String> getRegion() {
        if (region == null) {
            return Optional.empty();
        }
        return region;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("code")
    private Optional<String> _getCode() {
        return code;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("country_code")
    private Optional<String> _getCountryCode() {
        return countryCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("country_name")
    private Optional<String> _getCountryName() {
        return countryName;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("region")
    private Optional<String> _getRegion() {
        return region;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof LocationTargetingOption && equalTo((LocationTargetingOption) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(LocationTargetingOption other) {
        return code.equals(other.code)
                && countryCode.equals(other.countryCode)
                && countryName.equals(other.countryName)
                && key.equals(other.key)
                && locationType.equals(other.locationType)
                && name.equals(other.name)
                && region.equals(other.region);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.code, this.countryCode, this.countryName, this.key, this.locationType, this.name, this.region);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static KeyStage builder() {
        return new Builder();
    }

    public interface KeyStage {
        /**
         * <p>The ad platform's key for the location in its location taxonomy. Use it as the <code>key</code> of a <code>regions</code> city or zip entry.</p>
         */
        LocationTypeStage key(@NotNull String key);

        Builder from(LocationTargetingOption other);
    }

    public interface LocationTypeStage {
        /**
         * <p>Granularity of the location. Which of these an ad platform reports depends on how finely it divides its location taxonomy.</p>
         */
        NameStage locationType(@NotNull LocationTargetingOptionLocationType locationType);
    }

    public interface NameStage {
        /**
         * <p>Display name, such as <code>California</code>.</p>
         */
        _FinalStage name(@NotNull String name);
    }

    public interface _FinalStage {
        LocationTargetingOption build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The standardized code the ad-group <code>regions</code> field takes: an ISO 3166-1 code for countries (<code>US</code>) or an ISO 3166-2 code for states and provinces (<code>US-CA</code>, <code>CA-ON</code>). Null for a location that has no standard code, such as a city or a metro area — target those by <code>key</code> in the <code>regions</code> cities list instead.</p>
         */
        _FinalStage code(Optional<String> code);

        _FinalStage code(String code);

        _FinalStage code(Nullable<String> code);

        /**
         * <p>ISO 3166-1 code of the country the location sits in.</p>
         */
        _FinalStage countryCode(Optional<String> countryCode);

        _FinalStage countryCode(String countryCode);

        _FinalStage countryCode(Nullable<String> countryCode);

        /**
         * <p>Name of the country the location sits in.</p>
         */
        _FinalStage countryName(Optional<String> countryName);

        _FinalStage countryName(String countryName);

        _FinalStage countryName(Nullable<String> countryName);

        /**
         * <p>Name of the state or province a city sits in. Null for everything but cities.</p>
         */
        _FinalStage region(Optional<String> region);

        _FinalStage region(String region);

        _FinalStage region(Nullable<String> region);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements KeyStage, LocationTypeStage, NameStage, _FinalStage {
        private String key;

        private LocationTargetingOptionLocationType locationType;

        private String name;

        private Optional<String> region = Optional.empty();

        private Optional<String> countryName = Optional.empty();

        private Optional<String> countryCode = Optional.empty();

        private Optional<String> code = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(LocationTargetingOption other) {
            code(other.getCode());
            countryCode(other.getCountryCode());
            countryName(other.getCountryName());
            key(other.getKey());
            locationType(other.getLocationType());
            name(other.getName());
            region(other.getRegion());
            return this;
        }

        /**
         * <p>The ad platform's key for the location in its location taxonomy. Use it as the <code>key</code> of a <code>regions</code> city or zip entry.</p>
         * <p>The ad platform's key for the location in its location taxonomy. Use it as the <code>key</code> of a <code>regions</code> city or zip entry.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("key")
        public LocationTypeStage key(@NotNull String key) {
            this.key = Objects.requireNonNull(key, "key must not be null");
            return this;
        }

        /**
         * <p>Granularity of the location. Which of these an ad platform reports depends on how finely it divides its location taxonomy.</p>
         * <p>Granularity of the location. Which of these an ad platform reports depends on how finely it divides its location taxonomy.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("location_type")
        public NameStage locationType(@NotNull LocationTargetingOptionLocationType locationType) {
            this.locationType = Objects.requireNonNull(locationType, "locationType must not be null");
            return this;
        }

        /**
         * <p>Display name, such as <code>California</code>.</p>
         * <p>Display name, such as <code>California</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("name")
        public _FinalStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>Name of the state or province a city sits in. Null for everything but cities.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage region(Nullable<String> region) {
            if (region.isNull()) {
                this.region = null;
            } else if (region.isEmpty()) {
                this.region = Optional.empty();
            } else {
                this.region = Optional.of(region.get());
            }
            return this;
        }

        /**
         * <p>Name of the state or province a city sits in. Null for everything but cities.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage region(String region) {
            this.region = Optional.ofNullable(region);
            return this;
        }

        /**
         * <p>Name of the state or province a city sits in. Null for everything but cities.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "region", nulls = Nulls.SKIP)
        public _FinalStage region(Optional<String> region) {
            this.region = region;
            return this;
        }

        /**
         * <p>Name of the country the location sits in.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage countryName(Nullable<String> countryName) {
            if (countryName.isNull()) {
                this.countryName = null;
            } else if (countryName.isEmpty()) {
                this.countryName = Optional.empty();
            } else {
                this.countryName = Optional.of(countryName.get());
            }
            return this;
        }

        /**
         * <p>Name of the country the location sits in.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage countryName(String countryName) {
            this.countryName = Optional.ofNullable(countryName);
            return this;
        }

        /**
         * <p>Name of the country the location sits in.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "country_name", nulls = Nulls.SKIP)
        public _FinalStage countryName(Optional<String> countryName) {
            this.countryName = countryName;
            return this;
        }

        /**
         * <p>ISO 3166-1 code of the country the location sits in.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage countryCode(Nullable<String> countryCode) {
            if (countryCode.isNull()) {
                this.countryCode = null;
            } else if (countryCode.isEmpty()) {
                this.countryCode = Optional.empty();
            } else {
                this.countryCode = Optional.of(countryCode.get());
            }
            return this;
        }

        /**
         * <p>ISO 3166-1 code of the country the location sits in.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage countryCode(String countryCode) {
            this.countryCode = Optional.ofNullable(countryCode);
            return this;
        }

        /**
         * <p>ISO 3166-1 code of the country the location sits in.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "country_code", nulls = Nulls.SKIP)
        public _FinalStage countryCode(Optional<String> countryCode) {
            this.countryCode = countryCode;
            return this;
        }

        /**
         * <p>The standardized code the ad-group <code>regions</code> field takes: an ISO 3166-1 code for countries (<code>US</code>) or an ISO 3166-2 code for states and provinces (<code>US-CA</code>, <code>CA-ON</code>). Null for a location that has no standard code, such as a city or a metro area — target those by <code>key</code> in the <code>regions</code> cities list instead.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage code(Nullable<String> code) {
            if (code.isNull()) {
                this.code = null;
            } else if (code.isEmpty()) {
                this.code = Optional.empty();
            } else {
                this.code = Optional.of(code.get());
            }
            return this;
        }

        /**
         * <p>The standardized code the ad-group <code>regions</code> field takes: an ISO 3166-1 code for countries (<code>US</code>) or an ISO 3166-2 code for states and provinces (<code>US-CA</code>, <code>CA-ON</code>). Null for a location that has no standard code, such as a city or a metro area — target those by <code>key</code> in the <code>regions</code> cities list instead.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage code(String code) {
            this.code = Optional.ofNullable(code);
            return this;
        }

        /**
         * <p>The standardized code the ad-group <code>regions</code> field takes: an ISO 3166-1 code for countries (<code>US</code>) or an ISO 3166-2 code for states and provinces (<code>US-CA</code>, <code>CA-ON</code>). Null for a location that has no standard code, such as a city or a metro area — target those by <code>key</code> in the <code>regions</code> cities list instead.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "code", nulls = Nulls.SKIP)
        public _FinalStage code(Optional<String> code) {
            this.code = code;
            return this;
        }

        @java.lang.Override
        public LocationTargetingOption build() {
            return new LocationTargetingOption(
                    code, countryCode, countryName, key, locationType, name, region, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
