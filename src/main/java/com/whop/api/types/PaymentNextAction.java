/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class PaymentNextAction {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PaymentNextAction(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PaymentNextAction awaitConfirmation(PaymentNextActionAwaitConfirmation value) {
        return new PaymentNextAction(new AwaitConfirmationValue(value));
    }

    public static PaymentNextAction displayInstructions(PaymentNextActionDisplayInstructions value) {
        return new PaymentNextAction(new DisplayInstructionsValue(value));
    }

    public static PaymentNextAction redirect(PaymentNextActionRedirect value) {
        return new PaymentNextAction(new RedirectValue(value));
    }

    public boolean isAwaitConfirmation() {
        return value instanceof AwaitConfirmationValue;
    }

    public boolean isDisplayInstructions() {
        return value instanceof DisplayInstructionsValue;
    }

    public boolean isRedirect() {
        return value instanceof RedirectValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<PaymentNextActionAwaitConfirmation> getAwaitConfirmation() {
        if (isAwaitConfirmation()) {
            return Optional.of(((AwaitConfirmationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PaymentNextActionDisplayInstructions> getDisplayInstructions() {
        if (isDisplayInstructions()) {
            return Optional.of(((DisplayInstructionsValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PaymentNextActionRedirect> getRedirect() {
        if (isRedirect()) {
            return Optional.of(((RedirectValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentNextAction && value.equals(((PaymentNextAction) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAwaitConfirmation(PaymentNextActionAwaitConfirmation awaitConfirmation);

        T visitDisplayInstructions(PaymentNextActionDisplayInstructions displayInstructions);

        T visitRedirect(PaymentNextActionRedirect redirect);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(AwaitConfirmationValue.class),
        @JsonSubTypes.Type(DisplayInstructionsValue.class),
        @JsonSubTypes.Type(RedirectValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("await_confirmation")
    @JsonIgnoreProperties("type")
    private static final class AwaitConfirmationValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private PaymentNextActionAwaitConfirmation value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AwaitConfirmationValue() {}

        private AwaitConfirmationValue(PaymentNextActionAwaitConfirmation value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAwaitConfirmation(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AwaitConfirmationValue && equalTo((AwaitConfirmationValue) other);
        }

        private boolean equalTo(AwaitConfirmationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("display_instructions")
    @JsonIgnoreProperties("type")
    private static final class DisplayInstructionsValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private PaymentNextActionDisplayInstructions value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private DisplayInstructionsValue() {}

        private DisplayInstructionsValue(PaymentNextActionDisplayInstructions value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitDisplayInstructions(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof DisplayInstructionsValue && equalTo((DisplayInstructionsValue) other);
        }

        private boolean equalTo(DisplayInstructionsValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("redirect")
    @JsonIgnoreProperties("type")
    private static final class RedirectValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private PaymentNextActionRedirect value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RedirectValue() {}

        private RedirectValue(PaymentNextActionRedirect value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRedirect(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RedirectValue && equalTo((RedirectValue) other);
        }

        private boolean equalTo(RedirectValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentNextAction{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentNextAction{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
