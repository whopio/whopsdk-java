/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.whop.api.core.ObjectMappers;
import java.io.IOException;
import java.util.Map;
import java.util.Objects;

@JsonDeserialize(using = LedgerActivityResource.Deserializer.class)
public final class LedgerActivityResource {
    private final Object value;

    private final int type;

    private LedgerActivityResource(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((LedgerActivityResourceLogoUrl) this.value);
        } else if (this.type == 1) {
            return visitor.visit((LedgerActivityResourceName) this.value);
        } else if (this.type == 2) {
            return visitor.visit((LedgerActivityResourceTwo) this.value);
        } else if (this.type == 3) {
            return visitor.visit((LedgerActivityResourceOwner) this.value);
        } else if (this.type == 4) {
            return visitor.visit((LedgerActivityResourceBank) this.value);
        } else if (this.type == 5) {
            return visitor.visit((LedgerActivityResourceAccountReference) this.value);
        } else if (this.type == 6) {
            return visitor.visit((LedgerActivityResourceAuthorizedAt) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof LedgerActivityResource && equalTo((LedgerActivityResource) other);
    }

    private boolean equalTo(LedgerActivityResource other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static LedgerActivityResource of(LedgerActivityResourceLogoUrl value) {
        return new LedgerActivityResource(value, 0);
    }

    public static LedgerActivityResource of(LedgerActivityResourceName value) {
        return new LedgerActivityResource(value, 1);
    }

    public static LedgerActivityResource of(LedgerActivityResourceTwo value) {
        return new LedgerActivityResource(value, 2);
    }

    public static LedgerActivityResource of(LedgerActivityResourceOwner value) {
        return new LedgerActivityResource(value, 3);
    }

    public static LedgerActivityResource of(LedgerActivityResourceBank value) {
        return new LedgerActivityResource(value, 4);
    }

    public static LedgerActivityResource of(LedgerActivityResourceAccountReference value) {
        return new LedgerActivityResource(value, 5);
    }

    public static LedgerActivityResource of(LedgerActivityResourceAuthorizedAt value) {
        return new LedgerActivityResource(value, 6);
    }

    public interface Visitor<T> {
        T visit(LedgerActivityResourceLogoUrl value);

        T visit(LedgerActivityResourceName value);

        T visit(LedgerActivityResourceTwo value);

        T visit(LedgerActivityResourceOwner value);

        T visit(LedgerActivityResourceBank value);

        T visit(LedgerActivityResourceAccountReference value);

        T visit(LedgerActivityResourceAuthorizedAt value);
    }

    static final class Deserializer extends StdDeserializer<LedgerActivityResource> {
        Deserializer() {
            super(LedgerActivityResource.class);
        }

        @java.lang.Override
        public LedgerActivityResource deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("id")
                    && ((Map<?, ?>) value).containsKey("object")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LedgerActivityResourceLogoUrl.class));
                } catch (RuntimeException e) {
                }
            }
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("id")
                    && ((Map<?, ?>) value).containsKey("object")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LedgerActivityResourceName.class));
                } catch (RuntimeException e) {
                }
            }
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("id")
                    && ((Map<?, ?>) value).containsKey("object")
                    && ((Map<?, ?>) value).containsKey("status")
                    && ((Map<?, ?>) value).containsKey("title")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LedgerActivityResourceTwo.class));
                } catch (RuntimeException e) {
                }
            }
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("id")
                    && ((Map<?, ?>) value).containsKey("object")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LedgerActivityResourceOwner.class));
                } catch (RuntimeException e) {
                }
            }
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("id")
                    && ((Map<?, ?>) value).containsKey("object")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LedgerActivityResourceBank.class));
                } catch (RuntimeException e) {
                }
            }
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("id")
                    && ((Map<?, ?>) value).containsKey("object")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(
                            value, LedgerActivityResourceAccountReference.class));
                } catch (RuntimeException e) {
                }
            }
            if (value instanceof Map<?, ?>
                    && ((Map<?, ?>) value).containsKey("id")
                    && ((Map<?, ?>) value).containsKey("object")) {
                try {
                    return of(ObjectMappers.JSON_MAPPER.convertValue(value, LedgerActivityResourceAuthorizedAt.class));
                } catch (RuntimeException e) {
                }
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
