/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = MembershipMember.Builder.class)
public final class MembershipMember {
    private final MembershipMemberAccessLevel accessLevel;

    private final Optional<String> lastAccessedAt;

    private final Optional<Double> position;

    private final Map<String, Object> additionalProperties;

    private MembershipMember(
            MembershipMemberAccessLevel accessLevel,
            Optional<String> lastAccessedAt,
            Optional<Double> position,
            Map<String, Object> additionalProperties) {
        this.accessLevel = accessLevel;
        this.lastAccessedAt = lastAccessedAt;
        this.position = position;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.
     */
    @JsonProperty("access_level")
    public MembershipMemberAccessLevel getAccessLevel() {
        return accessLevel;
    }

    /**
     * @return When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.
     */
    @JsonIgnore
    public Optional<String> getLastAccessedAt() {
        if (lastAccessedAt == null) {
            return Optional.empty();
        }
        return lastAccessedAt;
    }

    /**
     * @return The member's sort position in the buyer's own account list. <code>null</code> until they arrange it.
     */
    @JsonIgnore
    public Optional<Double> getPosition() {
        if (position == null) {
            return Optional.empty();
        }
        return position;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_accessed_at")
    private Optional<String> _getLastAccessedAt() {
        return lastAccessedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("position")
    private Optional<Double> _getPosition() {
        return position;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof MembershipMember && equalTo((MembershipMember) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(MembershipMember other) {
        return accessLevel.equals(other.accessLevel)
                && lastAccessedAt.equals(other.lastAccessedAt)
                && position.equals(other.position);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.accessLevel, this.lastAccessedAt, this.position);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccessLevelStage builder() {
        return new Builder();
    }

    public interface AccessLevelStage {
        /**
         * <p>What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.</p>
         */
        _FinalStage accessLevel(@NotNull MembershipMemberAccessLevel accessLevel);

        Builder from(MembershipMember other);
    }

    public interface _FinalStage {
        MembershipMember build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         */
        _FinalStage lastAccessedAt(Optional<String> lastAccessedAt);

        _FinalStage lastAccessedAt(String lastAccessedAt);

        _FinalStage lastAccessedAt(Nullable<String> lastAccessedAt);

        /**
         * <p>The member's sort position in the buyer's own account list. <code>null</code> until they arrange it.</p>
         */
        _FinalStage position(Optional<Double> position);

        _FinalStage position(Double position);

        _FinalStage position(Nullable<Double> position);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AccessLevelStage, _FinalStage {
        private MembershipMemberAccessLevel accessLevel;

        private Optional<Double> position = Optional.empty();

        private Optional<String> lastAccessedAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(MembershipMember other) {
            accessLevel(other.getAccessLevel());
            lastAccessedAt(other.getLastAccessedAt());
            position(other.getPosition());
            return this;
        }

        /**
         * <p>What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.</p>
         * <p>What the member can reach on the account: <code>customer</code> for paying members, <code>admin</code> for team members, <code>no_access</code> once every grant has lapsed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("access_level")
        public _FinalStage accessLevel(@NotNull MembershipMemberAccessLevel accessLevel) {
            this.accessLevel = Objects.requireNonNull(accessLevel, "accessLevel must not be null");
            return this;
        }

        /**
         * <p>The member's sort position in the buyer's own account list. <code>null</code> until they arrange it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage position(Nullable<Double> position) {
            if (position.isNull()) {
                this.position = null;
            } else if (position.isEmpty()) {
                this.position = Optional.empty();
            } else {
                this.position = Optional.of(position.get());
            }
            return this;
        }

        /**
         * <p>The member's sort position in the buyer's own account list. <code>null</code> until they arrange it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage position(Double position) {
            this.position = Optional.ofNullable(position);
            return this;
        }

        /**
         * <p>The member's sort position in the buyer's own account list. <code>null</code> until they arrange it.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "position", nulls = Nulls.SKIP)
        public _FinalStage position(Optional<Double> position) {
            this.position = position;
            return this;
        }

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastAccessedAt(Nullable<String> lastAccessedAt) {
            if (lastAccessedAt.isNull()) {
                this.lastAccessedAt = null;
            } else if (lastAccessedAt.isEmpty()) {
                this.lastAccessedAt = Optional.empty();
            } else {
                this.lastAccessedAt = Optional.of(lastAccessedAt.get());
            }
            return this;
        }

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastAccessedAt(String lastAccessedAt) {
            this.lastAccessedAt = Optional.ofNullable(lastAccessedAt);
            return this;
        }

        /**
         * <p>When the member last opened the account's content, as an ISO 8601 timestamp. <code>null</code> if they never have.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_accessed_at", nulls = Nulls.SKIP)
        public _FinalStage lastAccessedAt(Optional<String> lastAccessedAt) {
            this.lastAccessedAt = lastAccessedAt;
            return this;
        }

        @java.lang.Override
        public MembershipMember build() {
            return new MembershipMember(accessLevel, lastAccessedAt, position, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
