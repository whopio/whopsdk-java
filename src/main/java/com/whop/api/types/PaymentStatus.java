/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PaymentStatus.Builder.class)
public final class PaymentStatus {
    private final String id;

    private final Optional<PaymentLastPaymentError> lastPaymentError;

    private final Optional<PaymentNextAction> nextAction;

    private final String object;

    private final Optional<PaymentProcessingDetails> processingDetails;

    private final Optional<String> returnUrl;

    private final PaymentStatusStatus status;

    private final Map<String, Object> additionalProperties;

    private PaymentStatus(
            String id,
            Optional<PaymentLastPaymentError> lastPaymentError,
            Optional<PaymentNextAction> nextAction,
            String object,
            Optional<PaymentProcessingDetails> processingDetails,
            Optional<String> returnUrl,
            PaymentStatusStatus status,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.lastPaymentError = lastPaymentError;
        this.nextAction = nextAction;
        this.object = object;
        this.processingDetails = processingDetails;
        this.returnUrl = returnUrl;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The payment this status describes, prefixed <code>pay_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Details of the most recent failed attempt, or <code>null</code> when the payment has not failed.
     */
    @JsonIgnore
    public Optional<PaymentLastPaymentError> getLastPaymentError() {
        if (lastPaymentError == null) {
            return Optional.empty();
        }
        return lastPaymentError;
    }

    /**
     * @return What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.
     */
    @JsonIgnore
    public Optional<PaymentNextAction> getNextAction() {
        if (nextAction == null) {
            return Optional.empty();
        }
        return nextAction;
    }

    /**
     * @return Always <code>payment_status</code>.
     */
    @JsonProperty("object")
    public String getObject() {
        return object;
    }

    /**
     * @return Present while <code>status</code> is <code>processing</code> on a settlement rail, otherwise <code>null</code>.
     */
    @JsonIgnore
    public Optional<PaymentProcessingDetails> getProcessingDetails() {
        if (processingDetails == null) {
            return Optional.empty();
        }
        return processingDetails;
    }

    /**
     * @return Where to send the buyer once the payment reaches a resting state, or <code>null</code> to leave them where they are. Editable until they return — see the return_url operation.
     */
    @JsonIgnore
    public Optional<String> getReturnUrl() {
        if (returnUrl == null) {
            return Optional.empty();
        }
        return returnUrl;
    }

    /**
     * @return How far the payment has got. <code>requires_confirmation</code> — nothing attempted yet, or the last attempt failed and can be retried. <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>confirming</code> — the buyer has done their part and the processor is deciding. <code>processing</code> — the money is moving; see <code>processing_details</code>. <code>succeeded</code> — collected. <code>canceled</code> — voided or written off.
     */
    @JsonProperty("status")
    public PaymentStatusStatus getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_payment_error")
    private Optional<PaymentLastPaymentError> _getLastPaymentError() {
        return lastPaymentError;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("next_action")
    private Optional<PaymentNextAction> _getNextAction() {
        return nextAction;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("processing_details")
    private Optional<PaymentProcessingDetails> _getProcessingDetails() {
        return processingDetails;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("return_url")
    private Optional<String> _getReturnUrl() {
        return returnUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentStatus && equalTo((PaymentStatus) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PaymentStatus other) {
        return id.equals(other.id)
                && lastPaymentError.equals(other.lastPaymentError)
                && nextAction.equals(other.nextAction)
                && object.equals(other.object)
                && processingDetails.equals(other.processingDetails)
                && returnUrl.equals(other.returnUrl)
                && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.id,
                this.lastPaymentError,
                this.nextAction,
                this.object,
                this.processingDetails,
                this.returnUrl,
                this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The payment this status describes, prefixed <code>pay_</code>.</p>
         */
        ObjectStage id(@NotNull String id);

        Builder from(PaymentStatus other);
    }

    public interface ObjectStage {
        /**
         * <p>Always <code>payment_status</code>.</p>
         */
        StatusStage object(@NotNull String object);
    }

    public interface StatusStage {
        /**
         * <p>How far the payment has got. <code>requires_confirmation</code> — nothing attempted yet, or the last attempt failed and can be retried. <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>confirming</code> — the buyer has done their part and the processor is deciding. <code>processing</code> — the money is moving; see <code>processing_details</code>. <code>succeeded</code> — collected. <code>canceled</code> — voided or written off.</p>
         */
        _FinalStage status(@NotNull PaymentStatusStatus status);
    }

    public interface _FinalStage {
        PaymentStatus build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Details of the most recent failed attempt, or <code>null</code> when the payment has not failed.</p>
         */
        _FinalStage lastPaymentError(Optional<PaymentLastPaymentError> lastPaymentError);

        _FinalStage lastPaymentError(PaymentLastPaymentError lastPaymentError);

        _FinalStage lastPaymentError(Nullable<PaymentLastPaymentError> lastPaymentError);

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         */
        _FinalStage nextAction(Optional<PaymentNextAction> nextAction);

        _FinalStage nextAction(PaymentNextAction nextAction);

        _FinalStage nextAction(Nullable<PaymentNextAction> nextAction);

        /**
         * <p>Present while <code>status</code> is <code>processing</code> on a settlement rail, otherwise <code>null</code>.</p>
         */
        _FinalStage processingDetails(Optional<PaymentProcessingDetails> processingDetails);

        _FinalStage processingDetails(PaymentProcessingDetails processingDetails);

        _FinalStage processingDetails(Nullable<PaymentProcessingDetails> processingDetails);

        /**
         * <p>Where to send the buyer once the payment reaches a resting state, or <code>null</code> to leave them where they are. Editable until they return — see the return_url operation.</p>
         */
        _FinalStage returnUrl(Optional<String> returnUrl);

        _FinalStage returnUrl(String returnUrl);

        _FinalStage returnUrl(Nullable<String> returnUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, ObjectStage, StatusStage, _FinalStage {
        private String id;

        private String object;

        private PaymentStatusStatus status;

        private Optional<String> returnUrl = Optional.empty();

        private Optional<PaymentProcessingDetails> processingDetails = Optional.empty();

        private Optional<PaymentNextAction> nextAction = Optional.empty();

        private Optional<PaymentLastPaymentError> lastPaymentError = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PaymentStatus other) {
            id(other.getId());
            lastPaymentError(other.getLastPaymentError());
            nextAction(other.getNextAction());
            object(other.getObject());
            processingDetails(other.getProcessingDetails());
            returnUrl(other.getReturnUrl());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>The payment this status describes, prefixed <code>pay_</code>.</p>
         * <p>The payment this status describes, prefixed <code>pay_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public ObjectStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Always <code>payment_status</code>.</p>
         * <p>Always <code>payment_status</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("object")
        public StatusStage object(@NotNull String object) {
            this.object = Objects.requireNonNull(object, "object must not be null");
            return this;
        }

        /**
         * <p>How far the payment has got. <code>requires_confirmation</code> — nothing attempted yet, or the last attempt failed and can be retried. <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>confirming</code> — the buyer has done their part and the processor is deciding. <code>processing</code> — the money is moving; see <code>processing_details</code>. <code>succeeded</code> — collected. <code>canceled</code> — voided or written off.</p>
         * <p>How far the payment has got. <code>requires_confirmation</code> — nothing attempted yet, or the last attempt failed and can be retried. <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>confirming</code> — the buyer has done their part and the processor is deciding. <code>processing</code> — the money is moving; see <code>processing_details</code>. <code>succeeded</code> — collected. <code>canceled</code> — voided or written off.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull PaymentStatusStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>Where to send the buyer once the payment reaches a resting state, or <code>null</code> to leave them where they are. Editable until they return — see the return_url operation.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage returnUrl(Nullable<String> returnUrl) {
            if (returnUrl.isNull()) {
                this.returnUrl = null;
            } else if (returnUrl.isEmpty()) {
                this.returnUrl = Optional.empty();
            } else {
                this.returnUrl = Optional.of(returnUrl.get());
            }
            return this;
        }

        /**
         * <p>Where to send the buyer once the payment reaches a resting state, or <code>null</code> to leave them where they are. Editable until they return — see the return_url operation.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage returnUrl(String returnUrl) {
            this.returnUrl = Optional.ofNullable(returnUrl);
            return this;
        }

        /**
         * <p>Where to send the buyer once the payment reaches a resting state, or <code>null</code> to leave them where they are. Editable until they return — see the return_url operation.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "return_url", nulls = Nulls.SKIP)
        public _FinalStage returnUrl(Optional<String> returnUrl) {
            this.returnUrl = returnUrl;
            return this;
        }

        /**
         * <p>Present while <code>status</code> is <code>processing</code> on a settlement rail, otherwise <code>null</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage processingDetails(Nullable<PaymentProcessingDetails> processingDetails) {
            if (processingDetails.isNull()) {
                this.processingDetails = null;
            } else if (processingDetails.isEmpty()) {
                this.processingDetails = Optional.empty();
            } else {
                this.processingDetails = Optional.of(processingDetails.get());
            }
            return this;
        }

        /**
         * <p>Present while <code>status</code> is <code>processing</code> on a settlement rail, otherwise <code>null</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage processingDetails(PaymentProcessingDetails processingDetails) {
            this.processingDetails = Optional.ofNullable(processingDetails);
            return this;
        }

        /**
         * <p>Present while <code>status</code> is <code>processing</code> on a settlement rail, otherwise <code>null</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "processing_details", nulls = Nulls.SKIP)
        public _FinalStage processingDetails(Optional<PaymentProcessingDetails> processingDetails) {
            this.processingDetails = processingDetails;
            return this;
        }

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage nextAction(Nullable<PaymentNextAction> nextAction) {
            if (nextAction.isNull()) {
                this.nextAction = null;
            } else if (nextAction.isEmpty()) {
                this.nextAction = Optional.empty();
            } else {
                this.nextAction = Optional.of(nextAction.get());
            }
            return this;
        }

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage nextAction(PaymentNextAction nextAction) {
            this.nextAction = Optional.ofNullable(nextAction);
            return this;
        }

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "next_action", nulls = Nulls.SKIP)
        public _FinalStage nextAction(Optional<PaymentNextAction> nextAction) {
            this.nextAction = nextAction;
            return this;
        }

        /**
         * <p>Details of the most recent failed attempt, or <code>null</code> when the payment has not failed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastPaymentError(Nullable<PaymentLastPaymentError> lastPaymentError) {
            if (lastPaymentError.isNull()) {
                this.lastPaymentError = null;
            } else if (lastPaymentError.isEmpty()) {
                this.lastPaymentError = Optional.empty();
            } else {
                this.lastPaymentError = Optional.of(lastPaymentError.get());
            }
            return this;
        }

        /**
         * <p>Details of the most recent failed attempt, or <code>null</code> when the payment has not failed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastPaymentError(PaymentLastPaymentError lastPaymentError) {
            this.lastPaymentError = Optional.ofNullable(lastPaymentError);
            return this;
        }

        /**
         * <p>Details of the most recent failed attempt, or <code>null</code> when the payment has not failed.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_payment_error", nulls = Nulls.SKIP)
        public _FinalStage lastPaymentError(Optional<PaymentLastPaymentError> lastPaymentError) {
            this.lastPaymentError = lastPaymentError;
            return this;
        }

        @java.lang.Override
        public PaymentStatus build() {
            return new PaymentStatus(
                    id,
                    lastPaymentError,
                    nextAction,
                    object,
                    processingDetails,
                    returnUrl,
                    status,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
