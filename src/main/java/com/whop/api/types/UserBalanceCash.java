/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = UserBalanceCash.Builder.class)
public final class UserBalanceCash {
    private final double balance;

    private final double balanceUsd;

    private final String currency;

    private final double inTransitBalanceUsd;

    private final double pendingBalanceUsd;

    private final Optional<Double> priceUsd;

    private final double reserveBalanceUsd;

    private final double totalWithdrawableBalance;

    private final Map<String, Object> additionalProperties;

    private UserBalanceCash(
            double balance,
            double balanceUsd,
            String currency,
            double inTransitBalanceUsd,
            double pendingBalanceUsd,
            Optional<Double> priceUsd,
            double reserveBalanceUsd,
            double totalWithdrawableBalance,
            Map<String, Object> additionalProperties) {
        this.balance = balance;
        this.balanceUsd = balanceUsd;
        this.currency = currency;
        this.inTransitBalanceUsd = inTransitBalanceUsd;
        this.pendingBalanceUsd = pendingBalanceUsd;
        this.priceUsd = priceUsd;
        this.reserveBalanceUsd = reserveBalanceUsd;
        this.totalWithdrawableBalance = totalWithdrawableBalance;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Available balance in the native currency.
     */
    @JsonProperty("balance")
    public double getBalance() {
        return balance;
    }

    /**
     * @return Available balance converted to USD.
     */
    @JsonProperty("balance_usd")
    public double getBalanceUsd() {
        return balanceUsd;
    }

    /**
     * @return Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>.
     */
    @JsonProperty("currency")
    public String getCurrency() {
        return currency;
    }

    /**
     * @return Balance moving to the user's own wallet or card, converted to USD.
     */
    @JsonProperty("in_transit_balance_usd")
    public double getInTransitBalanceUsd() {
        return inTransitBalanceUsd;
    }

    /**
     * @return Pending balance converted to USD.
     */
    @JsonProperty("pending_balance_usd")
    public double getPendingBalanceUsd() {
        return pendingBalanceUsd;
    }

    /**
     * @return USD price per native currency unit, or <code>null</code> when no exchange rate is available.
     */
    @JsonIgnore
    public Optional<Double> getPriceUsd() {
        if (priceUsd == null) {
            return Optional.empty();
        }
        return priceUsd;
    }

    /**
     * @return Reserved balance converted to USD.
     */
    @JsonProperty("reserve_balance_usd")
    public double getReserveBalanceUsd() {
        return reserveBalanceUsd;
    }

    /**
     * @return Withdrawable amount in the native currency.
     */
    @JsonProperty("total_withdrawable_balance")
    public double getTotalWithdrawableBalance() {
        return totalWithdrawableBalance;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("price_usd")
    private Optional<Double> _getPriceUsd() {
        return priceUsd;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UserBalanceCash && equalTo((UserBalanceCash) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(UserBalanceCash other) {
        return balance == other.balance
                && balanceUsd == other.balanceUsd
                && currency.equals(other.currency)
                && inTransitBalanceUsd == other.inTransitBalanceUsd
                && pendingBalanceUsd == other.pendingBalanceUsd
                && priceUsd.equals(other.priceUsd)
                && reserveBalanceUsd == other.reserveBalanceUsd
                && totalWithdrawableBalance == other.totalWithdrawableBalance;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.balance,
                this.balanceUsd,
                this.currency,
                this.inTransitBalanceUsd,
                this.pendingBalanceUsd,
                this.priceUsd,
                this.reserveBalanceUsd,
                this.totalWithdrawableBalance);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BalanceStage builder() {
        return new Builder();
    }

    public interface BalanceStage {
        /**
         * <p>Available balance in the native currency.</p>
         */
        BalanceUsdStage balance(double balance);

        Builder from(UserBalanceCash other);
    }

    public interface BalanceUsdStage {
        /**
         * <p>Available balance converted to USD.</p>
         */
        CurrencyStage balanceUsd(double balanceUsd);
    }

    public interface CurrencyStage {
        /**
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>.</p>
         */
        InTransitBalanceUsdStage currency(@NotNull String currency);
    }

    public interface InTransitBalanceUsdStage {
        /**
         * <p>Balance moving to the user's own wallet or card, converted to USD.</p>
         */
        PendingBalanceUsdStage inTransitBalanceUsd(double inTransitBalanceUsd);
    }

    public interface PendingBalanceUsdStage {
        /**
         * <p>Pending balance converted to USD.</p>
         */
        ReserveBalanceUsdStage pendingBalanceUsd(double pendingBalanceUsd);
    }

    public interface ReserveBalanceUsdStage {
        /**
         * <p>Reserved balance converted to USD.</p>
         */
        TotalWithdrawableBalanceStage reserveBalanceUsd(double reserveBalanceUsd);
    }

    public interface TotalWithdrawableBalanceStage {
        /**
         * <p>Withdrawable amount in the native currency.</p>
         */
        _FinalStage totalWithdrawableBalance(double totalWithdrawableBalance);
    }

    public interface _FinalStage {
        UserBalanceCash build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>USD price per native currency unit, or <code>null</code> when no exchange rate is available.</p>
         */
        _FinalStage priceUsd(Optional<Double> priceUsd);

        _FinalStage priceUsd(Double priceUsd);

        _FinalStage priceUsd(Nullable<Double> priceUsd);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements BalanceStage,
                    BalanceUsdStage,
                    CurrencyStage,
                    InTransitBalanceUsdStage,
                    PendingBalanceUsdStage,
                    ReserveBalanceUsdStage,
                    TotalWithdrawableBalanceStage,
                    _FinalStage {
        private double balance;

        private double balanceUsd;

        private String currency;

        private double inTransitBalanceUsd;

        private double pendingBalanceUsd;

        private double reserveBalanceUsd;

        private double totalWithdrawableBalance;

        private Optional<Double> priceUsd = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(UserBalanceCash other) {
            balance(other.getBalance());
            balanceUsd(other.getBalanceUsd());
            currency(other.getCurrency());
            inTransitBalanceUsd(other.getInTransitBalanceUsd());
            pendingBalanceUsd(other.getPendingBalanceUsd());
            priceUsd(other.getPriceUsd());
            reserveBalanceUsd(other.getReserveBalanceUsd());
            totalWithdrawableBalance(other.getTotalWithdrawableBalance());
            return this;
        }

        /**
         * <p>Available balance in the native currency.</p>
         * <p>Available balance in the native currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("balance")
        public BalanceUsdStage balance(double balance) {
            this.balance = balance;
            return this;
        }

        /**
         * <p>Available balance converted to USD.</p>
         * <p>Available balance converted to USD.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("balance_usd")
        public CurrencyStage balanceUsd(double balanceUsd) {
            this.balanceUsd = balanceUsd;
            return this;
        }

        /**
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>.</p>
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public InTransitBalanceUsdStage currency(@NotNull String currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>Balance moving to the user's own wallet or card, converted to USD.</p>
         * <p>Balance moving to the user's own wallet or card, converted to USD.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("in_transit_balance_usd")
        public PendingBalanceUsdStage inTransitBalanceUsd(double inTransitBalanceUsd) {
            this.inTransitBalanceUsd = inTransitBalanceUsd;
            return this;
        }

        /**
         * <p>Pending balance converted to USD.</p>
         * <p>Pending balance converted to USD.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("pending_balance_usd")
        public ReserveBalanceUsdStage pendingBalanceUsd(double pendingBalanceUsd) {
            this.pendingBalanceUsd = pendingBalanceUsd;
            return this;
        }

        /**
         * <p>Reserved balance converted to USD.</p>
         * <p>Reserved balance converted to USD.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("reserve_balance_usd")
        public TotalWithdrawableBalanceStage reserveBalanceUsd(double reserveBalanceUsd) {
            this.reserveBalanceUsd = reserveBalanceUsd;
            return this;
        }

        /**
         * <p>Withdrawable amount in the native currency.</p>
         * <p>Withdrawable amount in the native currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("total_withdrawable_balance")
        public _FinalStage totalWithdrawableBalance(double totalWithdrawableBalance) {
            this.totalWithdrawableBalance = totalWithdrawableBalance;
            return this;
        }

        /**
         * <p>USD price per native currency unit, or <code>null</code> when no exchange rate is available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage priceUsd(Nullable<Double> priceUsd) {
            if (priceUsd.isNull()) {
                this.priceUsd = null;
            } else if (priceUsd.isEmpty()) {
                this.priceUsd = Optional.empty();
            } else {
                this.priceUsd = Optional.of(priceUsd.get());
            }
            return this;
        }

        /**
         * <p>USD price per native currency unit, or <code>null</code> when no exchange rate is available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage priceUsd(Double priceUsd) {
            this.priceUsd = Optional.ofNullable(priceUsd);
            return this;
        }

        /**
         * <p>USD price per native currency unit, or <code>null</code> when no exchange rate is available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "price_usd", nulls = Nulls.SKIP)
        public _FinalStage priceUsd(Optional<Double> priceUsd) {
            this.priceUsd = priceUsd;
            return this;
        }

        @java.lang.Override
        public UserBalanceCash build() {
            return new UserBalanceCash(
                    balance,
                    balanceUsd,
                    currency,
                    inTransitBalanceUsd,
                    pendingBalanceUsd,
                    priceUsd,
                    reserveBalanceUsd,
                    totalWithdrawableBalance,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
