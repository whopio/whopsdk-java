/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AppDefaultApiKey.Builder.class)
public final class AppDefaultApiKey {
    private final String id;

    private final Optional<String> name;

    private final String obfuscatedSecretKey;

    private final Optional<String> secretKey;

    private final Map<String, Object> additionalProperties;

    private AppDefaultApiKey(
            String id,
            Optional<String> name,
            String obfuscatedSecretKey,
            Optional<String> secretKey,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.name = name;
        this.obfuscatedSecretKey = obfuscatedSecretKey;
        this.secretKey = secretKey;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return API key ID, prefixed <code>apik_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Human-readable name identifying the API key, or <code>null</code> when none was set.
     */
    @JsonIgnore
    public Optional<String> getName() {
        if (name == null) {
            return Optional.empty();
        }
        return name;
    }

    /**
     * @return Masked version of the secret key, so the key can be recognized without revealing the full secret.
     */
    @JsonProperty("obfuscated_secret_key")
    public String getObfuscatedSecretKey() {
        return obfuscatedSecretKey;
    }

    /**
     * @return The full secret used to authenticate requests. <code>null</code> unless the caller could have created the key themselves.
     */
    @JsonIgnore
    public Optional<String> getSecretKey() {
        if (secretKey == null) {
            return Optional.empty();
        }
        return secretKey;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("name")
    private Optional<String> _getName() {
        return name;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("secret_key")
    private Optional<String> _getSecretKey() {
        return secretKey;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AppDefaultApiKey && equalTo((AppDefaultApiKey) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AppDefaultApiKey other) {
        return id.equals(other.id)
                && name.equals(other.name)
                && obfuscatedSecretKey.equals(other.obfuscatedSecretKey)
                && secretKey.equals(other.secretKey);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.name, this.obfuscatedSecretKey, this.secretKey);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>API key ID, prefixed <code>apik_</code>.</p>
         */
        ObfuscatedSecretKeyStage id(@NotNull String id);

        Builder from(AppDefaultApiKey other);
    }

    public interface ObfuscatedSecretKeyStage {
        /**
         * <p>Masked version of the secret key, so the key can be recognized without revealing the full secret.</p>
         */
        _FinalStage obfuscatedSecretKey(@NotNull String obfuscatedSecretKey);
    }

    public interface _FinalStage {
        AppDefaultApiKey build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Human-readable name identifying the API key, or <code>null</code> when none was set.</p>
         */
        _FinalStage name(Optional<String> name);

        _FinalStage name(String name);

        _FinalStage name(Nullable<String> name);

        /**
         * <p>The full secret used to authenticate requests. <code>null</code> unless the caller could have created the key themselves.</p>
         */
        _FinalStage secretKey(Optional<String> secretKey);

        _FinalStage secretKey(String secretKey);

        _FinalStage secretKey(Nullable<String> secretKey);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, ObfuscatedSecretKeyStage, _FinalStage {
        private String id;

        private String obfuscatedSecretKey;

        private Optional<String> secretKey = Optional.empty();

        private Optional<String> name = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AppDefaultApiKey other) {
            id(other.getId());
            name(other.getName());
            obfuscatedSecretKey(other.getObfuscatedSecretKey());
            secretKey(other.getSecretKey());
            return this;
        }

        /**
         * <p>API key ID, prefixed <code>apik_</code>.</p>
         * <p>API key ID, prefixed <code>apik_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public ObfuscatedSecretKeyStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Masked version of the secret key, so the key can be recognized without revealing the full secret.</p>
         * <p>Masked version of the secret key, so the key can be recognized without revealing the full secret.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("obfuscated_secret_key")
        public _FinalStage obfuscatedSecretKey(@NotNull String obfuscatedSecretKey) {
            this.obfuscatedSecretKey =
                    Objects.requireNonNull(obfuscatedSecretKey, "obfuscatedSecretKey must not be null");
            return this;
        }

        /**
         * <p>The full secret used to authenticate requests. <code>null</code> unless the caller could have created the key themselves.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage secretKey(Nullable<String> secretKey) {
            if (secretKey.isNull()) {
                this.secretKey = null;
            } else if (secretKey.isEmpty()) {
                this.secretKey = Optional.empty();
            } else {
                this.secretKey = Optional.of(secretKey.get());
            }
            return this;
        }

        /**
         * <p>The full secret used to authenticate requests. <code>null</code> unless the caller could have created the key themselves.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage secretKey(String secretKey) {
            this.secretKey = Optional.ofNullable(secretKey);
            return this;
        }

        /**
         * <p>The full secret used to authenticate requests. <code>null</code> unless the caller could have created the key themselves.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "secret_key", nulls = Nulls.SKIP)
        public _FinalStage secretKey(Optional<String> secretKey) {
            this.secretKey = secretKey;
            return this;
        }

        /**
         * <p>Human-readable name identifying the API key, or <code>null</code> when none was set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage name(Nullable<String> name) {
            if (name.isNull()) {
                this.name = null;
            } else if (name.isEmpty()) {
                this.name = Optional.empty();
            } else {
                this.name = Optional.of(name.get());
            }
            return this;
        }

        /**
         * <p>Human-readable name identifying the API key, or <code>null</code> when none was set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage name(String name) {
            this.name = Optional.ofNullable(name);
            return this;
        }

        /**
         * <p>Human-readable name identifying the API key, or <code>null</code> when none was set.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "name", nulls = Nulls.SKIP)
        public _FinalStage name(Optional<String> name) {
            this.name = name;
            return this;
        }

        @java.lang.Override
        public AppDefaultApiKey build() {
            return new AppDefaultApiKey(id, name, obfuscatedSecretKey, secretKey, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
