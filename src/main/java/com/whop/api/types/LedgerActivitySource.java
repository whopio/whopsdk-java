/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = LedgerActivitySource.Builder.class)
public final class LedgerActivitySource {
    private final Optional<Double> amountFloat;

    private final Optional<String> chain;

    private final Optional<String> claimUrl;

    private final Optional<OffsetDateTime> createdAt;

    private final Optional<OffsetDateTime> estimatedArrival;

    private final Optional<String> fromAmount;

    private final Optional<String> fromCurrency;

    private final String id;

    private final String object;

    private final Optional<String> payerName;

    private final Optional<LedgerActivitySourcePayoutDestination> payoutDestination;

    private final Optional<String> payoutTokenNickname;

    private final Optional<String> reason;

    private final Optional<String> senderAddress;

    private final Optional<String> status;

    private final Optional<String> toAmount;

    private final Optional<String> toCurrency;

    private final Optional<String> txHash;

    private final Map<String, Object> additionalProperties;

    private LedgerActivitySource(
            Optional<Double> amountFloat,
            Optional<String> chain,
            Optional<String> claimUrl,
            Optional<OffsetDateTime> createdAt,
            Optional<OffsetDateTime> estimatedArrival,
            Optional<String> fromAmount,
            Optional<String> fromCurrency,
            String id,
            String object,
            Optional<String> payerName,
            Optional<LedgerActivitySourcePayoutDestination> payoutDestination,
            Optional<String> payoutTokenNickname,
            Optional<String> reason,
            Optional<String> senderAddress,
            Optional<String> status,
            Optional<String> toAmount,
            Optional<String> toCurrency,
            Optional<String> txHash,
            Map<String, Object> additionalProperties) {
        this.amountFloat = amountFloat;
        this.chain = chain;
        this.claimUrl = claimUrl;
        this.createdAt = createdAt;
        this.estimatedArrival = estimatedArrival;
        this.fromAmount = fromAmount;
        this.fromCurrency = fromCurrency;
        this.id = id;
        this.object = object;
        this.payerName = payerName;
        this.payoutDestination = payoutDestination;
        this.payoutTokenNickname = payoutTokenNickname;
        this.reason = reason;
        this.senderAddress = senderAddress;
        this.status = status;
        this.toAmount = toAmount;
        this.toCurrency = toCurrency;
        this.txHash = txHash;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Withdrawal amount as a decimal number in the destination currency (withdrawal sources only; requires payout:withdrawal:read).
     */
    @JsonIgnore
    public Optional<Double> getAmountFloat() {
        if (amountFloat == null) {
            return Optional.empty();
        }
        return amountFloat;
    }

    /**
     * @return Chain the deposit landed on, for example plasma (onchain_transaction sources only).
     */
    @JsonIgnore
    public Optional<String> getChain() {
        if (chain == null) {
            return Optional.empty();
        }
        return chain;
    }

    /**
     * @return Public claim URL for the airdrop link (airdrop_link sources only).
     */
    @JsonIgnore
    public Optional<String> getClaimUrl() {
        if (claimUrl == null) {
            return Optional.empty();
        }
        return claimUrl;
    }

    /**
     * @return Withdrawal creation time as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getCreatedAt() {
        if (createdAt == null) {
            return Optional.empty();
        }
        return createdAt;
    }

    /**
     * @return Estimated arrival as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getEstimatedArrival() {
        if (estimatedArrival == null) {
            return Optional.empty();
        }
        return estimatedArrival;
    }

    /**
     * @return Amount converted out of from_currency as a decimal string (swap sources only).
     */
    @JsonIgnore
    public Optional<String> getFromAmount() {
        if (fromAmount == null) {
            return Optional.empty();
        }
        return fromAmount;
    }

    /**
     * @return Lowercase currency code converted from (swap sources only).
     */
    @JsonIgnore
    public Optional<String> getFromCurrency() {
        if (fromCurrency == null) {
            return Optional.empty();
        }
        return fromCurrency;
    }

    @JsonProperty("id")
    public String getId() {
        return id;
    }

    @JsonProperty("object")
    public String getObject() {
        return object;
    }

    /**
     * @return Name of the entity processing the payout (withdrawal sources only; requires payout:withdrawal:read).
     */
    @JsonIgnore
    public Optional<String> getPayerName() {
        if (payerName == null) {
            return Optional.empty();
        }
        return payerName;
    }

    /**
     * @return Payout destination display info (withdrawal sources only).
     */
    @JsonIgnore
    public Optional<LedgerActivitySourcePayoutDestination> getPayoutDestination() {
        if (payoutDestination == null) {
            return Optional.empty();
        }
        return payoutDestination;
    }

    /**
     * @return Saved payout destination nickname (withdrawal sources only).
     */
    @JsonIgnore
    public Optional<String> getPayoutTokenNickname() {
        if (payoutTokenNickname == null) {
            return Optional.empty();
        }
        return payoutTokenNickname;
    }

    /**
     * @return Why the activity happened. On transfer sources this is the transfer reason, for example pool_top_up or bounty_return. On withdrawal sources it explains why the withdrawal was canceled, denied, or failed (requires payout:withdrawal:read); null while the withdrawal is progressing normally.
     */
    @JsonIgnore
    public Optional<String> getReason() {
        if (reason == null) {
            return Optional.empty();
        }
        return reason;
    }

    /**
     * @return Sender wallet address or onramp provider identifier (onchain_transaction sources only).
     */
    @JsonIgnore
    public Optional<String> getSenderAddress() {
        if (senderAddress == null) {
            return Optional.empty();
        }
        return senderAddress;
    }

    /**
     * @return Lifecycle status. On withdrawal sources this is the withdrawal status (requires payout:withdrawal:read); on airdrop_link sources it is the claim-link status (ungated); on payment and top-up sources it is the friendly payment status such as succeeded/pending/failed (ungated).
     */
    @JsonIgnore
    public Optional<String> getStatus() {
        if (status == null) {
            return Optional.empty();
        }
        return status;
    }

    /**
     * @return Amount received in to_currency as a decimal string (swap sources only).
     */
    @JsonIgnore
    public Optional<String> getToAmount() {
        if (toAmount == null) {
            return Optional.empty();
        }
        return toAmount;
    }

    /**
     * @return Lowercase currency code converted to (swap sources only).
     */
    @JsonIgnore
    public Optional<String> getToCurrency() {
        if (toCurrency == null) {
            return Optional.empty();
        }
        return toCurrency;
    }

    /**
     * @return On-chain transaction hash (onchain_transaction and swap sources only).
     */
    @JsonIgnore
    public Optional<String> getTxHash() {
        if (txHash == null) {
            return Optional.empty();
        }
        return txHash;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("amount_float")
    private Optional<Double> _getAmountFloat() {
        return amountFloat;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("chain")
    private Optional<String> _getChain() {
        return chain;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("claim_url")
    private Optional<String> _getClaimUrl() {
        return claimUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("created_at")
    private Optional<OffsetDateTime> _getCreatedAt() {
        return createdAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("estimated_arrival")
    private Optional<OffsetDateTime> _getEstimatedArrival() {
        return estimatedArrival;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("from_amount")
    private Optional<String> _getFromAmount() {
        return fromAmount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("from_currency")
    private Optional<String> _getFromCurrency() {
        return fromCurrency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payer_name")
    private Optional<String> _getPayerName() {
        return payerName;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payout_destination")
    private Optional<LedgerActivitySourcePayoutDestination> _getPayoutDestination() {
        return payoutDestination;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payout_token_nickname")
    private Optional<String> _getPayoutTokenNickname() {
        return payoutTokenNickname;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("reason")
    private Optional<String> _getReason() {
        return reason;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("sender_address")
    private Optional<String> _getSenderAddress() {
        return senderAddress;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("status")
    private Optional<String> _getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("to_amount")
    private Optional<String> _getToAmount() {
        return toAmount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("to_currency")
    private Optional<String> _getToCurrency() {
        return toCurrency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("tx_hash")
    private Optional<String> _getTxHash() {
        return txHash;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof LedgerActivitySource && equalTo((LedgerActivitySource) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(LedgerActivitySource other) {
        return amountFloat.equals(other.amountFloat)
                && chain.equals(other.chain)
                && claimUrl.equals(other.claimUrl)
                && createdAt.equals(other.createdAt)
                && estimatedArrival.equals(other.estimatedArrival)
                && fromAmount.equals(other.fromAmount)
                && fromCurrency.equals(other.fromCurrency)
                && id.equals(other.id)
                && object.equals(other.object)
                && payerName.equals(other.payerName)
                && payoutDestination.equals(other.payoutDestination)
                && payoutTokenNickname.equals(other.payoutTokenNickname)
                && reason.equals(other.reason)
                && senderAddress.equals(other.senderAddress)
                && status.equals(other.status)
                && toAmount.equals(other.toAmount)
                && toCurrency.equals(other.toCurrency)
                && txHash.equals(other.txHash);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amountFloat,
                this.chain,
                this.claimUrl,
                this.createdAt,
                this.estimatedArrival,
                this.fromAmount,
                this.fromCurrency,
                this.id,
                this.object,
                this.payerName,
                this.payoutDestination,
                this.payoutTokenNickname,
                this.reason,
                this.senderAddress,
                this.status,
                this.toAmount,
                this.toCurrency,
                this.txHash);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        ObjectStage id(@NotNull String id);

        Builder from(LedgerActivitySource other);
    }

    public interface ObjectStage {
        _FinalStage object(@NotNull String object);
    }

    public interface _FinalStage {
        LedgerActivitySource build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Withdrawal amount as a decimal number in the destination currency (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        _FinalStage amountFloat(Optional<Double> amountFloat);

        _FinalStage amountFloat(Double amountFloat);

        _FinalStage amountFloat(Nullable<Double> amountFloat);

        /**
         * <p>Chain the deposit landed on, for example plasma (onchain_transaction sources only).</p>
         */
        _FinalStage chain(Optional<String> chain);

        _FinalStage chain(String chain);

        _FinalStage chain(Nullable<String> chain);

        /**
         * <p>Public claim URL for the airdrop link (airdrop_link sources only).</p>
         */
        _FinalStage claimUrl(Optional<String> claimUrl);

        _FinalStage claimUrl(String claimUrl);

        _FinalStage claimUrl(Nullable<String> claimUrl);

        /**
         * <p>Withdrawal creation time as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        _FinalStage createdAt(Optional<OffsetDateTime> createdAt);

        _FinalStage createdAt(OffsetDateTime createdAt);

        _FinalStage createdAt(Nullable<OffsetDateTime> createdAt);

        /**
         * <p>Estimated arrival as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        _FinalStage estimatedArrival(Optional<OffsetDateTime> estimatedArrival);

        _FinalStage estimatedArrival(OffsetDateTime estimatedArrival);

        _FinalStage estimatedArrival(Nullable<OffsetDateTime> estimatedArrival);

        /**
         * <p>Amount converted out of from_currency as a decimal string (swap sources only).</p>
         */
        _FinalStage fromAmount(Optional<String> fromAmount);

        _FinalStage fromAmount(String fromAmount);

        _FinalStage fromAmount(Nullable<String> fromAmount);

        /**
         * <p>Lowercase currency code converted from (swap sources only).</p>
         */
        _FinalStage fromCurrency(Optional<String> fromCurrency);

        _FinalStage fromCurrency(String fromCurrency);

        _FinalStage fromCurrency(Nullable<String> fromCurrency);

        /**
         * <p>Name of the entity processing the payout (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        _FinalStage payerName(Optional<String> payerName);

        _FinalStage payerName(String payerName);

        _FinalStage payerName(Nullable<String> payerName);

        /**
         * <p>Payout destination display info (withdrawal sources only).</p>
         */
        _FinalStage payoutDestination(Optional<LedgerActivitySourcePayoutDestination> payoutDestination);

        _FinalStage payoutDestination(LedgerActivitySourcePayoutDestination payoutDestination);

        _FinalStage payoutDestination(Nullable<LedgerActivitySourcePayoutDestination> payoutDestination);

        /**
         * <p>Saved payout destination nickname (withdrawal sources only).</p>
         */
        _FinalStage payoutTokenNickname(Optional<String> payoutTokenNickname);

        _FinalStage payoutTokenNickname(String payoutTokenNickname);

        _FinalStage payoutTokenNickname(Nullable<String> payoutTokenNickname);

        /**
         * <p>Why the activity happened. On transfer sources this is the transfer reason, for example pool_top_up or bounty_return. On withdrawal sources it explains why the withdrawal was canceled, denied, or failed (requires payout:withdrawal:read); null while the withdrawal is progressing normally.</p>
         */
        _FinalStage reason(Optional<String> reason);

        _FinalStage reason(String reason);

        _FinalStage reason(Nullable<String> reason);

        /**
         * <p>Sender wallet address or onramp provider identifier (onchain_transaction sources only).</p>
         */
        _FinalStage senderAddress(Optional<String> senderAddress);

        _FinalStage senderAddress(String senderAddress);

        _FinalStage senderAddress(Nullable<String> senderAddress);

        /**
         * <p>Lifecycle status. On withdrawal sources this is the withdrawal status (requires payout:withdrawal:read); on airdrop_link sources it is the claim-link status (ungated); on payment and top-up sources it is the friendly payment status such as succeeded/pending/failed (ungated).</p>
         */
        _FinalStage status(Optional<String> status);

        _FinalStage status(String status);

        _FinalStage status(Nullable<String> status);

        /**
         * <p>Amount received in to_currency as a decimal string (swap sources only).</p>
         */
        _FinalStage toAmount(Optional<String> toAmount);

        _FinalStage toAmount(String toAmount);

        _FinalStage toAmount(Nullable<String> toAmount);

        /**
         * <p>Lowercase currency code converted to (swap sources only).</p>
         */
        _FinalStage toCurrency(Optional<String> toCurrency);

        _FinalStage toCurrency(String toCurrency);

        _FinalStage toCurrency(Nullable<String> toCurrency);

        /**
         * <p>On-chain transaction hash (onchain_transaction and swap sources only).</p>
         */
        _FinalStage txHash(Optional<String> txHash);

        _FinalStage txHash(String txHash);

        _FinalStage txHash(Nullable<String> txHash);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, ObjectStage, _FinalStage {
        private String id;

        private String object;

        private Optional<String> txHash = Optional.empty();

        private Optional<String> toCurrency = Optional.empty();

        private Optional<String> toAmount = Optional.empty();

        private Optional<String> status = Optional.empty();

        private Optional<String> senderAddress = Optional.empty();

        private Optional<String> reason = Optional.empty();

        private Optional<String> payoutTokenNickname = Optional.empty();

        private Optional<LedgerActivitySourcePayoutDestination> payoutDestination = Optional.empty();

        private Optional<String> payerName = Optional.empty();

        private Optional<String> fromCurrency = Optional.empty();

        private Optional<String> fromAmount = Optional.empty();

        private Optional<OffsetDateTime> estimatedArrival = Optional.empty();

        private Optional<OffsetDateTime> createdAt = Optional.empty();

        private Optional<String> claimUrl = Optional.empty();

        private Optional<String> chain = Optional.empty();

        private Optional<Double> amountFloat = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(LedgerActivitySource other) {
            amountFloat(other.getAmountFloat());
            chain(other.getChain());
            claimUrl(other.getClaimUrl());
            createdAt(other.getCreatedAt());
            estimatedArrival(other.getEstimatedArrival());
            fromAmount(other.getFromAmount());
            fromCurrency(other.getFromCurrency());
            id(other.getId());
            object(other.getObject());
            payerName(other.getPayerName());
            payoutDestination(other.getPayoutDestination());
            payoutTokenNickname(other.getPayoutTokenNickname());
            reason(other.getReason());
            senderAddress(other.getSenderAddress());
            status(other.getStatus());
            toAmount(other.getToAmount());
            toCurrency(other.getToCurrency());
            txHash(other.getTxHash());
            return this;
        }

        @java.lang.Override
        @JsonSetter("id")
        public ObjectStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("object")
        public _FinalStage object(@NotNull String object) {
            this.object = Objects.requireNonNull(object, "object must not be null");
            return this;
        }

        /**
         * <p>On-chain transaction hash (onchain_transaction and swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage txHash(Nullable<String> txHash) {
            if (txHash.isNull()) {
                this.txHash = null;
            } else if (txHash.isEmpty()) {
                this.txHash = Optional.empty();
            } else {
                this.txHash = Optional.of(txHash.get());
            }
            return this;
        }

        /**
         * <p>On-chain transaction hash (onchain_transaction and swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage txHash(String txHash) {
            this.txHash = Optional.ofNullable(txHash);
            return this;
        }

        /**
         * <p>On-chain transaction hash (onchain_transaction and swap sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "tx_hash", nulls = Nulls.SKIP)
        public _FinalStage txHash(Optional<String> txHash) {
            this.txHash = txHash;
            return this;
        }

        /**
         * <p>Lowercase currency code converted to (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toCurrency(Nullable<String> toCurrency) {
            if (toCurrency.isNull()) {
                this.toCurrency = null;
            } else if (toCurrency.isEmpty()) {
                this.toCurrency = Optional.empty();
            } else {
                this.toCurrency = Optional.of(toCurrency.get());
            }
            return this;
        }

        /**
         * <p>Lowercase currency code converted to (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toCurrency(String toCurrency) {
            this.toCurrency = Optional.ofNullable(toCurrency);
            return this;
        }

        /**
         * <p>Lowercase currency code converted to (swap sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_currency", nulls = Nulls.SKIP)
        public _FinalStage toCurrency(Optional<String> toCurrency) {
            this.toCurrency = toCurrency;
            return this;
        }

        /**
         * <p>Amount received in to_currency as a decimal string (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toAmount(Nullable<String> toAmount) {
            if (toAmount.isNull()) {
                this.toAmount = null;
            } else if (toAmount.isEmpty()) {
                this.toAmount = Optional.empty();
            } else {
                this.toAmount = Optional.of(toAmount.get());
            }
            return this;
        }

        /**
         * <p>Amount received in to_currency as a decimal string (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toAmount(String toAmount) {
            this.toAmount = Optional.ofNullable(toAmount);
            return this;
        }

        /**
         * <p>Amount received in to_currency as a decimal string (swap sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_amount", nulls = Nulls.SKIP)
        public _FinalStage toAmount(Optional<String> toAmount) {
            this.toAmount = toAmount;
            return this;
        }

        /**
         * <p>Lifecycle status. On withdrawal sources this is the withdrawal status (requires payout:withdrawal:read); on airdrop_link sources it is the claim-link status (ungated); on payment and top-up sources it is the friendly payment status such as succeeded/pending/failed (ungated).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage status(Nullable<String> status) {
            if (status.isNull()) {
                this.status = null;
            } else if (status.isEmpty()) {
                this.status = Optional.empty();
            } else {
                this.status = Optional.of(status.get());
            }
            return this;
        }

        /**
         * <p>Lifecycle status. On withdrawal sources this is the withdrawal status (requires payout:withdrawal:read); on airdrop_link sources it is the claim-link status (ungated); on payment and top-up sources it is the friendly payment status such as succeeded/pending/failed (ungated).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage status(String status) {
            this.status = Optional.ofNullable(status);
            return this;
        }

        /**
         * <p>Lifecycle status. On withdrawal sources this is the withdrawal status (requires payout:withdrawal:read); on airdrop_link sources it is the claim-link status (ungated); on payment and top-up sources it is the friendly payment status such as succeeded/pending/failed (ungated).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "status", nulls = Nulls.SKIP)
        public _FinalStage status(Optional<String> status) {
            this.status = status;
            return this;
        }

        /**
         * <p>Sender wallet address or onramp provider identifier (onchain_transaction sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage senderAddress(Nullable<String> senderAddress) {
            if (senderAddress.isNull()) {
                this.senderAddress = null;
            } else if (senderAddress.isEmpty()) {
                this.senderAddress = Optional.empty();
            } else {
                this.senderAddress = Optional.of(senderAddress.get());
            }
            return this;
        }

        /**
         * <p>Sender wallet address or onramp provider identifier (onchain_transaction sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage senderAddress(String senderAddress) {
            this.senderAddress = Optional.ofNullable(senderAddress);
            return this;
        }

        /**
         * <p>Sender wallet address or onramp provider identifier (onchain_transaction sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "sender_address", nulls = Nulls.SKIP)
        public _FinalStage senderAddress(Optional<String> senderAddress) {
            this.senderAddress = senderAddress;
            return this;
        }

        /**
         * <p>Why the activity happened. On transfer sources this is the transfer reason, for example pool_top_up or bounty_return. On withdrawal sources it explains why the withdrawal was canceled, denied, or failed (requires payout:withdrawal:read); null while the withdrawal is progressing normally.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage reason(Nullable<String> reason) {
            if (reason.isNull()) {
                this.reason = null;
            } else if (reason.isEmpty()) {
                this.reason = Optional.empty();
            } else {
                this.reason = Optional.of(reason.get());
            }
            return this;
        }

        /**
         * <p>Why the activity happened. On transfer sources this is the transfer reason, for example pool_top_up or bounty_return. On withdrawal sources it explains why the withdrawal was canceled, denied, or failed (requires payout:withdrawal:read); null while the withdrawal is progressing normally.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage reason(String reason) {
            this.reason = Optional.ofNullable(reason);
            return this;
        }

        /**
         * <p>Why the activity happened. On transfer sources this is the transfer reason, for example pool_top_up or bounty_return. On withdrawal sources it explains why the withdrawal was canceled, denied, or failed (requires payout:withdrawal:read); null while the withdrawal is progressing normally.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "reason", nulls = Nulls.SKIP)
        public _FinalStage reason(Optional<String> reason) {
            this.reason = reason;
            return this;
        }

        /**
         * <p>Saved payout destination nickname (withdrawal sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutTokenNickname(Nullable<String> payoutTokenNickname) {
            if (payoutTokenNickname.isNull()) {
                this.payoutTokenNickname = null;
            } else if (payoutTokenNickname.isEmpty()) {
                this.payoutTokenNickname = Optional.empty();
            } else {
                this.payoutTokenNickname = Optional.of(payoutTokenNickname.get());
            }
            return this;
        }

        /**
         * <p>Saved payout destination nickname (withdrawal sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutTokenNickname(String payoutTokenNickname) {
            this.payoutTokenNickname = Optional.ofNullable(payoutTokenNickname);
            return this;
        }

        /**
         * <p>Saved payout destination nickname (withdrawal sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payout_token_nickname", nulls = Nulls.SKIP)
        public _FinalStage payoutTokenNickname(Optional<String> payoutTokenNickname) {
            this.payoutTokenNickname = payoutTokenNickname;
            return this;
        }

        /**
         * <p>Payout destination display info (withdrawal sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutDestination(Nullable<LedgerActivitySourcePayoutDestination> payoutDestination) {
            if (payoutDestination.isNull()) {
                this.payoutDestination = null;
            } else if (payoutDestination.isEmpty()) {
                this.payoutDestination = Optional.empty();
            } else {
                this.payoutDestination = Optional.of(payoutDestination.get());
            }
            return this;
        }

        /**
         * <p>Payout destination display info (withdrawal sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutDestination(LedgerActivitySourcePayoutDestination payoutDestination) {
            this.payoutDestination = Optional.ofNullable(payoutDestination);
            return this;
        }

        /**
         * <p>Payout destination display info (withdrawal sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payout_destination", nulls = Nulls.SKIP)
        public _FinalStage payoutDestination(Optional<LedgerActivitySourcePayoutDestination> payoutDestination) {
            this.payoutDestination = payoutDestination;
            return this;
        }

        /**
         * <p>Name of the entity processing the payout (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payerName(Nullable<String> payerName) {
            if (payerName.isNull()) {
                this.payerName = null;
            } else if (payerName.isEmpty()) {
                this.payerName = Optional.empty();
            } else {
                this.payerName = Optional.of(payerName.get());
            }
            return this;
        }

        /**
         * <p>Name of the entity processing the payout (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payerName(String payerName) {
            this.payerName = Optional.ofNullable(payerName);
            return this;
        }

        /**
         * <p>Name of the entity processing the payout (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payer_name", nulls = Nulls.SKIP)
        public _FinalStage payerName(Optional<String> payerName) {
            this.payerName = payerName;
            return this;
        }

        /**
         * <p>Lowercase currency code converted from (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromCurrency(Nullable<String> fromCurrency) {
            if (fromCurrency.isNull()) {
                this.fromCurrency = null;
            } else if (fromCurrency.isEmpty()) {
                this.fromCurrency = Optional.empty();
            } else {
                this.fromCurrency = Optional.of(fromCurrency.get());
            }
            return this;
        }

        /**
         * <p>Lowercase currency code converted from (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromCurrency(String fromCurrency) {
            this.fromCurrency = Optional.ofNullable(fromCurrency);
            return this;
        }

        /**
         * <p>Lowercase currency code converted from (swap sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "from_currency", nulls = Nulls.SKIP)
        public _FinalStage fromCurrency(Optional<String> fromCurrency) {
            this.fromCurrency = fromCurrency;
            return this;
        }

        /**
         * <p>Amount converted out of from_currency as a decimal string (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromAmount(Nullable<String> fromAmount) {
            if (fromAmount.isNull()) {
                this.fromAmount = null;
            } else if (fromAmount.isEmpty()) {
                this.fromAmount = Optional.empty();
            } else {
                this.fromAmount = Optional.of(fromAmount.get());
            }
            return this;
        }

        /**
         * <p>Amount converted out of from_currency as a decimal string (swap sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromAmount(String fromAmount) {
            this.fromAmount = Optional.ofNullable(fromAmount);
            return this;
        }

        /**
         * <p>Amount converted out of from_currency as a decimal string (swap sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "from_amount", nulls = Nulls.SKIP)
        public _FinalStage fromAmount(Optional<String> fromAmount) {
            this.fromAmount = fromAmount;
            return this;
        }

        /**
         * <p>Estimated arrival as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedArrival(Nullable<OffsetDateTime> estimatedArrival) {
            if (estimatedArrival.isNull()) {
                this.estimatedArrival = null;
            } else if (estimatedArrival.isEmpty()) {
                this.estimatedArrival = Optional.empty();
            } else {
                this.estimatedArrival = Optional.of(estimatedArrival.get());
            }
            return this;
        }

        /**
         * <p>Estimated arrival as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedArrival(OffsetDateTime estimatedArrival) {
            this.estimatedArrival = Optional.ofNullable(estimatedArrival);
            return this;
        }

        /**
         * <p>Estimated arrival as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "estimated_arrival", nulls = Nulls.SKIP)
        public _FinalStage estimatedArrival(Optional<OffsetDateTime> estimatedArrival) {
            this.estimatedArrival = estimatedArrival;
            return this;
        }

        /**
         * <p>Withdrawal creation time as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage createdAt(Nullable<OffsetDateTime> createdAt) {
            if (createdAt.isNull()) {
                this.createdAt = null;
            } else if (createdAt.isEmpty()) {
                this.createdAt = Optional.empty();
            } else {
                this.createdAt = Optional.of(createdAt.get());
            }
            return this;
        }

        /**
         * <p>Withdrawal creation time as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage createdAt(OffsetDateTime createdAt) {
            this.createdAt = Optional.ofNullable(createdAt);
            return this;
        }

        /**
         * <p>Withdrawal creation time as an ISO 8601 timestamp (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "created_at", nulls = Nulls.SKIP)
        public _FinalStage createdAt(Optional<OffsetDateTime> createdAt) {
            this.createdAt = createdAt;
            return this;
        }

        /**
         * <p>Public claim URL for the airdrop link (airdrop_link sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage claimUrl(Nullable<String> claimUrl) {
            if (claimUrl.isNull()) {
                this.claimUrl = null;
            } else if (claimUrl.isEmpty()) {
                this.claimUrl = Optional.empty();
            } else {
                this.claimUrl = Optional.of(claimUrl.get());
            }
            return this;
        }

        /**
         * <p>Public claim URL for the airdrop link (airdrop_link sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage claimUrl(String claimUrl) {
            this.claimUrl = Optional.ofNullable(claimUrl);
            return this;
        }

        /**
         * <p>Public claim URL for the airdrop link (airdrop_link sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "claim_url", nulls = Nulls.SKIP)
        public _FinalStage claimUrl(Optional<String> claimUrl) {
            this.claimUrl = claimUrl;
            return this;
        }

        /**
         * <p>Chain the deposit landed on, for example plasma (onchain_transaction sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage chain(Nullable<String> chain) {
            if (chain.isNull()) {
                this.chain = null;
            } else if (chain.isEmpty()) {
                this.chain = Optional.empty();
            } else {
                this.chain = Optional.of(chain.get());
            }
            return this;
        }

        /**
         * <p>Chain the deposit landed on, for example plasma (onchain_transaction sources only).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage chain(String chain) {
            this.chain = Optional.ofNullable(chain);
            return this;
        }

        /**
         * <p>Chain the deposit landed on, for example plasma (onchain_transaction sources only).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "chain", nulls = Nulls.SKIP)
        public _FinalStage chain(Optional<String> chain) {
            this.chain = chain;
            return this;
        }

        /**
         * <p>Withdrawal amount as a decimal number in the destination currency (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountFloat(Nullable<Double> amountFloat) {
            if (amountFloat.isNull()) {
                this.amountFloat = null;
            } else if (amountFloat.isEmpty()) {
                this.amountFloat = Optional.empty();
            } else {
                this.amountFloat = Optional.of(amountFloat.get());
            }
            return this;
        }

        /**
         * <p>Withdrawal amount as a decimal number in the destination currency (withdrawal sources only; requires payout:withdrawal:read).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountFloat(Double amountFloat) {
            this.amountFloat = Optional.ofNullable(amountFloat);
            return this;
        }

        /**
         * <p>Withdrawal amount as a decimal number in the destination currency (withdrawal sources only; requires payout:withdrawal:read).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount_float", nulls = Nulls.SKIP)
        public _FinalStage amountFloat(Optional<Double> amountFloat) {
            this.amountFloat = amountFloat;
            return this;
        }

        @java.lang.Override
        public LedgerActivitySource build() {
            return new LedgerActivitySource(
                    amountFloat,
                    chain,
                    claimUrl,
                    createdAt,
                    estimatedArrival,
                    fromAmount,
                    fromCurrency,
                    id,
                    object,
                    payerName,
                    payoutDestination,
                    payoutTokenNickname,
                    reason,
                    senderAddress,
                    status,
                    toAmount,
                    toCurrency,
                    txHash,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
