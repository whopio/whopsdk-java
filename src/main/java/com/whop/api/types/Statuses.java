/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class Statuses {
    public static final Statuses PUBLISHED = new Statuses(Value.PUBLISHED, "published");

    public static final Statuses SCHEDULED = new Statuses(Value.SCHEDULED, "scheduled");

    public static final Statuses ARCHIVED = new Statuses(Value.ARCHIVED, "archived");

    private final Value value;

    private final String string;

    Statuses(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof Statuses && this.string.equals(((Statuses) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PUBLISHED:
                return visitor.visitPublished();
            case SCHEDULED:
                return visitor.visitScheduled();
            case ARCHIVED:
                return visitor.visitArchived();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static Statuses valueOf(String value) {
        switch (value) {
            case "published":
                return PUBLISHED;
            case "scheduled":
                return SCHEDULED;
            case "archived":
                return ARCHIVED;
            default:
                return new Statuses(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PUBLISHED,

        ARCHIVED,

        SCHEDULED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPublished();

        T visitArchived();

        T visitScheduled();

        T visitUnknown(String unknownType);
    }
}
