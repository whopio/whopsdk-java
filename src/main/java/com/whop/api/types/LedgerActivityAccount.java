/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class LedgerActivityAccount {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private LedgerActivityAccount(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static LedgerActivityAccount account(LedgerActivityAccountAccount value) {
        return new LedgerActivityAccount(new AccountValue(value));
    }

    public static LedgerActivityAccount user(LedgerActivityAccountUser value) {
        return new LedgerActivityAccount(new UserValue(value));
    }

    public boolean isAccount() {
        return value instanceof AccountValue;
    }

    public boolean isUser() {
        return value instanceof UserValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<LedgerActivityAccountAccount> getAccount() {
        if (isAccount()) {
            return Optional.of(((AccountValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<LedgerActivityAccountUser> getUser() {
        if (isUser()) {
            return Optional.of(((UserValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof LedgerActivityAccount && value.equals(((LedgerActivityAccount) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAccount(LedgerActivityAccountAccount account);

        T visitUser(LedgerActivityAccountUser user);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "object", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(AccountValue.class), @JsonSubTypes.Type(UserValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("account")
    @JsonIgnoreProperties("object")
    private static final class AccountValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "object", allowSetters = true)
        private LedgerActivityAccountAccount value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AccountValue() {}

        private AccountValue(LedgerActivityAccountAccount value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAccount(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AccountValue && equalTo((AccountValue) other);
        }

        private boolean equalTo(AccountValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "LedgerActivityAccount{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("user")
    @JsonIgnoreProperties("object")
    private static final class UserValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "object", allowSetters = true)
        private LedgerActivityAccountUser value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UserValue() {}

        private UserValue(LedgerActivityAccountUser value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUser(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UserValue && equalTo((UserValue) other);
        }

        private boolean equalTo(UserValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "LedgerActivityAccount{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("object")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "LedgerActivityAccount{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
