/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Withdrawal.Builder.class)
public final class Withdrawal {
    private final double amount;

    private final OffsetDateTime createdAt;

    private final Currencies currency;

    private final Optional<PayoutErrorCodes> errorCode;

    private final Optional<String> errorMessage;

    private final Optional<OffsetDateTime> estimatedAvailability;

    private final double feeAmount;

    private final Optional<WithdrawalFeeTypes> feeType;

    private final String id;

    private final WithdrawalLedgerAccount ledgerAccount;

    private final double markupFee;

    private final Optional<String> payoutRequestId;

    private final Optional<WithdrawalPayoutToken> payoutToken;

    private final WithdrawalSpeeds speed;

    private final WithdrawalStatus status;

    private final Optional<String> traceCode;

    private final Map<String, Object> additionalProperties;

    private Withdrawal(
            double amount,
            OffsetDateTime createdAt,
            Currencies currency,
            Optional<PayoutErrorCodes> errorCode,
            Optional<String> errorMessage,
            Optional<OffsetDateTime> estimatedAvailability,
            double feeAmount,
            Optional<WithdrawalFeeTypes> feeType,
            String id,
            WithdrawalLedgerAccount ledgerAccount,
            double markupFee,
            Optional<String> payoutRequestId,
            Optional<WithdrawalPayoutToken> payoutToken,
            WithdrawalSpeeds speed,
            WithdrawalStatus status,
            Optional<String> traceCode,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.createdAt = createdAt;
        this.currency = currency;
        this.errorCode = errorCode;
        this.errorMessage = errorMessage;
        this.estimatedAvailability = estimatedAvailability;
        this.feeAmount = feeAmount;
        this.feeType = feeType;
        this.id = id;
        this.ledgerAccount = ledgerAccount;
        this.markupFee = markupFee;
        this.payoutRequestId = payoutRequestId;
        this.payoutToken = payoutToken;
        this.speed = speed;
        this.status = status;
        this.traceCode = traceCode;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The withdrawal amount as a decimal number in the specified currency (e.g., 100.00 for $100.00 USD).
     */
    @JsonProperty("amount")
    public double getAmount() {
        return amount;
    }

    /**
     * @return The datetime the withdrawal was created.
     */
    @JsonProperty("created_at")
    public OffsetDateTime getCreatedAt() {
        return createdAt;
    }

    /**
     * @return The three-letter ISO currency code for this withdrawal (e.g., 'usd', 'eur').
     */
    @JsonProperty("currency")
    public Currencies getCurrency() {
        return currency;
    }

    /**
     * @return A machine-readable error code describing why the payout failed. Null if no error occurred.
     */
    @JsonIgnore
    public Optional<PayoutErrorCodes> getErrorCode() {
        if (errorCode == null) {
            return Optional.empty();
        }
        return errorCode;
    }

    /**
     * @return A human-readable message describing why the payout failed. Null if no error occurred.
     */
    @JsonIgnore
    public Optional<String> getErrorMessage() {
        if (errorMessage == null) {
            return Optional.empty();
        }
        return errorMessage;
    }

    /**
     * @return The estimated time at which the funds become available in the destination account. Null if no estimate is available. As a Unix timestamp.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getEstimatedAvailability() {
        if (estimatedAvailability == null) {
            return Optional.empty();
        }
        return estimatedAvailability;
    }

    /**
     * @return The fee charged for processing this withdrawal, in the same currency as the withdrawal amount.
     */
    @JsonProperty("fee_amount")
    public double getFeeAmount() {
        return feeAmount;
    }

    /**
     * @return How the fee was applied to the withdrawal. 'exclusive' means the fee was added on top (user receives the full requested amount). 'inclusive' means the fee was deducted from the withdrawal (user receives less than requested). Null if no fee was charged.
     */
    @JsonIgnore
    public Optional<WithdrawalFeeTypes> getFeeType() {
        if (feeType == null) {
            return Optional.empty();
        }
        return feeType;
    }

    /**
     * @return The unique identifier for the withdrawal.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The ledger account from which the withdrawal funds are sourced.
     */
    @JsonProperty("ledger_account")
    public WithdrawalLedgerAccount getLedgerAccount() {
        return ledgerAccount;
    }

    /**
     * @return An additional markup fee charged for the withdrawal, in the same currency as the withdrawal amount. Only applies to platform accounts using Whop Rails.
     */
    @JsonProperty("markup_fee")
    public double getMarkupFee() {
        return markupFee;
    }

    /**
     * @return The id of the payout request (returned by POST /payouts) that this withdrawal settles. Null unless the withdrawal originated from a stablecoin payout.
     */
    @JsonIgnore
    public Optional<String> getPayoutRequestId() {
        if (payoutRequestId == null) {
            return Optional.empty();
        }
        return payoutRequestId;
    }

    /**
     * @return The saved payout destination used for this withdrawal (e.g., a bank account or PayPal address). Null if no payout token was used.
     */
    @JsonIgnore
    public Optional<WithdrawalPayoutToken> getPayoutToken() {
        if (payoutToken == null) {
            return Optional.empty();
        }
        return payoutToken;
    }

    /**
     * @return The processing speed selected for this withdrawal ('standard' or 'instant').
     */
    @JsonProperty("speed")
    public WithdrawalSpeeds getSpeed() {
        return speed;
    }

    /**
     * @return The computed lifecycle status of the withdrawal, accounting for the state of associated payouts (e.g., 'requested', 'in_transit', 'completed', 'failed').
     */
    @JsonProperty("status")
    public WithdrawalStatus getStatus() {
        return status;
    }

    /**
     * @return The ACH trace number for tracking the payout through the banking network. Null if not available or not an ACH transaction.
     */
    @JsonIgnore
    public Optional<String> getTraceCode() {
        if (traceCode == null) {
            return Optional.empty();
        }
        return traceCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("error_code")
    private Optional<PayoutErrorCodes> _getErrorCode() {
        return errorCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("error_message")
    private Optional<String> _getErrorMessage() {
        return errorMessage;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("estimated_availability")
    private Optional<OffsetDateTime> _getEstimatedAvailability() {
        return estimatedAvailability;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("fee_type")
    private Optional<WithdrawalFeeTypes> _getFeeType() {
        return feeType;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payout_request_id")
    private Optional<String> _getPayoutRequestId() {
        return payoutRequestId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payout_token")
    private Optional<WithdrawalPayoutToken> _getPayoutToken() {
        return payoutToken;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("trace_code")
    private Optional<String> _getTraceCode() {
        return traceCode;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Withdrawal && equalTo((Withdrawal) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Withdrawal other) {
        return amount == other.amount
                && createdAt.equals(other.createdAt)
                && currency.equals(other.currency)
                && errorCode.equals(other.errorCode)
                && errorMessage.equals(other.errorMessage)
                && estimatedAvailability.equals(other.estimatedAvailability)
                && feeAmount == other.feeAmount
                && feeType.equals(other.feeType)
                && id.equals(other.id)
                && ledgerAccount.equals(other.ledgerAccount)
                && markupFee == other.markupFee
                && payoutRequestId.equals(other.payoutRequestId)
                && payoutToken.equals(other.payoutToken)
                && speed.equals(other.speed)
                && status.equals(other.status)
                && traceCode.equals(other.traceCode);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amount,
                this.createdAt,
                this.currency,
                this.errorCode,
                this.errorMessage,
                this.estimatedAvailability,
                this.feeAmount,
                this.feeType,
                this.id,
                this.ledgerAccount,
                this.markupFee,
                this.payoutRequestId,
                this.payoutToken,
                this.speed,
                this.status,
                this.traceCode);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>The withdrawal amount as a decimal number in the specified currency (e.g., 100.00 for $100.00 USD).</p>
         */
        CreatedAtStage amount(double amount);

        Builder from(Withdrawal other);
    }

    public interface CreatedAtStage {
        /**
         * <p>The datetime the withdrawal was created.</p>
         */
        CurrencyStage createdAt(@NotNull OffsetDateTime createdAt);
    }

    public interface CurrencyStage {
        /**
         * <p>The three-letter ISO currency code for this withdrawal (e.g., 'usd', 'eur').</p>
         */
        FeeAmountStage currency(@NotNull Currencies currency);
    }

    public interface FeeAmountStage {
        /**
         * <p>The fee charged for processing this withdrawal, in the same currency as the withdrawal amount.</p>
         */
        IdStage feeAmount(double feeAmount);
    }

    public interface IdStage {
        /**
         * <p>The unique identifier for the withdrawal.</p>
         */
        LedgerAccountStage id(@NotNull String id);
    }

    public interface LedgerAccountStage {
        /**
         * <p>The ledger account from which the withdrawal funds are sourced.</p>
         */
        MarkupFeeStage ledgerAccount(@NotNull WithdrawalLedgerAccount ledgerAccount);
    }

    public interface MarkupFeeStage {
        /**
         * <p>An additional markup fee charged for the withdrawal, in the same currency as the withdrawal amount. Only applies to platform accounts using Whop Rails.</p>
         */
        SpeedStage markupFee(double markupFee);
    }

    public interface SpeedStage {
        /**
         * <p>The processing speed selected for this withdrawal ('standard' or 'instant').</p>
         */
        StatusStage speed(@NotNull WithdrawalSpeeds speed);
    }

    public interface StatusStage {
        /**
         * <p>The computed lifecycle status of the withdrawal, accounting for the state of associated payouts (e.g., 'requested', 'in_transit', 'completed', 'failed').</p>
         */
        _FinalStage status(@NotNull WithdrawalStatus status);
    }

    public interface _FinalStage {
        Withdrawal build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>A machine-readable error code describing why the payout failed. Null if no error occurred.</p>
         */
        _FinalStage errorCode(Optional<PayoutErrorCodes> errorCode);

        _FinalStage errorCode(PayoutErrorCodes errorCode);

        _FinalStage errorCode(Nullable<PayoutErrorCodes> errorCode);

        /**
         * <p>A human-readable message describing why the payout failed. Null if no error occurred.</p>
         */
        _FinalStage errorMessage(Optional<String> errorMessage);

        _FinalStage errorMessage(String errorMessage);

        _FinalStage errorMessage(Nullable<String> errorMessage);

        /**
         * <p>The estimated time at which the funds become available in the destination account. Null if no estimate is available. As a Unix timestamp.</p>
         */
        _FinalStage estimatedAvailability(Optional<OffsetDateTime> estimatedAvailability);

        _FinalStage estimatedAvailability(OffsetDateTime estimatedAvailability);

        _FinalStage estimatedAvailability(Nullable<OffsetDateTime> estimatedAvailability);

        /**
         * <p>How the fee was applied to the withdrawal. 'exclusive' means the fee was added on top (user receives the full requested amount). 'inclusive' means the fee was deducted from the withdrawal (user receives less than requested). Null if no fee was charged.</p>
         */
        _FinalStage feeType(Optional<WithdrawalFeeTypes> feeType);

        _FinalStage feeType(WithdrawalFeeTypes feeType);

        _FinalStage feeType(Nullable<WithdrawalFeeTypes> feeType);

        /**
         * <p>The id of the payout request (returned by POST /payouts) that this withdrawal settles. Null unless the withdrawal originated from a stablecoin payout.</p>
         */
        _FinalStage payoutRequestId(Optional<String> payoutRequestId);

        _FinalStage payoutRequestId(String payoutRequestId);

        _FinalStage payoutRequestId(Nullable<String> payoutRequestId);

        /**
         * <p>The saved payout destination used for this withdrawal (e.g., a bank account or PayPal address). Null if no payout token was used.</p>
         */
        _FinalStage payoutToken(Optional<WithdrawalPayoutToken> payoutToken);

        _FinalStage payoutToken(WithdrawalPayoutToken payoutToken);

        _FinalStage payoutToken(Nullable<WithdrawalPayoutToken> payoutToken);

        /**
         * <p>The ACH trace number for tracking the payout through the banking network. Null if not available or not an ACH transaction.</p>
         */
        _FinalStage traceCode(Optional<String> traceCode);

        _FinalStage traceCode(String traceCode);

        _FinalStage traceCode(Nullable<String> traceCode);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AmountStage,
                    CreatedAtStage,
                    CurrencyStage,
                    FeeAmountStage,
                    IdStage,
                    LedgerAccountStage,
                    MarkupFeeStage,
                    SpeedStage,
                    StatusStage,
                    _FinalStage {
        private double amount;

        private OffsetDateTime createdAt;

        private Currencies currency;

        private double feeAmount;

        private String id;

        private WithdrawalLedgerAccount ledgerAccount;

        private double markupFee;

        private WithdrawalSpeeds speed;

        private WithdrawalStatus status;

        private Optional<String> traceCode = Optional.empty();

        private Optional<WithdrawalPayoutToken> payoutToken = Optional.empty();

        private Optional<String> payoutRequestId = Optional.empty();

        private Optional<WithdrawalFeeTypes> feeType = Optional.empty();

        private Optional<OffsetDateTime> estimatedAvailability = Optional.empty();

        private Optional<String> errorMessage = Optional.empty();

        private Optional<PayoutErrorCodes> errorCode = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Withdrawal other) {
            amount(other.getAmount());
            createdAt(other.getCreatedAt());
            currency(other.getCurrency());
            errorCode(other.getErrorCode());
            errorMessage(other.getErrorMessage());
            estimatedAvailability(other.getEstimatedAvailability());
            feeAmount(other.getFeeAmount());
            feeType(other.getFeeType());
            id(other.getId());
            ledgerAccount(other.getLedgerAccount());
            markupFee(other.getMarkupFee());
            payoutRequestId(other.getPayoutRequestId());
            payoutToken(other.getPayoutToken());
            speed(other.getSpeed());
            status(other.getStatus());
            traceCode(other.getTraceCode());
            return this;
        }

        /**
         * <p>The withdrawal amount as a decimal number in the specified currency (e.g., 100.00 for $100.00 USD).</p>
         * <p>The withdrawal amount as a decimal number in the specified currency (e.g., 100.00 for $100.00 USD).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public CreatedAtStage amount(double amount) {
            this.amount = amount;
            return this;
        }

        /**
         * <p>The datetime the withdrawal was created.</p>
         * <p>The datetime the withdrawal was created.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public CurrencyStage createdAt(@NotNull OffsetDateTime createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>The three-letter ISO currency code for this withdrawal (e.g., 'usd', 'eur').</p>
         * <p>The three-letter ISO currency code for this withdrawal (e.g., 'usd', 'eur').</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public FeeAmountStage currency(@NotNull Currencies currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>The fee charged for processing this withdrawal, in the same currency as the withdrawal amount.</p>
         * <p>The fee charged for processing this withdrawal, in the same currency as the withdrawal amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("fee_amount")
        public IdStage feeAmount(double feeAmount) {
            this.feeAmount = feeAmount;
            return this;
        }

        /**
         * <p>The unique identifier for the withdrawal.</p>
         * <p>The unique identifier for the withdrawal.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public LedgerAccountStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The ledger account from which the withdrawal funds are sourced.</p>
         * <p>The ledger account from which the withdrawal funds are sourced.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ledger_account")
        public MarkupFeeStage ledgerAccount(@NotNull WithdrawalLedgerAccount ledgerAccount) {
            this.ledgerAccount = Objects.requireNonNull(ledgerAccount, "ledgerAccount must not be null");
            return this;
        }

        /**
         * <p>An additional markup fee charged for the withdrawal, in the same currency as the withdrawal amount. Only applies to platform accounts using Whop Rails.</p>
         * <p>An additional markup fee charged for the withdrawal, in the same currency as the withdrawal amount. Only applies to platform accounts using Whop Rails.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("markup_fee")
        public SpeedStage markupFee(double markupFee) {
            this.markupFee = markupFee;
            return this;
        }

        /**
         * <p>The processing speed selected for this withdrawal ('standard' or 'instant').</p>
         * <p>The processing speed selected for this withdrawal ('standard' or 'instant').</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("speed")
        public StatusStage speed(@NotNull WithdrawalSpeeds speed) {
            this.speed = Objects.requireNonNull(speed, "speed must not be null");
            return this;
        }

        /**
         * <p>The computed lifecycle status of the withdrawal, accounting for the state of associated payouts (e.g., 'requested', 'in_transit', 'completed', 'failed').</p>
         * <p>The computed lifecycle status of the withdrawal, accounting for the state of associated payouts (e.g., 'requested', 'in_transit', 'completed', 'failed').</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull WithdrawalStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>The ACH trace number for tracking the payout through the banking network. Null if not available or not an ACH transaction.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage traceCode(Nullable<String> traceCode) {
            if (traceCode.isNull()) {
                this.traceCode = null;
            } else if (traceCode.isEmpty()) {
                this.traceCode = Optional.empty();
            } else {
                this.traceCode = Optional.of(traceCode.get());
            }
            return this;
        }

        /**
         * <p>The ACH trace number for tracking the payout through the banking network. Null if not available or not an ACH transaction.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage traceCode(String traceCode) {
            this.traceCode = Optional.ofNullable(traceCode);
            return this;
        }

        /**
         * <p>The ACH trace number for tracking the payout through the banking network. Null if not available or not an ACH transaction.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "trace_code", nulls = Nulls.SKIP)
        public _FinalStage traceCode(Optional<String> traceCode) {
            this.traceCode = traceCode;
            return this;
        }

        /**
         * <p>The saved payout destination used for this withdrawal (e.g., a bank account or PayPal address). Null if no payout token was used.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutToken(Nullable<WithdrawalPayoutToken> payoutToken) {
            if (payoutToken.isNull()) {
                this.payoutToken = null;
            } else if (payoutToken.isEmpty()) {
                this.payoutToken = Optional.empty();
            } else {
                this.payoutToken = Optional.of(payoutToken.get());
            }
            return this;
        }

        /**
         * <p>The saved payout destination used for this withdrawal (e.g., a bank account or PayPal address). Null if no payout token was used.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutToken(WithdrawalPayoutToken payoutToken) {
            this.payoutToken = Optional.ofNullable(payoutToken);
            return this;
        }

        /**
         * <p>The saved payout destination used for this withdrawal (e.g., a bank account or PayPal address). Null if no payout token was used.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payout_token", nulls = Nulls.SKIP)
        public _FinalStage payoutToken(Optional<WithdrawalPayoutToken> payoutToken) {
            this.payoutToken = payoutToken;
            return this;
        }

        /**
         * <p>The id of the payout request (returned by POST /payouts) that this withdrawal settles. Null unless the withdrawal originated from a stablecoin payout.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutRequestId(Nullable<String> payoutRequestId) {
            if (payoutRequestId.isNull()) {
                this.payoutRequestId = null;
            } else if (payoutRequestId.isEmpty()) {
                this.payoutRequestId = Optional.empty();
            } else {
                this.payoutRequestId = Optional.of(payoutRequestId.get());
            }
            return this;
        }

        /**
         * <p>The id of the payout request (returned by POST /payouts) that this withdrawal settles. Null unless the withdrawal originated from a stablecoin payout.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutRequestId(String payoutRequestId) {
            this.payoutRequestId = Optional.ofNullable(payoutRequestId);
            return this;
        }

        /**
         * <p>The id of the payout request (returned by POST /payouts) that this withdrawal settles. Null unless the withdrawal originated from a stablecoin payout.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payout_request_id", nulls = Nulls.SKIP)
        public _FinalStage payoutRequestId(Optional<String> payoutRequestId) {
            this.payoutRequestId = payoutRequestId;
            return this;
        }

        /**
         * <p>How the fee was applied to the withdrawal. 'exclusive' means the fee was added on top (user receives the full requested amount). 'inclusive' means the fee was deducted from the withdrawal (user receives less than requested). Null if no fee was charged.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage feeType(Nullable<WithdrawalFeeTypes> feeType) {
            if (feeType.isNull()) {
                this.feeType = null;
            } else if (feeType.isEmpty()) {
                this.feeType = Optional.empty();
            } else {
                this.feeType = Optional.of(feeType.get());
            }
            return this;
        }

        /**
         * <p>How the fee was applied to the withdrawal. 'exclusive' means the fee was added on top (user receives the full requested amount). 'inclusive' means the fee was deducted from the withdrawal (user receives less than requested). Null if no fee was charged.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage feeType(WithdrawalFeeTypes feeType) {
            this.feeType = Optional.ofNullable(feeType);
            return this;
        }

        /**
         * <p>How the fee was applied to the withdrawal. 'exclusive' means the fee was added on top (user receives the full requested amount). 'inclusive' means the fee was deducted from the withdrawal (user receives less than requested). Null if no fee was charged.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "fee_type", nulls = Nulls.SKIP)
        public _FinalStage feeType(Optional<WithdrawalFeeTypes> feeType) {
            this.feeType = feeType;
            return this;
        }

        /**
         * <p>The estimated time at which the funds become available in the destination account. Null if no estimate is available. As a Unix timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedAvailability(Nullable<OffsetDateTime> estimatedAvailability) {
            if (estimatedAvailability.isNull()) {
                this.estimatedAvailability = null;
            } else if (estimatedAvailability.isEmpty()) {
                this.estimatedAvailability = Optional.empty();
            } else {
                this.estimatedAvailability = Optional.of(estimatedAvailability.get());
            }
            return this;
        }

        /**
         * <p>The estimated time at which the funds become available in the destination account. Null if no estimate is available. As a Unix timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedAvailability(OffsetDateTime estimatedAvailability) {
            this.estimatedAvailability = Optional.ofNullable(estimatedAvailability);
            return this;
        }

        /**
         * <p>The estimated time at which the funds become available in the destination account. Null if no estimate is available. As a Unix timestamp.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "estimated_availability", nulls = Nulls.SKIP)
        public _FinalStage estimatedAvailability(Optional<OffsetDateTime> estimatedAvailability) {
            this.estimatedAvailability = estimatedAvailability;
            return this;
        }

        /**
         * <p>A human-readable message describing why the payout failed. Null if no error occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage errorMessage(Nullable<String> errorMessage) {
            if (errorMessage.isNull()) {
                this.errorMessage = null;
            } else if (errorMessage.isEmpty()) {
                this.errorMessage = Optional.empty();
            } else {
                this.errorMessage = Optional.of(errorMessage.get());
            }
            return this;
        }

        /**
         * <p>A human-readable message describing why the payout failed. Null if no error occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage errorMessage(String errorMessage) {
            this.errorMessage = Optional.ofNullable(errorMessage);
            return this;
        }

        /**
         * <p>A human-readable message describing why the payout failed. Null if no error occurred.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "error_message", nulls = Nulls.SKIP)
        public _FinalStage errorMessage(Optional<String> errorMessage) {
            this.errorMessage = errorMessage;
            return this;
        }

        /**
         * <p>A machine-readable error code describing why the payout failed. Null if no error occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage errorCode(Nullable<PayoutErrorCodes> errorCode) {
            if (errorCode.isNull()) {
                this.errorCode = null;
            } else if (errorCode.isEmpty()) {
                this.errorCode = Optional.empty();
            } else {
                this.errorCode = Optional.of(errorCode.get());
            }
            return this;
        }

        /**
         * <p>A machine-readable error code describing why the payout failed. Null if no error occurred.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage errorCode(PayoutErrorCodes errorCode) {
            this.errorCode = Optional.ofNullable(errorCode);
            return this;
        }

        /**
         * <p>A machine-readable error code describing why the payout failed. Null if no error occurred.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "error_code", nulls = Nulls.SKIP)
        public _FinalStage errorCode(Optional<PayoutErrorCodes> errorCode) {
            this.errorCode = errorCode;
            return this;
        }

        @java.lang.Override
        public Withdrawal build() {
            return new Withdrawal(
                    amount,
                    createdAt,
                    currency,
                    errorCode,
                    errorMessage,
                    estimatedAvailability,
                    feeAmount,
                    feeType,
                    id,
                    ledgerAccount,
                    markupFee,
                    payoutRequestId,
                    payoutToken,
                    speed,
                    status,
                    traceCode,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
