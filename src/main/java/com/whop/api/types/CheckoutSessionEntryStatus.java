/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CheckoutSessionEntryStatus {
    public static final CheckoutSessionEntryStatus FAILED = new CheckoutSessionEntryStatus(Value.FAILED, "failed");

    public static final CheckoutSessionEntryStatus SUCCEEDED =
            new CheckoutSessionEntryStatus(Value.SUCCEEDED, "succeeded");

    public static final CheckoutSessionEntryStatus PROCESSING =
            new CheckoutSessionEntryStatus(Value.PROCESSING, "processing");

    public static final CheckoutSessionEntryStatus REQUIRES_ACTION =
            new CheckoutSessionEntryStatus(Value.REQUIRES_ACTION, "requires_action");

    private final Value value;

    private final String string;

    CheckoutSessionEntryStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CheckoutSessionEntryStatus
                        && this.string.equals(((CheckoutSessionEntryStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FAILED:
                return visitor.visitFailed();
            case SUCCEEDED:
                return visitor.visitSucceeded();
            case PROCESSING:
                return visitor.visitProcessing();
            case REQUIRES_ACTION:
                return visitor.visitRequiresAction();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CheckoutSessionEntryStatus valueOf(String value) {
        switch (value) {
            case "failed":
                return FAILED;
            case "succeeded":
                return SUCCEEDED;
            case "processing":
                return PROCESSING;
            case "requires_action":
                return REQUIRES_ACTION;
            default:
                return new CheckoutSessionEntryStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        REQUIRES_ACTION,

        PROCESSING,

        SUCCEEDED,

        FAILED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitRequiresAction();

        T visitProcessing();

        T visitSucceeded();

        T visitFailed();

        T visitUnknown(String unknownType);
    }
}
