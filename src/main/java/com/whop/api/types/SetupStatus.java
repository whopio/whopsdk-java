/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = SetupStatus.Builder.class)
public final class SetupStatus {
    private final String id;

    private final Optional<SetupLastSetupError> lastSetupError;

    private final Optional<PaymentNextAction> nextAction;

    private final String object;

    private final Optional<String> returnUrl;

    private final SetupStatusStatus status;

    private final Map<String, Object> additionalProperties;

    private SetupStatus(
            String id,
            Optional<SetupLastSetupError> lastSetupError,
            Optional<PaymentNextAction> nextAction,
            String object,
            Optional<String> returnUrl,
            SetupStatusStatus status,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.lastSetupError = lastSetupError;
        this.nextAction = nextAction;
        this.object = object;
        this.returnUrl = returnUrl;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The setup this status describes, prefixed <code>sint_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Why the setup ended where it did, or <code>null</code> when nothing has failed. Present on <code>canceled</code> — a buyer who abandoned carries no code, one refused by the provider does.
     */
    @JsonIgnore
    public Optional<SetupLastSetupError> getLastSetupError() {
        if (lastSetupError == null) {
            return Optional.empty();
        }
        return lastSetupError;
    }

    /**
     * @return What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.
     */
    @JsonIgnore
    public Optional<PaymentNextAction> getNextAction() {
        if (nextAction == null) {
            return Optional.empty();
        }
        return nextAction;
    }

    /**
     * @return Always <code>setup_status</code>.
     */
    @JsonProperty("object")
    public String getObject() {
        return object;
    }

    /**
     * @return Where to send the buyer once the setup reaches a resting state, or <code>null</code> to leave them where they are.
     */
    @JsonIgnore
    public Optional<String> getReturnUrl() {
        if (returnUrl == null) {
            return Optional.empty();
        }
        return returnUrl;
    }

    /**
     * @return How far the setup has got. <strong>A 200 means we answered, not that the method was saved — always branch on this.</strong> <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>processing</code> — the buyer has done their part and the processor is deciding. <code>succeeded</code> — the payment method is saved, and only this one means saved. <code>canceled</code> — abandoned or refused; see <code>last_setup_error</code> to tell which.
     */
    @JsonProperty("status")
    public SetupStatusStatus getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_setup_error")
    private Optional<SetupLastSetupError> _getLastSetupError() {
        return lastSetupError;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("next_action")
    private Optional<PaymentNextAction> _getNextAction() {
        return nextAction;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("return_url")
    private Optional<String> _getReturnUrl() {
        return returnUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SetupStatus && equalTo((SetupStatus) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(SetupStatus other) {
        return id.equals(other.id)
                && lastSetupError.equals(other.lastSetupError)
                && nextAction.equals(other.nextAction)
                && object.equals(other.object)
                && returnUrl.equals(other.returnUrl)
                && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.lastSetupError, this.nextAction, this.object, this.returnUrl, this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The setup this status describes, prefixed <code>sint_</code>.</p>
         */
        ObjectStage id(@NotNull String id);

        Builder from(SetupStatus other);
    }

    public interface ObjectStage {
        /**
         * <p>Always <code>setup_status</code>.</p>
         */
        StatusStage object(@NotNull String object);
    }

    public interface StatusStage {
        /**
         * <p>How far the setup has got. <strong>A 200 means we answered, not that the method was saved — always branch on this.</strong> <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>processing</code> — the buyer has done their part and the processor is deciding. <code>succeeded</code> — the payment method is saved, and only this one means saved. <code>canceled</code> — abandoned or refused; see <code>last_setup_error</code> to tell which.</p>
         */
        _FinalStage status(@NotNull SetupStatusStatus status);
    }

    public interface _FinalStage {
        SetupStatus build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Why the setup ended where it did, or <code>null</code> when nothing has failed. Present on <code>canceled</code> — a buyer who abandoned carries no code, one refused by the provider does.</p>
         */
        _FinalStage lastSetupError(Optional<SetupLastSetupError> lastSetupError);

        _FinalStage lastSetupError(SetupLastSetupError lastSetupError);

        _FinalStage lastSetupError(Nullable<SetupLastSetupError> lastSetupError);

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         */
        _FinalStage nextAction(Optional<PaymentNextAction> nextAction);

        _FinalStage nextAction(PaymentNextAction nextAction);

        _FinalStage nextAction(Nullable<PaymentNextAction> nextAction);

        /**
         * <p>Where to send the buyer once the setup reaches a resting state, or <code>null</code> to leave them where they are.</p>
         */
        _FinalStage returnUrl(Optional<String> returnUrl);

        _FinalStage returnUrl(String returnUrl);

        _FinalStage returnUrl(Nullable<String> returnUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, ObjectStage, StatusStage, _FinalStage {
        private String id;

        private String object;

        private SetupStatusStatus status;

        private Optional<String> returnUrl = Optional.empty();

        private Optional<PaymentNextAction> nextAction = Optional.empty();

        private Optional<SetupLastSetupError> lastSetupError = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(SetupStatus other) {
            id(other.getId());
            lastSetupError(other.getLastSetupError());
            nextAction(other.getNextAction());
            object(other.getObject());
            returnUrl(other.getReturnUrl());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>The setup this status describes, prefixed <code>sint_</code>.</p>
         * <p>The setup this status describes, prefixed <code>sint_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public ObjectStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Always <code>setup_status</code>.</p>
         * <p>Always <code>setup_status</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("object")
        public StatusStage object(@NotNull String object) {
            this.object = Objects.requireNonNull(object, "object must not be null");
            return this;
        }

        /**
         * <p>How far the setup has got. <strong>A 200 means we answered, not that the method was saved — always branch on this.</strong> <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>processing</code> — the buyer has done their part and the processor is deciding. <code>succeeded</code> — the payment method is saved, and only this one means saved. <code>canceled</code> — abandoned or refused; see <code>last_setup_error</code> to tell which.</p>
         * <p>How far the setup has got. <strong>A 200 means we answered, not that the method was saved — always branch on this.</strong> <code>requires_action</code> — the buyer has a step outstanding; see <code>next_action</code>. <code>processing</code> — the buyer has done their part and the processor is deciding. <code>succeeded</code> — the payment method is saved, and only this one means saved. <code>canceled</code> — abandoned or refused; see <code>last_setup_error</code> to tell which.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull SetupStatusStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>Where to send the buyer once the setup reaches a resting state, or <code>null</code> to leave them where they are.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage returnUrl(Nullable<String> returnUrl) {
            if (returnUrl.isNull()) {
                this.returnUrl = null;
            } else if (returnUrl.isEmpty()) {
                this.returnUrl = Optional.empty();
            } else {
                this.returnUrl = Optional.of(returnUrl.get());
            }
            return this;
        }

        /**
         * <p>Where to send the buyer once the setup reaches a resting state, or <code>null</code> to leave them where they are.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage returnUrl(String returnUrl) {
            this.returnUrl = Optional.ofNullable(returnUrl);
            return this;
        }

        /**
         * <p>Where to send the buyer once the setup reaches a resting state, or <code>null</code> to leave them where they are.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "return_url", nulls = Nulls.SKIP)
        public _FinalStage returnUrl(Optional<String> returnUrl) {
            this.returnUrl = returnUrl;
            return this;
        }

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage nextAction(Nullable<PaymentNextAction> nextAction) {
            if (nextAction.isNull()) {
                this.nextAction = null;
            } else if (nextAction.isEmpty()) {
                this.nextAction = Optional.empty();
            } else {
                this.nextAction = Optional.of(nextAction.get());
            }
            return this;
        }

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage nextAction(PaymentNextAction nextAction) {
            this.nextAction = Optional.ofNullable(nextAction);
            return this;
        }

        /**
         * <p>What the buyer must do next while <code>status</code> is <code>requires_action</code>, otherwise <code>null</code>. <code>type</code> picks the shape and each variant carries only its own <code>data</code>, so switching on <code>type</code> gives you exactly that step's payload.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "next_action", nulls = Nulls.SKIP)
        public _FinalStage nextAction(Optional<PaymentNextAction> nextAction) {
            this.nextAction = nextAction;
            return this;
        }

        /**
         * <p>Why the setup ended where it did, or <code>null</code> when nothing has failed. Present on <code>canceled</code> — a buyer who abandoned carries no code, one refused by the provider does.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastSetupError(Nullable<SetupLastSetupError> lastSetupError) {
            if (lastSetupError.isNull()) {
                this.lastSetupError = null;
            } else if (lastSetupError.isEmpty()) {
                this.lastSetupError = Optional.empty();
            } else {
                this.lastSetupError = Optional.of(lastSetupError.get());
            }
            return this;
        }

        /**
         * <p>Why the setup ended where it did, or <code>null</code> when nothing has failed. Present on <code>canceled</code> — a buyer who abandoned carries no code, one refused by the provider does.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastSetupError(SetupLastSetupError lastSetupError) {
            this.lastSetupError = Optional.ofNullable(lastSetupError);
            return this;
        }

        /**
         * <p>Why the setup ended where it did, or <code>null</code> when nothing has failed. Present on <code>canceled</code> — a buyer who abandoned carries no code, one refused by the provider does.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_setup_error", nulls = Nulls.SKIP)
        public _FinalStage lastSetupError(Optional<SetupLastSetupError> lastSetupError) {
            this.lastSetupError = lastSetupError;
            return this;
        }

        @java.lang.Override
        public SetupStatus build() {
            return new SetupStatus(id, lastSetupError, nextAction, object, returnUrl, status, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
