/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AccountReserveType.Builder.class)
public final class AccountReserveType {
    private final String amount;

    private final Optional<Integer> holdPeriodDays;

    private final Optional<Double> percentage;

    private final AccountReserveTypeType type;

    private final Map<String, Object> additionalProperties;

    private AccountReserveType(
            String amount,
            Optional<Integer> holdPeriodDays,
            Optional<Double> percentage,
            AccountReserveTypeType type,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.holdPeriodDays = holdPeriodDays;
        this.percentage = percentage;
        this.type = type;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Amount held for this reason, in native units, as a decimal string.
     */
    @JsonProperty("amount")
    public String getAmount() {
        return amount;
    }

    /**
     * @return Days money is currently held for this reason before it unlocks, or <code>null</code> when release depends on something other than time. Money already held keeps the terms it was taken under.
     */
    @JsonIgnore
    public Optional<Integer> getHoldPeriodDays() {
        if (holdPeriodDays == null) {
            return Optional.empty();
        }
        return holdPeriodDays;
    }

    /**
     * @return Percentage of each incoming payment currently held for this reason, or <code>null</code> when the reason is not a percentage of anything. Money already held keeps the release date it was given, which <code>unlocks_by_date</code> reflects.
     */
    @JsonIgnore
    public Optional<Double> getPercentage() {
        if (percentage == null) {
            return Optional.empty();
        }
        return percentage;
    }

    /**
     * @return Why this part of the balance is held. <code>regular</code> is the account's standing risk reserve; <code>bnpl</code> and <code>sequra</code> cover buy-now-pay-later settlement; <code>preshipment_hold</code> covers a physical order that has not shipped yet; <code>fraud_hold</code> is held while activity is reviewed.
     */
    @JsonProperty("type")
    public AccountReserveTypeType getType() {
        return type;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("hold_period_days")
    private Optional<Integer> _getHoldPeriodDays() {
        return holdPeriodDays;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("percentage")
    private Optional<Double> _getPercentage() {
        return percentage;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AccountReserveType && equalTo((AccountReserveType) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AccountReserveType other) {
        return amount.equals(other.amount)
                && holdPeriodDays.equals(other.holdPeriodDays)
                && percentage.equals(other.percentage)
                && type.equals(other.type);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.amount, this.holdPeriodDays, this.percentage, this.type);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>Amount held for this reason, in native units, as a decimal string.</p>
         */
        TypeStage amount(@NotNull String amount);

        Builder from(AccountReserveType other);
    }

    public interface TypeStage {
        /**
         * <p>Why this part of the balance is held. <code>regular</code> is the account's standing risk reserve; <code>bnpl</code> and <code>sequra</code> cover buy-now-pay-later settlement; <code>preshipment_hold</code> covers a physical order that has not shipped yet; <code>fraud_hold</code> is held while activity is reviewed.</p>
         */
        _FinalStage type(@NotNull AccountReserveTypeType type);
    }

    public interface _FinalStage {
        AccountReserveType build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Days money is currently held for this reason before it unlocks, or <code>null</code> when release depends on something other than time. Money already held keeps the terms it was taken under.</p>
         */
        _FinalStage holdPeriodDays(Optional<Integer> holdPeriodDays);

        _FinalStage holdPeriodDays(Integer holdPeriodDays);

        _FinalStage holdPeriodDays(Nullable<Integer> holdPeriodDays);

        /**
         * <p>Percentage of each incoming payment currently held for this reason, or <code>null</code> when the reason is not a percentage of anything. Money already held keeps the release date it was given, which <code>unlocks_by_date</code> reflects.</p>
         */
        _FinalStage percentage(Optional<Double> percentage);

        _FinalStage percentage(Double percentage);

        _FinalStage percentage(Nullable<Double> percentage);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AmountStage, TypeStage, _FinalStage {
        private String amount;

        private AccountReserveTypeType type;

        private Optional<Double> percentage = Optional.empty();

        private Optional<Integer> holdPeriodDays = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AccountReserveType other) {
            amount(other.getAmount());
            holdPeriodDays(other.getHoldPeriodDays());
            percentage(other.getPercentage());
            type(other.getType());
            return this;
        }

        /**
         * <p>Amount held for this reason, in native units, as a decimal string.</p>
         * <p>Amount held for this reason, in native units, as a decimal string.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public TypeStage amount(@NotNull String amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>Why this part of the balance is held. <code>regular</code> is the account's standing risk reserve; <code>bnpl</code> and <code>sequra</code> cover buy-now-pay-later settlement; <code>preshipment_hold</code> covers a physical order that has not shipped yet; <code>fraud_hold</code> is held while activity is reviewed.</p>
         * <p>Why this part of the balance is held. <code>regular</code> is the account's standing risk reserve; <code>bnpl</code> and <code>sequra</code> cover buy-now-pay-later settlement; <code>preshipment_hold</code> covers a physical order that has not shipped yet; <code>fraud_hold</code> is held while activity is reviewed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("type")
        public _FinalStage type(@NotNull AccountReserveTypeType type) {
            this.type = Objects.requireNonNull(type, "type must not be null");
            return this;
        }

        /**
         * <p>Percentage of each incoming payment currently held for this reason, or <code>null</code> when the reason is not a percentage of anything. Money already held keeps the release date it was given, which <code>unlocks_by_date</code> reflects.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage percentage(Nullable<Double> percentage) {
            if (percentage.isNull()) {
                this.percentage = null;
            } else if (percentage.isEmpty()) {
                this.percentage = Optional.empty();
            } else {
                this.percentage = Optional.of(percentage.get());
            }
            return this;
        }

        /**
         * <p>Percentage of each incoming payment currently held for this reason, or <code>null</code> when the reason is not a percentage of anything. Money already held keeps the release date it was given, which <code>unlocks_by_date</code> reflects.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage percentage(Double percentage) {
            this.percentage = Optional.ofNullable(percentage);
            return this;
        }

        /**
         * <p>Percentage of each incoming payment currently held for this reason, or <code>null</code> when the reason is not a percentage of anything. Money already held keeps the release date it was given, which <code>unlocks_by_date</code> reflects.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "percentage", nulls = Nulls.SKIP)
        public _FinalStage percentage(Optional<Double> percentage) {
            this.percentage = percentage;
            return this;
        }

        /**
         * <p>Days money is currently held for this reason before it unlocks, or <code>null</code> when release depends on something other than time. Money already held keeps the terms it was taken under.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage holdPeriodDays(Nullable<Integer> holdPeriodDays) {
            if (holdPeriodDays.isNull()) {
                this.holdPeriodDays = null;
            } else if (holdPeriodDays.isEmpty()) {
                this.holdPeriodDays = Optional.empty();
            } else {
                this.holdPeriodDays = Optional.of(holdPeriodDays.get());
            }
            return this;
        }

        /**
         * <p>Days money is currently held for this reason before it unlocks, or <code>null</code> when release depends on something other than time. Money already held keeps the terms it was taken under.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage holdPeriodDays(Integer holdPeriodDays) {
            this.holdPeriodDays = Optional.ofNullable(holdPeriodDays);
            return this;
        }

        /**
         * <p>Days money is currently held for this reason before it unlocks, or <code>null</code> when release depends on something other than time. Money already held keeps the terms it was taken under.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "hold_period_days", nulls = Nulls.SKIP)
        public _FinalStage holdPeriodDays(Optional<Integer> holdPeriodDays) {
            this.holdPeriodDays = holdPeriodDays;
            return this;
        }

        @java.lang.Override
        public AccountReserveType build() {
            return new AccountReserveType(amount, holdPeriodDays, percentage, type, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
