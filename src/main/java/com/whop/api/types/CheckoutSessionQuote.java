/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionQuote.Builder.class)
public final class CheckoutSessionQuote {
    private final double baseAmount;

    private final String baseCurrency;

    private final CheckoutSessionBreakdown breakdown;

    private final String currency;

    private final Optional<Double> exchangeRate;

    private final String expiresAt;

    private final String quotedAt;

    private final boolean trialSkipped;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionQuote(
            double baseAmount,
            String baseCurrency,
            CheckoutSessionBreakdown breakdown,
            String currency,
            Optional<Double> exchangeRate,
            String expiresAt,
            String quotedAt,
            boolean trialSkipped,
            Map<String, Object> additionalProperties) {
        this.baseAmount = baseAmount;
        this.baseCurrency = baseCurrency;
        this.breakdown = breakdown;
        this.currency = currency;
        this.exchangeRate = exchangeRate;
        this.expiresAt = expiresAt;
        this.quotedAt = quotedAt;
        this.trialSkipped = trialSkipped;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The total in <code>base_currency</code>, before conversion — what the seller prices in. The figure that is actually charged is <code>breakdown.total</code>, in <code>currency</code>.
     */
    @JsonProperty("base_amount")
    public double getBaseAmount() {
        return baseAmount;
    }

    /**
     * @return The seller's own currency for these items, lowercase. Equal to <code>currency</code> when no conversion applied.
     */
    @JsonProperty("base_currency")
    public String getBaseCurrency() {
        return baseCurrency;
    }

    /**
     * @return The same total, itemized — what is being bought, what changes the price, and what is owed later. The tax adjustment arrives <code>pending</code> here and resolves through <code>calculate_tax</code>, which answers with this same shape: render whichever you hold, preferring the calculated one.
     */
    @JsonProperty("breakdown")
    public CheckoutSessionBreakdown getBreakdown() {
        return breakdown;
    }

    /**
     * @return ISO currency the confirm will charge in, lowercase. This is the buyer's own currency whenever adaptive pricing applies — the card is charged in it, not shown a conversion of it.
     */
    @JsonProperty("currency")
    public String getCurrency() {
        return currency;
    }

    /**
     * @return The locked rate <code>base_currency</code> was converted at, or <code>null</code> when the charge is in the seller's own currency. Fixed for the life of the quote.
     */
    @JsonIgnore
    public Optional<Double> getExchangeRate() {
        if (exchangeRate == null) {
            return Optional.empty();
        }
        return exchangeRate;
    }

    /**
     * @return When this quote stops being chargeable, as an ISO 8601 timestamp. A converted quote expires with its exchange rate, which is sooner than an unconverted one.
     */
    @JsonProperty("expires_at")
    public String getExpiresAt() {
        return expiresAt;
    }

    /**
     * @return When this quote was computed, as an ISO 8601 timestamp. A confirm against a stale quote is refused with <code>quote_expired</code> and the quote refreshes for the retry.
     */
    @JsonProperty("quoted_at")
    public String getQuotedAt() {
        return quotedAt;
    }

    /**
     * @return Whether this quote priced the plan's free trial as SKIPPED: the resolved buyer has held this plan before, so no trial applies and the full first price is due today. Tell the buyer so before they pay — the trial they can see on the plan is not theirs to start. <code>false</code> for a plan with no trial, and for a session whose buyer is not resolved yet: a returning buyer resolving at confirm is refused with <code>quote_changed</code> and this re-prices to <code>true</code> for the retry.
     */
    @JsonProperty("trial_skipped")
    public boolean getTrialSkipped() {
        return trialSkipped;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("exchange_rate")
    private Optional<Double> _getExchangeRate() {
        return exchangeRate;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionQuote && equalTo((CheckoutSessionQuote) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionQuote other) {
        return baseAmount == other.baseAmount
                && baseCurrency.equals(other.baseCurrency)
                && breakdown.equals(other.breakdown)
                && currency.equals(other.currency)
                && exchangeRate.equals(other.exchangeRate)
                && expiresAt.equals(other.expiresAt)
                && quotedAt.equals(other.quotedAt)
                && trialSkipped == other.trialSkipped;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.baseAmount,
                this.baseCurrency,
                this.breakdown,
                this.currency,
                this.exchangeRate,
                this.expiresAt,
                this.quotedAt,
                this.trialSkipped);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BaseAmountStage builder() {
        return new Builder();
    }

    public interface BaseAmountStage {
        /**
         * <p>The total in <code>base_currency</code>, before conversion — what the seller prices in. The figure that is actually charged is <code>breakdown.total</code>, in <code>currency</code>.</p>
         */
        BaseCurrencyStage baseAmount(double baseAmount);

        Builder from(CheckoutSessionQuote other);
    }

    public interface BaseCurrencyStage {
        /**
         * <p>The seller's own currency for these items, lowercase. Equal to <code>currency</code> when no conversion applied.</p>
         */
        BreakdownStage baseCurrency(@NotNull String baseCurrency);
    }

    public interface BreakdownStage {
        /**
         * <p>The same total, itemized — what is being bought, what changes the price, and what is owed later. The tax adjustment arrives <code>pending</code> here and resolves through <code>calculate_tax</code>, which answers with this same shape: render whichever you hold, preferring the calculated one.</p>
         */
        CurrencyStage breakdown(@NotNull CheckoutSessionBreakdown breakdown);
    }

    public interface CurrencyStage {
        /**
         * <p>ISO currency the confirm will charge in, lowercase. This is the buyer's own currency whenever adaptive pricing applies — the card is charged in it, not shown a conversion of it.</p>
         */
        ExpiresAtStage currency(@NotNull String currency);
    }

    public interface ExpiresAtStage {
        /**
         * <p>When this quote stops being chargeable, as an ISO 8601 timestamp. A converted quote expires with its exchange rate, which is sooner than an unconverted one.</p>
         */
        QuotedAtStage expiresAt(@NotNull String expiresAt);
    }

    public interface QuotedAtStage {
        /**
         * <p>When this quote was computed, as an ISO 8601 timestamp. A confirm against a stale quote is refused with <code>quote_expired</code> and the quote refreshes for the retry.</p>
         */
        TrialSkippedStage quotedAt(@NotNull String quotedAt);
    }

    public interface TrialSkippedStage {
        /**
         * <p>Whether this quote priced the plan's free trial as SKIPPED: the resolved buyer has held this plan before, so no trial applies and the full first price is due today. Tell the buyer so before they pay — the trial they can see on the plan is not theirs to start. <code>false</code> for a plan with no trial, and for a session whose buyer is not resolved yet: a returning buyer resolving at confirm is refused with <code>quote_changed</code> and this re-prices to <code>true</code> for the retry.</p>
         */
        _FinalStage trialSkipped(boolean trialSkipped);
    }

    public interface _FinalStage {
        CheckoutSessionQuote build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The locked rate <code>base_currency</code> was converted at, or <code>null</code> when the charge is in the seller's own currency. Fixed for the life of the quote.</p>
         */
        _FinalStage exchangeRate(Optional<Double> exchangeRate);

        _FinalStage exchangeRate(Double exchangeRate);

        _FinalStage exchangeRate(Nullable<Double> exchangeRate);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements BaseAmountStage,
                    BaseCurrencyStage,
                    BreakdownStage,
                    CurrencyStage,
                    ExpiresAtStage,
                    QuotedAtStage,
                    TrialSkippedStage,
                    _FinalStage {
        private double baseAmount;

        private String baseCurrency;

        private CheckoutSessionBreakdown breakdown;

        private String currency;

        private String expiresAt;

        private String quotedAt;

        private boolean trialSkipped;

        private Optional<Double> exchangeRate = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionQuote other) {
            baseAmount(other.getBaseAmount());
            baseCurrency(other.getBaseCurrency());
            breakdown(other.getBreakdown());
            currency(other.getCurrency());
            exchangeRate(other.getExchangeRate());
            expiresAt(other.getExpiresAt());
            quotedAt(other.getQuotedAt());
            trialSkipped(other.getTrialSkipped());
            return this;
        }

        /**
         * <p>The total in <code>base_currency</code>, before conversion — what the seller prices in. The figure that is actually charged is <code>breakdown.total</code>, in <code>currency</code>.</p>
         * <p>The total in <code>base_currency</code>, before conversion — what the seller prices in. The figure that is actually charged is <code>breakdown.total</code>, in <code>currency</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("base_amount")
        public BaseCurrencyStage baseAmount(double baseAmount) {
            this.baseAmount = baseAmount;
            return this;
        }

        /**
         * <p>The seller's own currency for these items, lowercase. Equal to <code>currency</code> when no conversion applied.</p>
         * <p>The seller's own currency for these items, lowercase. Equal to <code>currency</code> when no conversion applied.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("base_currency")
        public BreakdownStage baseCurrency(@NotNull String baseCurrency) {
            this.baseCurrency = Objects.requireNonNull(baseCurrency, "baseCurrency must not be null");
            return this;
        }

        /**
         * <p>The same total, itemized — what is being bought, what changes the price, and what is owed later. The tax adjustment arrives <code>pending</code> here and resolves through <code>calculate_tax</code>, which answers with this same shape: render whichever you hold, preferring the calculated one.</p>
         * <p>The same total, itemized — what is being bought, what changes the price, and what is owed later. The tax adjustment arrives <code>pending</code> here and resolves through <code>calculate_tax</code>, which answers with this same shape: render whichever you hold, preferring the calculated one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("breakdown")
        public CurrencyStage breakdown(@NotNull CheckoutSessionBreakdown breakdown) {
            this.breakdown = Objects.requireNonNull(breakdown, "breakdown must not be null");
            return this;
        }

        /**
         * <p>ISO currency the confirm will charge in, lowercase. This is the buyer's own currency whenever adaptive pricing applies — the card is charged in it, not shown a conversion of it.</p>
         * <p>ISO currency the confirm will charge in, lowercase. This is the buyer's own currency whenever adaptive pricing applies — the card is charged in it, not shown a conversion of it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public ExpiresAtStage currency(@NotNull String currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>When this quote stops being chargeable, as an ISO 8601 timestamp. A converted quote expires with its exchange rate, which is sooner than an unconverted one.</p>
         * <p>When this quote stops being chargeable, as an ISO 8601 timestamp. A converted quote expires with its exchange rate, which is sooner than an unconverted one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("expires_at")
        public QuotedAtStage expiresAt(@NotNull String expiresAt) {
            this.expiresAt = Objects.requireNonNull(expiresAt, "expiresAt must not be null");
            return this;
        }

        /**
         * <p>When this quote was computed, as an ISO 8601 timestamp. A confirm against a stale quote is refused with <code>quote_expired</code> and the quote refreshes for the retry.</p>
         * <p>When this quote was computed, as an ISO 8601 timestamp. A confirm against a stale quote is refused with <code>quote_expired</code> and the quote refreshes for the retry.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("quoted_at")
        public TrialSkippedStage quotedAt(@NotNull String quotedAt) {
            this.quotedAt = Objects.requireNonNull(quotedAt, "quotedAt must not be null");
            return this;
        }

        /**
         * <p>Whether this quote priced the plan's free trial as SKIPPED: the resolved buyer has held this plan before, so no trial applies and the full first price is due today. Tell the buyer so before they pay — the trial they can see on the plan is not theirs to start. <code>false</code> for a plan with no trial, and for a session whose buyer is not resolved yet: a returning buyer resolving at confirm is refused with <code>quote_changed</code> and this re-prices to <code>true</code> for the retry.</p>
         * <p>Whether this quote priced the plan's free trial as SKIPPED: the resolved buyer has held this plan before, so no trial applies and the full first price is due today. Tell the buyer so before they pay — the trial they can see on the plan is not theirs to start. <code>false</code> for a plan with no trial, and for a session whose buyer is not resolved yet: a returning buyer resolving at confirm is refused with <code>quote_changed</code> and this re-prices to <code>true</code> for the retry.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("trial_skipped")
        public _FinalStage trialSkipped(boolean trialSkipped) {
            this.trialSkipped = trialSkipped;
            return this;
        }

        /**
         * <p>The locked rate <code>base_currency</code> was converted at, or <code>null</code> when the charge is in the seller's own currency. Fixed for the life of the quote.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage exchangeRate(Nullable<Double> exchangeRate) {
            if (exchangeRate.isNull()) {
                this.exchangeRate = null;
            } else if (exchangeRate.isEmpty()) {
                this.exchangeRate = Optional.empty();
            } else {
                this.exchangeRate = Optional.of(exchangeRate.get());
            }
            return this;
        }

        /**
         * <p>The locked rate <code>base_currency</code> was converted at, or <code>null</code> when the charge is in the seller's own currency. Fixed for the life of the quote.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage exchangeRate(Double exchangeRate) {
            this.exchangeRate = Optional.ofNullable(exchangeRate);
            return this;
        }

        /**
         * <p>The locked rate <code>base_currency</code> was converted at, or <code>null</code> when the charge is in the seller's own currency. Fixed for the life of the quote.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "exchange_rate", nulls = Nulls.SKIP)
        public _FinalStage exchangeRate(Optional<Double> exchangeRate) {
            this.exchangeRate = exchangeRate;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionQuote build() {
            return new CheckoutSessionQuote(
                    baseAmount,
                    baseCurrency,
                    breakdown,
                    currency,
                    exchangeRate,
                    expiresAt,
                    quotedAt,
                    trialSkipped,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
