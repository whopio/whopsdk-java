/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionConfirmError.Builder.class)
public final class CheckoutSessionConfirmError {
    private final CheckoutSessionConfirmErrorCode code;

    private final String message;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionConfirmError(
            CheckoutSessionConfirmErrorCode code, String message, Map<String, Object> additionalProperties) {
        this.code = code;
        this.message = message;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What stopped the confirm. The session: <code>session_expired</code>, <code>session_completed</code>, <code>confirm_in_progress</code>, <code>attestation_required</code>. The price: <code>quote_expired</code> (already refreshed), <code>quote_changed</code> (the stored quote is not what this confirm would charge — the buyer who resolved at confirm prices differently, e.g. a returning buyer skipped past the plan's free trial; the session is already re-quoted, so re-read it, show the new total, and confirm again), <code>rate_expired</code> (the exchange rate behind a local-currency price aged out — re-read the session for a fresh one). The payment method: <code>token_invalid</code>, <code>payment_method_unsupported</code>, <code>payment_method_unavailable</code> (a saved method the token names no longer exists in the buyer's wallet — collect a fresh one). What is being sold, and whether it may still be sold: <code>seller_unavailable</code>, <code>promo_invalid</code>, <code>shipping_address_required</code>, <code>custom_field_invalid</code> (a required question is unanswered, or an answer names a question this product does not ask), <code>plan_archived</code>, <code>product_archived</code>, <code>out_of_stock</code>, <code>purchases_disabled</code>, <code>blocked_country</code>, <code>custom_password</code> (the plan's purchase password is unanswered — update the session's <code>password</code> and confirm again), <code>waitlist_unavailable</code> (this waitlist cannot be joined as asked — the message says why). The buyer: <code>user_suspended</code>, <code>user_banned_from_whop</code>, <code>email_required</code>, <code>free_plan_already_owned</code>, <code>waitlist_entry_pending</code>, <code>waitlist_already_member</code>, and <code>buyer_ineligible</code> — an account-state refusal read by someone who never proved they hold that account, so which state stays undisclosed. The payment itself: <code>payment_failed</code>. And <code>engine_unavailable</code> for a session whose mode this checkout cannot charge yet. Every one of these is terminal for this attempt as it stands — a refusal the buyer can resolve by doing something arrives as <code>next_action</code> instead.
     */
    @JsonProperty("code")
    public CheckoutSessionConfirmErrorCode getCode() {
        return code;
    }

    /**
     * @return A human-readable explanation of the failure, safe to show the buyer.
     */
    @JsonProperty("message")
    public String getMessage() {
        return message;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionConfirmError && equalTo((CheckoutSessionConfirmError) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionConfirmError other) {
        return code.equals(other.code) && message.equals(other.message);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.code, this.message);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CodeStage builder() {
        return new Builder();
    }

    public interface CodeStage {
        /**
         * <p>What stopped the confirm. The session: <code>session_expired</code>, <code>session_completed</code>, <code>confirm_in_progress</code>, <code>attestation_required</code>. The price: <code>quote_expired</code> (already refreshed), <code>quote_changed</code> (the stored quote is not what this confirm would charge — the buyer who resolved at confirm prices differently, e.g. a returning buyer skipped past the plan's free trial; the session is already re-quoted, so re-read it, show the new total, and confirm again), <code>rate_expired</code> (the exchange rate behind a local-currency price aged out — re-read the session for a fresh one). The payment method: <code>token_invalid</code>, <code>payment_method_unsupported</code>, <code>payment_method_unavailable</code> (a saved method the token names no longer exists in the buyer's wallet — collect a fresh one). What is being sold, and whether it may still be sold: <code>seller_unavailable</code>, <code>promo_invalid</code>, <code>shipping_address_required</code>, <code>custom_field_invalid</code> (a required question is unanswered, or an answer names a question this product does not ask), <code>plan_archived</code>, <code>product_archived</code>, <code>out_of_stock</code>, <code>purchases_disabled</code>, <code>blocked_country</code>, <code>custom_password</code> (the plan's purchase password is unanswered — update the session's <code>password</code> and confirm again), <code>waitlist_unavailable</code> (this waitlist cannot be joined as asked — the message says why). The buyer: <code>user_suspended</code>, <code>user_banned_from_whop</code>, <code>email_required</code>, <code>free_plan_already_owned</code>, <code>waitlist_entry_pending</code>, <code>waitlist_already_member</code>, and <code>buyer_ineligible</code> — an account-state refusal read by someone who never proved they hold that account, so which state stays undisclosed. The payment itself: <code>payment_failed</code>. And <code>engine_unavailable</code> for a session whose mode this checkout cannot charge yet. Every one of these is terminal for this attempt as it stands — a refusal the buyer can resolve by doing something arrives as <code>next_action</code> instead.</p>
         */
        MessageStage code(@NotNull CheckoutSessionConfirmErrorCode code);

        Builder from(CheckoutSessionConfirmError other);
    }

    public interface MessageStage {
        /**
         * <p>A human-readable explanation of the failure, safe to show the buyer.</p>
         */
        _FinalStage message(@NotNull String message);
    }

    public interface _FinalStage {
        CheckoutSessionConfirmError build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements CodeStage, MessageStage, _FinalStage {
        private CheckoutSessionConfirmErrorCode code;

        private String message;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionConfirmError other) {
            code(other.getCode());
            message(other.getMessage());
            return this;
        }

        /**
         * <p>What stopped the confirm. The session: <code>session_expired</code>, <code>session_completed</code>, <code>confirm_in_progress</code>, <code>attestation_required</code>. The price: <code>quote_expired</code> (already refreshed), <code>quote_changed</code> (the stored quote is not what this confirm would charge — the buyer who resolved at confirm prices differently, e.g. a returning buyer skipped past the plan's free trial; the session is already re-quoted, so re-read it, show the new total, and confirm again), <code>rate_expired</code> (the exchange rate behind a local-currency price aged out — re-read the session for a fresh one). The payment method: <code>token_invalid</code>, <code>payment_method_unsupported</code>, <code>payment_method_unavailable</code> (a saved method the token names no longer exists in the buyer's wallet — collect a fresh one). What is being sold, and whether it may still be sold: <code>seller_unavailable</code>, <code>promo_invalid</code>, <code>shipping_address_required</code>, <code>custom_field_invalid</code> (a required question is unanswered, or an answer names a question this product does not ask), <code>plan_archived</code>, <code>product_archived</code>, <code>out_of_stock</code>, <code>purchases_disabled</code>, <code>blocked_country</code>, <code>custom_password</code> (the plan's purchase password is unanswered — update the session's <code>password</code> and confirm again), <code>waitlist_unavailable</code> (this waitlist cannot be joined as asked — the message says why). The buyer: <code>user_suspended</code>, <code>user_banned_from_whop</code>, <code>email_required</code>, <code>free_plan_already_owned</code>, <code>waitlist_entry_pending</code>, <code>waitlist_already_member</code>, and <code>buyer_ineligible</code> — an account-state refusal read by someone who never proved they hold that account, so which state stays undisclosed. The payment itself: <code>payment_failed</code>. And <code>engine_unavailable</code> for a session whose mode this checkout cannot charge yet. Every one of these is terminal for this attempt as it stands — a refusal the buyer can resolve by doing something arrives as <code>next_action</code> instead.</p>
         * <p>What stopped the confirm. The session: <code>session_expired</code>, <code>session_completed</code>, <code>confirm_in_progress</code>, <code>attestation_required</code>. The price: <code>quote_expired</code> (already refreshed), <code>quote_changed</code> (the stored quote is not what this confirm would charge — the buyer who resolved at confirm prices differently, e.g. a returning buyer skipped past the plan's free trial; the session is already re-quoted, so re-read it, show the new total, and confirm again), <code>rate_expired</code> (the exchange rate behind a local-currency price aged out — re-read the session for a fresh one). The payment method: <code>token_invalid</code>, <code>payment_method_unsupported</code>, <code>payment_method_unavailable</code> (a saved method the token names no longer exists in the buyer's wallet — collect a fresh one). What is being sold, and whether it may still be sold: <code>seller_unavailable</code>, <code>promo_invalid</code>, <code>shipping_address_required</code>, <code>custom_field_invalid</code> (a required question is unanswered, or an answer names a question this product does not ask), <code>plan_archived</code>, <code>product_archived</code>, <code>out_of_stock</code>, <code>purchases_disabled</code>, <code>blocked_country</code>, <code>custom_password</code> (the plan's purchase password is unanswered — update the session's <code>password</code> and confirm again), <code>waitlist_unavailable</code> (this waitlist cannot be joined as asked — the message says why). The buyer: <code>user_suspended</code>, <code>user_banned_from_whop</code>, <code>email_required</code>, <code>free_plan_already_owned</code>, <code>waitlist_entry_pending</code>, <code>waitlist_already_member</code>, and <code>buyer_ineligible</code> — an account-state refusal read by someone who never proved they hold that account, so which state stays undisclosed. The payment itself: <code>payment_failed</code>. And <code>engine_unavailable</code> for a session whose mode this checkout cannot charge yet. Every one of these is terminal for this attempt as it stands — a refusal the buyer can resolve by doing something arrives as <code>next_action</code> instead.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("code")
        public MessageStage code(@NotNull CheckoutSessionConfirmErrorCode code) {
            this.code = Objects.requireNonNull(code, "code must not be null");
            return this;
        }

        /**
         * <p>A human-readable explanation of the failure, safe to show the buyer.</p>
         * <p>A human-readable explanation of the failure, safe to show the buyer.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("message")
        public _FinalStage message(@NotNull String message) {
            this.message = Objects.requireNonNull(message, "message must not be null");
            return this;
        }

        @java.lang.Override
        public CheckoutSessionConfirmError build() {
            return new CheckoutSessionConfirmError(code, message, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
