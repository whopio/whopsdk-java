/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AppDeployment.Builder.class)
public final class AppDeployment {
    private final String appId;

    private final Optional<String> buildId;

    private final boolean draft;

    private final Optional<String> error;

    private final Optional<Integer> estimatedDurationMs;

    private final Optional<Integer> estimatedRemainingMs;

    private final Optional<Integer> finishedAt;

    private final Optional<AppDeploymentPhase> phase;

    private final Optional<Double> progress;

    private final Optional<Integer> startedAt;

    private final AppDeploymentStatus status;

    private final Optional<String> url;

    private final Map<String, Object> additionalProperties;

    private AppDeployment(
            String appId,
            Optional<String> buildId,
            boolean draft,
            Optional<String> error,
            Optional<Integer> estimatedDurationMs,
            Optional<Integer> estimatedRemainingMs,
            Optional<Integer> finishedAt,
            Optional<AppDeploymentPhase> phase,
            Optional<Double> progress,
            Optional<Integer> startedAt,
            AppDeploymentStatus status,
            Optional<String> url,
            Map<String, Object> additionalProperties) {
        this.appId = appId;
        this.buildId = buildId;
        this.draft = draft;
        this.error = error;
        this.estimatedDurationMs = estimatedDurationMs;
        this.estimatedRemainingMs = estimatedRemainingMs;
        this.finishedAt = finishedAt;
        this.phase = phase;
        this.progress = progress;
        this.startedAt = startedAt;
        this.status = status;
        this.url = url;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The app being deployed, prefixed <code>app_</code>.
     */
    @JsonProperty("app_id")
    public String getAppId() {
        return appId;
    }

    /**
     * @return The build the deployment produced, prefixed <code>abld_</code>, or <code>null</code> until it succeeds.
     */
    @JsonIgnore
    public Optional<String> getBuildId() {
        if (buildId == null) {
            return Optional.empty();
        }
        return buildId;
    }

    /**
     * @return Whether the running or last deployment uploaded a build without making it live.
     */
    @JsonProperty("draft")
    public boolean getDraft() {
        return draft;
    }

    /**
     * @return Why the deployment failed, or <code>null</code> when it did not.
     */
    @JsonIgnore
    public Optional<String> getError() {
        if (error == null) {
            return Optional.empty();
        }
        return error;
    }

    /**
     * @return How long this deployment is expected to take in total, estimated from previous runs.
     */
    @JsonIgnore
    public Optional<Integer> getEstimatedDurationMs() {
        if (estimatedDurationMs == null) {
            return Optional.empty();
        }
        return estimatedDurationMs;
    }

    /**
     * @return How much longer the deployment is expected to take. Held above zero until it actually finishes.
     */
    @JsonIgnore
    public Optional<Integer> getEstimatedRemainingMs() {
        if (estimatedRemainingMs == null) {
            return Optional.empty();
        }
        return estimatedRemainingMs;
    }

    /**
     * @return When the deployment ended, in milliseconds since the epoch, or <code>null</code> while it is still running.
     */
    @JsonIgnore
    public Optional<Integer> getFinishedAt() {
        if (finishedAt == null) {
            return Optional.empty();
        }
        return finishedAt;
    }

    /**
     * @return The stage a running deployment has reached, or <code>null</code> when none is running. Later phases dominate the wall clock: <code>process_archive</code> waits on the upload pipeline and <code>promote</code> waits for the build to go live.
     */
    @JsonIgnore
    public Optional<AppDeploymentPhase> getPhase() {
        if (phase == null) {
            return Optional.empty();
        }
        return phase;
    }

    /**
     * @return Fraction of the deployment estimated to be complete, from 0 to 1. Stops just short of 1 until the run ends.
     */
    @JsonIgnore
    public Optional<Double> getProgress() {
        if (progress == null) {
            return Optional.empty();
        }
        return progress;
    }

    /**
     * @return When the deployment began, in milliseconds since the epoch, or <code>null</code> when none has run.
     */
    @JsonIgnore
    public Optional<Integer> getStartedAt() {
        if (startedAt == null) {
            return Optional.empty();
        }
        return startedAt;
    }

    /**
     * @return Whether the app has anything to publish, and what a publish in flight is doing. <code>unpublished</code> means publishing would ship something new; <code>no_source</code> means the sandbox holds no copy of this app, so there is nothing to publish from.
     */
    @JsonProperty("status")
    public AppDeploymentStatus getStatus() {
        return status;
    }

    /**
     * @return Where the deployed site is served, or <code>null</code> unless the deployment went live.
     */
    @JsonIgnore
    public Optional<String> getUrl() {
        if (url == null) {
            return Optional.empty();
        }
        return url;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("build_id")
    private Optional<String> _getBuildId() {
        return buildId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("error")
    private Optional<String> _getError() {
        return error;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("estimated_duration_ms")
    private Optional<Integer> _getEstimatedDurationMs() {
        return estimatedDurationMs;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("estimated_remaining_ms")
    private Optional<Integer> _getEstimatedRemainingMs() {
        return estimatedRemainingMs;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("finished_at")
    private Optional<Integer> _getFinishedAt() {
        return finishedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("phase")
    private Optional<AppDeploymentPhase> _getPhase() {
        return phase;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("progress")
    private Optional<Double> _getProgress() {
        return progress;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("started_at")
    private Optional<Integer> _getStartedAt() {
        return startedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("url")
    private Optional<String> _getUrl() {
        return url;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AppDeployment && equalTo((AppDeployment) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AppDeployment other) {
        return appId.equals(other.appId)
                && buildId.equals(other.buildId)
                && draft == other.draft
                && error.equals(other.error)
                && estimatedDurationMs.equals(other.estimatedDurationMs)
                && estimatedRemainingMs.equals(other.estimatedRemainingMs)
                && finishedAt.equals(other.finishedAt)
                && phase.equals(other.phase)
                && progress.equals(other.progress)
                && startedAt.equals(other.startedAt)
                && status.equals(other.status)
                && url.equals(other.url);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.appId,
                this.buildId,
                this.draft,
                this.error,
                this.estimatedDurationMs,
                this.estimatedRemainingMs,
                this.finishedAt,
                this.phase,
                this.progress,
                this.startedAt,
                this.status,
                this.url);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AppIdStage builder() {
        return new Builder();
    }

    public interface AppIdStage {
        /**
         * <p>The app being deployed, prefixed <code>app_</code>.</p>
         */
        DraftStage appId(@NotNull String appId);

        Builder from(AppDeployment other);
    }

    public interface DraftStage {
        /**
         * <p>Whether the running or last deployment uploaded a build without making it live.</p>
         */
        StatusStage draft(boolean draft);
    }

    public interface StatusStage {
        /**
         * <p>Whether the app has anything to publish, and what a publish in flight is doing. <code>unpublished</code> means publishing would ship something new; <code>no_source</code> means the sandbox holds no copy of this app, so there is nothing to publish from.</p>
         */
        _FinalStage status(@NotNull AppDeploymentStatus status);
    }

    public interface _FinalStage {
        AppDeployment build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The build the deployment produced, prefixed <code>abld_</code>, or <code>null</code> until it succeeds.</p>
         */
        _FinalStage buildId(Optional<String> buildId);

        _FinalStage buildId(String buildId);

        _FinalStage buildId(Nullable<String> buildId);

        /**
         * <p>Why the deployment failed, or <code>null</code> when it did not.</p>
         */
        _FinalStage error(Optional<String> error);

        _FinalStage error(String error);

        _FinalStage error(Nullable<String> error);

        /**
         * <p>How long this deployment is expected to take in total, estimated from previous runs.</p>
         */
        _FinalStage estimatedDurationMs(Optional<Integer> estimatedDurationMs);

        _FinalStage estimatedDurationMs(Integer estimatedDurationMs);

        _FinalStage estimatedDurationMs(Nullable<Integer> estimatedDurationMs);

        /**
         * <p>How much longer the deployment is expected to take. Held above zero until it actually finishes.</p>
         */
        _FinalStage estimatedRemainingMs(Optional<Integer> estimatedRemainingMs);

        _FinalStage estimatedRemainingMs(Integer estimatedRemainingMs);

        _FinalStage estimatedRemainingMs(Nullable<Integer> estimatedRemainingMs);

        /**
         * <p>When the deployment ended, in milliseconds since the epoch, or <code>null</code> while it is still running.</p>
         */
        _FinalStage finishedAt(Optional<Integer> finishedAt);

        _FinalStage finishedAt(Integer finishedAt);

        _FinalStage finishedAt(Nullable<Integer> finishedAt);

        /**
         * <p>The stage a running deployment has reached, or <code>null</code> when none is running. Later phases dominate the wall clock: <code>process_archive</code> waits on the upload pipeline and <code>promote</code> waits for the build to go live.</p>
         */
        _FinalStage phase(Optional<AppDeploymentPhase> phase);

        _FinalStage phase(AppDeploymentPhase phase);

        _FinalStage phase(Nullable<AppDeploymentPhase> phase);

        /**
         * <p>Fraction of the deployment estimated to be complete, from 0 to 1. Stops just short of 1 until the run ends.</p>
         */
        _FinalStage progress(Optional<Double> progress);

        _FinalStage progress(Double progress);

        _FinalStage progress(Nullable<Double> progress);

        /**
         * <p>When the deployment began, in milliseconds since the epoch, or <code>null</code> when none has run.</p>
         */
        _FinalStage startedAt(Optional<Integer> startedAt);

        _FinalStage startedAt(Integer startedAt);

        _FinalStage startedAt(Nullable<Integer> startedAt);

        /**
         * <p>Where the deployed site is served, or <code>null</code> unless the deployment went live.</p>
         */
        _FinalStage url(Optional<String> url);

        _FinalStage url(String url);

        _FinalStage url(Nullable<String> url);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AppIdStage, DraftStage, StatusStage, _FinalStage {
        private String appId;

        private boolean draft;

        private AppDeploymentStatus status;

        private Optional<String> url = Optional.empty();

        private Optional<Integer> startedAt = Optional.empty();

        private Optional<Double> progress = Optional.empty();

        private Optional<AppDeploymentPhase> phase = Optional.empty();

        private Optional<Integer> finishedAt = Optional.empty();

        private Optional<Integer> estimatedRemainingMs = Optional.empty();

        private Optional<Integer> estimatedDurationMs = Optional.empty();

        private Optional<String> error = Optional.empty();

        private Optional<String> buildId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AppDeployment other) {
            appId(other.getAppId());
            buildId(other.getBuildId());
            draft(other.getDraft());
            error(other.getError());
            estimatedDurationMs(other.getEstimatedDurationMs());
            estimatedRemainingMs(other.getEstimatedRemainingMs());
            finishedAt(other.getFinishedAt());
            phase(other.getPhase());
            progress(other.getProgress());
            startedAt(other.getStartedAt());
            status(other.getStatus());
            url(other.getUrl());
            return this;
        }

        /**
         * <p>The app being deployed, prefixed <code>app_</code>.</p>
         * <p>The app being deployed, prefixed <code>app_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("app_id")
        public DraftStage appId(@NotNull String appId) {
            this.appId = Objects.requireNonNull(appId, "appId must not be null");
            return this;
        }

        /**
         * <p>Whether the running or last deployment uploaded a build without making it live.</p>
         * <p>Whether the running or last deployment uploaded a build without making it live.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("draft")
        public StatusStage draft(boolean draft) {
            this.draft = draft;
            return this;
        }

        /**
         * <p>Whether the app has anything to publish, and what a publish in flight is doing. <code>unpublished</code> means publishing would ship something new; <code>no_source</code> means the sandbox holds no copy of this app, so there is nothing to publish from.</p>
         * <p>Whether the app has anything to publish, and what a publish in flight is doing. <code>unpublished</code> means publishing would ship something new; <code>no_source</code> means the sandbox holds no copy of this app, so there is nothing to publish from.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull AppDeploymentStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>Where the deployed site is served, or <code>null</code> unless the deployment went live.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage url(Nullable<String> url) {
            if (url.isNull()) {
                this.url = null;
            } else if (url.isEmpty()) {
                this.url = Optional.empty();
            } else {
                this.url = Optional.of(url.get());
            }
            return this;
        }

        /**
         * <p>Where the deployed site is served, or <code>null</code> unless the deployment went live.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage url(String url) {
            this.url = Optional.ofNullable(url);
            return this;
        }

        /**
         * <p>Where the deployed site is served, or <code>null</code> unless the deployment went live.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "url", nulls = Nulls.SKIP)
        public _FinalStage url(Optional<String> url) {
            this.url = url;
            return this;
        }

        /**
         * <p>When the deployment began, in milliseconds since the epoch, or <code>null</code> when none has run.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage startedAt(Nullable<Integer> startedAt) {
            if (startedAt.isNull()) {
                this.startedAt = null;
            } else if (startedAt.isEmpty()) {
                this.startedAt = Optional.empty();
            } else {
                this.startedAt = Optional.of(startedAt.get());
            }
            return this;
        }

        /**
         * <p>When the deployment began, in milliseconds since the epoch, or <code>null</code> when none has run.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage startedAt(Integer startedAt) {
            this.startedAt = Optional.ofNullable(startedAt);
            return this;
        }

        /**
         * <p>When the deployment began, in milliseconds since the epoch, or <code>null</code> when none has run.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "started_at", nulls = Nulls.SKIP)
        public _FinalStage startedAt(Optional<Integer> startedAt) {
            this.startedAt = startedAt;
            return this;
        }

        /**
         * <p>Fraction of the deployment estimated to be complete, from 0 to 1. Stops just short of 1 until the run ends.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage progress(Nullable<Double> progress) {
            if (progress.isNull()) {
                this.progress = null;
            } else if (progress.isEmpty()) {
                this.progress = Optional.empty();
            } else {
                this.progress = Optional.of(progress.get());
            }
            return this;
        }

        /**
         * <p>Fraction of the deployment estimated to be complete, from 0 to 1. Stops just short of 1 until the run ends.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage progress(Double progress) {
            this.progress = Optional.ofNullable(progress);
            return this;
        }

        /**
         * <p>Fraction of the deployment estimated to be complete, from 0 to 1. Stops just short of 1 until the run ends.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "progress", nulls = Nulls.SKIP)
        public _FinalStage progress(Optional<Double> progress) {
            this.progress = progress;
            return this;
        }

        /**
         * <p>The stage a running deployment has reached, or <code>null</code> when none is running. Later phases dominate the wall clock: <code>process_archive</code> waits on the upload pipeline and <code>promote</code> waits for the build to go live.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage phase(Nullable<AppDeploymentPhase> phase) {
            if (phase.isNull()) {
                this.phase = null;
            } else if (phase.isEmpty()) {
                this.phase = Optional.empty();
            } else {
                this.phase = Optional.of(phase.get());
            }
            return this;
        }

        /**
         * <p>The stage a running deployment has reached, or <code>null</code> when none is running. Later phases dominate the wall clock: <code>process_archive</code> waits on the upload pipeline and <code>promote</code> waits for the build to go live.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage phase(AppDeploymentPhase phase) {
            this.phase = Optional.ofNullable(phase);
            return this;
        }

        /**
         * <p>The stage a running deployment has reached, or <code>null</code> when none is running. Later phases dominate the wall clock: <code>process_archive</code> waits on the upload pipeline and <code>promote</code> waits for the build to go live.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "phase", nulls = Nulls.SKIP)
        public _FinalStage phase(Optional<AppDeploymentPhase> phase) {
            this.phase = phase;
            return this;
        }

        /**
         * <p>When the deployment ended, in milliseconds since the epoch, or <code>null</code> while it is still running.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage finishedAt(Nullable<Integer> finishedAt) {
            if (finishedAt.isNull()) {
                this.finishedAt = null;
            } else if (finishedAt.isEmpty()) {
                this.finishedAt = Optional.empty();
            } else {
                this.finishedAt = Optional.of(finishedAt.get());
            }
            return this;
        }

        /**
         * <p>When the deployment ended, in milliseconds since the epoch, or <code>null</code> while it is still running.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage finishedAt(Integer finishedAt) {
            this.finishedAt = Optional.ofNullable(finishedAt);
            return this;
        }

        /**
         * <p>When the deployment ended, in milliseconds since the epoch, or <code>null</code> while it is still running.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "finished_at", nulls = Nulls.SKIP)
        public _FinalStage finishedAt(Optional<Integer> finishedAt) {
            this.finishedAt = finishedAt;
            return this;
        }

        /**
         * <p>How much longer the deployment is expected to take. Held above zero until it actually finishes.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedRemainingMs(Nullable<Integer> estimatedRemainingMs) {
            if (estimatedRemainingMs.isNull()) {
                this.estimatedRemainingMs = null;
            } else if (estimatedRemainingMs.isEmpty()) {
                this.estimatedRemainingMs = Optional.empty();
            } else {
                this.estimatedRemainingMs = Optional.of(estimatedRemainingMs.get());
            }
            return this;
        }

        /**
         * <p>How much longer the deployment is expected to take. Held above zero until it actually finishes.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedRemainingMs(Integer estimatedRemainingMs) {
            this.estimatedRemainingMs = Optional.ofNullable(estimatedRemainingMs);
            return this;
        }

        /**
         * <p>How much longer the deployment is expected to take. Held above zero until it actually finishes.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "estimated_remaining_ms", nulls = Nulls.SKIP)
        public _FinalStage estimatedRemainingMs(Optional<Integer> estimatedRemainingMs) {
            this.estimatedRemainingMs = estimatedRemainingMs;
            return this;
        }

        /**
         * <p>How long this deployment is expected to take in total, estimated from previous runs.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedDurationMs(Nullable<Integer> estimatedDurationMs) {
            if (estimatedDurationMs.isNull()) {
                this.estimatedDurationMs = null;
            } else if (estimatedDurationMs.isEmpty()) {
                this.estimatedDurationMs = Optional.empty();
            } else {
                this.estimatedDurationMs = Optional.of(estimatedDurationMs.get());
            }
            return this;
        }

        /**
         * <p>How long this deployment is expected to take in total, estimated from previous runs.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedDurationMs(Integer estimatedDurationMs) {
            this.estimatedDurationMs = Optional.ofNullable(estimatedDurationMs);
            return this;
        }

        /**
         * <p>How long this deployment is expected to take in total, estimated from previous runs.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "estimated_duration_ms", nulls = Nulls.SKIP)
        public _FinalStage estimatedDurationMs(Optional<Integer> estimatedDurationMs) {
            this.estimatedDurationMs = estimatedDurationMs;
            return this;
        }

        /**
         * <p>Why the deployment failed, or <code>null</code> when it did not.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage error(Nullable<String> error) {
            if (error.isNull()) {
                this.error = null;
            } else if (error.isEmpty()) {
                this.error = Optional.empty();
            } else {
                this.error = Optional.of(error.get());
            }
            return this;
        }

        /**
         * <p>Why the deployment failed, or <code>null</code> when it did not.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage error(String error) {
            this.error = Optional.ofNullable(error);
            return this;
        }

        /**
         * <p>Why the deployment failed, or <code>null</code> when it did not.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "error", nulls = Nulls.SKIP)
        public _FinalStage error(Optional<String> error) {
            this.error = error;
            return this;
        }

        /**
         * <p>The build the deployment produced, prefixed <code>abld_</code>, or <code>null</code> until it succeeds.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage buildId(Nullable<String> buildId) {
            if (buildId.isNull()) {
                this.buildId = null;
            } else if (buildId.isEmpty()) {
                this.buildId = Optional.empty();
            } else {
                this.buildId = Optional.of(buildId.get());
            }
            return this;
        }

        /**
         * <p>The build the deployment produced, prefixed <code>abld_</code>, or <code>null</code> until it succeeds.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage buildId(String buildId) {
            this.buildId = Optional.ofNullable(buildId);
            return this;
        }

        /**
         * <p>The build the deployment produced, prefixed <code>abld_</code>, or <code>null</code> until it succeeds.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "build_id", nulls = Nulls.SKIP)
        public _FinalStage buildId(Optional<String> buildId) {
            this.buildId = buildId;
            return this;
        }

        @java.lang.Override
        public AppDeployment build() {
            return new AppDeployment(
                    appId,
                    buildId,
                    draft,
                    error,
                    estimatedDurationMs,
                    estimatedRemainingMs,
                    finishedAt,
                    phase,
                    progress,
                    startedAt,
                    status,
                    url,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
