/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class TeamMemberStatus {
    public static final TeamMemberStatus PENDING = new TeamMemberStatus(Value.PENDING, "pending");

    public static final TeamMemberStatus JOINED = new TeamMemberStatus(Value.JOINED, "joined");

    private final Value value;

    private final String string;

    TeamMemberStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof TeamMemberStatus && this.string.equals(((TeamMemberStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PENDING:
                return visitor.visitPending();
            case JOINED:
                return visitor.visitJoined();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static TeamMemberStatus valueOf(String value) {
        switch (value) {
            case "pending":
                return PENDING;
            case "joined":
                return JOINED;
            default:
                return new TeamMemberStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        JOINED,

        PENDING,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitJoined();

        T visitPending();

        T visitUnknown(String unknownType);
    }
}
