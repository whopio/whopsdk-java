/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PaymentNextActionRedirectData.Builder.class)
public final class PaymentNextActionRedirectData {
    private final Optional<Integer> frameMaxWidth;

    private final String url;

    private final Map<String, Object> additionalProperties;

    private PaymentNextActionRedirectData(
            Optional<Integer> frameMaxWidth, String url, Map<String, Object> additionalProperties) {
        this.frameMaxWidth = frameMaxWidth;
        this.url = url;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The widest the provider's page lays out usefully, in CSS pixels — cap a frame or dialog presenting it at this width. <code>null</code> when the page fills whatever width it is given.
     */
    @JsonIgnore
    public Optional<Integer> getFrameMaxWidth() {
        if (frameMaxWidth == null) {
            return Optional.empty();
        }
        return frameMaxWidth;
    }

    /**
     * @return The provider's page for this payment, as an absolute URL — take the buyer there.
     */
    @JsonProperty("url")
    public String getUrl() {
        return url;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("frame_max_width")
    private Optional<Integer> _getFrameMaxWidth() {
        return frameMaxWidth;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentNextActionRedirectData && equalTo((PaymentNextActionRedirectData) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PaymentNextActionRedirectData other) {
        return frameMaxWidth.equals(other.frameMaxWidth) && url.equals(other.url);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.frameMaxWidth, this.url);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static UrlStage builder() {
        return new Builder();
    }

    public interface UrlStage {
        /**
         * <p>The provider's page for this payment, as an absolute URL — take the buyer there.</p>
         */
        _FinalStage url(@NotNull String url);

        Builder from(PaymentNextActionRedirectData other);
    }

    public interface _FinalStage {
        PaymentNextActionRedirectData build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The widest the provider's page lays out usefully, in CSS pixels — cap a frame or dialog presenting it at this width. <code>null</code> when the page fills whatever width it is given.</p>
         */
        _FinalStage frameMaxWidth(Optional<Integer> frameMaxWidth);

        _FinalStage frameMaxWidth(Integer frameMaxWidth);

        _FinalStage frameMaxWidth(Nullable<Integer> frameMaxWidth);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements UrlStage, _FinalStage {
        private String url;

        private Optional<Integer> frameMaxWidth = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PaymentNextActionRedirectData other) {
            frameMaxWidth(other.getFrameMaxWidth());
            url(other.getUrl());
            return this;
        }

        /**
         * <p>The provider's page for this payment, as an absolute URL — take the buyer there.</p>
         * <p>The provider's page for this payment, as an absolute URL — take the buyer there.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("url")
        public _FinalStage url(@NotNull String url) {
            this.url = Objects.requireNonNull(url, "url must not be null");
            return this;
        }

        /**
         * <p>The widest the provider's page lays out usefully, in CSS pixels — cap a frame or dialog presenting it at this width. <code>null</code> when the page fills whatever width it is given.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage frameMaxWidth(Nullable<Integer> frameMaxWidth) {
            if (frameMaxWidth.isNull()) {
                this.frameMaxWidth = null;
            } else if (frameMaxWidth.isEmpty()) {
                this.frameMaxWidth = Optional.empty();
            } else {
                this.frameMaxWidth = Optional.of(frameMaxWidth.get());
            }
            return this;
        }

        /**
         * <p>The widest the provider's page lays out usefully, in CSS pixels — cap a frame or dialog presenting it at this width. <code>null</code> when the page fills whatever width it is given.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage frameMaxWidth(Integer frameMaxWidth) {
            this.frameMaxWidth = Optional.ofNullable(frameMaxWidth);
            return this;
        }

        /**
         * <p>The widest the provider's page lays out usefully, in CSS pixels — cap a frame or dialog presenting it at this width. <code>null</code> when the page fills whatever width it is given.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "frame_max_width", nulls = Nulls.SKIP)
        public _FinalStage frameMaxWidth(Optional<Integer> frameMaxWidth) {
            this.frameMaxWidth = frameMaxWidth;
            return this;
        }

        @java.lang.Override
        public PaymentNextActionRedirectData build() {
            return new PaymentNextActionRedirectData(frameMaxWidth, url, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
