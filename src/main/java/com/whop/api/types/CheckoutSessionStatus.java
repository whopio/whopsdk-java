/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CheckoutSessionStatus {
    public static final CheckoutSessionStatus COMPLETED = new CheckoutSessionStatus(Value.COMPLETED, "completed");

    public static final CheckoutSessionStatus EXPIRED = new CheckoutSessionStatus(Value.EXPIRED, "expired");

    public static final CheckoutSessionStatus OPEN = new CheckoutSessionStatus(Value.OPEN, "open");

    private final Value value;

    private final String string;

    CheckoutSessionStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CheckoutSessionStatus
                        && this.string.equals(((CheckoutSessionStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case COMPLETED:
                return visitor.visitCompleted();
            case EXPIRED:
                return visitor.visitExpired();
            case OPEN:
                return visitor.visitOpen();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CheckoutSessionStatus valueOf(String value) {
        switch (value) {
            case "completed":
                return COMPLETED;
            case "expired":
                return EXPIRED;
            case "open":
                return OPEN;
            default:
                return new CheckoutSessionStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        OPEN,

        COMPLETED,

        EXPIRED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitOpen();

        T visitCompleted();

        T visitExpired();

        T visitUnknown(String unknownType);
    }
}
