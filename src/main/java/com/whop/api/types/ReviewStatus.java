/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ReviewStatus {
    public static final ReviewStatus PENDING = new ReviewStatus(Value.PENDING, "pending");

    public static final ReviewStatus PUBLISHED = new ReviewStatus(Value.PUBLISHED, "published");

    public static final ReviewStatus REMOVED = new ReviewStatus(Value.REMOVED, "removed");

    private final Value value;

    private final String string;

    ReviewStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof ReviewStatus && this.string.equals(((ReviewStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PENDING:
                return visitor.visitPending();
            case PUBLISHED:
                return visitor.visitPublished();
            case REMOVED:
                return visitor.visitRemoved();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ReviewStatus valueOf(String value) {
        switch (value) {
            case "pending":
                return PENDING;
            case "published":
                return PUBLISHED;
            case "removed":
                return REMOVED;
            default:
                return new ReviewStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        PUBLISHED,

        REMOVED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitPublished();

        T visitRemoved();

        T visitUnknown(String unknownType);
    }
}
