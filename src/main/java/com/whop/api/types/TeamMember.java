/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = TeamMember.Builder.class)
public final class TeamMember {
    private final String accountId;

    private final Optional<TeamMemberAuthorizedRole> authorizedRole;

    private final String createdAt;

    private final Optional<String> email;

    private final String id;

    private final boolean isAgent;

    private final TeamMemberRole role;

    private final TeamMemberStatus status;

    private final String updatedAt;

    private final Optional<UserSummary> user;

    private final Map<String, Object> additionalProperties;

    private TeamMember(
            String accountId,
            Optional<TeamMemberAuthorizedRole> authorizedRole,
            String createdAt,
            Optional<String> email,
            String id,
            boolean isAgent,
            TeamMemberRole role,
            TeamMemberStatus status,
            String updatedAt,
            Optional<UserSummary> user,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.authorizedRole = authorizedRole;
        this.createdAt = createdAt;
        this.email = email;
        this.id = id;
        this.isAgent = isAgent;
        this.role = role;
        this.status = status;
        this.updatedAt = updatedAt;
        this.user = user;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The account this membership belongs to, prefixed <code>biz_</code>.
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return Custom role assigned to this member, or <code>null</code> when the member has a system role.
     */
    @JsonIgnore
    public Optional<TeamMemberAuthorizedRole> getAuthorizedRole() {
        if (authorizedRole == null) {
            return Optional.empty();
        }
        return authorizedRole;
    }

    /**
     * @return When the member joined or the invite was sent, as an ISO 8601 timestamp.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return The member's email address. For accepted members, <code>null</code> unless the caller holds the email read scope; for invites, the invited address.
     */
    @JsonIgnore
    public Optional<String> getEmail() {
        if (email == null) {
            return Optional.empty();
        }
        return email;
    }

    /**
     * @return Team member ID — <code>ausr_</code> for accepted members, <code>ausri_</code> for pending invites.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Whether this member is an agent (app-controlled account) rather than a human team member. Always <code>false</code> for invites.
     */
    @JsonProperty("is_agent")
    public boolean getIsAgent() {
        return isAgent;
    }

    /**
     * @return The member's role on the account. <code>custom</code> means a bespoke dashboard-managed role; the API can read but not grant it.
     */
    @JsonProperty("role")
    public TeamMemberRole getRole() {
        return role;
    }

    /**
     * @return <code>joined</code> for accepted members, <code>pending</code> while the invite is pending.
     */
    @JsonProperty("status")
    public TeamMemberStatus getStatus() {
        return status;
    }

    /**
     * @return When the membership was last updated, as an ISO 8601 timestamp.
     */
    @JsonProperty("updated_at")
    public String getUpdatedAt() {
        return updatedAt;
    }

    /**
     * @return The user behind this team membership. <code>null</code> for an invite sent to an email with no Whop account yet.
     */
    @JsonIgnore
    public Optional<UserSummary> getUser() {
        if (user == null) {
            return Optional.empty();
        }
        return user;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("authorized_role")
    private Optional<TeamMemberAuthorizedRole> _getAuthorizedRole() {
        return authorizedRole;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("email")
    private Optional<String> _getEmail() {
        return email;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("user")
    private Optional<UserSummary> _getUser() {
        return user;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof TeamMember && equalTo((TeamMember) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(TeamMember other) {
        return accountId.equals(other.accountId)
                && authorizedRole.equals(other.authorizedRole)
                && createdAt.equals(other.createdAt)
                && email.equals(other.email)
                && id.equals(other.id)
                && isAgent == other.isAgent
                && role.equals(other.role)
                && status.equals(other.status)
                && updatedAt.equals(other.updatedAt)
                && user.equals(other.user);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.authorizedRole,
                this.createdAt,
                this.email,
                this.id,
                this.isAgent,
                this.role,
                this.status,
                this.updatedAt,
                this.user);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountIdStage builder() {
        return new Builder();
    }

    public interface AccountIdStage {
        /**
         * <p>The account this membership belongs to, prefixed <code>biz_</code>.</p>
         */
        CreatedAtStage accountId(@NotNull String accountId);

        Builder from(TeamMember other);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the member joined or the invite was sent, as an ISO 8601 timestamp.</p>
         */
        IdStage createdAt(@NotNull String createdAt);
    }

    public interface IdStage {
        /**
         * <p>Team member ID — <code>ausr_</code> for accepted members, <code>ausri_</code> for pending invites.</p>
         */
        IsAgentStage id(@NotNull String id);
    }

    public interface IsAgentStage {
        /**
         * <p>Whether this member is an agent (app-controlled account) rather than a human team member. Always <code>false</code> for invites.</p>
         */
        RoleStage isAgent(boolean isAgent);
    }

    public interface RoleStage {
        /**
         * <p>The member's role on the account. <code>custom</code> means a bespoke dashboard-managed role; the API can read but not grant it.</p>
         */
        StatusStage role(@NotNull TeamMemberRole role);
    }

    public interface StatusStage {
        /**
         * <p><code>joined</code> for accepted members, <code>pending</code> while the invite is pending.</p>
         */
        UpdatedAtStage status(@NotNull TeamMemberStatus status);
    }

    public interface UpdatedAtStage {
        /**
         * <p>When the membership was last updated, as an ISO 8601 timestamp.</p>
         */
        _FinalStage updatedAt(@NotNull String updatedAt);
    }

    public interface _FinalStage {
        TeamMember build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Custom role assigned to this member, or <code>null</code> when the member has a system role.</p>
         */
        _FinalStage authorizedRole(Optional<TeamMemberAuthorizedRole> authorizedRole);

        _FinalStage authorizedRole(TeamMemberAuthorizedRole authorizedRole);

        _FinalStage authorizedRole(Nullable<TeamMemberAuthorizedRole> authorizedRole);

        /**
         * <p>The member's email address. For accepted members, <code>null</code> unless the caller holds the email read scope; for invites, the invited address.</p>
         */
        _FinalStage email(Optional<String> email);

        _FinalStage email(String email);

        _FinalStage email(Nullable<String> email);

        /**
         * <p>The user behind this team membership. <code>null</code> for an invite sent to an email with no Whop account yet.</p>
         */
        _FinalStage user(Optional<UserSummary> user);

        _FinalStage user(UserSummary user);

        _FinalStage user(Nullable<UserSummary> user);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AccountIdStage,
                    CreatedAtStage,
                    IdStage,
                    IsAgentStage,
                    RoleStage,
                    StatusStage,
                    UpdatedAtStage,
                    _FinalStage {
        private String accountId;

        private String createdAt;

        private String id;

        private boolean isAgent;

        private TeamMemberRole role;

        private TeamMemberStatus status;

        private String updatedAt;

        private Optional<UserSummary> user = Optional.empty();

        private Optional<String> email = Optional.empty();

        private Optional<TeamMemberAuthorizedRole> authorizedRole = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(TeamMember other) {
            accountId(other.getAccountId());
            authorizedRole(other.getAuthorizedRole());
            createdAt(other.getCreatedAt());
            email(other.getEmail());
            id(other.getId());
            isAgent(other.getIsAgent());
            role(other.getRole());
            status(other.getStatus());
            updatedAt(other.getUpdatedAt());
            user(other.getUser());
            return this;
        }

        /**
         * <p>The account this membership belongs to, prefixed <code>biz_</code>.</p>
         * <p>The account this membership belongs to, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public CreatedAtStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        /**
         * <p>When the member joined or the invite was sent, as an ISO 8601 timestamp.</p>
         * <p>When the member joined or the invite was sent, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public IdStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Team member ID — <code>ausr_</code> for accepted members, <code>ausri_</code> for pending invites.</p>
         * <p>Team member ID — <code>ausr_</code> for accepted members, <code>ausri_</code> for pending invites.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public IsAgentStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Whether this member is an agent (app-controlled account) rather than a human team member. Always <code>false</code> for invites.</p>
         * <p>Whether this member is an agent (app-controlled account) rather than a human team member. Always <code>false</code> for invites.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("is_agent")
        public RoleStage isAgent(boolean isAgent) {
            this.isAgent = isAgent;
            return this;
        }

        /**
         * <p>The member's role on the account. <code>custom</code> means a bespoke dashboard-managed role; the API can read but not grant it.</p>
         * <p>The member's role on the account. <code>custom</code> means a bespoke dashboard-managed role; the API can read but not grant it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("role")
        public StatusStage role(@NotNull TeamMemberRole role) {
            this.role = Objects.requireNonNull(role, "role must not be null");
            return this;
        }

        /**
         * <p><code>joined</code> for accepted members, <code>pending</code> while the invite is pending.</p>
         * <p><code>joined</code> for accepted members, <code>pending</code> while the invite is pending.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public UpdatedAtStage status(@NotNull TeamMemberStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>When the membership was last updated, as an ISO 8601 timestamp.</p>
         * <p>When the membership was last updated, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("updated_at")
        public _FinalStage updatedAt(@NotNull String updatedAt) {
            this.updatedAt = Objects.requireNonNull(updatedAt, "updatedAt must not be null");
            return this;
        }

        /**
         * <p>The user behind this team membership. <code>null</code> for an invite sent to an email with no Whop account yet.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage user(Nullable<UserSummary> user) {
            if (user.isNull()) {
                this.user = null;
            } else if (user.isEmpty()) {
                this.user = Optional.empty();
            } else {
                this.user = Optional.of(user.get());
            }
            return this;
        }

        /**
         * <p>The user behind this team membership. <code>null</code> for an invite sent to an email with no Whop account yet.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage user(UserSummary user) {
            this.user = Optional.ofNullable(user);
            return this;
        }

        /**
         * <p>The user behind this team membership. <code>null</code> for an invite sent to an email with no Whop account yet.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "user", nulls = Nulls.SKIP)
        public _FinalStage user(Optional<UserSummary> user) {
            this.user = user;
            return this;
        }

        /**
         * <p>The member's email address. For accepted members, <code>null</code> unless the caller holds the email read scope; for invites, the invited address.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage email(Nullable<String> email) {
            if (email.isNull()) {
                this.email = null;
            } else if (email.isEmpty()) {
                this.email = Optional.empty();
            } else {
                this.email = Optional.of(email.get());
            }
            return this;
        }

        /**
         * <p>The member's email address. For accepted members, <code>null</code> unless the caller holds the email read scope; for invites, the invited address.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage email(String email) {
            this.email = Optional.ofNullable(email);
            return this;
        }

        /**
         * <p>The member's email address. For accepted members, <code>null</code> unless the caller holds the email read scope; for invites, the invited address.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "email", nulls = Nulls.SKIP)
        public _FinalStage email(Optional<String> email) {
            this.email = email;
            return this;
        }

        /**
         * <p>Custom role assigned to this member, or <code>null</code> when the member has a system role.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage authorizedRole(Nullable<TeamMemberAuthorizedRole> authorizedRole) {
            if (authorizedRole.isNull()) {
                this.authorizedRole = null;
            } else if (authorizedRole.isEmpty()) {
                this.authorizedRole = Optional.empty();
            } else {
                this.authorizedRole = Optional.of(authorizedRole.get());
            }
            return this;
        }

        /**
         * <p>Custom role assigned to this member, or <code>null</code> when the member has a system role.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage authorizedRole(TeamMemberAuthorizedRole authorizedRole) {
            this.authorizedRole = Optional.ofNullable(authorizedRole);
            return this;
        }

        /**
         * <p>Custom role assigned to this member, or <code>null</code> when the member has a system role.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "authorized_role", nulls = Nulls.SKIP)
        public _FinalStage authorizedRole(Optional<TeamMemberAuthorizedRole> authorizedRole) {
            this.authorizedRole = authorizedRole;
            return this;
        }

        @java.lang.Override
        public TeamMember build() {
            return new TeamMember(
                    accountId,
                    authorizedRole,
                    createdAt,
                    email,
                    id,
                    isAgent,
                    role,
                    status,
                    updatedAt,
                    user,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
