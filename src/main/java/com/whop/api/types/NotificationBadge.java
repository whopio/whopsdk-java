/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = NotificationBadge.Builder.class)
public final class NotificationBadge {
    private final Optional<String> accountId;

    private final String experienceId;

    private final boolean hasUnread;

    private final int importantCount;

    private final Optional<String> lastViewedAt;

    private final Map<String, Object> additionalProperties;

    private NotificationBadge(
            Optional<String> accountId,
            String experienceId,
            boolean hasUnread,
            int importantCount,
            Optional<String> lastViewedAt,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.experienceId = experienceId;
        this.hasUnread = hasUnread;
        this.importantCount = importantCount;
        this.lastViewedAt = lastViewedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Account the experience belongs to, prefixed <code>biz_</code>.
     */
    @JsonIgnore
    public Optional<String> getAccountId() {
        if (accountId == null) {
            return Optional.empty();
        }
        return accountId;
    }

    /**
     * @return Experience the badge counts, prefixed <code>exp_</code>.
     */
    @JsonProperty("experience_id")
    public String getExperienceId() {
        return experienceId;
    }

    /**
     * @return Whether the caller has unread notifications in this experience.
     */
    @JsonProperty("has_unread")
    public boolean getHasUnread() {
        return hasUnread;
    }

    /**
     * @return Number of unread important (mention) notifications in this experience.
     */
    @JsonProperty("important_count")
    public int getImportantCount() {
        return importantCount;
    }

    /**
     * @return When the caller last viewed the experience, as an ISO 8601 timestamp. <code>null</code> when never viewed.
     */
    @JsonIgnore
    public Optional<String> getLastViewedAt() {
        if (lastViewedAt == null) {
            return Optional.empty();
        }
        return lastViewedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("account_id")
    private Optional<String> _getAccountId() {
        return accountId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_viewed_at")
    private Optional<String> _getLastViewedAt() {
        return lastViewedAt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof NotificationBadge && equalTo((NotificationBadge) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(NotificationBadge other) {
        return accountId.equals(other.accountId)
                && experienceId.equals(other.experienceId)
                && hasUnread == other.hasUnread
                && importantCount == other.importantCount
                && lastViewedAt.equals(other.lastViewedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.accountId, this.experienceId, this.hasUnread, this.importantCount, this.lastViewedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ExperienceIdStage builder() {
        return new Builder();
    }

    public interface ExperienceIdStage {
        /**
         * <p>Experience the badge counts, prefixed <code>exp_</code>.</p>
         */
        HasUnreadStage experienceId(@NotNull String experienceId);

        Builder from(NotificationBadge other);
    }

    public interface HasUnreadStage {
        /**
         * <p>Whether the caller has unread notifications in this experience.</p>
         */
        ImportantCountStage hasUnread(boolean hasUnread);
    }

    public interface ImportantCountStage {
        /**
         * <p>Number of unread important (mention) notifications in this experience.</p>
         */
        _FinalStage importantCount(int importantCount);
    }

    public interface _FinalStage {
        NotificationBadge build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Account the experience belongs to, prefixed <code>biz_</code>.</p>
         */
        _FinalStage accountId(Optional<String> accountId);

        _FinalStage accountId(String accountId);

        _FinalStage accountId(Nullable<String> accountId);

        /**
         * <p>When the caller last viewed the experience, as an ISO 8601 timestamp. <code>null</code> when never viewed.</p>
         */
        _FinalStage lastViewedAt(Optional<String> lastViewedAt);

        _FinalStage lastViewedAt(String lastViewedAt);

        _FinalStage lastViewedAt(Nullable<String> lastViewedAt);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements ExperienceIdStage, HasUnreadStage, ImportantCountStage, _FinalStage {
        private String experienceId;

        private boolean hasUnread;

        private int importantCount;

        private Optional<String> lastViewedAt = Optional.empty();

        private Optional<String> accountId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(NotificationBadge other) {
            accountId(other.getAccountId());
            experienceId(other.getExperienceId());
            hasUnread(other.getHasUnread());
            importantCount(other.getImportantCount());
            lastViewedAt(other.getLastViewedAt());
            return this;
        }

        /**
         * <p>Experience the badge counts, prefixed <code>exp_</code>.</p>
         * <p>Experience the badge counts, prefixed <code>exp_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("experience_id")
        public HasUnreadStage experienceId(@NotNull String experienceId) {
            this.experienceId = Objects.requireNonNull(experienceId, "experienceId must not be null");
            return this;
        }

        /**
         * <p>Whether the caller has unread notifications in this experience.</p>
         * <p>Whether the caller has unread notifications in this experience.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("has_unread")
        public ImportantCountStage hasUnread(boolean hasUnread) {
            this.hasUnread = hasUnread;
            return this;
        }

        /**
         * <p>Number of unread important (mention) notifications in this experience.</p>
         * <p>Number of unread important (mention) notifications in this experience.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("important_count")
        public _FinalStage importantCount(int importantCount) {
            this.importantCount = importantCount;
            return this;
        }

        /**
         * <p>When the caller last viewed the experience, as an ISO 8601 timestamp. <code>null</code> when never viewed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastViewedAt(Nullable<String> lastViewedAt) {
            if (lastViewedAt.isNull()) {
                this.lastViewedAt = null;
            } else if (lastViewedAt.isEmpty()) {
                this.lastViewedAt = Optional.empty();
            } else {
                this.lastViewedAt = Optional.of(lastViewedAt.get());
            }
            return this;
        }

        /**
         * <p>When the caller last viewed the experience, as an ISO 8601 timestamp. <code>null</code> when never viewed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastViewedAt(String lastViewedAt) {
            this.lastViewedAt = Optional.ofNullable(lastViewedAt);
            return this;
        }

        /**
         * <p>When the caller last viewed the experience, as an ISO 8601 timestamp. <code>null</code> when never viewed.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_viewed_at", nulls = Nulls.SKIP)
        public _FinalStage lastViewedAt(Optional<String> lastViewedAt) {
            this.lastViewedAt = lastViewedAt;
            return this;
        }

        /**
         * <p>Account the experience belongs to, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountId(Nullable<String> accountId) {
            if (accountId.isNull()) {
                this.accountId = null;
            } else if (accountId.isEmpty()) {
                this.accountId = Optional.empty();
            } else {
                this.accountId = Optional.of(accountId.get());
            }
            return this;
        }

        /**
         * <p>Account the experience belongs to, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountId(String accountId) {
            this.accountId = Optional.ofNullable(accountId);
            return this;
        }

        /**
         * <p>Account the experience belongs to, prefixed <code>biz_</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "account_id", nulls = Nulls.SKIP)
        public _FinalStage accountId(Optional<String> accountId) {
            this.accountId = accountId;
            return this;
        }

        @java.lang.Override
        public NotificationBadge build() {
            return new NotificationBadge(
                    accountId, experienceId, hasUnread, importantCount, lastViewedAt, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
