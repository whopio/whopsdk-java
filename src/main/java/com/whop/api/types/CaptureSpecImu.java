/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CaptureSpecImu.Builder.class)
public final class CaptureSpecImu {
    private final String deviceMotionUnits;

    private final String magnetometerUnits;

    private final double minRateHz;

    private final int targetRateHz;

    private final double warmupMinRateHz;

    private final int warmupNs;

    private final Map<String, Object> additionalProperties;

    private CaptureSpecImu(
            String deviceMotionUnits,
            String magnetometerUnits,
            double minRateHz,
            int targetRateHz,
            double warmupMinRateHz,
            int warmupNs,
            Map<String, Object> additionalProperties) {
        this.deviceMotionUnits = deviceMotionUnits;
        this.magnetometerUnits = magnetometerUnits;
        this.minRateHz = minRateHz;
        this.targetRateHz = targetRateHz;
        this.warmupMinRateHz = warmupMinRateHz;
        this.warmupNs = warmupNs;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Units for the device-motion channels, as a compact key=unit string.
     */
    @JsonProperty("device_motion_units")
    public String getDeviceMotionUnits() {
        return deviceMotionUnits;
    }

    /**
     * @return Units for the magnetometer channel.
     */
    @JsonProperty("magnetometer_units")
    public String getMagnetometerUnits() {
        return magnetometerUnits;
    }

    /**
     * @return Minimum sustained IMU sample rate in hertz for a clip to pass validation.
     */
    @JsonProperty("min_rate_hz")
    public double getMinRateHz() {
        return minRateHz;
    }

    /**
     * @return Target IMU sample rate in hertz.
     */
    @JsonProperty("target_rate_hz")
    public int getTargetRateHz() {
        return targetRateHz;
    }

    /**
     * @return Minimum IMU sample rate in hertz tolerated during the warmup window.
     */
    @JsonProperty("warmup_min_rate_hz")
    public double getWarmupMinRateHz() {
        return warmupMinRateHz;
    }

    /**
     * @return Startup window, in nanoseconds, during which the relaxed warmup rate applies.
     */
    @JsonProperty("warmup_ns")
    public int getWarmupNs() {
        return warmupNs;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CaptureSpecImu && equalTo((CaptureSpecImu) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CaptureSpecImu other) {
        return deviceMotionUnits.equals(other.deviceMotionUnits)
                && magnetometerUnits.equals(other.magnetometerUnits)
                && minRateHz == other.minRateHz
                && targetRateHz == other.targetRateHz
                && warmupMinRateHz == other.warmupMinRateHz
                && warmupNs == other.warmupNs;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.deviceMotionUnits,
                this.magnetometerUnits,
                this.minRateHz,
                this.targetRateHz,
                this.warmupMinRateHz,
                this.warmupNs);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static DeviceMotionUnitsStage builder() {
        return new Builder();
    }

    public interface DeviceMotionUnitsStage {
        /**
         * <p>Units for the device-motion channels, as a compact key=unit string.</p>
         */
        MagnetometerUnitsStage deviceMotionUnits(@NotNull String deviceMotionUnits);

        Builder from(CaptureSpecImu other);
    }

    public interface MagnetometerUnitsStage {
        /**
         * <p>Units for the magnetometer channel.</p>
         */
        MinRateHzStage magnetometerUnits(@NotNull String magnetometerUnits);
    }

    public interface MinRateHzStage {
        /**
         * <p>Minimum sustained IMU sample rate in hertz for a clip to pass validation.</p>
         */
        TargetRateHzStage minRateHz(double minRateHz);
    }

    public interface TargetRateHzStage {
        /**
         * <p>Target IMU sample rate in hertz.</p>
         */
        WarmupMinRateHzStage targetRateHz(int targetRateHz);
    }

    public interface WarmupMinRateHzStage {
        /**
         * <p>Minimum IMU sample rate in hertz tolerated during the warmup window.</p>
         */
        WarmupNsStage warmupMinRateHz(double warmupMinRateHz);
    }

    public interface WarmupNsStage {
        /**
         * <p>Startup window, in nanoseconds, during which the relaxed warmup rate applies.</p>
         */
        _FinalStage warmupNs(int warmupNs);
    }

    public interface _FinalStage {
        CaptureSpecImu build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements DeviceMotionUnitsStage,
                    MagnetometerUnitsStage,
                    MinRateHzStage,
                    TargetRateHzStage,
                    WarmupMinRateHzStage,
                    WarmupNsStage,
                    _FinalStage {
        private String deviceMotionUnits;

        private String magnetometerUnits;

        private double minRateHz;

        private int targetRateHz;

        private double warmupMinRateHz;

        private int warmupNs;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CaptureSpecImu other) {
            deviceMotionUnits(other.getDeviceMotionUnits());
            magnetometerUnits(other.getMagnetometerUnits());
            minRateHz(other.getMinRateHz());
            targetRateHz(other.getTargetRateHz());
            warmupMinRateHz(other.getWarmupMinRateHz());
            warmupNs(other.getWarmupNs());
            return this;
        }

        /**
         * <p>Units for the device-motion channels, as a compact key=unit string.</p>
         * <p>Units for the device-motion channels, as a compact key=unit string.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("device_motion_units")
        public MagnetometerUnitsStage deviceMotionUnits(@NotNull String deviceMotionUnits) {
            this.deviceMotionUnits = Objects.requireNonNull(deviceMotionUnits, "deviceMotionUnits must not be null");
            return this;
        }

        /**
         * <p>Units for the magnetometer channel.</p>
         * <p>Units for the magnetometer channel.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("magnetometer_units")
        public MinRateHzStage magnetometerUnits(@NotNull String magnetometerUnits) {
            this.magnetometerUnits = Objects.requireNonNull(magnetometerUnits, "magnetometerUnits must not be null");
            return this;
        }

        /**
         * <p>Minimum sustained IMU sample rate in hertz for a clip to pass validation.</p>
         * <p>Minimum sustained IMU sample rate in hertz for a clip to pass validation.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("min_rate_hz")
        public TargetRateHzStage minRateHz(double minRateHz) {
            this.minRateHz = minRateHz;
            return this;
        }

        /**
         * <p>Target IMU sample rate in hertz.</p>
         * <p>Target IMU sample rate in hertz.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("target_rate_hz")
        public WarmupMinRateHzStage targetRateHz(int targetRateHz) {
            this.targetRateHz = targetRateHz;
            return this;
        }

        /**
         * <p>Minimum IMU sample rate in hertz tolerated during the warmup window.</p>
         * <p>Minimum IMU sample rate in hertz tolerated during the warmup window.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("warmup_min_rate_hz")
        public WarmupNsStage warmupMinRateHz(double warmupMinRateHz) {
            this.warmupMinRateHz = warmupMinRateHz;
            return this;
        }

        /**
         * <p>Startup window, in nanoseconds, during which the relaxed warmup rate applies.</p>
         * <p>Startup window, in nanoseconds, during which the relaxed warmup rate applies.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("warmup_ns")
        public _FinalStage warmupNs(int warmupNs) {
            this.warmupNs = warmupNs;
            return this;
        }

        @java.lang.Override
        public CaptureSpecImu build() {
            return new CaptureSpecImu(
                    deviceMotionUnits,
                    magnetometerUnits,
                    minRateHz,
                    targetRateHz,
                    warmupMinRateHz,
                    warmupNs,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
