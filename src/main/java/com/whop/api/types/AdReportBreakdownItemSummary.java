/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AdReportBreakdownItemSummary.Builder.class)
public final class AdReportBreakdownItemSummary {
    private final double clickThroughRate;

    private final int clicks;

    private final double costPerClick;

    private final Optional<Double> costPerMille;

    private final Optional<Double> costPerResult;

    private final Optional<Double> frequency;

    private final int impressions;

    private final int reach;

    private final Optional<Integer> resultCount;

    private final Optional<ResultLabelKeys> resultLabelKey;

    private final Optional<String> resultLabelOverride;

    private final Optional<Double> returnOnAdSpend;

    private final double spend;

    private final Optional<Currencies> spendCurrency;

    private final Map<String, Object> additionalProperties;

    private AdReportBreakdownItemSummary(
            double clickThroughRate,
            int clicks,
            double costPerClick,
            Optional<Double> costPerMille,
            Optional<Double> costPerResult,
            Optional<Double> frequency,
            int impressions,
            int reach,
            Optional<Integer> resultCount,
            Optional<ResultLabelKeys> resultLabelKey,
            Optional<String> resultLabelOverride,
            Optional<Double> returnOnAdSpend,
            double spend,
            Optional<Currencies> spendCurrency,
            Map<String, Object> additionalProperties) {
        this.clickThroughRate = clickThroughRate;
        this.clicks = clicks;
        this.costPerClick = costPerClick;
        this.costPerMille = costPerMille;
        this.costPerResult = costPerResult;
        this.frequency = frequency;
        this.impressions = impressions;
        this.reach = reach;
        this.resultCount = resultCount;
        this.resultLabelKey = resultLabelKey;
        this.resultLabelOverride = resultLabelOverride;
        this.returnOnAdSpend = returnOnAdSpend;
        this.spend = spend;
        this.spendCurrency = spendCurrency;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Click-through rate (clicks / impressions).
     */
    @JsonProperty("click_through_rate")
    public double getClickThroughRate() {
        return clickThroughRate;
    }

    /**
     * @return Total clicks over the date range.
     */
    @JsonProperty("clicks")
    public int getClicks() {
        return clicks;
    }

    /**
     * @return Cost per click in the requested reporting currency.
     */
    @JsonProperty("cost_per_click")
    public double getCostPerClick() {
        return costPerClick;
    }

    /**
     * @return Cost per thousand impressions in the requested reporting currency.
     */
    @JsonIgnore
    public Optional<Double> getCostPerMille() {
        if (costPerMille == null) {
            return Optional.empty();
        }
        return costPerMille;
    }

    /**
     * @return Spend divided by <code>resultCount</code>. Null when there are no results.
     */
    @JsonIgnore
    public Optional<Double> getCostPerResult() {
        if (costPerResult == null) {
            return Optional.empty();
        }
        return costPerResult;
    }

    /**
     * @return Average number of times each reached user saw an ad.
     */
    @JsonIgnore
    public Optional<Double> getFrequency() {
        if (frequency == null) {
            return Optional.empty();
        }
        return frequency;
    }

    /**
     * @return Total impressions over the date range.
     */
    @JsonProperty("impressions")
    public int getImpressions() {
        return impressions;
    }

    /**
     * @return Unique users reached, deduplicated by the external ad platform.
     */
    @JsonProperty("reach")
    public int getReach() {
        return reach;
    }

    /**
     * @return Count of the campaign's primary optimization result (purchases, clicks, etc.) — see <code>resultLabelKey</code>.
     */
    @JsonIgnore
    public Optional<Integer> getResultCount() {
        if (resultCount == null) {
            return Optional.empty();
        }
        return resultCount;
    }

    /**
     * @return The type of optimization result represented by <code>resultCount</code>.
     */
    @JsonIgnore
    public Optional<ResultLabelKeys> getResultLabelKey() {
        if (resultLabelKey == null) {
            return Optional.empty();
        }
        return resultLabelKey;
    }

    /**
     * @return Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.
     */
    @JsonIgnore
    public Optional<String> getResultLabelOverride() {
        if (resultLabelOverride == null) {
            return Optional.empty();
        }
        return resultLabelOverride;
    }

    /**
     * @return Alias for <code>purchaseReturnOnAdSpend</code> — return on ad spend for purchases, as reported by the external ad platform.
     */
    @JsonIgnore
    public Optional<Double> getReturnOnAdSpend() {
        if (returnOnAdSpend == null) {
            return Optional.empty();
        }
        return returnOnAdSpend;
    }

    /**
     * @return Total spend over the date range in the requested reporting currency.
     */
    @JsonProperty("spend")
    public double getSpend() {
        return spend;
    }

    /**
     * @return Currency of the <code>spend</code> value. Matches the requested <code>currency</code> when set.
     */
    @JsonIgnore
    public Optional<Currencies> getSpendCurrency() {
        if (spendCurrency == null) {
            return Optional.empty();
        }
        return spendCurrency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("cost_per_mille")
    private Optional<Double> _getCostPerMille() {
        return costPerMille;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("cost_per_result")
    private Optional<Double> _getCostPerResult() {
        return costPerResult;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("frequency")
    private Optional<Double> _getFrequency() {
        return frequency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("result_count")
    private Optional<Integer> _getResultCount() {
        return resultCount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("result_label_key")
    private Optional<ResultLabelKeys> _getResultLabelKey() {
        return resultLabelKey;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("result_label_override")
    private Optional<String> _getResultLabelOverride() {
        return resultLabelOverride;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("return_on_ad_spend")
    private Optional<Double> _getReturnOnAdSpend() {
        return returnOnAdSpend;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("spend_currency")
    private Optional<Currencies> _getSpendCurrency() {
        return spendCurrency;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AdReportBreakdownItemSummary && equalTo((AdReportBreakdownItemSummary) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AdReportBreakdownItemSummary other) {
        return clickThroughRate == other.clickThroughRate
                && clicks == other.clicks
                && costPerClick == other.costPerClick
                && costPerMille.equals(other.costPerMille)
                && costPerResult.equals(other.costPerResult)
                && frequency.equals(other.frequency)
                && impressions == other.impressions
                && reach == other.reach
                && resultCount.equals(other.resultCount)
                && resultLabelKey.equals(other.resultLabelKey)
                && resultLabelOverride.equals(other.resultLabelOverride)
                && returnOnAdSpend.equals(other.returnOnAdSpend)
                && spend == other.spend
                && spendCurrency.equals(other.spendCurrency);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.clickThroughRate,
                this.clicks,
                this.costPerClick,
                this.costPerMille,
                this.costPerResult,
                this.frequency,
                this.impressions,
                this.reach,
                this.resultCount,
                this.resultLabelKey,
                this.resultLabelOverride,
                this.returnOnAdSpend,
                this.spend,
                this.spendCurrency);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ClickThroughRateStage builder() {
        return new Builder();
    }

    public interface ClickThroughRateStage {
        /**
         * <p>Click-through rate (clicks / impressions).</p>
         */
        ClicksStage clickThroughRate(double clickThroughRate);

        Builder from(AdReportBreakdownItemSummary other);
    }

    public interface ClicksStage {
        /**
         * <p>Total clicks over the date range.</p>
         */
        CostPerClickStage clicks(int clicks);
    }

    public interface CostPerClickStage {
        /**
         * <p>Cost per click in the requested reporting currency.</p>
         */
        ImpressionsStage costPerClick(double costPerClick);
    }

    public interface ImpressionsStage {
        /**
         * <p>Total impressions over the date range.</p>
         */
        ReachStage impressions(int impressions);
    }

    public interface ReachStage {
        /**
         * <p>Unique users reached, deduplicated by the external ad platform.</p>
         */
        SpendStage reach(int reach);
    }

    public interface SpendStage {
        /**
         * <p>Total spend over the date range in the requested reporting currency.</p>
         */
        _FinalStage spend(double spend);
    }

    public interface _FinalStage {
        AdReportBreakdownItemSummary build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Cost per thousand impressions in the requested reporting currency.</p>
         */
        _FinalStage costPerMille(Optional<Double> costPerMille);

        _FinalStage costPerMille(Double costPerMille);

        _FinalStage costPerMille(Nullable<Double> costPerMille);

        /**
         * <p>Spend divided by <code>resultCount</code>. Null when there are no results.</p>
         */
        _FinalStage costPerResult(Optional<Double> costPerResult);

        _FinalStage costPerResult(Double costPerResult);

        _FinalStage costPerResult(Nullable<Double> costPerResult);

        /**
         * <p>Average number of times each reached user saw an ad.</p>
         */
        _FinalStage frequency(Optional<Double> frequency);

        _FinalStage frequency(Double frequency);

        _FinalStage frequency(Nullable<Double> frequency);

        /**
         * <p>Count of the campaign's primary optimization result (purchases, clicks, etc.) — see <code>resultLabelKey</code>.</p>
         */
        _FinalStage resultCount(Optional<Integer> resultCount);

        _FinalStage resultCount(Integer resultCount);

        _FinalStage resultCount(Nullable<Integer> resultCount);

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         */
        _FinalStage resultLabelKey(Optional<ResultLabelKeys> resultLabelKey);

        _FinalStage resultLabelKey(ResultLabelKeys resultLabelKey);

        _FinalStage resultLabelKey(Nullable<ResultLabelKeys> resultLabelKey);

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         */
        _FinalStage resultLabelOverride(Optional<String> resultLabelOverride);

        _FinalStage resultLabelOverride(String resultLabelOverride);

        _FinalStage resultLabelOverride(Nullable<String> resultLabelOverride);

        /**
         * <p>Alias for <code>purchaseReturnOnAdSpend</code> — return on ad spend for purchases, as reported by the external ad platform.</p>
         */
        _FinalStage returnOnAdSpend(Optional<Double> returnOnAdSpend);

        _FinalStage returnOnAdSpend(Double returnOnAdSpend);

        _FinalStage returnOnAdSpend(Nullable<Double> returnOnAdSpend);

        /**
         * <p>Currency of the <code>spend</code> value. Matches the requested <code>currency</code> when set.</p>
         */
        _FinalStage spendCurrency(Optional<Currencies> spendCurrency);

        _FinalStage spendCurrency(Currencies spendCurrency);

        _FinalStage spendCurrency(Nullable<Currencies> spendCurrency);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements ClickThroughRateStage,
                    ClicksStage,
                    CostPerClickStage,
                    ImpressionsStage,
                    ReachStage,
                    SpendStage,
                    _FinalStage {
        private double clickThroughRate;

        private int clicks;

        private double costPerClick;

        private int impressions;

        private int reach;

        private double spend;

        private Optional<Currencies> spendCurrency = Optional.empty();

        private Optional<Double> returnOnAdSpend = Optional.empty();

        private Optional<String> resultLabelOverride = Optional.empty();

        private Optional<ResultLabelKeys> resultLabelKey = Optional.empty();

        private Optional<Integer> resultCount = Optional.empty();

        private Optional<Double> frequency = Optional.empty();

        private Optional<Double> costPerResult = Optional.empty();

        private Optional<Double> costPerMille = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AdReportBreakdownItemSummary other) {
            clickThroughRate(other.getClickThroughRate());
            clicks(other.getClicks());
            costPerClick(other.getCostPerClick());
            costPerMille(other.getCostPerMille());
            costPerResult(other.getCostPerResult());
            frequency(other.getFrequency());
            impressions(other.getImpressions());
            reach(other.getReach());
            resultCount(other.getResultCount());
            resultLabelKey(other.getResultLabelKey());
            resultLabelOverride(other.getResultLabelOverride());
            returnOnAdSpend(other.getReturnOnAdSpend());
            spend(other.getSpend());
            spendCurrency(other.getSpendCurrency());
            return this;
        }

        /**
         * <p>Click-through rate (clicks / impressions).</p>
         * <p>Click-through rate (clicks / impressions).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("click_through_rate")
        public ClicksStage clickThroughRate(double clickThroughRate) {
            this.clickThroughRate = clickThroughRate;
            return this;
        }

        /**
         * <p>Total clicks over the date range.</p>
         * <p>Total clicks over the date range.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("clicks")
        public CostPerClickStage clicks(int clicks) {
            this.clicks = clicks;
            return this;
        }

        /**
         * <p>Cost per click in the requested reporting currency.</p>
         * <p>Cost per click in the requested reporting currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("cost_per_click")
        public ImpressionsStage costPerClick(double costPerClick) {
            this.costPerClick = costPerClick;
            return this;
        }

        /**
         * <p>Total impressions over the date range.</p>
         * <p>Total impressions over the date range.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("impressions")
        public ReachStage impressions(int impressions) {
            this.impressions = impressions;
            return this;
        }

        /**
         * <p>Unique users reached, deduplicated by the external ad platform.</p>
         * <p>Unique users reached, deduplicated by the external ad platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("reach")
        public SpendStage reach(int reach) {
            this.reach = reach;
            return this;
        }

        /**
         * <p>Total spend over the date range in the requested reporting currency.</p>
         * <p>Total spend over the date range in the requested reporting currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("spend")
        public _FinalStage spend(double spend) {
            this.spend = spend;
            return this;
        }

        /**
         * <p>Currency of the <code>spend</code> value. Matches the requested <code>currency</code> when set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage spendCurrency(Nullable<Currencies> spendCurrency) {
            if (spendCurrency.isNull()) {
                this.spendCurrency = null;
            } else if (spendCurrency.isEmpty()) {
                this.spendCurrency = Optional.empty();
            } else {
                this.spendCurrency = Optional.of(spendCurrency.get());
            }
            return this;
        }

        /**
         * <p>Currency of the <code>spend</code> value. Matches the requested <code>currency</code> when set.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage spendCurrency(Currencies spendCurrency) {
            this.spendCurrency = Optional.ofNullable(spendCurrency);
            return this;
        }

        /**
         * <p>Currency of the <code>spend</code> value. Matches the requested <code>currency</code> when set.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "spend_currency", nulls = Nulls.SKIP)
        public _FinalStage spendCurrency(Optional<Currencies> spendCurrency) {
            this.spendCurrency = spendCurrency;
            return this;
        }

        /**
         * <p>Alias for <code>purchaseReturnOnAdSpend</code> — return on ad spend for purchases, as reported by the external ad platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage returnOnAdSpend(Nullable<Double> returnOnAdSpend) {
            if (returnOnAdSpend.isNull()) {
                this.returnOnAdSpend = null;
            } else if (returnOnAdSpend.isEmpty()) {
                this.returnOnAdSpend = Optional.empty();
            } else {
                this.returnOnAdSpend = Optional.of(returnOnAdSpend.get());
            }
            return this;
        }

        /**
         * <p>Alias for <code>purchaseReturnOnAdSpend</code> — return on ad spend for purchases, as reported by the external ad platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage returnOnAdSpend(Double returnOnAdSpend) {
            this.returnOnAdSpend = Optional.ofNullable(returnOnAdSpend);
            return this;
        }

        /**
         * <p>Alias for <code>purchaseReturnOnAdSpend</code> — return on ad spend for purchases, as reported by the external ad platform.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "return_on_ad_spend", nulls = Nulls.SKIP)
        public _FinalStage returnOnAdSpend(Optional<Double> returnOnAdSpend) {
            this.returnOnAdSpend = returnOnAdSpend;
            return this;
        }

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelOverride(Nullable<String> resultLabelOverride) {
            if (resultLabelOverride.isNull()) {
                this.resultLabelOverride = null;
            } else if (resultLabelOverride.isEmpty()) {
                this.resultLabelOverride = Optional.empty();
            } else {
                this.resultLabelOverride = Optional.of(resultLabelOverride.get());
            }
            return this;
        }

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelOverride(String resultLabelOverride) {
            this.resultLabelOverride = Optional.ofNullable(resultLabelOverride);
            return this;
        }

        /**
         * <p>Advertiser-defined label for the result when <code>resultLabelKey</code> is <code>custom</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "result_label_override", nulls = Nulls.SKIP)
        public _FinalStage resultLabelOverride(Optional<String> resultLabelOverride) {
            this.resultLabelOverride = resultLabelOverride;
            return this;
        }

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelKey(Nullable<ResultLabelKeys> resultLabelKey) {
            if (resultLabelKey.isNull()) {
                this.resultLabelKey = null;
            } else if (resultLabelKey.isEmpty()) {
                this.resultLabelKey = Optional.empty();
            } else {
                this.resultLabelKey = Optional.of(resultLabelKey.get());
            }
            return this;
        }

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultLabelKey(ResultLabelKeys resultLabelKey) {
            this.resultLabelKey = Optional.ofNullable(resultLabelKey);
            return this;
        }

        /**
         * <p>The type of optimization result represented by <code>resultCount</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "result_label_key", nulls = Nulls.SKIP)
        public _FinalStage resultLabelKey(Optional<ResultLabelKeys> resultLabelKey) {
            this.resultLabelKey = resultLabelKey;
            return this;
        }

        /**
         * <p>Count of the campaign's primary optimization result (purchases, clicks, etc.) — see <code>resultLabelKey</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultCount(Nullable<Integer> resultCount) {
            if (resultCount.isNull()) {
                this.resultCount = null;
            } else if (resultCount.isEmpty()) {
                this.resultCount = Optional.empty();
            } else {
                this.resultCount = Optional.of(resultCount.get());
            }
            return this;
        }

        /**
         * <p>Count of the campaign's primary optimization result (purchases, clicks, etc.) — see <code>resultLabelKey</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage resultCount(Integer resultCount) {
            this.resultCount = Optional.ofNullable(resultCount);
            return this;
        }

        /**
         * <p>Count of the campaign's primary optimization result (purchases, clicks, etc.) — see <code>resultLabelKey</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "result_count", nulls = Nulls.SKIP)
        public _FinalStage resultCount(Optional<Integer> resultCount) {
            this.resultCount = resultCount;
            return this;
        }

        /**
         * <p>Average number of times each reached user saw an ad.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage frequency(Nullable<Double> frequency) {
            if (frequency.isNull()) {
                this.frequency = null;
            } else if (frequency.isEmpty()) {
                this.frequency = Optional.empty();
            } else {
                this.frequency = Optional.of(frequency.get());
            }
            return this;
        }

        /**
         * <p>Average number of times each reached user saw an ad.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage frequency(Double frequency) {
            this.frequency = Optional.ofNullable(frequency);
            return this;
        }

        /**
         * <p>Average number of times each reached user saw an ad.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "frequency", nulls = Nulls.SKIP)
        public _FinalStage frequency(Optional<Double> frequency) {
            this.frequency = frequency;
            return this;
        }

        /**
         * <p>Spend divided by <code>resultCount</code>. Null when there are no results.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage costPerResult(Nullable<Double> costPerResult) {
            if (costPerResult.isNull()) {
                this.costPerResult = null;
            } else if (costPerResult.isEmpty()) {
                this.costPerResult = Optional.empty();
            } else {
                this.costPerResult = Optional.of(costPerResult.get());
            }
            return this;
        }

        /**
         * <p>Spend divided by <code>resultCount</code>. Null when there are no results.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage costPerResult(Double costPerResult) {
            this.costPerResult = Optional.ofNullable(costPerResult);
            return this;
        }

        /**
         * <p>Spend divided by <code>resultCount</code>. Null when there are no results.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "cost_per_result", nulls = Nulls.SKIP)
        public _FinalStage costPerResult(Optional<Double> costPerResult) {
            this.costPerResult = costPerResult;
            return this;
        }

        /**
         * <p>Cost per thousand impressions in the requested reporting currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage costPerMille(Nullable<Double> costPerMille) {
            if (costPerMille.isNull()) {
                this.costPerMille = null;
            } else if (costPerMille.isEmpty()) {
                this.costPerMille = Optional.empty();
            } else {
                this.costPerMille = Optional.of(costPerMille.get());
            }
            return this;
        }

        /**
         * <p>Cost per thousand impressions in the requested reporting currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage costPerMille(Double costPerMille) {
            this.costPerMille = Optional.ofNullable(costPerMille);
            return this;
        }

        /**
         * <p>Cost per thousand impressions in the requested reporting currency.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "cost_per_mille", nulls = Nulls.SKIP)
        public _FinalStage costPerMille(Optional<Double> costPerMille) {
            this.costPerMille = costPerMille;
            return this;
        }

        @java.lang.Override
        public AdReportBreakdownItemSummary build() {
            return new AdReportBreakdownItemSummary(
                    clickThroughRate,
                    clicks,
                    costPerClick,
                    costPerMille,
                    costPerResult,
                    frequency,
                    impressions,
                    reach,
                    resultCount,
                    resultLabelKey,
                    resultLabelOverride,
                    returnOnAdSpend,
                    spend,
                    spendCurrency,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
