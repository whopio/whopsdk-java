/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionBreakdownLine.Builder.class)
public final class CheckoutSessionBreakdownLine {
    private final Money amount;

    private final String description;

    private final CheckoutSessionBreakdownLineKind kind;

    private final int quantity;

    private final Money unitAmount;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionBreakdownLine(
            Money amount,
            String description,
            CheckoutSessionBreakdownLineKind kind,
            int quantity,
            Money unitAmount,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.description = description;
        this.kind = kind;
        this.quantity = quantity;
        this.unitAmount = unitAmount;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The line's own total — <code>unit_amount</code> × <code>quantity</code>, before any adjustment.
     */
    @JsonProperty("amount")
    public Money getAmount() {
        return amount;
    }

    /**
     * @return What to show the buyer for this line.
     */
    @JsonProperty("description")
    public String getDescription() {
        return description;
    }

    /**
     * @return What this line is — <code>plan</code> today. New kinds arrive as checkout learns to sell more than one thing at once.
     */
    @JsonProperty("kind")
    public CheckoutSessionBreakdownLineKind getKind() {
        return kind;
    }

    /**
     * @return How many.
     */
    @JsonProperty("quantity")
    public int getQuantity() {
        return quantity;
    }

    /**
     * @return Price of one, before any adjustment.
     */
    @JsonProperty("unit_amount")
    public Money getUnitAmount() {
        return unitAmount;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionBreakdownLine && equalTo((CheckoutSessionBreakdownLine) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionBreakdownLine other) {
        return amount.equals(other.amount)
                && description.equals(other.description)
                && kind.equals(other.kind)
                && quantity == other.quantity
                && unitAmount.equals(other.unitAmount);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.amount, this.description, this.kind, this.quantity, this.unitAmount);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>The line's own total — <code>unit_amount</code> × <code>quantity</code>, before any adjustment.</p>
         */
        DescriptionStage amount(@NotNull Money amount);

        Builder from(CheckoutSessionBreakdownLine other);
    }

    public interface DescriptionStage {
        /**
         * <p>What to show the buyer for this line.</p>
         */
        KindStage description(@NotNull String description);
    }

    public interface KindStage {
        /**
         * <p>What this line is — <code>plan</code> today. New kinds arrive as checkout learns to sell more than one thing at once.</p>
         */
        QuantityStage kind(@NotNull CheckoutSessionBreakdownLineKind kind);
    }

    public interface QuantityStage {
        /**
         * <p>How many.</p>
         */
        UnitAmountStage quantity(int quantity);
    }

    public interface UnitAmountStage {
        /**
         * <p>Price of one, before any adjustment.</p>
         */
        _FinalStage unitAmount(@NotNull Money unitAmount);
    }

    public interface _FinalStage {
        CheckoutSessionBreakdownLine build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AmountStage, DescriptionStage, KindStage, QuantityStage, UnitAmountStage, _FinalStage {
        private Money amount;

        private String description;

        private CheckoutSessionBreakdownLineKind kind;

        private int quantity;

        private Money unitAmount;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionBreakdownLine other) {
            amount(other.getAmount());
            description(other.getDescription());
            kind(other.getKind());
            quantity(other.getQuantity());
            unitAmount(other.getUnitAmount());
            return this;
        }

        /**
         * <p>The line's own total — <code>unit_amount</code> × <code>quantity</code>, before any adjustment.</p>
         * <p>The line's own total — <code>unit_amount</code> × <code>quantity</code>, before any adjustment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public DescriptionStage amount(@NotNull Money amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>What to show the buyer for this line.</p>
         * <p>What to show the buyer for this line.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("description")
        public KindStage description(@NotNull String description) {
            this.description = Objects.requireNonNull(description, "description must not be null");
            return this;
        }

        /**
         * <p>What this line is — <code>plan</code> today. New kinds arrive as checkout learns to sell more than one thing at once.</p>
         * <p>What this line is — <code>plan</code> today. New kinds arrive as checkout learns to sell more than one thing at once.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("kind")
        public QuantityStage kind(@NotNull CheckoutSessionBreakdownLineKind kind) {
            this.kind = Objects.requireNonNull(kind, "kind must not be null");
            return this;
        }

        /**
         * <p>How many.</p>
         * <p>How many.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("quantity")
        public UnitAmountStage quantity(int quantity) {
            this.quantity = quantity;
            return this;
        }

        /**
         * <p>Price of one, before any adjustment.</p>
         * <p>Price of one, before any adjustment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("unit_amount")
        public _FinalStage unitAmount(@NotNull Money unitAmount) {
            this.unitAmount = Objects.requireNonNull(unitAmount, "unitAmount must not be null");
            return this;
        }

        @java.lang.Override
        public CheckoutSessionBreakdownLine build() {
            return new CheckoutSessionBreakdownLine(
                    amount, description, kind, quantity, unitAmount, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
