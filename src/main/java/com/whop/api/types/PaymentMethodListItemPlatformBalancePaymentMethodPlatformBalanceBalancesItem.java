/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem.Builder.class)
public final class PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem {
    private final String amount;

    private final Currencies currency;

    private final int decimals;

    private final int displayDecimals;

    private final Map<String, Object> additionalProperties;

    private PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem(
            String amount,
            Currencies currency,
            int decimals,
            int displayDecimals,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.currency = currency;
        this.decimals = decimals;
        this.displayDecimals = displayDecimals;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The amount in major units, as an exact decimal string — <code>&quot;10.00&quot;</code> is ten dollars. A string so no float rounds it in transit.
     */
    @JsonProperty("amount")
    public String getAmount() {
        return amount;
    }

    /**
     * @return Three-letter ISO 4217 currency code, lowercase.
     */
    @JsonProperty("currency")
    public Currencies getCurrency() {
        return currency;
    }

    /**
     * @return How many decimal places the amount CARRIES — the precision the charge itself runs at.
     */
    @JsonProperty("decimals")
    public int getDecimals() {
        return decimals;
    }

    /**
     * @return How many decimal places to SHOW. Usually equal to <code>decimals</code>, and deliberately not always: COP is charged in centavos but written in whole pesos, so it is <code>2</code> and <code>0</code>. Format the number in your own locale using this.
     */
    @JsonProperty("display_decimals")
    public int getDisplayDecimals() {
        return displayDecimals;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem
                && equalTo((PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem other) {
        return amount.equals(other.amount)
                && currency.equals(other.currency)
                && decimals == other.decimals
                && displayDecimals == other.displayDecimals;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.amount, this.currency, this.decimals, this.displayDecimals);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>The amount in major units, as an exact decimal string — <code>&quot;10.00&quot;</code> is ten dollars. A string so no float rounds it in transit.</p>
         */
        CurrencyStage amount(@NotNull String amount);

        Builder from(PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem other);
    }

    public interface CurrencyStage {
        /**
         * <p>Three-letter ISO 4217 currency code, lowercase.</p>
         */
        DecimalsStage currency(@NotNull Currencies currency);
    }

    public interface DecimalsStage {
        /**
         * <p>How many decimal places the amount CARRIES — the precision the charge itself runs at.</p>
         */
        DisplayDecimalsStage decimals(int decimals);
    }

    public interface DisplayDecimalsStage {
        /**
         * <p>How many decimal places to SHOW. Usually equal to <code>decimals</code>, and deliberately not always: COP is charged in centavos but written in whole pesos, so it is <code>2</code> and <code>0</code>. Format the number in your own locale using this.</p>
         */
        _FinalStage displayDecimals(int displayDecimals);
    }

    public interface _FinalStage {
        PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AmountStage, CurrencyStage, DecimalsStage, DisplayDecimalsStage, _FinalStage {
        private String amount;

        private Currencies currency;

        private int decimals;

        private int displayDecimals;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem other) {
            amount(other.getAmount());
            currency(other.getCurrency());
            decimals(other.getDecimals());
            displayDecimals(other.getDisplayDecimals());
            return this;
        }

        /**
         * <p>The amount in major units, as an exact decimal string — <code>&quot;10.00&quot;</code> is ten dollars. A string so no float rounds it in transit.</p>
         * <p>The amount in major units, as an exact decimal string — <code>&quot;10.00&quot;</code> is ten dollars. A string so no float rounds it in transit.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public CurrencyStage amount(@NotNull String amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>Three-letter ISO 4217 currency code, lowercase.</p>
         * <p>Three-letter ISO 4217 currency code, lowercase.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public DecimalsStage currency(@NotNull Currencies currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>How many decimal places the amount CARRIES — the precision the charge itself runs at.</p>
         * <p>How many decimal places the amount CARRIES — the precision the charge itself runs at.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("decimals")
        public DisplayDecimalsStage decimals(int decimals) {
            this.decimals = decimals;
            return this;
        }

        /**
         * <p>How many decimal places to SHOW. Usually equal to <code>decimals</code>, and deliberately not always: COP is charged in centavos but written in whole pesos, so it is <code>2</code> and <code>0</code>. Format the number in your own locale using this.</p>
         * <p>How many decimal places to SHOW. Usually equal to <code>decimals</code>, and deliberately not always: COP is charged in centavos but written in whole pesos, so it is <code>2</code> and <code>0</code>. Format the number in your own locale using this.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("display_decimals")
        public _FinalStage displayDecimals(int displayDecimals) {
            this.displayDecimals = displayDecimals;
            return this;
        }

        @java.lang.Override
        public PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem build() {
            return new PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalanceBalancesItem(
                    amount, currency, decimals, displayDecimals, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
