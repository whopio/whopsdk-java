/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = UserPreferences.Builder.class)
public final class UserPreferences {
    private final boolean bountyWorkerOnboardingDismissed;

    private final boolean investigationEnabled;

    private final boolean termsAccepted;

    private final Optional<String> termsAcceptedAt;

    private final Map<String, Object> additionalProperties;

    private UserPreferences(
            boolean bountyWorkerOnboardingDismissed,
            boolean investigationEnabled,
            boolean termsAccepted,
            Optional<String> termsAcceptedAt,
            Map<String, Object> additionalProperties) {
        this.bountyWorkerOnboardingDismissed = bountyWorkerOnboardingDismissed;
        this.investigationEnabled = investigationEnabled;
        this.termsAccepted = termsAccepted;
        this.termsAcceptedAt = termsAcceptedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether the user has dismissed the first-time bounty worker onboarding. Set to <code>false</code> to show it again.
     */
    @JsonProperty("bounty_worker_onboarding_dismissed")
    public boolean getBountyWorkerOnboardingDismissed() {
        return bountyWorkerOnboardingDismissed;
    }

    /**
     * @return Whether investigation mode is enabled for the user. Only meaningful for staff users with investigation access.
     */
    @JsonProperty("investigation_enabled")
    public boolean getInvestigationEnabled() {
        return investigationEnabled;
    }

    /**
     * @return Whether the user has accepted Whop's terms and policies. <code>false</code> until recorded via <code>PATCH</code> with <code>terms_accepted: true</code>.
     */
    @JsonProperty("terms_accepted")
    public boolean getTermsAccepted() {
        return termsAccepted;
    }

    /**
     * @return When the user most recently accepted Whop's terms and policies, as an ISO 8601 timestamp. <code>null</code> until accepted.
     */
    @JsonIgnore
    public Optional<String> getTermsAcceptedAt() {
        if (termsAcceptedAt == null) {
            return Optional.empty();
        }
        return termsAcceptedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("terms_accepted_at")
    private Optional<String> _getTermsAcceptedAt() {
        return termsAcceptedAt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UserPreferences && equalTo((UserPreferences) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(UserPreferences other) {
        return bountyWorkerOnboardingDismissed == other.bountyWorkerOnboardingDismissed
                && investigationEnabled == other.investigationEnabled
                && termsAccepted == other.termsAccepted
                && termsAcceptedAt.equals(other.termsAcceptedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.bountyWorkerOnboardingDismissed,
                this.investigationEnabled,
                this.termsAccepted,
                this.termsAcceptedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BountyWorkerOnboardingDismissedStage builder() {
        return new Builder();
    }

    public interface BountyWorkerOnboardingDismissedStage {
        /**
         * <p>Whether the user has dismissed the first-time bounty worker onboarding. Set to <code>false</code> to show it again.</p>
         */
        InvestigationEnabledStage bountyWorkerOnboardingDismissed(boolean bountyWorkerOnboardingDismissed);

        Builder from(UserPreferences other);
    }

    public interface InvestigationEnabledStage {
        /**
         * <p>Whether investigation mode is enabled for the user. Only meaningful for staff users with investigation access.</p>
         */
        TermsAcceptedStage investigationEnabled(boolean investigationEnabled);
    }

    public interface TermsAcceptedStage {
        /**
         * <p>Whether the user has accepted Whop's terms and policies. <code>false</code> until recorded via <code>PATCH</code> with <code>terms_accepted: true</code>.</p>
         */
        _FinalStage termsAccepted(boolean termsAccepted);
    }

    public interface _FinalStage {
        UserPreferences build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When the user most recently accepted Whop's terms and policies, as an ISO 8601 timestamp. <code>null</code> until accepted.</p>
         */
        _FinalStage termsAcceptedAt(Optional<String> termsAcceptedAt);

        _FinalStage termsAcceptedAt(String termsAcceptedAt);

        _FinalStage termsAcceptedAt(Nullable<String> termsAcceptedAt);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements BountyWorkerOnboardingDismissedStage,
                    InvestigationEnabledStage,
                    TermsAcceptedStage,
                    _FinalStage {
        private boolean bountyWorkerOnboardingDismissed;

        private boolean investigationEnabled;

        private boolean termsAccepted;

        private Optional<String> termsAcceptedAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(UserPreferences other) {
            bountyWorkerOnboardingDismissed(other.getBountyWorkerOnboardingDismissed());
            investigationEnabled(other.getInvestigationEnabled());
            termsAccepted(other.getTermsAccepted());
            termsAcceptedAt(other.getTermsAcceptedAt());
            return this;
        }

        /**
         * <p>Whether the user has dismissed the first-time bounty worker onboarding. Set to <code>false</code> to show it again.</p>
         * <p>Whether the user has dismissed the first-time bounty worker onboarding. Set to <code>false</code> to show it again.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("bounty_worker_onboarding_dismissed")
        public InvestigationEnabledStage bountyWorkerOnboardingDismissed(boolean bountyWorkerOnboardingDismissed) {
            this.bountyWorkerOnboardingDismissed = bountyWorkerOnboardingDismissed;
            return this;
        }

        /**
         * <p>Whether investigation mode is enabled for the user. Only meaningful for staff users with investigation access.</p>
         * <p>Whether investigation mode is enabled for the user. Only meaningful for staff users with investigation access.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("investigation_enabled")
        public TermsAcceptedStage investigationEnabled(boolean investigationEnabled) {
            this.investigationEnabled = investigationEnabled;
            return this;
        }

        /**
         * <p>Whether the user has accepted Whop's terms and policies. <code>false</code> until recorded via <code>PATCH</code> with <code>terms_accepted: true</code>.</p>
         * <p>Whether the user has accepted Whop's terms and policies. <code>false</code> until recorded via <code>PATCH</code> with <code>terms_accepted: true</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("terms_accepted")
        public _FinalStage termsAccepted(boolean termsAccepted) {
            this.termsAccepted = termsAccepted;
            return this;
        }

        /**
         * <p>When the user most recently accepted Whop's terms and policies, as an ISO 8601 timestamp. <code>null</code> until accepted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage termsAcceptedAt(Nullable<String> termsAcceptedAt) {
            if (termsAcceptedAt.isNull()) {
                this.termsAcceptedAt = null;
            } else if (termsAcceptedAt.isEmpty()) {
                this.termsAcceptedAt = Optional.empty();
            } else {
                this.termsAcceptedAt = Optional.of(termsAcceptedAt.get());
            }
            return this;
        }

        /**
         * <p>When the user most recently accepted Whop's terms and policies, as an ISO 8601 timestamp. <code>null</code> until accepted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage termsAcceptedAt(String termsAcceptedAt) {
            this.termsAcceptedAt = Optional.ofNullable(termsAcceptedAt);
            return this;
        }

        /**
         * <p>When the user most recently accepted Whop's terms and policies, as an ISO 8601 timestamp. <code>null</code> until accepted.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "terms_accepted_at", nulls = Nulls.SKIP)
        public _FinalStage termsAcceptedAt(Optional<String> termsAcceptedAt) {
            this.termsAcceptedAt = termsAcceptedAt;
            return this;
        }

        @java.lang.Override
        public UserPreferences build() {
            return new UserPreferences(
                    bountyWorkerOnboardingDismissed,
                    investigationEnabled,
                    termsAccepted,
                    termsAcceptedAt,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
