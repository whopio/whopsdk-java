/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class PaymentInstructions {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PaymentInstructions(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PaymentInstructions bankTransfer(PaymentBankTransferInstructions value) {
        return new PaymentInstructions(new BankTransferValue(value));
    }

    public static PaymentInstructions qr(PaymentQrInstructions value) {
        return new PaymentInstructions(new QrValue(value));
    }

    public static PaymentInstructions voucher(PaymentVoucherInstructions value) {
        return new PaymentInstructions(new VoucherValue(value));
    }

    public boolean isBankTransfer() {
        return value instanceof BankTransferValue;
    }

    public boolean isQr() {
        return value instanceof QrValue;
    }

    public boolean isVoucher() {
        return value instanceof VoucherValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<PaymentBankTransferInstructions> getBankTransfer() {
        if (isBankTransfer()) {
            return Optional.of(((BankTransferValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PaymentQrInstructions> getQr() {
        if (isQr()) {
            return Optional.of(((QrValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PaymentVoucherInstructions> getVoucher() {
        if (isVoucher()) {
            return Optional.of(((VoucherValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentInstructions && value.equals(((PaymentInstructions) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitBankTransfer(PaymentBankTransferInstructions bankTransfer);

        T visitQr(PaymentQrInstructions qr);

        T visitVoucher(PaymentVoucherInstructions voucher);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "kind", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(BankTransferValue.class),
        @JsonSubTypes.Type(QrValue.class),
        @JsonSubTypes.Type(VoucherValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("bank_transfer")
    @JsonIgnoreProperties("kind")
    private static final class BankTransferValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private PaymentBankTransferInstructions value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BankTransferValue() {}

        private BankTransferValue(PaymentBankTransferInstructions value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBankTransfer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BankTransferValue && equalTo((BankTransferValue) other);
        }

        private boolean equalTo(BankTransferValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentInstructions{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("qr")
    @JsonIgnoreProperties("kind")
    private static final class QrValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private PaymentQrInstructions value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private QrValue() {}

        private QrValue(PaymentQrInstructions value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitQr(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof QrValue && equalTo((QrValue) other);
        }

        private boolean equalTo(QrValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentInstructions{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("voucher")
    @JsonIgnoreProperties("kind")
    private static final class VoucherValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private PaymentVoucherInstructions value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private VoucherValue() {}

        private VoucherValue(PaymentVoucherInstructions value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitVoucher(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof VoucherValue && equalTo((VoucherValue) other);
        }

        private boolean equalTo(VoucherValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentInstructions{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("kind")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentInstructions{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
