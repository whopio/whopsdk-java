/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionBreakdownUpcomingRecurring.Builder.class)
public final class CheckoutSessionBreakdownUpcomingRecurring {
    private final Money amount;

    private final String chargeAt;

    private final String description;

    private final int intervalDays;

    private final Optional<Money> postPromoAmount;

    private final Optional<Integer> trialDays;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionBreakdownUpcomingRecurring(
            Money amount,
            String chargeAt,
            String description,
            int intervalDays,
            Optional<Money> postPromoAmount,
            Optional<Integer> trialDays,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.chargeAt = chargeAt;
        this.description = description;
        this.intervalDays = intervalDays;
        this.postPromoAmount = postPromoAmount;
        this.trialDays = trialDays;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What the NEXT charge takes, priced as its receipt will be: the renewal for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge — a <code>forever</code> code discounts every cycle, a <code>once</code> or <code>repeating</code> one only the paying charges it has left. Stated before tax.
     */
    @JsonProperty("amount")
    public Money getAmount() {
        return amount;
    }

    /**
     * @return When the NEXT charge happens — a trial's end, a transfer's inherited period end, or the next cycle — as an ISO 8601 timestamp.
     */
    @JsonProperty("charge_at")
    public String getChargeAt() {
        return chargeAt;
    }

    /**
     * @return What to show the buyer for this future charge.
     */
    @JsonProperty("description")
    public String getDescription() {
        return description;
    }

    /**
     * @return Days between charges.
     */
    @JsonProperty("interval_days")
    public int getIntervalDays() {
        return intervalDays;
    }

    /**
     * @return What each charge takes once the applied promo stops covering renewals — the steady price the subscription returns to, priced like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires (a <code>once</code> code covering the first real charge, or a <code>repeating</code> window the schedule outlives — <code>promo.duration</code> says which). Absent when no promo discounts the next charge, and for a <code>forever</code> code, whose discount never ends.
     */
    @JsonProperty("post_promo_amount")
    public Optional<Money> getPostPromoAmount() {
        return postPromoAmount;
    }

    /**
     * @return Free-trial days before the first charge. Absent when billing starts with this purchase — and on a membership transfer, which inherits the origin's clock and never re-trials.
     */
    @JsonProperty("trial_days")
    public Optional<Integer> getTrialDays() {
        return trialDays;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionBreakdownUpcomingRecurring
                && equalTo((CheckoutSessionBreakdownUpcomingRecurring) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionBreakdownUpcomingRecurring other) {
        return amount.equals(other.amount)
                && chargeAt.equals(other.chargeAt)
                && description.equals(other.description)
                && intervalDays == other.intervalDays
                && postPromoAmount.equals(other.postPromoAmount)
                && trialDays.equals(other.trialDays);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amount, this.chargeAt, this.description, this.intervalDays, this.postPromoAmount, this.trialDays);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>What the NEXT charge takes, priced as its receipt will be: the renewal for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge — a <code>forever</code> code discounts every cycle, a <code>once</code> or <code>repeating</code> one only the paying charges it has left. Stated before tax.</p>
         */
        ChargeAtStage amount(@NotNull Money amount);

        Builder from(CheckoutSessionBreakdownUpcomingRecurring other);
    }

    public interface ChargeAtStage {
        /**
         * <p>When the NEXT charge happens — a trial's end, a transfer's inherited period end, or the next cycle — as an ISO 8601 timestamp.</p>
         */
        DescriptionStage chargeAt(@NotNull String chargeAt);
    }

    public interface DescriptionStage {
        /**
         * <p>What to show the buyer for this future charge.</p>
         */
        IntervalDaysStage description(@NotNull String description);
    }

    public interface IntervalDaysStage {
        /**
         * <p>Days between charges.</p>
         */
        _FinalStage intervalDays(int intervalDays);
    }

    public interface _FinalStage {
        CheckoutSessionBreakdownUpcomingRecurring build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>What each charge takes once the applied promo stops covering renewals — the steady price the subscription returns to, priced like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires (a <code>once</code> code covering the first real charge, or a <code>repeating</code> window the schedule outlives — <code>promo.duration</code> says which). Absent when no promo discounts the next charge, and for a <code>forever</code> code, whose discount never ends.</p>
         */
        _FinalStage postPromoAmount(Optional<Money> postPromoAmount);

        _FinalStage postPromoAmount(Money postPromoAmount);

        /**
         * <p>Free-trial days before the first charge. Absent when billing starts with this purchase — and on a membership transfer, which inherits the origin's clock and never re-trials.</p>
         */
        _FinalStage trialDays(Optional<Integer> trialDays);

        _FinalStage trialDays(Integer trialDays);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AmountStage, ChargeAtStage, DescriptionStage, IntervalDaysStage, _FinalStage {
        private Money amount;

        private String chargeAt;

        private String description;

        private int intervalDays;

        private Optional<Integer> trialDays = Optional.empty();

        private Optional<Money> postPromoAmount = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionBreakdownUpcomingRecurring other) {
            amount(other.getAmount());
            chargeAt(other.getChargeAt());
            description(other.getDescription());
            intervalDays(other.getIntervalDays());
            postPromoAmount(other.getPostPromoAmount());
            trialDays(other.getTrialDays());
            return this;
        }

        /**
         * <p>What the NEXT charge takes, priced as its receipt will be: the renewal for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge — a <code>forever</code> code discounts every cycle, a <code>once</code> or <code>repeating</code> one only the paying charges it has left. Stated before tax.</p>
         * <p>What the NEXT charge takes, priced as its receipt will be: the renewal for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge — a <code>forever</code> code discounts every cycle, a <code>once</code> or <code>repeating</code> one only the paying charges it has left. Stated before tax.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public ChargeAtStage amount(@NotNull Money amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>When the NEXT charge happens — a trial's end, a transfer's inherited period end, or the next cycle — as an ISO 8601 timestamp.</p>
         * <p>When the NEXT charge happens — a trial's end, a transfer's inherited period end, or the next cycle — as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("charge_at")
        public DescriptionStage chargeAt(@NotNull String chargeAt) {
            this.chargeAt = Objects.requireNonNull(chargeAt, "chargeAt must not be null");
            return this;
        }

        /**
         * <p>What to show the buyer for this future charge.</p>
         * <p>What to show the buyer for this future charge.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("description")
        public IntervalDaysStage description(@NotNull String description) {
            this.description = Objects.requireNonNull(description, "description must not be null");
            return this;
        }

        /**
         * <p>Days between charges.</p>
         * <p>Days between charges.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("interval_days")
        public _FinalStage intervalDays(int intervalDays) {
            this.intervalDays = intervalDays;
            return this;
        }

        /**
         * <p>Free-trial days before the first charge. Absent when billing starts with this purchase — and on a membership transfer, which inherits the origin's clock and never re-trials.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage trialDays(Integer trialDays) {
            this.trialDays = Optional.ofNullable(trialDays);
            return this;
        }

        /**
         * <p>Free-trial days before the first charge. Absent when billing starts with this purchase — and on a membership transfer, which inherits the origin's clock and never re-trials.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "trial_days", nulls = Nulls.SKIP)
        public _FinalStage trialDays(Optional<Integer> trialDays) {
            this.trialDays = trialDays;
            return this;
        }

        /**
         * <p>What each charge takes once the applied promo stops covering renewals — the steady price the subscription returns to, priced like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires (a <code>once</code> code covering the first real charge, or a <code>repeating</code> window the schedule outlives — <code>promo.duration</code> says which). Absent when no promo discounts the next charge, and for a <code>forever</code> code, whose discount never ends.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage postPromoAmount(Money postPromoAmount) {
            this.postPromoAmount = Optional.ofNullable(postPromoAmount);
            return this;
        }

        /**
         * <p>What each charge takes once the applied promo stops covering renewals — the steady price the subscription returns to, priced like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires (a <code>once</code> code covering the first real charge, or a <code>repeating</code> window the schedule outlives — <code>promo.duration</code> says which). Absent when no promo discounts the next charge, and for a <code>forever</code> code, whose discount never ends.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "post_promo_amount", nulls = Nulls.SKIP)
        public _FinalStage postPromoAmount(Optional<Money> postPromoAmount) {
            this.postPromoAmount = postPromoAmount;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionBreakdownUpcomingRecurring build() {
            return new CheckoutSessionBreakdownUpcomingRecurring(
                    amount, chargeAt, description, intervalDays, postPromoAmount, trialDays, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
