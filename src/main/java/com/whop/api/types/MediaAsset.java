/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = MediaAsset.Builder.class)
public final class MediaAsset {
    private final Optional<Double> amountCharged;

    private final Optional<String> completedAt;

    private final String createdAt;

    private final String currency;

    private final Optional<String> errorMessage;

    private final Optional<MediaAssetFile> file;

    private final MediaAssetGeneration generation;

    private final String id;

    private final MediaAssetMediaType mediaType;

    private final MediaAssetSource source;

    private final MediaAssetStatus status;

    private final Map<String, Object> additionalProperties;

    private MediaAsset(
            Optional<Double> amountCharged,
            Optional<String> completedAt,
            String createdAt,
            String currency,
            Optional<String> errorMessage,
            Optional<MediaAssetFile> file,
            MediaAssetGeneration generation,
            String id,
            MediaAssetMediaType mediaType,
            MediaAssetSource source,
            MediaAssetStatus status,
            Map<String, Object> additionalProperties) {
        this.amountCharged = amountCharged;
        this.completedAt = completedAt;
        this.createdAt = createdAt;
        this.currency = currency;
        this.errorMessage = errorMessage;
        this.file = file;
        this.generation = generation;
        this.id = id;
        this.mediaType = mediaType;
        this.source = source;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return USD amount charged to the account's balance for this generation. <code>null</code> if the generation wasn't billed.
     */
    @JsonIgnore
    public Optional<Double> getAmountCharged() {
        if (amountCharged == null) {
            return Optional.empty();
        }
        return amountCharged;
    }

    /**
     * @return ISO 8601 timestamp when the asset reached a terminal state. <code>null</code> while <code>processing</code>.
     */
    @JsonIgnore
    public Optional<String> getCompletedAt() {
        if (completedAt == null) {
            return Optional.empty();
        }
        return completedAt;
    }

    /**
     * @return ISO 8601 timestamp when the generation was requested.
     */
    @JsonProperty("created_at")
    public String getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Currency of <code>amount_charged</code>. Always <code>usd</code>.
     */
    @JsonProperty("currency")
    public String getCurrency() {
        return currency;
    }

    /**
     * @return Why generation failed. <code>null</code> unless status is <code>failed</code>.
     */
    @JsonIgnore
    public Optional<String> getErrorMessage() {
        if (errorMessage == null) {
            return Optional.empty();
        }
        return errorMessage;
    }

    /**
     * @return The produced file, usable anywhere attachments are accepted. <code>null</code> until the asset is <code>ready</code>.
     */
    @JsonIgnore
    public Optional<MediaAssetFile> getFile() {
        if (file == null) {
            return Optional.empty();
        }
        return file;
    }

    /**
     * @return The inputs the asset was generated from.
     */
    @JsonProperty("generation")
    public MediaAssetGeneration getGeneration() {
        return generation;
    }

    /**
     * @return Media asset ID, prefixed <code>media_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The kind of media this asset holds.
     */
    @JsonProperty("media_type")
    public MediaAssetMediaType getMediaType() {
        return mediaType;
    }

    /**
     * @return How the asset was created. Always <code>generated</code>.
     */
    @JsonProperty("source")
    public MediaAssetSource getSource() {
        return source;
    }

    /**
     * @return Lifecycle state: <code>processing</code> while generation runs, <code>ready</code> when the file is available, <code>failed</code> when generation failed and the charge was refunded.
     */
    @JsonProperty("status")
    public MediaAssetStatus getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("amount_charged")
    private Optional<Double> _getAmountCharged() {
        return amountCharged;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("completed_at")
    private Optional<String> _getCompletedAt() {
        return completedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("error_message")
    private Optional<String> _getErrorMessage() {
        return errorMessage;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("file")
    private Optional<MediaAssetFile> _getFile() {
        return file;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof MediaAsset && equalTo((MediaAsset) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(MediaAsset other) {
        return amountCharged.equals(other.amountCharged)
                && completedAt.equals(other.completedAt)
                && createdAt.equals(other.createdAt)
                && currency.equals(other.currency)
                && errorMessage.equals(other.errorMessage)
                && file.equals(other.file)
                && generation.equals(other.generation)
                && id.equals(other.id)
                && mediaType.equals(other.mediaType)
                && source.equals(other.source)
                && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amountCharged,
                this.completedAt,
                this.createdAt,
                this.currency,
                this.errorMessage,
                this.file,
                this.generation,
                this.id,
                this.mediaType,
                this.source,
                this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CreatedAtStage builder() {
        return new Builder();
    }

    public interface CreatedAtStage {
        /**
         * <p>ISO 8601 timestamp when the generation was requested.</p>
         */
        CurrencyStage createdAt(@NotNull String createdAt);

        Builder from(MediaAsset other);
    }

    public interface CurrencyStage {
        /**
         * <p>Currency of <code>amount_charged</code>. Always <code>usd</code>.</p>
         */
        GenerationStage currency(@NotNull String currency);
    }

    public interface GenerationStage {
        /**
         * <p>The inputs the asset was generated from.</p>
         */
        IdStage generation(@NotNull MediaAssetGeneration generation);
    }

    public interface IdStage {
        /**
         * <p>Media asset ID, prefixed <code>media_</code>.</p>
         */
        MediaTypeStage id(@NotNull String id);
    }

    public interface MediaTypeStage {
        /**
         * <p>The kind of media this asset holds.</p>
         */
        SourceStage mediaType(@NotNull MediaAssetMediaType mediaType);
    }

    public interface SourceStage {
        /**
         * <p>How the asset was created. Always <code>generated</code>.</p>
         */
        StatusStage source(@NotNull MediaAssetSource source);
    }

    public interface StatusStage {
        /**
         * <p>Lifecycle state: <code>processing</code> while generation runs, <code>ready</code> when the file is available, <code>failed</code> when generation failed and the charge was refunded.</p>
         */
        _FinalStage status(@NotNull MediaAssetStatus status);
    }

    public interface _FinalStage {
        MediaAsset build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>USD amount charged to the account's balance for this generation. <code>null</code> if the generation wasn't billed.</p>
         */
        _FinalStage amountCharged(Optional<Double> amountCharged);

        _FinalStage amountCharged(Double amountCharged);

        _FinalStage amountCharged(Nullable<Double> amountCharged);

        /**
         * <p>ISO 8601 timestamp when the asset reached a terminal state. <code>null</code> while <code>processing</code>.</p>
         */
        _FinalStage completedAt(Optional<String> completedAt);

        _FinalStage completedAt(String completedAt);

        _FinalStage completedAt(Nullable<String> completedAt);

        /**
         * <p>Why generation failed. <code>null</code> unless status is <code>failed</code>.</p>
         */
        _FinalStage errorMessage(Optional<String> errorMessage);

        _FinalStage errorMessage(String errorMessage);

        _FinalStage errorMessage(Nullable<String> errorMessage);

        /**
         * <p>The produced file, usable anywhere attachments are accepted. <code>null</code> until the asset is <code>ready</code>.</p>
         */
        _FinalStage file(Optional<MediaAssetFile> file);

        _FinalStage file(MediaAssetFile file);

        _FinalStage file(Nullable<MediaAssetFile> file);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements CreatedAtStage,
                    CurrencyStage,
                    GenerationStage,
                    IdStage,
                    MediaTypeStage,
                    SourceStage,
                    StatusStage,
                    _FinalStage {
        private String createdAt;

        private String currency;

        private MediaAssetGeneration generation;

        private String id;

        private MediaAssetMediaType mediaType;

        private MediaAssetSource source;

        private MediaAssetStatus status;

        private Optional<MediaAssetFile> file = Optional.empty();

        private Optional<String> errorMessage = Optional.empty();

        private Optional<String> completedAt = Optional.empty();

        private Optional<Double> amountCharged = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(MediaAsset other) {
            amountCharged(other.getAmountCharged());
            completedAt(other.getCompletedAt());
            createdAt(other.getCreatedAt());
            currency(other.getCurrency());
            errorMessage(other.getErrorMessage());
            file(other.getFile());
            generation(other.getGeneration());
            id(other.getId());
            mediaType(other.getMediaType());
            source(other.getSource());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>ISO 8601 timestamp when the generation was requested.</p>
         * <p>ISO 8601 timestamp when the generation was requested.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public CurrencyStage createdAt(@NotNull String createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Currency of <code>amount_charged</code>. Always <code>usd</code>.</p>
         * <p>Currency of <code>amount_charged</code>. Always <code>usd</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public GenerationStage currency(@NotNull String currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>The inputs the asset was generated from.</p>
         * <p>The inputs the asset was generated from.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("generation")
        public IdStage generation(@NotNull MediaAssetGeneration generation) {
            this.generation = Objects.requireNonNull(generation, "generation must not be null");
            return this;
        }

        /**
         * <p>Media asset ID, prefixed <code>media_</code>.</p>
         * <p>Media asset ID, prefixed <code>media_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public MediaTypeStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The kind of media this asset holds.</p>
         * <p>The kind of media this asset holds.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("media_type")
        public SourceStage mediaType(@NotNull MediaAssetMediaType mediaType) {
            this.mediaType = Objects.requireNonNull(mediaType, "mediaType must not be null");
            return this;
        }

        /**
         * <p>How the asset was created. Always <code>generated</code>.</p>
         * <p>How the asset was created. Always <code>generated</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("source")
        public StatusStage source(@NotNull MediaAssetSource source) {
            this.source = Objects.requireNonNull(source, "source must not be null");
            return this;
        }

        /**
         * <p>Lifecycle state: <code>processing</code> while generation runs, <code>ready</code> when the file is available, <code>failed</code> when generation failed and the charge was refunded.</p>
         * <p>Lifecycle state: <code>processing</code> while generation runs, <code>ready</code> when the file is available, <code>failed</code> when generation failed and the charge was refunded.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull MediaAssetStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>The produced file, usable anywhere attachments are accepted. <code>null</code> until the asset is <code>ready</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage file(Nullable<MediaAssetFile> file) {
            if (file.isNull()) {
                this.file = null;
            } else if (file.isEmpty()) {
                this.file = Optional.empty();
            } else {
                this.file = Optional.of(file.get());
            }
            return this;
        }

        /**
         * <p>The produced file, usable anywhere attachments are accepted. <code>null</code> until the asset is <code>ready</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage file(MediaAssetFile file) {
            this.file = Optional.ofNullable(file);
            return this;
        }

        /**
         * <p>The produced file, usable anywhere attachments are accepted. <code>null</code> until the asset is <code>ready</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "file", nulls = Nulls.SKIP)
        public _FinalStage file(Optional<MediaAssetFile> file) {
            this.file = file;
            return this;
        }

        /**
         * <p>Why generation failed. <code>null</code> unless status is <code>failed</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage errorMessage(Nullable<String> errorMessage) {
            if (errorMessage.isNull()) {
                this.errorMessage = null;
            } else if (errorMessage.isEmpty()) {
                this.errorMessage = Optional.empty();
            } else {
                this.errorMessage = Optional.of(errorMessage.get());
            }
            return this;
        }

        /**
         * <p>Why generation failed. <code>null</code> unless status is <code>failed</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage errorMessage(String errorMessage) {
            this.errorMessage = Optional.ofNullable(errorMessage);
            return this;
        }

        /**
         * <p>Why generation failed. <code>null</code> unless status is <code>failed</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "error_message", nulls = Nulls.SKIP)
        public _FinalStage errorMessage(Optional<String> errorMessage) {
            this.errorMessage = errorMessage;
            return this;
        }

        /**
         * <p>ISO 8601 timestamp when the asset reached a terminal state. <code>null</code> while <code>processing</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage completedAt(Nullable<String> completedAt) {
            if (completedAt.isNull()) {
                this.completedAt = null;
            } else if (completedAt.isEmpty()) {
                this.completedAt = Optional.empty();
            } else {
                this.completedAt = Optional.of(completedAt.get());
            }
            return this;
        }

        /**
         * <p>ISO 8601 timestamp when the asset reached a terminal state. <code>null</code> while <code>processing</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage completedAt(String completedAt) {
            this.completedAt = Optional.ofNullable(completedAt);
            return this;
        }

        /**
         * <p>ISO 8601 timestamp when the asset reached a terminal state. <code>null</code> while <code>processing</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "completed_at", nulls = Nulls.SKIP)
        public _FinalStage completedAt(Optional<String> completedAt) {
            this.completedAt = completedAt;
            return this;
        }

        /**
         * <p>USD amount charged to the account's balance for this generation. <code>null</code> if the generation wasn't billed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountCharged(Nullable<Double> amountCharged) {
            if (amountCharged.isNull()) {
                this.amountCharged = null;
            } else if (amountCharged.isEmpty()) {
                this.amountCharged = Optional.empty();
            } else {
                this.amountCharged = Optional.of(amountCharged.get());
            }
            return this;
        }

        /**
         * <p>USD amount charged to the account's balance for this generation. <code>null</code> if the generation wasn't billed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountCharged(Double amountCharged) {
            this.amountCharged = Optional.ofNullable(amountCharged);
            return this;
        }

        /**
         * <p>USD amount charged to the account's balance for this generation. <code>null</code> if the generation wasn't billed.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount_charged", nulls = Nulls.SKIP)
        public _FinalStage amountCharged(Optional<Double> amountCharged) {
            this.amountCharged = amountCharged;
            return this;
        }

        @java.lang.Override
        public MediaAsset build() {
            return new MediaAsset(
                    amountCharged,
                    completedAt,
                    createdAt,
                    currency,
                    errorMessage,
                    file,
                    generation,
                    id,
                    mediaType,
                    source,
                    status,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
