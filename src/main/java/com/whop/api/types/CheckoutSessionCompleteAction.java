/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionCompleteAction.Builder.class)
public final class CheckoutSessionCompleteAction {
    private final boolean blocking;

    private final String clientSecret;

    private final CheckoutSessionCompleteActionKind kind;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionCompleteAction(
            boolean blocking,
            String clientSecret,
            CheckoutSessionCompleteActionKind kind,
            Map<String, Object> additionalProperties) {
        this.blocking = blocking;
        this.clientSecret = clientSecret;
        this.kind = kind;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.
     */
    @JsonProperty("blocking")
    public boolean getBlocking() {
        return blocking;
    }

    /**
     * @return The ATTEMPT's own credential — the payment's or the setup's, never the session's. Hand it to the payments element to finish the remaining ceremony.
     */
    @JsonProperty("client_secret")
    public String getClientSecret() {
        return clientSecret;
    }

    /**
     * @return What the ceremony finishes: <code>payment</code> for a charge with a step left, <code>setup</code> for a payment method still being saved — a paid waitlist vaults the card without charging it. Informational: the executor is the same either way, so run the secret regardless of a <code>kind</code> you do not recognize.
     */
    @JsonProperty("kind")
    public CheckoutSessionCompleteActionKind getKind() {
        return kind;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionCompleteAction && equalTo((CheckoutSessionCompleteAction) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionCompleteAction other) {
        return blocking == other.blocking && clientSecret.equals(other.clientSecret) && kind.equals(other.kind);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.blocking, this.clientSecret, this.kind);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BlockingStage builder() {
        return new Builder();
    }

    public interface BlockingStage {
        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         */
        ClientSecretStage blocking(boolean blocking);

        Builder from(CheckoutSessionCompleteAction other);
    }

    public interface ClientSecretStage {
        /**
         * <p>The ATTEMPT's own credential — the payment's or the setup's, never the session's. Hand it to the payments element to finish the remaining ceremony.</p>
         */
        KindStage clientSecret(@NotNull String clientSecret);
    }

    public interface KindStage {
        /**
         * <p>What the ceremony finishes: <code>payment</code> for a charge with a step left, <code>setup</code> for a payment method still being saved — a paid waitlist vaults the card without charging it. Informational: the executor is the same either way, so run the secret regardless of a <code>kind</code> you do not recognize.</p>
         */
        _FinalStage kind(@NotNull CheckoutSessionCompleteActionKind kind);
    }

    public interface _FinalStage {
        CheckoutSessionCompleteAction build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BlockingStage, ClientSecretStage, KindStage, _FinalStage {
        private boolean blocking;

        private String clientSecret;

        private CheckoutSessionCompleteActionKind kind;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionCompleteAction other) {
            blocking(other.getBlocking());
            clientSecret(other.getClientSecret());
            kind(other.getKind());
            return this;
        }

        /**
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * <p>Whether the sale depends on this. <code>true</code> stops the purchase until it is done — confirm refuses regardless of whether you ran it, so skipping it only costs the buyer an unexplained refusal. <code>false</code> is advisory: the purchase is already made and it stands whether or not the buyer ever does this, so the buyer may close the tab.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("blocking")
        public ClientSecretStage blocking(boolean blocking) {
            this.blocking = blocking;
            return this;
        }

        /**
         * <p>The ATTEMPT's own credential — the payment's or the setup's, never the session's. Hand it to the payments element to finish the remaining ceremony.</p>
         * <p>The ATTEMPT's own credential — the payment's or the setup's, never the session's. Hand it to the payments element to finish the remaining ceremony.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("client_secret")
        public KindStage clientSecret(@NotNull String clientSecret) {
            this.clientSecret = Objects.requireNonNull(clientSecret, "clientSecret must not be null");
            return this;
        }

        /**
         * <p>What the ceremony finishes: <code>payment</code> for a charge with a step left, <code>setup</code> for a payment method still being saved — a paid waitlist vaults the card without charging it. Informational: the executor is the same either way, so run the secret regardless of a <code>kind</code> you do not recognize.</p>
         * <p>What the ceremony finishes: <code>payment</code> for a charge with a step left, <code>setup</code> for a payment method still being saved — a paid waitlist vaults the card without charging it. Informational: the executor is the same either way, so run the secret regardless of a <code>kind</code> you do not recognize.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("kind")
        public _FinalStage kind(@NotNull CheckoutSessionCompleteActionKind kind) {
            this.kind = Objects.requireNonNull(kind, "kind must not be null");
            return this;
        }

        @java.lang.Override
        public CheckoutSessionCompleteAction build() {
            return new CheckoutSessionCompleteAction(blocking, clientSecret, kind, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
