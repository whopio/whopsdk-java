/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AudienceStatus {
    public static final AudienceStatus FAILED = new AudienceStatus(Value.FAILED, "failed");

    public static final AudienceStatus PARTIAL = new AudienceStatus(Value.PARTIAL, "partial");

    public static final AudienceStatus PENDING = new AudienceStatus(Value.PENDING, "pending");

    public static final AudienceStatus PROCESSING = new AudienceStatus(Value.PROCESSING, "processing");

    public static final AudienceStatus READY = new AudienceStatus(Value.READY, "ready");

    public static final AudienceStatus SYNCING = new AudienceStatus(Value.SYNCING, "syncing");

    private final Value value;

    private final String string;

    AudienceStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof AudienceStatus && this.string.equals(((AudienceStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FAILED:
                return visitor.visitFailed();
            case PARTIAL:
                return visitor.visitPartial();
            case PENDING:
                return visitor.visitPending();
            case PROCESSING:
                return visitor.visitProcessing();
            case READY:
                return visitor.visitReady();
            case SYNCING:
                return visitor.visitSyncing();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AudienceStatus valueOf(String value) {
        switch (value) {
            case "failed":
                return FAILED;
            case "partial":
                return PARTIAL;
            case "pending":
                return PENDING;
            case "processing":
                return PROCESSING;
            case "ready":
                return READY;
            case "syncing":
                return SYNCING;
            default:
                return new AudienceStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        PROCESSING,

        SYNCING,

        READY,

        PARTIAL,

        FAILED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitProcessing();

        T visitSyncing();

        T visitReady();

        T visitPartial();

        T visitFailed();

        T visitUnknown(String unknownType);
    }
}
