/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = SocialAccountParent.Builder.class)
public final class SocialAccountParent {
    private final Optional<String> externalId;

    private final String id;

    private final Optional<String> name;

    private final SocialAccountParentPlatform platform;

    private final Optional<String> profilePictureUrl;

    private final Optional<String> username;

    private final boolean verified;

    private final Map<String, Object> additionalProperties;

    private SocialAccountParent(
            Optional<String> externalId,
            String id,
            Optional<String> name,
            SocialAccountParentPlatform platform,
            Optional<String> profilePictureUrl,
            Optional<String> username,
            boolean verified,
            Map<String, Object> additionalProperties) {
        this.externalId = externalId;
        this.id = id;
        this.name = name;
        this.platform = platform;
        this.profilePictureUrl = profilePictureUrl;
        this.username = username;
        this.verified = verified;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The platform-specific ID for the parent social account.
     */
    @JsonIgnore
    public Optional<String> getExternalId() {
        if (externalId == null) {
            return Optional.empty();
        }
        return externalId;
    }

    /**
     * @return Social account ID, prefixed <code>sacc_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return The display name of the parent social account on the platform.
     */
    @JsonIgnore
    public Optional<String> getName() {
        if (name == null) {
            return Optional.empty();
        }
        return name;
    }

    /**
     * @return The platform the parent social account exists on.
     */
    @JsonProperty("platform")
    public SocialAccountParentPlatform getPlatform() {
        return platform;
    }

    /**
     * @return The URL where the profile picture of the parent social account can be accessed.
     */
    @JsonIgnore
    public Optional<String> getProfilePictureUrl() {
        if (profilePictureUrl == null) {
            return Optional.empty();
        }
        return profilePictureUrl;
    }

    /**
     * @return The username of the parent social account on the platform.
     */
    @JsonIgnore
    public Optional<String> getUsername() {
        if (username == null) {
            return Optional.empty();
        }
        return username;
    }

    /**
     * @return Whether the parent social account is verified on the platform.
     */
    @JsonProperty("verified")
    public boolean getVerified() {
        return verified;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("external_id")
    private Optional<String> _getExternalId() {
        return externalId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("name")
    private Optional<String> _getName() {
        return name;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("profile_picture_url")
    private Optional<String> _getProfilePictureUrl() {
        return profilePictureUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("username")
    private Optional<String> _getUsername() {
        return username;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SocialAccountParent && equalTo((SocialAccountParent) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(SocialAccountParent other) {
        return externalId.equals(other.externalId)
                && id.equals(other.id)
                && name.equals(other.name)
                && platform.equals(other.platform)
                && profilePictureUrl.equals(other.profilePictureUrl)
                && username.equals(other.username)
                && verified == other.verified;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.externalId,
                this.id,
                this.name,
                this.platform,
                this.profilePictureUrl,
                this.username,
                this.verified);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>Social account ID, prefixed <code>sacc_</code>.</p>
         */
        PlatformStage id(@NotNull String id);

        Builder from(SocialAccountParent other);
    }

    public interface PlatformStage {
        /**
         * <p>The platform the parent social account exists on.</p>
         */
        VerifiedStage platform(@NotNull SocialAccountParentPlatform platform);
    }

    public interface VerifiedStage {
        /**
         * <p>Whether the parent social account is verified on the platform.</p>
         */
        _FinalStage verified(boolean verified);
    }

    public interface _FinalStage {
        SocialAccountParent build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The platform-specific ID for the parent social account.</p>
         */
        _FinalStage externalId(Optional<String> externalId);

        _FinalStage externalId(String externalId);

        _FinalStage externalId(Nullable<String> externalId);

        /**
         * <p>The display name of the parent social account on the platform.</p>
         */
        _FinalStage name(Optional<String> name);

        _FinalStage name(String name);

        _FinalStage name(Nullable<String> name);

        /**
         * <p>The URL where the profile picture of the parent social account can be accessed.</p>
         */
        _FinalStage profilePictureUrl(Optional<String> profilePictureUrl);

        _FinalStage profilePictureUrl(String profilePictureUrl);

        _FinalStage profilePictureUrl(Nullable<String> profilePictureUrl);

        /**
         * <p>The username of the parent social account on the platform.</p>
         */
        _FinalStage username(Optional<String> username);

        _FinalStage username(String username);

        _FinalStage username(Nullable<String> username);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, PlatformStage, VerifiedStage, _FinalStage {
        private String id;

        private SocialAccountParentPlatform platform;

        private boolean verified;

        private Optional<String> username = Optional.empty();

        private Optional<String> profilePictureUrl = Optional.empty();

        private Optional<String> name = Optional.empty();

        private Optional<String> externalId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(SocialAccountParent other) {
            externalId(other.getExternalId());
            id(other.getId());
            name(other.getName());
            platform(other.getPlatform());
            profilePictureUrl(other.getProfilePictureUrl());
            username(other.getUsername());
            verified(other.getVerified());
            return this;
        }

        /**
         * <p>Social account ID, prefixed <code>sacc_</code>.</p>
         * <p>Social account ID, prefixed <code>sacc_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public PlatformStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The platform the parent social account exists on.</p>
         * <p>The platform the parent social account exists on.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("platform")
        public VerifiedStage platform(@NotNull SocialAccountParentPlatform platform) {
            this.platform = Objects.requireNonNull(platform, "platform must not be null");
            return this;
        }

        /**
         * <p>Whether the parent social account is verified on the platform.</p>
         * <p>Whether the parent social account is verified on the platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("verified")
        public _FinalStage verified(boolean verified) {
            this.verified = verified;
            return this;
        }

        /**
         * <p>The username of the parent social account on the platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage username(Nullable<String> username) {
            if (username.isNull()) {
                this.username = null;
            } else if (username.isEmpty()) {
                this.username = Optional.empty();
            } else {
                this.username = Optional.of(username.get());
            }
            return this;
        }

        /**
         * <p>The username of the parent social account on the platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage username(String username) {
            this.username = Optional.ofNullable(username);
            return this;
        }

        /**
         * <p>The username of the parent social account on the platform.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "username", nulls = Nulls.SKIP)
        public _FinalStage username(Optional<String> username) {
            this.username = username;
            return this;
        }

        /**
         * <p>The URL where the profile picture of the parent social account can be accessed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage profilePictureUrl(Nullable<String> profilePictureUrl) {
            if (profilePictureUrl.isNull()) {
                this.profilePictureUrl = null;
            } else if (profilePictureUrl.isEmpty()) {
                this.profilePictureUrl = Optional.empty();
            } else {
                this.profilePictureUrl = Optional.of(profilePictureUrl.get());
            }
            return this;
        }

        /**
         * <p>The URL where the profile picture of the parent social account can be accessed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage profilePictureUrl(String profilePictureUrl) {
            this.profilePictureUrl = Optional.ofNullable(profilePictureUrl);
            return this;
        }

        /**
         * <p>The URL where the profile picture of the parent social account can be accessed.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "profile_picture_url", nulls = Nulls.SKIP)
        public _FinalStage profilePictureUrl(Optional<String> profilePictureUrl) {
            this.profilePictureUrl = profilePictureUrl;
            return this;
        }

        /**
         * <p>The display name of the parent social account on the platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage name(Nullable<String> name) {
            if (name.isNull()) {
                this.name = null;
            } else if (name.isEmpty()) {
                this.name = Optional.empty();
            } else {
                this.name = Optional.of(name.get());
            }
            return this;
        }

        /**
         * <p>The display name of the parent social account on the platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage name(String name) {
            this.name = Optional.ofNullable(name);
            return this;
        }

        /**
         * <p>The display name of the parent social account on the platform.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "name", nulls = Nulls.SKIP)
        public _FinalStage name(Optional<String> name) {
            this.name = name;
            return this;
        }

        /**
         * <p>The platform-specific ID for the parent social account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage externalId(Nullable<String> externalId) {
            if (externalId.isNull()) {
                this.externalId = null;
            } else if (externalId.isEmpty()) {
                this.externalId = Optional.empty();
            } else {
                this.externalId = Optional.of(externalId.get());
            }
            return this;
        }

        /**
         * <p>The platform-specific ID for the parent social account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage externalId(String externalId) {
            this.externalId = Optional.ofNullable(externalId);
            return this;
        }

        /**
         * <p>The platform-specific ID for the parent social account.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "external_id", nulls = Nulls.SKIP)
        public _FinalStage externalId(Optional<String> externalId) {
            this.externalId = externalId;
            return this;
        }

        @java.lang.Override
        public SocialAccountParent build() {
            return new SocialAccountParent(
                    externalId, id, name, platform, profilePictureUrl, username, verified, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
