/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = UserBalanceToken.Builder.class)
public final class UserBalanceToken {
    private final String balance;

    private final AccountBalanceBreakdown breakdown;

    private final Optional<String> iconUrl;

    private final Optional<String> name;

    private final Optional<Double> priceUsd;

    private final String symbol;

    private final double valueUsd;

    private final Map<String, Object> additionalProperties;

    private UserBalanceToken(
            String balance,
            AccountBalanceBreakdown breakdown,
            Optional<String> iconUrl,
            Optional<String> name,
            Optional<Double> priceUsd,
            String symbol,
            double valueUsd,
            Map<String, Object> additionalProperties) {
        this.balance = balance;
        this.breakdown = breakdown;
        this.iconUrl = iconUrl;
        this.name = name;
        this.priceUsd = priceUsd;
        this.symbol = symbol;
        this.valueUsd = valueUsd;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Amount held in native token units, as a decimal string.
     */
    @JsonProperty("balance")
    public String getBalance() {
        return balance;
    }

    /**
     * @return Balance split into available, pending, in-transit, and reserve amounts, as native-unit decimal strings. Transfers between the user's own wallet and card are reported in <code>in_transit</code> until they arrive.
     */
    @JsonProperty("breakdown")
    public AccountBalanceBreakdown getBreakdown() {
        return breakdown;
    }

    /**
     * @return Token icon URL.
     */
    @JsonIgnore
    public Optional<String> getIconUrl() {
        if (iconUrl == null) {
            return Optional.empty();
        }
        return iconUrl;
    }

    /**
     * @return The token's display name.
     */
    @JsonIgnore
    public Optional<String> getName() {
        if (name == null) {
            return Optional.empty();
        }
        return name;
    }

    /**
     * @return USD price per token, or <code>null</code> when unknown.
     */
    @JsonIgnore
    public Optional<Double> getPriceUsd() {
        if (priceUsd == null) {
            return Optional.empty();
        }
        return priceUsd;
    }

    /**
     * @return Token display symbol, such as <code>USDT</code>, <code>XAUT</code>, or <code>cbBTC</code>.
     */
    @JsonProperty("symbol")
    public String getSymbol() {
        return symbol;
    }

    /**
     * @return Holding USD value.
     */
    @JsonProperty("value_usd")
    public double getValueUsd() {
        return valueUsd;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("icon_url")
    private Optional<String> _getIconUrl() {
        return iconUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("name")
    private Optional<String> _getName() {
        return name;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("price_usd")
    private Optional<Double> _getPriceUsd() {
        return priceUsd;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UserBalanceToken && equalTo((UserBalanceToken) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(UserBalanceToken other) {
        return balance.equals(other.balance)
                && breakdown.equals(other.breakdown)
                && iconUrl.equals(other.iconUrl)
                && name.equals(other.name)
                && priceUsd.equals(other.priceUsd)
                && symbol.equals(other.symbol)
                && valueUsd == other.valueUsd;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.balance, this.breakdown, this.iconUrl, this.name, this.priceUsd, this.symbol, this.valueUsd);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BalanceStage builder() {
        return new Builder();
    }

    public interface BalanceStage {
        /**
         * <p>Amount held in native token units, as a decimal string.</p>
         */
        BreakdownStage balance(@NotNull String balance);

        Builder from(UserBalanceToken other);
    }

    public interface BreakdownStage {
        /**
         * <p>Balance split into available, pending, in-transit, and reserve amounts, as native-unit decimal strings. Transfers between the user's own wallet and card are reported in <code>in_transit</code> until they arrive.</p>
         */
        SymbolStage breakdown(@NotNull AccountBalanceBreakdown breakdown);
    }

    public interface SymbolStage {
        /**
         * <p>Token display symbol, such as <code>USDT</code>, <code>XAUT</code>, or <code>cbBTC</code>.</p>
         */
        ValueUsdStage symbol(@NotNull String symbol);
    }

    public interface ValueUsdStage {
        /**
         * <p>Holding USD value.</p>
         */
        _FinalStage valueUsd(double valueUsd);
    }

    public interface _FinalStage {
        UserBalanceToken build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Token icon URL.</p>
         */
        _FinalStage iconUrl(Optional<String> iconUrl);

        _FinalStage iconUrl(String iconUrl);

        _FinalStage iconUrl(Nullable<String> iconUrl);

        /**
         * <p>The token's display name.</p>
         */
        _FinalStage name(Optional<String> name);

        _FinalStage name(String name);

        _FinalStage name(Nullable<String> name);

        /**
         * <p>USD price per token, or <code>null</code> when unknown.</p>
         */
        _FinalStage priceUsd(Optional<Double> priceUsd);

        _FinalStage priceUsd(Double priceUsd);

        _FinalStage priceUsd(Nullable<Double> priceUsd);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BalanceStage, BreakdownStage, SymbolStage, ValueUsdStage, _FinalStage {
        private String balance;

        private AccountBalanceBreakdown breakdown;

        private String symbol;

        private double valueUsd;

        private Optional<Double> priceUsd = Optional.empty();

        private Optional<String> name = Optional.empty();

        private Optional<String> iconUrl = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(UserBalanceToken other) {
            balance(other.getBalance());
            breakdown(other.getBreakdown());
            iconUrl(other.getIconUrl());
            name(other.getName());
            priceUsd(other.getPriceUsd());
            symbol(other.getSymbol());
            valueUsd(other.getValueUsd());
            return this;
        }

        /**
         * <p>Amount held in native token units, as a decimal string.</p>
         * <p>Amount held in native token units, as a decimal string.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("balance")
        public BreakdownStage balance(@NotNull String balance) {
            this.balance = Objects.requireNonNull(balance, "balance must not be null");
            return this;
        }

        /**
         * <p>Balance split into available, pending, in-transit, and reserve amounts, as native-unit decimal strings. Transfers between the user's own wallet and card are reported in <code>in_transit</code> until they arrive.</p>
         * <p>Balance split into available, pending, in-transit, and reserve amounts, as native-unit decimal strings. Transfers between the user's own wallet and card are reported in <code>in_transit</code> until they arrive.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("breakdown")
        public SymbolStage breakdown(@NotNull AccountBalanceBreakdown breakdown) {
            this.breakdown = Objects.requireNonNull(breakdown, "breakdown must not be null");
            return this;
        }

        /**
         * <p>Token display symbol, such as <code>USDT</code>, <code>XAUT</code>, or <code>cbBTC</code>.</p>
         * <p>Token display symbol, such as <code>USDT</code>, <code>XAUT</code>, or <code>cbBTC</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("symbol")
        public ValueUsdStage symbol(@NotNull String symbol) {
            this.symbol = Objects.requireNonNull(symbol, "symbol must not be null");
            return this;
        }

        /**
         * <p>Holding USD value.</p>
         * <p>Holding USD value.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("value_usd")
        public _FinalStage valueUsd(double valueUsd) {
            this.valueUsd = valueUsd;
            return this;
        }

        /**
         * <p>USD price per token, or <code>null</code> when unknown.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage priceUsd(Nullable<Double> priceUsd) {
            if (priceUsd.isNull()) {
                this.priceUsd = null;
            } else if (priceUsd.isEmpty()) {
                this.priceUsd = Optional.empty();
            } else {
                this.priceUsd = Optional.of(priceUsd.get());
            }
            return this;
        }

        /**
         * <p>USD price per token, or <code>null</code> when unknown.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage priceUsd(Double priceUsd) {
            this.priceUsd = Optional.ofNullable(priceUsd);
            return this;
        }

        /**
         * <p>USD price per token, or <code>null</code> when unknown.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "price_usd", nulls = Nulls.SKIP)
        public _FinalStage priceUsd(Optional<Double> priceUsd) {
            this.priceUsd = priceUsd;
            return this;
        }

        /**
         * <p>The token's display name.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage name(Nullable<String> name) {
            if (name.isNull()) {
                this.name = null;
            } else if (name.isEmpty()) {
                this.name = Optional.empty();
            } else {
                this.name = Optional.of(name.get());
            }
            return this;
        }

        /**
         * <p>The token's display name.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage name(String name) {
            this.name = Optional.ofNullable(name);
            return this;
        }

        /**
         * <p>The token's display name.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "name", nulls = Nulls.SKIP)
        public _FinalStage name(Optional<String> name) {
            this.name = name;
            return this;
        }

        /**
         * <p>Token icon URL.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage iconUrl(Nullable<String> iconUrl) {
            if (iconUrl.isNull()) {
                this.iconUrl = null;
            } else if (iconUrl.isEmpty()) {
                this.iconUrl = Optional.empty();
            } else {
                this.iconUrl = Optional.of(iconUrl.get());
            }
            return this;
        }

        /**
         * <p>Token icon URL.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage iconUrl(String iconUrl) {
            this.iconUrl = Optional.ofNullable(iconUrl);
            return this;
        }

        /**
         * <p>Token icon URL.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "icon_url", nulls = Nulls.SKIP)
        public _FinalStage iconUrl(Optional<String> iconUrl) {
            this.iconUrl = iconUrl;
            return this;
        }

        @java.lang.Override
        public UserBalanceToken build() {
            return new UserBalanceToken(
                    balance, breakdown, iconUrl, name, priceUsd, symbol, valueUsd, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
