/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ScheduleFrequencies {
    public static final ScheduleFrequencies HOURLY = new ScheduleFrequencies(Value.HOURLY, "hourly");

    public static final ScheduleFrequencies ONCE = new ScheduleFrequencies(Value.ONCE, "once");

    public static final ScheduleFrequencies DAILY = new ScheduleFrequencies(Value.DAILY, "daily");

    public static final ScheduleFrequencies WEEKLY = new ScheduleFrequencies(Value.WEEKLY, "weekly");

    public static final ScheduleFrequencies MONTHLY = new ScheduleFrequencies(Value.MONTHLY, "monthly");

    private final Value value;

    private final String string;

    ScheduleFrequencies(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ScheduleFrequencies && this.string.equals(((ScheduleFrequencies) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case HOURLY:
                return visitor.visitHourly();
            case ONCE:
                return visitor.visitOnce();
            case DAILY:
                return visitor.visitDaily();
            case WEEKLY:
                return visitor.visitWeekly();
            case MONTHLY:
                return visitor.visitMonthly();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ScheduleFrequencies valueOf(String value) {
        switch (value) {
            case "hourly":
                return HOURLY;
            case "once":
                return ONCE;
            case "daily":
                return DAILY;
            case "weekly":
                return WEEKLY;
            case "monthly":
                return MONTHLY;
            default:
                return new ScheduleFrequencies(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ONCE,

        HOURLY,

        DAILY,

        WEEKLY,

        MONTHLY,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitOnce();

        T visitHourly();

        T visitDaily();

        T visitWeekly();

        T visitMonthly();

        T visitUnknown(String unknownType);
    }
}
