/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = BountySubmissionLivestreamFeed.Builder.class)
public final class BountySubmissionLivestreamFeed {
    private final Optional<String> endedAt;

    private final String id;

    private final Optional<BountySubmissionLivestreamFeedRecordingStatus> recordingStatus;

    private final Optional<String> recordingUrl;

    private final Optional<String> startedAt;

    private final Optional<String> thumbnailUrl;

    private final String title;

    private final Map<String, Object> additionalProperties;

    private BountySubmissionLivestreamFeed(
            Optional<String> endedAt,
            String id,
            Optional<BountySubmissionLivestreamFeedRecordingStatus> recordingStatus,
            Optional<String> recordingUrl,
            Optional<String> startedAt,
            Optional<String> thumbnailUrl,
            String title,
            Map<String, Object> additionalProperties) {
        this.endedAt = endedAt;
        this.id = id;
        this.recordingStatus = recordingStatus;
        this.recordingUrl = recordingUrl;
        this.startedAt = startedAt;
        this.thumbnailUrl = thumbnailUrl;
        this.title = title;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return When the proof livestream ended, as an ISO 8601 timestamp. <code>null</code> while it is still live — a feed with a <code>started_at</code> and no <code>ended_at</code> is streaming right now.
     */
    @JsonIgnore
    public Optional<String> getEndedAt() {
        if (endedAt == null) {
            return Optional.empty();
        }
        return endedAt;
    }

    /**
     * @return Livestream feed ID.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Recording lifecycle state.
     */
    @JsonIgnore
    public Optional<BountySubmissionLivestreamFeedRecordingStatus> getRecordingStatus() {
        if (recordingStatus == null) {
            return Optional.empty();
        }
        return recordingStatus;
    }

    /**
     * @return Playback URL for a completed proof recording, when available.
     */
    @JsonIgnore
    public Optional<String> getRecordingUrl() {
        if (recordingUrl == null) {
            return Optional.empty();
        }
        return recordingUrl;
    }

    /**
     * @return When the proof livestream went live, as an ISO 8601 timestamp. <code>null</code> before it starts.
     */
    @JsonIgnore
    public Optional<String> getStartedAt() {
        if (startedAt == null) {
            return Optional.empty();
        }
        return startedAt;
    }

    /**
     * @return Current proof thumbnail URL, when available.
     */
    @JsonIgnore
    public Optional<String> getThumbnailUrl() {
        if (thumbnailUrl == null) {
            return Optional.empty();
        }
        return thumbnailUrl;
    }

    /**
     * @return Display title for the proof livestream.
     */
    @JsonProperty("title")
    public String getTitle() {
        return title;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("ended_at")
    private Optional<String> _getEndedAt() {
        return endedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("recording_status")
    private Optional<BountySubmissionLivestreamFeedRecordingStatus> _getRecordingStatus() {
        return recordingStatus;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("recording_url")
    private Optional<String> _getRecordingUrl() {
        return recordingUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("started_at")
    private Optional<String> _getStartedAt() {
        return startedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("thumbnail_url")
    private Optional<String> _getThumbnailUrl() {
        return thumbnailUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof BountySubmissionLivestreamFeed && equalTo((BountySubmissionLivestreamFeed) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(BountySubmissionLivestreamFeed other) {
        return endedAt.equals(other.endedAt)
                && id.equals(other.id)
                && recordingStatus.equals(other.recordingStatus)
                && recordingUrl.equals(other.recordingUrl)
                && startedAt.equals(other.startedAt)
                && thumbnailUrl.equals(other.thumbnailUrl)
                && title.equals(other.title);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.endedAt,
                this.id,
                this.recordingStatus,
                this.recordingUrl,
                this.startedAt,
                this.thumbnailUrl,
                this.title);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>Livestream feed ID.</p>
         */
        TitleStage id(@NotNull String id);

        Builder from(BountySubmissionLivestreamFeed other);
    }

    public interface TitleStage {
        /**
         * <p>Display title for the proof livestream.</p>
         */
        _FinalStage title(@NotNull String title);
    }

    public interface _FinalStage {
        BountySubmissionLivestreamFeed build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>When the proof livestream ended, as an ISO 8601 timestamp. <code>null</code> while it is still live — a feed with a <code>started_at</code> and no <code>ended_at</code> is streaming right now.</p>
         */
        _FinalStage endedAt(Optional<String> endedAt);

        _FinalStage endedAt(String endedAt);

        _FinalStage endedAt(Nullable<String> endedAt);

        /**
         * <p>Recording lifecycle state.</p>
         */
        _FinalStage recordingStatus(Optional<BountySubmissionLivestreamFeedRecordingStatus> recordingStatus);

        _FinalStage recordingStatus(BountySubmissionLivestreamFeedRecordingStatus recordingStatus);

        _FinalStage recordingStatus(Nullable<BountySubmissionLivestreamFeedRecordingStatus> recordingStatus);

        /**
         * <p>Playback URL for a completed proof recording, when available.</p>
         */
        _FinalStage recordingUrl(Optional<String> recordingUrl);

        _FinalStage recordingUrl(String recordingUrl);

        _FinalStage recordingUrl(Nullable<String> recordingUrl);

        /**
         * <p>When the proof livestream went live, as an ISO 8601 timestamp. <code>null</code> before it starts.</p>
         */
        _FinalStage startedAt(Optional<String> startedAt);

        _FinalStage startedAt(String startedAt);

        _FinalStage startedAt(Nullable<String> startedAt);

        /**
         * <p>Current proof thumbnail URL, when available.</p>
         */
        _FinalStage thumbnailUrl(Optional<String> thumbnailUrl);

        _FinalStage thumbnailUrl(String thumbnailUrl);

        _FinalStage thumbnailUrl(Nullable<String> thumbnailUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, TitleStage, _FinalStage {
        private String id;

        private String title;

        private Optional<String> thumbnailUrl = Optional.empty();

        private Optional<String> startedAt = Optional.empty();

        private Optional<String> recordingUrl = Optional.empty();

        private Optional<BountySubmissionLivestreamFeedRecordingStatus> recordingStatus = Optional.empty();

        private Optional<String> endedAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(BountySubmissionLivestreamFeed other) {
            endedAt(other.getEndedAt());
            id(other.getId());
            recordingStatus(other.getRecordingStatus());
            recordingUrl(other.getRecordingUrl());
            startedAt(other.getStartedAt());
            thumbnailUrl(other.getThumbnailUrl());
            title(other.getTitle());
            return this;
        }

        /**
         * <p>Livestream feed ID.</p>
         * <p>Livestream feed ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public TitleStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Display title for the proof livestream.</p>
         * <p>Display title for the proof livestream.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("title")
        public _FinalStage title(@NotNull String title) {
            this.title = Objects.requireNonNull(title, "title must not be null");
            return this;
        }

        /**
         * <p>Current proof thumbnail URL, when available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage thumbnailUrl(Nullable<String> thumbnailUrl) {
            if (thumbnailUrl.isNull()) {
                this.thumbnailUrl = null;
            } else if (thumbnailUrl.isEmpty()) {
                this.thumbnailUrl = Optional.empty();
            } else {
                this.thumbnailUrl = Optional.of(thumbnailUrl.get());
            }
            return this;
        }

        /**
         * <p>Current proof thumbnail URL, when available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage thumbnailUrl(String thumbnailUrl) {
            this.thumbnailUrl = Optional.ofNullable(thumbnailUrl);
            return this;
        }

        /**
         * <p>Current proof thumbnail URL, when available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "thumbnail_url", nulls = Nulls.SKIP)
        public _FinalStage thumbnailUrl(Optional<String> thumbnailUrl) {
            this.thumbnailUrl = thumbnailUrl;
            return this;
        }

        /**
         * <p>When the proof livestream went live, as an ISO 8601 timestamp. <code>null</code> before it starts.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage startedAt(Nullable<String> startedAt) {
            if (startedAt.isNull()) {
                this.startedAt = null;
            } else if (startedAt.isEmpty()) {
                this.startedAt = Optional.empty();
            } else {
                this.startedAt = Optional.of(startedAt.get());
            }
            return this;
        }

        /**
         * <p>When the proof livestream went live, as an ISO 8601 timestamp. <code>null</code> before it starts.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage startedAt(String startedAt) {
            this.startedAt = Optional.ofNullable(startedAt);
            return this;
        }

        /**
         * <p>When the proof livestream went live, as an ISO 8601 timestamp. <code>null</code> before it starts.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "started_at", nulls = Nulls.SKIP)
        public _FinalStage startedAt(Optional<String> startedAt) {
            this.startedAt = startedAt;
            return this;
        }

        /**
         * <p>Playback URL for a completed proof recording, when available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage recordingUrl(Nullable<String> recordingUrl) {
            if (recordingUrl.isNull()) {
                this.recordingUrl = null;
            } else if (recordingUrl.isEmpty()) {
                this.recordingUrl = Optional.empty();
            } else {
                this.recordingUrl = Optional.of(recordingUrl.get());
            }
            return this;
        }

        /**
         * <p>Playback URL for a completed proof recording, when available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage recordingUrl(String recordingUrl) {
            this.recordingUrl = Optional.ofNullable(recordingUrl);
            return this;
        }

        /**
         * <p>Playback URL for a completed proof recording, when available.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "recording_url", nulls = Nulls.SKIP)
        public _FinalStage recordingUrl(Optional<String> recordingUrl) {
            this.recordingUrl = recordingUrl;
            return this;
        }

        /**
         * <p>Recording lifecycle state.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage recordingStatus(Nullable<BountySubmissionLivestreamFeedRecordingStatus> recordingStatus) {
            if (recordingStatus.isNull()) {
                this.recordingStatus = null;
            } else if (recordingStatus.isEmpty()) {
                this.recordingStatus = Optional.empty();
            } else {
                this.recordingStatus = Optional.of(recordingStatus.get());
            }
            return this;
        }

        /**
         * <p>Recording lifecycle state.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage recordingStatus(BountySubmissionLivestreamFeedRecordingStatus recordingStatus) {
            this.recordingStatus = Optional.ofNullable(recordingStatus);
            return this;
        }

        /**
         * <p>Recording lifecycle state.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "recording_status", nulls = Nulls.SKIP)
        public _FinalStage recordingStatus(Optional<BountySubmissionLivestreamFeedRecordingStatus> recordingStatus) {
            this.recordingStatus = recordingStatus;
            return this;
        }

        /**
         * <p>When the proof livestream ended, as an ISO 8601 timestamp. <code>null</code> while it is still live — a feed with a <code>started_at</code> and no <code>ended_at</code> is streaming right now.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage endedAt(Nullable<String> endedAt) {
            if (endedAt.isNull()) {
                this.endedAt = null;
            } else if (endedAt.isEmpty()) {
                this.endedAt = Optional.empty();
            } else {
                this.endedAt = Optional.of(endedAt.get());
            }
            return this;
        }

        /**
         * <p>When the proof livestream ended, as an ISO 8601 timestamp. <code>null</code> while it is still live — a feed with a <code>started_at</code> and no <code>ended_at</code> is streaming right now.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage endedAt(String endedAt) {
            this.endedAt = Optional.ofNullable(endedAt);
            return this;
        }

        /**
         * <p>When the proof livestream ended, as an ISO 8601 timestamp. <code>null</code> while it is still live — a feed with a <code>started_at</code> and no <code>ended_at</code> is streaming right now.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "ended_at", nulls = Nulls.SKIP)
        public _FinalStage endedAt(Optional<String> endedAt) {
            this.endedAt = endedAt;
            return this;
        }

        @java.lang.Override
        public BountySubmissionLivestreamFeed build() {
            return new BountySubmissionLivestreamFeed(
                    endedAt, id, recordingStatus, recordingUrl, startedAt, thumbnailUrl, title, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
