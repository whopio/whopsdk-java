/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionPayment.Builder.class)
public final class CheckoutSessionPayment {
    private final String id;

    private final CheckoutSessionPaymentStatus status;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionPayment(
            String id, CheckoutSessionPaymentStatus status, Map<String, Object> additionalProperties) {
        this.id = id;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The payment created by the confirm, prefixed <code>pay_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Where the payment stands, and the only honest reading of a completed checkout's outcome. <code>requires_action</code> — a step remains and <code>next_action</code> carries it. <code>processing</code> — accepted and settling (or deciding); hold. <code>succeeded</code> — the money moved. <code>failed</code> — the charge died (declined, expired, voided): the checkout did not go through, whatever the session's own <code>status</code> says, and the buyer needs a fresh checkout to try again.
     */
    @JsonProperty("status")
    public CheckoutSessionPaymentStatus getStatus() {
        return status;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionPayment && equalTo((CheckoutSessionPayment) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionPayment other) {
        return id.equals(other.id) && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The payment created by the confirm, prefixed <code>pay_</code>.</p>
         */
        StatusStage id(@NotNull String id);

        Builder from(CheckoutSessionPayment other);
    }

    public interface StatusStage {
        /**
         * <p>Where the payment stands, and the only honest reading of a completed checkout's outcome. <code>requires_action</code> — a step remains and <code>next_action</code> carries it. <code>processing</code> — accepted and settling (or deciding); hold. <code>succeeded</code> — the money moved. <code>failed</code> — the charge died (declined, expired, voided): the checkout did not go through, whatever the session's own <code>status</code> says, and the buyer needs a fresh checkout to try again.</p>
         */
        _FinalStage status(@NotNull CheckoutSessionPaymentStatus status);
    }

    public interface _FinalStage {
        CheckoutSessionPayment build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, StatusStage, _FinalStage {
        private String id;

        private CheckoutSessionPaymentStatus status;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionPayment other) {
            id(other.getId());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>The payment created by the confirm, prefixed <code>pay_</code>.</p>
         * <p>The payment created by the confirm, prefixed <code>pay_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public StatusStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Where the payment stands, and the only honest reading of a completed checkout's outcome. <code>requires_action</code> — a step remains and <code>next_action</code> carries it. <code>processing</code> — accepted and settling (or deciding); hold. <code>succeeded</code> — the money moved. <code>failed</code> — the charge died (declined, expired, voided): the checkout did not go through, whatever the session's own <code>status</code> says, and the buyer needs a fresh checkout to try again.</p>
         * <p>Where the payment stands, and the only honest reading of a completed checkout's outcome. <code>requires_action</code> — a step remains and <code>next_action</code> carries it. <code>processing</code> — accepted and settling (or deciding); hold. <code>succeeded</code> — the money moved. <code>failed</code> — the charge died (declined, expired, voided): the checkout did not go through, whatever the session's own <code>status</code> says, and the buyer needs a fresh checkout to try again.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull CheckoutSessionPaymentStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        @java.lang.Override
        public CheckoutSessionPayment build() {
            return new CheckoutSessionPayment(id, status, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
