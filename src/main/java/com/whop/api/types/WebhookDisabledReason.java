/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class WebhookDisabledReason {
    public static final WebhookDisabledReason DELIVERY_FAILURES =
            new WebhookDisabledReason(Value.DELIVERY_FAILURES, "delivery_failures");

    private final Value value;

    private final String string;

    WebhookDisabledReason(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof WebhookDisabledReason
                        && this.string.equals(((WebhookDisabledReason) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DELIVERY_FAILURES:
                return visitor.visitDeliveryFailures();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static WebhookDisabledReason valueOf(String value) {
        switch (value) {
            case "delivery_failures":
                return DELIVERY_FAILURES;
            default:
                return new WebhookDisabledReason(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        DELIVERY_FAILURES,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitDeliveryFailures();

        T visitUnknown(String unknownType);
    }
}
