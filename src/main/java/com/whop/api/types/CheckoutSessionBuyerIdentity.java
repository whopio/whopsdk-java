/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CheckoutSessionBuyerIdentity {
    public static final CheckoutSessionBuyerIdentity AUTHENTICATED =
            new CheckoutSessionBuyerIdentity(Value.AUTHENTICATED, "authenticated");

    public static final CheckoutSessionBuyerIdentity ATTRIBUTED =
            new CheckoutSessionBuyerIdentity(Value.ATTRIBUTED, "attributed");

    private final Value value;

    private final String string;

    CheckoutSessionBuyerIdentity(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CheckoutSessionBuyerIdentity
                        && this.string.equals(((CheckoutSessionBuyerIdentity) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case AUTHENTICATED:
                return visitor.visitAuthenticated();
            case ATTRIBUTED:
                return visitor.visitAttributed();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CheckoutSessionBuyerIdentity valueOf(String value) {
        switch (value) {
            case "authenticated":
                return AUTHENTICATED;
            case "attributed":
                return ATTRIBUTED;
            default:
                return new CheckoutSessionBuyerIdentity(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ATTRIBUTED,

        AUTHENTICATED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitAttributed();

        T visitAuthenticated();

        T visitUnknown(String unknownType);
    }
}
