/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AccountTaxType {
    public static final AccountTaxType EXCLUSIVE = new AccountTaxType(Value.EXCLUSIVE, "exclusive");

    public static final AccountTaxType INCLUSIVE = new AccountTaxType(Value.INCLUSIVE, "inclusive");

    private final Value value;

    private final String string;

    AccountTaxType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof AccountTaxType && this.string.equals(((AccountTaxType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case EXCLUSIVE:
                return visitor.visitExclusive();
            case INCLUSIVE:
                return visitor.visitInclusive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AccountTaxType valueOf(String value) {
        switch (value) {
            case "exclusive":
                return EXCLUSIVE;
            case "inclusive":
                return INCLUSIVE;
            default:
                return new AccountTaxType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        INCLUSIVE,

        EXCLUSIVE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitInclusive();

        T visitExclusive();

        T visitUnknown(String unknownType);
    }
}
