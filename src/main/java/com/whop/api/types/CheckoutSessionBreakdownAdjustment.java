/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionBreakdownAdjustment.Builder.class)
public final class CheckoutSessionBreakdownAdjustment {
    private final Optional<Money> amount;

    private final CheckoutSessionBreakdownAdjustmentApplied applied;

    private final CheckoutSessionBreakdownAdjustmentKind kind;

    private final String label;

    private final Optional<String> rate;

    private final Optional<CheckoutSessionBreakdownAdjustmentStatus> status;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionBreakdownAdjustment(
            Optional<Money> amount,
            CheckoutSessionBreakdownAdjustmentApplied applied,
            CheckoutSessionBreakdownAdjustmentKind kind,
            String label,
            Optional<String> rate,
            Optional<CheckoutSessionBreakdownAdjustmentStatus> status,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.applied = applied;
        this.kind = kind;
        this.label = label;
        this.rate = rate;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What this adjustment does to the total — negative for a discount. <code>null</code> only while <code>status</code> is <code>pending</code>.
     */
    @JsonIgnore
    public Optional<Money> getAmount() {
        if (amount == null) {
            return Optional.empty();
        }
        return amount;
    }

    /**
     * @return <code>added</code> counts toward the total; <code>included</code> is already inside the lines and is shown for disclosure only (inclusive-tax markets).
     */
    @JsonProperty("applied")
    public CheckoutSessionBreakdownAdjustmentApplied getApplied() {
        return applied;
    }

    /**
     * @return What changes the price: the promo's <code>discount</code>, the <code>buyer_fee</code> the charge adds, or <code>tax</code>.
     */
    @JsonProperty("kind")
    public CheckoutSessionBreakdownAdjustmentKind getKind() {
        return kind;
    }

    /**
     * @return What to show the buyer for this row.
     */
    @JsonProperty("label")
    public String getLabel() {
        return label;
    }

    /**
     * @return The <code>tax</code> row's effective rate as a decimal fraction (<code>&quot;0.0725&quot;</code> is 7.25%) — the tax as a share of the base it was priced on, stated so a surface can label the row with the percentage. Present only on a resolved tax adjustment from <code>calculate_breakdown</code> or <code>calculate_tax</code>; absent on other kinds, on a <code>pending</code> row, and on a completed session's settled row.
     */
    @JsonProperty("rate")
    public Optional<String> getRate() {
        return rate;
    }

    /**
     * @return Present only on an adjustment with no figure yet — render its row in a loading state and expect <code>calculate_breakdown</code> to resolve it. An adjustment that does not apply is absent from the list entirely, never a zero row.
     */
    @JsonProperty("status")
    public Optional<CheckoutSessionBreakdownAdjustmentStatus> getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("amount")
    private Optional<Money> _getAmount() {
        return amount;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionBreakdownAdjustment
                && equalTo((CheckoutSessionBreakdownAdjustment) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionBreakdownAdjustment other) {
        return amount.equals(other.amount)
                && applied.equals(other.applied)
                && kind.equals(other.kind)
                && label.equals(other.label)
                && rate.equals(other.rate)
                && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.amount, this.applied, this.kind, this.label, this.rate, this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AppliedStage builder() {
        return new Builder();
    }

    public interface AppliedStage {
        /**
         * <p><code>added</code> counts toward the total; <code>included</code> is already inside the lines and is shown for disclosure only (inclusive-tax markets).</p>
         */
        KindStage applied(@NotNull CheckoutSessionBreakdownAdjustmentApplied applied);

        Builder from(CheckoutSessionBreakdownAdjustment other);
    }

    public interface KindStage {
        /**
         * <p>What changes the price: the promo's <code>discount</code>, the <code>buyer_fee</code> the charge adds, or <code>tax</code>.</p>
         */
        LabelStage kind(@NotNull CheckoutSessionBreakdownAdjustmentKind kind);
    }

    public interface LabelStage {
        /**
         * <p>What to show the buyer for this row.</p>
         */
        _FinalStage label(@NotNull String label);
    }

    public interface _FinalStage {
        CheckoutSessionBreakdownAdjustment build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>What this adjustment does to the total — negative for a discount. <code>null</code> only while <code>status</code> is <code>pending</code>.</p>
         */
        _FinalStage amount(Optional<Money> amount);

        _FinalStage amount(Money amount);

        _FinalStage amount(Nullable<Money> amount);

        /**
         * <p>The <code>tax</code> row's effective rate as a decimal fraction (<code>&quot;0.0725&quot;</code> is 7.25%) — the tax as a share of the base it was priced on, stated so a surface can label the row with the percentage. Present only on a resolved tax adjustment from <code>calculate_breakdown</code> or <code>calculate_tax</code>; absent on other kinds, on a <code>pending</code> row, and on a completed session's settled row.</p>
         */
        _FinalStage rate(Optional<String> rate);

        _FinalStage rate(String rate);

        /**
         * <p>Present only on an adjustment with no figure yet — render its row in a loading state and expect <code>calculate_breakdown</code> to resolve it. An adjustment that does not apply is absent from the list entirely, never a zero row.</p>
         */
        _FinalStage status(Optional<CheckoutSessionBreakdownAdjustmentStatus> status);

        _FinalStage status(CheckoutSessionBreakdownAdjustmentStatus status);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AppliedStage, KindStage, LabelStage, _FinalStage {
        private CheckoutSessionBreakdownAdjustmentApplied applied;

        private CheckoutSessionBreakdownAdjustmentKind kind;

        private String label;

        private Optional<CheckoutSessionBreakdownAdjustmentStatus> status = Optional.empty();

        private Optional<String> rate = Optional.empty();

        private Optional<Money> amount = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionBreakdownAdjustment other) {
            amount(other.getAmount());
            applied(other.getApplied());
            kind(other.getKind());
            label(other.getLabel());
            rate(other.getRate());
            status(other.getStatus());
            return this;
        }

        /**
         * <p><code>added</code> counts toward the total; <code>included</code> is already inside the lines and is shown for disclosure only (inclusive-tax markets).</p>
         * <p><code>added</code> counts toward the total; <code>included</code> is already inside the lines and is shown for disclosure only (inclusive-tax markets).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("applied")
        public KindStage applied(@NotNull CheckoutSessionBreakdownAdjustmentApplied applied) {
            this.applied = Objects.requireNonNull(applied, "applied must not be null");
            return this;
        }

        /**
         * <p>What changes the price: the promo's <code>discount</code>, the <code>buyer_fee</code> the charge adds, or <code>tax</code>.</p>
         * <p>What changes the price: the promo's <code>discount</code>, the <code>buyer_fee</code> the charge adds, or <code>tax</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("kind")
        public LabelStage kind(@NotNull CheckoutSessionBreakdownAdjustmentKind kind) {
            this.kind = Objects.requireNonNull(kind, "kind must not be null");
            return this;
        }

        /**
         * <p>What to show the buyer for this row.</p>
         * <p>What to show the buyer for this row.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("label")
        public _FinalStage label(@NotNull String label) {
            this.label = Objects.requireNonNull(label, "label must not be null");
            return this;
        }

        /**
         * <p>Present only on an adjustment with no figure yet — render its row in a loading state and expect <code>calculate_breakdown</code> to resolve it. An adjustment that does not apply is absent from the list entirely, never a zero row.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage status(CheckoutSessionBreakdownAdjustmentStatus status) {
            this.status = Optional.ofNullable(status);
            return this;
        }

        /**
         * <p>Present only on an adjustment with no figure yet — render its row in a loading state and expect <code>calculate_breakdown</code> to resolve it. An adjustment that does not apply is absent from the list entirely, never a zero row.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "status", nulls = Nulls.SKIP)
        public _FinalStage status(Optional<CheckoutSessionBreakdownAdjustmentStatus> status) {
            this.status = status;
            return this;
        }

        /**
         * <p>The <code>tax</code> row's effective rate as a decimal fraction (<code>&quot;0.0725&quot;</code> is 7.25%) — the tax as a share of the base it was priced on, stated so a surface can label the row with the percentage. Present only on a resolved tax adjustment from <code>calculate_breakdown</code> or <code>calculate_tax</code>; absent on other kinds, on a <code>pending</code> row, and on a completed session's settled row.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage rate(String rate) {
            this.rate = Optional.ofNullable(rate);
            return this;
        }

        /**
         * <p>The <code>tax</code> row's effective rate as a decimal fraction (<code>&quot;0.0725&quot;</code> is 7.25%) — the tax as a share of the base it was priced on, stated so a surface can label the row with the percentage. Present only on a resolved tax adjustment from <code>calculate_breakdown</code> or <code>calculate_tax</code>; absent on other kinds, on a <code>pending</code> row, and on a completed session's settled row.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "rate", nulls = Nulls.SKIP)
        public _FinalStage rate(Optional<String> rate) {
            this.rate = rate;
            return this;
        }

        /**
         * <p>What this adjustment does to the total — negative for a discount. <code>null</code> only while <code>status</code> is <code>pending</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amount(Nullable<Money> amount) {
            if (amount.isNull()) {
                this.amount = null;
            } else if (amount.isEmpty()) {
                this.amount = Optional.empty();
            } else {
                this.amount = Optional.of(amount.get());
            }
            return this;
        }

        /**
         * <p>What this adjustment does to the total — negative for a discount. <code>null</code> only while <code>status</code> is <code>pending</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amount(Money amount) {
            this.amount = Optional.ofNullable(amount);
            return this;
        }

        /**
         * <p>What this adjustment does to the total — negative for a discount. <code>null</code> only while <code>status</code> is <code>pending</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount", nulls = Nulls.SKIP)
        public _FinalStage amount(Optional<Money> amount) {
            this.amount = amount;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionBreakdownAdjustment build() {
            return new CheckoutSessionBreakdownAdjustment(
                    amount, applied, kind, label, rate, status, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
