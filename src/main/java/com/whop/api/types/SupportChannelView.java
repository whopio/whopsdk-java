/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class SupportChannelView {
    public static final SupportChannelView ALL = new SupportChannelView(Value.ALL, "all");

    public static final SupportChannelView ADMIN = new SupportChannelView(Value.ADMIN, "admin");

    public static final SupportChannelView CUSTOMER = new SupportChannelView(Value.CUSTOMER, "customer");

    private final Value value;

    private final String string;

    SupportChannelView(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof SupportChannelView && this.string.equals(((SupportChannelView) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case ALL:
                return visitor.visitAll();
            case ADMIN:
                return visitor.visitAdmin();
            case CUSTOMER:
                return visitor.visitCustomer();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static SupportChannelView valueOf(String value) {
        switch (value) {
            case "all":
                return ALL;
            case "admin":
                return ADMIN;
            case "customer":
                return CUSTOMER;
            default:
                return new SupportChannelView(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ALL,

        ADMIN,

        CUSTOMER,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitAll();

        T visitAdmin();

        T visitCustomer();

        T visitUnknown(String unknownType);
    }
}
