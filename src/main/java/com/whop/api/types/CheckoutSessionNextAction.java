/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class CheckoutSessionNextAction {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private CheckoutSessionNextAction(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static CheckoutSessionNextAction authenticate(CheckoutSessionAuthenticateAction value) {
        return new CheckoutSessionNextAction(new AuthenticateValue(value));
    }

    public static CheckoutSessionNextAction awaitClaim(CheckoutSessionAwaitClaimAction value) {
        return new CheckoutSessionNextAction(new AwaitClaimValue(value));
    }

    public static CheckoutSessionNextAction complete(CheckoutSessionCompleteAction value) {
        return new CheckoutSessionNextAction(new CompleteValue(value));
    }

    public static CheckoutSessionNextAction redirect(CheckoutSessionRedirectAction value) {
        return new CheckoutSessionNextAction(new RedirectValue(value));
    }

    public static CheckoutSessionNextAction upgradeAuthentication(CheckoutSessionUpgradeAuthenticationAction value) {
        return new CheckoutSessionNextAction(new UpgradeAuthenticationValue(value));
    }

    public static CheckoutSessionNextAction verifyPhone(CheckoutSessionVerifyPhoneAction value) {
        return new CheckoutSessionNextAction(new VerifyPhoneValue(value));
    }

    public static CheckoutSessionNextAction waitForPayment(CheckoutSessionWaitForPaymentAction value) {
        return new CheckoutSessionNextAction(new WaitForPaymentValue(value));
    }

    public boolean isAuthenticate() {
        return value instanceof AuthenticateValue;
    }

    public boolean isAwaitClaim() {
        return value instanceof AwaitClaimValue;
    }

    public boolean isComplete() {
        return value instanceof CompleteValue;
    }

    public boolean isRedirect() {
        return value instanceof RedirectValue;
    }

    public boolean isUpgradeAuthentication() {
        return value instanceof UpgradeAuthenticationValue;
    }

    public boolean isVerifyPhone() {
        return value instanceof VerifyPhoneValue;
    }

    public boolean isWaitForPayment() {
        return value instanceof WaitForPaymentValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CheckoutSessionAuthenticateAction> getAuthenticate() {
        if (isAuthenticate()) {
            return Optional.of(((AuthenticateValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionAwaitClaimAction> getAwaitClaim() {
        if (isAwaitClaim()) {
            return Optional.of(((AwaitClaimValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionCompleteAction> getComplete() {
        if (isComplete()) {
            return Optional.of(((CompleteValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionRedirectAction> getRedirect() {
        if (isRedirect()) {
            return Optional.of(((RedirectValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionUpgradeAuthenticationAction> getUpgradeAuthentication() {
        if (isUpgradeAuthentication()) {
            return Optional.of(((UpgradeAuthenticationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionVerifyPhoneAction> getVerifyPhone() {
        if (isVerifyPhone()) {
            return Optional.of(((VerifyPhoneValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CheckoutSessionWaitForPaymentAction> getWaitForPayment() {
        if (isWaitForPayment()) {
            return Optional.of(((WaitForPaymentValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionNextAction && value.equals(((CheckoutSessionNextAction) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAuthenticate(CheckoutSessionAuthenticateAction authenticate);

        T visitAwaitClaim(CheckoutSessionAwaitClaimAction awaitClaim);

        T visitComplete(CheckoutSessionCompleteAction complete);

        T visitRedirect(CheckoutSessionRedirectAction redirect);

        T visitUpgradeAuthentication(CheckoutSessionUpgradeAuthenticationAction upgradeAuthentication);

        T visitVerifyPhone(CheckoutSessionVerifyPhoneAction verifyPhone);

        T visitWaitForPayment(CheckoutSessionWaitForPaymentAction waitForPayment);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(AuthenticateValue.class),
        @JsonSubTypes.Type(AwaitClaimValue.class),
        @JsonSubTypes.Type(CompleteValue.class),
        @JsonSubTypes.Type(RedirectValue.class),
        @JsonSubTypes.Type(UpgradeAuthenticationValue.class),
        @JsonSubTypes.Type(VerifyPhoneValue.class),
        @JsonSubTypes.Type(WaitForPaymentValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("authenticate")
    @JsonIgnoreProperties("type")
    private static final class AuthenticateValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionAuthenticateAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AuthenticateValue() {}

        private AuthenticateValue(CheckoutSessionAuthenticateAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAuthenticate(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AuthenticateValue && equalTo((AuthenticateValue) other);
        }

        private boolean equalTo(AuthenticateValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("await_claim")
    @JsonIgnoreProperties("type")
    private static final class AwaitClaimValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionAwaitClaimAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AwaitClaimValue() {}

        private AwaitClaimValue(CheckoutSessionAwaitClaimAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAwaitClaim(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AwaitClaimValue && equalTo((AwaitClaimValue) other);
        }

        private boolean equalTo(AwaitClaimValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("complete")
    @JsonIgnoreProperties("type")
    private static final class CompleteValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionCompleteAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CompleteValue() {}

        private CompleteValue(CheckoutSessionCompleteAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitComplete(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CompleteValue && equalTo((CompleteValue) other);
        }

        private boolean equalTo(CompleteValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("redirect")
    @JsonIgnoreProperties("type")
    private static final class RedirectValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionRedirectAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RedirectValue() {}

        private RedirectValue(CheckoutSessionRedirectAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRedirect(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RedirectValue && equalTo((RedirectValue) other);
        }

        private boolean equalTo(RedirectValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("upgrade_authentication")
    @JsonIgnoreProperties("type")
    private static final class UpgradeAuthenticationValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionUpgradeAuthenticationAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UpgradeAuthenticationValue() {}

        private UpgradeAuthenticationValue(CheckoutSessionUpgradeAuthenticationAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUpgradeAuthentication(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UpgradeAuthenticationValue && equalTo((UpgradeAuthenticationValue) other);
        }

        private boolean equalTo(UpgradeAuthenticationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("verify_phone")
    @JsonIgnoreProperties("type")
    private static final class VerifyPhoneValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionVerifyPhoneAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private VerifyPhoneValue() {}

        private VerifyPhoneValue(CheckoutSessionVerifyPhoneAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitVerifyPhone(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof VerifyPhoneValue && equalTo((VerifyPhoneValue) other);
        }

        private boolean equalTo(VerifyPhoneValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("wait_for_payment")
    @JsonIgnoreProperties("type")
    private static final class WaitForPaymentValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private CheckoutSessionWaitForPaymentAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private WaitForPaymentValue() {}

        private WaitForPaymentValue(CheckoutSessionWaitForPaymentAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitWaitForPayment(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof WaitForPaymentValue && equalTo((WaitForPaymentValue) other);
        }

        private boolean equalTo(WaitForPaymentValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CheckoutSessionNextAction{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
