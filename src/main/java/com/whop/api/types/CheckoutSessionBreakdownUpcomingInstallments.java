/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CheckoutSessionBreakdownUpcomingInstallments.Builder.class)
public final class CheckoutSessionBreakdownUpcomingInstallments {
    private final Money amount;

    private final String chargeAt;

    private final String description;

    private final int intervalDays;

    private final Optional<Money> postPromoAmount;

    private final int remaining;

    private final Optional<Integer> trialDays;

    private final Map<String, Object> additionalProperties;

    private CheckoutSessionBreakdownUpcomingInstallments(
            Money amount,
            String chargeAt,
            String description,
            int intervalDays,
            Optional<Money> postPromoAmount,
            int remaining,
            Optional<Integer> trialDays,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.chargeAt = chargeAt;
        this.description = description;
        this.intervalDays = intervalDays;
        this.postPromoAmount = postPromoAmount;
        this.remaining = remaining;
        this.trialDays = trialDays;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What the NEXT installment takes, priced as its receipt will be: the cycle for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge. Stated before tax.
     */
    @JsonProperty("amount")
    public Money getAmount() {
        return amount;
    }

    /**
     * @return When the next installment is charged, as an ISO 8601 timestamp.
     */
    @JsonProperty("charge_at")
    public String getChargeAt() {
        return chargeAt;
    }

    /**
     * @return What to show the buyer for this future charge.
     */
    @JsonProperty("description")
    public String getDescription() {
        return description;
    }

    /**
     * @return Days between installments.
     */
    @JsonProperty("interval_days")
    public int getIntervalDays() {
        return intervalDays;
    }

    /**
     * @return What each installment takes once the applied promo stops covering them — the plan's own installment price, stated like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires before the remaining installments do; absent when no promo discounts the next installment, and for a <code>forever</code> code.
     */
    @JsonProperty("post_promo_amount")
    public Optional<Money> getPostPromoAmount() {
        return postPromoAmount;
    }

    /**
     * @return How many of the plan's payments are LEFT after today. Only a charge that takes money consumes one — today's does when something is due now, while a free trial or a transfer leaves every payment ahead.
     */
    @JsonProperty("remaining")
    public int getRemaining() {
        return remaining;
    }

    /**
     * @return Free-trial days before the first installment. Absent when the plan starts billing with this purchase.
     */
    @JsonProperty("trial_days")
    public Optional<Integer> getTrialDays() {
        return trialDays;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CheckoutSessionBreakdownUpcomingInstallments
                && equalTo((CheckoutSessionBreakdownUpcomingInstallments) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CheckoutSessionBreakdownUpcomingInstallments other) {
        return amount.equals(other.amount)
                && chargeAt.equals(other.chargeAt)
                && description.equals(other.description)
                && intervalDays == other.intervalDays
                && postPromoAmount.equals(other.postPromoAmount)
                && remaining == other.remaining
                && trialDays.equals(other.trialDays);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amount,
                this.chargeAt,
                this.description,
                this.intervalDays,
                this.postPromoAmount,
                this.remaining,
                this.trialDays);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>What the NEXT installment takes, priced as its receipt will be: the cycle for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge. Stated before tax.</p>
         */
        ChargeAtStage amount(@NotNull Money amount);

        Builder from(CheckoutSessionBreakdownUpcomingInstallments other);
    }

    public interface ChargeAtStage {
        /**
         * <p>When the next installment is charged, as an ISO 8601 timestamp.</p>
         */
        DescriptionStage chargeAt(@NotNull String chargeAt);
    }

    public interface DescriptionStage {
        /**
         * <p>What to show the buyer for this future charge.</p>
         */
        IntervalDaysStage description(@NotNull String description);
    }

    public interface IntervalDaysStage {
        /**
         * <p>Days between installments.</p>
         */
        RemainingStage intervalDays(int intervalDays);
    }

    public interface RemainingStage {
        /**
         * <p>How many of the plan's payments are LEFT after today. Only a charge that takes money consumes one — today's does when something is due now, while a free trial or a transfer leaves every payment ahead.</p>
         */
        _FinalStage remaining(int remaining);
    }

    public interface _FinalStage {
        CheckoutSessionBreakdownUpcomingInstallments build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>What each installment takes once the applied promo stops covering them — the plan's own installment price, stated like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires before the remaining installments do; absent when no promo discounts the next installment, and for a <code>forever</code> code.</p>
         */
        _FinalStage postPromoAmount(Optional<Money> postPromoAmount);

        _FinalStage postPromoAmount(Money postPromoAmount);

        /**
         * <p>Free-trial days before the first installment. Absent when the plan starts billing with this purchase.</p>
         */
        _FinalStage trialDays(Optional<Integer> trialDays);

        _FinalStage trialDays(Integer trialDays);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AmountStage, ChargeAtStage, DescriptionStage, IntervalDaysStage, RemainingStage, _FinalStage {
        private Money amount;

        private String chargeAt;

        private String description;

        private int intervalDays;

        private int remaining;

        private Optional<Integer> trialDays = Optional.empty();

        private Optional<Money> postPromoAmount = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CheckoutSessionBreakdownUpcomingInstallments other) {
            amount(other.getAmount());
            chargeAt(other.getChargeAt());
            description(other.getDescription());
            intervalDays(other.getIntervalDays());
            postPromoAmount(other.getPostPromoAmount());
            remaining(other.getRemaining());
            trialDays(other.getTrialDays());
            return this;
        }

        /**
         * <p>What the NEXT installment takes, priced as its receipt will be: the cycle for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge. Stated before tax.</p>
         * <p>What the NEXT installment takes, priced as its receipt will be: the cycle for the quantity, the buyer service fee it carries, and the promo applied only when its duration still covers that charge. Stated before tax.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public ChargeAtStage amount(@NotNull Money amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>When the next installment is charged, as an ISO 8601 timestamp.</p>
         * <p>When the next installment is charged, as an ISO 8601 timestamp.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("charge_at")
        public DescriptionStage chargeAt(@NotNull String chargeAt) {
            this.chargeAt = Objects.requireNonNull(chargeAt, "chargeAt must not be null");
            return this;
        }

        /**
         * <p>What to show the buyer for this future charge.</p>
         * <p>What to show the buyer for this future charge.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("description")
        public IntervalDaysStage description(@NotNull String description) {
            this.description = Objects.requireNonNull(description, "description must not be null");
            return this;
        }

        /**
         * <p>Days between installments.</p>
         * <p>Days between installments.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("interval_days")
        public RemainingStage intervalDays(int intervalDays) {
            this.intervalDays = intervalDays;
            return this;
        }

        /**
         * <p>How many of the plan's payments are LEFT after today. Only a charge that takes money consumes one — today's does when something is due now, while a free trial or a transfer leaves every payment ahead.</p>
         * <p>How many of the plan's payments are LEFT after today. Only a charge that takes money consumes one — today's does when something is due now, while a free trial or a transfer leaves every payment ahead.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("remaining")
        public _FinalStage remaining(int remaining) {
            this.remaining = remaining;
            return this;
        }

        /**
         * <p>Free-trial days before the first installment. Absent when the plan starts billing with this purchase.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage trialDays(Integer trialDays) {
            this.trialDays = Optional.ofNullable(trialDays);
            return this;
        }

        /**
         * <p>Free-trial days before the first installment. Absent when the plan starts billing with this purchase.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "trial_days", nulls = Nulls.SKIP)
        public _FinalStage trialDays(Optional<Integer> trialDays) {
            this.trialDays = trialDays;
            return this;
        }

        /**
         * <p>What each installment takes once the applied promo stops covering them — the plan's own installment price, stated like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires before the remaining installments do; absent when no promo discounts the next installment, and for a <code>forever</code> code.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage postPromoAmount(Money postPromoAmount) {
            this.postPromoAmount = Optional.ofNullable(postPromoAmount);
            return this;
        }

        /**
         * <p>What each installment takes once the applied promo stops covering them — the plan's own installment price, stated like <code>amount</code> (quantity, buyer fee, before tax). Present only when <code>amount</code> is discounted by a promo that expires before the remaining installments do; absent when no promo discounts the next installment, and for a <code>forever</code> code.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "post_promo_amount", nulls = Nulls.SKIP)
        public _FinalStage postPromoAmount(Optional<Money> postPromoAmount) {
            this.postPromoAmount = postPromoAmount;
            return this;
        }

        @java.lang.Override
        public CheckoutSessionBreakdownUpcomingInstallments build() {
            return new CheckoutSessionBreakdownUpcomingInstallments(
                    amount,
                    chargeAt,
                    description,
                    intervalDays,
                    postPromoAmount,
                    remaining,
                    trialDays,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
