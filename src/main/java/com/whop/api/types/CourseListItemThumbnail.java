/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CourseListItemThumbnail.Builder.class)
public final class CourseListItemThumbnail {
    private final Optional<String> contentType;

    private final Optional<String> filename;

    private final String id;

    private final Optional<String> optimizedUrl;

    private final Optional<String> sourceUrl;

    private final Map<String, Object> additionalProperties;

    private CourseListItemThumbnail(
            Optional<String> contentType,
            Optional<String> filename,
            String id,
            Optional<String> optimizedUrl,
            Optional<String> sourceUrl,
            Map<String, Object> additionalProperties) {
        this.contentType = contentType;
        this.filename = filename;
        this.id = id;
        this.optimizedUrl = optimizedUrl;
        this.sourceUrl = sourceUrl;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg.
     */
    @JsonIgnore
    public Optional<String> getContentType() {
        if (contentType == null) {
            return Optional.empty();
        }
        return contentType;
    }

    /**
     * @return The original filename of the uploaded attachment, including its file extension.
     */
    @JsonIgnore
    public Optional<String> getFilename() {
        if (filename == null) {
            return Optional.empty();
        }
        return filename;
    }

    /**
     * @return Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as <code>&quot;VXNlci0xMA==&quot;</code>) or integer (such as <code>4</code>) input value will be accepted as an ID.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps.
     */
    @JsonIgnore
    public Optional<String> getOptimizedUrl() {
        if (optimizedUrl == null) {
            return Optional.empty();
        }
        return optimizedUrl;
    }

    /**
     * @return The original source URL of the attachment, such as a direct link to S3. This should never be displayed on the client and should always be passed through an Imgproxy transformer.
     */
    @JsonIgnore
    public Optional<String> getSourceUrl() {
        if (sourceUrl == null) {
            return Optional.empty();
        }
        return sourceUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("content_type")
    private Optional<String> _getContentType() {
        return contentType;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("filename")
    private Optional<String> _getFilename() {
        return filename;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("optimized_url")
    private Optional<String> _getOptimizedUrl() {
        return optimizedUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("source_url")
    private Optional<String> _getSourceUrl() {
        return sourceUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CourseListItemThumbnail && equalTo((CourseListItemThumbnail) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CourseListItemThumbnail other) {
        return contentType.equals(other.contentType)
                && filename.equals(other.filename)
                && id.equals(other.id)
                && optimizedUrl.equals(other.optimizedUrl)
                && sourceUrl.equals(other.sourceUrl);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.contentType, this.filename, this.id, this.optimizedUrl, this.sourceUrl);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as <code>&quot;VXNlci0xMA==&quot;</code>) or integer (such as <code>4</code>) input value will be accepted as an ID.</p>
         */
        _FinalStage id(@NotNull String id);

        Builder from(CourseListItemThumbnail other);
    }

    public interface _FinalStage {
        CourseListItemThumbnail build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg.</p>
         */
        _FinalStage contentType(Optional<String> contentType);

        _FinalStage contentType(String contentType);

        _FinalStage contentType(Nullable<String> contentType);

        /**
         * <p>The original filename of the uploaded attachment, including its file extension.</p>
         */
        _FinalStage filename(Optional<String> filename);

        _FinalStage filename(String filename);

        _FinalStage filename(Nullable<String> filename);

        /**
         * <p>A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps.</p>
         */
        _FinalStage optimizedUrl(Optional<String> optimizedUrl);

        _FinalStage optimizedUrl(String optimizedUrl);

        _FinalStage optimizedUrl(Nullable<String> optimizedUrl);

        /**
         * <p>The original source URL of the attachment, such as a direct link to S3. This should never be displayed on the client and should always be passed through an Imgproxy transformer.</p>
         */
        _FinalStage sourceUrl(Optional<String> sourceUrl);

        _FinalStage sourceUrl(String sourceUrl);

        _FinalStage sourceUrl(Nullable<String> sourceUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, _FinalStage {
        private String id;

        private Optional<String> sourceUrl = Optional.empty();

        private Optional<String> optimizedUrl = Optional.empty();

        private Optional<String> filename = Optional.empty();

        private Optional<String> contentType = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CourseListItemThumbnail other) {
            contentType(other.getContentType());
            filename(other.getFilename());
            id(other.getId());
            optimizedUrl(other.getOptimizedUrl());
            sourceUrl(other.getSourceUrl());
            return this;
        }

        /**
         * <p>Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as <code>&quot;VXNlci0xMA==&quot;</code>) or integer (such as <code>4</code>) input value will be accepted as an ID.</p>
         * <p>Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as <code>&quot;VXNlci0xMA==&quot;</code>) or integer (such as <code>4</code>) input value will be accepted as an ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public _FinalStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The original source URL of the attachment, such as a direct link to S3. This should never be displayed on the client and should always be passed through an Imgproxy transformer.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sourceUrl(Nullable<String> sourceUrl) {
            if (sourceUrl.isNull()) {
                this.sourceUrl = null;
            } else if (sourceUrl.isEmpty()) {
                this.sourceUrl = Optional.empty();
            } else {
                this.sourceUrl = Optional.of(sourceUrl.get());
            }
            return this;
        }

        /**
         * <p>The original source URL of the attachment, such as a direct link to S3. This should never be displayed on the client and should always be passed through an Imgproxy transformer.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sourceUrl(String sourceUrl) {
            this.sourceUrl = Optional.ofNullable(sourceUrl);
            return this;
        }

        /**
         * <p>The original source URL of the attachment, such as a direct link to S3. This should never be displayed on the client and should always be passed through an Imgproxy transformer.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "source_url", nulls = Nulls.SKIP)
        public _FinalStage sourceUrl(Optional<String> sourceUrl) {
            this.sourceUrl = sourceUrl;
            return this;
        }

        /**
         * <p>A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage optimizedUrl(Nullable<String> optimizedUrl) {
            if (optimizedUrl.isNull()) {
                this.optimizedUrl = null;
            } else if (optimizedUrl.isEmpty()) {
                this.optimizedUrl = Optional.empty();
            } else {
                this.optimizedUrl = Optional.of(optimizedUrl.get());
            }
            return this;
        }

        /**
         * <p>A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage optimizedUrl(String optimizedUrl) {
            this.optimizedUrl = Optional.ofNullable(optimizedUrl);
            return this;
        }

        /**
         * <p>A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "optimized_url", nulls = Nulls.SKIP)
        public _FinalStage optimizedUrl(Optional<String> optimizedUrl) {
            this.optimizedUrl = optimizedUrl;
            return this;
        }

        /**
         * <p>The original filename of the uploaded attachment, including its file extension.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage filename(Nullable<String> filename) {
            if (filename.isNull()) {
                this.filename = null;
            } else if (filename.isEmpty()) {
                this.filename = Optional.empty();
            } else {
                this.filename = Optional.of(filename.get());
            }
            return this;
        }

        /**
         * <p>The original filename of the uploaded attachment, including its file extension.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage filename(String filename) {
            this.filename = Optional.ofNullable(filename);
            return this;
        }

        /**
         * <p>The original filename of the uploaded attachment, including its file extension.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "filename", nulls = Nulls.SKIP)
        public _FinalStage filename(Optional<String> filename) {
            this.filename = filename;
            return this;
        }

        /**
         * <p>Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage contentType(Nullable<String> contentType) {
            if (contentType.isNull()) {
                this.contentType = null;
            } else if (contentType.isEmpty()) {
                this.contentType = Optional.empty();
            } else {
                this.contentType = Optional.of(contentType.get());
            }
            return this;
        }

        /**
         * <p>Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage contentType(String contentType) {
            this.contentType = Optional.ofNullable(contentType);
            return this;
        }

        /**
         * <p>Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "content_type", nulls = Nulls.SKIP)
        public _FinalStage contentType(Optional<String> contentType) {
            this.contentType = contentType;
            return this;
        }

        @java.lang.Override
        public CourseListItemThumbnail build() {
            return new CourseListItemThumbnail(
                    contentType, filename, id, optimizedUrl, sourceUrl, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
