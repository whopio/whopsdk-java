/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = DetailedTargetingOption.Builder.class)
public final class DetailedTargetingOption {
    private final Optional<Double> audienceSizeLowerBound;

    private final Optional<Double> audienceSizeUpperBound;

    private final Optional<DetailedTargetingOptionBehaviorType> behaviorType;

    private final Optional<String> description;

    private final String id;

    private final String name;

    private final Map<String, Object> additionalProperties;

    private DetailedTargetingOption(
            Optional<Double> audienceSizeLowerBound,
            Optional<Double> audienceSizeUpperBound,
            Optional<DetailedTargetingOptionBehaviorType> behaviorType,
            Optional<String> description,
            String id,
            String name,
            Map<String, Object> additionalProperties) {
        this.audienceSizeLowerBound = audienceSizeLowerBound;
        this.audienceSizeUpperBound = audienceSizeUpperBound;
        this.behaviorType = behaviorType;
        this.description = description;
        this.id = id;
        this.name = name;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Low end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.
     */
    @JsonIgnore
    public Optional<Double> getAudienceSizeLowerBound() {
        if (audienceSizeLowerBound == null) {
            return Optional.empty();
        }
        return audienceSizeLowerBound;
    }

    /**
     * @return High end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.
     */
    @JsonIgnore
    public Optional<Double> getAudienceSizeUpperBound() {
        if (audienceSizeUpperBound == null) {
            return Optional.empty();
        }
        return audienceSizeUpperBound;
    }

    /**
     * @return What a behavior category is measured on, on ad platforms that scope them. Send it back on the <code>detailed_targeting.behaviors</code> entry alongside the id. Null for options that aren't scoped.
     */
    @JsonIgnore
    public Optional<DetailedTargetingOptionBehaviorType> getBehaviorType() {
        if (behaviorType == null) {
            return Optional.empty();
        }
        return behaviorType;
    }

    /**
     * @return The ad platform's description of who the option covers, when it publishes one.
     */
    @JsonIgnore
    public Optional<String> getDescription() {
        if (description == null) {
            return Optional.empty();
        }
        return description;
    }

    /**
     * @return The ad platform's ID for the option in its targeting taxonomy. Use it as the <code>id</code> of a <code>detailed_targeting</code> entry.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Display name, such as <code>Movies</code>.
     */
    @JsonProperty("name")
    public String getName() {
        return name;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("audience_size_lower_bound")
    private Optional<Double> _getAudienceSizeLowerBound() {
        return audienceSizeLowerBound;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("audience_size_upper_bound")
    private Optional<Double> _getAudienceSizeUpperBound() {
        return audienceSizeUpperBound;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("behavior_type")
    private Optional<DetailedTargetingOptionBehaviorType> _getBehaviorType() {
        return behaviorType;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("description")
    private Optional<String> _getDescription() {
        return description;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof DetailedTargetingOption && equalTo((DetailedTargetingOption) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(DetailedTargetingOption other) {
        return audienceSizeLowerBound.equals(other.audienceSizeLowerBound)
                && audienceSizeUpperBound.equals(other.audienceSizeUpperBound)
                && behaviorType.equals(other.behaviorType)
                && description.equals(other.description)
                && id.equals(other.id)
                && name.equals(other.name);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.audienceSizeLowerBound,
                this.audienceSizeUpperBound,
                this.behaviorType,
                this.description,
                this.id,
                this.name);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>The ad platform's ID for the option in its targeting taxonomy. Use it as the <code>id</code> of a <code>detailed_targeting</code> entry.</p>
         */
        NameStage id(@NotNull String id);

        Builder from(DetailedTargetingOption other);
    }

    public interface NameStage {
        /**
         * <p>Display name, such as <code>Movies</code>.</p>
         */
        _FinalStage name(@NotNull String name);
    }

    public interface _FinalStage {
        DetailedTargetingOption build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Low end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         */
        _FinalStage audienceSizeLowerBound(Optional<Double> audienceSizeLowerBound);

        _FinalStage audienceSizeLowerBound(Double audienceSizeLowerBound);

        _FinalStage audienceSizeLowerBound(Nullable<Double> audienceSizeLowerBound);

        /**
         * <p>High end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         */
        _FinalStage audienceSizeUpperBound(Optional<Double> audienceSizeUpperBound);

        _FinalStage audienceSizeUpperBound(Double audienceSizeUpperBound);

        _FinalStage audienceSizeUpperBound(Nullable<Double> audienceSizeUpperBound);

        /**
         * <p>What a behavior category is measured on, on ad platforms that scope them. Send it back on the <code>detailed_targeting.behaviors</code> entry alongside the id. Null for options that aren't scoped.</p>
         */
        _FinalStage behaviorType(Optional<DetailedTargetingOptionBehaviorType> behaviorType);

        _FinalStage behaviorType(DetailedTargetingOptionBehaviorType behaviorType);

        _FinalStage behaviorType(Nullable<DetailedTargetingOptionBehaviorType> behaviorType);

        /**
         * <p>The ad platform's description of who the option covers, when it publishes one.</p>
         */
        _FinalStage description(Optional<String> description);

        _FinalStage description(String description);

        _FinalStage description(Nullable<String> description);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, NameStage, _FinalStage {
        private String id;

        private String name;

        private Optional<String> description = Optional.empty();

        private Optional<DetailedTargetingOptionBehaviorType> behaviorType = Optional.empty();

        private Optional<Double> audienceSizeUpperBound = Optional.empty();

        private Optional<Double> audienceSizeLowerBound = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(DetailedTargetingOption other) {
            audienceSizeLowerBound(other.getAudienceSizeLowerBound());
            audienceSizeUpperBound(other.getAudienceSizeUpperBound());
            behaviorType(other.getBehaviorType());
            description(other.getDescription());
            id(other.getId());
            name(other.getName());
            return this;
        }

        /**
         * <p>The ad platform's ID for the option in its targeting taxonomy. Use it as the <code>id</code> of a <code>detailed_targeting</code> entry.</p>
         * <p>The ad platform's ID for the option in its targeting taxonomy. Use it as the <code>id</code> of a <code>detailed_targeting</code> entry.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public NameStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Display name, such as <code>Movies</code>.</p>
         * <p>Display name, such as <code>Movies</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("name")
        public _FinalStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>The ad platform's description of who the option covers, when it publishes one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage description(Nullable<String> description) {
            if (description.isNull()) {
                this.description = null;
            } else if (description.isEmpty()) {
                this.description = Optional.empty();
            } else {
                this.description = Optional.of(description.get());
            }
            return this;
        }

        /**
         * <p>The ad platform's description of who the option covers, when it publishes one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage description(String description) {
            this.description = Optional.ofNullable(description);
            return this;
        }

        /**
         * <p>The ad platform's description of who the option covers, when it publishes one.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "description", nulls = Nulls.SKIP)
        public _FinalStage description(Optional<String> description) {
            this.description = description;
            return this;
        }

        /**
         * <p>What a behavior category is measured on, on ad platforms that scope them. Send it back on the <code>detailed_targeting.behaviors</code> entry alongside the id. Null for options that aren't scoped.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage behaviorType(Nullable<DetailedTargetingOptionBehaviorType> behaviorType) {
            if (behaviorType.isNull()) {
                this.behaviorType = null;
            } else if (behaviorType.isEmpty()) {
                this.behaviorType = Optional.empty();
            } else {
                this.behaviorType = Optional.of(behaviorType.get());
            }
            return this;
        }

        /**
         * <p>What a behavior category is measured on, on ad platforms that scope them. Send it back on the <code>detailed_targeting.behaviors</code> entry alongside the id. Null for options that aren't scoped.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage behaviorType(DetailedTargetingOptionBehaviorType behaviorType) {
            this.behaviorType = Optional.ofNullable(behaviorType);
            return this;
        }

        /**
         * <p>What a behavior category is measured on, on ad platforms that scope them. Send it back on the <code>detailed_targeting.behaviors</code> entry alongside the id. Null for options that aren't scoped.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "behavior_type", nulls = Nulls.SKIP)
        public _FinalStage behaviorType(Optional<DetailedTargetingOptionBehaviorType> behaviorType) {
            this.behaviorType = behaviorType;
            return this;
        }

        /**
         * <p>High end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage audienceSizeUpperBound(Nullable<Double> audienceSizeUpperBound) {
            if (audienceSizeUpperBound.isNull()) {
                this.audienceSizeUpperBound = null;
            } else if (audienceSizeUpperBound.isEmpty()) {
                this.audienceSizeUpperBound = Optional.empty();
            } else {
                this.audienceSizeUpperBound = Optional.of(audienceSizeUpperBound.get());
            }
            return this;
        }

        /**
         * <p>High end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage audienceSizeUpperBound(Double audienceSizeUpperBound) {
            this.audienceSizeUpperBound = Optional.ofNullable(audienceSizeUpperBound);
            return this;
        }

        /**
         * <p>High end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "audience_size_upper_bound", nulls = Nulls.SKIP)
        public _FinalStage audienceSizeUpperBound(Optional<Double> audienceSizeUpperBound) {
            this.audienceSizeUpperBound = audienceSizeUpperBound;
            return this;
        }

        /**
         * <p>Low end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage audienceSizeLowerBound(Nullable<Double> audienceSizeLowerBound) {
            if (audienceSizeLowerBound.isNull()) {
                this.audienceSizeLowerBound = null;
            } else if (audienceSizeLowerBound.isEmpty()) {
                this.audienceSizeLowerBound = Optional.empty();
            } else {
                this.audienceSizeLowerBound = Optional.of(audienceSizeLowerBound.get());
            }
            return this;
        }

        /**
         * <p>Low end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage audienceSizeLowerBound(Double audienceSizeLowerBound) {
            this.audienceSizeLowerBound = Optional.ofNullable(audienceSizeLowerBound);
            return this;
        }

        /**
         * <p>Low end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "audience_size_lower_bound", nulls = Nulls.SKIP)
        public _FinalStage audienceSizeLowerBound(Optional<Double> audienceSizeLowerBound) {
            this.audienceSizeLowerBound = audienceSizeLowerBound;
            return this;
        }

        @java.lang.Override
        public DetailedTargetingOption build() {
            return new DetailedTargetingOption(
                    audienceSizeLowerBound,
                    audienceSizeUpperBound,
                    behaviorType,
                    description,
                    id,
                    name,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
