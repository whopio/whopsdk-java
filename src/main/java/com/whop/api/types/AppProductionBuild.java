/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AppProductionBuild.Builder.class)
public final class AppProductionBuild {
    private final Optional<String> checksum;

    private final Optional<String> fileUrl;

    private final String id;

    private final Optional<String> sourceUrl;

    private final AppProductionBuildStatus status;

    private final Map<String, Object> additionalProperties;

    private AppProductionBuild(
            Optional<String> checksum,
            Optional<String> fileUrl,
            String id,
            Optional<String> sourceUrl,
            AppProductionBuildStatus status,
            Map<String, Object> additionalProperties) {
        this.checksum = checksum;
        this.fileUrl = fileUrl;
        this.id = id;
        this.sourceUrl = sourceUrl;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Client-generated checksum of the build file, used to verify file integrity.
     */
    @JsonIgnore
    public Optional<String> getChecksum() {
        if (checksum == null) {
            return Optional.empty();
        }
        return checksum;
    }

    /**
     * @return URL to download the uploaded build artifact.
     */
    @JsonIgnore
    public Optional<String> getFileUrl() {
        if (fileUrl == null) {
            return Optional.empty();
        }
        return fileUrl;
    }

    /**
     * @return App build ID, prefixed <code>abld_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return URL to download the compressed source code archive that produced this build, or <code>null</code> when the build was uploaded without a source archive.
     */
    @JsonIgnore
    public Optional<String> getSourceUrl() {
        if (sourceUrl == null) {
            return Optional.empty();
        }
        return sourceUrl;
    }

    /**
     * @return The build's review status.
     */
    @JsonProperty("status")
    public AppProductionBuildStatus getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("checksum")
    private Optional<String> _getChecksum() {
        return checksum;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("file_url")
    private Optional<String> _getFileUrl() {
        return fileUrl;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("source_url")
    private Optional<String> _getSourceUrl() {
        return sourceUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AppProductionBuild && equalTo((AppProductionBuild) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AppProductionBuild other) {
        return checksum.equals(other.checksum)
                && fileUrl.equals(other.fileUrl)
                && id.equals(other.id)
                && sourceUrl.equals(other.sourceUrl)
                && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.checksum, this.fileUrl, this.id, this.sourceUrl, this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>App build ID, prefixed <code>abld_</code>.</p>
         */
        StatusStage id(@NotNull String id);

        Builder from(AppProductionBuild other);
    }

    public interface StatusStage {
        /**
         * <p>The build's review status.</p>
         */
        _FinalStage status(@NotNull AppProductionBuildStatus status);
    }

    public interface _FinalStage {
        AppProductionBuild build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Client-generated checksum of the build file, used to verify file integrity.</p>
         */
        _FinalStage checksum(Optional<String> checksum);

        _FinalStage checksum(String checksum);

        _FinalStage checksum(Nullable<String> checksum);

        /**
         * <p>URL to download the uploaded build artifact.</p>
         */
        _FinalStage fileUrl(Optional<String> fileUrl);

        _FinalStage fileUrl(String fileUrl);

        _FinalStage fileUrl(Nullable<String> fileUrl);

        /**
         * <p>URL to download the compressed source code archive that produced this build, or <code>null</code> when the build was uploaded without a source archive.</p>
         */
        _FinalStage sourceUrl(Optional<String> sourceUrl);

        _FinalStage sourceUrl(String sourceUrl);

        _FinalStage sourceUrl(Nullable<String> sourceUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, StatusStage, _FinalStage {
        private String id;

        private AppProductionBuildStatus status;

        private Optional<String> sourceUrl = Optional.empty();

        private Optional<String> fileUrl = Optional.empty();

        private Optional<String> checksum = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AppProductionBuild other) {
            checksum(other.getChecksum());
            fileUrl(other.getFileUrl());
            id(other.getId());
            sourceUrl(other.getSourceUrl());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>App build ID, prefixed <code>abld_</code>.</p>
         * <p>App build ID, prefixed <code>abld_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public StatusStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>The build's review status.</p>
         * <p>The build's review status.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull AppProductionBuildStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>URL to download the compressed source code archive that produced this build, or <code>null</code> when the build was uploaded without a source archive.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sourceUrl(Nullable<String> sourceUrl) {
            if (sourceUrl.isNull()) {
                this.sourceUrl = null;
            } else if (sourceUrl.isEmpty()) {
                this.sourceUrl = Optional.empty();
            } else {
                this.sourceUrl = Optional.of(sourceUrl.get());
            }
            return this;
        }

        /**
         * <p>URL to download the compressed source code archive that produced this build, or <code>null</code> when the build was uploaded without a source archive.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sourceUrl(String sourceUrl) {
            this.sourceUrl = Optional.ofNullable(sourceUrl);
            return this;
        }

        /**
         * <p>URL to download the compressed source code archive that produced this build, or <code>null</code> when the build was uploaded without a source archive.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "source_url", nulls = Nulls.SKIP)
        public _FinalStage sourceUrl(Optional<String> sourceUrl) {
            this.sourceUrl = sourceUrl;
            return this;
        }

        /**
         * <p>URL to download the uploaded build artifact.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fileUrl(Nullable<String> fileUrl) {
            if (fileUrl.isNull()) {
                this.fileUrl = null;
            } else if (fileUrl.isEmpty()) {
                this.fileUrl = Optional.empty();
            } else {
                this.fileUrl = Optional.of(fileUrl.get());
            }
            return this;
        }

        /**
         * <p>URL to download the uploaded build artifact.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fileUrl(String fileUrl) {
            this.fileUrl = Optional.ofNullable(fileUrl);
            return this;
        }

        /**
         * <p>URL to download the uploaded build artifact.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "file_url", nulls = Nulls.SKIP)
        public _FinalStage fileUrl(Optional<String> fileUrl) {
            this.fileUrl = fileUrl;
            return this;
        }

        /**
         * <p>Client-generated checksum of the build file, used to verify file integrity.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage checksum(Nullable<String> checksum) {
            if (checksum.isNull()) {
                this.checksum = null;
            } else if (checksum.isEmpty()) {
                this.checksum = Optional.empty();
            } else {
                this.checksum = Optional.of(checksum.get());
            }
            return this;
        }

        /**
         * <p>Client-generated checksum of the build file, used to verify file integrity.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage checksum(String checksum) {
            this.checksum = Optional.ofNullable(checksum);
            return this;
        }

        /**
         * <p>Client-generated checksum of the build file, used to verify file integrity.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "checksum", nulls = Nulls.SKIP)
        public _FinalStage checksum(Optional<String> checksum) {
            this.checksum = checksum;
            return this;
        }

        @java.lang.Override
        public AppProductionBuild build() {
            return new AppProductionBuild(checksum, fileUrl, id, sourceUrl, status, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
