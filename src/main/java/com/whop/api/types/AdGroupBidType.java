/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AdGroupBidType {
    public static final AdGroupBidType AVERAGE_TARGET = new AdGroupBidType(Value.AVERAGE_TARGET, "average_target");

    public static final AdGroupBidType MAXIMUM_TARGET = new AdGroupBidType(Value.MAXIMUM_TARGET, "maximum_target");

    public static final AdGroupBidType MINIMUM_COST = new AdGroupBidType(Value.MINIMUM_COST, "minimum_cost");

    private final Value value;

    private final String string;

    AdGroupBidType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof AdGroupBidType && this.string.equals(((AdGroupBidType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case AVERAGE_TARGET:
                return visitor.visitAverageTarget();
            case MAXIMUM_TARGET:
                return visitor.visitMaximumTarget();
            case MINIMUM_COST:
                return visitor.visitMinimumCost();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AdGroupBidType valueOf(String value) {
        switch (value) {
            case "average_target":
                return AVERAGE_TARGET;
            case "maximum_target":
                return MAXIMUM_TARGET;
            case "minimum_cost":
                return MINIMUM_COST;
            default:
                return new AdGroupBidType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        MINIMUM_COST,

        AVERAGE_TARGET,

        MAXIMUM_TARGET,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitMinimumCost();

        T visitAverageTarget();

        T visitMaximumTarget();

        T visitUnknown(String unknownType);
    }
}
