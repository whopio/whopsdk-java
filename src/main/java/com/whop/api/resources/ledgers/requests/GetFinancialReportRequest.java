/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.ledgers.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import com.whop.api.resources.ledgers.types.GetFinancialReportRequestGroupBy;
import com.whop.api.resources.ledgers.types.GetFinancialReportRequestReportType;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = GetFinancialReportRequest.Builder.class)
public final class GetFinancialReportRequest {
    private final String accountId;

    private final GetFinancialReportRequestReportType reportType;

    private final Optional<String> currency;

    private final Optional<String> inCurrency;

    private final Optional<String> fromDate;

    private final Optional<String> toDate;

    private final Optional<GetFinancialReportRequestGroupBy> groupBy;

    private final Optional<String> timezone;

    private final Optional<Boolean> cumulative;

    private final Optional<String> scopeAccountId;

    private final Map<String, Object> additionalProperties;

    private GetFinancialReportRequest(
            String accountId,
            GetFinancialReportRequestReportType reportType,
            Optional<String> currency,
            Optional<String> inCurrency,
            Optional<String> fromDate,
            Optional<String> toDate,
            Optional<GetFinancialReportRequestGroupBy> groupBy,
            Optional<String> timezone,
            Optional<Boolean> cumulative,
            Optional<String> scopeAccountId,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.reportType = reportType;
        this.currency = currency;
        this.inCurrency = inCurrency;
        this.fromDate = fromDate;
        this.toDate = toDate;
        this.groupBy = groupBy;
        this.timezone = timezone;
        this.cumulative = cumulative;
        this.scopeAccountId = scopeAccountId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The owning account ID (a biz_ identifier), or <code>global</code> for a platform-wide report across all ledger accounts (requires internal admin access).
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return The type of financial report to generate.
     */
    @JsonProperty("report_type")
    public GetFinancialReportRequestReportType getReportType() {
        return reportType;
    }

    /**
     * @return Filter rows to this currency, for example <code>usd</code>. Defaults to <code>usd</code> unless <code>in_currency</code> is provided.
     */
    @JsonProperty("currency")
    public Optional<String> getCurrency() {
        return currency;
    }

    /**
     * @return Aggregate all activity into this display currency via FX conversion.
     */
    @JsonProperty("in_currency")
    public Optional<String> getInCurrency() {
        return inCurrency;
    }

    /**
     * @return Start of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.
     */
    @JsonProperty("from_date")
    public Optional<String> getFromDate() {
        return fromDate;
    }

    /**
     * @return End of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.
     */
    @JsonProperty("to_date")
    public Optional<String> getToDate() {
        return toDate;
    }

    /**
     * @return Grouping granularity for report rows.
     */
    @JsonProperty("group_by")
    public Optional<GetFinancialReportRequestGroupBy> getGroupBy() {
        return groupBy;
    }

    /**
     * @return IANA timezone (for example <code>America/New_York</code>) used to bucket report periods and to interpret calendar-day boundaries for balance snapshots. Defaults to UTC. from_date/to_date remain exact instants regardless of this setting.
     */
    @JsonProperty("timezone")
    public Optional<String> getTimezone() {
        return timezone;
    }

    /**
     * @return Platform-wide (global) reports only: when true, return cumulative balances as of to_date (all history, no lower bound) instead of activity within the period.
     */
    @JsonProperty("cumulative")
    public Optional<Boolean> getCumulative() {
        return cumulative;
    }

    /**
     * @return Platform-wide (global) reports only: narrow the report to ledger lines on the ledger account owned by this account ID (a biz_ identifier). Ignored unless account_id is <code>global</code>.
     */
    @JsonProperty("scope_account_id")
    public Optional<String> getScopeAccountId() {
        return scopeAccountId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof GetFinancialReportRequest && equalTo((GetFinancialReportRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(GetFinancialReportRequest other) {
        return accountId.equals(other.accountId)
                && reportType.equals(other.reportType)
                && currency.equals(other.currency)
                && inCurrency.equals(other.inCurrency)
                && fromDate.equals(other.fromDate)
                && toDate.equals(other.toDate)
                && groupBy.equals(other.groupBy)
                && timezone.equals(other.timezone)
                && cumulative.equals(other.cumulative)
                && scopeAccountId.equals(other.scopeAccountId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.reportType,
                this.currency,
                this.inCurrency,
                this.fromDate,
                this.toDate,
                this.groupBy,
                this.timezone,
                this.cumulative,
                this.scopeAccountId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountIdStage builder() {
        return new Builder();
    }

    public interface AccountIdStage {
        /**
         * <p>The owning account ID (a biz_ identifier), or <code>global</code> for a platform-wide report across all ledger accounts (requires internal admin access).</p>
         */
        ReportTypeStage accountId(@NotNull String accountId);

        Builder from(GetFinancialReportRequest other);
    }

    public interface ReportTypeStage {
        /**
         * <p>The type of financial report to generate.</p>
         */
        _FinalStage reportType(@NotNull GetFinancialReportRequestReportType reportType);
    }

    public interface _FinalStage {
        GetFinancialReportRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Filter rows to this currency, for example <code>usd</code>. Defaults to <code>usd</code> unless <code>in_currency</code> is provided.</p>
         */
        _FinalStage currency(Optional<String> currency);

        _FinalStage currency(String currency);

        /**
         * <p>Aggregate all activity into this display currency via FX conversion.</p>
         */
        _FinalStage inCurrency(Optional<String> inCurrency);

        _FinalStage inCurrency(String inCurrency);

        /**
         * <p>Start of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.</p>
         */
        _FinalStage fromDate(Optional<String> fromDate);

        _FinalStage fromDate(String fromDate);

        /**
         * <p>End of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.</p>
         */
        _FinalStage toDate(Optional<String> toDate);

        _FinalStage toDate(String toDate);

        /**
         * <p>Grouping granularity for report rows.</p>
         */
        _FinalStage groupBy(Optional<GetFinancialReportRequestGroupBy> groupBy);

        _FinalStage groupBy(GetFinancialReportRequestGroupBy groupBy);

        /**
         * <p>IANA timezone (for example <code>America/New_York</code>) used to bucket report periods and to interpret calendar-day boundaries for balance snapshots. Defaults to UTC. from_date/to_date remain exact instants regardless of this setting.</p>
         */
        _FinalStage timezone(Optional<String> timezone);

        _FinalStage timezone(String timezone);

        /**
         * <p>Platform-wide (global) reports only: when true, return cumulative balances as of to_date (all history, no lower bound) instead of activity within the period.</p>
         */
        _FinalStage cumulative(Optional<Boolean> cumulative);

        _FinalStage cumulative(Boolean cumulative);

        /**
         * <p>Platform-wide (global) reports only: narrow the report to ledger lines on the ledger account owned by this account ID (a biz_ identifier). Ignored unless account_id is <code>global</code>.</p>
         */
        _FinalStage scopeAccountId(Optional<String> scopeAccountId);

        _FinalStage scopeAccountId(String scopeAccountId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AccountIdStage, ReportTypeStage, _FinalStage {
        private String accountId;

        private GetFinancialReportRequestReportType reportType;

        private Optional<String> scopeAccountId = Optional.empty();

        private Optional<Boolean> cumulative = Optional.empty();

        private Optional<String> timezone = Optional.empty();

        private Optional<GetFinancialReportRequestGroupBy> groupBy = Optional.empty();

        private Optional<String> toDate = Optional.empty();

        private Optional<String> fromDate = Optional.empty();

        private Optional<String> inCurrency = Optional.empty();

        private Optional<String> currency = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(GetFinancialReportRequest other) {
            accountId(other.getAccountId());
            reportType(other.getReportType());
            currency(other.getCurrency());
            inCurrency(other.getInCurrency());
            fromDate(other.getFromDate());
            toDate(other.getToDate());
            groupBy(other.getGroupBy());
            timezone(other.getTimezone());
            cumulative(other.getCumulative());
            scopeAccountId(other.getScopeAccountId());
            return this;
        }

        /**
         * <p>The owning account ID (a biz_ identifier), or <code>global</code> for a platform-wide report across all ledger accounts (requires internal admin access).</p>
         * <p>The owning account ID (a biz_ identifier), or <code>global</code> for a platform-wide report across all ledger accounts (requires internal admin access).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public ReportTypeStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        /**
         * <p>The type of financial report to generate.</p>
         * <p>The type of financial report to generate.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("report_type")
        public _FinalStage reportType(@NotNull GetFinancialReportRequestReportType reportType) {
            this.reportType = Objects.requireNonNull(reportType, "reportType must not be null");
            return this;
        }

        /**
         * <p>Platform-wide (global) reports only: narrow the report to ledger lines on the ledger account owned by this account ID (a biz_ identifier). Ignored unless account_id is <code>global</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage scopeAccountId(String scopeAccountId) {
            this.scopeAccountId = Optional.ofNullable(scopeAccountId);
            return this;
        }

        /**
         * <p>Platform-wide (global) reports only: narrow the report to ledger lines on the ledger account owned by this account ID (a biz_ identifier). Ignored unless account_id is <code>global</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "scope_account_id", nulls = Nulls.SKIP)
        public _FinalStage scopeAccountId(Optional<String> scopeAccountId) {
            this.scopeAccountId = scopeAccountId;
            return this;
        }

        /**
         * <p>Platform-wide (global) reports only: when true, return cumulative balances as of to_date (all history, no lower bound) instead of activity within the period.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage cumulative(Boolean cumulative) {
            this.cumulative = Optional.ofNullable(cumulative);
            return this;
        }

        /**
         * <p>Platform-wide (global) reports only: when true, return cumulative balances as of to_date (all history, no lower bound) instead of activity within the period.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "cumulative", nulls = Nulls.SKIP)
        public _FinalStage cumulative(Optional<Boolean> cumulative) {
            this.cumulative = cumulative;
            return this;
        }

        /**
         * <p>IANA timezone (for example <code>America/New_York</code>) used to bucket report periods and to interpret calendar-day boundaries for balance snapshots. Defaults to UTC. from_date/to_date remain exact instants regardless of this setting.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage timezone(String timezone) {
            this.timezone = Optional.ofNullable(timezone);
            return this;
        }

        /**
         * <p>IANA timezone (for example <code>America/New_York</code>) used to bucket report periods and to interpret calendar-day boundaries for balance snapshots. Defaults to UTC. from_date/to_date remain exact instants regardless of this setting.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "timezone", nulls = Nulls.SKIP)
        public _FinalStage timezone(Optional<String> timezone) {
            this.timezone = timezone;
            return this;
        }

        /**
         * <p>Grouping granularity for report rows.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage groupBy(GetFinancialReportRequestGroupBy groupBy) {
            this.groupBy = Optional.ofNullable(groupBy);
            return this;
        }

        /**
         * <p>Grouping granularity for report rows.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "group_by", nulls = Nulls.SKIP)
        public _FinalStage groupBy(Optional<GetFinancialReportRequestGroupBy> groupBy) {
            this.groupBy = groupBy;
            return this;
        }

        /**
         * <p>End of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toDate(String toDate) {
            this.toDate = Optional.ofNullable(toDate);
            return this;
        }

        /**
         * <p>End of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_date", nulls = Nulls.SKIP)
        public _FinalStage toDate(Optional<String> toDate) {
            this.toDate = toDate;
            return this;
        }

        /**
         * <p>Start of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromDate(String fromDate) {
            this.fromDate = Optional.ofNullable(fromDate);
            return this;
        }

        /**
         * <p>Start of the report window as an ISO 8601 timestamp (UTC). Required for platform-wide (global) reports.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "from_date", nulls = Nulls.SKIP)
        public _FinalStage fromDate(Optional<String> fromDate) {
            this.fromDate = fromDate;
            return this;
        }

        /**
         * <p>Aggregate all activity into this display currency via FX conversion.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage inCurrency(String inCurrency) {
            this.inCurrency = Optional.ofNullable(inCurrency);
            return this;
        }

        /**
         * <p>Aggregate all activity into this display currency via FX conversion.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "in_currency", nulls = Nulls.SKIP)
        public _FinalStage inCurrency(Optional<String> inCurrency) {
            this.inCurrency = inCurrency;
            return this;
        }

        /**
         * <p>Filter rows to this currency, for example <code>usd</code>. Defaults to <code>usd</code> unless <code>in_currency</code> is provided.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(String currency) {
            this.currency = Optional.ofNullable(currency);
            return this;
        }

        /**
         * <p>Filter rows to this currency, for example <code>usd</code>. Defaults to <code>usd</code> unless <code>in_currency</code> is provided.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "currency", nulls = Nulls.SKIP)
        public _FinalStage currency(Optional<String> currency) {
            this.currency = currency;
            return this;
        }

        @java.lang.Override
        public GetFinancialReportRequest build() {
            return new GetFinancialReportRequest(
                    accountId,
                    reportType,
                    currency,
                    inCurrency,
                    fromDate,
                    toDate,
                    groupBy,
                    timezone,
                    cumulative,
                    scopeAccountId,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
