/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.payouts.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PostWithdrawalCreatedPayloadData.Builder.class)
public final class PostWithdrawalCreatedPayloadData {
    private final String amount;

    private final OffsetDateTime createdAt;

    private final String currency;

    private final Optional<String> destinationAmount;

    private final Optional<String> destinationCurrency;

    private final Optional<OffsetDateTime> estimatedArrival;

    private final Optional<Float> exchangeRate;

    private final Optional<PostWithdrawalCreatedPayloadDataFailure> failure;

    private final String feeAmount;

    private final PostWithdrawalCreatedPayloadDataFeePaidBy feePaidBy;

    private final String id;

    private final String markupFee;

    private final Map<String, String> metadata;

    private final String netAmount;

    private final Optional<String> notes;

    private final PostWithdrawalCreatedPayloadDataObject object;

    private final Optional<String> payerName;

    private final Optional<PostWithdrawalCreatedPayloadDataPayoutMethod> payoutMethod;

    private final Optional<String> payoutRequestId;

    private final Optional<PostWithdrawalCreatedPayloadDataSource> source;

    private final PostWithdrawalCreatedPayloadDataSpeed speed;

    private final PostWithdrawalCreatedPayloadDataStatus status;

    private final String statusDetail;

    private final Optional<String> traceCode;

    private final Map<String, Object> additionalProperties;

    private PostWithdrawalCreatedPayloadData(
            String amount,
            OffsetDateTime createdAt,
            String currency,
            Optional<String> destinationAmount,
            Optional<String> destinationCurrency,
            Optional<OffsetDateTime> estimatedArrival,
            Optional<Float> exchangeRate,
            Optional<PostWithdrawalCreatedPayloadDataFailure> failure,
            String feeAmount,
            PostWithdrawalCreatedPayloadDataFeePaidBy feePaidBy,
            String id,
            String markupFee,
            Map<String, String> metadata,
            String netAmount,
            Optional<String> notes,
            PostWithdrawalCreatedPayloadDataObject object,
            Optional<String> payerName,
            Optional<PostWithdrawalCreatedPayloadDataPayoutMethod> payoutMethod,
            Optional<String> payoutRequestId,
            Optional<PostWithdrawalCreatedPayloadDataSource> source,
            PostWithdrawalCreatedPayloadDataSpeed speed,
            PostWithdrawalCreatedPayloadDataStatus status,
            String statusDetail,
            Optional<String> traceCode,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.createdAt = createdAt;
        this.currency = currency;
        this.destinationAmount = destinationAmount;
        this.destinationCurrency = destinationCurrency;
        this.estimatedArrival = estimatedArrival;
        this.exchangeRate = exchangeRate;
        this.failure = failure;
        this.feeAmount = feeAmount;
        this.feePaidBy = feePaidBy;
        this.id = id;
        this.markupFee = markupFee;
        this.metadata = metadata;
        this.netAmount = netAmount;
        this.notes = notes;
        this.object = object;
        this.payerName = payerName;
        this.payoutMethod = payoutMethod;
        this.payoutRequestId = payoutRequestId;
        this.source = source;
        this.speed = speed;
        this.status = status;
        this.statusDetail = statusDetail;
        this.traceCode = traceCode;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The payout amount in whole currency units, as a decimal string.
     */
    @JsonProperty("amount")
    public String getAmount() {
        return amount;
    }

    /**
     * @return When the payout was created.
     */
    @JsonProperty("created_at")
    public OffsetDateTime getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Payout currency.
     */
    @JsonProperty("currency")
    public String getCurrency() {
        return currency;
    }

    /**
     * @return The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded conversion.
     */
    @JsonIgnore
    public Optional<String> getDestinationAmount() {
        if (destinationAmount == null) {
            return Optional.empty();
        }
        return destinationAmount;
    }

    /**
     * @return Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the <code>GET /payouts</code> row carrying this payout's id as <code>payout_request_id</code> — and is <code>null</code> only when no settlement payout exists.
     */
    @JsonIgnore
    public Optional<String> getDestinationCurrency() {
        if (destinationCurrency == null) {
            return Optional.empty();
        }
        return destinationCurrency;
    }

    /**
     * @return Estimated time the funds become available in the destination account.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getEstimatedArrival() {
        if (estimatedArrival == null) {
            return Optional.empty();
        }
        return estimatedArrival;
    }

    /**
     * @return Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded rate.
     */
    @JsonIgnore
    public Optional<Float> getExchangeRate() {
        if (exchangeRate == null) {
            return Optional.empty();
        }
        return exchangeRate;
    }

    /**
     * @return Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; <code>null</code> otherwise.
     */
    @JsonIgnore
    public Optional<PostWithdrawalCreatedPayloadDataFailure> getFailure() {
        if (failure == null) {
            return Optional.empty();
        }
        return failure;
    }

    /**
     * @return The fee charged for the payout, in the payout currency, as a decimal string.
     */
    @JsonProperty("fee_amount")
    public String getFeeAmount() {
        return feeAmount;
    }

    /**
     * @return Who bore the payout fee: the account itself, or its parent platform.
     */
    @JsonProperty("fee_paid_by")
    public PostWithdrawalCreatedPayloadDataFeePaidBy getFeePaidBy() {
        return feePaidBy;
    }

    /**
     * @return Payout ID, prefixed <code>wdrl_</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Whop's markup on the provider fee, in the payout currency, as a decimal string. <code>&quot;0.0&quot;</code> when none applies.
     */
    @JsonProperty("markup_fee")
    public String getMarkupFee() {
        return markupFee;
    }

    /**
     * @return Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters.
     */
    @JsonProperty("metadata")
    public Map<String, String> getMetadata() {
        return metadata;
    }

    /**
     * @return The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is <code>self</code>; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and <code>failure</code> says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: <code>amount</code> carries what stayed in the balance, fees are zero because none were charged, and <code>net_amount</code> is 0 because nothing was delivered.
     */
    @JsonProperty("net_amount")
    public String getNetAmount() {
        return netAmount;
    }

    /**
     * @return Free-form notes attached by the payout creator, or <code>null</code> when none were provided. Maximum 255 characters.
     */
    @JsonIgnore
    public Optional<String> getNotes() {
        if (notes == null) {
            return Optional.empty();
        }
        return notes;
    }

    @JsonProperty("object")
    public PostWithdrawalCreatedPayloadDataObject getObject() {
        return object;
    }

    /**
     * @return Name of the entity processing the payout.
     */
    @JsonIgnore
    public Optional<String> getPayerName() {
        if (payerName == null) {
            return Optional.empty();
        }
        return payerName;
    }

    /**
     * @return The saved payout method used. Requires payout:destination:read; null without it.
     */
    @JsonIgnore
    public Optional<PostWithdrawalCreatedPayloadDataPayoutMethod> getPayoutMethod() {
        if (payoutMethod == null) {
            return Optional.empty();
        }
        return payoutMethod;
    }

    /**
     * @return Payout request ID, prefixed <code>cofr_</code>, returned by <code>POST /payouts</code>. Match it to the settled payout in <code>GET /payouts</code>. Returns <code>null</code> for payouts not created by <code>POST /payouts</code>.
     */
    @JsonIgnore
    public Optional<String> getPayoutRequestId() {
        if (payoutRequestId == null) {
            return Optional.empty();
        }
        return payoutRequestId;
    }

    /**
     * @return How the payout was created. <code>automatic</code> means a scheduled auto-payout; <code>null</code> on payouts created before source tracking or through internal tooling.
     */
    @JsonIgnore
    public Optional<PostWithdrawalCreatedPayloadDataSource> getSource() {
        if (source == null) {
            return Optional.empty();
        }
        return source;
    }

    /**
     * @return Payout delivery speed.
     */
    @JsonProperty("speed")
    public PostWithdrawalCreatedPayloadDataSpeed getSpeed() {
        return speed;
    }

    /**
     * @return Current payout status.
     */
    @JsonProperty("status")
    public PostWithdrawalCreatedPayloadDataStatus getStatus() {
        return status;
    }

    /**
     * @return The finest machine phase under <code>status</code> — for example <code>awaiting_provider_acceptance</code> vs <code>in_transit</code> under <code>processing</code>, or the stablecoin conversion phase under <code>requested</code>. Informational vocabulary: values can be added without a version bump; <code>status</code> is the versioned contract.
     */
    @JsonProperty("status_detail")
    public String getStatusDetail() {
        return statusDetail;
    }

    /**
     * @return ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is <code>null</code> before then and on payouts not sent over ACH.
     */
    @JsonIgnore
    public Optional<String> getTraceCode() {
        if (traceCode == null) {
            return Optional.empty();
        }
        return traceCode;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("destination_amount")
    private Optional<String> _getDestinationAmount() {
        return destinationAmount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("destination_currency")
    private Optional<String> _getDestinationCurrency() {
        return destinationCurrency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("estimated_arrival")
    private Optional<OffsetDateTime> _getEstimatedArrival() {
        return estimatedArrival;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("exchange_rate")
    private Optional<Float> _getExchangeRate() {
        return exchangeRate;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("failure")
    private Optional<PostWithdrawalCreatedPayloadDataFailure> _getFailure() {
        return failure;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("notes")
    private Optional<String> _getNotes() {
        return notes;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payer_name")
    private Optional<String> _getPayerName() {
        return payerName;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payout_method")
    private Optional<PostWithdrawalCreatedPayloadDataPayoutMethod> _getPayoutMethod() {
        return payoutMethod;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payout_request_id")
    private Optional<String> _getPayoutRequestId() {
        return payoutRequestId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("source")
    private Optional<PostWithdrawalCreatedPayloadDataSource> _getSource() {
        return source;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("trace_code")
    private Optional<String> _getTraceCode() {
        return traceCode;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PostWithdrawalCreatedPayloadData && equalTo((PostWithdrawalCreatedPayloadData) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PostWithdrawalCreatedPayloadData other) {
        return amount.equals(other.amount)
                && createdAt.equals(other.createdAt)
                && currency.equals(other.currency)
                && destinationAmount.equals(other.destinationAmount)
                && destinationCurrency.equals(other.destinationCurrency)
                && estimatedArrival.equals(other.estimatedArrival)
                && exchangeRate.equals(other.exchangeRate)
                && failure.equals(other.failure)
                && feeAmount.equals(other.feeAmount)
                && feePaidBy.equals(other.feePaidBy)
                && id.equals(other.id)
                && markupFee.equals(other.markupFee)
                && metadata.equals(other.metadata)
                && netAmount.equals(other.netAmount)
                && notes.equals(other.notes)
                && object.equals(other.object)
                && payerName.equals(other.payerName)
                && payoutMethod.equals(other.payoutMethod)
                && payoutRequestId.equals(other.payoutRequestId)
                && source.equals(other.source)
                && speed.equals(other.speed)
                && status.equals(other.status)
                && statusDetail.equals(other.statusDetail)
                && traceCode.equals(other.traceCode);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amount,
                this.createdAt,
                this.currency,
                this.destinationAmount,
                this.destinationCurrency,
                this.estimatedArrival,
                this.exchangeRate,
                this.failure,
                this.feeAmount,
                this.feePaidBy,
                this.id,
                this.markupFee,
                this.metadata,
                this.netAmount,
                this.notes,
                this.object,
                this.payerName,
                this.payoutMethod,
                this.payoutRequestId,
                this.source,
                this.speed,
                this.status,
                this.statusDetail,
                this.traceCode);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>The payout amount in whole currency units, as a decimal string.</p>
         */
        CreatedAtStage amount(@NotNull String amount);

        Builder from(PostWithdrawalCreatedPayloadData other);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the payout was created.</p>
         */
        CurrencyStage createdAt(@NotNull OffsetDateTime createdAt);
    }

    public interface CurrencyStage {
        /**
         * <p>Payout currency.</p>
         */
        FeeAmountStage currency(@NotNull String currency);
    }

    public interface FeeAmountStage {
        /**
         * <p>The fee charged for the payout, in the payout currency, as a decimal string.</p>
         */
        FeePaidByStage feeAmount(@NotNull String feeAmount);
    }

    public interface FeePaidByStage {
        /**
         * <p>Who bore the payout fee: the account itself, or its parent platform.</p>
         */
        IdStage feePaidBy(@NotNull PostWithdrawalCreatedPayloadDataFeePaidBy feePaidBy);
    }

    public interface IdStage {
        /**
         * <p>Payout ID, prefixed <code>wdrl_</code>.</p>
         */
        MarkupFeeStage id(@NotNull String id);
    }

    public interface MarkupFeeStage {
        /**
         * <p>Whop's markup on the provider fee, in the payout currency, as a decimal string. <code>&quot;0.0&quot;</code> when none applies.</p>
         */
        NetAmountStage markupFee(@NotNull String markupFee);
    }

    public interface NetAmountStage {
        /**
         * <p>The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is <code>self</code>; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and <code>failure</code> says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: <code>amount</code> carries what stayed in the balance, fees are zero because none were charged, and <code>net_amount</code> is 0 because nothing was delivered.</p>
         */
        ObjectStage netAmount(@NotNull String netAmount);
    }

    public interface ObjectStage {
        SpeedStage object(@NotNull PostWithdrawalCreatedPayloadDataObject object);
    }

    public interface SpeedStage {
        /**
         * <p>Payout delivery speed.</p>
         */
        StatusStage speed(@NotNull PostWithdrawalCreatedPayloadDataSpeed speed);
    }

    public interface StatusStage {
        /**
         * <p>Current payout status.</p>
         */
        StatusDetailStage status(@NotNull PostWithdrawalCreatedPayloadDataStatus status);
    }

    public interface StatusDetailStage {
        /**
         * <p>The finest machine phase under <code>status</code> — for example <code>awaiting_provider_acceptance</code> vs <code>in_transit</code> under <code>processing</code>, or the stablecoin conversion phase under <code>requested</code>. Informational vocabulary: values can be added without a version bump; <code>status</code> is the versioned contract.</p>
         */
        _FinalStage statusDetail(@NotNull String statusDetail);
    }

    public interface _FinalStage {
        PostWithdrawalCreatedPayloadData build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded conversion.</p>
         */
        _FinalStage destinationAmount(Optional<String> destinationAmount);

        _FinalStage destinationAmount(String destinationAmount);

        _FinalStage destinationAmount(Nullable<String> destinationAmount);

        /**
         * <p>Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the <code>GET /payouts</code> row carrying this payout's id as <code>payout_request_id</code> — and is <code>null</code> only when no settlement payout exists.</p>
         */
        _FinalStage destinationCurrency(Optional<String> destinationCurrency);

        _FinalStage destinationCurrency(String destinationCurrency);

        _FinalStage destinationCurrency(Nullable<String> destinationCurrency);

        /**
         * <p>Estimated time the funds become available in the destination account.</p>
         */
        _FinalStage estimatedArrival(Optional<OffsetDateTime> estimatedArrival);

        _FinalStage estimatedArrival(OffsetDateTime estimatedArrival);

        _FinalStage estimatedArrival(Nullable<OffsetDateTime> estimatedArrival);

        /**
         * <p>Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded rate.</p>
         */
        _FinalStage exchangeRate(Optional<Float> exchangeRate);

        _FinalStage exchangeRate(Float exchangeRate);

        _FinalStage exchangeRate(Nullable<Float> exchangeRate);

        /**
         * <p>Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; <code>null</code> otherwise.</p>
         */
        _FinalStage failure(Optional<PostWithdrawalCreatedPayloadDataFailure> failure);

        _FinalStage failure(PostWithdrawalCreatedPayloadDataFailure failure);

        _FinalStage failure(Nullable<PostWithdrawalCreatedPayloadDataFailure> failure);

        /**
         * <p>Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters.</p>
         */
        _FinalStage metadata(Map<String, String> metadata);

        _FinalStage putAllMetadata(Map<String, String> metadata);

        _FinalStage metadata(String key, String value);

        /**
         * <p>Free-form notes attached by the payout creator, or <code>null</code> when none were provided. Maximum 255 characters.</p>
         */
        _FinalStage notes(Optional<String> notes);

        _FinalStage notes(String notes);

        _FinalStage notes(Nullable<String> notes);

        /**
         * <p>Name of the entity processing the payout.</p>
         */
        _FinalStage payerName(Optional<String> payerName);

        _FinalStage payerName(String payerName);

        _FinalStage payerName(Nullable<String> payerName);

        /**
         * <p>The saved payout method used. Requires payout:destination:read; null without it.</p>
         */
        _FinalStage payoutMethod(Optional<PostWithdrawalCreatedPayloadDataPayoutMethod> payoutMethod);

        _FinalStage payoutMethod(PostWithdrawalCreatedPayloadDataPayoutMethod payoutMethod);

        _FinalStage payoutMethod(Nullable<PostWithdrawalCreatedPayloadDataPayoutMethod> payoutMethod);

        /**
         * <p>Payout request ID, prefixed <code>cofr_</code>, returned by <code>POST /payouts</code>. Match it to the settled payout in <code>GET /payouts</code>. Returns <code>null</code> for payouts not created by <code>POST /payouts</code>.</p>
         */
        _FinalStage payoutRequestId(Optional<String> payoutRequestId);

        _FinalStage payoutRequestId(String payoutRequestId);

        _FinalStage payoutRequestId(Nullable<String> payoutRequestId);

        /**
         * <p>How the payout was created. <code>automatic</code> means a scheduled auto-payout; <code>null</code> on payouts created before source tracking or through internal tooling.</p>
         */
        _FinalStage source(Optional<PostWithdrawalCreatedPayloadDataSource> source);

        _FinalStage source(PostWithdrawalCreatedPayloadDataSource source);

        _FinalStage source(Nullable<PostWithdrawalCreatedPayloadDataSource> source);

        /**
         * <p>ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is <code>null</code> before then and on payouts not sent over ACH.</p>
         */
        _FinalStage traceCode(Optional<String> traceCode);

        _FinalStage traceCode(String traceCode);

        _FinalStage traceCode(Nullable<String> traceCode);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AmountStage,
                    CreatedAtStage,
                    CurrencyStage,
                    FeeAmountStage,
                    FeePaidByStage,
                    IdStage,
                    MarkupFeeStage,
                    NetAmountStage,
                    ObjectStage,
                    SpeedStage,
                    StatusStage,
                    StatusDetailStage,
                    _FinalStage {
        private String amount;

        private OffsetDateTime createdAt;

        private String currency;

        private String feeAmount;

        private PostWithdrawalCreatedPayloadDataFeePaidBy feePaidBy;

        private String id;

        private String markupFee;

        private String netAmount;

        private PostWithdrawalCreatedPayloadDataObject object;

        private PostWithdrawalCreatedPayloadDataSpeed speed;

        private PostWithdrawalCreatedPayloadDataStatus status;

        private String statusDetail;

        private Optional<String> traceCode = Optional.empty();

        private Optional<PostWithdrawalCreatedPayloadDataSource> source = Optional.empty();

        private Optional<String> payoutRequestId = Optional.empty();

        private Optional<PostWithdrawalCreatedPayloadDataPayoutMethod> payoutMethod = Optional.empty();

        private Optional<String> payerName = Optional.empty();

        private Optional<String> notes = Optional.empty();

        private Map<String, String> metadata = new LinkedHashMap<>();

        private Optional<PostWithdrawalCreatedPayloadDataFailure> failure = Optional.empty();

        private Optional<Float> exchangeRate = Optional.empty();

        private Optional<OffsetDateTime> estimatedArrival = Optional.empty();

        private Optional<String> destinationCurrency = Optional.empty();

        private Optional<String> destinationAmount = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PostWithdrawalCreatedPayloadData other) {
            amount(other.getAmount());
            createdAt(other.getCreatedAt());
            currency(other.getCurrency());
            destinationAmount(other.getDestinationAmount());
            destinationCurrency(other.getDestinationCurrency());
            estimatedArrival(other.getEstimatedArrival());
            exchangeRate(other.getExchangeRate());
            failure(other.getFailure());
            feeAmount(other.getFeeAmount());
            feePaidBy(other.getFeePaidBy());
            id(other.getId());
            markupFee(other.getMarkupFee());
            metadata(other.getMetadata());
            netAmount(other.getNetAmount());
            notes(other.getNotes());
            object(other.getObject());
            payerName(other.getPayerName());
            payoutMethod(other.getPayoutMethod());
            payoutRequestId(other.getPayoutRequestId());
            source(other.getSource());
            speed(other.getSpeed());
            status(other.getStatus());
            statusDetail(other.getStatusDetail());
            traceCode(other.getTraceCode());
            return this;
        }

        /**
         * <p>The payout amount in whole currency units, as a decimal string.</p>
         * <p>The payout amount in whole currency units, as a decimal string.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public CreatedAtStage amount(@NotNull String amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>When the payout was created.</p>
         * <p>When the payout was created.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public CurrencyStage createdAt(@NotNull OffsetDateTime createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Payout currency.</p>
         * <p>Payout currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public FeeAmountStage currency(@NotNull String currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>The fee charged for the payout, in the payout currency, as a decimal string.</p>
         * <p>The fee charged for the payout, in the payout currency, as a decimal string.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("fee_amount")
        public FeePaidByStage feeAmount(@NotNull String feeAmount) {
            this.feeAmount = Objects.requireNonNull(feeAmount, "feeAmount must not be null");
            return this;
        }

        /**
         * <p>Who bore the payout fee: the account itself, or its parent platform.</p>
         * <p>Who bore the payout fee: the account itself, or its parent platform.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("fee_paid_by")
        public IdStage feePaidBy(@NotNull PostWithdrawalCreatedPayloadDataFeePaidBy feePaidBy) {
            this.feePaidBy = Objects.requireNonNull(feePaidBy, "feePaidBy must not be null");
            return this;
        }

        /**
         * <p>Payout ID, prefixed <code>wdrl_</code>.</p>
         * <p>Payout ID, prefixed <code>wdrl_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public MarkupFeeStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Whop's markup on the provider fee, in the payout currency, as a decimal string. <code>&quot;0.0&quot;</code> when none applies.</p>
         * <p>Whop's markup on the provider fee, in the payout currency, as a decimal string. <code>&quot;0.0&quot;</code> when none applies.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("markup_fee")
        public NetAmountStage markupFee(@NotNull String markupFee) {
            this.markupFee = Objects.requireNonNull(markupFee, "markupFee must not be null");
            return this;
        }

        /**
         * <p>The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is <code>self</code>; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and <code>failure</code> says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: <code>amount</code> carries what stayed in the balance, fees are zero because none were charged, and <code>net_amount</code> is 0 because nothing was delivered.</p>
         * <p>The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is <code>self</code>; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and <code>failure</code> says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: <code>amount</code> carries what stayed in the balance, fees are zero because none were charged, and <code>net_amount</code> is 0 because nothing was delivered.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("net_amount")
        public ObjectStage netAmount(@NotNull String netAmount) {
            this.netAmount = Objects.requireNonNull(netAmount, "netAmount must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("object")
        public SpeedStage object(@NotNull PostWithdrawalCreatedPayloadDataObject object) {
            this.object = Objects.requireNonNull(object, "object must not be null");
            return this;
        }

        /**
         * <p>Payout delivery speed.</p>
         * <p>Payout delivery speed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("speed")
        public StatusStage speed(@NotNull PostWithdrawalCreatedPayloadDataSpeed speed) {
            this.speed = Objects.requireNonNull(speed, "speed must not be null");
            return this;
        }

        /**
         * <p>Current payout status.</p>
         * <p>Current payout status.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public StatusDetailStage status(@NotNull PostWithdrawalCreatedPayloadDataStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>The finest machine phase under <code>status</code> — for example <code>awaiting_provider_acceptance</code> vs <code>in_transit</code> under <code>processing</code>, or the stablecoin conversion phase under <code>requested</code>. Informational vocabulary: values can be added without a version bump; <code>status</code> is the versioned contract.</p>
         * <p>The finest machine phase under <code>status</code> — for example <code>awaiting_provider_acceptance</code> vs <code>in_transit</code> under <code>processing</code>, or the stablecoin conversion phase under <code>requested</code>. Informational vocabulary: values can be added without a version bump; <code>status</code> is the versioned contract.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status_detail")
        public _FinalStage statusDetail(@NotNull String statusDetail) {
            this.statusDetail = Objects.requireNonNull(statusDetail, "statusDetail must not be null");
            return this;
        }

        /**
         * <p>ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is <code>null</code> before then and on payouts not sent over ACH.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage traceCode(Nullable<String> traceCode) {
            if (traceCode.isNull()) {
                this.traceCode = null;
            } else if (traceCode.isEmpty()) {
                this.traceCode = Optional.empty();
            } else {
                this.traceCode = Optional.of(traceCode.get());
            }
            return this;
        }

        /**
         * <p>ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is <code>null</code> before then and on payouts not sent over ACH.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage traceCode(String traceCode) {
            this.traceCode = Optional.ofNullable(traceCode);
            return this;
        }

        /**
         * <p>ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is <code>null</code> before then and on payouts not sent over ACH.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "trace_code", nulls = Nulls.SKIP)
        public _FinalStage traceCode(Optional<String> traceCode) {
            this.traceCode = traceCode;
            return this;
        }

        /**
         * <p>How the payout was created. <code>automatic</code> means a scheduled auto-payout; <code>null</code> on payouts created before source tracking or through internal tooling.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage source(Nullable<PostWithdrawalCreatedPayloadDataSource> source) {
            if (source.isNull()) {
                this.source = null;
            } else if (source.isEmpty()) {
                this.source = Optional.empty();
            } else {
                this.source = Optional.of(source.get());
            }
            return this;
        }

        /**
         * <p>How the payout was created. <code>automatic</code> means a scheduled auto-payout; <code>null</code> on payouts created before source tracking or through internal tooling.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage source(PostWithdrawalCreatedPayloadDataSource source) {
            this.source = Optional.ofNullable(source);
            return this;
        }

        /**
         * <p>How the payout was created. <code>automatic</code> means a scheduled auto-payout; <code>null</code> on payouts created before source tracking or through internal tooling.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "source", nulls = Nulls.SKIP)
        public _FinalStage source(Optional<PostWithdrawalCreatedPayloadDataSource> source) {
            this.source = source;
            return this;
        }

        /**
         * <p>Payout request ID, prefixed <code>cofr_</code>, returned by <code>POST /payouts</code>. Match it to the settled payout in <code>GET /payouts</code>. Returns <code>null</code> for payouts not created by <code>POST /payouts</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutRequestId(Nullable<String> payoutRequestId) {
            if (payoutRequestId.isNull()) {
                this.payoutRequestId = null;
            } else if (payoutRequestId.isEmpty()) {
                this.payoutRequestId = Optional.empty();
            } else {
                this.payoutRequestId = Optional.of(payoutRequestId.get());
            }
            return this;
        }

        /**
         * <p>Payout request ID, prefixed <code>cofr_</code>, returned by <code>POST /payouts</code>. Match it to the settled payout in <code>GET /payouts</code>. Returns <code>null</code> for payouts not created by <code>POST /payouts</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutRequestId(String payoutRequestId) {
            this.payoutRequestId = Optional.ofNullable(payoutRequestId);
            return this;
        }

        /**
         * <p>Payout request ID, prefixed <code>cofr_</code>, returned by <code>POST /payouts</code>. Match it to the settled payout in <code>GET /payouts</code>. Returns <code>null</code> for payouts not created by <code>POST /payouts</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payout_request_id", nulls = Nulls.SKIP)
        public _FinalStage payoutRequestId(Optional<String> payoutRequestId) {
            this.payoutRequestId = payoutRequestId;
            return this;
        }

        /**
         * <p>The saved payout method used. Requires payout:destination:read; null without it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutMethod(Nullable<PostWithdrawalCreatedPayloadDataPayoutMethod> payoutMethod) {
            if (payoutMethod.isNull()) {
                this.payoutMethod = null;
            } else if (payoutMethod.isEmpty()) {
                this.payoutMethod = Optional.empty();
            } else {
                this.payoutMethod = Optional.of(payoutMethod.get());
            }
            return this;
        }

        /**
         * <p>The saved payout method used. Requires payout:destination:read; null without it.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payoutMethod(PostWithdrawalCreatedPayloadDataPayoutMethod payoutMethod) {
            this.payoutMethod = Optional.ofNullable(payoutMethod);
            return this;
        }

        /**
         * <p>The saved payout method used. Requires payout:destination:read; null without it.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payout_method", nulls = Nulls.SKIP)
        public _FinalStage payoutMethod(Optional<PostWithdrawalCreatedPayloadDataPayoutMethod> payoutMethod) {
            this.payoutMethod = payoutMethod;
            return this;
        }

        /**
         * <p>Name of the entity processing the payout.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payerName(Nullable<String> payerName) {
            if (payerName.isNull()) {
                this.payerName = null;
            } else if (payerName.isEmpty()) {
                this.payerName = Optional.empty();
            } else {
                this.payerName = Optional.of(payerName.get());
            }
            return this;
        }

        /**
         * <p>Name of the entity processing the payout.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payerName(String payerName) {
            this.payerName = Optional.ofNullable(payerName);
            return this;
        }

        /**
         * <p>Name of the entity processing the payout.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payer_name", nulls = Nulls.SKIP)
        public _FinalStage payerName(Optional<String> payerName) {
            this.payerName = payerName;
            return this;
        }

        /**
         * <p>Free-form notes attached by the payout creator, or <code>null</code> when none were provided. Maximum 255 characters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage notes(Nullable<String> notes) {
            if (notes.isNull()) {
                this.notes = null;
            } else if (notes.isEmpty()) {
                this.notes = Optional.empty();
            } else {
                this.notes = Optional.of(notes.get());
            }
            return this;
        }

        /**
         * <p>Free-form notes attached by the payout creator, or <code>null</code> when none were provided. Maximum 255 characters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage notes(String notes) {
            this.notes = Optional.ofNullable(notes);
            return this;
        }

        /**
         * <p>Free-form notes attached by the payout creator, or <code>null</code> when none were provided. Maximum 255 characters.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "notes", nulls = Nulls.SKIP)
        public _FinalStage notes(Optional<String> notes) {
            this.notes = notes;
            return this;
        }

        /**
         * <p>Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(String key, String value) {
            this.metadata.put(key, value);
            return this;
        }

        /**
         * <p>Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage putAllMetadata(Map<String, String> metadata) {
            if (metadata != null) {
                this.metadata.putAll(metadata);
            }
            return this;
        }

        /**
         * <p>Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Map<String, String> metadata) {
            this.metadata.clear();
            if (metadata != null) {
                this.metadata.putAll(metadata);
            }
            return this;
        }

        /**
         * <p>Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; <code>null</code> otherwise.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage failure(Nullable<PostWithdrawalCreatedPayloadDataFailure> failure) {
            if (failure.isNull()) {
                this.failure = null;
            } else if (failure.isEmpty()) {
                this.failure = Optional.empty();
            } else {
                this.failure = Optional.of(failure.get());
            }
            return this;
        }

        /**
         * <p>Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; <code>null</code> otherwise.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage failure(PostWithdrawalCreatedPayloadDataFailure failure) {
            this.failure = Optional.ofNullable(failure);
            return this;
        }

        /**
         * <p>Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; <code>null</code> otherwise.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "failure", nulls = Nulls.SKIP)
        public _FinalStage failure(Optional<PostWithdrawalCreatedPayloadDataFailure> failure) {
            this.failure = failure;
            return this;
        }

        /**
         * <p>Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded rate.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage exchangeRate(Nullable<Float> exchangeRate) {
            if (exchangeRate.isNull()) {
                this.exchangeRate = null;
            } else if (exchangeRate.isEmpty()) {
                this.exchangeRate = Optional.empty();
            } else {
                this.exchangeRate = Optional.of(exchangeRate.get());
            }
            return this;
        }

        /**
         * <p>Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded rate.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage exchangeRate(Float exchangeRate) {
            this.exchangeRate = Optional.ofNullable(exchangeRate);
            return this;
        }

        /**
         * <p>Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded rate.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "exchange_rate", nulls = Nulls.SKIP)
        public _FinalStage exchangeRate(Optional<Float> exchangeRate) {
            this.exchangeRate = exchangeRate;
            return this;
        }

        /**
         * <p>Estimated time the funds become available in the destination account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedArrival(Nullable<OffsetDateTime> estimatedArrival) {
            if (estimatedArrival.isNull()) {
                this.estimatedArrival = null;
            } else if (estimatedArrival.isEmpty()) {
                this.estimatedArrival = Optional.empty();
            } else {
                this.estimatedArrival = Optional.of(estimatedArrival.get());
            }
            return this;
        }

        /**
         * <p>Estimated time the funds become available in the destination account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedArrival(OffsetDateTime estimatedArrival) {
            this.estimatedArrival = Optional.ofNullable(estimatedArrival);
            return this;
        }

        /**
         * <p>Estimated time the funds become available in the destination account.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "estimated_arrival", nulls = Nulls.SKIP)
        public _FinalStage estimatedArrival(Optional<OffsetDateTime> estimatedArrival) {
            this.estimatedArrival = estimatedArrival;
            return this;
        }

        /**
         * <p>Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the <code>GET /payouts</code> row carrying this payout's id as <code>payout_request_id</code> — and is <code>null</code> only when no settlement payout exists.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage destinationCurrency(Nullable<String> destinationCurrency) {
            if (destinationCurrency.isNull()) {
                this.destinationCurrency = null;
            } else if (destinationCurrency.isEmpty()) {
                this.destinationCurrency = Optional.empty();
            } else {
                this.destinationCurrency = Optional.of(destinationCurrency.get());
            }
            return this;
        }

        /**
         * <p>Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the <code>GET /payouts</code> row carrying this payout's id as <code>payout_request_id</code> — and is <code>null</code> only when no settlement payout exists.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage destinationCurrency(String destinationCurrency) {
            this.destinationCurrency = Optional.ofNullable(destinationCurrency);
            return this;
        }

        /**
         * <p>Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the <code>GET /payouts</code> row carrying this payout's id as <code>payout_request_id</code> — and is <code>null</code> only when no settlement payout exists.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "destination_currency", nulls = Nulls.SKIP)
        public _FinalStage destinationCurrency(Optional<String> destinationCurrency) {
            this.destinationCurrency = destinationCurrency;
            return this;
        }

        /**
         * <p>The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded conversion.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage destinationAmount(Nullable<String> destinationAmount) {
            if (destinationAmount.isNull()) {
                this.destinationAmount = null;
            } else if (destinationAmount.isEmpty()) {
                this.destinationAmount = Optional.empty();
            } else {
                this.destinationAmount = Optional.of(destinationAmount.get());
            }
            return this;
        }

        /**
         * <p>The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded conversion.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage destinationAmount(String destinationAmount) {
            this.destinationAmount = Optional.ofNullable(destinationAmount);
            return this;
        }

        /**
         * <p>The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is <code>null</code> before then and on payouts without a recorded conversion.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "destination_amount", nulls = Nulls.SKIP)
        public _FinalStage destinationAmount(Optional<String> destinationAmount) {
            this.destinationAmount = destinationAmount;
            return this;
        }

        @java.lang.Override
        public PostWithdrawalCreatedPayloadData build() {
            return new PostWithdrawalCreatedPayloadData(
                    amount,
                    createdAt,
                    currency,
                    destinationAmount,
                    destinationCurrency,
                    estimatedArrival,
                    exchangeRate,
                    failure,
                    feeAmount,
                    feePaidBy,
                    id,
                    markupFee,
                    metadata,
                    netAmount,
                    notes,
                    object,
                    payerName,
                    payoutMethod,
                    payoutRequestId,
                    source,
                    speed,
                    status,
                    statusDetail,
                    traceCode,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
