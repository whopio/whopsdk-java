/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.payouts.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PostPayoutMethodCreatedPayloadData.Builder.class)
public final class PostPayoutMethodCreatedPayloadData {
    private final Optional<String> accountReference;

    private final Optional<PostPayoutMethodCreatedPayloadDataBankVerificationState> bankVerificationState;

    private final OffsetDateTime createdAt;

    private final String destinationCurrency;

    private final Optional<PostPayoutMethodCreatedPayloadDataEstimatedArrival> estimatedArrival;

    private final Optional<PostPayoutMethodCreatedPayloadDataFeeStructure> feeStructure;

    private final String id;

    private final Optional<String> institutionName;

    private final boolean isClone;

    private final boolean isDefault;

    private final Optional<OffsetDateTime> lastPaidOutAt;

    private final boolean linkedViaPlaid;

    private final boolean needsPlaidReconnect;

    private final Optional<String> nickname;

    private final PostPayoutMethodCreatedPayloadDataObject object;

    private final Optional<String> payerName;

    private final Optional<PostPayoutMethodCreatedPayloadDataQuote> quote;

    private final PostPayoutMethodCreatedPayloadDataStatus status;

    private final Optional<String> statusReason;

    private final Optional<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> supportedPayoutMethod;

    private final Optional<PostPayoutMethodCreatedPayloadDataUnavailableReason> unavailableReason;

    private final Map<String, Object> additionalProperties;

    private PostPayoutMethodCreatedPayloadData(
            Optional<String> accountReference,
            Optional<PostPayoutMethodCreatedPayloadDataBankVerificationState> bankVerificationState,
            OffsetDateTime createdAt,
            String destinationCurrency,
            Optional<PostPayoutMethodCreatedPayloadDataEstimatedArrival> estimatedArrival,
            Optional<PostPayoutMethodCreatedPayloadDataFeeStructure> feeStructure,
            String id,
            Optional<String> institutionName,
            boolean isClone,
            boolean isDefault,
            Optional<OffsetDateTime> lastPaidOutAt,
            boolean linkedViaPlaid,
            boolean needsPlaidReconnect,
            Optional<String> nickname,
            PostPayoutMethodCreatedPayloadDataObject object,
            Optional<String> payerName,
            Optional<PostPayoutMethodCreatedPayloadDataQuote> quote,
            PostPayoutMethodCreatedPayloadDataStatus status,
            Optional<String> statusReason,
            Optional<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> supportedPayoutMethod,
            Optional<PostPayoutMethodCreatedPayloadDataUnavailableReason> unavailableReason,
            Map<String, Object> additionalProperties) {
        this.accountReference = accountReference;
        this.bankVerificationState = bankVerificationState;
        this.createdAt = createdAt;
        this.destinationCurrency = destinationCurrency;
        this.estimatedArrival = estimatedArrival;
        this.feeStructure = feeStructure;
        this.id = id;
        this.institutionName = institutionName;
        this.isClone = isClone;
        this.isDefault = isDefault;
        this.lastPaidOutAt = lastPaidOutAt;
        this.linkedViaPlaid = linkedViaPlaid;
        this.needsPlaidReconnect = needsPlaidReconnect;
        this.nickname = nickname;
        this.object = object;
        this.payerName = payerName;
        this.quote = quote;
        this.status = status;
        this.statusReason = statusReason;
        this.supportedPayoutMethod = supportedPayoutMethod;
        this.unavailableReason = unavailableReason;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Masked identifier for the destination, such as the last four digits of a bank account.
     */
    @JsonIgnore
    public Optional<String> getAccountReference() {
        if (accountReference == null) {
            return Optional.empty();
        }
        return accountReference;
    }

    /**
     * @return Lifecycle trust state: <code>checking</code> (verification still running), <code>verified</code> (bank confirmed ownership or a payout already completed to it), <code>no_data</code> (verification unavailable or bank returned no ownership data), <code>warning</code> (bank could not confirm the destination's owner), <code>broken</code> (payouts failed with a permanent account error), <code>null</code> (never checked).
     */
    @JsonIgnore
    public Optional<PostPayoutMethodCreatedPayloadDataBankVerificationState> getBankVerificationState() {
        if (bankVerificationState == null) {
            return Optional.empty();
        }
        return bankVerificationState;
    }

    /**
     * @return When the payout method was added.
     */
    @JsonProperty("created_at")
    public OffsetDateTime getCreatedAt() {
        return createdAt;
    }

    /**
     * @return Currency payouts are delivered in for this method.
     */
    @JsonProperty("destination_currency")
    public String getDestinationCurrency() {
        return destinationCurrency;
    }

    /**
     * @return Estimated arrival times before an amount-specific quote is requested. Null when the method is not currently eligible.
     */
    @JsonIgnore
    public Optional<PostPayoutMethodCreatedPayloadDataEstimatedArrival> getEstimatedArrival() {
        if (estimatedArrival == null) {
            return Optional.empty();
        }
        return estimatedArrival;
    }

    /**
     * @return Configured fee terms for this payout method. Null when the method is not currently eligible. An amount-specific quote remains authoritative.
     */
    @JsonIgnore
    public Optional<PostPayoutMethodCreatedPayloadDataFeeStructure> getFeeStructure() {
        if (feeStructure == null) {
            return Optional.empty();
        }
        return feeStructure;
    }

    /**
     * @return Payout method ID.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return Name of the bank or institution receiving payouts.
     */
    @JsonIgnore
    public Optional<String> getInstitutionName() {
        if (institutionName == null) {
            return Optional.empty();
        }
        return institutionName;
    }

    /**
     * @return Whether this method is a copy of one saved on another of the payer's accounts.
     */
    @JsonProperty("is_clone")
    public boolean getIsClone() {
        return isClone;
    }

    /**
     * @return Whether this is the default payout method for the account.
     */
    @JsonProperty("is_default")
    public boolean getIsDefault() {
        return isDefault;
    }

    /**
     * @return When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. <code>null</code> when nothing has been paid out to it yet.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getLastPaidOutAt() {
        if (lastPaidOutAt == null) {
            return Optional.empty();
        }
        return lastPaidOutAt;
    }

    /**
     * @return Whether the payer added this method by signing in to their bank rather than typing account details.
     */
    @JsonProperty("linked_via_plaid")
    public boolean getLinkedViaPlaid() {
        return linkedViaPlaid;
    }

    /**
     * @return Whether the bank sign-in behind this method has expired and must be redone before it counts as linked.
     */
    @JsonProperty("needs_plaid_reconnect")
    public boolean getNeedsPlaidReconnect() {
        return needsPlaidReconnect;
    }

    /**
     * @return User-defined label for the payout method.
     */
    @JsonIgnore
    public Optional<String> getNickname() {
        if (nickname == null) {
            return Optional.empty();
        }
        return nickname;
    }

    @JsonProperty("object")
    public PostPayoutMethodCreatedPayloadDataObject getObject() {
        return object;
    }

    /**
     * @return Display name of the payout rail, such as <code>ACH Bank Deposit</code>.
     */
    @JsonIgnore
    public Optional<String> getPayerName() {
        if (payerName == null) {
            return Optional.empty();
        }
        return payerName;
    }

    /**
     * @return Fee and delivery estimate for withdrawing the requested amount through this method. Null unless an amount was provided, or when the estimate is unavailable.
     */
    @JsonIgnore
    public Optional<PostPayoutMethodCreatedPayloadDataQuote> getQuote() {
        if (quote == null) {
            return Optional.empty();
        }
        return quote;
    }

    /**
     * @return Lifecycle status: <code>created</code> means saved but unused, <code>active</code> means a payout succeeded through it, <code>broken</code> means a payout failure disabled it; a later successful payout returns it to <code>active</code>.
     */
    @JsonProperty("status")
    public PostPayoutMethodCreatedPayloadDataStatus getStatus() {
        return status;
    }

    /**
     * @return Machine-readable code for why the method is <code>broken</code> — the newest disabling failure recorded through it, whether a payout error or a pre-payout rejection. <code>null</code> unless the method is broken, or when it was disabled without a recorded failure.
     */
    @JsonIgnore
    public Optional<String> getStatusReason() {
        if (statusReason == null) {
            return Optional.empty();
        }
        return statusReason;
    }

    /**
     * @return The supported payout method this saved method was created from.
     */
    @JsonIgnore
    public Optional<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> getSupportedPayoutMethod() {
        if (supportedPayoutMethod == null) {
            return Optional.empty();
        }
        return supportedPayoutMethod;
    }

    /**
     * @return Why this method is unavailable: <code>destination_retired</code> means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. <code>null</code> means no unavailability reason is known.
     */
    @JsonIgnore
    public Optional<PostPayoutMethodCreatedPayloadDataUnavailableReason> getUnavailableReason() {
        if (unavailableReason == null) {
            return Optional.empty();
        }
        return unavailableReason;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("account_reference")
    private Optional<String> _getAccountReference() {
        return accountReference;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("bank_verification_state")
    private Optional<PostPayoutMethodCreatedPayloadDataBankVerificationState> _getBankVerificationState() {
        return bankVerificationState;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("estimated_arrival")
    private Optional<PostPayoutMethodCreatedPayloadDataEstimatedArrival> _getEstimatedArrival() {
        return estimatedArrival;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("fee_structure")
    private Optional<PostPayoutMethodCreatedPayloadDataFeeStructure> _getFeeStructure() {
        return feeStructure;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("institution_name")
    private Optional<String> _getInstitutionName() {
        return institutionName;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("last_paid_out_at")
    private Optional<OffsetDateTime> _getLastPaidOutAt() {
        return lastPaidOutAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("nickname")
    private Optional<String> _getNickname() {
        return nickname;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("payer_name")
    private Optional<String> _getPayerName() {
        return payerName;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("quote")
    private Optional<PostPayoutMethodCreatedPayloadDataQuote> _getQuote() {
        return quote;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("status_reason")
    private Optional<String> _getStatusReason() {
        return statusReason;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("supported_payout_method")
    private Optional<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> _getSupportedPayoutMethod() {
        return supportedPayoutMethod;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("unavailable_reason")
    private Optional<PostPayoutMethodCreatedPayloadDataUnavailableReason> _getUnavailableReason() {
        return unavailableReason;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PostPayoutMethodCreatedPayloadData
                && equalTo((PostPayoutMethodCreatedPayloadData) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PostPayoutMethodCreatedPayloadData other) {
        return accountReference.equals(other.accountReference)
                && bankVerificationState.equals(other.bankVerificationState)
                && createdAt.equals(other.createdAt)
                && destinationCurrency.equals(other.destinationCurrency)
                && estimatedArrival.equals(other.estimatedArrival)
                && feeStructure.equals(other.feeStructure)
                && id.equals(other.id)
                && institutionName.equals(other.institutionName)
                && isClone == other.isClone
                && isDefault == other.isDefault
                && lastPaidOutAt.equals(other.lastPaidOutAt)
                && linkedViaPlaid == other.linkedViaPlaid
                && needsPlaidReconnect == other.needsPlaidReconnect
                && nickname.equals(other.nickname)
                && object.equals(other.object)
                && payerName.equals(other.payerName)
                && quote.equals(other.quote)
                && status.equals(other.status)
                && statusReason.equals(other.statusReason)
                && supportedPayoutMethod.equals(other.supportedPayoutMethod)
                && unavailableReason.equals(other.unavailableReason);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountReference,
                this.bankVerificationState,
                this.createdAt,
                this.destinationCurrency,
                this.estimatedArrival,
                this.feeStructure,
                this.id,
                this.institutionName,
                this.isClone,
                this.isDefault,
                this.lastPaidOutAt,
                this.linkedViaPlaid,
                this.needsPlaidReconnect,
                this.nickname,
                this.object,
                this.payerName,
                this.quote,
                this.status,
                this.statusReason,
                this.supportedPayoutMethod,
                this.unavailableReason);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CreatedAtStage builder() {
        return new Builder();
    }

    public interface CreatedAtStage {
        /**
         * <p>When the payout method was added.</p>
         */
        DestinationCurrencyStage createdAt(@NotNull OffsetDateTime createdAt);

        Builder from(PostPayoutMethodCreatedPayloadData other);
    }

    public interface DestinationCurrencyStage {
        /**
         * <p>Currency payouts are delivered in for this method.</p>
         */
        IdStage destinationCurrency(@NotNull String destinationCurrency);
    }

    public interface IdStage {
        /**
         * <p>Payout method ID.</p>
         */
        IsCloneStage id(@NotNull String id);
    }

    public interface IsCloneStage {
        /**
         * <p>Whether this method is a copy of one saved on another of the payer's accounts.</p>
         */
        IsDefaultStage isClone(boolean isClone);
    }

    public interface IsDefaultStage {
        /**
         * <p>Whether this is the default payout method for the account.</p>
         */
        LinkedViaPlaidStage isDefault(boolean isDefault);
    }

    public interface LinkedViaPlaidStage {
        /**
         * <p>Whether the payer added this method by signing in to their bank rather than typing account details.</p>
         */
        NeedsPlaidReconnectStage linkedViaPlaid(boolean linkedViaPlaid);
    }

    public interface NeedsPlaidReconnectStage {
        /**
         * <p>Whether the bank sign-in behind this method has expired and must be redone before it counts as linked.</p>
         */
        ObjectStage needsPlaidReconnect(boolean needsPlaidReconnect);
    }

    public interface ObjectStage {
        StatusStage object(@NotNull PostPayoutMethodCreatedPayloadDataObject object);
    }

    public interface StatusStage {
        /**
         * <p>Lifecycle status: <code>created</code> means saved but unused, <code>active</code> means a payout succeeded through it, <code>broken</code> means a payout failure disabled it; a later successful payout returns it to <code>active</code>.</p>
         */
        _FinalStage status(@NotNull PostPayoutMethodCreatedPayloadDataStatus status);
    }

    public interface _FinalStage {
        PostPayoutMethodCreatedPayloadData build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Masked identifier for the destination, such as the last four digits of a bank account.</p>
         */
        _FinalStage accountReference(Optional<String> accountReference);

        _FinalStage accountReference(String accountReference);

        _FinalStage accountReference(Nullable<String> accountReference);

        /**
         * <p>Lifecycle trust state: <code>checking</code> (verification still running), <code>verified</code> (bank confirmed ownership or a payout already completed to it), <code>no_data</code> (verification unavailable or bank returned no ownership data), <code>warning</code> (bank could not confirm the destination's owner), <code>broken</code> (payouts failed with a permanent account error), <code>null</code> (never checked).</p>
         */
        _FinalStage bankVerificationState(
                Optional<PostPayoutMethodCreatedPayloadDataBankVerificationState> bankVerificationState);

        _FinalStage bankVerificationState(
                PostPayoutMethodCreatedPayloadDataBankVerificationState bankVerificationState);

        _FinalStage bankVerificationState(
                Nullable<PostPayoutMethodCreatedPayloadDataBankVerificationState> bankVerificationState);

        /**
         * <p>Estimated arrival times before an amount-specific quote is requested. Null when the method is not currently eligible.</p>
         */
        _FinalStage estimatedArrival(Optional<PostPayoutMethodCreatedPayloadDataEstimatedArrival> estimatedArrival);

        _FinalStage estimatedArrival(PostPayoutMethodCreatedPayloadDataEstimatedArrival estimatedArrival);

        _FinalStage estimatedArrival(Nullable<PostPayoutMethodCreatedPayloadDataEstimatedArrival> estimatedArrival);

        /**
         * <p>Configured fee terms for this payout method. Null when the method is not currently eligible. An amount-specific quote remains authoritative.</p>
         */
        _FinalStage feeStructure(Optional<PostPayoutMethodCreatedPayloadDataFeeStructure> feeStructure);

        _FinalStage feeStructure(PostPayoutMethodCreatedPayloadDataFeeStructure feeStructure);

        _FinalStage feeStructure(Nullable<PostPayoutMethodCreatedPayloadDataFeeStructure> feeStructure);

        /**
         * <p>Name of the bank or institution receiving payouts.</p>
         */
        _FinalStage institutionName(Optional<String> institutionName);

        _FinalStage institutionName(String institutionName);

        _FinalStage institutionName(Nullable<String> institutionName);

        /**
         * <p>When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. <code>null</code> when nothing has been paid out to it yet.</p>
         */
        _FinalStage lastPaidOutAt(Optional<OffsetDateTime> lastPaidOutAt);

        _FinalStage lastPaidOutAt(OffsetDateTime lastPaidOutAt);

        _FinalStage lastPaidOutAt(Nullable<OffsetDateTime> lastPaidOutAt);

        /**
         * <p>User-defined label for the payout method.</p>
         */
        _FinalStage nickname(Optional<String> nickname);

        _FinalStage nickname(String nickname);

        _FinalStage nickname(Nullable<String> nickname);

        /**
         * <p>Display name of the payout rail, such as <code>ACH Bank Deposit</code>.</p>
         */
        _FinalStage payerName(Optional<String> payerName);

        _FinalStage payerName(String payerName);

        _FinalStage payerName(Nullable<String> payerName);

        /**
         * <p>Fee and delivery estimate for withdrawing the requested amount through this method. Null unless an amount was provided, or when the estimate is unavailable.</p>
         */
        _FinalStage quote(Optional<PostPayoutMethodCreatedPayloadDataQuote> quote);

        _FinalStage quote(PostPayoutMethodCreatedPayloadDataQuote quote);

        _FinalStage quote(Nullable<PostPayoutMethodCreatedPayloadDataQuote> quote);

        /**
         * <p>Machine-readable code for why the method is <code>broken</code> — the newest disabling failure recorded through it, whether a payout error or a pre-payout rejection. <code>null</code> unless the method is broken, or when it was disabled without a recorded failure.</p>
         */
        _FinalStage statusReason(Optional<String> statusReason);

        _FinalStage statusReason(String statusReason);

        _FinalStage statusReason(Nullable<String> statusReason);

        /**
         * <p>The supported payout method this saved method was created from.</p>
         */
        _FinalStage supportedPayoutMethod(
                Optional<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> supportedPayoutMethod);

        _FinalStage supportedPayoutMethod(
                PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod supportedPayoutMethod);

        _FinalStage supportedPayoutMethod(
                Nullable<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> supportedPayoutMethod);

        /**
         * <p>Why this method is unavailable: <code>destination_retired</code> means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. <code>null</code> means no unavailability reason is known.</p>
         */
        _FinalStage unavailableReason(Optional<PostPayoutMethodCreatedPayloadDataUnavailableReason> unavailableReason);

        _FinalStage unavailableReason(PostPayoutMethodCreatedPayloadDataUnavailableReason unavailableReason);

        _FinalStage unavailableReason(Nullable<PostPayoutMethodCreatedPayloadDataUnavailableReason> unavailableReason);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements CreatedAtStage,
                    DestinationCurrencyStage,
                    IdStage,
                    IsCloneStage,
                    IsDefaultStage,
                    LinkedViaPlaidStage,
                    NeedsPlaidReconnectStage,
                    ObjectStage,
                    StatusStage,
                    _FinalStage {
        private OffsetDateTime createdAt;

        private String destinationCurrency;

        private String id;

        private boolean isClone;

        private boolean isDefault;

        private boolean linkedViaPlaid;

        private boolean needsPlaidReconnect;

        private PostPayoutMethodCreatedPayloadDataObject object;

        private PostPayoutMethodCreatedPayloadDataStatus status;

        private Optional<PostPayoutMethodCreatedPayloadDataUnavailableReason> unavailableReason = Optional.empty();

        private Optional<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> supportedPayoutMethod =
                Optional.empty();

        private Optional<String> statusReason = Optional.empty();

        private Optional<PostPayoutMethodCreatedPayloadDataQuote> quote = Optional.empty();

        private Optional<String> payerName = Optional.empty();

        private Optional<String> nickname = Optional.empty();

        private Optional<OffsetDateTime> lastPaidOutAt = Optional.empty();

        private Optional<String> institutionName = Optional.empty();

        private Optional<PostPayoutMethodCreatedPayloadDataFeeStructure> feeStructure = Optional.empty();

        private Optional<PostPayoutMethodCreatedPayloadDataEstimatedArrival> estimatedArrival = Optional.empty();

        private Optional<PostPayoutMethodCreatedPayloadDataBankVerificationState> bankVerificationState =
                Optional.empty();

        private Optional<String> accountReference = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PostPayoutMethodCreatedPayloadData other) {
            accountReference(other.getAccountReference());
            bankVerificationState(other.getBankVerificationState());
            createdAt(other.getCreatedAt());
            destinationCurrency(other.getDestinationCurrency());
            estimatedArrival(other.getEstimatedArrival());
            feeStructure(other.getFeeStructure());
            id(other.getId());
            institutionName(other.getInstitutionName());
            isClone(other.getIsClone());
            isDefault(other.getIsDefault());
            lastPaidOutAt(other.getLastPaidOutAt());
            linkedViaPlaid(other.getLinkedViaPlaid());
            needsPlaidReconnect(other.getNeedsPlaidReconnect());
            nickname(other.getNickname());
            object(other.getObject());
            payerName(other.getPayerName());
            quote(other.getQuote());
            status(other.getStatus());
            statusReason(other.getStatusReason());
            supportedPayoutMethod(other.getSupportedPayoutMethod());
            unavailableReason(other.getUnavailableReason());
            return this;
        }

        /**
         * <p>When the payout method was added.</p>
         * <p>When the payout method was added.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("created_at")
        public DestinationCurrencyStage createdAt(@NotNull OffsetDateTime createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        /**
         * <p>Currency payouts are delivered in for this method.</p>
         * <p>Currency payouts are delivered in for this method.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("destination_currency")
        public IdStage destinationCurrency(@NotNull String destinationCurrency) {
            this.destinationCurrency =
                    Objects.requireNonNull(destinationCurrency, "destinationCurrency must not be null");
            return this;
        }

        /**
         * <p>Payout method ID.</p>
         * <p>Payout method ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public IsCloneStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>Whether this method is a copy of one saved on another of the payer's accounts.</p>
         * <p>Whether this method is a copy of one saved on another of the payer's accounts.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("is_clone")
        public IsDefaultStage isClone(boolean isClone) {
            this.isClone = isClone;
            return this;
        }

        /**
         * <p>Whether this is the default payout method for the account.</p>
         * <p>Whether this is the default payout method for the account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("is_default")
        public LinkedViaPlaidStage isDefault(boolean isDefault) {
            this.isDefault = isDefault;
            return this;
        }

        /**
         * <p>Whether the payer added this method by signing in to their bank rather than typing account details.</p>
         * <p>Whether the payer added this method by signing in to their bank rather than typing account details.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("linked_via_plaid")
        public NeedsPlaidReconnectStage linkedViaPlaid(boolean linkedViaPlaid) {
            this.linkedViaPlaid = linkedViaPlaid;
            return this;
        }

        /**
         * <p>Whether the bank sign-in behind this method has expired and must be redone before it counts as linked.</p>
         * <p>Whether the bank sign-in behind this method has expired and must be redone before it counts as linked.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("needs_plaid_reconnect")
        public ObjectStage needsPlaidReconnect(boolean needsPlaidReconnect) {
            this.needsPlaidReconnect = needsPlaidReconnect;
            return this;
        }

        @java.lang.Override
        @JsonSetter("object")
        public StatusStage object(@NotNull PostPayoutMethodCreatedPayloadDataObject object) {
            this.object = Objects.requireNonNull(object, "object must not be null");
            return this;
        }

        /**
         * <p>Lifecycle status: <code>created</code> means saved but unused, <code>active</code> means a payout succeeded through it, <code>broken</code> means a payout failure disabled it; a later successful payout returns it to <code>active</code>.</p>
         * <p>Lifecycle status: <code>created</code> means saved but unused, <code>active</code> means a payout succeeded through it, <code>broken</code> means a payout failure disabled it; a later successful payout returns it to <code>active</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull PostPayoutMethodCreatedPayloadDataStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>Why this method is unavailable: <code>destination_retired</code> means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. <code>null</code> means no unavailability reason is known.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage unavailableReason(
                Nullable<PostPayoutMethodCreatedPayloadDataUnavailableReason> unavailableReason) {
            if (unavailableReason.isNull()) {
                this.unavailableReason = null;
            } else if (unavailableReason.isEmpty()) {
                this.unavailableReason = Optional.empty();
            } else {
                this.unavailableReason = Optional.of(unavailableReason.get());
            }
            return this;
        }

        /**
         * <p>Why this method is unavailable: <code>destination_retired</code> means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. <code>null</code> means no unavailability reason is known.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage unavailableReason(PostPayoutMethodCreatedPayloadDataUnavailableReason unavailableReason) {
            this.unavailableReason = Optional.ofNullable(unavailableReason);
            return this;
        }

        /**
         * <p>Why this method is unavailable: <code>destination_retired</code> means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. <code>null</code> means no unavailability reason is known.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "unavailable_reason", nulls = Nulls.SKIP)
        public _FinalStage unavailableReason(
                Optional<PostPayoutMethodCreatedPayloadDataUnavailableReason> unavailableReason) {
            this.unavailableReason = unavailableReason;
            return this;
        }

        /**
         * <p>The supported payout method this saved method was created from.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage supportedPayoutMethod(
                Nullable<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> supportedPayoutMethod) {
            if (supportedPayoutMethod.isNull()) {
                this.supportedPayoutMethod = null;
            } else if (supportedPayoutMethod.isEmpty()) {
                this.supportedPayoutMethod = Optional.empty();
            } else {
                this.supportedPayoutMethod = Optional.of(supportedPayoutMethod.get());
            }
            return this;
        }

        /**
         * <p>The supported payout method this saved method was created from.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage supportedPayoutMethod(
                PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod supportedPayoutMethod) {
            this.supportedPayoutMethod = Optional.ofNullable(supportedPayoutMethod);
            return this;
        }

        /**
         * <p>The supported payout method this saved method was created from.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "supported_payout_method", nulls = Nulls.SKIP)
        public _FinalStage supportedPayoutMethod(
                Optional<PostPayoutMethodCreatedPayloadDataSupportedPayoutMethod> supportedPayoutMethod) {
            this.supportedPayoutMethod = supportedPayoutMethod;
            return this;
        }

        /**
         * <p>Machine-readable code for why the method is <code>broken</code> — the newest disabling failure recorded through it, whether a payout error or a pre-payout rejection. <code>null</code> unless the method is broken, or when it was disabled without a recorded failure.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage statusReason(Nullable<String> statusReason) {
            if (statusReason.isNull()) {
                this.statusReason = null;
            } else if (statusReason.isEmpty()) {
                this.statusReason = Optional.empty();
            } else {
                this.statusReason = Optional.of(statusReason.get());
            }
            return this;
        }

        /**
         * <p>Machine-readable code for why the method is <code>broken</code> — the newest disabling failure recorded through it, whether a payout error or a pre-payout rejection. <code>null</code> unless the method is broken, or when it was disabled without a recorded failure.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage statusReason(String statusReason) {
            this.statusReason = Optional.ofNullable(statusReason);
            return this;
        }

        /**
         * <p>Machine-readable code for why the method is <code>broken</code> — the newest disabling failure recorded through it, whether a payout error or a pre-payout rejection. <code>null</code> unless the method is broken, or when it was disabled without a recorded failure.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "status_reason", nulls = Nulls.SKIP)
        public _FinalStage statusReason(Optional<String> statusReason) {
            this.statusReason = statusReason;
            return this;
        }

        /**
         * <p>Fee and delivery estimate for withdrawing the requested amount through this method. Null unless an amount was provided, or when the estimate is unavailable.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage quote(Nullable<PostPayoutMethodCreatedPayloadDataQuote> quote) {
            if (quote.isNull()) {
                this.quote = null;
            } else if (quote.isEmpty()) {
                this.quote = Optional.empty();
            } else {
                this.quote = Optional.of(quote.get());
            }
            return this;
        }

        /**
         * <p>Fee and delivery estimate for withdrawing the requested amount through this method. Null unless an amount was provided, or when the estimate is unavailable.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage quote(PostPayoutMethodCreatedPayloadDataQuote quote) {
            this.quote = Optional.ofNullable(quote);
            return this;
        }

        /**
         * <p>Fee and delivery estimate for withdrawing the requested amount through this method. Null unless an amount was provided, or when the estimate is unavailable.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "quote", nulls = Nulls.SKIP)
        public _FinalStage quote(Optional<PostPayoutMethodCreatedPayloadDataQuote> quote) {
            this.quote = quote;
            return this;
        }

        /**
         * <p>Display name of the payout rail, such as <code>ACH Bank Deposit</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payerName(Nullable<String> payerName) {
            if (payerName.isNull()) {
                this.payerName = null;
            } else if (payerName.isEmpty()) {
                this.payerName = Optional.empty();
            } else {
                this.payerName = Optional.of(payerName.get());
            }
            return this;
        }

        /**
         * <p>Display name of the payout rail, such as <code>ACH Bank Deposit</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage payerName(String payerName) {
            this.payerName = Optional.ofNullable(payerName);
            return this;
        }

        /**
         * <p>Display name of the payout rail, such as <code>ACH Bank Deposit</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "payer_name", nulls = Nulls.SKIP)
        public _FinalStage payerName(Optional<String> payerName) {
            this.payerName = payerName;
            return this;
        }

        /**
         * <p>User-defined label for the payout method.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage nickname(Nullable<String> nickname) {
            if (nickname.isNull()) {
                this.nickname = null;
            } else if (nickname.isEmpty()) {
                this.nickname = Optional.empty();
            } else {
                this.nickname = Optional.of(nickname.get());
            }
            return this;
        }

        /**
         * <p>User-defined label for the payout method.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage nickname(String nickname) {
            this.nickname = Optional.ofNullable(nickname);
            return this;
        }

        /**
         * <p>User-defined label for the payout method.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "nickname", nulls = Nulls.SKIP)
        public _FinalStage nickname(Optional<String> nickname) {
            this.nickname = nickname;
            return this;
        }

        /**
         * <p>When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. <code>null</code> when nothing has been paid out to it yet.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastPaidOutAt(Nullable<OffsetDateTime> lastPaidOutAt) {
            if (lastPaidOutAt.isNull()) {
                this.lastPaidOutAt = null;
            } else if (lastPaidOutAt.isEmpty()) {
                this.lastPaidOutAt = Optional.empty();
            } else {
                this.lastPaidOutAt = Optional.of(lastPaidOutAt.get());
            }
            return this;
        }

        /**
         * <p>When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. <code>null</code> when nothing has been paid out to it yet.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage lastPaidOutAt(OffsetDateTime lastPaidOutAt) {
            this.lastPaidOutAt = Optional.ofNullable(lastPaidOutAt);
            return this;
        }

        /**
         * <p>When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. <code>null</code> when nothing has been paid out to it yet.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "last_paid_out_at", nulls = Nulls.SKIP)
        public _FinalStage lastPaidOutAt(Optional<OffsetDateTime> lastPaidOutAt) {
            this.lastPaidOutAt = lastPaidOutAt;
            return this;
        }

        /**
         * <p>Name of the bank or institution receiving payouts.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage institutionName(Nullable<String> institutionName) {
            if (institutionName.isNull()) {
                this.institutionName = null;
            } else if (institutionName.isEmpty()) {
                this.institutionName = Optional.empty();
            } else {
                this.institutionName = Optional.of(institutionName.get());
            }
            return this;
        }

        /**
         * <p>Name of the bank or institution receiving payouts.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage institutionName(String institutionName) {
            this.institutionName = Optional.ofNullable(institutionName);
            return this;
        }

        /**
         * <p>Name of the bank or institution receiving payouts.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "institution_name", nulls = Nulls.SKIP)
        public _FinalStage institutionName(Optional<String> institutionName) {
            this.institutionName = institutionName;
            return this;
        }

        /**
         * <p>Configured fee terms for this payout method. Null when the method is not currently eligible. An amount-specific quote remains authoritative.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage feeStructure(Nullable<PostPayoutMethodCreatedPayloadDataFeeStructure> feeStructure) {
            if (feeStructure.isNull()) {
                this.feeStructure = null;
            } else if (feeStructure.isEmpty()) {
                this.feeStructure = Optional.empty();
            } else {
                this.feeStructure = Optional.of(feeStructure.get());
            }
            return this;
        }

        /**
         * <p>Configured fee terms for this payout method. Null when the method is not currently eligible. An amount-specific quote remains authoritative.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage feeStructure(PostPayoutMethodCreatedPayloadDataFeeStructure feeStructure) {
            this.feeStructure = Optional.ofNullable(feeStructure);
            return this;
        }

        /**
         * <p>Configured fee terms for this payout method. Null when the method is not currently eligible. An amount-specific quote remains authoritative.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "fee_structure", nulls = Nulls.SKIP)
        public _FinalStage feeStructure(Optional<PostPayoutMethodCreatedPayloadDataFeeStructure> feeStructure) {
            this.feeStructure = feeStructure;
            return this;
        }

        /**
         * <p>Estimated arrival times before an amount-specific quote is requested. Null when the method is not currently eligible.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedArrival(
                Nullable<PostPayoutMethodCreatedPayloadDataEstimatedArrival> estimatedArrival) {
            if (estimatedArrival.isNull()) {
                this.estimatedArrival = null;
            } else if (estimatedArrival.isEmpty()) {
                this.estimatedArrival = Optional.empty();
            } else {
                this.estimatedArrival = Optional.of(estimatedArrival.get());
            }
            return this;
        }

        /**
         * <p>Estimated arrival times before an amount-specific quote is requested. Null when the method is not currently eligible.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage estimatedArrival(PostPayoutMethodCreatedPayloadDataEstimatedArrival estimatedArrival) {
            this.estimatedArrival = Optional.ofNullable(estimatedArrival);
            return this;
        }

        /**
         * <p>Estimated arrival times before an amount-specific quote is requested. Null when the method is not currently eligible.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "estimated_arrival", nulls = Nulls.SKIP)
        public _FinalStage estimatedArrival(
                Optional<PostPayoutMethodCreatedPayloadDataEstimatedArrival> estimatedArrival) {
            this.estimatedArrival = estimatedArrival;
            return this;
        }

        /**
         * <p>Lifecycle trust state: <code>checking</code> (verification still running), <code>verified</code> (bank confirmed ownership or a payout already completed to it), <code>no_data</code> (verification unavailable or bank returned no ownership data), <code>warning</code> (bank could not confirm the destination's owner), <code>broken</code> (payouts failed with a permanent account error), <code>null</code> (never checked).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage bankVerificationState(
                Nullable<PostPayoutMethodCreatedPayloadDataBankVerificationState> bankVerificationState) {
            if (bankVerificationState.isNull()) {
                this.bankVerificationState = null;
            } else if (bankVerificationState.isEmpty()) {
                this.bankVerificationState = Optional.empty();
            } else {
                this.bankVerificationState = Optional.of(bankVerificationState.get());
            }
            return this;
        }

        /**
         * <p>Lifecycle trust state: <code>checking</code> (verification still running), <code>verified</code> (bank confirmed ownership or a payout already completed to it), <code>no_data</code> (verification unavailable or bank returned no ownership data), <code>warning</code> (bank could not confirm the destination's owner), <code>broken</code> (payouts failed with a permanent account error), <code>null</code> (never checked).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage bankVerificationState(
                PostPayoutMethodCreatedPayloadDataBankVerificationState bankVerificationState) {
            this.bankVerificationState = Optional.ofNullable(bankVerificationState);
            return this;
        }

        /**
         * <p>Lifecycle trust state: <code>checking</code> (verification still running), <code>verified</code> (bank confirmed ownership or a payout already completed to it), <code>no_data</code> (verification unavailable or bank returned no ownership data), <code>warning</code> (bank could not confirm the destination's owner), <code>broken</code> (payouts failed with a permanent account error), <code>null</code> (never checked).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "bank_verification_state", nulls = Nulls.SKIP)
        public _FinalStage bankVerificationState(
                Optional<PostPayoutMethodCreatedPayloadDataBankVerificationState> bankVerificationState) {
            this.bankVerificationState = bankVerificationState;
            return this;
        }

        /**
         * <p>Masked identifier for the destination, such as the last four digits of a bank account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountReference(Nullable<String> accountReference) {
            if (accountReference.isNull()) {
                this.accountReference = null;
            } else if (accountReference.isEmpty()) {
                this.accountReference = Optional.empty();
            } else {
                this.accountReference = Optional.of(accountReference.get());
            }
            return this;
        }

        /**
         * <p>Masked identifier for the destination, such as the last four digits of a bank account.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountReference(String accountReference) {
            this.accountReference = Optional.ofNullable(accountReference);
            return this;
        }

        /**
         * <p>Masked identifier for the destination, such as the last four digits of a bank account.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "account_reference", nulls = Nulls.SKIP)
        public _FinalStage accountReference(Optional<String> accountReference) {
            this.accountReference = accountReference;
            return this;
        }

        @java.lang.Override
        public PostPayoutMethodCreatedPayloadData build() {
            return new PostPayoutMethodCreatedPayloadData(
                    accountReference,
                    bankVerificationState,
                    createdAt,
                    destinationCurrency,
                    estimatedArrival,
                    feeStructure,
                    id,
                    institutionName,
                    isClone,
                    isDefault,
                    lastPaidOutAt,
                    linkedViaPlaid,
                    needsPlaidReconnect,
                    nickname,
                    object,
                    payerName,
                    quote,
                    status,
                    statusReason,
                    supportedPayoutMethod,
                    unavailableReason,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
