/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.payouts.supportedmethods;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.resources.payouts.supportedmethods.requests.ListSupportedMethodsRequest;
import com.whop.api.resources.payouts.supportedmethods.types.ListSupportedMethodsResponse;
import com.whop.api.resources.payouts.supportedmethods.types.ListSupportedMethodsResponseDataItem;
import java.io.IOException;
import java.util.List;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.ExecutionException;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawSupportedMethodsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawSupportedMethodsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists the payout methods an account or user is eligible to add.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListSupportedMethodsResponseDataItem>>> list() {
        return list(ListSupportedMethodsRequest.builder().build());
    }

    /**
     * Lists the payout methods an account or user is eligible to add.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListSupportedMethodsResponseDataItem>>> list(
            RequestOptions requestOptions) {
        return list(ListSupportedMethodsRequest.builder().build(), requestOptions);
    }

    /**
     * Lists the payout methods an account or user is eligible to add.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListSupportedMethodsResponseDataItem>>> list(
            ListSupportedMethodsRequest request) {
        return list(request, null);
    }

    /**
     * Lists the payout methods an account or user is eligible to add.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListSupportedMethodsResponseDataItem>>> list(
            ListSupportedMethodsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("payouts/supported_methods");
        if (request.getAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "account_id", request.getAccountId().get(), false);
        }
        if (request.getUserId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_id", request.getUserId().get(), false);
        }
        if (request.getCountry().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "country", request.getCountry().get(), false);
        }
        if (request.getAmount().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "amount", request.getAmount().get(), false);
        }
        if (request.getCurrency().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "currency", request.getCurrency().get(), false);
        }
        if (request.getSupportedPayoutMethodId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "supported_payout_method_id",
                    request.getSupportedPayoutMethodId().get(),
                    false);
        }
        if (request.getDestinationCurrency().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "destination_currency",
                    request.getDestinationCurrency().get(),
                    false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListSupportedMethodsResponseDataItem>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListSupportedMethodsResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListSupportedMethodsResponse.class);
                        Optional<String> startingAfter =
                                parsedResponse.getPageInfo().getEndCursor();
                        ListSupportedMethodsRequest nextRequest = ListSupportedMethodsRequest.builder()
                                .from(request)
                                .after(startingAfter)
                                .build();
                        List<ListSupportedMethodsResponseDataItem> result = parsedResponse.getData();
                        future.complete(new WhopApiHttpResponse<>(
                                new SyncPagingIterable<ListSupportedMethodsResponseDataItem>(
                                        startingAfter != null, result, parsedResponse, () -> {
                                            try {
                                                return list(nextRequest, requestOptions)
                                                        .get()
                                                        .body();
                                            } catch (InterruptedException | ExecutionException e) {
                                                throw new RuntimeException(e);
                                            }
                                        }),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
