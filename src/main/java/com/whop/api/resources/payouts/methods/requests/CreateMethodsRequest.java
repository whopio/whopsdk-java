/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.payouts.methods.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateMethodsRequest.Builder.class)
public final class CreateMethodsRequest {
    private final Optional<String> accountId;

    private final Optional<String> destinationCurrency;

    private final Optional<Map<String, String>> fields;

    private final Optional<Boolean> isDefault;

    private final Optional<String> nickname;

    private final String supportedPayoutMethodId;

    private final Optional<String> userId;

    private final Map<String, Object> additionalProperties;

    private CreateMethodsRequest(
            Optional<String> accountId,
            Optional<String> destinationCurrency,
            Optional<Map<String, String>> fields,
            Optional<Boolean> isDefault,
            Optional<String> nickname,
            String supportedPayoutMethodId,
            Optional<String> userId,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.destinationCurrency = destinationCurrency;
        this.fields = fields;
        this.isDefault = isDefault;
        this.nickname = nickname;
        this.supportedPayoutMethodId = supportedPayoutMethodId;
        this.userId = userId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The account to add the payout method for, prefixed <code>biz_</code>. Provide this or <code>user_id</code>.
     */
    @JsonProperty("account_id")
    public Optional<String> getAccountId() {
        return accountId;
    }

    /**
     * @return Currency the supported payout method delivers payouts in.
     */
    @JsonProperty("destination_currency")
    public Optional<String> getDestinationCurrency() {
        return destinationCurrency;
    }

    /**
     * @return The supported payout method's required field values, keyed by field id — list them with <code>GET /payouts/supported_methods?supported_payout_method_id=...</code>. Field ids are stable <code>fld_</code> identifiers you may hardcode; they never change for a given field. A Basis Theory token id may be passed in place of a raw value. For a U.S. bank routing-number field, a raw nine-digit value must also pass the ABA checksum. A validation failure returns the method's full required_fields schema alongside the error. Required whenever the account details are supplied directly.
     */
    @JsonProperty("fields")
    public Optional<Map<String, String>> getFields() {
        return fields;
    }

    /**
     * @return Whether to make this the account's default payout method.
     */
    @JsonProperty("is_default")
    public Optional<Boolean> getIsDefault() {
        return isDefault;
    }

    /**
     * @return A label for the payout method, unique per destination.
     */
    @JsonProperty("nickname")
    public Optional<String> getNickname() {
        return nickname;
    }

    /**
     * @return The supported payout method to save (a podst_ identifier from a previous listing).
     */
    @JsonProperty("supported_payout_method_id")
    public String getSupportedPayoutMethodId() {
        return supportedPayoutMethodId;
    }

    /**
     * @return The user to add the payout method for, prefixed <code>user_</code>. Provide this or <code>account_id</code>.
     */
    @JsonProperty("user_id")
    public Optional<String> getUserId() {
        return userId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateMethodsRequest && equalTo((CreateMethodsRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateMethodsRequest other) {
        return accountId.equals(other.accountId)
                && destinationCurrency.equals(other.destinationCurrency)
                && fields.equals(other.fields)
                && isDefault.equals(other.isDefault)
                && nickname.equals(other.nickname)
                && supportedPayoutMethodId.equals(other.supportedPayoutMethodId)
                && userId.equals(other.userId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.destinationCurrency,
                this.fields,
                this.isDefault,
                this.nickname,
                this.supportedPayoutMethodId,
                this.userId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static SupportedPayoutMethodIdStage builder() {
        return new Builder();
    }

    public interface SupportedPayoutMethodIdStage {
        /**
         * <p>The supported payout method to save (a podst_ identifier from a previous listing).</p>
         */
        _FinalStage supportedPayoutMethodId(@NotNull String supportedPayoutMethodId);

        Builder from(CreateMethodsRequest other);
    }

    public interface _FinalStage {
        CreateMethodsRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The account to add the payout method for, prefixed <code>biz_</code>. Provide this or <code>user_id</code>.</p>
         */
        _FinalStage accountId(Optional<String> accountId);

        _FinalStage accountId(String accountId);

        /**
         * <p>Currency the supported payout method delivers payouts in.</p>
         */
        _FinalStage destinationCurrency(Optional<String> destinationCurrency);

        _FinalStage destinationCurrency(String destinationCurrency);

        /**
         * <p>The supported payout method's required field values, keyed by field id — list them with <code>GET /payouts/supported_methods?supported_payout_method_id=...</code>. Field ids are stable <code>fld_</code> identifiers you may hardcode; they never change for a given field. A Basis Theory token id may be passed in place of a raw value. For a U.S. bank routing-number field, a raw nine-digit value must also pass the ABA checksum. A validation failure returns the method's full required_fields schema alongside the error. Required whenever the account details are supplied directly.</p>
         */
        _FinalStage fields(Optional<Map<String, String>> fields);

        _FinalStage fields(Map<String, String> fields);

        /**
         * <p>Whether to make this the account's default payout method.</p>
         */
        _FinalStage isDefault(Optional<Boolean> isDefault);

        _FinalStage isDefault(Boolean isDefault);

        /**
         * <p>A label for the payout method, unique per destination.</p>
         */
        _FinalStage nickname(Optional<String> nickname);

        _FinalStage nickname(String nickname);

        /**
         * <p>The user to add the payout method for, prefixed <code>user_</code>. Provide this or <code>account_id</code>.</p>
         */
        _FinalStage userId(Optional<String> userId);

        _FinalStage userId(String userId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements SupportedPayoutMethodIdStage, _FinalStage {
        private String supportedPayoutMethodId;

        private Optional<String> userId = Optional.empty();

        private Optional<String> nickname = Optional.empty();

        private Optional<Boolean> isDefault = Optional.empty();

        private Optional<Map<String, String>> fields = Optional.empty();

        private Optional<String> destinationCurrency = Optional.empty();

        private Optional<String> accountId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateMethodsRequest other) {
            accountId(other.getAccountId());
            destinationCurrency(other.getDestinationCurrency());
            fields(other.getFields());
            isDefault(other.getIsDefault());
            nickname(other.getNickname());
            supportedPayoutMethodId(other.getSupportedPayoutMethodId());
            userId(other.getUserId());
            return this;
        }

        /**
         * <p>The supported payout method to save (a podst_ identifier from a previous listing).</p>
         * <p>The supported payout method to save (a podst_ identifier from a previous listing).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("supported_payout_method_id")
        public _FinalStage supportedPayoutMethodId(@NotNull String supportedPayoutMethodId) {
            this.supportedPayoutMethodId =
                    Objects.requireNonNull(supportedPayoutMethodId, "supportedPayoutMethodId must not be null");
            return this;
        }

        /**
         * <p>The user to add the payout method for, prefixed <code>user_</code>. Provide this or <code>account_id</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage userId(String userId) {
            this.userId = Optional.ofNullable(userId);
            return this;
        }

        /**
         * <p>The user to add the payout method for, prefixed <code>user_</code>. Provide this or <code>account_id</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "user_id", nulls = Nulls.SKIP)
        public _FinalStage userId(Optional<String> userId) {
            this.userId = userId;
            return this;
        }

        /**
         * <p>A label for the payout method, unique per destination.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage nickname(String nickname) {
            this.nickname = Optional.ofNullable(nickname);
            return this;
        }

        /**
         * <p>A label for the payout method, unique per destination.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "nickname", nulls = Nulls.SKIP)
        public _FinalStage nickname(Optional<String> nickname) {
            this.nickname = nickname;
            return this;
        }

        /**
         * <p>Whether to make this the account's default payout method.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage isDefault(Boolean isDefault) {
            this.isDefault = Optional.ofNullable(isDefault);
            return this;
        }

        /**
         * <p>Whether to make this the account's default payout method.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "is_default", nulls = Nulls.SKIP)
        public _FinalStage isDefault(Optional<Boolean> isDefault) {
            this.isDefault = isDefault;
            return this;
        }

        /**
         * <p>The supported payout method's required field values, keyed by field id — list them with <code>GET /payouts/supported_methods?supported_payout_method_id=...</code>. Field ids are stable <code>fld_</code> identifiers you may hardcode; they never change for a given field. A Basis Theory token id may be passed in place of a raw value. For a U.S. bank routing-number field, a raw nine-digit value must also pass the ABA checksum. A validation failure returns the method's full required_fields schema alongside the error. Required whenever the account details are supplied directly.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fields(Map<String, String> fields) {
            this.fields = Optional.ofNullable(fields);
            return this;
        }

        /**
         * <p>The supported payout method's required field values, keyed by field id — list them with <code>GET /payouts/supported_methods?supported_payout_method_id=...</code>. Field ids are stable <code>fld_</code> identifiers you may hardcode; they never change for a given field. A Basis Theory token id may be passed in place of a raw value. For a U.S. bank routing-number field, a raw nine-digit value must also pass the ABA checksum. A validation failure returns the method's full required_fields schema alongside the error. Required whenever the account details are supplied directly.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "fields", nulls = Nulls.SKIP)
        public _FinalStage fields(Optional<Map<String, String>> fields) {
            this.fields = fields;
            return this;
        }

        /**
         * <p>Currency the supported payout method delivers payouts in.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage destinationCurrency(String destinationCurrency) {
            this.destinationCurrency = Optional.ofNullable(destinationCurrency);
            return this;
        }

        /**
         * <p>Currency the supported payout method delivers payouts in.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "destination_currency", nulls = Nulls.SKIP)
        public _FinalStage destinationCurrency(Optional<String> destinationCurrency) {
            this.destinationCurrency = destinationCurrency;
            return this;
        }

        /**
         * <p>The account to add the payout method for, prefixed <code>biz_</code>. Provide this or <code>user_id</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage accountId(String accountId) {
            this.accountId = Optional.ofNullable(accountId);
            return this;
        }

        /**
         * <p>The account to add the payout method for, prefixed <code>biz_</code>. Provide this or <code>user_id</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "account_id", nulls = Nulls.SKIP)
        public _FinalStage accountId(Optional<String> accountId) {
            this.accountId = accountId;
            return this;
        }

        @java.lang.Override
        public CreateMethodsRequest build() {
            return new CreateMethodsRequest(
                    accountId,
                    destinationCurrency,
                    fields,
                    isDefault,
                    nickname,
                    supportedPayoutMethodId,
                    userId,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
