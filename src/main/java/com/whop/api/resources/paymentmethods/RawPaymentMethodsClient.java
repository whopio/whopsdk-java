/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.paymentmethods;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.InternalServerError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.TooManyRequestsError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.errors.UnprocessableEntityError;
import com.whop.api.resources.paymentmethods.requests.DeletePaymentMethodRequest;
import com.whop.api.resources.paymentmethods.requests.ListPaymentMethodsRequest;
import com.whop.api.resources.paymentmethods.requests.RetrievePaymentMethodsRequest;
import com.whop.api.resources.paymentmethods.types.ListPaymentMethodsResponse;
import com.whop.api.types.PaymentMethod;
import com.whop.api.types.PaymentMethodListItem;
import java.io.IOException;
import java.util.List;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawPaymentMethodsClient {
    protected final ClientOptions clientOptions;

    public RawPaymentMethodsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of payment methods for a member or company, or for the authenticated user when neither is given, with optional filtering by creation date. A payment method is a stored representation of how a customer intends to pay, such as a card, bank account, or digital wallet.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<PaymentMethodListItem>> list() {
        return list(ListPaymentMethodsRequest.builder().build());
    }

    /**
     * Returns a paginated list of payment methods for a member or company, or for the authenticated user when neither is given, with optional filtering by creation date. A payment method is a stored representation of how a customer intends to pay, such as a card, bank account, or digital wallet.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<PaymentMethodListItem>> list(RequestOptions requestOptions) {
        return list(ListPaymentMethodsRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of payment methods for a member or company, or for the authenticated user when neither is given, with optional filtering by creation date. A payment method is a stored representation of how a customer intends to pay, such as a card, bank account, or digital wallet.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<PaymentMethodListItem>> list(ListPaymentMethodsRequest request) {
        return list(request, null);
    }

    /**
     * Returns a paginated list of payment methods for a member or company, or for the authenticated user when neither is given, with optional filtering by creation date. A payment method is a stored representation of how a customer intends to pay, such as a card, bank account, or digital wallet.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<PaymentMethodListItem>> list(
            ListPaymentMethodsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("payment_methods");
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        if (request.getMemberId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "member_id", request.getMemberId().get(), false);
        }
        if (request.getCompanyId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "company_id", request.getCompanyId().get(), false);
        }
        if (request.getDirection().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "direction", request.getDirection().get(), false);
        }
        if (request.getCreatedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "created_before", request.getCreatedBefore().get(), false);
        }
        if (request.getCreatedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "created_after", request.getCreatedAfter().get(), false);
        }
        if (request.getFutureUsage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "future_usage", request.getFutureUsage().get(), false);
        }
        if (request.getHasPayerDocument().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "has_payer_document", request.getHasPayerDocument().get(), false);
        }
        if (request.getExpired().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "expired", request.getExpired().get(), false);
        }
        if (request.getBroken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "broken", request.getBroken().get(), false);
        }
        if (request.getPaymentMethodTypes().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "payment_method_types",
                    request.getPaymentMethodTypes().get(),
                    true);
        }
        if (request.getCardBrands().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "card_brands", request.getCardBrands().get(), true);
        }
        if (request.getCardFundingTypes().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "card_funding_types", request.getCardFundingTypes().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListPaymentMethodsResponse parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListPaymentMethodsResponse.class);
                Optional<String> startingAfter = parsedResponse.getPageInfo().getEndCursor();
                ListPaymentMethodsRequest nextRequest = ListPaymentMethodsRequest.builder()
                        .from(request)
                        .after(startingAfter)
                        .build();
                List<PaymentMethodListItem> result = parsedResponse.getData();
                return new WhopApiHttpResponse<>(
                        new SyncPagingIterable<PaymentMethodListItem>(
                                startingAfter != null, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves the details of an existing payment method. Addresses a member's wallet when member_id or company_id is given, otherwise your own.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<PaymentMethod> retrieve(String id) {
        return retrieve(id, RetrievePaymentMethodsRequest.builder().build());
    }

    /**
     * Retrieves the details of an existing payment method. Addresses a member's wallet when member_id or company_id is given, otherwise your own.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<PaymentMethod> retrieve(String id, RequestOptions requestOptions) {
        return retrieve(id, RetrievePaymentMethodsRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves the details of an existing payment method. Addresses a member's wallet when member_id or company_id is given, otherwise your own.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<PaymentMethod> retrieve(String id, RetrievePaymentMethodsRequest request) {
        return retrieve(id, request, null);
    }

    /**
     * Retrieves the details of an existing payment method. Addresses a member's wallet when member_id or company_id is given, otherwise your own.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<PaymentMethod> retrieve(
            String id, RetrievePaymentMethodsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("payment_methods")
                .addPathSegment(id);
        if (request.getCompanyId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "company_id", request.getCompanyId().get(), false);
        }
        if (request.getMemberId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "member_id", request.getMemberId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentMethod.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Delete a saved payment method. Cannot delete a payment method attached to an active subscription.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:manage</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> deletePaymentMethod(String id) {
        return deletePaymentMethod(id, DeletePaymentMethodRequest.builder().build());
    }

    /**
     * Delete a saved payment method. Cannot delete a payment method attached to an active subscription.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:manage</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> deletePaymentMethod(String id, RequestOptions requestOptions) {
        return deletePaymentMethod(id, DeletePaymentMethodRequest.builder().build(), requestOptions);
    }

    /**
     * Delete a saved payment method. Cannot delete a payment method attached to an active subscription.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:manage</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> deletePaymentMethod(String id, DeletePaymentMethodRequest request) {
        return deletePaymentMethod(id, request, null);
    }

    /**
     * Delete a saved payment method. Cannot delete a payment method attached to an active subscription.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>member:payment_methods:manage</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> deletePaymentMethod(
            String id, DeletePaymentMethodRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("payment_methods")
                .addPathSegment(id);
        if (request.getCompanyId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "company_id", request.getCompanyId().get(), false);
        }
        if (request.getMemberId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "member_id", request.getMemberId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, boolean.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }
}
