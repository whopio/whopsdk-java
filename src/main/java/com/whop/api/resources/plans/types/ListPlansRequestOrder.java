/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.plans.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ListPlansRequestOrder {
    public static final ListPlansRequestOrder INTERNAL_NOTES =
            new ListPlansRequestOrder(Value.INTERNAL_NOTES, "internal_notes");

    public static final ListPlansRequestOrder CREATED_AT = new ListPlansRequestOrder(Value.CREATED_AT, "created_at");

    public static final ListPlansRequestOrder ID = new ListPlansRequestOrder(Value.ID, "id");

    public static final ListPlansRequestOrder EXPIRATION_DAYS =
            new ListPlansRequestOrder(Value.EXPIRATION_DAYS, "expiration_days");

    public static final ListPlansRequestOrder ACTIVE_MEMBERS_COUNT =
            new ListPlansRequestOrder(Value.ACTIVE_MEMBERS_COUNT, "active_members_count");

    private final Value value;

    private final String string;

    ListPlansRequestOrder(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ListPlansRequestOrder
                        && this.string.equals(((ListPlansRequestOrder) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case INTERNAL_NOTES:
                return visitor.visitInternalNotes();
            case CREATED_AT:
                return visitor.visitCreatedAt();
            case ID:
                return visitor.visitId();
            case EXPIRATION_DAYS:
                return visitor.visitExpirationDays();
            case ACTIVE_MEMBERS_COUNT:
                return visitor.visitActiveMembersCount();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ListPlansRequestOrder valueOf(String value) {
        switch (value) {
            case "internal_notes":
                return INTERNAL_NOTES;
            case "created_at":
                return CREATED_AT;
            case "id":
                return ID;
            case "expiration_days":
                return EXPIRATION_DAYS;
            case "active_members_count":
                return ACTIVE_MEMBERS_COUNT;
            default:
                return new ListPlansRequestOrder(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ID,

        ACTIVE_MEMBERS_COUNT,

        CREATED_AT,

        INTERNAL_NOTES,

        EXPIRATION_DAYS,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitId();

        T visitActiveMembersCount();

        T visitCreatedAt();

        T visitInternalNotes();

        T visitExpirationDays();

        T visitUnknown(String unknownType);
    }
}
