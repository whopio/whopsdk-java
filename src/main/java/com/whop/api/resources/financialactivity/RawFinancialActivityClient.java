/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.financialactivity;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.resources.financialactivity.requests.ListFinancialActivityRequest;
import com.whop.api.resources.financialactivity.types.ListFinancialActivityResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawFinancialActivityClient {
    protected final ClientOptions clientOptions;

    public RawFinancialActivityClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns an account's or user's activity feed: every movement of money in or out.
     */
    public WhopApiHttpResponse<ListFinancialActivityResponse> list() {
        return list(ListFinancialActivityRequest.builder().build());
    }

    /**
     * Returns an account's or user's activity feed: every movement of money in or out.
     */
    public WhopApiHttpResponse<ListFinancialActivityResponse> list(RequestOptions requestOptions) {
        return list(ListFinancialActivityRequest.builder().build(), requestOptions);
    }

    /**
     * Returns an account's or user's activity feed: every movement of money in or out.
     */
    public WhopApiHttpResponse<ListFinancialActivityResponse> list(ListFinancialActivityRequest request) {
        return list(request, null);
    }

    /**
     * Returns an account's or user's activity feed: every movement of money in or out.
     */
    public WhopApiHttpResponse<ListFinancialActivityResponse> list(
            ListFinancialActivityRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("financial-activity");
        if (request.getAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "account_id", request.getAccountId().get(), false);
        }
        if (request.getUserId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_id", request.getUserId().get(), false);
        }
        if (request.getIncludeOwnedAccounts().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "include_owned_accounts",
                    request.getIncludeOwnedAccounts().get(),
                    false);
        }
        if (request.getIncludeResource().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "include_resource", request.getIncludeResource().get(), false);
        }
        if (request.getCurrency().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "currency", request.getCurrency().get(), false);
        }
        if (request.getPostedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "posted_after", request.getPostedAfter().get(), false);
        }
        if (request.getPostedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "posted_before", request.getPostedBefore().get(), false);
        }
        if (request.getAvailableAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "available_after", request.getAvailableAfter().get(), false);
        }
        if (request.getAvailableBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "available_before", request.getAvailableBefore().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getLineTypes().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "line_types", request.getLineTypes().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListFinancialActivityResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }
}
