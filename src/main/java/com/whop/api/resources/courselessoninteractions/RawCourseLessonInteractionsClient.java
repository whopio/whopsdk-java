/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.courselessoninteractions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.InternalServerError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.TooManyRequestsError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.errors.UnprocessableEntityError;
import com.whop.api.resources.courselessoninteractions.requests.ListCourseLessonInteractionsRequest;
import com.whop.api.resources.courselessoninteractions.requests.RetrieveCourseLessonInteractionsRequest;
import com.whop.api.resources.courselessoninteractions.types.ListCourseLessonInteractionsResponse;
import com.whop.api.types.CourseLessonInteraction;
import com.whop.api.types.CourseLessonInteractionListItem;
import java.io.IOException;
import java.util.List;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCourseLessonInteractionsClient {
    protected final ClientOptions clientOptions;

    public RawCourseLessonInteractionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of lesson interactions, filtered by lesson, course, user, or completion status.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseLessonInteractionListItem>> list() {
        return list(ListCourseLessonInteractionsRequest.builder().build());
    }

    /**
     * Returns a paginated list of lesson interactions, filtered by lesson, course, user, or completion status.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseLessonInteractionListItem>> list(
            RequestOptions requestOptions) {
        return list(ListCourseLessonInteractionsRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of lesson interactions, filtered by lesson, course, user, or completion status.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseLessonInteractionListItem>> list(
            ListCourseLessonInteractionsRequest request) {
        return list(request, null);
    }

    /**
     * Returns a paginated list of lesson interactions, filtered by lesson, course, user, or completion status.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseLessonInteractionListItem>> list(
            ListCourseLessonInteractionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("course_lesson_interactions");
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        if (request.getUserId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_id", request.getUserId().get(), false);
        }
        if (request.getLessonId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lesson_id", request.getLessonId().get(), false);
        }
        if (request.getCourseId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "course_id", request.getCourseId().get(), false);
        }
        if (request.getCompleted().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "completed", request.getCompleted().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListCourseLessonInteractionsResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                        responseBodyString, ListCourseLessonInteractionsResponse.class);
                Optional<String> startingAfter = parsedResponse.getPageInfo().getEndCursor();
                ListCourseLessonInteractionsRequest nextRequest = ListCourseLessonInteractionsRequest.builder()
                        .from(request)
                        .after(startingAfter)
                        .build();
                List<CourseLessonInteractionListItem> result = parsedResponse.getData();
                return new WhopApiHttpResponse<>(
                        new SyncPagingIterable<CourseLessonInteractionListItem>(
                                startingAfter != null, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves the details of an existing course lesson interaction.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<CourseLessonInteraction> retrieve(String id) {
        return retrieve(id, RetrieveCourseLessonInteractionsRequest.builder().build());
    }

    /**
     * Retrieves the details of an existing course lesson interaction.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<CourseLessonInteraction> retrieve(String id, RequestOptions requestOptions) {
        return retrieve(id, RetrieveCourseLessonInteractionsRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves the details of an existing course lesson interaction.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<CourseLessonInteraction> retrieve(
            String id, RetrieveCourseLessonInteractionsRequest request) {
        return retrieve(id, request, null);
    }

    /**
     * Retrieves the details of an existing course lesson interaction.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * <li><code>course_analytics:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<CourseLessonInteraction> retrieve(
            String id, RetrieveCourseLessonInteractionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("course_lesson_interactions")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CourseLessonInteraction.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }
}
