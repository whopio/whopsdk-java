/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.notifications;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.Suppliers;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.resources.notifications.requests.BadgesNotificationsRequest;
import com.whop.api.resources.notifications.requests.CreateNotificationsRequest;
import com.whop.api.resources.notifications.requests.ListNotificationsRequest;
import com.whop.api.resources.notifications.requests.MarkReadNotificationsRequest;
import com.whop.api.resources.notifications.requests.RetrieveNotificationsRequest;
import com.whop.api.resources.notifications.topics.TopicsClient;
import com.whop.api.resources.notifications.types.BadgesNotificationsResponse;
import com.whop.api.resources.notifications.types.CreateNotificationsResponse;
import com.whop.api.resources.notifications.types.MarkReadNotificationsResponse;
import com.whop.api.types.Notification;
import java.util.function.Supplier;

public class NotificationsClient {
    protected final ClientOptions clientOptions;

    private final RawNotificationsClient rawClient;

    protected final Supplier<TopicsClient> topicsClient;

    public NotificationsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawNotificationsClient(clientOptions);
        this.topicsClient = Suppliers.memoize(() -> new TopicsClient(clientOptions));
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawNotificationsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists the authenticated user's notifications, newest first. Requires a user credential — an account API key has no notification feed. Without filters the feed spans every experience the user belongs to plus the teams they are a member of.
     */
    public SyncPagingIterable<Notification> list() {
        return this.rawClient.list().body();
    }

    /**
     * Lists the authenticated user's notifications, newest first. Requires a user credential — an account API key has no notification feed. Without filters the feed spans every experience the user belongs to plus the teams they are a member of.
     */
    public SyncPagingIterable<Notification> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * Lists the authenticated user's notifications, newest first. Requires a user credential — an account API key has no notification feed. Without filters the feed spans every experience the user belongs to plus the teams they are a member of.
     */
    public SyncPagingIterable<Notification> list(ListNotificationsRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * Lists the authenticated user's notifications, newest first. Requires a user credential — an account API key has no notification feed. Without filters the feed spans every experience the user belongs to plus the teams they are a member of.
     */
    public SyncPagingIterable<Notification> list(ListNotificationsRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }

    /**
     * Queues a notification to every user of an experience or to an account's team, processed asynchronously. Every send is attributed to an app: use an app API key, or a credential acting on behalf of an app. Narrow the audience with <code>user_ids</code> to send a mention.
     */
    public CreateNotificationsResponse create(CreateNotificationsRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Queues a notification to every user of an experience or to an account's team, processed asynchronously. Every send is attributed to an app: use an app API key, or a credential acting on behalf of an app. Narrow the audience with <code>user_ids</code> to send a mention.
     */
    public CreateNotificationsResponse create(CreateNotificationsRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }

    /**
     * Lists the authenticated user's per-experience unread badge state. Requires a user credential. Returns one row per experience the user belongs to (or per requested experience).
     */
    public BadgesNotificationsResponse badges() {
        return this.rawClient.badges().body();
    }

    /**
     * Lists the authenticated user's per-experience unread badge state. Requires a user credential. Returns one row per experience the user belongs to (or per requested experience).
     */
    public BadgesNotificationsResponse badges(RequestOptions requestOptions) {
        return this.rawClient.badges(requestOptions).body();
    }

    /**
     * Lists the authenticated user's per-experience unread badge state. Requires a user credential. Returns one row per experience the user belongs to (or per requested experience).
     */
    public BadgesNotificationsResponse badges(BadgesNotificationsRequest request) {
        return this.rawClient.badges(request).body();
    }

    /**
     * Lists the authenticated user's per-experience unread badge state. Requires a user credential. Returns one row per experience the user belongs to (or per requested experience).
     */
    public BadgesNotificationsResponse badges(BadgesNotificationsRequest request, RequestOptions requestOptions) {
        return this.rawClient.badges(request, requestOptions).body();
    }

    /**
     * Marks the authenticated user's notifications as read: one experience's (<code>experience_id</code>) or everything (<code>all: true</code>) — exactly one of the two. Requires a user credential. Responds with the refreshed badge rows for the affected scope.
     */
    public MarkReadNotificationsResponse markRead() {
        return this.rawClient.markRead().body();
    }

    /**
     * Marks the authenticated user's notifications as read: one experience's (<code>experience_id</code>) or everything (<code>all: true</code>) — exactly one of the two. Requires a user credential. Responds with the refreshed badge rows for the affected scope.
     */
    public MarkReadNotificationsResponse markRead(RequestOptions requestOptions) {
        return this.rawClient.markRead(requestOptions).body();
    }

    /**
     * Marks the authenticated user's notifications as read: one experience's (<code>experience_id</code>) or everything (<code>all: true</code>) — exactly one of the two. Requires a user credential. Responds with the refreshed badge rows for the affected scope.
     */
    public MarkReadNotificationsResponse markRead(MarkReadNotificationsRequest request) {
        return this.rawClient.markRead(request).body();
    }

    /**
     * Marks the authenticated user's notifications as read: one experience's (<code>experience_id</code>) or everything (<code>all: true</code>) — exactly one of the two. Requires a user credential. Responds with the refreshed badge rows for the affected scope.
     */
    public MarkReadNotificationsResponse markRead(MarkReadNotificationsRequest request, RequestOptions requestOptions) {
        return this.rawClient.markRead(request, requestOptions).body();
    }

    /**
     * Retrieves a single notification by id — either an <code>id</code> returned by List Notifications, or the ephemeral id delivered with a push/websocket event. Requires a user credential.
     */
    public Notification retrieve(String id) {
        return this.rawClient.retrieve(id).body();
    }

    /**
     * Retrieves a single notification by id — either an <code>id</code> returned by List Notifications, or the ephemeral id delivered with a push/websocket event. Requires a user credential.
     */
    public Notification retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).body();
    }

    /**
     * Retrieves a single notification by id — either an <code>id</code> returned by List Notifications, or the ephemeral id delivered with a push/websocket event. Requires a user credential.
     */
    public Notification retrieve(String id, RetrieveNotificationsRequest request) {
        return this.rawClient.retrieve(id, request).body();
    }

    /**
     * Retrieves a single notification by id — either an <code>id</code> returned by List Notifications, or the ephemeral id delivered with a push/websocket event. Requires a user credential.
     */
    public Notification retrieve(String id, RetrieveNotificationsRequest request, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, request, requestOptions).body();
    }

    public TopicsClient topics() {
        return this.topicsClient.get();
    }
}
