/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.dmchannels;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.resources.dmchannels.requests.CreateDmChannelsRequest;
import com.whop.api.resources.dmchannels.requests.DeleteDmChannelsRequest;
import com.whop.api.resources.dmchannels.requests.ListDmChannelsRequest;
import com.whop.api.resources.dmchannels.requests.RetrieveDmChannelsRequest;
import com.whop.api.resources.dmchannels.requests.UpdateDmChannelsRequest;
import com.whop.api.types.DmChannel;
import com.whop.api.types.DmChannelListItem;

public class DmChannelsClient {
    protected final ClientOptions clientOptions;

    private final RawDmChannelsClient rawClient;

    public DmChannelsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawDmChannelsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawDmChannelsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns a paginated list of DM channels for the currently authenticated user, sorted by most recently active.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * </ul>
     */
    public SyncPagingIterable<DmChannelListItem> list() {
        return this.rawClient.list().body();
    }

    /**
     * Returns a paginated list of DM channels for the currently authenticated user, sorted by most recently active.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * </ul>
     */
    public SyncPagingIterable<DmChannelListItem> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * Returns a paginated list of DM channels for the currently authenticated user, sorted by most recently active.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * </ul>
     */
    public SyncPagingIterable<DmChannelListItem> list(ListDmChannelsRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * Returns a paginated list of DM channels for the currently authenticated user, sorted by most recently active.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * </ul>
     */
    public SyncPagingIterable<DmChannelListItem> list(ListDmChannelsRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }

    /**
     * Create a new DM channel between two or more users, optionally scoped to a specific company. Returns the existing channel if one already exists.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * </ul>
     */
    public DmChannel create(CreateDmChannelsRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Create a new DM channel between two or more users, optionally scoped to a specific company. Returns the existing channel if one already exists.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * </ul>
     */
    public DmChannel create(CreateDmChannelsRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }

    /**
     * Retrieves the details of an existing DM channel.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public DmChannel retrieve(String id) {
        return this.rawClient.retrieve(id).body();
    }

    /**
     * Retrieves the details of an existing DM channel.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public DmChannel retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).body();
    }

    /**
     * Retrieves the details of an existing DM channel.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public DmChannel retrieve(String id, RetrieveDmChannelsRequest request) {
        return this.rawClient.retrieve(id, request).body();
    }

    /**
     * Retrieves the details of an existing DM channel.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public DmChannel retrieve(String id, RetrieveDmChannelsRequest request, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, request, requestOptions).body();
    }

    /**
     * Permanently delete a DM channel and all of its messages. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public boolean delete(String id) {
        return this.rawClient.delete(id).body();
    }

    /**
     * Permanently delete a DM channel and all of its messages. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public boolean delete(String id, RequestOptions requestOptions) {
        return this.rawClient.delete(id, requestOptions).body();
    }

    /**
     * Permanently delete a DM channel and all of its messages. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public boolean delete(String id, DeleteDmChannelsRequest request) {
        return this.rawClient.delete(id, request).body();
    }

    /**
     * Permanently delete a DM channel and all of its messages. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public boolean delete(String id, DeleteDmChannelsRequest request, RequestOptions requestOptions) {
        return this.rawClient.delete(id, request, requestOptions).body();
    }

    /**
     * Update the settings of an existing DM channel, such as its display name. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public DmChannel update(String id) {
        return this.rawClient.update(id).body();
    }

    /**
     * Update the settings of an existing DM channel, such as its display name. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public DmChannel update(String id, RequestOptions requestOptions) {
        return this.rawClient.update(id, requestOptions).body();
    }

    /**
     * Update the settings of an existing DM channel, such as its display name. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public DmChannel update(String id, UpdateDmChannelsRequest request) {
        return this.rawClient.update(id, request).body();
    }

    /**
     * Update the settings of an existing DM channel, such as its display name. Only an admin of the channel can perform this action.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>dms:channel:manage</code></li>
     * <li><code>support_chat:create</code></li>
     * </ul>
     */
    public DmChannel update(String id, UpdateDmChannelsRequest request, RequestOptions requestOptions) {
        return this.rawClient.update(id, request, requestOptions).body();
    }
}
