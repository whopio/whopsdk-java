/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.audiences.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import com.whop.api.resources.audiences.types.CreateAudiencesRequestAudienceType;
import com.whop.api.resources.audiences.types.CreateAudiencesRequestColumnMapping;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateAudiencesRequest.Builder.class)
public final class CreateAudiencesRequest {
    private final String accountId;

    private final Optional<CreateAudiencesRequestAudienceType> audienceType;

    private final Optional<Boolean> autoRefresh;

    private final Optional<CreateAudiencesRequestColumnMapping> columnMapping;

    private final Optional<Integer> count;

    private final Optional<String> fileId;

    private final Optional<Map<String, Object>> filters;

    private final Optional<String> name;

    private final Optional<Integer> percentage;

    private final Optional<String> sourceAudienceId;

    private final Map<String, Object> additionalProperties;

    private CreateAudiencesRequest(
            String accountId,
            Optional<CreateAudiencesRequestAudienceType> audienceType,
            Optional<Boolean> autoRefresh,
            Optional<CreateAudiencesRequestColumnMapping> columnMapping,
            Optional<Integer> count,
            Optional<String> fileId,
            Optional<Map<String, Object>> filters,
            Optional<String> name,
            Optional<Integer> percentage,
            Optional<String> sourceAudienceId,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.audienceType = audienceType;
        this.autoRefresh = autoRefresh;
        this.columnMapping = columnMapping;
        this.count = count;
        this.fileId = fileId;
        this.filters = filters;
        this.name = name;
        this.percentage = percentage;
        this.sourceAudienceId = sourceAudienceId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Account ID, prefixed <code>biz_</code>.
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return What to create. Defaults to <code>custom</code> (CSV upload).
     */
    @JsonProperty("audience_type")
    public Optional<CreateAudiencesRequestAudienceType> getAudienceType() {
        return audienceType;
    }

    /**
     * @return Filter audiences only, and set only at creation. <code>true</code> (the default) rebuilds membership from the filters twice a day. <code>false</code> keeps whoever matched at creation and never rebuilds.
     */
    @JsonProperty("auto_refresh")
    public Optional<Boolean> getAutoRefresh() {
        return autoRefresh;
    }

    /**
     * @return Custom audiences only. Maps supported identity fields to CSV column headers. Map at least one of <code>email</code> or <code>phone</code>.
     */
    @JsonProperty("column_mapping")
    public Optional<CreateAudiencesRequestColumnMapping> getColumnMapping() {
        return columnMapping;
    }

    /**
     * @return Lookalikes only. Number of lookalike audiences to create (1–6).
     */
    @JsonProperty("count")
    public Optional<Integer> getCount() {
        return count;
    }

    /**
     * @return Custom audiences only. The uploaded customer CSV — a file id (<code>file_...</code>) returned by <code>POST /files</code>.
     */
    @JsonProperty("file_id")
    public Optional<String> getFileId() {
        return fileId;
    }

    /**
     * @return Filter audiences only. The People filters that define membership, keyed exactly as <code>GET /people</code> accepts them — for example <code>{&quot;os&quot;: &quot;iOS&quot;, &quot;country&quot;: &quot;US&quot;}</code>. Date filters must be rolling windows — <code>first_seen_within_days</code> or <code>last_seen_within_days</code> — so the audience re-anchors on every refresh; fixed dates such as <code>first_seen_after</code> are rejected. Source values are canonical source paths (<code>whop:&lt;campaign&gt;:&lt;group&gt;:&lt;ad&gt;</code>, <code>ext:&lt;platform&gt;:...</code>, <code>referrer:&lt;domain&gt;</code>, <code>direct</code>), exact or with a trailing <code>:*</code> wildcard.
     */
    @JsonProperty("filters")
    public Optional<Map<String, Object>> getFilters() {
        return filters;
    }

    /**
     * @return Audience display name. Required for custom audiences; lookalike names are generated from the source audience.
     */
    @JsonProperty("name")
    public Optional<String> getName() {
        return name;
    }

    /**
     * @return Lookalikes only. Total similarity reach as a whole percent (1–20), sliced evenly across <code>count</code> — must be divisible by <code>count</code>.
     */
    @JsonProperty("percentage")
    public Optional<Integer> getPercentage() {
        return percentage;
    }

    /**
     * @return Lookalikes only. The ready custom audience (<code>adaud_</code>) to build from; it needs at least 100 matched people.
     */
    @JsonProperty("source_audience_id")
    public Optional<String> getSourceAudienceId() {
        return sourceAudienceId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateAudiencesRequest && equalTo((CreateAudiencesRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateAudiencesRequest other) {
        return accountId.equals(other.accountId)
                && audienceType.equals(other.audienceType)
                && autoRefresh.equals(other.autoRefresh)
                && columnMapping.equals(other.columnMapping)
                && count.equals(other.count)
                && fileId.equals(other.fileId)
                && filters.equals(other.filters)
                && name.equals(other.name)
                && percentage.equals(other.percentage)
                && sourceAudienceId.equals(other.sourceAudienceId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.audienceType,
                this.autoRefresh,
                this.columnMapping,
                this.count,
                this.fileId,
                this.filters,
                this.name,
                this.percentage,
                this.sourceAudienceId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountIdStage builder() {
        return new Builder();
    }

    public interface AccountIdStage {
        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         */
        _FinalStage accountId(@NotNull String accountId);

        Builder from(CreateAudiencesRequest other);
    }

    public interface _FinalStage {
        CreateAudiencesRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>What to create. Defaults to <code>custom</code> (CSV upload).</p>
         */
        _FinalStage audienceType(Optional<CreateAudiencesRequestAudienceType> audienceType);

        _FinalStage audienceType(CreateAudiencesRequestAudienceType audienceType);

        /**
         * <p>Filter audiences only, and set only at creation. <code>true</code> (the default) rebuilds membership from the filters twice a day. <code>false</code> keeps whoever matched at creation and never rebuilds.</p>
         */
        _FinalStage autoRefresh(Optional<Boolean> autoRefresh);

        _FinalStage autoRefresh(Boolean autoRefresh);

        /**
         * <p>Custom audiences only. Maps supported identity fields to CSV column headers. Map at least one of <code>email</code> or <code>phone</code>.</p>
         */
        _FinalStage columnMapping(Optional<CreateAudiencesRequestColumnMapping> columnMapping);

        _FinalStage columnMapping(CreateAudiencesRequestColumnMapping columnMapping);

        /**
         * <p>Lookalikes only. Number of lookalike audiences to create (1–6).</p>
         */
        _FinalStage count(Optional<Integer> count);

        _FinalStage count(Integer count);

        /**
         * <p>Custom audiences only. The uploaded customer CSV — a file id (<code>file_...</code>) returned by <code>POST /files</code>.</p>
         */
        _FinalStage fileId(Optional<String> fileId);

        _FinalStage fileId(String fileId);

        /**
         * <p>Filter audiences only. The People filters that define membership, keyed exactly as <code>GET /people</code> accepts them — for example <code>{&quot;os&quot;: &quot;iOS&quot;, &quot;country&quot;: &quot;US&quot;}</code>. Date filters must be rolling windows — <code>first_seen_within_days</code> or <code>last_seen_within_days</code> — so the audience re-anchors on every refresh; fixed dates such as <code>first_seen_after</code> are rejected. Source values are canonical source paths (<code>whop:&lt;campaign&gt;:&lt;group&gt;:&lt;ad&gt;</code>, <code>ext:&lt;platform&gt;:...</code>, <code>referrer:&lt;domain&gt;</code>, <code>direct</code>), exact or with a trailing <code>:*</code> wildcard.</p>
         */
        _FinalStage filters(Optional<Map<String, Object>> filters);

        _FinalStage filters(Map<String, Object> filters);

        /**
         * <p>Audience display name. Required for custom audiences; lookalike names are generated from the source audience.</p>
         */
        _FinalStage name(Optional<String> name);

        _FinalStage name(String name);

        /**
         * <p>Lookalikes only. Total similarity reach as a whole percent (1–20), sliced evenly across <code>count</code> — must be divisible by <code>count</code>.</p>
         */
        _FinalStage percentage(Optional<Integer> percentage);

        _FinalStage percentage(Integer percentage);

        /**
         * <p>Lookalikes only. The ready custom audience (<code>adaud_</code>) to build from; it needs at least 100 matched people.</p>
         */
        _FinalStage sourceAudienceId(Optional<String> sourceAudienceId);

        _FinalStage sourceAudienceId(String sourceAudienceId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AccountIdStage, _FinalStage {
        private String accountId;

        private Optional<String> sourceAudienceId = Optional.empty();

        private Optional<Integer> percentage = Optional.empty();

        private Optional<String> name = Optional.empty();

        private Optional<Map<String, Object>> filters = Optional.empty();

        private Optional<String> fileId = Optional.empty();

        private Optional<Integer> count = Optional.empty();

        private Optional<CreateAudiencesRequestColumnMapping> columnMapping = Optional.empty();

        private Optional<Boolean> autoRefresh = Optional.empty();

        private Optional<CreateAudiencesRequestAudienceType> audienceType = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateAudiencesRequest other) {
            accountId(other.getAccountId());
            audienceType(other.getAudienceType());
            autoRefresh(other.getAutoRefresh());
            columnMapping(other.getColumnMapping());
            count(other.getCount());
            fileId(other.getFileId());
            filters(other.getFilters());
            name(other.getName());
            percentage(other.getPercentage());
            sourceAudienceId(other.getSourceAudienceId());
            return this;
        }

        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public _FinalStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        /**
         * <p>Lookalikes only. The ready custom audience (<code>adaud_</code>) to build from; it needs at least 100 matched people.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sourceAudienceId(String sourceAudienceId) {
            this.sourceAudienceId = Optional.ofNullable(sourceAudienceId);
            return this;
        }

        /**
         * <p>Lookalikes only. The ready custom audience (<code>adaud_</code>) to build from; it needs at least 100 matched people.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "source_audience_id", nulls = Nulls.SKIP)
        public _FinalStage sourceAudienceId(Optional<String> sourceAudienceId) {
            this.sourceAudienceId = sourceAudienceId;
            return this;
        }

        /**
         * <p>Lookalikes only. Total similarity reach as a whole percent (1–20), sliced evenly across <code>count</code> — must be divisible by <code>count</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage percentage(Integer percentage) {
            this.percentage = Optional.ofNullable(percentage);
            return this;
        }

        /**
         * <p>Lookalikes only. Total similarity reach as a whole percent (1–20), sliced evenly across <code>count</code> — must be divisible by <code>count</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "percentage", nulls = Nulls.SKIP)
        public _FinalStage percentage(Optional<Integer> percentage) {
            this.percentage = percentage;
            return this;
        }

        /**
         * <p>Audience display name. Required for custom audiences; lookalike names are generated from the source audience.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage name(String name) {
            this.name = Optional.ofNullable(name);
            return this;
        }

        /**
         * <p>Audience display name. Required for custom audiences; lookalike names are generated from the source audience.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "name", nulls = Nulls.SKIP)
        public _FinalStage name(Optional<String> name) {
            this.name = name;
            return this;
        }

        /**
         * <p>Filter audiences only. The People filters that define membership, keyed exactly as <code>GET /people</code> accepts them — for example <code>{&quot;os&quot;: &quot;iOS&quot;, &quot;country&quot;: &quot;US&quot;}</code>. Date filters must be rolling windows — <code>first_seen_within_days</code> or <code>last_seen_within_days</code> — so the audience re-anchors on every refresh; fixed dates such as <code>first_seen_after</code> are rejected. Source values are canonical source paths (<code>whop:&lt;campaign&gt;:&lt;group&gt;:&lt;ad&gt;</code>, <code>ext:&lt;platform&gt;:...</code>, <code>referrer:&lt;domain&gt;</code>, <code>direct</code>), exact or with a trailing <code>:*</code> wildcard.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage filters(Map<String, Object> filters) {
            this.filters = Optional.ofNullable(filters);
            return this;
        }

        /**
         * <p>Filter audiences only. The People filters that define membership, keyed exactly as <code>GET /people</code> accepts them — for example <code>{&quot;os&quot;: &quot;iOS&quot;, &quot;country&quot;: &quot;US&quot;}</code>. Date filters must be rolling windows — <code>first_seen_within_days</code> or <code>last_seen_within_days</code> — so the audience re-anchors on every refresh; fixed dates such as <code>first_seen_after</code> are rejected. Source values are canonical source paths (<code>whop:&lt;campaign&gt;:&lt;group&gt;:&lt;ad&gt;</code>, <code>ext:&lt;platform&gt;:...</code>, <code>referrer:&lt;domain&gt;</code>, <code>direct</code>), exact or with a trailing <code>:*</code> wildcard.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "filters", nulls = Nulls.SKIP)
        public _FinalStage filters(Optional<Map<String, Object>> filters) {
            this.filters = filters;
            return this;
        }

        /**
         * <p>Custom audiences only. The uploaded customer CSV — a file id (<code>file_...</code>) returned by <code>POST /files</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fileId(String fileId) {
            this.fileId = Optional.ofNullable(fileId);
            return this;
        }

        /**
         * <p>Custom audiences only. The uploaded customer CSV — a file id (<code>file_...</code>) returned by <code>POST /files</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "file_id", nulls = Nulls.SKIP)
        public _FinalStage fileId(Optional<String> fileId) {
            this.fileId = fileId;
            return this;
        }

        /**
         * <p>Lookalikes only. Number of lookalike audiences to create (1–6).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage count(Integer count) {
            this.count = Optional.ofNullable(count);
            return this;
        }

        /**
         * <p>Lookalikes only. Number of lookalike audiences to create (1–6).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "count", nulls = Nulls.SKIP)
        public _FinalStage count(Optional<Integer> count) {
            this.count = count;
            return this;
        }

        /**
         * <p>Custom audiences only. Maps supported identity fields to CSV column headers. Map at least one of <code>email</code> or <code>phone</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage columnMapping(CreateAudiencesRequestColumnMapping columnMapping) {
            this.columnMapping = Optional.ofNullable(columnMapping);
            return this;
        }

        /**
         * <p>Custom audiences only. Maps supported identity fields to CSV column headers. Map at least one of <code>email</code> or <code>phone</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "column_mapping", nulls = Nulls.SKIP)
        public _FinalStage columnMapping(Optional<CreateAudiencesRequestColumnMapping> columnMapping) {
            this.columnMapping = columnMapping;
            return this;
        }

        /**
         * <p>Filter audiences only, and set only at creation. <code>true</code> (the default) rebuilds membership from the filters twice a day. <code>false</code> keeps whoever matched at creation and never rebuilds.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage autoRefresh(Boolean autoRefresh) {
            this.autoRefresh = Optional.ofNullable(autoRefresh);
            return this;
        }

        /**
         * <p>Filter audiences only, and set only at creation. <code>true</code> (the default) rebuilds membership from the filters twice a day. <code>false</code> keeps whoever matched at creation and never rebuilds.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "auto_refresh", nulls = Nulls.SKIP)
        public _FinalStage autoRefresh(Optional<Boolean> autoRefresh) {
            this.autoRefresh = autoRefresh;
            return this;
        }

        /**
         * <p>What to create. Defaults to <code>custom</code> (CSV upload).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage audienceType(CreateAudiencesRequestAudienceType audienceType) {
            this.audienceType = Optional.ofNullable(audienceType);
            return this;
        }

        /**
         * <p>What to create. Defaults to <code>custom</code> (CSV upload).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "audience_type", nulls = Nulls.SKIP)
        public _FinalStage audienceType(Optional<CreateAudiencesRequestAudienceType> audienceType) {
            this.audienceType = audienceType;
            return this;
        }

        @java.lang.Override
        public CreateAudiencesRequest build() {
            return new CreateAudiencesRequest(
                    accountId,
                    audienceType,
                    autoRefresh,
                    columnMapping,
                    count,
                    fileId,
                    filters,
                    name,
                    percentage,
                    sourceAudienceId,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
