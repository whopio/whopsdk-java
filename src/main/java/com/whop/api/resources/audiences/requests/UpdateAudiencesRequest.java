/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.audiences.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = UpdateAudiencesRequest.Builder.class)
public final class UpdateAudiencesRequest {
    private final Optional<Map<String, Object>> filters;

    private final Optional<String> name;

    private final Map<String, Object> additionalProperties;

    private UpdateAudiencesRequest(
            Optional<Map<String, Object>> filters, Optional<String> name, Map<String, Object> additionalProperties) {
        this.filters = filters;
        this.name = name;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Replaces the People filters that define membership. The whole definition is replaced rather than merged, so send every filter you want to keep — a filter you leave out stops applying. Keys and values are the ones <code>GET /people</code> accepts, such as an <code>os</code> of <code>iOS</code> or a <code>country</code> of <code>US</code>, and at least one filter is required. Date filters must be rolling windows — <code>first_seen_within_days</code> or <code>last_seen_within_days</code> — so the audience re-anchors every time it rebuilds; fixed dates such as <code>first_seen_after</code> are rejected, as is <code>audience_id</code>. An array value holds at most 500 items, and each value at most 10 KB. Only an audience with a <code>source_type</code> of <code>people_filter</code> and <code>auto_refresh</code> of <code>true</code> accepts filters: an uploaded list has no filters to replace, and with auto refresh off the audience keeps the people it matched when it was built, so create a new audience instead.
     */
    @JsonProperty("filters")
    public Optional<Map<String, Object>> getFilters() {
        return filters;
    }

    /**
     * @return New audience display name. A blank value is ignored rather than clearing the name.
     */
    @JsonProperty("name")
    public Optional<String> getName() {
        return name;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UpdateAudiencesRequest && equalTo((UpdateAudiencesRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(UpdateAudiencesRequest other) {
        return filters.equals(other.filters) && name.equals(other.name);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.filters, this.name);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<Map<String, Object>> filters = Optional.empty();

        private Optional<String> name = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(UpdateAudiencesRequest other) {
            filters(other.getFilters());
            name(other.getName());
            return this;
        }

        /**
         * <p>Replaces the People filters that define membership. The whole definition is replaced rather than merged, so send every filter you want to keep — a filter you leave out stops applying. Keys and values are the ones <code>GET /people</code> accepts, such as an <code>os</code> of <code>iOS</code> or a <code>country</code> of <code>US</code>, and at least one filter is required. Date filters must be rolling windows — <code>first_seen_within_days</code> or <code>last_seen_within_days</code> — so the audience re-anchors every time it rebuilds; fixed dates such as <code>first_seen_after</code> are rejected, as is <code>audience_id</code>. An array value holds at most 500 items, and each value at most 10 KB. Only an audience with a <code>source_type</code> of <code>people_filter</code> and <code>auto_refresh</code> of <code>true</code> accepts filters: an uploaded list has no filters to replace, and with auto refresh off the audience keeps the people it matched when it was built, so create a new audience instead.</p>
         */
        @JsonSetter(value = "filters", nulls = Nulls.SKIP)
        public Builder filters(Optional<Map<String, Object>> filters) {
            this.filters = filters;
            return this;
        }

        public Builder filters(Map<String, Object> filters) {
            this.filters = Optional.ofNullable(filters);
            return this;
        }

        /**
         * <p>New audience display name. A blank value is ignored rather than clearing the name.</p>
         */
        @JsonSetter(value = "name", nulls = Nulls.SKIP)
        public Builder name(Optional<String> name) {
            this.name = name;
            return this;
        }

        public Builder name(String name) {
            this.name = Optional.ofNullable(name);
            return this;
        }

        public UpdateAudiencesRequest build() {
            return new UpdateAudiencesRequest(filters, name, additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
