/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.entries;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.resources.entries.requests.ApproveEntriesRequest;
import com.whop.api.resources.entries.requests.DenyEntriesRequest;
import com.whop.api.resources.entries.requests.ListEntriesRequest;
import com.whop.api.resources.entries.requests.RetrieveEntriesRequest;
import com.whop.api.resources.entries.types.ApproveEntriesResponse;
import com.whop.api.types.Entry;
import com.whop.api.types.EntryListItem;
import java.util.concurrent.CompletableFuture;

public class AsyncEntriesClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawEntriesClient rawClient;

    public AsyncEntriesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawEntriesClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawEntriesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns a paginated list of waitlist entries for a company, with optional filtering by product, plan, status, and creation date.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<SyncPagingIterable<EntryListItem>> list(ListEntriesRequest request) {
        return this.rawClient.list(request).thenApply(response -> response.body());
    }

    /**
     * Returns a paginated list of waitlist entries for a company, with optional filtering by product, plan, status, and creation date.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<SyncPagingIterable<EntryListItem>> list(
            ListEntriesRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieves the details of an existing waitlist entry.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> retrieve(String id) {
        return this.rawClient.retrieve(id).thenApply(response -> response.body());
    }

    /**
     * Retrieves the details of an existing waitlist entry.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieves the details of an existing waitlist entry.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> retrieve(String id, RetrieveEntriesRequest request) {
        return this.rawClient.retrieve(id, request).thenApply(response -> response.body());
    }

    /**
     * Retrieves the details of an existing waitlist entry.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> retrieve(String id, RetrieveEntriesRequest request, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Approve a pending waitlist entry, triggering the checkout process to grant the user access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * </ul>
     */
    public CompletableFuture<ApproveEntriesResponse> approve(String id) {
        return this.rawClient.approve(id).thenApply(response -> response.body());
    }

    /**
     * Approve a pending waitlist entry, triggering the checkout process to grant the user access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * </ul>
     */
    public CompletableFuture<ApproveEntriesResponse> approve(String id, RequestOptions requestOptions) {
        return this.rawClient.approve(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Approve a pending waitlist entry, triggering the checkout process to grant the user access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * </ul>
     */
    public CompletableFuture<ApproveEntriesResponse> approve(String id, ApproveEntriesRequest request) {
        return this.rawClient.approve(id, request).thenApply(response -> response.body());
    }

    /**
     * Approve a pending waitlist entry, triggering the checkout process to grant the user access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * </ul>
     */
    public CompletableFuture<ApproveEntriesResponse> approve(
            String id, ApproveEntriesRequest request, RequestOptions requestOptions) {
        return this.rawClient.approve(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Deny a pending waitlist entry, preventing the user from gaining access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * <li><code>plan:basic:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> deny(String id) {
        return this.rawClient.deny(id).thenApply(response -> response.body());
    }

    /**
     * Deny a pending waitlist entry, preventing the user from gaining access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * <li><code>plan:basic:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> deny(String id, RequestOptions requestOptions) {
        return this.rawClient.deny(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Deny a pending waitlist entry, preventing the user from gaining access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * <li><code>plan:basic:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> deny(String id, DenyEntriesRequest request) {
        return this.rawClient.deny(id, request).thenApply(response -> response.body());
    }

    /**
     * Deny a pending waitlist entry, preventing the user from gaining access to the plan.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>plan:waitlist:manage</code></li>
     * <li><code>plan:basic:read</code></li>
     * <li><code>member:email:read</code></li>
     * </ul>
     */
    public CompletableFuture<Entry> deny(String id, DenyEntriesRequest request, RequestOptions requestOptions) {
        return this.rawClient.deny(id, request, requestOptions).thenApply(response -> response.body());
    }
}
