/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.checkoutsessions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.MediaTypes;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ConflictError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.resources.checkoutsessions.requests.CreateCheckoutSessionsRequest;
import com.whop.api.types.CheckoutSession;
import com.whop.api.types.V1ErrorResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawCheckoutSessionsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawCheckoutSessionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Opens a checkout session. No credentials required. Pass exactly one of <code>items</code>, <code>checkout_configuration</code>, or <code>link</code>. The response includes <code>client_secret</code> once; later calls authenticate with it.
     */
    public CompletableFuture<WhopApiHttpResponse<CheckoutSession>> create() {
        return create(CreateCheckoutSessionsRequest.builder().build());
    }

    /**
     * Opens a checkout session. No credentials required. Pass exactly one of <code>items</code>, <code>checkout_configuration</code>, or <code>link</code>. The response includes <code>client_secret</code> once; later calls authenticate with it.
     */
    public CompletableFuture<WhopApiHttpResponse<CheckoutSession>> create(RequestOptions requestOptions) {
        return create(CreateCheckoutSessionsRequest.builder().build(), requestOptions);
    }

    /**
     * Opens a checkout session. No credentials required. Pass exactly one of <code>items</code>, <code>checkout_configuration</code>, or <code>link</code>. The response includes <code>client_secret</code> once; later calls authenticate with it.
     */
    public CompletableFuture<WhopApiHttpResponse<CheckoutSession>> create(CreateCheckoutSessionsRequest request) {
        return create(request, null);
    }

    /**
     * Opens a checkout session. No credentials required. Pass exactly one of <code>items</code>, <code>checkout_configuration</code>, or <code>link</code>. The response includes <code>client_secret</code> once; later calls authenticate with it.
     */
    public CompletableFuture<WhopApiHttpResponse<CheckoutSession>> create(
            CreateCheckoutSessionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("checkout_sessions");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new WhopApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<CheckoutSession>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new WhopApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutSession.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 409:
                                future.completeExceptionally(new ConflictError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, V1ErrorResponse.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
