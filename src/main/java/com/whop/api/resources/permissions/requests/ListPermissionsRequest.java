/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.permissions.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ListPermissionsRequest.Builder.class)
public final class ListPermissionsRequest {
    private final String resourceId;

    private final Optional<String> actions;

    private final Map<String, Object> additionalProperties;

    private ListPermissionsRequest(
            String resourceId, Optional<String> actions, Map<String, Object> additionalProperties) {
        this.resourceId = resourceId;
        this.actions = actions;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Tag of the resource to check against: an account (<code>biz_</code>), product (<code>prod_</code>), experience (<code>exp_</code>), or app (<code>app_</code>). A resource the credential cannot see is reported as granted nothing rather than as an error.
     */
    @JsonProperty("resource_id")
    public String getResourceId() {
        return resourceId;
    }

    /**
     * @return Comma-separated permission actions to check, for example <code>stats:read,payment:basic:read</code>. Every action is returned when omitted.
     */
    @JsonProperty("actions")
    public Optional<String> getActions() {
        return actions;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ListPermissionsRequest && equalTo((ListPermissionsRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ListPermissionsRequest other) {
        return resourceId.equals(other.resourceId) && actions.equals(other.actions);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.resourceId, this.actions);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ResourceIdStage builder() {
        return new Builder();
    }

    public interface ResourceIdStage {
        /**
         * <p>Tag of the resource to check against: an account (<code>biz_</code>), product (<code>prod_</code>), experience (<code>exp_</code>), or app (<code>app_</code>). A resource the credential cannot see is reported as granted nothing rather than as an error.</p>
         */
        _FinalStage resourceId(@NotNull String resourceId);

        Builder from(ListPermissionsRequest other);
    }

    public interface _FinalStage {
        ListPermissionsRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Comma-separated permission actions to check, for example <code>stats:read,payment:basic:read</code>. Every action is returned when omitted.</p>
         */
        _FinalStage actions(Optional<String> actions);

        _FinalStage actions(String actions);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements ResourceIdStage, _FinalStage {
        private String resourceId;

        private Optional<String> actions = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ListPermissionsRequest other) {
            resourceId(other.getResourceId());
            actions(other.getActions());
            return this;
        }

        /**
         * <p>Tag of the resource to check against: an account (<code>biz_</code>), product (<code>prod_</code>), experience (<code>exp_</code>), or app (<code>app_</code>). A resource the credential cannot see is reported as granted nothing rather than as an error.</p>
         * <p>Tag of the resource to check against: an account (<code>biz_</code>), product (<code>prod_</code>), experience (<code>exp_</code>), or app (<code>app_</code>). A resource the credential cannot see is reported as granted nothing rather than as an error.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("resource_id")
        public _FinalStage resourceId(@NotNull String resourceId) {
            this.resourceId = Objects.requireNonNull(resourceId, "resourceId must not be null");
            return this;
        }

        /**
         * <p>Comma-separated permission actions to check, for example <code>stats:read,payment:basic:read</code>. Every action is returned when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage actions(String actions) {
            this.actions = Optional.ofNullable(actions);
            return this;
        }

        /**
         * <p>Comma-separated permission actions to check, for example <code>stats:read,payment:basic:read</code>. Every action is returned when omitted.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "actions", nulls = Nulls.SKIP)
        public _FinalStage actions(Optional<String> actions) {
            this.actions = actions;
            return this;
        }

        @java.lang.Override
        public ListPermissionsRequest build() {
            return new ListPermissionsRequest(resourceId, actions, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
