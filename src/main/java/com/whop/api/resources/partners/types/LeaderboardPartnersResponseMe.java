/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.partners.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = LeaderboardPartnersResponseMe.Builder.class)
public final class LeaderboardPartnersResponseMe {
    private final OffsetDateTime firstReferralStartedAt;

    private final int rank;

    private final String totalEarningsUsd;

    private final String totalVolumeUsd;

    private final Optional<LeaderboardPartnersResponseMeUser> user;

    private final Map<String, Object> additionalProperties;

    private LeaderboardPartnersResponseMe(
            OffsetDateTime firstReferralStartedAt,
            int rank,
            String totalEarningsUsd,
            String totalVolumeUsd,
            Optional<LeaderboardPartnersResponseMeUser> user,
            Map<String, Object> additionalProperties) {
        this.firstReferralStartedAt = firstReferralStartedAt;
        this.rank = rank;
        this.totalEarningsUsd = totalEarningsUsd;
        this.totalVolumeUsd = totalVolumeUsd;
        this.user = user;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return When the referrer's earliest partner business became active.
     */
    @JsonProperty("first_referral_started_at")
    public OffsetDateTime getFirstReferralStartedAt() {
        return firstReferralStartedAt;
    }

    /**
     * @return 1-based leaderboard position.
     */
    @JsonProperty("rank")
    public int getRank() {
        return rank;
    }

    /**
     * @return The referrer's pending + completed earnings across all referred businesses, in USD.
     */
    @JsonProperty("total_earnings_usd")
    public String getTotalEarningsUsd() {
        return totalEarningsUsd;
    }

    /**
     * @return Credited GMV across all the referrer's referred businesses, in USD.
     */
    @JsonProperty("total_volume_usd")
    public String getTotalVolumeUsd() {
        return totalVolumeUsd;
    }

    /**
     * @return The ranked referrer. Identity fields (id, name, username, profile_picture) are returned only on the caller's own entry; other referrers expose coarse location only.
     */
    @JsonIgnore
    public Optional<LeaderboardPartnersResponseMeUser> getUser() {
        if (user == null) {
            return Optional.empty();
        }
        return user;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("user")
    private Optional<LeaderboardPartnersResponseMeUser> _getUser() {
        return user;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof LeaderboardPartnersResponseMe && equalTo((LeaderboardPartnersResponseMe) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(LeaderboardPartnersResponseMe other) {
        return firstReferralStartedAt.equals(other.firstReferralStartedAt)
                && rank == other.rank
                && totalEarningsUsd.equals(other.totalEarningsUsd)
                && totalVolumeUsd.equals(other.totalVolumeUsd)
                && user.equals(other.user);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.firstReferralStartedAt, this.rank, this.totalEarningsUsd, this.totalVolumeUsd, this.user);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static FirstReferralStartedAtStage builder() {
        return new Builder();
    }

    public interface FirstReferralStartedAtStage {
        /**
         * <p>When the referrer's earliest partner business became active.</p>
         */
        RankStage firstReferralStartedAt(@NotNull OffsetDateTime firstReferralStartedAt);

        Builder from(LeaderboardPartnersResponseMe other);
    }

    public interface RankStage {
        /**
         * <p>1-based leaderboard position.</p>
         */
        TotalEarningsUsdStage rank(int rank);
    }

    public interface TotalEarningsUsdStage {
        /**
         * <p>The referrer's pending + completed earnings across all referred businesses, in USD.</p>
         */
        TotalVolumeUsdStage totalEarningsUsd(@NotNull String totalEarningsUsd);
    }

    public interface TotalVolumeUsdStage {
        /**
         * <p>Credited GMV across all the referrer's referred businesses, in USD.</p>
         */
        _FinalStage totalVolumeUsd(@NotNull String totalVolumeUsd);
    }

    public interface _FinalStage {
        LeaderboardPartnersResponseMe build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The ranked referrer. Identity fields (id, name, username, profile_picture) are returned only on the caller's own entry; other referrers expose coarse location only.</p>
         */
        _FinalStage user(Optional<LeaderboardPartnersResponseMeUser> user);

        _FinalStage user(LeaderboardPartnersResponseMeUser user);

        _FinalStage user(Nullable<LeaderboardPartnersResponseMeUser> user);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements FirstReferralStartedAtStage, RankStage, TotalEarningsUsdStage, TotalVolumeUsdStage, _FinalStage {
        private OffsetDateTime firstReferralStartedAt;

        private int rank;

        private String totalEarningsUsd;

        private String totalVolumeUsd;

        private Optional<LeaderboardPartnersResponseMeUser> user = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(LeaderboardPartnersResponseMe other) {
            firstReferralStartedAt(other.getFirstReferralStartedAt());
            rank(other.getRank());
            totalEarningsUsd(other.getTotalEarningsUsd());
            totalVolumeUsd(other.getTotalVolumeUsd());
            user(other.getUser());
            return this;
        }

        /**
         * <p>When the referrer's earliest partner business became active.</p>
         * <p>When the referrer's earliest partner business became active.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("first_referral_started_at")
        public RankStage firstReferralStartedAt(@NotNull OffsetDateTime firstReferralStartedAt) {
            this.firstReferralStartedAt =
                    Objects.requireNonNull(firstReferralStartedAt, "firstReferralStartedAt must not be null");
            return this;
        }

        /**
         * <p>1-based leaderboard position.</p>
         * <p>1-based leaderboard position.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("rank")
        public TotalEarningsUsdStage rank(int rank) {
            this.rank = rank;
            return this;
        }

        /**
         * <p>The referrer's pending + completed earnings across all referred businesses, in USD.</p>
         * <p>The referrer's pending + completed earnings across all referred businesses, in USD.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("total_earnings_usd")
        public TotalVolumeUsdStage totalEarningsUsd(@NotNull String totalEarningsUsd) {
            this.totalEarningsUsd = Objects.requireNonNull(totalEarningsUsd, "totalEarningsUsd must not be null");
            return this;
        }

        /**
         * <p>Credited GMV across all the referrer's referred businesses, in USD.</p>
         * <p>Credited GMV across all the referrer's referred businesses, in USD.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("total_volume_usd")
        public _FinalStage totalVolumeUsd(@NotNull String totalVolumeUsd) {
            this.totalVolumeUsd = Objects.requireNonNull(totalVolumeUsd, "totalVolumeUsd must not be null");
            return this;
        }

        /**
         * <p>The ranked referrer. Identity fields (id, name, username, profile_picture) are returned only on the caller's own entry; other referrers expose coarse location only.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage user(Nullable<LeaderboardPartnersResponseMeUser> user) {
            if (user.isNull()) {
                this.user = null;
            } else if (user.isEmpty()) {
                this.user = Optional.empty();
            } else {
                this.user = Optional.of(user.get());
            }
            return this;
        }

        /**
         * <p>The ranked referrer. Identity fields (id, name, username, profile_picture) are returned only on the caller's own entry; other referrers expose coarse location only.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage user(LeaderboardPartnersResponseMeUser user) {
            this.user = Optional.ofNullable(user);
            return this;
        }

        /**
         * <p>The ranked referrer. Identity fields (id, name, username, profile_picture) are returned only on the caller's own entry; other referrers expose coarse location only.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "user", nulls = Nulls.SKIP)
        public _FinalStage user(Optional<LeaderboardPartnersResponseMeUser> user) {
            this.user = user;
            return this;
        }

        @java.lang.Override
        public LeaderboardPartnersResponseMe build() {
            return new LeaderboardPartnersResponseMe(
                    firstReferralStartedAt, rank, totalEarningsUsd, totalVolumeUsd, user, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
