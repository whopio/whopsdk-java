/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.events;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.resources.events.requests.CreateEventsRequest;
import com.whop.api.resources.events.requests.ListEventsRequest;
import com.whop.api.resources.events.requests.PulseEventsRequest;
import com.whop.api.resources.events.requests.ValidatePixelEventsRequest;
import com.whop.api.resources.events.types.CreateEventsResponse;
import com.whop.api.resources.events.types.ListEventsResponseDataItem;
import com.whop.api.resources.events.types.PulseEventsResponseDataItem;
import com.whop.api.types.PixelValidation;
import java.util.concurrent.CompletableFuture;

public class AsyncEventsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawEventsClient rawClient;

    public AsyncEventsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawEventsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawEventsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists identity-linked events, most recent first by default. Pass identifier for one person's journey, or omit it to list events for an account within an explicit time range. Pass direction=asc to read a journey forwards from where it starts. Events are shaped like the POST /events intake: attribution in context, identity in user.
     */
    public CompletableFuture<SyncPagingIterable<ListEventsResponseDataItem>> list() {
        return this.rawClient.list().thenApply(response -> response.body());
    }

    /**
     * Lists identity-linked events, most recent first by default. Pass identifier for one person's journey, or omit it to list events for an account within an explicit time range. Pass direction=asc to read a journey forwards from where it starts. Events are shaped like the POST /events intake: attribution in context, identity in user.
     */
    public CompletableFuture<SyncPagingIterable<ListEventsResponseDataItem>> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists identity-linked events, most recent first by default. Pass identifier for one person's journey, or omit it to list events for an account within an explicit time range. Pass direction=asc to read a journey forwards from where it starts. Events are shaped like the POST /events intake: attribution in context, identity in user.
     */
    public CompletableFuture<SyncPagingIterable<ListEventsResponseDataItem>> list(ListEventsRequest request) {
        return this.rawClient.list(request).thenApply(response -> response.body());
    }

    /**
     * Lists identity-linked events, most recent first by default. Pass identifier for one person's journey, or omit it to list events for an account within an explicit time range. Pass direction=asc to read a journey forwards from where it starts. Events are shaped like the POST /events intake: attribution in context, identity in user.
     */
    public CompletableFuture<SyncPagingIterable<ListEventsResponseDataItem>> list(
            ListEventsRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Tracks a conversion or engagement event for an account.
     */
    public CompletableFuture<CreateEventsResponse> create(CreateEventsRequest request) {
        return this.rawClient.create(request).thenApply(response -> response.body());
    }

    /**
     * Tracks a conversion or engagement event for an account.
     */
    public CompletableFuture<CreateEventsResponse> create(CreateEventsRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a fully anonymized feed of recent platform-wide money movement, most recent first: purchases, affiliate commissions, card and ad spend, app revenue, off-platform sales, wallet deposits, card loads, claimed drops, transfers between accounts, and referral bonuses. Items carry only a <code>type</code>, the underlying event name, a USD amount, a coarse location under <code>user</code>, and a timestamp coarsened to the start of the minute; missing fields are omitted, not nulled. The payload is identical for every caller; no auth is required.
     */
    public CompletableFuture<SyncPagingIterable<PulseEventsResponseDataItem>> pulse() {
        return this.rawClient.pulse().thenApply(response -> response.body());
    }

    /**
     * Returns a fully anonymized feed of recent platform-wide money movement, most recent first: purchases, affiliate commissions, card and ad spend, app revenue, off-platform sales, wallet deposits, card loads, claimed drops, transfers between accounts, and referral bonuses. Items carry only a <code>type</code>, the underlying event name, a USD amount, a coarse location under <code>user</code>, and a timestamp coarsened to the start of the minute; missing fields are omitted, not nulled. The payload is identical for every caller; no auth is required.
     */
    public CompletableFuture<SyncPagingIterable<PulseEventsResponseDataItem>> pulse(RequestOptions requestOptions) {
        return this.rawClient.pulse(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns a fully anonymized feed of recent platform-wide money movement, most recent first: purchases, affiliate commissions, card and ad spend, app revenue, off-platform sales, wallet deposits, card loads, claimed drops, transfers between accounts, and referral bonuses. Items carry only a <code>type</code>, the underlying event name, a USD amount, a coarse location under <code>user</code>, and a timestamp coarsened to the start of the minute; missing fields are omitted, not nulled. The payload is identical for every caller; no auth is required.
     */
    public CompletableFuture<SyncPagingIterable<PulseEventsResponseDataItem>> pulse(PulseEventsRequest request) {
        return this.rawClient.pulse(request).thenApply(response -> response.body());
    }

    /**
     * Returns a fully anonymized feed of recent platform-wide money movement, most recent first: purchases, affiliate commissions, card and ad spend, app revenue, off-platform sales, wallet deposits, card loads, claimed drops, transfers between accounts, and referral bonuses. Items carry only a <code>type</code>, the underlying event name, a USD amount, a coarse location under <code>user</code>, and a timestamp coarsened to the start of the minute; missing fields are omitted, not nulled. The payload is identical for every caller; no auth is required.
     */
    public CompletableFuture<SyncPagingIterable<PulseEventsResponseDataItem>> pulse(
            PulseEventsRequest request, RequestOptions requestOptions) {
        return this.rawClient.pulse(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Checks whether the Whop pixel is installed for an account. Recent pixel events count as proof on their own, so an account that has sent data lately comes back installed without a <code>url</code>. Pass a <code>url</code> and events from that page settle it; conversion events are also read across the hostname because they commonly fire on a later confirmation page. If the requested page hasn't sent any events lately, it is fetched and read for the pixel and conversion events wired on it. <code>installed</code> is only true when the pixel was actually seen — in the account's events or in the page.
     */
    public CompletableFuture<PixelValidation> validatePixel() {
        return this.rawClient.validatePixel().thenApply(response -> response.body());
    }

    /**
     * Checks whether the Whop pixel is installed for an account. Recent pixel events count as proof on their own, so an account that has sent data lately comes back installed without a <code>url</code>. Pass a <code>url</code> and events from that page settle it; conversion events are also read across the hostname because they commonly fire on a later confirmation page. If the requested page hasn't sent any events lately, it is fetched and read for the pixel and conversion events wired on it. <code>installed</code> is only true when the pixel was actually seen — in the account's events or in the page.
     */
    public CompletableFuture<PixelValidation> validatePixel(RequestOptions requestOptions) {
        return this.rawClient.validatePixel(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Checks whether the Whop pixel is installed for an account. Recent pixel events count as proof on their own, so an account that has sent data lately comes back installed without a <code>url</code>. Pass a <code>url</code> and events from that page settle it; conversion events are also read across the hostname because they commonly fire on a later confirmation page. If the requested page hasn't sent any events lately, it is fetched and read for the pixel and conversion events wired on it. <code>installed</code> is only true when the pixel was actually seen — in the account's events or in the page.
     */
    public CompletableFuture<PixelValidation> validatePixel(ValidatePixelEventsRequest request) {
        return this.rawClient.validatePixel(request).thenApply(response -> response.body());
    }

    /**
     * Checks whether the Whop pixel is installed for an account. Recent pixel events count as proof on their own, so an account that has sent data lately comes back installed without a <code>url</code>. Pass a <code>url</code> and events from that page settle it; conversion events are also read across the hostname because they commonly fire on a later confirmation page. If the requested page hasn't sent any events lately, it is fetched and read for the pixel and conversion events wired on it. <code>installed</code> is only true when the pixel was actually seen — in the account's events or in the page.
     */
    public CompletableFuture<PixelValidation> validatePixel(
            ValidatePixelEventsRequest request, RequestOptions requestOptions) {
        return this.rawClient.validatePixel(request, requestOptions).thenApply(response -> response.body());
    }
}
