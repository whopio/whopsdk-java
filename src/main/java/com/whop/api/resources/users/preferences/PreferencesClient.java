/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.users.preferences;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.Suppliers;
import com.whop.api.resources.users.preferences.notifications.NotificationsClient;
import com.whop.api.resources.users.preferences.requests.UpdatePreferencesRequest;
import com.whop.api.types.UserPreferences;
import java.util.function.Supplier;

public class PreferencesClient {
    protected final ClientOptions clientOptions;

    private final RawPreferencesClient rawClient;

    protected final Supplier<NotificationsClient> notificationsClient;

    public PreferencesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawPreferencesClient(clientOptions);
        this.notificationsClient = Suppliers.memoize(() -> new NotificationsClient(clientOptions));
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawPreferencesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Retrieves the authenticated user's settings document. Addressed only as <code>me</code> — the document always belongs to the session user.
     */
    public UserPreferences retrieve() {
        return this.rawClient.retrieve().body();
    }

    /**
     * Retrieves the authenticated user's settings document. Addressed only as <code>me</code> — the document always belongs to the session user.
     */
    public UserPreferences retrieve(RequestOptions requestOptions) {
        return this.rawClient.retrieve(requestOptions).body();
    }

    /**
     * Updates the authenticated user's settings document. Replaces the top-level keys it is given and leaves the rest untouched.
     */
    public UserPreferences update() {
        return this.rawClient.update().body();
    }

    /**
     * Updates the authenticated user's settings document. Replaces the top-level keys it is given and leaves the rest untouched.
     */
    public UserPreferences update(RequestOptions requestOptions) {
        return this.rawClient.update(requestOptions).body();
    }

    /**
     * Updates the authenticated user's settings document. Replaces the top-level keys it is given and leaves the rest untouched.
     */
    public UserPreferences update(UpdatePreferencesRequest request) {
        return this.rawClient.update(request).body();
    }

    /**
     * Updates the authenticated user's settings document. Replaces the top-level keys it is given and leaves the rest untouched.
     */
    public UserPreferences update(UpdatePreferencesRequest request, RequestOptions requestOptions) {
        return this.rawClient.update(request, requestOptions).body();
    }

    public NotificationsClient notifications() {
        return this.notificationsClient.get();
    }
}
