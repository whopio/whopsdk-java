/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.people;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.resources.people.requests.ListPeopleRequest;
import com.whop.api.resources.people.requests.RetrievePeopleRequest;
import com.whop.api.resources.people.types.ListPeopleResponse;
import com.whop.api.resources.people.types.ListPeopleResponseDataItem;
import com.whop.api.resources.people.types.RetrievePeopleResponse;
import java.io.IOException;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.ExecutionException;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawPeopleClient {
    protected final ClientOptions clientOptions;

    public AsyncRawPeopleClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists the people (visitors and customers) of an account: the identity-linked person profiles aggregated from every pixel, payment, and platform event — identities, purchases and LTV, geo/device profile, traffic sources, and first/last marketing touches.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListPeopleResponseDataItem>>> list() {
        return list(ListPeopleRequest.builder().build());
    }

    /**
     * Lists the people (visitors and customers) of an account: the identity-linked person profiles aggregated from every pixel, payment, and platform event — identities, purchases and LTV, geo/device profile, traffic sources, and first/last marketing touches.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListPeopleResponseDataItem>>> list(
            RequestOptions requestOptions) {
        return list(ListPeopleRequest.builder().build(), requestOptions);
    }

    /**
     * Lists the people (visitors and customers) of an account: the identity-linked person profiles aggregated from every pixel, payment, and platform event — identities, purchases and LTV, geo/device profile, traffic sources, and first/last marketing touches.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListPeopleResponseDataItem>>> list(
            ListPeopleRequest request) {
        return list(request, null);
    }

    /**
     * Lists the people (visitors and customers) of an account: the identity-linked person profiles aggregated from every pixel, payment, and platform event — identities, purchases and LTV, geo/device profile, traffic sources, and first/last marketing touches.
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListPeopleResponseDataItem>>> list(
            ListPeopleRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("people");
        if (request.getAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "account_id", request.getAccountId().get(), false);
        }
        if (request.getQuery().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "query", request.getQuery().get(), false);
        }
        if (request.getAttributionModel().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "attribution_model", request.getAttributionModel().get(), false);
        }
        if (request.getCustomEvent().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "custom_event", request.getCustomEvent().get(), false);
        }
        if (request.getEventFrom().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "event_from", request.getEventFrom().get(), false);
        }
        if (request.getEventTo().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "event_to", request.getEventTo().get(), false);
        }
        if (request.getAudienceId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "audience_id", request.getAudienceId().get(), false);
        }
        if (request.getUserId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_id", request.getUserId().get(), false);
        }
        if (request.getEmail().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "email", request.getEmail().get(), false);
        }
        if (request.getPhone().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "phone", request.getPhone().get(), false);
        }
        if (request.getCountry().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "country", request.getCountry().get(), false);
        }
        if (request.getHasPurchased().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "has_purchased", request.getHasPurchased().get(), false);
        }
        if (request.getContactable().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "contactable", request.getContactable().get(), false);
        }
        if (request.getFirstSeenWithinDays().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "first_seen_within_days",
                    request.getFirstSeenWithinDays().get(),
                    false);
        }
        if (request.getLastSeenWithinDays().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "last_seen_within_days",
                    request.getLastSeenWithinDays().get(),
                    false);
        }
        if (request.getFirstSeenAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first_seen_after", request.getFirstSeenAfter().get(), false);
        }
        if (request.getFirstSeenBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first_seen_before", request.getFirstSeenBefore().get(), false);
        }
        if (request.getLastSeenAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last_seen_after", request.getLastSeenAfter().get(), false);
        }
        if (request.getLastSeenBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last_seen_before", request.getLastSeenBefore().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getOrder().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order", request.getOrder().get(), false);
        }
        if (request.getDirection().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "direction", request.getDirection().get(), false);
        }
        if (request.getSource().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "source", request.getSource().get(), true);
        }
        if (request.getEventName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "event_name", request.getEventName().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListPeopleResponseDataItem>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListPeopleResponse parsedResponse =
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListPeopleResponse.class);
                        String startingAfter =
                                parsedResponse.getPageInfo().getEndCursor().orElse(null);
                        ListPeopleRequest nextRequest = ListPeopleRequest.builder()
                                .from(request)
                                .after(startingAfter)
                                .build();
                        List<ListPeopleResponseDataItem> result = parsedResponse.getData();
                        future.complete(new WhopApiHttpResponse<>(
                                new SyncPagingIterable<ListPeopleResponseDataItem>(
                                        !startingAfter.isEmpty(), result, parsedResponse, () -> {
                                            try {
                                                return list(nextRequest, requestOptions)
                                                        .get()
                                                        .body();
                                            } catch (InterruptedException | ExecutionException e) {
                                                throw new RuntimeException(e);
                                            }
                                        }),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Retrieves one person for an account. The identifier can be a person ID (prefixed <code>prsn_</code>), a user ID (prefixed <code>user_</code>), an email address, or a phone number — merged people resolve to the surviving profile.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePeopleResponse>> retrieve(String id) {
        return retrieve(id, RetrievePeopleRequest.builder().build());
    }

    /**
     * Retrieves one person for an account. The identifier can be a person ID (prefixed <code>prsn_</code>), a user ID (prefixed <code>user_</code>), an email address, or a phone number — merged people resolve to the surviving profile.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePeopleResponse>> retrieve(
            String id, RequestOptions requestOptions) {
        return retrieve(id, RetrievePeopleRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves one person for an account. The identifier can be a person ID (prefixed <code>prsn_</code>), a user ID (prefixed <code>user_</code>), an email address, or a phone number — merged people resolve to the surviving profile.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePeopleResponse>> retrieve(
            String id, RetrievePeopleRequest request) {
        return retrieve(id, request, null);
    }

    /**
     * Retrieves one person for an account. The identifier can be a person ID (prefixed <code>prsn_</code>), a user ID (prefixed <code>user_</code>), an email address, or a phone number — merged people resolve to the surviving profile.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePeopleResponse>> retrieve(
            String id, RetrievePeopleRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("people")
                .addPathSegment(id);
        if (request.getAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "account_id", request.getAccountId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<RetrievePeopleResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new WhopApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RetrievePeopleResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
