/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.teammembers.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import com.whop.api.resources.teammembers.types.CreateTeamMembersRequestRole;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateTeamMembersRequest.Builder.class)
public final class CreateTeamMembersRequest {
    private final String accountId;

    private final Optional<String> email;

    private final CreateTeamMembersRequestRole role;

    private final Optional<String> userId;

    private final Map<String, Object> additionalProperties;

    private CreateTeamMembersRequest(
            String accountId,
            Optional<String> email,
            CreateTeamMembersRequestRole role,
            Optional<String> userId,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.email = email;
        this.role = role;
        this.userId = userId;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Account ID, prefixed <code>biz_</code>.
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return Email address to invite. Mutually exclusive with <code>user_id</code>. If the email already belongs to a Whop account it is treated the same as passing that account's <code>user_id</code>; otherwise a pending invite is created for the email.
     */
    @JsonProperty("email")
    public Optional<String> getEmail() {
        return email;
    }

    /**
     * @return The system role to grant.
     */
    @JsonProperty("role")
    public CreateTeamMembersRequestRole getRole() {
        return role;
    }

    /**
     * @return The user to add to the team, prefixed <code>user_</code>. Mutually exclusive with <code>email</code>.
     */
    @JsonProperty("user_id")
    public Optional<String> getUserId() {
        return userId;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateTeamMembersRequest && equalTo((CreateTeamMembersRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateTeamMembersRequest other) {
        return accountId.equals(other.accountId)
                && email.equals(other.email)
                && role.equals(other.role)
                && userId.equals(other.userId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.accountId, this.email, this.role, this.userId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountIdStage builder() {
        return new Builder();
    }

    public interface AccountIdStage {
        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         */
        RoleStage accountId(@NotNull String accountId);

        Builder from(CreateTeamMembersRequest other);
    }

    public interface RoleStage {
        /**
         * <p>The system role to grant.</p>
         */
        _FinalStage role(@NotNull CreateTeamMembersRequestRole role);
    }

    public interface _FinalStage {
        CreateTeamMembersRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Email address to invite. Mutually exclusive with <code>user_id</code>. If the email already belongs to a Whop account it is treated the same as passing that account's <code>user_id</code>; otherwise a pending invite is created for the email.</p>
         */
        _FinalStage email(Optional<String> email);

        _FinalStage email(String email);

        /**
         * <p>The user to add to the team, prefixed <code>user_</code>. Mutually exclusive with <code>email</code>.</p>
         */
        _FinalStage userId(Optional<String> userId);

        _FinalStage userId(String userId);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AccountIdStage, RoleStage, _FinalStage {
        private String accountId;

        private CreateTeamMembersRequestRole role;

        private Optional<String> userId = Optional.empty();

        private Optional<String> email = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateTeamMembersRequest other) {
            accountId(other.getAccountId());
            email(other.getEmail());
            role(other.getRole());
            userId(other.getUserId());
            return this;
        }

        /**
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * <p>Account ID, prefixed <code>biz_</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public RoleStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        /**
         * <p>The system role to grant.</p>
         * <p>The system role to grant.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("role")
        public _FinalStage role(@NotNull CreateTeamMembersRequestRole role) {
            this.role = Objects.requireNonNull(role, "role must not be null");
            return this;
        }

        /**
         * <p>The user to add to the team, prefixed <code>user_</code>. Mutually exclusive with <code>email</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage userId(String userId) {
            this.userId = Optional.ofNullable(userId);
            return this;
        }

        /**
         * <p>The user to add to the team, prefixed <code>user_</code>. Mutually exclusive with <code>email</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "user_id", nulls = Nulls.SKIP)
        public _FinalStage userId(Optional<String> userId) {
            this.userId = userId;
            return this;
        }

        /**
         * <p>Email address to invite. Mutually exclusive with <code>user_id</code>. If the email already belongs to a Whop account it is treated the same as passing that account's <code>user_id</code>; otherwise a pending invite is created for the email.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage email(String email) {
            this.email = Optional.ofNullable(email);
            return this;
        }

        /**
         * <p>Email address to invite. Mutually exclusive with <code>user_id</code>. If the email already belongs to a Whop account it is treated the same as passing that account's <code>user_id</code>; otherwise a pending invite is created for the email.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "email", nulls = Nulls.SKIP)
        public _FinalStage email(Optional<String> email) {
            this.email = email;
            return this;
        }

        @java.lang.Override
        public CreateTeamMembersRequest build() {
            return new CreateTeamMembersRequest(accountId, email, role, userId, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
