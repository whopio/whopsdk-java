/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.accounts.reserves;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.resources.accounts.reserves.requests.ListReservesRequest;
import com.whop.api.resources.accounts.reserves.types.ListReservesResponse;

public class ReservesClient {
    protected final ClientOptions clientOptions;

    private final RawReservesClient rawClient;

    public ReservesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawReservesClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawReservesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists what the account's held balance is made of, one entry per currency: the total held, why each part is held, and the days it unlocks.
     */
    public ListReservesResponse list(String accountId) {
        return this.rawClient.list(accountId).body();
    }

    /**
     * Lists what the account's held balance is made of, one entry per currency: the total held, why each part is held, and the days it unlocks.
     */
    public ListReservesResponse list(String accountId, RequestOptions requestOptions) {
        return this.rawClient.list(accountId, requestOptions).body();
    }

    /**
     * Lists what the account's held balance is made of, one entry per currency: the total held, why each part is held, and the days it unlocks.
     */
    public ListReservesResponse list(String accountId, ListReservesRequest request) {
        return this.rawClient.list(accountId, request).body();
    }

    /**
     * Lists what the account's held balance is made of, one entry per currency: the total held, why each part is held, and the days it unlocks.
     */
    public ListReservesResponse list(String accountId, ListReservesRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(accountId, request, requestOptions).body();
    }
}
