/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.accounts.preferences.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = UpdatePreferencesResponseAdsTripleWhaleIntegration.Builder.class)
public final class UpdatePreferencesResponseAdsTripleWhaleIntegration {
    private final Optional<String> maskedApiKey;

    private final Optional<String> shopDomain;

    private final UpdatePreferencesResponseAdsTripleWhaleIntegrationStatus status;

    private final Map<String, Object> additionalProperties;

    private UpdatePreferencesResponseAdsTripleWhaleIntegration(
            Optional<String> maskedApiKey,
            Optional<String> shopDomain,
            UpdatePreferencesResponseAdsTripleWhaleIntegrationStatus status,
            Map<String, Object> additionalProperties) {
        this.maskedApiKey = maskedApiKey;
        this.shopDomain = shopDomain;
        this.status = status;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The leading characters of the stored Data-In API key, followed by asterisks. The full key is never returned. <code>null</code> when no key is stored.
     */
    @JsonIgnore
    public Optional<String> getMaskedApiKey() {
        if (maskedApiKey == null) {
            return Optional.empty();
        }
        return maskedApiKey;
    }

    /**
     * @return The connected Shopify store domain spend is reported for, such as <code>acme.myshopify.com</code>. <code>null</code> when no store is connected.
     */
    @JsonIgnore
    public Optional<String> getShopDomain() {
        if (shopDomain == null) {
            return Optional.empty();
        }
        return shopDomain;
    }

    /**
     * @return Where the integration stands. <code>requires_shopify_store</code> means no Shopify store is connected — Triple Whale keys records by Shopify shop, so no spend is reported until one is.
     */
    @JsonProperty("status")
    public UpdatePreferencesResponseAdsTripleWhaleIntegrationStatus getStatus() {
        return status;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("masked_api_key")
    private Optional<String> _getMaskedApiKey() {
        return maskedApiKey;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("shop_domain")
    private Optional<String> _getShopDomain() {
        return shopDomain;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UpdatePreferencesResponseAdsTripleWhaleIntegration
                && equalTo((UpdatePreferencesResponseAdsTripleWhaleIntegration) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(UpdatePreferencesResponseAdsTripleWhaleIntegration other) {
        return maskedApiKey.equals(other.maskedApiKey)
                && shopDomain.equals(other.shopDomain)
                && status.equals(other.status);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.maskedApiKey, this.shopDomain, this.status);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static StatusStage builder() {
        return new Builder();
    }

    public interface StatusStage {
        /**
         * <p>Where the integration stands. <code>requires_shopify_store</code> means no Shopify store is connected — Triple Whale keys records by Shopify shop, so no spend is reported until one is.</p>
         */
        _FinalStage status(@NotNull UpdatePreferencesResponseAdsTripleWhaleIntegrationStatus status);

        Builder from(UpdatePreferencesResponseAdsTripleWhaleIntegration other);
    }

    public interface _FinalStage {
        UpdatePreferencesResponseAdsTripleWhaleIntegration build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The leading characters of the stored Data-In API key, followed by asterisks. The full key is never returned. <code>null</code> when no key is stored.</p>
         */
        _FinalStage maskedApiKey(Optional<String> maskedApiKey);

        _FinalStage maskedApiKey(String maskedApiKey);

        _FinalStage maskedApiKey(Nullable<String> maskedApiKey);

        /**
         * <p>The connected Shopify store domain spend is reported for, such as <code>acme.myshopify.com</code>. <code>null</code> when no store is connected.</p>
         */
        _FinalStage shopDomain(Optional<String> shopDomain);

        _FinalStage shopDomain(String shopDomain);

        _FinalStage shopDomain(Nullable<String> shopDomain);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements StatusStage, _FinalStage {
        private UpdatePreferencesResponseAdsTripleWhaleIntegrationStatus status;

        private Optional<String> shopDomain = Optional.empty();

        private Optional<String> maskedApiKey = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(UpdatePreferencesResponseAdsTripleWhaleIntegration other) {
            maskedApiKey(other.getMaskedApiKey());
            shopDomain(other.getShopDomain());
            status(other.getStatus());
            return this;
        }

        /**
         * <p>Where the integration stands. <code>requires_shopify_store</code> means no Shopify store is connected — Triple Whale keys records by Shopify shop, so no spend is reported until one is.</p>
         * <p>Where the integration stands. <code>requires_shopify_store</code> means no Shopify store is connected — Triple Whale keys records by Shopify shop, so no spend is reported until one is.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull UpdatePreferencesResponseAdsTripleWhaleIntegrationStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>The connected Shopify store domain spend is reported for, such as <code>acme.myshopify.com</code>. <code>null</code> when no store is connected.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage shopDomain(Nullable<String> shopDomain) {
            if (shopDomain.isNull()) {
                this.shopDomain = null;
            } else if (shopDomain.isEmpty()) {
                this.shopDomain = Optional.empty();
            } else {
                this.shopDomain = Optional.of(shopDomain.get());
            }
            return this;
        }

        /**
         * <p>The connected Shopify store domain spend is reported for, such as <code>acme.myshopify.com</code>. <code>null</code> when no store is connected.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage shopDomain(String shopDomain) {
            this.shopDomain = Optional.ofNullable(shopDomain);
            return this;
        }

        /**
         * <p>The connected Shopify store domain spend is reported for, such as <code>acme.myshopify.com</code>. <code>null</code> when no store is connected.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "shop_domain", nulls = Nulls.SKIP)
        public _FinalStage shopDomain(Optional<String> shopDomain) {
            this.shopDomain = shopDomain;
            return this;
        }

        /**
         * <p>The leading characters of the stored Data-In API key, followed by asterisks. The full key is never returned. <code>null</code> when no key is stored.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage maskedApiKey(Nullable<String> maskedApiKey) {
            if (maskedApiKey.isNull()) {
                this.maskedApiKey = null;
            } else if (maskedApiKey.isEmpty()) {
                this.maskedApiKey = Optional.empty();
            } else {
                this.maskedApiKey = Optional.of(maskedApiKey.get());
            }
            return this;
        }

        /**
         * <p>The leading characters of the stored Data-In API key, followed by asterisks. The full key is never returned. <code>null</code> when no key is stored.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage maskedApiKey(String maskedApiKey) {
            this.maskedApiKey = Optional.ofNullable(maskedApiKey);
            return this;
        }

        /**
         * <p>The leading characters of the stored Data-In API key, followed by asterisks. The full key is never returned. <code>null</code> when no key is stored.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "masked_api_key", nulls = Nulls.SKIP)
        public _FinalStage maskedApiKey(Optional<String> maskedApiKey) {
            this.maskedApiKey = maskedApiKey;
            return this;
        }

        @java.lang.Override
        public UpdatePreferencesResponseAdsTripleWhaleIntegration build() {
            return new UpdatePreferencesResponseAdsTripleWhaleIntegration(
                    maskedApiKey, shopDomain, status, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
