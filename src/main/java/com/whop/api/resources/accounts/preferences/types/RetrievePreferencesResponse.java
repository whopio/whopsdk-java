/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.accounts.preferences.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = RetrievePreferencesResponse.Builder.class)
public final class RetrievePreferencesResponse {
    private final RetrievePreferencesResponseAdsAgreement adsAgreement;

    private final Optional<RetrievePreferencesResponseAdsPaymentMethods> adsPaymentMethods;

    private final String adsReportingCurrency;

    private final String adsSchedulingTimezone;

    private final RetrievePreferencesResponseAdsTripleWhaleIntegration adsTripleWhaleIntegration;

    private final boolean cardsAutoTopUp;

    private final boolean disputeFighterEnabled;

    private final Map<String, Object> additionalProperties;

    private RetrievePreferencesResponse(
            RetrievePreferencesResponseAdsAgreement adsAgreement,
            Optional<RetrievePreferencesResponseAdsPaymentMethods> adsPaymentMethods,
            String adsReportingCurrency,
            String adsSchedulingTimezone,
            RetrievePreferencesResponseAdsTripleWhaleIntegration adsTripleWhaleIntegration,
            boolean cardsAutoTopUp,
            boolean disputeFighterEnabled,
            Map<String, Object> additionalProperties) {
        this.adsAgreement = adsAgreement;
        this.adsPaymentMethods = adsPaymentMethods;
        this.adsReportingCurrency = adsReportingCurrency;
        this.adsSchedulingTimezone = adsSchedulingTimezone;
        this.adsTripleWhaleIntegration = adsTripleWhaleIntegration;
        this.cardsAutoTopUp = cardsAutoTopUp;
        this.disputeFighterEnabled = disputeFighterEnabled;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The account's Whop Ads services and payment authorization agreement. While <code>pending_signature</code>, campaign launch is blocked; sign by answering <code>requested_information</code> via <code>PATCH /verifications/{id}</code>.
     */
    @JsonProperty("ads_agreement")
    public RetrievePreferencesResponseAdsAgreement getAdsAgreement() {
        return adsAgreement;
    }

    /**
     * @return How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when the primary fails. <code>null</code> until ads billing has been configured.
     */
    @JsonIgnore
    public Optional<RetrievePreferencesResponseAdsPaymentMethods> getAdsPaymentMethods() {
        if (adsPaymentMethods == null) {
            return Optional.empty();
        }
        return adsPaymentMethods;
    }

    /**
     * @return Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.
     */
    @JsonProperty("ads_reporting_currency")
    public String getAdsReportingCurrency() {
        return adsReportingCurrency;
    }

    /**
     * @return IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.
     */
    @JsonProperty("ads_scheduling_timezone")
    public String getAdsSchedulingTimezone() {
        return adsSchedulingTimezone;
    }

    /**
     * @return The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel.
     */
    @JsonProperty("ads_triple_whale_integration")
    public RetrievePreferencesResponseAdsTripleWhaleIntegration getAdsTripleWhaleIntegration() {
        return adsTripleWhaleIntegration;
    }

    /**
     * @return Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.
     */
    @JsonProperty("cards_auto_top_up")
    public boolean getCardsAutoTopUp() {
        return cardsAutoTopUp;
    }

    /**
     * @return Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.
     */
    @JsonProperty("dispute_fighter_enabled")
    public boolean getDisputeFighterEnabled() {
        return disputeFighterEnabled;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("ads_payment_methods")
    private Optional<RetrievePreferencesResponseAdsPaymentMethods> _getAdsPaymentMethods() {
        return adsPaymentMethods;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof RetrievePreferencesResponse && equalTo((RetrievePreferencesResponse) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(RetrievePreferencesResponse other) {
        return adsAgreement.equals(other.adsAgreement)
                && adsPaymentMethods.equals(other.adsPaymentMethods)
                && adsReportingCurrency.equals(other.adsReportingCurrency)
                && adsSchedulingTimezone.equals(other.adsSchedulingTimezone)
                && adsTripleWhaleIntegration.equals(other.adsTripleWhaleIntegration)
                && cardsAutoTopUp == other.cardsAutoTopUp
                && disputeFighterEnabled == other.disputeFighterEnabled;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.adsAgreement,
                this.adsPaymentMethods,
                this.adsReportingCurrency,
                this.adsSchedulingTimezone,
                this.adsTripleWhaleIntegration,
                this.cardsAutoTopUp,
                this.disputeFighterEnabled);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AdsAgreementStage builder() {
        return new Builder();
    }

    public interface AdsAgreementStage {
        /**
         * <p>The account's Whop Ads services and payment authorization agreement. While <code>pending_signature</code>, campaign launch is blocked; sign by answering <code>requested_information</code> via <code>PATCH /verifications/{id}</code>.</p>
         */
        AdsReportingCurrencyStage adsAgreement(@NotNull RetrievePreferencesResponseAdsAgreement adsAgreement);

        Builder from(RetrievePreferencesResponse other);
    }

    public interface AdsReportingCurrencyStage {
        /**
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.</p>
         */
        AdsSchedulingTimezoneStage adsReportingCurrency(@NotNull String adsReportingCurrency);
    }

    public interface AdsSchedulingTimezoneStage {
        /**
         * <p>IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.</p>
         */
        AdsTripleWhaleIntegrationStage adsSchedulingTimezone(@NotNull String adsSchedulingTimezone);
    }

    public interface AdsTripleWhaleIntegrationStage {
        /**
         * <p>The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel.</p>
         */
        CardsAutoTopUpStage adsTripleWhaleIntegration(
                @NotNull RetrievePreferencesResponseAdsTripleWhaleIntegration adsTripleWhaleIntegration);
    }

    public interface CardsAutoTopUpStage {
        /**
         * <p>Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.</p>
         */
        DisputeFighterEnabledStage cardsAutoTopUp(boolean cardsAutoTopUp);
    }

    public interface DisputeFighterEnabledStage {
        /**
         * <p>Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.</p>
         */
        _FinalStage disputeFighterEnabled(boolean disputeFighterEnabled);
    }

    public interface _FinalStage {
        RetrievePreferencesResponse build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when the primary fails. <code>null</code> until ads billing has been configured.</p>
         */
        _FinalStage adsPaymentMethods(Optional<RetrievePreferencesResponseAdsPaymentMethods> adsPaymentMethods);

        _FinalStage adsPaymentMethods(RetrievePreferencesResponseAdsPaymentMethods adsPaymentMethods);

        _FinalStage adsPaymentMethods(Nullable<RetrievePreferencesResponseAdsPaymentMethods> adsPaymentMethods);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements AdsAgreementStage,
                    AdsReportingCurrencyStage,
                    AdsSchedulingTimezoneStage,
                    AdsTripleWhaleIntegrationStage,
                    CardsAutoTopUpStage,
                    DisputeFighterEnabledStage,
                    _FinalStage {
        private RetrievePreferencesResponseAdsAgreement adsAgreement;

        private String adsReportingCurrency;

        private String adsSchedulingTimezone;

        private RetrievePreferencesResponseAdsTripleWhaleIntegration adsTripleWhaleIntegration;

        private boolean cardsAutoTopUp;

        private boolean disputeFighterEnabled;

        private Optional<RetrievePreferencesResponseAdsPaymentMethods> adsPaymentMethods = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(RetrievePreferencesResponse other) {
            adsAgreement(other.getAdsAgreement());
            adsPaymentMethods(other.getAdsPaymentMethods());
            adsReportingCurrency(other.getAdsReportingCurrency());
            adsSchedulingTimezone(other.getAdsSchedulingTimezone());
            adsTripleWhaleIntegration(other.getAdsTripleWhaleIntegration());
            cardsAutoTopUp(other.getCardsAutoTopUp());
            disputeFighterEnabled(other.getDisputeFighterEnabled());
            return this;
        }

        /**
         * <p>The account's Whop Ads services and payment authorization agreement. While <code>pending_signature</code>, campaign launch is blocked; sign by answering <code>requested_information</code> via <code>PATCH /verifications/{id}</code>.</p>
         * <p>The account's Whop Ads services and payment authorization agreement. While <code>pending_signature</code>, campaign launch is blocked; sign by answering <code>requested_information</code> via <code>PATCH /verifications/{id}</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ads_agreement")
        public AdsReportingCurrencyStage adsAgreement(@NotNull RetrievePreferencesResponseAdsAgreement adsAgreement) {
            this.adsAgreement = Objects.requireNonNull(adsAgreement, "adsAgreement must not be null");
            return this;
        }

        /**
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.</p>
         * <p>Lowercase ISO currency code, such as <code>usd</code> or <code>eur</code>, used to display ad spend and stats. Defaults to <code>usd</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ads_reporting_currency")
        public AdsSchedulingTimezoneStage adsReportingCurrency(@NotNull String adsReportingCurrency) {
            this.adsReportingCurrency =
                    Objects.requireNonNull(adsReportingCurrency, "adsReportingCurrency must not be null");
            return this;
        }

        /**
         * <p>IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.</p>
         * <p>IANA timezone (e.g. <code>America/New_York</code>) used to interpret campaign start/end times and to bucket reports. Defaults to <code>America/New_York</code> until explicitly overridden.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ads_scheduling_timezone")
        public AdsTripleWhaleIntegrationStage adsSchedulingTimezone(@NotNull String adsSchedulingTimezone) {
            this.adsSchedulingTimezone =
                    Objects.requireNonNull(adsSchedulingTimezone, "adsSchedulingTimezone must not be null");
            return this;
        }

        /**
         * <p>The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel.</p>
         * <p>The account's Triple Whale integration, which pushes Whop ad spend to Triple Whale's Data-In API so it reports as a <code>whop</code> channel.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("ads_triple_whale_integration")
        public CardsAutoTopUpStage adsTripleWhaleIntegration(
                @NotNull RetrievePreferencesResponseAdsTripleWhaleIntegration adsTripleWhaleIntegration) {
            this.adsTripleWhaleIntegration =
                    Objects.requireNonNull(adsTripleWhaleIntegration, "adsTripleWhaleIntegration must not be null");
            return this;
        }

        /**
         * <p>Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.</p>
         * <p>Whether incoming funds are automatically moved to the account's cards balance. <code>false</code> when the account has no cards balance.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("cards_auto_top_up")
        public DisputeFighterEnabledStage cardsAutoTopUp(boolean cardsAutoTopUp) {
            this.cardsAutoTopUp = cardsAutoTopUp;
            return this;
        }

        /**
         * <p>Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.</p>
         * <p>Whether Whop assembles and files the evidence response when this account's payments are disputed. Off by default; enabling it also opts the account into the success fee charged only on disputes it wins.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("dispute_fighter_enabled")
        public _FinalStage disputeFighterEnabled(boolean disputeFighterEnabled) {
            this.disputeFighterEnabled = disputeFighterEnabled;
            return this;
        }

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when the primary fails. <code>null</code> until ads billing has been configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage adsPaymentMethods(Nullable<RetrievePreferencesResponseAdsPaymentMethods> adsPaymentMethods) {
            if (adsPaymentMethods.isNull()) {
                this.adsPaymentMethods = null;
            } else if (adsPaymentMethods.isEmpty()) {
                this.adsPaymentMethods = Optional.empty();
            } else {
                this.adsPaymentMethods = Optional.of(adsPaymentMethods.get());
            }
            return this;
        }

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when the primary fails. <code>null</code> until ads billing has been configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage adsPaymentMethods(RetrievePreferencesResponseAdsPaymentMethods adsPaymentMethods) {
            this.adsPaymentMethods = Optional.ofNullable(adsPaymentMethods);
            return this;
        }

        /**
         * <p>How the account pays for Whop Ads spend. <code>primary</code> is charged first; <code>backup</code> covers the charge when the primary fails. <code>null</code> until ads billing has been configured.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "ads_payment_methods", nulls = Nulls.SKIP)
        public _FinalStage adsPaymentMethods(Optional<RetrievePreferencesResponseAdsPaymentMethods> adsPaymentMethods) {
            this.adsPaymentMethods = adsPaymentMethods;
            return this;
        }

        @java.lang.Override
        public RetrievePreferencesResponse build() {
            return new RetrievePreferencesResponse(
                    adsAgreement,
                    adsPaymentMethods,
                    adsReportingCurrency,
                    adsSchedulingTimezone,
                    adsTripleWhaleIntegration,
                    cardsAutoTopUp,
                    disputeFighterEnabled,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
