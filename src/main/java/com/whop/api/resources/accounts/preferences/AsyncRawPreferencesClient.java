/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.accounts.preferences;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.MediaTypes;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.resources.accounts.preferences.requests.RetrievePreferencesRequest;
import com.whop.api.resources.accounts.preferences.requests.UpdatePreferencesRequest;
import com.whop.api.resources.accounts.preferences.types.RetrievePreferencesResponse;
import com.whop.api.resources.accounts.preferences.types.UpdatePreferencesResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawPreferencesClient {
    protected final ClientOptions clientOptions;

    public AsyncRawPreferencesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Retrieves the account's preferences: a singleton settings document keyed by preference name.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePreferencesResponse>> retrieve(String accountId) {
        return retrieve(accountId, RetrievePreferencesRequest.builder().build());
    }

    /**
     * Retrieves the account's preferences: a singleton settings document keyed by preference name.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePreferencesResponse>> retrieve(
            String accountId, RequestOptions requestOptions) {
        return retrieve(accountId, RetrievePreferencesRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves the account's preferences: a singleton settings document keyed by preference name.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePreferencesResponse>> retrieve(
            String accountId, RetrievePreferencesRequest request) {
        return retrieve(accountId, request, null);
    }

    /**
     * Retrieves the account's preferences: a singleton settings document keyed by preference name.
     */
    public CompletableFuture<WhopApiHttpResponse<RetrievePreferencesResponse>> retrieve(
            String accountId, RetrievePreferencesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("accounts")
                .addPathSegment(accountId)
                .addPathSegments("preferences");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<RetrievePreferencesResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new WhopApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, RetrievePreferencesResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Updates the account's preferences. Each top-level key present in the body is replaced as a whole; omitted keys are left untouched. <code>ads_triple_whale_integration</code> takes the Data-In API key to connect with, or <code>null</code> to disconnect. <code>ads_payment_methods</code> always requires a <code>primary</code> entry. <code>backup</code> is optional and any pairing is allowed — two cards, <code>card</code>+<code>platform_balance</code>, or a single method — so a card-only advertiser can fund ads without a platform balance. The <code>primary</code> and <code>backup</code> must be different sources. A <code>platform_balance</code> entry may omit <code>id</code> to use the account's default Whop balance. Configuring a <code>card</code> requires a user token; account API keys can set up platform-balance billing only.
     */
    public CompletableFuture<WhopApiHttpResponse<UpdatePreferencesResponse>> update(String accountId) {
        return update(accountId, UpdatePreferencesRequest.builder().build());
    }

    /**
     * Updates the account's preferences. Each top-level key present in the body is replaced as a whole; omitted keys are left untouched. <code>ads_triple_whale_integration</code> takes the Data-In API key to connect with, or <code>null</code> to disconnect. <code>ads_payment_methods</code> always requires a <code>primary</code> entry. <code>backup</code> is optional and any pairing is allowed — two cards, <code>card</code>+<code>platform_balance</code>, or a single method — so a card-only advertiser can fund ads without a platform balance. The <code>primary</code> and <code>backup</code> must be different sources. A <code>platform_balance</code> entry may omit <code>id</code> to use the account's default Whop balance. Configuring a <code>card</code> requires a user token; account API keys can set up platform-balance billing only.
     */
    public CompletableFuture<WhopApiHttpResponse<UpdatePreferencesResponse>> update(
            String accountId, RequestOptions requestOptions) {
        return update(accountId, UpdatePreferencesRequest.builder().build(), requestOptions);
    }

    /**
     * Updates the account's preferences. Each top-level key present in the body is replaced as a whole; omitted keys are left untouched. <code>ads_triple_whale_integration</code> takes the Data-In API key to connect with, or <code>null</code> to disconnect. <code>ads_payment_methods</code> always requires a <code>primary</code> entry. <code>backup</code> is optional and any pairing is allowed — two cards, <code>card</code>+<code>platform_balance</code>, or a single method — so a card-only advertiser can fund ads without a platform balance. The <code>primary</code> and <code>backup</code> must be different sources. A <code>platform_balance</code> entry may omit <code>id</code> to use the account's default Whop balance. Configuring a <code>card</code> requires a user token; account API keys can set up platform-balance billing only.
     */
    public CompletableFuture<WhopApiHttpResponse<UpdatePreferencesResponse>> update(
            String accountId, UpdatePreferencesRequest request) {
        return update(accountId, request, null);
    }

    /**
     * Updates the account's preferences. Each top-level key present in the body is replaced as a whole; omitted keys are left untouched. <code>ads_triple_whale_integration</code> takes the Data-In API key to connect with, or <code>null</code> to disconnect. <code>ads_payment_methods</code> always requires a <code>primary</code> entry. <code>backup</code> is optional and any pairing is allowed — two cards, <code>card</code>+<code>platform_balance</code>, or a single method — so a card-only advertiser can fund ads without a platform balance. The <code>primary</code> and <code>backup</code> must be different sources. A <code>platform_balance</code> entry may omit <code>id</code> to use the account's default Whop balance. Configuring a <code>card</code> requires a user token; account API keys can set up platform-balance billing only.
     */
    public CompletableFuture<WhopApiHttpResponse<UpdatePreferencesResponse>> update(
            String accountId, UpdatePreferencesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("accounts")
                .addPathSegment(accountId)
                .addPathSegments("preferences");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new WhopApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<UpdatePreferencesResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new WhopApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, UpdatePreferencesResponse.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
