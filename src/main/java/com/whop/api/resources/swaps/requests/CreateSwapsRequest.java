/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.swaps.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import com.whop.api.resources.swaps.types.CreateSwapsRequestFromChain;
import com.whop.api.resources.swaps.types.CreateSwapsRequestToChain;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateSwapsRequest.Builder.class)
public final class CreateSwapsRequest {
    private final String accountId;

    private final Optional<String> amount;

    private final Optional<CreateSwapsRequestFromChain> fromChain;

    private final String fromToken;

    private final Optional<Integer> slippageBps;

    private final Optional<String> toAmount;

    private final Optional<CreateSwapsRequestToChain> toChain;

    private final String toToken;

    private final Map<String, Object> additionalProperties;

    private CreateSwapsRequest(
            String accountId,
            Optional<String> amount,
            Optional<CreateSwapsRequestFromChain> fromChain,
            String fromToken,
            Optional<Integer> slippageBps,
            Optional<String> toAmount,
            Optional<CreateSwapsRequestToChain> toChain,
            String toToken,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.amount = amount;
        this.fromChain = fromChain;
        this.fromToken = fromToken;
        this.slippageBps = slippageBps;
        this.toAmount = toAmount;
        this.toChain = toChain;
        this.toToken = toToken;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Business or user account ID (biz_* / user_*).
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead.
     */
    @JsonIgnore
    public Optional<String> getAmount() {
        if (amount == null) {
            return Optional.empty();
        }
        return amount;
    }

    /**
     * @return Source chain name or chain ID. Defaults to the source token's chain when omitted.
     */
    @JsonIgnore
    public Optional<CreateSwapsRequestFromChain> getFromChain() {
        if (fromChain == null) {
            return Optional.empty();
        }
        return fromChain;
    }

    /**
     * @return Source token contract address or ticker symbol, such as &quot;USDT&quot;.
     */
    @JsonProperty("from_token")
    public String getFromToken() {
        return fromToken;
    }

    /**
     * @return Maximum slippage tolerance in basis points.
     */
    @JsonIgnore
    public Optional<Integer> getSlippageBps() {
        if (slippageBps == null) {
            return Optional.empty();
        }
        return slippageBps;
    }

    /**
     * @return Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount.
     */
    @JsonIgnore
    public Optional<String> getToAmount() {
        if (toAmount == null) {
            return Optional.empty();
        }
        return toAmount;
    }

    /**
     * @return Destination chain name or chain ID. Defaults to the destination token's chain when omitted.
     */
    @JsonIgnore
    public Optional<CreateSwapsRequestToChain> getToChain() {
        if (toChain == null) {
            return Optional.empty();
        }
        return toChain;
    }

    /**
     * @return Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.
     */
    @JsonProperty("to_token")
    public String getToToken() {
        return toToken;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("amount")
    private Optional<String> _getAmount() {
        return amount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("from_chain")
    private Optional<CreateSwapsRequestFromChain> _getFromChain() {
        return fromChain;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("slippage_bps")
    private Optional<Integer> _getSlippageBps() {
        return slippageBps;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("to_amount")
    private Optional<String> _getToAmount() {
        return toAmount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("to_chain")
    private Optional<CreateSwapsRequestToChain> _getToChain() {
        return toChain;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateSwapsRequest && equalTo((CreateSwapsRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateSwapsRequest other) {
        return accountId.equals(other.accountId)
                && amount.equals(other.amount)
                && fromChain.equals(other.fromChain)
                && fromToken.equals(other.fromToken)
                && slippageBps.equals(other.slippageBps)
                && toAmount.equals(other.toAmount)
                && toChain.equals(other.toChain)
                && toToken.equals(other.toToken);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.amount,
                this.fromChain,
                this.fromToken,
                this.slippageBps,
                this.toAmount,
                this.toChain,
                this.toToken);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountIdStage builder() {
        return new Builder();
    }

    public interface AccountIdStage {
        /**
         * <p>Business or user account ID (biz_* / user_*).</p>
         */
        FromTokenStage accountId(@NotNull String accountId);

        Builder from(CreateSwapsRequest other);
    }

    public interface FromTokenStage {
        /**
         * <p>Source token contract address or ticker symbol, such as &quot;USDT&quot;.</p>
         */
        ToTokenStage fromToken(@NotNull String fromToken);
    }

    public interface ToTokenStage {
        /**
         * <p>Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.</p>
         */
        _FinalStage toToken(@NotNull String toToken);
    }

    public interface _FinalStage {
        CreateSwapsRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead.</p>
         */
        _FinalStage amount(Optional<String> amount);

        _FinalStage amount(String amount);

        _FinalStage amount(Nullable<String> amount);

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         */
        _FinalStage fromChain(Optional<CreateSwapsRequestFromChain> fromChain);

        _FinalStage fromChain(CreateSwapsRequestFromChain fromChain);

        _FinalStage fromChain(Nullable<CreateSwapsRequestFromChain> fromChain);

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         */
        _FinalStage slippageBps(Optional<Integer> slippageBps);

        _FinalStage slippageBps(Integer slippageBps);

        _FinalStage slippageBps(Nullable<Integer> slippageBps);

        /**
         * <p>Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount.</p>
         */
        _FinalStage toAmount(Optional<String> toAmount);

        _FinalStage toAmount(String toAmount);

        _FinalStage toAmount(Nullable<String> toAmount);

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         */
        _FinalStage toChain(Optional<CreateSwapsRequestToChain> toChain);

        _FinalStage toChain(CreateSwapsRequestToChain toChain);

        _FinalStage toChain(Nullable<CreateSwapsRequestToChain> toChain);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AccountIdStage, FromTokenStage, ToTokenStage, _FinalStage {
        private String accountId;

        private String fromToken;

        private String toToken;

        private Optional<CreateSwapsRequestToChain> toChain = Optional.empty();

        private Optional<String> toAmount = Optional.empty();

        private Optional<Integer> slippageBps = Optional.empty();

        private Optional<CreateSwapsRequestFromChain> fromChain = Optional.empty();

        private Optional<String> amount = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateSwapsRequest other) {
            accountId(other.getAccountId());
            amount(other.getAmount());
            fromChain(other.getFromChain());
            fromToken(other.getFromToken());
            slippageBps(other.getSlippageBps());
            toAmount(other.getToAmount());
            toChain(other.getToChain());
            toToken(other.getToToken());
            return this;
        }

        /**
         * <p>Business or user account ID (biz_* / user_*).</p>
         * <p>Business or user account ID (biz_* / user_*).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public FromTokenStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        /**
         * <p>Source token contract address or ticker symbol, such as &quot;USDT&quot;.</p>
         * <p>Source token contract address or ticker symbol, such as &quot;USDT&quot;.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("from_token")
        public ToTokenStage fromToken(@NotNull String fromToken) {
            this.fromToken = Objects.requireNonNull(fromToken, "fromToken must not be null");
            return this;
        }

        /**
         * <p>Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.</p>
         * <p>Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("to_token")
        public _FinalStage toToken(@NotNull String toToken) {
            this.toToken = Objects.requireNonNull(toToken, "toToken must not be null");
            return this;
        }

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toChain(Nullable<CreateSwapsRequestToChain> toChain) {
            if (toChain.isNull()) {
                this.toChain = null;
            } else if (toChain.isEmpty()) {
                this.toChain = Optional.empty();
            } else {
                this.toChain = Optional.of(toChain.get());
            }
            return this;
        }

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toChain(CreateSwapsRequestToChain toChain) {
            this.toChain = Optional.ofNullable(toChain);
            return this;
        }

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_chain", nulls = Nulls.SKIP)
        public _FinalStage toChain(Optional<CreateSwapsRequestToChain> toChain) {
            this.toChain = toChain;
            return this;
        }

        /**
         * <p>Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toAmount(Nullable<String> toAmount) {
            if (toAmount.isNull()) {
                this.toAmount = null;
            } else if (toAmount.isEmpty()) {
                this.toAmount = Optional.empty();
            } else {
                this.toAmount = Optional.of(toAmount.get());
            }
            return this;
        }

        /**
         * <p>Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toAmount(String toAmount) {
            this.toAmount = Optional.ofNullable(toAmount);
            return this;
        }

        /**
         * <p>Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_amount", nulls = Nulls.SKIP)
        public _FinalStage toAmount(Optional<String> toAmount) {
            this.toAmount = toAmount;
            return this;
        }

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage slippageBps(Nullable<Integer> slippageBps) {
            if (slippageBps.isNull()) {
                this.slippageBps = null;
            } else if (slippageBps.isEmpty()) {
                this.slippageBps = Optional.empty();
            } else {
                this.slippageBps = Optional.of(slippageBps.get());
            }
            return this;
        }

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage slippageBps(Integer slippageBps) {
            this.slippageBps = Optional.ofNullable(slippageBps);
            return this;
        }

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "slippage_bps", nulls = Nulls.SKIP)
        public _FinalStage slippageBps(Optional<Integer> slippageBps) {
            this.slippageBps = slippageBps;
            return this;
        }

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromChain(Nullable<CreateSwapsRequestFromChain> fromChain) {
            if (fromChain.isNull()) {
                this.fromChain = null;
            } else if (fromChain.isEmpty()) {
                this.fromChain = Optional.empty();
            } else {
                this.fromChain = Optional.of(fromChain.get());
            }
            return this;
        }

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromChain(CreateSwapsRequestFromChain fromChain) {
            this.fromChain = Optional.ofNullable(fromChain);
            return this;
        }

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "from_chain", nulls = Nulls.SKIP)
        public _FinalStage fromChain(Optional<CreateSwapsRequestFromChain> fromChain) {
            this.fromChain = fromChain;
            return this;
        }

        /**
         * <p>Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amount(Nullable<String> amount) {
            if (amount.isNull()) {
                this.amount = null;
            } else if (amount.isEmpty()) {
                this.amount = Optional.empty();
            } else {
                this.amount = Optional.of(amount.get());
            }
            return this;
        }

        /**
         * <p>Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amount(String amount) {
            this.amount = Optional.ofNullable(amount);
            return this;
        }

        /**
         * <p>Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount", nulls = Nulls.SKIP)
        public _FinalStage amount(Optional<String> amount) {
            this.amount = amount;
            return this;
        }

        @java.lang.Override
        public CreateSwapsRequest build() {
            return new CreateSwapsRequest(
                    accountId,
                    amount,
                    fromChain,
                    fromToken,
                    slippageBps,
                    toAmount,
                    toChain,
                    toToken,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
