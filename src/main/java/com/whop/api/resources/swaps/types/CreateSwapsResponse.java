/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.swaps.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateSwapsResponse.Builder.class)
public final class CreateSwapsResponse {
    private final String accountId;

    private final Optional<Double> amountIn;

    private final Optional<Double> amountOut;

    private final Optional<String> amountOutExpected;

    private final Optional<String> amountOutMin;

    private final Optional<CreateSwapsResponseFromToken> fromToken;

    private final Optional<String> id;

    private final CreateSwapsResponseObject object;

    private final Optional<String> rate;

    private final CreateSwapsResponseStatus status;

    private final Optional<String> toChain;

    private final Optional<CreateSwapsResponseToToken> toToken;

    private final Map<String, Object> additionalProperties;

    private CreateSwapsResponse(
            String accountId,
            Optional<Double> amountIn,
            Optional<Double> amountOut,
            Optional<String> amountOutExpected,
            Optional<String> amountOutMin,
            Optional<CreateSwapsResponseFromToken> fromToken,
            Optional<String> id,
            CreateSwapsResponseObject object,
            Optional<String> rate,
            CreateSwapsResponseStatus status,
            Optional<String> toChain,
            Optional<CreateSwapsResponseToToken> toToken,
            Map<String, Object> additionalProperties) {
        this.accountId = accountId;
        this.amountIn = amountIn;
        this.amountOut = amountOut;
        this.amountOutExpected = amountOutExpected;
        this.amountOutMin = amountOutMin;
        this.fromToken = fromToken;
        this.id = id;
        this.object = object;
        this.rate = rate;
        this.status = status;
        this.toChain = toChain;
        this.toToken = toToken;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Account ID that owns the wallet used for the swap.
     */
    @JsonProperty("account_id")
    public String getAccountId() {
        return accountId;
    }

    /**
     * @return Fiat pairs only: amount of the source currency converted. Null while a stablecoin repayment is processing.
     */
    @JsonIgnore
    public Optional<Double> getAmountIn() {
        if (amountIn == null) {
            return Optional.empty();
        }
        return amountIn;
    }

    /**
     * @return Fiat pairs only: amount credited in the destination currency. Null while a stablecoin repayment is processing.
     */
    @JsonIgnore
    public Optional<Double> getAmountOut() {
        if (amountOut == null) {
            return Optional.empty();
        }
        return amountOut;
    }

    /**
     * @return Expected destination token amount.
     */
    @JsonProperty("amount_out_expected")
    public Optional<String> getAmountOutExpected() {
        return amountOutExpected;
    }

    /**
     * @return Minimum destination amount after slippage.
     */
    @JsonProperty("amount_out_min")
    public Optional<String> getAmountOutMin() {
        return amountOutMin;
    }

    /**
     * @return Fiat pairs only: the source currency.
     */
    @JsonIgnore
    public Optional<CreateSwapsResponseFromToken> getFromToken() {
        if (fromToken == null) {
            return Optional.empty();
        }
        return fromToken;
    }

    /**
     * @return Swap ID. Poll <code>GET /swaps/:id</code> for status.
     */
    @JsonProperty("id")
    public Optional<String> getId() {
        return id;
    }

    @JsonProperty("object")
    public CreateSwapsResponseObject getObject() {
        return object;
    }

    /**
     * @return Quoted exchange rate used to create the swap.
     */
    @JsonProperty("rate")
    public Optional<String> getRate() {
        return rate;
    }

    /**
     * @return Swap status. Crypto swaps start <code>queued</code>; fiat conversions return <code>complete</code>, or <code>working</code> while a stablecoin repayment settles.
     */
    @JsonProperty("status")
    public CreateSwapsResponseStatus getStatus() {
        return status;
    }

    /**
     * @return Destination chain for the swap.
     */
    @JsonProperty("to_chain")
    public Optional<String> getToChain() {
        return toChain;
    }

    /**
     * @return Fiat pairs only: the destination currency.
     */
    @JsonIgnore
    public Optional<CreateSwapsResponseToToken> getToToken() {
        if (toToken == null) {
            return Optional.empty();
        }
        return toToken;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("amount_in")
    private Optional<Double> _getAmountIn() {
        return amountIn;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("amount_out")
    private Optional<Double> _getAmountOut() {
        return amountOut;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("from_token")
    private Optional<CreateSwapsResponseFromToken> _getFromToken() {
        return fromToken;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("to_token")
    private Optional<CreateSwapsResponseToToken> _getToToken() {
        return toToken;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateSwapsResponse && equalTo((CreateSwapsResponse) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateSwapsResponse other) {
        return accountId.equals(other.accountId)
                && amountIn.equals(other.amountIn)
                && amountOut.equals(other.amountOut)
                && amountOutExpected.equals(other.amountOutExpected)
                && amountOutMin.equals(other.amountOutMin)
                && fromToken.equals(other.fromToken)
                && id.equals(other.id)
                && object.equals(other.object)
                && rate.equals(other.rate)
                && status.equals(other.status)
                && toChain.equals(other.toChain)
                && toToken.equals(other.toToken);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.accountId,
                this.amountIn,
                this.amountOut,
                this.amountOutExpected,
                this.amountOutMin,
                this.fromToken,
                this.id,
                this.object,
                this.rate,
                this.status,
                this.toChain,
                this.toToken);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AccountIdStage builder() {
        return new Builder();
    }

    public interface AccountIdStage {
        /**
         * <p>Account ID that owns the wallet used for the swap.</p>
         */
        ObjectStage accountId(@NotNull String accountId);

        Builder from(CreateSwapsResponse other);
    }

    public interface ObjectStage {
        StatusStage object(@NotNull CreateSwapsResponseObject object);
    }

    public interface StatusStage {
        /**
         * <p>Swap status. Crypto swaps start <code>queued</code>; fiat conversions return <code>complete</code>, or <code>working</code> while a stablecoin repayment settles.</p>
         */
        _FinalStage status(@NotNull CreateSwapsResponseStatus status);
    }

    public interface _FinalStage {
        CreateSwapsResponse build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Fiat pairs only: amount of the source currency converted. Null while a stablecoin repayment is processing.</p>
         */
        _FinalStage amountIn(Optional<Double> amountIn);

        _FinalStage amountIn(Double amountIn);

        _FinalStage amountIn(Nullable<Double> amountIn);

        /**
         * <p>Fiat pairs only: amount credited in the destination currency. Null while a stablecoin repayment is processing.</p>
         */
        _FinalStage amountOut(Optional<Double> amountOut);

        _FinalStage amountOut(Double amountOut);

        _FinalStage amountOut(Nullable<Double> amountOut);

        /**
         * <p>Expected destination token amount.</p>
         */
        _FinalStage amountOutExpected(Optional<String> amountOutExpected);

        _FinalStage amountOutExpected(String amountOutExpected);

        /**
         * <p>Minimum destination amount after slippage.</p>
         */
        _FinalStage amountOutMin(Optional<String> amountOutMin);

        _FinalStage amountOutMin(String amountOutMin);

        /**
         * <p>Fiat pairs only: the source currency.</p>
         */
        _FinalStage fromToken(Optional<CreateSwapsResponseFromToken> fromToken);

        _FinalStage fromToken(CreateSwapsResponseFromToken fromToken);

        _FinalStage fromToken(Nullable<CreateSwapsResponseFromToken> fromToken);

        /**
         * <p>Swap ID. Poll <code>GET /swaps/:id</code> for status.</p>
         */
        _FinalStage id(Optional<String> id);

        _FinalStage id(String id);

        /**
         * <p>Quoted exchange rate used to create the swap.</p>
         */
        _FinalStage rate(Optional<String> rate);

        _FinalStage rate(String rate);

        /**
         * <p>Destination chain for the swap.</p>
         */
        _FinalStage toChain(Optional<String> toChain);

        _FinalStage toChain(String toChain);

        /**
         * <p>Fiat pairs only: the destination currency.</p>
         */
        _FinalStage toToken(Optional<CreateSwapsResponseToToken> toToken);

        _FinalStage toToken(CreateSwapsResponseToToken toToken);

        _FinalStage toToken(Nullable<CreateSwapsResponseToToken> toToken);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AccountIdStage, ObjectStage, StatusStage, _FinalStage {
        private String accountId;

        private CreateSwapsResponseObject object;

        private CreateSwapsResponseStatus status;

        private Optional<CreateSwapsResponseToToken> toToken = Optional.empty();

        private Optional<String> toChain = Optional.empty();

        private Optional<String> rate = Optional.empty();

        private Optional<String> id = Optional.empty();

        private Optional<CreateSwapsResponseFromToken> fromToken = Optional.empty();

        private Optional<String> amountOutMin = Optional.empty();

        private Optional<String> amountOutExpected = Optional.empty();

        private Optional<Double> amountOut = Optional.empty();

        private Optional<Double> amountIn = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateSwapsResponse other) {
            accountId(other.getAccountId());
            amountIn(other.getAmountIn());
            amountOut(other.getAmountOut());
            amountOutExpected(other.getAmountOutExpected());
            amountOutMin(other.getAmountOutMin());
            fromToken(other.getFromToken());
            id(other.getId());
            object(other.getObject());
            rate(other.getRate());
            status(other.getStatus());
            toChain(other.getToChain());
            toToken(other.getToToken());
            return this;
        }

        /**
         * <p>Account ID that owns the wallet used for the swap.</p>
         * <p>Account ID that owns the wallet used for the swap.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("account_id")
        public ObjectStage accountId(@NotNull String accountId) {
            this.accountId = Objects.requireNonNull(accountId, "accountId must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("object")
        public StatusStage object(@NotNull CreateSwapsResponseObject object) {
            this.object = Objects.requireNonNull(object, "object must not be null");
            return this;
        }

        /**
         * <p>Swap status. Crypto swaps start <code>queued</code>; fiat conversions return <code>complete</code>, or <code>working</code> while a stablecoin repayment settles.</p>
         * <p>Swap status. Crypto swaps start <code>queued</code>; fiat conversions return <code>complete</code>, or <code>working</code> while a stablecoin repayment settles.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public _FinalStage status(@NotNull CreateSwapsResponseStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>Fiat pairs only: the destination currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toToken(Nullable<CreateSwapsResponseToToken> toToken) {
            if (toToken.isNull()) {
                this.toToken = null;
            } else if (toToken.isEmpty()) {
                this.toToken = Optional.empty();
            } else {
                this.toToken = Optional.of(toToken.get());
            }
            return this;
        }

        /**
         * <p>Fiat pairs only: the destination currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toToken(CreateSwapsResponseToToken toToken) {
            this.toToken = Optional.ofNullable(toToken);
            return this;
        }

        /**
         * <p>Fiat pairs only: the destination currency.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_token", nulls = Nulls.SKIP)
        public _FinalStage toToken(Optional<CreateSwapsResponseToToken> toToken) {
            this.toToken = toToken;
            return this;
        }

        /**
         * <p>Destination chain for the swap.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toChain(String toChain) {
            this.toChain = Optional.ofNullable(toChain);
            return this;
        }

        /**
         * <p>Destination chain for the swap.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_chain", nulls = Nulls.SKIP)
        public _FinalStage toChain(Optional<String> toChain) {
            this.toChain = toChain;
            return this;
        }

        /**
         * <p>Quoted exchange rate used to create the swap.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage rate(String rate) {
            this.rate = Optional.ofNullable(rate);
            return this;
        }

        /**
         * <p>Quoted exchange rate used to create the swap.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "rate", nulls = Nulls.SKIP)
        public _FinalStage rate(Optional<String> rate) {
            this.rate = rate;
            return this;
        }

        /**
         * <p>Swap ID. Poll <code>GET /swaps/:id</code> for status.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage id(String id) {
            this.id = Optional.ofNullable(id);
            return this;
        }

        /**
         * <p>Swap ID. Poll <code>GET /swaps/:id</code> for status.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "id", nulls = Nulls.SKIP)
        public _FinalStage id(Optional<String> id) {
            this.id = id;
            return this;
        }

        /**
         * <p>Fiat pairs only: the source currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromToken(Nullable<CreateSwapsResponseFromToken> fromToken) {
            if (fromToken.isNull()) {
                this.fromToken = null;
            } else if (fromToken.isEmpty()) {
                this.fromToken = Optional.empty();
            } else {
                this.fromToken = Optional.of(fromToken.get());
            }
            return this;
        }

        /**
         * <p>Fiat pairs only: the source currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromToken(CreateSwapsResponseFromToken fromToken) {
            this.fromToken = Optional.ofNullable(fromToken);
            return this;
        }

        /**
         * <p>Fiat pairs only: the source currency.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "from_token", nulls = Nulls.SKIP)
        public _FinalStage fromToken(Optional<CreateSwapsResponseFromToken> fromToken) {
            this.fromToken = fromToken;
            return this;
        }

        /**
         * <p>Minimum destination amount after slippage.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountOutMin(String amountOutMin) {
            this.amountOutMin = Optional.ofNullable(amountOutMin);
            return this;
        }

        /**
         * <p>Minimum destination amount after slippage.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount_out_min", nulls = Nulls.SKIP)
        public _FinalStage amountOutMin(Optional<String> amountOutMin) {
            this.amountOutMin = amountOutMin;
            return this;
        }

        /**
         * <p>Expected destination token amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountOutExpected(String amountOutExpected) {
            this.amountOutExpected = Optional.ofNullable(amountOutExpected);
            return this;
        }

        /**
         * <p>Expected destination token amount.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount_out_expected", nulls = Nulls.SKIP)
        public _FinalStage amountOutExpected(Optional<String> amountOutExpected) {
            this.amountOutExpected = amountOutExpected;
            return this;
        }

        /**
         * <p>Fiat pairs only: amount credited in the destination currency. Null while a stablecoin repayment is processing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountOut(Nullable<Double> amountOut) {
            if (amountOut.isNull()) {
                this.amountOut = null;
            } else if (amountOut.isEmpty()) {
                this.amountOut = Optional.empty();
            } else {
                this.amountOut = Optional.of(amountOut.get());
            }
            return this;
        }

        /**
         * <p>Fiat pairs only: amount credited in the destination currency. Null while a stablecoin repayment is processing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountOut(Double amountOut) {
            this.amountOut = Optional.ofNullable(amountOut);
            return this;
        }

        /**
         * <p>Fiat pairs only: amount credited in the destination currency. Null while a stablecoin repayment is processing.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount_out", nulls = Nulls.SKIP)
        public _FinalStage amountOut(Optional<Double> amountOut) {
            this.amountOut = amountOut;
            return this;
        }

        /**
         * <p>Fiat pairs only: amount of the source currency converted. Null while a stablecoin repayment is processing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountIn(Nullable<Double> amountIn) {
            if (amountIn.isNull()) {
                this.amountIn = null;
            } else if (amountIn.isEmpty()) {
                this.amountIn = Optional.empty();
            } else {
                this.amountIn = Optional.of(amountIn.get());
            }
            return this;
        }

        /**
         * <p>Fiat pairs only: amount of the source currency converted. Null while a stablecoin repayment is processing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amountIn(Double amountIn) {
            this.amountIn = Optional.ofNullable(amountIn);
            return this;
        }

        /**
         * <p>Fiat pairs only: amount of the source currency converted. Null while a stablecoin repayment is processing.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount_in", nulls = Nulls.SKIP)
        public _FinalStage amountIn(Optional<Double> amountIn) {
            this.amountIn = amountIn;
            return this;
        }

        @java.lang.Override
        public CreateSwapsResponse build() {
            return new CreateSwapsResponse(
                    accountId,
                    amountIn,
                    amountOut,
                    amountOutExpected,
                    amountOutMin,
                    fromToken,
                    id,
                    object,
                    rate,
                    status,
                    toChain,
                    toToken,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
