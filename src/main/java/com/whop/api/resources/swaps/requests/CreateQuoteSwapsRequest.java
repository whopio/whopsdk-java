/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.swaps.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import com.whop.api.resources.swaps.types.CreateQuoteSwapsRequestFromChain;
import com.whop.api.resources.swaps.types.CreateQuoteSwapsRequestToChain;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateQuoteSwapsRequest.Builder.class)
public final class CreateQuoteSwapsRequest {
    private final String amount;

    private final Optional<String> fromAddress;

    private final Optional<CreateQuoteSwapsRequestFromChain> fromChain;

    private final String fromToken;

    private final Optional<Map<String, Object>> metadata;

    private final Optional<Integer> slippageBps;

    private final Optional<String> toAddress;

    private final Optional<CreateQuoteSwapsRequestToChain> toChain;

    private final String toToken;

    private final Map<String, Object> additionalProperties;

    private CreateQuoteSwapsRequest(
            String amount,
            Optional<String> fromAddress,
            Optional<CreateQuoteSwapsRequestFromChain> fromChain,
            String fromToken,
            Optional<Map<String, Object>> metadata,
            Optional<Integer> slippageBps,
            Optional<String> toAddress,
            Optional<CreateQuoteSwapsRequestToChain> toChain,
            String toToken,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.fromAddress = fromAddress;
        this.fromChain = fromChain;
        this.fromToken = fromToken;
        this.metadata = metadata;
        this.slippageBps = slippageBps;
        this.toAddress = toAddress;
        this.toChain = toChain;
        this.toToken = toToken;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Source token amount.
     */
    @JsonProperty("amount")
    public String getAmount() {
        return amount;
    }

    /**
     * @return Source wallet address used for the quote.
     */
    @JsonIgnore
    public Optional<String> getFromAddress() {
        if (fromAddress == null) {
            return Optional.empty();
        }
        return fromAddress;
    }

    /**
     * @return Source chain name or chain ID. Defaults to the source token's chain when omitted.
     */
    @JsonIgnore
    public Optional<CreateQuoteSwapsRequestFromChain> getFromChain() {
        if (fromChain == null) {
            return Optional.empty();
        }
        return fromChain;
    }

    /**
     * @return Source token contract address or ticker symbol, such as &quot;USDT&quot;.
     */
    @JsonProperty("from_token")
    public String getFromToken() {
        return fromToken;
    }

    /**
     * @return Metadata to include with the quote response.
     */
    @JsonProperty("metadata")
    public Optional<Map<String, Object>> getMetadata() {
        return metadata;
    }

    /**
     * @return Maximum slippage tolerance in basis points.
     */
    @JsonIgnore
    public Optional<Integer> getSlippageBps() {
        if (slippageBps == null) {
            return Optional.empty();
        }
        return slippageBps;
    }

    /**
     * @return Destination wallet address used for the quote.
     */
    @JsonIgnore
    public Optional<String> getToAddress() {
        if (toAddress == null) {
            return Optional.empty();
        }
        return toAddress;
    }

    /**
     * @return Destination chain name or chain ID. Defaults to the destination token's chain when omitted.
     */
    @JsonIgnore
    public Optional<CreateQuoteSwapsRequestToChain> getToChain() {
        if (toChain == null) {
            return Optional.empty();
        }
        return toChain;
    }

    /**
     * @return Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.
     */
    @JsonProperty("to_token")
    public String getToToken() {
        return toToken;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("from_address")
    private Optional<String> _getFromAddress() {
        return fromAddress;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("from_chain")
    private Optional<CreateQuoteSwapsRequestFromChain> _getFromChain() {
        return fromChain;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("slippage_bps")
    private Optional<Integer> _getSlippageBps() {
        return slippageBps;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("to_address")
    private Optional<String> _getToAddress() {
        return toAddress;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("to_chain")
    private Optional<CreateQuoteSwapsRequestToChain> _getToChain() {
        return toChain;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateQuoteSwapsRequest && equalTo((CreateQuoteSwapsRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateQuoteSwapsRequest other) {
        return amount.equals(other.amount)
                && fromAddress.equals(other.fromAddress)
                && fromChain.equals(other.fromChain)
                && fromToken.equals(other.fromToken)
                && metadata.equals(other.metadata)
                && slippageBps.equals(other.slippageBps)
                && toAddress.equals(other.toAddress)
                && toChain.equals(other.toChain)
                && toToken.equals(other.toToken);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amount,
                this.fromAddress,
                this.fromChain,
                this.fromToken,
                this.metadata,
                this.slippageBps,
                this.toAddress,
                this.toChain,
                this.toToken);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>Source token amount.</p>
         */
        FromTokenStage amount(@NotNull String amount);

        Builder from(CreateQuoteSwapsRequest other);
    }

    public interface FromTokenStage {
        /**
         * <p>Source token contract address or ticker symbol, such as &quot;USDT&quot;.</p>
         */
        ToTokenStage fromToken(@NotNull String fromToken);
    }

    public interface ToTokenStage {
        /**
         * <p>Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.</p>
         */
        _FinalStage toToken(@NotNull String toToken);
    }

    public interface _FinalStage {
        CreateQuoteSwapsRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Source wallet address used for the quote.</p>
         */
        _FinalStage fromAddress(Optional<String> fromAddress);

        _FinalStage fromAddress(String fromAddress);

        _FinalStage fromAddress(Nullable<String> fromAddress);

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         */
        _FinalStage fromChain(Optional<CreateQuoteSwapsRequestFromChain> fromChain);

        _FinalStage fromChain(CreateQuoteSwapsRequestFromChain fromChain);

        _FinalStage fromChain(Nullable<CreateQuoteSwapsRequestFromChain> fromChain);

        /**
         * <p>Metadata to include with the quote response.</p>
         */
        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         */
        _FinalStage slippageBps(Optional<Integer> slippageBps);

        _FinalStage slippageBps(Integer slippageBps);

        _FinalStage slippageBps(Nullable<Integer> slippageBps);

        /**
         * <p>Destination wallet address used for the quote.</p>
         */
        _FinalStage toAddress(Optional<String> toAddress);

        _FinalStage toAddress(String toAddress);

        _FinalStage toAddress(Nullable<String> toAddress);

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         */
        _FinalStage toChain(Optional<CreateQuoteSwapsRequestToChain> toChain);

        _FinalStage toChain(CreateQuoteSwapsRequestToChain toChain);

        _FinalStage toChain(Nullable<CreateQuoteSwapsRequestToChain> toChain);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AmountStage, FromTokenStage, ToTokenStage, _FinalStage {
        private String amount;

        private String fromToken;

        private String toToken;

        private Optional<CreateQuoteSwapsRequestToChain> toChain = Optional.empty();

        private Optional<String> toAddress = Optional.empty();

        private Optional<Integer> slippageBps = Optional.empty();

        private Optional<Map<String, Object>> metadata = Optional.empty();

        private Optional<CreateQuoteSwapsRequestFromChain> fromChain = Optional.empty();

        private Optional<String> fromAddress = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateQuoteSwapsRequest other) {
            amount(other.getAmount());
            fromAddress(other.getFromAddress());
            fromChain(other.getFromChain());
            fromToken(other.getFromToken());
            metadata(other.getMetadata());
            slippageBps(other.getSlippageBps());
            toAddress(other.getToAddress());
            toChain(other.getToChain());
            toToken(other.getToToken());
            return this;
        }

        /**
         * <p>Source token amount.</p>
         * <p>Source token amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public FromTokenStage amount(@NotNull String amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>Source token contract address or ticker symbol, such as &quot;USDT&quot;.</p>
         * <p>Source token contract address or ticker symbol, such as &quot;USDT&quot;.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("from_token")
        public ToTokenStage fromToken(@NotNull String fromToken) {
            this.fromToken = Objects.requireNonNull(fromToken, "fromToken must not be null");
            return this;
        }

        /**
         * <p>Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.</p>
         * <p>Destination token contract address or ticker symbol, such as &quot;XAUT&quot;.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("to_token")
        public _FinalStage toToken(@NotNull String toToken) {
            this.toToken = Objects.requireNonNull(toToken, "toToken must not be null");
            return this;
        }

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toChain(Nullable<CreateQuoteSwapsRequestToChain> toChain) {
            if (toChain.isNull()) {
                this.toChain = null;
            } else if (toChain.isEmpty()) {
                this.toChain = Optional.empty();
            } else {
                this.toChain = Optional.of(toChain.get());
            }
            return this;
        }

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toChain(CreateQuoteSwapsRequestToChain toChain) {
            this.toChain = Optional.ofNullable(toChain);
            return this;
        }

        /**
         * <p>Destination chain name or chain ID. Defaults to the destination token's chain when omitted.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_chain", nulls = Nulls.SKIP)
        public _FinalStage toChain(Optional<CreateQuoteSwapsRequestToChain> toChain) {
            this.toChain = toChain;
            return this;
        }

        /**
         * <p>Destination wallet address used for the quote.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toAddress(Nullable<String> toAddress) {
            if (toAddress.isNull()) {
                this.toAddress = null;
            } else if (toAddress.isEmpty()) {
                this.toAddress = Optional.empty();
            } else {
                this.toAddress = Optional.of(toAddress.get());
            }
            return this;
        }

        /**
         * <p>Destination wallet address used for the quote.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage toAddress(String toAddress) {
            this.toAddress = Optional.ofNullable(toAddress);
            return this;
        }

        /**
         * <p>Destination wallet address used for the quote.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "to_address", nulls = Nulls.SKIP)
        public _FinalStage toAddress(Optional<String> toAddress) {
            this.toAddress = toAddress;
            return this;
        }

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage slippageBps(Nullable<Integer> slippageBps) {
            if (slippageBps.isNull()) {
                this.slippageBps = null;
            } else if (slippageBps.isEmpty()) {
                this.slippageBps = Optional.empty();
            } else {
                this.slippageBps = Optional.of(slippageBps.get());
            }
            return this;
        }

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage slippageBps(Integer slippageBps) {
            this.slippageBps = Optional.ofNullable(slippageBps);
            return this;
        }

        /**
         * <p>Maximum slippage tolerance in basis points.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "slippage_bps", nulls = Nulls.SKIP)
        public _FinalStage slippageBps(Optional<Integer> slippageBps) {
            this.slippageBps = slippageBps;
            return this;
        }

        /**
         * <p>Metadata to include with the quote response.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        /**
         * <p>Metadata to include with the quote response.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromChain(Nullable<CreateQuoteSwapsRequestFromChain> fromChain) {
            if (fromChain.isNull()) {
                this.fromChain = null;
            } else if (fromChain.isEmpty()) {
                this.fromChain = Optional.empty();
            } else {
                this.fromChain = Optional.of(fromChain.get());
            }
            return this;
        }

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromChain(CreateQuoteSwapsRequestFromChain fromChain) {
            this.fromChain = Optional.ofNullable(fromChain);
            return this;
        }

        /**
         * <p>Source chain name or chain ID. Defaults to the source token's chain when omitted.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "from_chain", nulls = Nulls.SKIP)
        public _FinalStage fromChain(Optional<CreateQuoteSwapsRequestFromChain> fromChain) {
            this.fromChain = fromChain;
            return this;
        }

        /**
         * <p>Source wallet address used for the quote.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromAddress(Nullable<String> fromAddress) {
            if (fromAddress.isNull()) {
                this.fromAddress = null;
            } else if (fromAddress.isEmpty()) {
                this.fromAddress = Optional.empty();
            } else {
                this.fromAddress = Optional.of(fromAddress.get());
            }
            return this;
        }

        /**
         * <p>Source wallet address used for the quote.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage fromAddress(String fromAddress) {
            this.fromAddress = Optional.ofNullable(fromAddress);
            return this;
        }

        /**
         * <p>Source wallet address used for the quote.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "from_address", nulls = Nulls.SKIP)
        public _FinalStage fromAddress(Optional<String> fromAddress) {
            this.fromAddress = fromAddress;
            return this;
        }

        @java.lang.Override
        public CreateQuoteSwapsRequest build() {
            return new CreateQuoteSwapsRequest(
                    amount,
                    fromAddress,
                    fromChain,
                    fromToken,
                    metadata,
                    slippageBps,
                    toAddress,
                    toChain,
                    toToken,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
