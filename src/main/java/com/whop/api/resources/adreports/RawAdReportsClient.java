/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.adreports;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.InternalServerError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.TooManyRequestsError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.errors.UnprocessableEntityError;
import com.whop.api.resources.adreports.requests.RetrieveAdReportsRequest;
import com.whop.api.types.AdReport;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawAdReportsClient {
    protected final ClientOptions clientOptions;

    public RawAdReportsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Performance report for a company, ad campaigns, ad groups, or ads. Always returns aggregate <code>summary</code> totals summed across the scope. Set <code>granularity</code> to additionally get a time series, or set <code>breakdown</code> (<code>campaign</code>/<code>ad_group</code>/<code>ad</code>) to additionally get per-entity rows inside the requested scope. Exactly one of <code>companyId</code>, <code>adCampaignIds</code>, <code>adGroupIds</code>, or <code>adIds</code> must be provided.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>ad_campaign:stats:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<AdReport> retrieve(RetrieveAdReportsRequest request) {
        return retrieve(request, null);
    }

    /**
     * Performance report for a company, ad campaigns, ad groups, or ads. Always returns aggregate <code>summary</code> totals summed across the scope. Set <code>granularity</code> to additionally get a time series, or set <code>breakdown</code> (<code>campaign</code>/<code>ad_group</code>/<code>ad</code>) to additionally get per-entity rows inside the requested scope. Exactly one of <code>companyId</code>, <code>adCampaignIds</code>, <code>adGroupIds</code>, or <code>adIds</code> must be provided.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>ad_campaign:stats:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<AdReport> retrieve(RetrieveAdReportsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("ad_reports");
        if (request.getBreakdown().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "breakdown", request.getBreakdown().get(), false);
        }
        if (request.getCompanyId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "company_id", request.getCompanyId().get(), false);
        }
        if (request.getCurrency().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "currency", request.getCurrency().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "from", request.getFrom(), false);
        if (request.getGranularity().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "granularity", request.getGranularity().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "to", request.getTo(), false);
        if (request.getAdCampaignIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ad_campaign_ids", request.getAdCampaignIds().get(), true);
        }
        if (request.getAdGroupIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ad_group_ids", request.getAdGroupIds().get(), true);
        }
        if (request.getAdIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "ad_ids", request.getAdIds().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AdReport.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }
}
