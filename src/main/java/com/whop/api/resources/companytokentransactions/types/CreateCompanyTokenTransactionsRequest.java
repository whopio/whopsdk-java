/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.companytokentransactions.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class CreateCompanyTokenTransactionsRequest {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private CreateCompanyTokenTransactionsRequest(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static CreateCompanyTokenTransactionsRequest transfer(CreateCompanyTokenTransactionsRequestTransfer value) {
        return new CreateCompanyTokenTransactionsRequest(new TransferValue(value));
    }

    public static CreateCompanyTokenTransactionsRequest add(CreateCompanyTokenTransactionsRequestAdd value) {
        return new CreateCompanyTokenTransactionsRequest(new AddValue(value));
    }

    public static CreateCompanyTokenTransactionsRequest subtract(CreateCompanyTokenTransactionsRequestSubtract value) {
        return new CreateCompanyTokenTransactionsRequest(new SubtractValue(value));
    }

    public boolean isTransfer() {
        return value instanceof TransferValue;
    }

    public boolean isAdd() {
        return value instanceof AddValue;
    }

    public boolean isSubtract() {
        return value instanceof SubtractValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CreateCompanyTokenTransactionsRequestTransfer> getTransfer() {
        if (isTransfer()) {
            return Optional.of(((TransferValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CreateCompanyTokenTransactionsRequestAdd> getAdd() {
        if (isAdd()) {
            return Optional.of(((AddValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CreateCompanyTokenTransactionsRequestSubtract> getSubtract() {
        if (isSubtract()) {
            return Optional.of(((SubtractValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateCompanyTokenTransactionsRequest
                && value.equals(((CreateCompanyTokenTransactionsRequest) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitTransfer(CreateCompanyTokenTransactionsRequestTransfer transfer);

        T visitAdd(CreateCompanyTokenTransactionsRequestAdd add);

        T visitSubtract(CreateCompanyTokenTransactionsRequestSubtract subtract);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "transaction_type",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(TransferValue.class),
        @JsonSubTypes.Type(AddValue.class),
        @JsonSubTypes.Type(SubtractValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("transfer")
    @JsonIgnoreProperties("transaction_type")
    private static final class TransferValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "transaction_type", allowSetters = true)
        private CreateCompanyTokenTransactionsRequestTransfer value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TransferValue() {}

        private TransferValue(CreateCompanyTokenTransactionsRequestTransfer value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTransfer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TransferValue && equalTo((TransferValue) other);
        }

        private boolean equalTo(TransferValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateCompanyTokenTransactionsRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("add")
    @JsonIgnoreProperties("transaction_type")
    private static final class AddValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "transaction_type", allowSetters = true)
        private CreateCompanyTokenTransactionsRequestAdd value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AddValue() {}

        private AddValue(CreateCompanyTokenTransactionsRequestAdd value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAdd(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AddValue && equalTo((AddValue) other);
        }

        private boolean equalTo(AddValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateCompanyTokenTransactionsRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("subtract")
    @JsonIgnoreProperties("transaction_type")
    private static final class SubtractValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "transaction_type", allowSetters = true)
        private CreateCompanyTokenTransactionsRequestSubtract value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SubtractValue() {}

        private SubtractValue(CreateCompanyTokenTransactionsRequestSubtract value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSubtract(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SubtractValue && equalTo((SubtractValue) other);
        }

        private boolean equalTo(SubtractValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateCompanyTokenTransactionsRequest{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("transaction_type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateCompanyTokenTransactionsRequest{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
