/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.recommendedactions;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.resources.recommendedactions.requests.ListExecutionsRecommendedActionsRequest;
import com.whop.api.resources.recommendedactions.requests.ListRecommendedActionsRequest;
import com.whop.api.resources.recommendedactions.requests.RetrieveRecommendedActionsRequest;
import com.whop.api.resources.recommendedactions.requests.RunRecommendedActionsRequest;
import com.whop.api.resources.recommendedactions.types.ListExecutionsRecommendedActionsResponse;
import com.whop.api.resources.recommendedactions.types.ListRecommendedActionsResponse;
import com.whop.api.resources.recommendedactions.types.RunRecommendedActionsResponse;
import com.whop.api.types.AccountRecommendedActionChain;
import java.util.concurrent.CompletableFuture;

public class AsyncRecommendedActionsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawRecommendedActionsClient rawClient;

    public AsyncRecommendedActionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawRecommendedActionsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawRecommendedActionsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists the recommended action chains for an account — short sequences of actions (create a product, price it, publish it) the account should run next, gated on what it already has.
     */
    public CompletableFuture<ListRecommendedActionsResponse> list() {
        return this.rawClient.list().thenApply(response -> response.body());
    }

    /**
     * Lists the recommended action chains for an account — short sequences of actions (create a product, price it, publish it) the account should run next, gated on what it already has.
     */
    public CompletableFuture<ListRecommendedActionsResponse> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists the recommended action chains for an account — short sequences of actions (create a product, price it, publish it) the account should run next, gated on what it already has.
     */
    public CompletableFuture<ListRecommendedActionsResponse> list(ListRecommendedActionsRequest request) {
        return this.rawClient.list(request).thenApply(response -> response.body());
    }

    /**
     * Lists the recommended action chains for an account — short sequences of actions (create a product, price it, publish it) the account should run next, gated on what it already has.
     */
    public CompletableFuture<ListRecommendedActionsResponse> list(
            ListRecommendedActionsRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieves a recommended action chain by id, including chains that have already been run. Seeded chains are reconstructed from their hard-coded chain; generated chains are read from the account's stored chain, with each step's filled-in input.
     */
    public CompletableFuture<AccountRecommendedActionChain> retrieve(String id) {
        return this.rawClient.retrieve(id).thenApply(response -> response.body());
    }

    /**
     * Retrieves a recommended action chain by id, including chains that have already been run. Seeded chains are reconstructed from their hard-coded chain; generated chains are read from the account's stored chain, with each step's filled-in input.
     */
    public CompletableFuture<AccountRecommendedActionChain> retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Retrieves a recommended action chain by id, including chains that have already been run. Seeded chains are reconstructed from their hard-coded chain; generated chains are read from the account's stored chain, with each step's filled-in input.
     */
    public CompletableFuture<AccountRecommendedActionChain> retrieve(
            String id, RetrieveRecommendedActionsRequest request) {
        return this.rawClient.retrieve(id, request).thenApply(response -> response.body());
    }

    /**
     * Retrieves a recommended action chain by id, including chains that have already been run. Seeded chains are reconstructed from their hard-coded chain; generated chains are read from the account's stored chain, with each step's filled-in input.
     */
    public CompletableFuture<AccountRecommendedActionChain> retrieve(
            String id, RetrieveRecommendedActionsRequest request, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Records that the caller ran a recommended action chain. Nothing is executed server-side yet — the client follows the chain's step CTAs itself; this writes the <code>recommended_action_chain.executed</code> analytics event.
     */
    public CompletableFuture<RunRecommendedActionsResponse> run(String id) {
        return this.rawClient.run(id).thenApply(response -> response.body());
    }

    /**
     * Records that the caller ran a recommended action chain. Nothing is executed server-side yet — the client follows the chain's step CTAs itself; this writes the <code>recommended_action_chain.executed</code> analytics event.
     */
    public CompletableFuture<RunRecommendedActionsResponse> run(String id, RequestOptions requestOptions) {
        return this.rawClient.run(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Records that the caller ran a recommended action chain. Nothing is executed server-side yet — the client follows the chain's step CTAs itself; this writes the <code>recommended_action_chain.executed</code> analytics event.
     */
    public CompletableFuture<RunRecommendedActionsResponse> run(String id, RunRecommendedActionsRequest request) {
        return this.rawClient.run(id, request).thenApply(response -> response.body());
    }

    /**
     * Records that the caller ran a recommended action chain. Nothing is executed server-side yet — the client follows the chain's step CTAs itself; this writes the <code>recommended_action_chain.executed</code> analytics event.
     */
    public CompletableFuture<RunRecommendedActionsResponse> run(
            String id, RunRecommendedActionsRequest request, RequestOptions requestOptions) {
        return this.rawClient.run(id, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists the per-step record of a recommended action chain the server ran — one entry per step in position order, each carrying its current status and, once the step completed, the API response it produced. A chain that was never run server-side returns an empty list.
     */
    public CompletableFuture<ListExecutionsRecommendedActionsResponse> listExecutions(String id) {
        return this.rawClient.listExecutions(id).thenApply(response -> response.body());
    }

    /**
     * Lists the per-step record of a recommended action chain the server ran — one entry per step in position order, each carrying its current status and, once the step completed, the API response it produced. A chain that was never run server-side returns an empty list.
     */
    public CompletableFuture<ListExecutionsRecommendedActionsResponse> listExecutions(
            String id, RequestOptions requestOptions) {
        return this.rawClient.listExecutions(id, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists the per-step record of a recommended action chain the server ran — one entry per step in position order, each carrying its current status and, once the step completed, the API response it produced. A chain that was never run server-side returns an empty list.
     */
    public CompletableFuture<ListExecutionsRecommendedActionsResponse> listExecutions(
            String id, ListExecutionsRecommendedActionsRequest request) {
        return this.rawClient.listExecutions(id, request).thenApply(response -> response.body());
    }

    /**
     * Lists the per-step record of a recommended action chain the server ran — one entry per step in position order, each carrying its current status and, once the step completed, the API response it produced. A chain that was never run server-side returns an empty list.
     */
    public CompletableFuture<ListExecutionsRecommendedActionsResponse> listExecutions(
            String id, ListExecutionsRecommendedActionsRequest request, RequestOptions requestOptions) {
        return this.rawClient.listExecutions(id, request, requestOptions).thenApply(response -> response.body());
    }
}
