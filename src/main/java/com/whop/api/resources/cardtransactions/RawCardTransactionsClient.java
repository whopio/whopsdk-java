/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.cardtransactions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.resources.cardtransactions.requests.ListCardTransactionsRequest;
import com.whop.api.resources.cardtransactions.requests.RetrieveCardTransactionsRequest;
import com.whop.api.resources.cardtransactions.types.ListCardTransactionsResponse;
import com.whop.api.types.CardTransaction;
import java.io.IOException;
import java.util.List;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCardTransactionsClient {
    protected final ClientOptions clientOptions;

    public RawCardTransactionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Lists an account's card transactions, newest first. Defaults to the account the credential belongs to. Covers every card the owner has ever had, including canceled cards and spend that predates a re-application, and team members only see transactions on the cards assigned to them. Pass <code>transaction_ids</code> to fetch specific transactions instead of paging for them.
     */
    public WhopApiHttpResponse<SyncPagingIterable<CardTransaction>> list() {
        return list(ListCardTransactionsRequest.builder().build());
    }

    /**
     * Lists an account's card transactions, newest first. Defaults to the account the credential belongs to. Covers every card the owner has ever had, including canceled cards and spend that predates a re-application, and team members only see transactions on the cards assigned to them. Pass <code>transaction_ids</code> to fetch specific transactions instead of paging for them.
     */
    public WhopApiHttpResponse<SyncPagingIterable<CardTransaction>> list(RequestOptions requestOptions) {
        return list(ListCardTransactionsRequest.builder().build(), requestOptions);
    }

    /**
     * Lists an account's card transactions, newest first. Defaults to the account the credential belongs to. Covers every card the owner has ever had, including canceled cards and spend that predates a re-application, and team members only see transactions on the cards assigned to them. Pass <code>transaction_ids</code> to fetch specific transactions instead of paging for them.
     */
    public WhopApiHttpResponse<SyncPagingIterable<CardTransaction>> list(ListCardTransactionsRequest request) {
        return list(request, null);
    }

    /**
     * Lists an account's card transactions, newest first. Defaults to the account the credential belongs to. Covers every card the owner has ever had, including canceled cards and spend that predates a re-application, and team members only see transactions on the cards assigned to them. Pass <code>transaction_ids</code> to fetch specific transactions instead of paging for them.
     */
    public WhopApiHttpResponse<SyncPagingIterable<CardTransaction>> list(
            ListCardTransactionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("card_transactions");
        if (request.getAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "account_id", request.getAccountId().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (request.getCreatedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "created_after", request.getCreatedAfter().get(), false);
        }
        if (request.getCreatedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "created_before", request.getCreatedBefore().get(), false);
        }
        if (request.getOrder().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order", request.getOrder().get(), false);
        }
        if (request.getDirection().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "direction", request.getDirection().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getTransactionIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "transaction_ids", request.getTransactionIds().get(), true);
        }
        if (request.getCardId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "card_id", request.getCardId().get(), true);
        }
        if (request.getCardholderId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cardholder_id", request.getCardholderId().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListCardTransactionsResponse parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCardTransactionsResponse.class);
                Optional<String> startingAfter = parsedResponse.getPageInfo().getEndCursor();
                ListCardTransactionsRequest nextRequest = ListCardTransactionsRequest.builder()
                        .from(request)
                        .after(startingAfter)
                        .build();
                List<CardTransaction> result = parsedResponse.getData();
                return new WhopApiHttpResponse<>(
                        new SyncPagingIterable<CardTransaction>(
                                startingAfter != null, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Fetches a single card transaction by its <code>citx_</code> identifier. The owner defaults to the account the credential belongs to.
     */
    public WhopApiHttpResponse<CardTransaction> retrieve(String id) {
        return retrieve(id, RetrieveCardTransactionsRequest.builder().build());
    }

    /**
     * Fetches a single card transaction by its <code>citx_</code> identifier. The owner defaults to the account the credential belongs to.
     */
    public WhopApiHttpResponse<CardTransaction> retrieve(String id, RequestOptions requestOptions) {
        return retrieve(id, RetrieveCardTransactionsRequest.builder().build(), requestOptions);
    }

    /**
     * Fetches a single card transaction by its <code>citx_</code> identifier. The owner defaults to the account the credential belongs to.
     */
    public WhopApiHttpResponse<CardTransaction> retrieve(String id, RetrieveCardTransactionsRequest request) {
        return retrieve(id, request, null);
    }

    /**
     * Fetches a single card transaction by its <code>citx_</code> identifier. The owner defaults to the account the credential belongs to.
     */
    public WhopApiHttpResponse<CardTransaction> retrieve(
            String id, RetrieveCardTransactionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("card_transactions")
                .addPathSegment(id);
        if (request.getAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "account_id", request.getAccountId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CardTransaction.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }
}
