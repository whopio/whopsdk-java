/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.courses;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.MediaTypes;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.InternalServerError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.TooManyRequestsError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.errors.UnprocessableEntityError;
import com.whop.api.resources.courses.requests.CreateCoursesRequest;
import com.whop.api.resources.courses.requests.DeleteCoursesRequest;
import com.whop.api.resources.courses.requests.ListCoursesRequest;
import com.whop.api.resources.courses.requests.RetrieveCoursesRequest;
import com.whop.api.resources.courses.requests.UpdateCoursesRequest;
import com.whop.api.resources.courses.types.ListCoursesResponse;
import com.whop.api.types.Course;
import com.whop.api.types.CourseListItem;
import java.io.IOException;
import java.util.List;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCoursesClient {
    protected final ClientOptions clientOptions;

    public RawCoursesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of courses, filtered by either an experience or a company.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseListItem>> list() {
        return list(ListCoursesRequest.builder().build());
    }

    /**
     * Returns a paginated list of courses, filtered by either an experience or a company.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseListItem>> list(RequestOptions requestOptions) {
        return list(ListCoursesRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of courses, filtered by either an experience or a company.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseListItem>> list(ListCoursesRequest request) {
        return list(request, null);
    }

    /**
     * Returns a paginated list of courses, filtered by either an experience or a company.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<CourseListItem>> list(
            ListCoursesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("courses");
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        if (request.getExperienceId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "experience_id", request.getExperienceId().get(), false);
        }
        if (request.getCompanyId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "company_id", request.getCompanyId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListCoursesResponse parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCoursesResponse.class);
                Optional<String> startingAfter = parsedResponse.getPageInfo().getEndCursor();
                ListCoursesRequest nextRequest = ListCoursesRequest.builder()
                        .from(request)
                        .after(startingAfter)
                        .build();
                List<CourseListItem> result = parsedResponse.getData();
                return new WhopApiHttpResponse<>(
                        new SyncPagingIterable<CourseListItem>(
                                startingAfter != null, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Create a new course within an experience, with optional chapters, lessons, and a certificate.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> create(CreateCoursesRequest request) {
        return create(request, null);
    }

    /**
     * Create a new course within an experience, with optional chapters, lessons, and a certificate.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> create(CreateCoursesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("courses");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new WhopApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Course.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves the details of an existing course.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> retrieve(String id) {
        return retrieve(id, RetrieveCoursesRequest.builder().build());
    }

    /**
     * Retrieves the details of an existing course.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> retrieve(String id, RequestOptions requestOptions) {
        return retrieve(id, RetrieveCoursesRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves the details of an existing course.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> retrieve(String id, RetrieveCoursesRequest request) {
        return retrieve(id, request, null);
    }

    /**
     * Retrieves the details of an existing course.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> retrieve(
            String id, RetrieveCoursesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("courses")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Course.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Permanently delete a course and all of its chapters, lessons, and student progress.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(String id) {
        return delete(id, DeleteCoursesRequest.builder().build());
    }

    /**
     * Permanently delete a course and all of its chapters, lessons, and student progress.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(String id, RequestOptions requestOptions) {
        return delete(id, DeleteCoursesRequest.builder().build(), requestOptions);
    }

    /**
     * Permanently delete a course and all of its chapters, lessons, and student progress.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(String id, DeleteCoursesRequest request) {
        return delete(id, request, null);
    }

    /**
     * Permanently delete a course and all of its chapters, lessons, and student progress.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(String id, DeleteCoursesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("courses")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, boolean.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Update a course's title, description, visibility, thumbnail, or chapter ordering.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> update(String id) {
        return update(id, UpdateCoursesRequest.builder().build());
    }

    /**
     * Update a course's title, description, visibility, thumbnail, or chapter ordering.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> update(String id, RequestOptions requestOptions) {
        return update(id, UpdateCoursesRequest.builder().build(), requestOptions);
    }

    /**
     * Update a course's title, description, visibility, thumbnail, or chapter ordering.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> update(String id, UpdateCoursesRequest request) {
        return update(id, request, null);
    }

    /**
     * Update a course's title, description, visibility, thumbnail, or chapter ordering.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>courses:update</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Course> update(String id, UpdateCoursesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("courses")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new WhopApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Course.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }
}
