/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.cards.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CreateCardsRequestSpendLimitFrequency {
    public static final CreateCardsRequestSpendLimitFrequency ONE_TIME =
            new CreateCardsRequestSpendLimitFrequency(Value.ONE_TIME, "one_time");

    public static final CreateCardsRequestSpendLimitFrequency DAILY =
            new CreateCardsRequestSpendLimitFrequency(Value.DAILY, "daily");

    public static final CreateCardsRequestSpendLimitFrequency WEEKLY =
            new CreateCardsRequestSpendLimitFrequency(Value.WEEKLY, "weekly");

    public static final CreateCardsRequestSpendLimitFrequency MONTHLY =
            new CreateCardsRequestSpendLimitFrequency(Value.MONTHLY, "monthly");

    private final Value value;

    private final String string;

    CreateCardsRequestSpendLimitFrequency(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CreateCardsRequestSpendLimitFrequency
                        && this.string.equals(((CreateCardsRequestSpendLimitFrequency) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case ONE_TIME:
                return visitor.visitOneTime();
            case DAILY:
                return visitor.visitDaily();
            case WEEKLY:
                return visitor.visitWeekly();
            case MONTHLY:
                return visitor.visitMonthly();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CreateCardsRequestSpendLimitFrequency valueOf(String value) {
        switch (value) {
            case "one_time":
                return ONE_TIME;
            case "daily":
                return DAILY;
            case "weekly":
                return WEEKLY;
            case "monthly":
                return MONTHLY;
            default:
                return new CreateCardsRequestSpendLimitFrequency(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        DAILY,

        WEEKLY,

        MONTHLY,

        ONE_TIME,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitDaily();

        T visitWeekly();

        T visitMonthly();

        T visitOneTime();

        T visitUnknown(String unknownType);
    }
}
