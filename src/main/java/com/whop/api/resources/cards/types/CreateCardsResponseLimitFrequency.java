/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.cards.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CreateCardsResponseLimitFrequency {
    public static final CreateCardsResponseLimitFrequency PER_TRANSACTION =
            new CreateCardsResponseLimitFrequency(Value.PER_TRANSACTION, "per_transaction");

    public static final CreateCardsResponseLimitFrequency ONE_TIME =
            new CreateCardsResponseLimitFrequency(Value.ONE_TIME, "one_time");

    public static final CreateCardsResponseLimitFrequency DAILY =
            new CreateCardsResponseLimitFrequency(Value.DAILY, "daily");

    public static final CreateCardsResponseLimitFrequency WEEKLY =
            new CreateCardsResponseLimitFrequency(Value.WEEKLY, "weekly");

    public static final CreateCardsResponseLimitFrequency MONTHLY =
            new CreateCardsResponseLimitFrequency(Value.MONTHLY, "monthly");

    private final Value value;

    private final String string;

    CreateCardsResponseLimitFrequency(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CreateCardsResponseLimitFrequency
                        && this.string.equals(((CreateCardsResponseLimitFrequency) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PER_TRANSACTION:
                return visitor.visitPerTransaction();
            case ONE_TIME:
                return visitor.visitOneTime();
            case DAILY:
                return visitor.visitDaily();
            case WEEKLY:
                return visitor.visitWeekly();
            case MONTHLY:
                return visitor.visitMonthly();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CreateCardsResponseLimitFrequency valueOf(String value) {
        switch (value) {
            case "per_transaction":
                return PER_TRANSACTION;
            case "one_time":
                return ONE_TIME;
            case "daily":
                return DAILY;
            case "weekly":
                return WEEKLY;
            case "monthly":
                return MONTHLY;
            default:
                return new CreateCardsResponseLimitFrequency(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        DAILY,

        WEEKLY,

        MONTHLY,

        ONE_TIME,

        PER_TRANSACTION,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitDaily();

        T visitWeekly();

        T visitMonthly();

        T visitOneTime();

        T visitPerTransaction();

        T visitUnknown(String unknownType);
    }
}
