/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.bounties;

import com.whop.api.core.ClientOptions;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.Suppliers;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.resources.bounties.requests.CancelBountiesRequest;
import com.whop.api.resources.bounties.requests.CreateBountiesRequest;
import com.whop.api.resources.bounties.requests.ListBountiesRequest;
import com.whop.api.resources.bounties.requests.RetrieveBountiesRequest;
import com.whop.api.resources.bounties.requests.UpdateBountiesRequest;
import com.whop.api.resources.bounties.submissions.SubmissionsClient;
import com.whop.api.types.Bounty;
import com.whop.api.types.BountyListItem;
import java.util.function.Supplier;

public class BountiesClient {
    protected final ClientOptions clientOptions;

    private final RawBountiesClient rawClient;

    protected final Supplier<SubmissionsClient> submissionsClient;

    public BountiesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawBountiesClient(clientOptions);
        this.submissionsClient = Suppliers.memoize(() -> new SubmissionsClient(clientOptions));
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawBountiesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists bounties visible to the credential — for an account API key, the account's bounties including scheduled drafts; for a user token, the bounties the user can see and work.
     */
    public SyncPagingIterable<BountyListItem> list() {
        return this.rawClient.list().body();
    }

    /**
     * Lists bounties visible to the credential — for an account API key, the account's bounties including scheduled drafts; for a user token, the bounties the user can see and work.
     */
    public SyncPagingIterable<BountyListItem> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).body();
    }

    /**
     * Lists bounties visible to the credential — for an account API key, the account's bounties including scheduled drafts; for a user token, the bounties the user can see and work.
     */
    public SyncPagingIterable<BountyListItem> list(ListBountiesRequest request) {
        return this.rawClient.list(request).body();
    }

    /**
     * Lists bounties visible to the credential — for an account API key, the account's bounties including scheduled drafts; for a user token, the bounties the user can see and work.
     */
    public SyncPagingIterable<BountyListItem> list(ListBountiesRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).body();
    }

    /**
     * Creates a bounty and escrows its reward pool. Publishes immediately, or as a scheduled draft when you set <code>publish_at</code>.
     */
    public Bounty create(CreateBountiesRequest request) {
        return this.rawClient.create(request).body();
    }

    /**
     * Creates a bounty and escrows its reward pool. Publishes immediately, or as a scheduled draft when you set <code>publish_at</code>.
     */
    public Bounty create(CreateBountiesRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).body();
    }

    /**
     * Retrieves a bounty by ID. Authentication is optional: a request with no credential reads the bounty when it is publicly visible — published or completed, and not restricted to a private experience's members. Bounties outside the caller's scope, and bounties not publicly visible to an anonymous caller, return <code>404</code>.
     */
    public Bounty retrieve(String id) {
        return this.rawClient.retrieve(id).body();
    }

    /**
     * Retrieves a bounty by ID. Authentication is optional: a request with no credential reads the bounty when it is publicly visible — published or completed, and not restricted to a private experience's members. Bounties outside the caller's scope, and bounties not publicly visible to an anonymous caller, return <code>404</code>.
     */
    public Bounty retrieve(String id, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, requestOptions).body();
    }

    /**
     * Retrieves a bounty by ID. Authentication is optional: a request with no credential reads the bounty when it is publicly visible — published or completed, and not restricted to a private experience's members. Bounties outside the caller's scope, and bounties not publicly visible to an anonymous caller, return <code>404</code>.
     */
    public Bounty retrieve(String id, RetrieveBountiesRequest request) {
        return this.rawClient.retrieve(id, request).body();
    }

    /**
     * Retrieves a bounty by ID. Authentication is optional: a request with no credential reads the bounty when it is publicly visible — published or completed, and not restricted to a private experience's members. Bounties outside the caller's scope, and bounties not publicly visible to an anonymous caller, return <code>404</code>.
     */
    public Bounty retrieve(String id, RetrieveBountiesRequest request, RequestOptions requestOptions) {
        return this.rawClient.retrieve(id, request, requestOptions).body();
    }

    /**
     * Updates a bounty. A published bounty accepts title, description, and country targeting while it is still open with nothing under review. A scheduled (not-yet-published) draft additionally accepts the reward, winner slots, and schedule.
     */
    public Bounty update(String id) {
        return this.rawClient.update(id).body();
    }

    /**
     * Updates a bounty. A published bounty accepts title, description, and country targeting while it is still open with nothing under review. A scheduled (not-yet-published) draft additionally accepts the reward, winner slots, and schedule.
     */
    public Bounty update(String id, RequestOptions requestOptions) {
        return this.rawClient.update(id, requestOptions).body();
    }

    /**
     * Updates a bounty. A published bounty accepts title, description, and country targeting while it is still open with nothing under review. A scheduled (not-yet-published) draft additionally accepts the reward, winner slots, and schedule.
     */
    public Bounty update(String id, UpdateBountiesRequest request) {
        return this.rawClient.update(id, request).body();
    }

    /**
     * Updates a bounty. A published bounty accepts title, description, and country targeting while it is still open with nothing under review. A scheduled (not-yet-published) draft additionally accepts the reward, winner slots, and schedule.
     */
    public Bounty update(String id, UpdateBountiesRequest request, RequestOptions requestOptions) {
        return this.rawClient.update(id, request, requestOptions).body();
    }

    /**
     * Cancels a bounty. With no in-flight work, it cancels immediately and refunds the funder. Otherwise it stops new submissions and cancels once the in-flight work resolves and pays out. Repeating the request is a no-op. A bounty that already paid out every slot returns <code>400</code>.
     */
    public Bounty cancel(String id) {
        return this.rawClient.cancel(id).body();
    }

    /**
     * Cancels a bounty. With no in-flight work, it cancels immediately and refunds the funder. Otherwise it stops new submissions and cancels once the in-flight work resolves and pays out. Repeating the request is a no-op. A bounty that already paid out every slot returns <code>400</code>.
     */
    public Bounty cancel(String id, RequestOptions requestOptions) {
        return this.rawClient.cancel(id, requestOptions).body();
    }

    /**
     * Cancels a bounty. With no in-flight work, it cancels immediately and refunds the funder. Otherwise it stops new submissions and cancels once the in-flight work resolves and pays out. Repeating the request is a no-op. A bounty that already paid out every slot returns <code>400</code>.
     */
    public Bounty cancel(String id, CancelBountiesRequest request) {
        return this.rawClient.cancel(id, request).body();
    }

    /**
     * Cancels a bounty. With no in-flight work, it cancels immediately and refunds the funder. Otherwise it stops new submissions and cancels once the in-flight work resolves and pays out. Repeating the request is a no-op. A bounty that already paid out every slot returns <code>400</code>.
     */
    public Bounty cancel(String id, CancelBountiesRequest request, RequestOptions requestOptions) {
        return this.rawClient.cancel(id, request, requestOptions).body();
    }

    public SubmissionsClient submissions() {
        return this.submissionsClient.get();
    }
}
