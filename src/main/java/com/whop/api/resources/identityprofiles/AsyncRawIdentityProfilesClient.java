/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.identityprofiles;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.InternalServerError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.TooManyRequestsError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.errors.UnprocessableEntityError;
import com.whop.api.resources.identityprofiles.requests.ListIdentityProfileRequest;
import com.whop.api.resources.identityprofiles.requests.ListVerificationsIdentityProfileRequest;
import com.whop.api.resources.identityprofiles.requests.RetrieveIdentityProfileRequest;
import com.whop.api.resources.identityprofiles.requests.UnlinkIdentityProfileRequest;
import com.whop.api.resources.identityprofiles.types.ListIdentityProfileResponse;
import com.whop.api.resources.identityprofiles.types.ListVerificationsIdentityProfileResponse;
import com.whop.api.resources.identityprofiles.types.ListVerificationsIdentityProfileResponseDataItem;
import com.whop.api.types.IdentityProfile;
import com.whop.api.types.IdentityProfileListItem;
import java.io.IOException;
import java.util.List;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.ExecutionException;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawIdentityProfilesClient {
    protected final ClientOptions clientOptions;

    public AsyncRawIdentityProfilesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of identity profiles. When company_id is provided, lists IPs currently linked to that company's ledger. When omitted, lists IPs linked to any ledger the actor can read (including child companies under a parent).
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<IdentityProfileListItem>>> listIdentityProfile() {
        return listIdentityProfile(ListIdentityProfileRequest.builder().build());
    }

    /**
     * Returns a paginated list of identity profiles. When company_id is provided, lists IPs currently linked to that company's ledger. When omitted, lists IPs linked to any ledger the actor can read (including child companies under a parent).
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<IdentityProfileListItem>>> listIdentityProfile(
            RequestOptions requestOptions) {
        return listIdentityProfile(ListIdentityProfileRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a paginated list of identity profiles. When company_id is provided, lists IPs currently linked to that company's ledger. When omitted, lists IPs linked to any ledger the actor can read (including child companies under a parent).
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<IdentityProfileListItem>>> listIdentityProfile(
            ListIdentityProfileRequest request) {
        return listIdentityProfile(request, null);
    }

    /**
     * Returns a paginated list of identity profiles. When company_id is provided, lists IPs currently linked to that company's ledger. When omitted, lists IPs linked to any ledger the actor can read (including child companies under a parent).
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<IdentityProfileListItem>>> listIdentityProfile(
            ListIdentityProfileRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("identity_profiles");
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        if (request.getCompanyId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "company_id", request.getCompanyId().get(), false);
        }
        if (request.getProfileType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "profile_type", request.getProfileType().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<IdentityProfileListItem>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListIdentityProfileResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListIdentityProfileResponse.class);
                        Optional<String> startingAfter =
                                parsedResponse.getPageInfo().getEndCursor();
                        ListIdentityProfileRequest nextRequest = ListIdentityProfileRequest.builder()
                                .from(request)
                                .after(startingAfter)
                                .build();
                        List<IdentityProfileListItem> result = parsedResponse.getData();
                        future.complete(new WhopApiHttpResponse<>(
                                new SyncPagingIterable<IdentityProfileListItem>(
                                        startingAfter != null, result, parsedResponse, () -> {
                                            try {
                                                return listIdentityProfile(nextRequest, requestOptions)
                                                        .get()
                                                        .body();
                                            } catch (InterruptedException | ExecutionException e) {
                                                throw new RuntimeException(e);
                                            }
                                        }),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 429:
                                future.completeExceptionally(new TooManyRequestsError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Retrieves the details of an existing identity profile.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<IdentityProfile>> retrieveIdentityProfile(String id) {
        return retrieveIdentityProfile(
                id, RetrieveIdentityProfileRequest.builder().build());
    }

    /**
     * Retrieves the details of an existing identity profile.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<IdentityProfile>> retrieveIdentityProfile(
            String id, RequestOptions requestOptions) {
        return retrieveIdentityProfile(
                id, RetrieveIdentityProfileRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves the details of an existing identity profile.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<IdentityProfile>> retrieveIdentityProfile(
            String id, RetrieveIdentityProfileRequest request) {
        return retrieveIdentityProfile(id, request, null);
    }

    /**
     * Retrieves the details of an existing identity profile.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<IdentityProfile>> retrieveIdentityProfile(
            String id, RetrieveIdentityProfileRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("identity_profiles")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<IdentityProfile>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new WhopApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, IdentityProfile.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 429:
                                future.completeExceptionally(new TooManyRequestsError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Unlinks an IdentityProfile from a LedgerAccount (flips the matching link to is_current=false).
     */
    public CompletableFuture<WhopApiHttpResponse<IdentityProfile>> unlinkIdentityProfile(
            String id, UnlinkIdentityProfileRequest request) {
        return unlinkIdentityProfile(id, request, null);
    }

    /**
     * Unlinks an IdentityProfile from a LedgerAccount (flips the matching link to is_current=false).
     */
    public CompletableFuture<WhopApiHttpResponse<IdentityProfile>> unlinkIdentityProfile(
            String id, UnlinkIdentityProfileRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("identity_profiles")
                .addPathSegment(id);
        QueryStringMapper.addQueryParameter(httpUrl, "ledger_account_id", request.getLedgerAccountId(), false);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<IdentityProfile>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new WhopApiHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, IdentityProfile.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 429:
                                future.completeExceptionally(new TooManyRequestsError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Returns a list of verifications attached to an identity profile, ordered by most recent first.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListVerificationsIdentityProfileResponseDataItem>>>
            listVerificationsIdentityProfile(String id) {
        return listVerificationsIdentityProfile(
                id, ListVerificationsIdentityProfileRequest.builder().build());
    }

    /**
     * Returns a list of verifications attached to an identity profile, ordered by most recent first.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListVerificationsIdentityProfileResponseDataItem>>>
            listVerificationsIdentityProfile(String id, RequestOptions requestOptions) {
        return listVerificationsIdentityProfile(
                id, ListVerificationsIdentityProfileRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a list of verifications attached to an identity profile, ordered by most recent first.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListVerificationsIdentityProfileResponseDataItem>>>
            listVerificationsIdentityProfile(String id, ListVerificationsIdentityProfileRequest request) {
        return listVerificationsIdentityProfile(id, request, null);
    }

    /**
     * Returns a list of verifications attached to an identity profile, ordered by most recent first.
     * <p>Required permissions:</p>
     * <ul>
     * <li><code>identity:read</code></li>
     * </ul>
     */
    public CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListVerificationsIdentityProfileResponseDataItem>>>
            listVerificationsIdentityProfile(
                    String id, ListVerificationsIdentityProfileRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("identity_profiles")
                .addPathSegment(id)
                .addPathSegments("verifications");
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<WhopApiHttpResponse<SyncPagingIterable<ListVerificationsIdentityProfileResponseDataItem>>>
                future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        ListVerificationsIdentityProfileResponse parsedResponse = ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, ListVerificationsIdentityProfileResponse.class);
                        Optional<String> startingAfter =
                                parsedResponse.getPageInfo().getEndCursor();
                        ListVerificationsIdentityProfileRequest nextRequest =
                                ListVerificationsIdentityProfileRequest.builder()
                                        .from(request)
                                        .after(startingAfter)
                                        .build();
                        List<ListVerificationsIdentityProfileResponseDataItem> result = parsedResponse.getData();
                        future.complete(new WhopApiHttpResponse<>(
                                new SyncPagingIterable<ListVerificationsIdentityProfileResponseDataItem>(
                                        startingAfter != null, result, parsedResponse, () -> {
                                            try {
                                                return listVerificationsIdentityProfile(id, nextRequest, requestOptions)
                                                        .get()
                                                        .body();
                                            } catch (InterruptedException | ExecutionException e) {
                                                throw new RuntimeException(e);
                                            }
                                        }),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 400:
                                future.completeExceptionally(new BadRequestError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 401:
                                future.completeExceptionally(new UnauthorizedError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 403:
                                future.completeExceptionally(new ForbiddenError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 429:
                                future.completeExceptionally(new TooManyRequestsError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                            case 500:
                                future.completeExceptionally(new InternalServerError(
                                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new WhopApiApiException(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new WhopApiException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
