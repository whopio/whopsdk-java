/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.reactions;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.whop.api.core.ClientOptions;
import com.whop.api.core.MediaTypes;
import com.whop.api.core.ObjectMappers;
import com.whop.api.core.QueryStringMapper;
import com.whop.api.core.RequestOptions;
import com.whop.api.core.WhopApiApiException;
import com.whop.api.core.WhopApiException;
import com.whop.api.core.WhopApiHttpResponse;
import com.whop.api.core.pagination.SyncPagingIterable;
import com.whop.api.errors.BadRequestError;
import com.whop.api.errors.ForbiddenError;
import com.whop.api.errors.InternalServerError;
import com.whop.api.errors.NotFoundError;
import com.whop.api.errors.TooManyRequestsError;
import com.whop.api.errors.UnauthorizedError;
import com.whop.api.errors.UnprocessableEntityError;
import com.whop.api.resources.reactions.requests.CreateReactionsRequest;
import com.whop.api.resources.reactions.requests.DeleteReactionsRequest;
import com.whop.api.resources.reactions.requests.ListReactionsRequest;
import com.whop.api.resources.reactions.requests.RetrieveReactionsRequest;
import com.whop.api.resources.reactions.types.ListReactionsResponse;
import com.whop.api.types.Reaction;
import com.whop.api.types.ReactionListItem;
import java.io.IOException;
import java.util.List;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawReactionsClient {
    protected final ClientOptions clientOptions;

    public RawReactionsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a paginated list of emoji reactions on a specific message or forum post, sorted by most recent.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<ReactionListItem>> list(ListReactionsRequest request) {
        return list(request, null);
    }

    /**
     * Returns a paginated list of emoji reactions on a specific message or forum post, sorted by most recent.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<SyncPagingIterable<ReactionListItem>> list(
            ListReactionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("reactions");
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (request.getFirst().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "first", request.getFirst().get(), false);
        }
        if (request.getLast().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "last", request.getLast().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "resource_id", request.getResourceId(), false);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                ListReactionsResponse parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListReactionsResponse.class);
                Optional<String> startingAfter = parsedResponse.getPageInfo().getEndCursor();
                ListReactionsRequest nextRequest = ListReactionsRequest.builder()
                        .from(request)
                        .after(startingAfter)
                        .build();
                List<ReactionListItem> result = parsedResponse.getData();
                return new WhopApiHttpResponse<>(
                        new SyncPagingIterable<ReactionListItem>(
                                startingAfter != null, result, parsedResponse, () -> list(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Add an emoji reaction or poll vote to a message or forum post. In forums, the reaction is always a like.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Reaction> create(CreateReactionsRequest request) {
        return create(request, null);
    }

    /**
     * Add an emoji reaction or poll vote to a message or forum post. In forums, the reaction is always a like.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Reaction> create(CreateReactionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("reactions");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new WhopApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Reaction.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves the details of an existing reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Reaction> retrieve(String id) {
        return retrieve(id, RetrieveReactionsRequest.builder().build());
    }

    /**
     * Retrieves the details of an existing reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Reaction> retrieve(String id, RequestOptions requestOptions) {
        return retrieve(id, RetrieveReactionsRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves the details of an existing reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Reaction> retrieve(String id, RetrieveReactionsRequest request) {
        return retrieve(id, request, null);
    }

    /**
     * Retrieves the details of an existing reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Reaction> retrieve(
            String id, RetrieveReactionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("reactions")
                .addPathSegment(id);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Reaction.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Remove an emoji reaction from a message or forum post. Only the reaction author or a channel admin can remove a reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(String id) {
        return delete(id, DeleteReactionsRequest.builder().build());
    }

    /**
     * Remove an emoji reaction from a message or forum post. Only the reaction author or a channel admin can remove a reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(String id, RequestOptions requestOptions) {
        return delete(id, DeleteReactionsRequest.builder().build(), requestOptions);
    }

    /**
     * Remove an emoji reaction from a message or forum post. Only the reaction author or a channel admin can remove a reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(String id, DeleteReactionsRequest request) {
        return delete(id, request, null);
    }

    /**
     * Remove an emoji reaction from a message or forum post. Only the reaction author or a channel admin can remove a reaction.
     * <p>Required permissions (one of):</p>
     * <ul>
     * <li><code>chat:read</code></li>
     * <li><code>dms:read</code></li>
     * <li><code>forum:read</code></li>
     * <li><code>livestream:chat:read</code></li>
     * <li><code>support_chat:read</code></li>
     * </ul>
     */
    public WhopApiHttpResponse<Boolean> delete(
            String id, DeleteReactionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("reactions")
                .addPathSegment(id);
        if (request.getEmoji().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "emoji", request.getEmoji().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new WhopApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, boolean.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new WhopApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new WhopApiException("Network error executing HTTP request", e);
        }
    }
}
