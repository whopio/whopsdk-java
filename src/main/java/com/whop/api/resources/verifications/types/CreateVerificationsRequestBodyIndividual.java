/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.verifications.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateVerificationsRequestBodyIndividual.Builder.class)
public final class CreateVerificationsRequestBodyIndividual {
    private final Optional<CreateVerificationsRequestBodyIndividualAddress> address;

    private final Optional<String> businessName;

    private final Optional<String> businessStructure;

    private final Optional<String> businessTaxIdentificationNumber;

    private final Optional<String> businessWebsite;

    private final Optional<String> country;

    private final Optional<String> dateOfBirth;

    private final Optional<CreateVerificationsRequestBodyIndividualDocumentType> documentType;

    private final Optional<CreateVerificationsRequestBodyIndividualDocuments> documents;

    private final Optional<String> firstName;

    private final Optional<String> lastName;

    private final Optional<String> phone;

    private final Optional<String> shareToken;

    private final Optional<String> taxIdentificationNumber;

    private final Map<String, Object> additionalProperties;

    private CreateVerificationsRequestBodyIndividual(
            Optional<CreateVerificationsRequestBodyIndividualAddress> address,
            Optional<String> businessName,
            Optional<String> businessStructure,
            Optional<String> businessTaxIdentificationNumber,
            Optional<String> businessWebsite,
            Optional<String> country,
            Optional<String> dateOfBirth,
            Optional<CreateVerificationsRequestBodyIndividualDocumentType> documentType,
            Optional<CreateVerificationsRequestBodyIndividualDocuments> documents,
            Optional<String> firstName,
            Optional<String> lastName,
            Optional<String> phone,
            Optional<String> shareToken,
            Optional<String> taxIdentificationNumber,
            Map<String, Object> additionalProperties) {
        this.address = address;
        this.businessName = businessName;
        this.businessStructure = businessStructure;
        this.businessTaxIdentificationNumber = businessTaxIdentificationNumber;
        this.businessWebsite = businessWebsite;
        this.country = country;
        this.dateOfBirth = dateOfBirth;
        this.documentType = documentType;
        this.documents = documents;
        this.firstName = firstName;
        this.lastName = lastName;
        this.phone = phone;
        this.shareToken = shareToken;
        this.taxIdentificationNumber = taxIdentificationNumber;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Every value accepts <code>null</code>, so a form seeded from a previous response can round-trip unset fields unchanged.
     */
    @JsonProperty("address")
    public Optional<CreateVerificationsRequestBodyIndividualAddress> getAddress() {
        return address;
    }

    /**
     * @return Legal business name for a sole proprietor or single-member LLC.
     */
    @JsonProperty("business_name")
    public Optional<String> getBusinessName() {
        return businessName;
    }

    /**
     * @return Entity type for sole proprietors, such as <code>single_member_llc</code>. Supported values vary by country of incorporation — see <a href="/developer/verification/business-structures">Business structures</a>.
     */
    @JsonProperty("business_structure")
    public Optional<String> getBusinessStructure() {
        return businessStructure;
    }

    /**
     * @return The business ID number of the company, as appropriate for the company's country. Examples are an Employer Identification Number (EIN) in the US, a Business Number in Canada, or a Company Number in the UK.
     */
    @JsonProperty("business_tax_identification_number")
    public Optional<String> getBusinessTaxIdentificationNumber() {
        return businessTaxIdentificationNumber;
    }

    /**
     * @return Business website URL. Whop store pages are not accepted.
     */
    @JsonProperty("business_website")
    public Optional<String> getBusinessWebsite() {
        return businessWebsite;
    }

    /**
     * @return Two-letter ISO 3166-1 country code, for example <code>US</code>, <code>DE</code>, or <code>GB</code>.
     */
    @JsonProperty("country")
    public Optional<String> getCountry() {
        return country;
    }

    /**
     * @return Formatted as <code>YYYY-MM-DD</code>.
     */
    @JsonProperty("date_of_birth")
    public Optional<String> getDateOfBirth() {
        return dateOfBirth;
    }

    /**
     * @return Identity document being sent, when verifying with <code>documents</code>. Decides exactly which file slots to send: <code>ID_CARD</code> → <code>id_card_front</code> + <code>id_card_back</code> + <code>selfie</code>; <code>DRIVERS</code> → <code>drivers_front</code> + <code>drivers_back</code> + <code>selfie</code>; <code>RESIDENCE_PERMIT</code> → <code>residence_permit_front</code> + <code>residence_permit_back</code> + <code>selfie</code>; <code>PASSPORT</code> → <code>passport_front</code> + <code>selfie</code>. See <a href="/developer/verification/identity-documents">Identity documents</a>.
     */
    @JsonProperty("document_type")
    public Optional<CreateVerificationsRequestBodyIndividualDocumentType> getDocumentType() {
        return documentType;
    }

    /**
     * @return Identity document files, each value the file's raw bytes base64-encoded (JPEG, PNG, or PDF, up to 5MB per file before encoding). Sending this object verifies the person from the files in this request instead of a hosted session — individual verifications only, and the request must also carry <code>document_type</code>, <code>first_name</code>, <code>last_name</code>, <code>date_of_birth</code>, <code>country</code>, <code>phone</code>, <code>tax_identification_number</code>, and an <code>address</code> with <code>line1</code>, <code>city</code>, <code>state</code>, and <code>postal_code</code>. Send every slot for your <code>document_type</code> — a missing or rejected file fails the whole request and nothing is submitted; review starts automatically once every document is accepted. See <a href="/developer/verification/identity-documents">Identity documents</a> for a full walkthrough.
     */
    @JsonProperty("documents")
    public Optional<CreateVerificationsRequestBodyIndividualDocuments> getDocuments() {
        return documents;
    }

    @JsonProperty("first_name")
    public Optional<String> getFirstName() {
        return firstName;
    }

    @JsonProperty("last_name")
    public Optional<String> getLastName() {
        return lastName;
    }

    @JsonProperty("phone")
    public Optional<String> getPhone() {
        return phone;
    }

    /**
     * @return Sumsub share token for a person a partner has already verified, generated by their Sumsub account for Whop's client ID. Reuses that verification instead of collecting anything: Whop imports the shared identity, rechecks it against its own requirements, and returns <code>approved</code> when it satisfies them, or <code>pending</code> with a <code>session_url</code> covering only what is still missing. Requires a Reusable KYC agreement between the partner's Sumsub account and Whop's. Each token works once, and cannot be combined with <code>documents</code> or <code>document_type</code>.
     */
    @JsonProperty("share_token")
    public Optional<String> getShareToken() {
        return shareToken;
    }

    /**
     * @return The government-issued ID number of the person being verified — the individual for a KYC verification, or the business representative for a KYB verification — as appropriate for their country. Examples are a Social Security Number (SSN) in the US, or a Social Insurance Number in Canada.
     */
    @JsonProperty("tax_identification_number")
    public Optional<String> getTaxIdentificationNumber() {
        return taxIdentificationNumber;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateVerificationsRequestBodyIndividual
                && equalTo((CreateVerificationsRequestBodyIndividual) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateVerificationsRequestBodyIndividual other) {
        return address.equals(other.address)
                && businessName.equals(other.businessName)
                && businessStructure.equals(other.businessStructure)
                && businessTaxIdentificationNumber.equals(other.businessTaxIdentificationNumber)
                && businessWebsite.equals(other.businessWebsite)
                && country.equals(other.country)
                && dateOfBirth.equals(other.dateOfBirth)
                && documentType.equals(other.documentType)
                && documents.equals(other.documents)
                && firstName.equals(other.firstName)
                && lastName.equals(other.lastName)
                && phone.equals(other.phone)
                && shareToken.equals(other.shareToken)
                && taxIdentificationNumber.equals(other.taxIdentificationNumber);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.address,
                this.businessName,
                this.businessStructure,
                this.businessTaxIdentificationNumber,
                this.businessWebsite,
                this.country,
                this.dateOfBirth,
                this.documentType,
                this.documents,
                this.firstName,
                this.lastName,
                this.phone,
                this.shareToken,
                this.taxIdentificationNumber);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<CreateVerificationsRequestBodyIndividualAddress> address = Optional.empty();

        private Optional<String> businessName = Optional.empty();

        private Optional<String> businessStructure = Optional.empty();

        private Optional<String> businessTaxIdentificationNumber = Optional.empty();

        private Optional<String> businessWebsite = Optional.empty();

        private Optional<String> country = Optional.empty();

        private Optional<String> dateOfBirth = Optional.empty();

        private Optional<CreateVerificationsRequestBodyIndividualDocumentType> documentType = Optional.empty();

        private Optional<CreateVerificationsRequestBodyIndividualDocuments> documents = Optional.empty();

        private Optional<String> firstName = Optional.empty();

        private Optional<String> lastName = Optional.empty();

        private Optional<String> phone = Optional.empty();

        private Optional<String> shareToken = Optional.empty();

        private Optional<String> taxIdentificationNumber = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(CreateVerificationsRequestBodyIndividual other) {
            address(other.getAddress());
            businessName(other.getBusinessName());
            businessStructure(other.getBusinessStructure());
            businessTaxIdentificationNumber(other.getBusinessTaxIdentificationNumber());
            businessWebsite(other.getBusinessWebsite());
            country(other.getCountry());
            dateOfBirth(other.getDateOfBirth());
            documentType(other.getDocumentType());
            documents(other.getDocuments());
            firstName(other.getFirstName());
            lastName(other.getLastName());
            phone(other.getPhone());
            shareToken(other.getShareToken());
            taxIdentificationNumber(other.getTaxIdentificationNumber());
            return this;
        }

        /**
         * <p>Every value accepts <code>null</code>, so a form seeded from a previous response can round-trip unset fields unchanged.</p>
         */
        @JsonSetter(value = "address", nulls = Nulls.SKIP)
        public Builder address(Optional<CreateVerificationsRequestBodyIndividualAddress> address) {
            this.address = address;
            return this;
        }

        public Builder address(CreateVerificationsRequestBodyIndividualAddress address) {
            this.address = Optional.ofNullable(address);
            return this;
        }

        /**
         * <p>Legal business name for a sole proprietor or single-member LLC.</p>
         */
        @JsonSetter(value = "business_name", nulls = Nulls.SKIP)
        public Builder businessName(Optional<String> businessName) {
            this.businessName = businessName;
            return this;
        }

        public Builder businessName(String businessName) {
            this.businessName = Optional.ofNullable(businessName);
            return this;
        }

        /**
         * <p>Entity type for sole proprietors, such as <code>single_member_llc</code>. Supported values vary by country of incorporation — see <a href="/developer/verification/business-structures">Business structures</a>.</p>
         */
        @JsonSetter(value = "business_structure", nulls = Nulls.SKIP)
        public Builder businessStructure(Optional<String> businessStructure) {
            this.businessStructure = businessStructure;
            return this;
        }

        public Builder businessStructure(String businessStructure) {
            this.businessStructure = Optional.ofNullable(businessStructure);
            return this;
        }

        /**
         * <p>The business ID number of the company, as appropriate for the company's country. Examples are an Employer Identification Number (EIN) in the US, a Business Number in Canada, or a Company Number in the UK.</p>
         */
        @JsonSetter(value = "business_tax_identification_number", nulls = Nulls.SKIP)
        public Builder businessTaxIdentificationNumber(Optional<String> businessTaxIdentificationNumber) {
            this.businessTaxIdentificationNumber = businessTaxIdentificationNumber;
            return this;
        }

        public Builder businessTaxIdentificationNumber(String businessTaxIdentificationNumber) {
            this.businessTaxIdentificationNumber = Optional.ofNullable(businessTaxIdentificationNumber);
            return this;
        }

        /**
         * <p>Business website URL. Whop store pages are not accepted.</p>
         */
        @JsonSetter(value = "business_website", nulls = Nulls.SKIP)
        public Builder businessWebsite(Optional<String> businessWebsite) {
            this.businessWebsite = businessWebsite;
            return this;
        }

        public Builder businessWebsite(String businessWebsite) {
            this.businessWebsite = Optional.ofNullable(businessWebsite);
            return this;
        }

        /**
         * <p>Two-letter ISO 3166-1 country code, for example <code>US</code>, <code>DE</code>, or <code>GB</code>.</p>
         */
        @JsonSetter(value = "country", nulls = Nulls.SKIP)
        public Builder country(Optional<String> country) {
            this.country = country;
            return this;
        }

        public Builder country(String country) {
            this.country = Optional.ofNullable(country);
            return this;
        }

        /**
         * <p>Formatted as <code>YYYY-MM-DD</code>.</p>
         */
        @JsonSetter(value = "date_of_birth", nulls = Nulls.SKIP)
        public Builder dateOfBirth(Optional<String> dateOfBirth) {
            this.dateOfBirth = dateOfBirth;
            return this;
        }

        public Builder dateOfBirth(String dateOfBirth) {
            this.dateOfBirth = Optional.ofNullable(dateOfBirth);
            return this;
        }

        /**
         * <p>Identity document being sent, when verifying with <code>documents</code>. Decides exactly which file slots to send: <code>ID_CARD</code> → <code>id_card_front</code> + <code>id_card_back</code> + <code>selfie</code>; <code>DRIVERS</code> → <code>drivers_front</code> + <code>drivers_back</code> + <code>selfie</code>; <code>RESIDENCE_PERMIT</code> → <code>residence_permit_front</code> + <code>residence_permit_back</code> + <code>selfie</code>; <code>PASSPORT</code> → <code>passport_front</code> + <code>selfie</code>. See <a href="/developer/verification/identity-documents">Identity documents</a>.</p>
         */
        @JsonSetter(value = "document_type", nulls = Nulls.SKIP)
        public Builder documentType(Optional<CreateVerificationsRequestBodyIndividualDocumentType> documentType) {
            this.documentType = documentType;
            return this;
        }

        public Builder documentType(CreateVerificationsRequestBodyIndividualDocumentType documentType) {
            this.documentType = Optional.ofNullable(documentType);
            return this;
        }

        /**
         * <p>Identity document files, each value the file's raw bytes base64-encoded (JPEG, PNG, or PDF, up to 5MB per file before encoding). Sending this object verifies the person from the files in this request instead of a hosted session — individual verifications only, and the request must also carry <code>document_type</code>, <code>first_name</code>, <code>last_name</code>, <code>date_of_birth</code>, <code>country</code>, <code>phone</code>, <code>tax_identification_number</code>, and an <code>address</code> with <code>line1</code>, <code>city</code>, <code>state</code>, and <code>postal_code</code>. Send every slot for your <code>document_type</code> — a missing or rejected file fails the whole request and nothing is submitted; review starts automatically once every document is accepted. See <a href="/developer/verification/identity-documents">Identity documents</a> for a full walkthrough.</p>
         */
        @JsonSetter(value = "documents", nulls = Nulls.SKIP)
        public Builder documents(Optional<CreateVerificationsRequestBodyIndividualDocuments> documents) {
            this.documents = documents;
            return this;
        }

        public Builder documents(CreateVerificationsRequestBodyIndividualDocuments documents) {
            this.documents = Optional.ofNullable(documents);
            return this;
        }

        @JsonSetter(value = "first_name", nulls = Nulls.SKIP)
        public Builder firstName(Optional<String> firstName) {
            this.firstName = firstName;
            return this;
        }

        public Builder firstName(String firstName) {
            this.firstName = Optional.ofNullable(firstName);
            return this;
        }

        @JsonSetter(value = "last_name", nulls = Nulls.SKIP)
        public Builder lastName(Optional<String> lastName) {
            this.lastName = lastName;
            return this;
        }

        public Builder lastName(String lastName) {
            this.lastName = Optional.ofNullable(lastName);
            return this;
        }

        @JsonSetter(value = "phone", nulls = Nulls.SKIP)
        public Builder phone(Optional<String> phone) {
            this.phone = phone;
            return this;
        }

        public Builder phone(String phone) {
            this.phone = Optional.ofNullable(phone);
            return this;
        }

        /**
         * <p>Sumsub share token for a person a partner has already verified, generated by their Sumsub account for Whop's client ID. Reuses that verification instead of collecting anything: Whop imports the shared identity, rechecks it against its own requirements, and returns <code>approved</code> when it satisfies them, or <code>pending</code> with a <code>session_url</code> covering only what is still missing. Requires a Reusable KYC agreement between the partner's Sumsub account and Whop's. Each token works once, and cannot be combined with <code>documents</code> or <code>document_type</code>.</p>
         */
        @JsonSetter(value = "share_token", nulls = Nulls.SKIP)
        public Builder shareToken(Optional<String> shareToken) {
            this.shareToken = shareToken;
            return this;
        }

        public Builder shareToken(String shareToken) {
            this.shareToken = Optional.ofNullable(shareToken);
            return this;
        }

        /**
         * <p>The government-issued ID number of the person being verified — the individual for a KYC verification, or the business representative for a KYB verification — as appropriate for their country. Examples are a Social Security Number (SSN) in the US, or a Social Insurance Number in Canada.</p>
         */
        @JsonSetter(value = "tax_identification_number", nulls = Nulls.SKIP)
        public Builder taxIdentificationNumber(Optional<String> taxIdentificationNumber) {
            this.taxIdentificationNumber = taxIdentificationNumber;
            return this;
        }

        public Builder taxIdentificationNumber(String taxIdentificationNumber) {
            this.taxIdentificationNumber = Optional.ofNullable(taxIdentificationNumber);
            return this;
        }

        public CreateVerificationsRequestBodyIndividual build() {
            return new CreateVerificationsRequestBodyIndividual(
                    address,
                    businessName,
                    businessStructure,
                    businessTaxIdentificationNumber,
                    businessWebsite,
                    country,
                    dateOfBirth,
                    documentType,
                    documents,
                    firstName,
                    lastName,
                    phone,
                    shareToken,
                    taxIdentificationNumber,
                    additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
