/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.verifications.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class CreateVerificationsRequestBody {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private CreateVerificationsRequestBody(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static CreateVerificationsRequestBody individual(CreateVerificationsRequestBodyIndividual value) {
        return new CreateVerificationsRequestBody(new IndividualValue(value));
    }

    public static CreateVerificationsRequestBody business(CreateVerificationsRequestBodyBusiness value) {
        return new CreateVerificationsRequestBody(new BusinessValue(value));
    }

    public boolean isIndividual() {
        return value instanceof IndividualValue;
    }

    public boolean isBusiness() {
        return value instanceof BusinessValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CreateVerificationsRequestBodyIndividual> getIndividual() {
        if (isIndividual()) {
            return Optional.of(((IndividualValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CreateVerificationsRequestBodyBusiness> getBusiness() {
        if (isBusiness()) {
            return Optional.of(((BusinessValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateVerificationsRequestBody
                && value.equals(((CreateVerificationsRequestBody) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitIndividual(CreateVerificationsRequestBodyIndividual individual);

        T visitBusiness(CreateVerificationsRequestBodyBusiness business);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "kind", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(IndividualValue.class), @JsonSubTypes.Type(BusinessValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("individual")
    @JsonIgnoreProperties("kind")
    private static final class IndividualValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private CreateVerificationsRequestBodyIndividual value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private IndividualValue() {}

        private IndividualValue(CreateVerificationsRequestBodyIndividual value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitIndividual(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof IndividualValue && equalTo((IndividualValue) other);
        }

        private boolean equalTo(IndividualValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateVerificationsRequestBody{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("business")
    @JsonIgnoreProperties("kind")
    private static final class BusinessValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private CreateVerificationsRequestBodyBusiness value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BusinessValue() {}

        private BusinessValue(CreateVerificationsRequestBodyBusiness value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBusiness(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BusinessValue && equalTo((BusinessValue) other);
        }

        private boolean equalTo(BusinessValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateVerificationsRequestBody{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("kind")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateVerificationsRequestBody{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
