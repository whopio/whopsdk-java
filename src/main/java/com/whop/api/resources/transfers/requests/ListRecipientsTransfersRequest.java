/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.transfers.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ListRecipientsTransfersRequest.Builder.class)
public final class ListRecipientsTransfersRequest {
    private final String originId;

    private final Optional<String> query;

    private final Optional<Integer> first;

    private final Optional<String> after;

    private final Map<String, Object> additionalProperties;

    private ListRecipientsTransfersRequest(
            String originId,
            Optional<String> query,
            Optional<Integer> first,
            Optional<String> after,
            Map<String, Object> additionalProperties) {
        this.originId = originId;
        this.query = query;
        this.first = first;
        this.after = after;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The account sending the money: a company account ID (<code>biz_</code>), or a user ID (<code>user_</code>) for that user's own personal balance.
     */
    @JsonProperty("origin_id")
    public String getOriginId() {
        return originId;
    }

    /**
     * @return Search anyone on Whop by name or username, plus your own accounts by name or ID. Omit it to get the team around the balance, the people you follow, and your own accounts. The list is the same whether the balance belongs to a company or to you. Searching from a <code>biz_</code> origin additionally requires the member:basic:read scope. A credential scoped to a single company is the exception to the search itself: it only ever sees that company's own people. Complete email addresses return no matches.
     */
    @JsonProperty("query")
    public Optional<String> getQuery() {
        return query;
    }

    /**
     * @return Number of recipients per page. Search queries preserve the dashboard's 20-result maximum.
     */
    @JsonProperty("first")
    public Optional<Integer> getFirst() {
        return first;
    }

    /**
     * @return Cursor to fetch the page after (from page_info.end_cursor).
     */
    @JsonProperty("after")
    public Optional<String> getAfter() {
        return after;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ListRecipientsTransfersRequest && equalTo((ListRecipientsTransfersRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ListRecipientsTransfersRequest other) {
        return originId.equals(other.originId)
                && query.equals(other.query)
                && first.equals(other.first)
                && after.equals(other.after);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.originId, this.query, this.first, this.after);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static OriginIdStage builder() {
        return new Builder();
    }

    public interface OriginIdStage {
        /**
         * <p>The account sending the money: a company account ID (<code>biz_</code>), or a user ID (<code>user_</code>) for that user's own personal balance.</p>
         */
        _FinalStage originId(@NotNull String originId);

        Builder from(ListRecipientsTransfersRequest other);
    }

    public interface _FinalStage {
        ListRecipientsTransfersRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Search anyone on Whop by name or username, plus your own accounts by name or ID. Omit it to get the team around the balance, the people you follow, and your own accounts. The list is the same whether the balance belongs to a company or to you. Searching from a <code>biz_</code> origin additionally requires the member:basic:read scope. A credential scoped to a single company is the exception to the search itself: it only ever sees that company's own people. Complete email addresses return no matches.</p>
         */
        _FinalStage query(Optional<String> query);

        _FinalStage query(String query);

        /**
         * <p>Number of recipients per page. Search queries preserve the dashboard's 20-result maximum.</p>
         */
        _FinalStage first(Optional<Integer> first);

        _FinalStage first(Integer first);

        /**
         * <p>Cursor to fetch the page after (from page_info.end_cursor).</p>
         */
        _FinalStage after(Optional<String> after);

        _FinalStage after(String after);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements OriginIdStage, _FinalStage {
        private String originId;

        private Optional<String> after = Optional.empty();

        private Optional<Integer> first = Optional.empty();

        private Optional<String> query = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ListRecipientsTransfersRequest other) {
            originId(other.getOriginId());
            query(other.getQuery());
            first(other.getFirst());
            after(other.getAfter());
            return this;
        }

        /**
         * <p>The account sending the money: a company account ID (<code>biz_</code>), or a user ID (<code>user_</code>) for that user's own personal balance.</p>
         * <p>The account sending the money: a company account ID (<code>biz_</code>), or a user ID (<code>user_</code>) for that user's own personal balance.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("origin_id")
        public _FinalStage originId(@NotNull String originId) {
            this.originId = Objects.requireNonNull(originId, "originId must not be null");
            return this;
        }

        /**
         * <p>Cursor to fetch the page after (from page_info.end_cursor).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage after(String after) {
            this.after = Optional.ofNullable(after);
            return this;
        }

        /**
         * <p>Cursor to fetch the page after (from page_info.end_cursor).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "after", nulls = Nulls.SKIP)
        public _FinalStage after(Optional<String> after) {
            this.after = after;
            return this;
        }

        /**
         * <p>Number of recipients per page. Search queries preserve the dashboard's 20-result maximum.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage first(Integer first) {
            this.first = Optional.ofNullable(first);
            return this;
        }

        /**
         * <p>Number of recipients per page. Search queries preserve the dashboard's 20-result maximum.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "first", nulls = Nulls.SKIP)
        public _FinalStage first(Optional<Integer> first) {
            this.first = first;
            return this;
        }

        /**
         * <p>Search anyone on Whop by name or username, plus your own accounts by name or ID. Omit it to get the team around the balance, the people you follow, and your own accounts. The list is the same whether the balance belongs to a company or to you. Searching from a <code>biz_</code> origin additionally requires the member:basic:read scope. A credential scoped to a single company is the exception to the search itself: it only ever sees that company's own people. Complete email addresses return no matches.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage query(String query) {
            this.query = Optional.ofNullable(query);
            return this;
        }

        /**
         * <p>Search anyone on Whop by name or username, plus your own accounts by name or ID. Omit it to get the team around the balance, the people you follow, and your own accounts. The list is the same whether the balance belongs to a company or to you. Searching from a <code>biz_</code> origin additionally requires the member:basic:read scope. A credential scoped to a single company is the exception to the search itself: it only ever sees that company's own people. Complete email addresses return no matches.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "query", nulls = Nulls.SKIP)
        public _FinalStage query(Optional<String> query) {
            this.query = query;
            return this;
        }

        @java.lang.Override
        public ListRecipientsTransfersRequest build() {
            return new ListRecipientsTransfersRequest(originId, query, first, after, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
