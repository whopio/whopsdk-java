/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.transfers.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class RetrieveTransfersResponseDestination {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private RetrieveTransfersResponseDestination(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static RetrieveTransfersResponseDestination company(RetrieveTransfersResponseDestinationCompany value) {
        return new RetrieveTransfersResponseDestination(new CompanyValue(value));
    }

    public static RetrieveTransfersResponseDestination user(RetrieveTransfersResponseDestinationUser value) {
        return new RetrieveTransfersResponseDestination(new UserValue(value));
    }

    public boolean isCompany() {
        return value instanceof CompanyValue;
    }

    public boolean isUser() {
        return value instanceof UserValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<RetrieveTransfersResponseDestinationCompany> getCompany() {
        if (isCompany()) {
            return Optional.of(((CompanyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<RetrieveTransfersResponseDestinationUser> getUser() {
        if (isUser()) {
            return Optional.of(((UserValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof RetrieveTransfersResponseDestination
                && value.equals(((RetrieveTransfersResponseDestination) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitCompany(RetrieveTransfersResponseDestinationCompany company);

        T visitUser(RetrieveTransfersResponseDestinationUser user);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "typename", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(CompanyValue.class), @JsonSubTypes.Type(UserValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("Company")
    @JsonIgnoreProperties("typename")
    private static final class CompanyValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "typename", allowSetters = true)
        private RetrieveTransfersResponseDestinationCompany value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CompanyValue() {}

        private CompanyValue(RetrieveTransfersResponseDestinationCompany value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCompany(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CompanyValue && equalTo((CompanyValue) other);
        }

        private boolean equalTo(CompanyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "RetrieveTransfersResponseDestination{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("User")
    @JsonIgnoreProperties("typename")
    private static final class UserValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "typename", allowSetters = true)
        private RetrieveTransfersResponseDestinationUser value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UserValue() {}

        private UserValue(RetrieveTransfersResponseDestinationUser value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUser(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UserValue && equalTo((UserValue) other);
        }

        private boolean equalTo(UserValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "RetrieveTransfersResponseDestination{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("typename")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "RetrieveTransfersResponseDestination{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
