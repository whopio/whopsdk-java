/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.transfers.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.whop.api.core.Nullable;
import com.whop.api.core.NullableNonemptyFilter;
import com.whop.api.core.ObjectMappers;
import com.whop.api.resources.transfers.types.CreateTransfersRequestType;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateTransfersRequest.Builder.class)
public final class CreateTransfersRequest {
    private final double amount;

    private final Optional<String> currency;

    private final Optional<String> destinationId;

    private final Optional<OffsetDateTime> expiresAt;

    private final Optional<String> idempotenceKey;

    private final Optional<Map<String, Object>> metadata;

    private final Optional<String> notes;

    private final String originId;

    private final Optional<Integer> redeemableCount;

    private final Optional<CreateTransfersRequestType> type;

    private final Map<String, Object> additionalProperties;

    private CreateTransfersRequest(
            double amount,
            Optional<String> currency,
            Optional<String> destinationId,
            Optional<OffsetDateTime> expiresAt,
            Optional<String> idempotenceKey,
            Optional<Map<String, Object>> metadata,
            Optional<String> notes,
            String originId,
            Optional<Integer> redeemableCount,
            Optional<CreateTransfersRequestType> type,
            Map<String, Object> additionalProperties) {
        this.amount = amount;
        this.currency = currency;
        this.destinationId = destinationId;
        this.expiresAt = expiresAt;
        this.idempotenceKey = idempotenceKey;
        this.metadata = metadata;
        this.notes = notes;
        this.originId = originId;
        this.redeemableCount = redeemableCount;
        this.type = type;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The amount to move, in the transfer currency. For example 25.00.
     */
    @JsonProperty("amount")
    public double getAmount() {
        return amount;
    }

    /**
     * @return Currency, such as <code>usd</code>. Required for ledger transfers.
     */
    @JsonProperty("currency")
    public Optional<String> getCurrency() {
        return currency;
    }

    /**
     * @return The recipient. Required for ledger and wallet_send (a user_/biz_/ldgr_ ID, or — for sends — an email). Omit for claim_link.
     */
    @JsonProperty("destination_id")
    public Optional<String> getDestinationId() {
        return destinationId;
    }

    /**
     * @return claim_link only. Link expiry as an ISO 8601 timestamp. Defaults to 24 hours from creation.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> getExpiresAt() {
        if (expiresAt == null) {
            return Optional.empty();
        }
        return expiresAt;
    }

    /**
     * @return Ledger transfers and wallet sends. A unique key that makes retries safe. Retrying with the same key returns the original transfer, or attaches to the original wallet send, instead of moving money twice.
     */
    @JsonIgnore
    public Optional<String> getIdempotenceKey() {
        if (idempotenceKey == null) {
            return Optional.empty();
        }
        return idempotenceKey;
    }

    /**
     * @return Ledger transfers only. Custom key-value pairs attached to the transfer. Max 50 keys, 100 chars per key, 500 chars per string value.
     */
    @JsonIgnore
    public Optional<Map<String, Object>> getMetadata() {
        if (metadata == null) {
            return Optional.empty();
        }
        return metadata;
    }

    /**
     * @return Ledger transfers only. A short note describing the transfer.
     */
    @JsonIgnore
    public Optional<String> getNotes() {
        if (notes == null) {
            return Optional.empty();
        }
        return notes;
    }

    /**
     * @return The account sending the funds. A user ID (user_xxx), account ID (biz_xxx), or ledger account ID (ldgr_xxx).
     */
    @JsonProperty("origin_id")
    public String getOriginId() {
        return originId;
    }

    /**
     * @return claim_link only. How many different users can claim the link. Defaults to 1.
     */
    @JsonProperty("redeemable_count")
    public Optional<Integer> getRedeemableCount() {
        return redeemableCount;
    }

    /**
     * @return The kind of money movement, which decides what comes back. Defaults to ledger. <code>ledger</code> moves credit between two Whop balances and returns a <code>transfer</code>; <code>wallet_send</code> sends USDT from the origin account's Ethereum wallet and returns a <code>send</code>; <code>claim_link</code> funds a shareable link anyone with the URL can redeem and returns a <code>claim_link</code>. A <code>ledger</code> transfer from a stablecoin-rails account settles on-chain when covered, and still returns a <code>transfer</code>.
     */
    @JsonProperty("type")
    public Optional<CreateTransfersRequestType> getType() {
        return type;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("expires_at")
    private Optional<OffsetDateTime> _getExpiresAt() {
        return expiresAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("idempotence_key")
    private Optional<String> _getIdempotenceKey() {
        return idempotenceKey;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("metadata")
    private Optional<Map<String, Object>> _getMetadata() {
        return metadata;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("notes")
    private Optional<String> _getNotes() {
        return notes;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateTransfersRequest && equalTo((CreateTransfersRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateTransfersRequest other) {
        return amount == other.amount
                && currency.equals(other.currency)
                && destinationId.equals(other.destinationId)
                && expiresAt.equals(other.expiresAt)
                && idempotenceKey.equals(other.idempotenceKey)
                && metadata.equals(other.metadata)
                && notes.equals(other.notes)
                && originId.equals(other.originId)
                && redeemableCount.equals(other.redeemableCount)
                && type.equals(other.type);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.amount,
                this.currency,
                this.destinationId,
                this.expiresAt,
                this.idempotenceKey,
                this.metadata,
                this.notes,
                this.originId,
                this.redeemableCount,
                this.type);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AmountStage builder() {
        return new Builder();
    }

    public interface AmountStage {
        /**
         * <p>The amount to move, in the transfer currency. For example 25.00.</p>
         */
        OriginIdStage amount(double amount);

        Builder from(CreateTransfersRequest other);
    }

    public interface OriginIdStage {
        /**
         * <p>The account sending the funds. A user ID (user_xxx), account ID (biz_xxx), or ledger account ID (ldgr_xxx).</p>
         */
        _FinalStage originId(@NotNull String originId);
    }

    public interface _FinalStage {
        CreateTransfersRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Currency, such as <code>usd</code>. Required for ledger transfers.</p>
         */
        _FinalStage currency(Optional<String> currency);

        _FinalStage currency(String currency);

        /**
         * <p>The recipient. Required for ledger and wallet_send (a user_/biz_/ldgr_ ID, or — for sends — an email). Omit for claim_link.</p>
         */
        _FinalStage destinationId(Optional<String> destinationId);

        _FinalStage destinationId(String destinationId);

        /**
         * <p>claim_link only. Link expiry as an ISO 8601 timestamp. Defaults to 24 hours from creation.</p>
         */
        _FinalStage expiresAt(Optional<OffsetDateTime> expiresAt);

        _FinalStage expiresAt(OffsetDateTime expiresAt);

        _FinalStage expiresAt(Nullable<OffsetDateTime> expiresAt);

        /**
         * <p>Ledger transfers and wallet sends. A unique key that makes retries safe. Retrying with the same key returns the original transfer, or attaches to the original wallet send, instead of moving money twice.</p>
         */
        _FinalStage idempotenceKey(Optional<String> idempotenceKey);

        _FinalStage idempotenceKey(String idempotenceKey);

        _FinalStage idempotenceKey(Nullable<String> idempotenceKey);

        /**
         * <p>Ledger transfers only. Custom key-value pairs attached to the transfer. Max 50 keys, 100 chars per key, 500 chars per string value.</p>
         */
        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);

        _FinalStage metadata(Nullable<Map<String, Object>> metadata);

        /**
         * <p>Ledger transfers only. A short note describing the transfer.</p>
         */
        _FinalStage notes(Optional<String> notes);

        _FinalStage notes(String notes);

        _FinalStage notes(Nullable<String> notes);

        /**
         * <p>claim_link only. How many different users can claim the link. Defaults to 1.</p>
         */
        _FinalStage redeemableCount(Optional<Integer> redeemableCount);

        _FinalStage redeemableCount(Integer redeemableCount);

        /**
         * <p>The kind of money movement, which decides what comes back. Defaults to ledger. <code>ledger</code> moves credit between two Whop balances and returns a <code>transfer</code>; <code>wallet_send</code> sends USDT from the origin account's Ethereum wallet and returns a <code>send</code>; <code>claim_link</code> funds a shareable link anyone with the URL can redeem and returns a <code>claim_link</code>. A <code>ledger</code> transfer from a stablecoin-rails account settles on-chain when covered, and still returns a <code>transfer</code>.</p>
         */
        _FinalStage type(Optional<CreateTransfersRequestType> type);

        _FinalStage type(CreateTransfersRequestType type);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AmountStage, OriginIdStage, _FinalStage {
        private double amount;

        private String originId;

        private Optional<CreateTransfersRequestType> type = Optional.empty();

        private Optional<Integer> redeemableCount = Optional.empty();

        private Optional<String> notes = Optional.empty();

        private Optional<Map<String, Object>> metadata = Optional.empty();

        private Optional<String> idempotenceKey = Optional.empty();

        private Optional<OffsetDateTime> expiresAt = Optional.empty();

        private Optional<String> destinationId = Optional.empty();

        private Optional<String> currency = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateTransfersRequest other) {
            amount(other.getAmount());
            currency(other.getCurrency());
            destinationId(other.getDestinationId());
            expiresAt(other.getExpiresAt());
            idempotenceKey(other.getIdempotenceKey());
            metadata(other.getMetadata());
            notes(other.getNotes());
            originId(other.getOriginId());
            redeemableCount(other.getRedeemableCount());
            type(other.getType());
            return this;
        }

        /**
         * <p>The amount to move, in the transfer currency. For example 25.00.</p>
         * <p>The amount to move, in the transfer currency. For example 25.00.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public OriginIdStage amount(double amount) {
            this.amount = amount;
            return this;
        }

        /**
         * <p>The account sending the funds. A user ID (user_xxx), account ID (biz_xxx), or ledger account ID (ldgr_xxx).</p>
         * <p>The account sending the funds. A user ID (user_xxx), account ID (biz_xxx), or ledger account ID (ldgr_xxx).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("origin_id")
        public _FinalStage originId(@NotNull String originId) {
            this.originId = Objects.requireNonNull(originId, "originId must not be null");
            return this;
        }

        /**
         * <p>The kind of money movement, which decides what comes back. Defaults to ledger. <code>ledger</code> moves credit between two Whop balances and returns a <code>transfer</code>; <code>wallet_send</code> sends USDT from the origin account's Ethereum wallet and returns a <code>send</code>; <code>claim_link</code> funds a shareable link anyone with the URL can redeem and returns a <code>claim_link</code>. A <code>ledger</code> transfer from a stablecoin-rails account settles on-chain when covered, and still returns a <code>transfer</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage type(CreateTransfersRequestType type) {
            this.type = Optional.ofNullable(type);
            return this;
        }

        /**
         * <p>The kind of money movement, which decides what comes back. Defaults to ledger. <code>ledger</code> moves credit between two Whop balances and returns a <code>transfer</code>; <code>wallet_send</code> sends USDT from the origin account's Ethereum wallet and returns a <code>send</code>; <code>claim_link</code> funds a shareable link anyone with the URL can redeem and returns a <code>claim_link</code>. A <code>ledger</code> transfer from a stablecoin-rails account settles on-chain when covered, and still returns a <code>transfer</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "type", nulls = Nulls.SKIP)
        public _FinalStage type(Optional<CreateTransfersRequestType> type) {
            this.type = type;
            return this;
        }

        /**
         * <p>claim_link only. How many different users can claim the link. Defaults to 1.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage redeemableCount(Integer redeemableCount) {
            this.redeemableCount = Optional.ofNullable(redeemableCount);
            return this;
        }

        /**
         * <p>claim_link only. How many different users can claim the link. Defaults to 1.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "redeemable_count", nulls = Nulls.SKIP)
        public _FinalStage redeemableCount(Optional<Integer> redeemableCount) {
            this.redeemableCount = redeemableCount;
            return this;
        }

        /**
         * <p>Ledger transfers only. A short note describing the transfer.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage notes(Nullable<String> notes) {
            if (notes.isNull()) {
                this.notes = null;
            } else if (notes.isEmpty()) {
                this.notes = Optional.empty();
            } else {
                this.notes = Optional.of(notes.get());
            }
            return this;
        }

        /**
         * <p>Ledger transfers only. A short note describing the transfer.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage notes(String notes) {
            this.notes = Optional.ofNullable(notes);
            return this;
        }

        /**
         * <p>Ledger transfers only. A short note describing the transfer.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "notes", nulls = Nulls.SKIP)
        public _FinalStage notes(Optional<String> notes) {
            this.notes = notes;
            return this;
        }

        /**
         * <p>Ledger transfers only. Custom key-value pairs attached to the transfer. Max 50 keys, 100 chars per key, 500 chars per string value.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Nullable<Map<String, Object>> metadata) {
            if (metadata.isNull()) {
                this.metadata = null;
            } else if (metadata.isEmpty()) {
                this.metadata = Optional.empty();
            } else {
                this.metadata = Optional.of(metadata.get());
            }
            return this;
        }

        /**
         * <p>Ledger transfers only. Custom key-value pairs attached to the transfer. Max 50 keys, 100 chars per key, 500 chars per string value.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        /**
         * <p>Ledger transfers only. Custom key-value pairs attached to the transfer. Max 50 keys, 100 chars per key, 500 chars per string value.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        /**
         * <p>Ledger transfers and wallet sends. A unique key that makes retries safe. Retrying with the same key returns the original transfer, or attaches to the original wallet send, instead of moving money twice.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage idempotenceKey(Nullable<String> idempotenceKey) {
            if (idempotenceKey.isNull()) {
                this.idempotenceKey = null;
            } else if (idempotenceKey.isEmpty()) {
                this.idempotenceKey = Optional.empty();
            } else {
                this.idempotenceKey = Optional.of(idempotenceKey.get());
            }
            return this;
        }

        /**
         * <p>Ledger transfers and wallet sends. A unique key that makes retries safe. Retrying with the same key returns the original transfer, or attaches to the original wallet send, instead of moving money twice.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage idempotenceKey(String idempotenceKey) {
            this.idempotenceKey = Optional.ofNullable(idempotenceKey);
            return this;
        }

        /**
         * <p>Ledger transfers and wallet sends. A unique key that makes retries safe. Retrying with the same key returns the original transfer, or attaches to the original wallet send, instead of moving money twice.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "idempotence_key", nulls = Nulls.SKIP)
        public _FinalStage idempotenceKey(Optional<String> idempotenceKey) {
            this.idempotenceKey = idempotenceKey;
            return this;
        }

        /**
         * <p>claim_link only. Link expiry as an ISO 8601 timestamp. Defaults to 24 hours from creation.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expiresAt(Nullable<OffsetDateTime> expiresAt) {
            if (expiresAt.isNull()) {
                this.expiresAt = null;
            } else if (expiresAt.isEmpty()) {
                this.expiresAt = Optional.empty();
            } else {
                this.expiresAt = Optional.of(expiresAt.get());
            }
            return this;
        }

        /**
         * <p>claim_link only. Link expiry as an ISO 8601 timestamp. Defaults to 24 hours from creation.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage expiresAt(OffsetDateTime expiresAt) {
            this.expiresAt = Optional.ofNullable(expiresAt);
            return this;
        }

        /**
         * <p>claim_link only. Link expiry as an ISO 8601 timestamp. Defaults to 24 hours from creation.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "expires_at", nulls = Nulls.SKIP)
        public _FinalStage expiresAt(Optional<OffsetDateTime> expiresAt) {
            this.expiresAt = expiresAt;
            return this;
        }

        /**
         * <p>The recipient. Required for ledger and wallet_send (a user_/biz_/ldgr_ ID, or — for sends — an email). Omit for claim_link.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage destinationId(String destinationId) {
            this.destinationId = Optional.ofNullable(destinationId);
            return this;
        }

        /**
         * <p>The recipient. Required for ledger and wallet_send (a user_/biz_/ldgr_ ID, or — for sends — an email). Omit for claim_link.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "destination_id", nulls = Nulls.SKIP)
        public _FinalStage destinationId(Optional<String> destinationId) {
            this.destinationId = destinationId;
            return this;
        }

        /**
         * <p>Currency, such as <code>usd</code>. Required for ledger transfers.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage currency(String currency) {
            this.currency = Optional.ofNullable(currency);
            return this;
        }

        /**
         * <p>Currency, such as <code>usd</code>. Required for ledger transfers.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "currency", nulls = Nulls.SKIP)
        public _FinalStage currency(Optional<String> currency) {
            this.currency = currency;
            return this;
        }

        @java.lang.Override
        public CreateTransfersRequest build() {
            return new CreateTransfersRequest(
                    amount,
                    currency,
                    destinationId,
                    expiresAt,
                    idempotenceKey,
                    metadata,
                    notes,
                    originId,
                    redeemableCount,
                    type,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
