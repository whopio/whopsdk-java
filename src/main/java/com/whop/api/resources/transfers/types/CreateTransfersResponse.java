/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.whop.api.resources.transfers.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class CreateTransfersResponse {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private CreateTransfersResponse(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static CreateTransfersResponse transfer(CreateTransfersResponseTransfer value) {
        return new CreateTransfersResponse(new TransferValue(value));
    }

    public static CreateTransfersResponse send(CreateTransfersResponseSend value) {
        return new CreateTransfersResponse(new SendValue(value));
    }

    public static CreateTransfersResponse claimLink(CreateTransfersResponseClaimLink value) {
        return new CreateTransfersResponse(new ClaimLinkValue(value));
    }

    public boolean isTransfer() {
        return value instanceof TransferValue;
    }

    public boolean isSend() {
        return value instanceof SendValue;
    }

    public boolean isClaimLink() {
        return value instanceof ClaimLinkValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CreateTransfersResponseTransfer> getTransfer() {
        if (isTransfer()) {
            return Optional.of(((TransferValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CreateTransfersResponseSend> getSend() {
        if (isSend()) {
            return Optional.of(((SendValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CreateTransfersResponseClaimLink> getClaimLink() {
        if (isClaimLink()) {
            return Optional.of(((ClaimLinkValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateTransfersResponse && value.equals(((CreateTransfersResponse) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitTransfer(CreateTransfersResponseTransfer transfer);

        T visitSend(CreateTransfersResponseSend send);

        T visitClaimLink(CreateTransfersResponseClaimLink claimLink);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "object", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(TransferValue.class),
        @JsonSubTypes.Type(SendValue.class),
        @JsonSubTypes.Type(ClaimLinkValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("transfer")
    @JsonIgnoreProperties("object")
    private static final class TransferValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "object", allowSetters = true)
        private CreateTransfersResponseTransfer value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TransferValue() {}

        private TransferValue(CreateTransfersResponseTransfer value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTransfer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TransferValue && equalTo((TransferValue) other);
        }

        private boolean equalTo(TransferValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateTransfersResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("send")
    @JsonIgnoreProperties("object")
    private static final class SendValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "object", allowSetters = true)
        private CreateTransfersResponseSend value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SendValue() {}

        private SendValue(CreateTransfersResponseSend value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSend(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SendValue && equalTo((SendValue) other);
        }

        private boolean equalTo(SendValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateTransfersResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("claim_link")
    @JsonIgnoreProperties("object")
    private static final class ClaimLinkValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "object", allowSetters = true)
        private CreateTransfersResponseClaimLink value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ClaimLinkValue() {}

        private ClaimLinkValue(CreateTransfersResponseClaimLink value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitClaimLink(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ClaimLinkValue && equalTo((ClaimLinkValue) other);
        }

        private boolean equalTo(ClaimLinkValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateTransfersResponse{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("object")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "CreateTransfersResponse{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
